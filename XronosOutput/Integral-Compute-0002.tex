\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Func@Rational, Sub@Riemann, File@0002}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{46181}{176400}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1669}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{29}{20}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{743}{840}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{25}{12}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{153}{140}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{19}{20}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1669}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{153}{140}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{61}{144}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{46181}{176400}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{26581}{176400}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{137}{60}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{47}{60}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{19}{20}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{47}{60}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{5269}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{37}{60}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{13}{12}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{319}{420}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{29}{20}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1669}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{869}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{469}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{13}{12}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{205}{144}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{46181}{176400}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{46181}{176400}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{61}{144}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{61}{144}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{117349}{705600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{117349}{705600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{11}{6}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{77}{60}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1669}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{77}{60}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{869}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{5269}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{26581}{176400}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{469}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{49}{36}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{49}{36}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{205}{144}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{61}{144}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{1769}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={-\frac{1}{x^{2}}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{right}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{869}{3600}}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0002. \\ \fi
\begin{problem}

Estimate the area under the graph of $f(x)={\frac{1}{x^{2}}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{left}}$ endpoints. (Hint: Remember to check for negative area).

\input{Integral-Compute-0002.HELP.tex}

\[\mbox{Area}\approx\answer{\frac{869}{3600}}\]
\end{problem}}%}

