\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@ByParts, Sub@Sub-u, File@0067}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{7 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{7 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{3 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{-\frac{3}{2} \, \sin\left(x\right)^{2} - \frac{3}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{\sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\frac{1}{2} \, \sin\left(x\right)^{2} + \frac{1}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{5 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{5 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{\arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{6 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{3 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{8 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{4 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{9 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{-\frac{9}{2} \, \sin\left(x\right)^{2} - \frac{9}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{9 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{9 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{7 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{7 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{8 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{4 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{2 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\cos\left(x\right)^{2} - \log\left(-\cos\left(x\right)^{2}\right) - 1}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{\sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{4 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{-2 \, \sin\left(x\right)^{2} - 2 \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{6 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{-3 \, \sin\left(x\right)^{2} - 3 \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{6 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{3 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{2 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{3 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{3 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{8 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{4 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{4 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{2 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{8 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{4 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{5 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{5 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{6 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{3 \, \sin\left(x\right)^{2} + 3 \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{9 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{9 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{9 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{9 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{\sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{7 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{-\frac{7}{2} \, \sin\left(x\right)^{2} - \frac{7}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{4 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{2 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{7 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{7 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{5 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{-\frac{5}{2} \, \sin\left(x\right)^{2} - \frac{5}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{2 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{3 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{3 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{4 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{2 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{\arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{2 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\sin\left(x\right)^{2} + \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{8 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{-4 \, \sin\left(x\right)^{2} - 4 \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{3 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\frac{3}{2} \, \sin\left(x\right)^{2} + \frac{3}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{4 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{2 \, \sin\left(x\right)^{2} + 2 \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{7 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\frac{7}{2} \, \sin\left(x\right)^{2} + \frac{7}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{6 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{3 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{4 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{2 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{9 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{9 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{5 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{5 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{2 \, x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{6 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{\frac{3 \, {\left(x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)\right)}}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{\sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\frac{1}{2} \, \cos\left(x\right)^{2} - \frac{1}{2} \, \log\left(-\cos\left(x\right)^{2}\right) - \frac{1}{2}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{7 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{7 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{8 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{4 \, \sin\left(x\right)^{2} + 4 \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{2 \, \arctan\left(x\right)}{x^{2}}}\;dx} = \answer{{-\frac{x \log\left(x^{2} + 1\right) - x \log\left(x^{2}\right) + 2 \, \arctan\left(x\right)}{x}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{5 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\frac{5}{2} \, \sin\left(x\right)^{2} + \frac{5}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{3 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{3 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{3 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{3 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{\frac{2 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{\frac{\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2}{\sin\left(x\right)}}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{9 \, \sin\left(x\right)^{3}}{\cos\left(x\right)}}\;dx} = \answer{{\frac{9}{2} \, \sin\left(x\right)^{2} + \frac{9}{2} \, \log\left(\sin\left(x\right)^{2} - 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0067. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0067.HELP.tex}

\[\int{{-\frac{5 \, \sec\left(x\right)^{3}}{\tan\left(x\right)^{2}}}\;dx} = \answer{{-\frac{5 \, {\left(\log\left(\sin\left(x\right) + 1\right) \sin\left(x\right) - \log\left(\sin\left(x\right) - 1\right) \sin\left(x\right) - 2\right)}}{2 \, \sin\left(x\right)}}+C}\]
\end{problem}}%}

