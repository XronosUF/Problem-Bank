\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@PartialFractions, Sub@LinearFactors, Func@Rational, Func@Log, File@0053}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{15 \, x - 22}{x^{2} - 3 \, x + 2}}\;dx} = \answer{{7 \, \log\left(x - 1\right) + 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{12}{x + 9}}\;dx} = \answer{{12 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9}{x + 2}}\;dx} = \answer{{-9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 23\right)}}{x^{2} - 5 \, x - 36}}\;dx} = \answer{{6 \, \log\left(x + 4\right) + 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x + 49}{x^{2} - 49}}\;dx} = \answer{{-2 \, \log\left(x + 7\right) + 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(4 \, x - 31\right)}}{x^{2} - 13 \, x + 36}}\;dx} = \answer{{-9 \, \log\left(x - 4\right) - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x - 20}{x^{2} - 4 \, x - 32}}\;dx} = \answer{{6 \, \log\left(x + 4\right) + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x - 8}{x^{2} - 3 \, x - 10}}\;dx} = \answer{{2 \, \log\left(x + 2\right) + \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 35}{x^{2} + 5 \, x}}\;dx} = \answer{{8 \, \log\left(x + 5\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 2\right)}}{x^{2} - 16}}\;dx} = \answer{{3 \, \log\left(x + 4\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 7\right)}}{x^{2} + 3 \, x}}\;dx} = \answer{{\log\left(x + 3\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 6\right)}}{x^{2} - 16}}\;dx} = \answer{{5 \, \log\left(x + 4\right) - \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 21\right)}}{x^{2} - 2 \, x - 15}}\;dx} = \answer{{6 \, \log\left(x + 3\right) - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(7 \, x - 27\right)}}{x^{2} - 10 \, x + 9}}\;dx} = \answer{{-5 \, \log\left(x - 1\right) - 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x - 38}{x^{2} - 7 \, x + 10}}\;dx} = \answer{{8 \, \log\left(x - 2\right) - \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{10}{x^{2} - 2 \, x - 24}}\;dx} = \answer{{-\log\left(x + 4\right) + \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x - 5}{x^{2} + x - 42}}\;dx} = \answer{{-2 \, \log\left(x + 7\right) - \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 39}{x^{2} + 7 \, x + 6}}\;dx} = \answer{{9 \, \log\left(x + 6\right) - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x - 17}{x^{2} + 4 \, x - 45}}\;dx} = \answer{{7 \, \log\left(x + 9\right) + 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x - 17}{x^{2} + 3 \, x - 4}}\;dx} = \answer{{5 \, \log\left(x + 4\right) - 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(7 \, x - 20\right)}}{x^{2} - 8 \, x}}\;dx} = \answer{{-9 \, \log\left(x - 8\right) - 5 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(x + 3\right)}}{x^{2} - 9 \, x}}\;dx} = \answer{{8 \, \log\left(x - 9\right) - 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 23\right)}}{x^{2} + 2 \, x - 8}}\;dx} = \answer{{9 \, \log\left(x + 4\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x + 8\right)}}{x^{2} + 3 \, x - 10}}\;dx} = \answer{{-3 \, \log\left(x + 5\right) - 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{13 \, x - 48}{x^{2} - 8 \, x}}\;dx} = \answer{{-7 \, \log\left(x - 8\right) - 6 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x + 5\right)}}{x^{2} - 8 \, x + 7}}\;dx} = \answer{{4 \, \log\left(x - 1\right) - 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8}{x + 1}}\;dx} = \answer{{-8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 19}{x^{2} - 13 \, x + 40}}\;dx} = \answer{{-8 \, \log\left(x - 5\right) + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 11\right)}}{x^{2} - 17 \, x + 72}}\;dx} = \answer{{-9 \, \log\left(x - 8\right) + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 26}{x^{2} - 4}}\;dx} = \answer{{-4 \, \log\left(x + 2\right) + 9 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 8\right)}}{x^{2} - 64}}\;dx} = \answer{{4 \, \log\left(x + 8\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x + 6\right)}}{x^{2} + 3 \, x}}\;dx} = \answer{{-6 \, \log\left(x + 3\right) - 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x + 80}{x^{2} + 7 \, x - 8}}\;dx} = \answer{{8 \, \log\left(x + 8\right) - 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(2 \, x - 5\right)}}{x^{2} - 5 \, x + 6}}\;dx} = \answer{{6 \, \log\left(x - 2\right) + 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14 \, {\left(x - 1\right)}}{x^{2} - 2 \, x - 3}}\;dx} = \answer{{-7 \, \log\left(x + 1\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{10 \, {\left(x + 4\right)}}{x^{2} + 8 \, x - 9}}\;dx} = \answer{{-5 \, \log\left(x + 9\right) - 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, {\left(2 \, x + 1\right)}}{x^{2} + x - 6}}\;dx} = \answer{{-6 \, \log\left(x + 3\right) - 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x + 20}{x^{2} + x - 20}}\;dx} = \answer{{5 \, \log\left(x + 5\right) + 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 18}{x^{2} - 36}}\;dx} = \answer{{\log\left(x + 6\right) + 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{54}{x^{2} + 2 \, x - 8}}\;dx} = \answer{{-9 \, \log\left(x + 4\right) + 9 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 5\right)}}{x^{2} + 4 \, x - 5}}\;dx} = \answer{{5 \, \log\left(x + 5\right) + 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 46}{x^{2} + 7 \, x - 18}}\;dx} = \answer{{5 \, \log\left(x + 9\right) - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x - 1}{x^{2} + 2 \, x - 15}}\;dx} = \answer{{2 \, \log\left(x + 5\right) + \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{11 \, x - 54}{x^{2} - 6 \, x}}\;dx} = \answer{{2 \, \log\left(x - 6\right) + 9 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(3 \, x + 1\right)}}{x^{2} - 2 \, x - 15}}\;dx} = \answer{{4 \, \log\left(x + 3\right) + 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 35}{x^{2} - 7 \, x - 18}}\;dx} = \answer{{-3 \, \log\left(x + 2\right) + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{10 \, x - 11}{x^{2} - 5 \, x - 6}}\;dx} = \answer{{-3 \, \log\left(x + 1\right) - 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 2}{x^{2} + 10 \, x + 24}}\;dx} = \answer{{-8 \, \log\left(x + 6\right) + 5 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 16}{x^{2} - 4 \, x - 32}}\;dx} = \answer{{3 \, \log\left(x + 4\right) + 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x - 23}{x^{2} - 6 \, x + 5}}\;dx} = \answer{{-5 \, \log\left(x - 1\right) + 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{12 \, {\left(x - 3\right)}}{x^{2} - 2 \, x - 15}}\;dx} = \answer{{9 \, \log\left(x + 3\right) + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, {\left(2 \, x - 5\right)}}{x^{2} - 5 \, x}}\;dx} = \answer{{9 \, \log\left(x - 5\right) + 9 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x + 100}{x^{2} + 5 \, x - 36}}\;dx} = \answer{{7 \, \log\left(x + 9\right) - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x - 77}{x^{2} + 5 \, x - 36}}\;dx} = \answer{{8 \, \log\left(x + 9\right) - 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x - 33\right)}}{x^{2} - 15 \, x + 54}}\;dx} = \answer{{2 \, \log\left(x - 6\right) + 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8 \, x - 65}{x^{2} - 17 \, x + 72}}\;dx} = \answer{{-\log\left(x - 8\right) - 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, {\left(2 \, x - 3\right)}}{x^{2} - 3 \, x - 54}}\;dx} = \answer{{7 \, \log\left(x + 6\right) + 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(5 \, x + 13\right)}}{x^{2} + 7 \, x - 8}}\;dx} = \answer{{9 \, \log\left(x + 8\right) + 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8 \, {\left(x - 4\right)}}{x^{2} - 64}}\;dx} = \answer{{6 \, \log\left(x + 8\right) + 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9 \, x - 13}{x^{2} - 3 \, x + 2}}\;dx} = \answer{{-4 \, \log\left(x - 1\right) - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x}{x^{2} + 3 \, x - 54}}\;dx} = \answer{{-3 \, \log\left(x + 9\right) - 2 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, x - 59}{x^{2} + x - 56}}\;dx} = \answer{{-5 \, \log\left(x + 8\right) + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 21\right)}}{x^{2} - 12 \, x + 27}}\;dx} = \answer{{5 \, \log\left(x - 3\right) - \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 3\right)}}{x^{2} + 2 \, x - 24}}\;dx} = \answer{{-3 \, \log\left(x + 6\right) - \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 58}{x^{2} - 5 \, x - 6}}\;dx} = \answer{{9 \, \log\left(x + 1\right) - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(7 \, x - 45\right)}}{x^{2} - 12 \, x + 27}}\;dx} = \answer{{-8 \, \log\left(x - 3\right) - 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 14\right)}}{x^{2} - 49}}\;dx} = \answer{{-\log\left(x + 7\right) - 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x - 1\right)}}{x^{2} + 2 \, x - 35}}\;dx} = \answer{{-6 \, \log\left(x + 7\right) - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{11 \, x - 43}{x^{2} - 6 \, x + 5}}\;dx} = \answer{{8 \, \log\left(x - 1\right) + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x - 28}{x^{2} - 5 \, x - 24}}\;dx} = \answer{{5 \, \log\left(x + 3\right) + 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, x + 19}{x^{2} + 13 \, x + 40}}\;dx} = \answer{{\log\left(x + 8\right) - 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x + 5\right)}}{x^{2} + 7 \, x}}\;dx} = \answer{{-2 \, \log\left(x + 7\right) - 5 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, {\left(2 \, x + 11\right)}}{x^{2} + 9 \, x + 14}}\;dx} = \answer{{3 \, \log\left(x + 7\right) + 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{10 \, {\left(x - 2\right)}}{x^{2} - 5 \, x}}\;dx} = \answer{{-6 \, \log\left(x - 5\right) - 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{35}{x^{2} + 5 \, x - 6}}\;dx} = \answer{{5 \, \log\left(x + 6\right) - 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x + 16\right)}}{x^{2} + 8 \, x + 12}}\;dx} = \answer{{-7 \, \log\left(x + 6\right) - 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x + 16}{x^{2} - 8 \, x}}\;dx} = \answer{{-7 \, \log\left(x - 8\right) + 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 6\right)}}{x^{2} - 81}}\;dx} = \answer{{-\log\left(x + 9\right) - 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{12 \, x + 49}{x^{2} + 9 \, x + 14}}\;dx} = \answer{{7 \, \log\left(x + 7\right) + 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2}{x + 1}}\;dx} = \answer{{2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 16\right)}}{x^{2} + 3 \, x - 10}}\;dx} = \answer{{-9 \, \log\left(x + 5\right) + 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 16\right)}}{x^{2} + 3 \, x - 54}}\;dx} = \answer{{-5 \, \log\left(x + 9\right) + 2 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, x + 19}{x^{2} + 5 \, x - 14}}\;dx} = \answer{{-\log\left(x + 7\right) - 3 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 3\right)}}{x^{2} + 13 \, x + 36}}\;dx} = \answer{{-6 \, \log\left(x + 9\right) + \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x - 32}{x^{2} - 8 \, x}}\;dx} = \answer{{5 \, \log\left(x - 8\right) + 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 16}{x^{2} - 12 \, x + 32}}\;dx} = \answer{{-5 \, \log\left(x - 4\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{10 \, {\left(x - 5\right)}}{x^{2} - 11 \, x + 24}}\;dx} = \answer{{4 \, \log\left(x - 3\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 25}{x^{2} - 2 \, x - 63}}\;dx} = \answer{{2 \, \log\left(x + 7\right) - \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x - 5\right)}}{x^{2} - 4}}\;dx} = \answer{{9 \, \log\left(x + 2\right) - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x + 60\right)}}{x^{2} + 17 \, x + 72}}\;dx} = \answer{{6 \, \log\left(x + 9\right) + 8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{0}\;dx} = \answer{{0}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x + 8}{x^{2} + 5 \, x + 6}}\;dx} = \answer{{\log\left(x + 3\right) + 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 17\right)}}{x^{2} + 13 \, x + 42}}\;dx} = \answer{{8 \, \log\left(x + 7\right) - 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{10 \, x + 39}{x^{2} + 7 \, x + 12}}\;dx} = \answer{{-\log\left(x + 4\right) - 9 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 29}{x^{2} + 2 \, x - 15}}\;dx} = \answer{{-3 \, \log\left(x + 5\right) + 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x + 19}{x^{2} + 5 \, x - 36}}\;dx} = \answer{{-2 \, \log\left(x + 9\right) - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x + 5\right)}}{x^{2} + 6 \, x - 27}}\;dx} = \answer{{-3 \, \log\left(x + 9\right) - 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 14}{x^{2} - 10 \, x + 16}}\;dx} = \answer{{-4 \, \log\left(x - 2\right) + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x + 5\right)}}{x^{2} + 8 \, x + 7}}\;dx} = \answer{{6 \, \log\left(x + 7\right) + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 20}{x^{2} + 17 \, x + 72}}\;dx} = \answer{{-7 \, \log\left(x + 9\right) + 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 31\right)}}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{-6 \, \log\left(x + 7\right) + 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x - 37}{x^{2} - 13 \, x + 40}}\;dx} = \answer{{9 \, \log\left(x - 5\right) - 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 37}{x^{2} + 12 \, x + 35}}\;dx} = \answer{{-\log\left(x + 7\right) + 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, {\left(2 \, x + 3\right)}}{x^{2} + 3 \, x - 40}}\;dx} = \answer{{7 \, \log\left(x + 8\right) + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{1}{x - 7}}\;dx} = \answer{{\log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 7\right)}}{x^{2} - 14 \, x + 48}}\;dx} = \answer{{-5 \, \log\left(x - 6\right) + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 6}{x^{2} + 9 \, x + 20}}\;dx} = \answer{{-\log\left(x + 5\right) + 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 17\right)}}{x^{2} + 6 \, x + 5}}\;dx} = \answer{{\log\left(x + 5\right) - 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x - 68}{x^{2} - 5 \, x - 24}}\;dx} = \answer{{-7 \, \log\left(x + 3\right) + 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 11\right)}}{x^{2} + 14 \, x + 48}}\;dx} = \answer{{3 \, \log\left(x + 8\right) - 5 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 25\right)}}{x^{2} - 16 \, x + 63}}\;dx} = \answer{{-6 \, \log\left(x - 7\right) - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, x - 12}{x^{2} + x - 6}}\;dx} = \answer{{-9 \, \log\left(x + 3\right) - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{18 \, {\left(x - 4\right)}}{x^{2} - 8 \, x}}\;dx} = \answer{{9 \, \log\left(x - 8\right) + 9 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{10 \, x + 41}{x^{2} + 3 \, x - 40}}\;dx} = \answer{{3 \, \log\left(x + 8\right) + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{16 \, x - 109}{x^{2} - 13 \, x + 36}}\;dx} = \answer{{-9 \, \log\left(x - 4\right) - 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{18 \, {\left(x - 1\right)}}{x^{2} - 2 \, x - 8}}\;dx} = \answer{{9 \, \log\left(x + 2\right) + 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x - 10\right)}}{x^{2} - x - 42}}\;dx} = \answer{{8 \, \log\left(x + 6\right) + 6 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x - 18\right)}}{x^{2} - 9 \, x + 18}}\;dx} = \answer{{2 \, \log\left(x - 3\right) + 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x + 11\right)}}{x^{2} + 12 \, x + 35}}\;dx} = \answer{{6 \, \log\left(x + 7\right) + 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 23\right)}}{x^{2} + 2 \, x - 24}}\;dx} = \answer{{-\log\left(x + 6\right) + 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 5\right)}}{x^{2} - 5 \, x}}\;dx} = \answer{{-4 \, \log\left(x - 5\right) - 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 27\right)}}{x^{2} + 6 \, x - 27}}\;dx} = \answer{{-3 \, \log\left(x + 9\right) + 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x - 22}{x^{2} - 10 \, x + 16}}\;dx} = \answer{{-2 \, \log\left(x - 2\right) - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 11\right)}}{x^{2} - 2 \, x - 8}}\;dx} = \answer{{-5 \, \log\left(x + 2\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{14 \, {\left(x + 5\right)}}{x^{2} + 11 \, x + 18}}\;dx} = \answer{{8 \, \log\left(x + 9\right) + 6 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x + 19}{x^{2} + x - 6}}\;dx} = \answer{{-2 \, \log\left(x + 3\right) + 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x - 31}{x^{2} - 10 \, x + 21}}\;dx} = \answer{{\log\left(x - 3\right) + 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(4 \, x + 7\right)}}{x^{2} + 4 \, x}}\;dx} = \answer{{-9 \, \log\left(x + 4\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 21\right)}}{x^{2} + 7 \, x}}\;dx} = \answer{{4 \, \log\left(x + 7\right) - 6 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 39}{x^{2} - 9}}\;dx} = \answer{{9 \, \log\left(x + 3\right) - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{17 \, x + 53}{x^{2} + 6 \, x + 5}}\;dx} = \answer{{-8 \, \log\left(x + 5\right) - 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x - 23}{x^{2} + x - 2}}\;dx} = \answer{{9 \, \log\left(x + 2\right) - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{1}{x - 1}}\;dx} = \answer{{\log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 104}{x^{2} - 64}}\;dx} = \answer{{-6 \, \log\left(x + 8\right) + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x + 5}{x^{2} + 2 \, x - 15}}\;dx} = \answer{{5 \, \log\left(x + 5\right) + 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5}{x}}\;dx} = \answer{{-5 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x - 59}{x^{2} + x - 56}}\;dx} = \answer{{5 \, \log\left(x + 8\right) - 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 71}{x^{2} - 5 \, x - 14}}\;dx} = \answer{{9 \, \log\left(x + 2\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 5\right)}}{x^{2} + 2 \, x - 3}}\;dx} = \answer{{-4 \, \log\left(x + 3\right) + 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x - 31}{x^{2} - 8 \, x + 7}}\;dx} = \answer{{4 \, \log\left(x - 1\right) + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 46\right)}}{x^{2} + x - 42}}\;dx} = \answer{{-6 \, \log\left(x + 7\right) + 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 18}{x^{2} - 4}}\;dx} = \answer{{-4 \, \log\left(x + 2\right) + 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 8\right)}}{x^{2} - 4 \, x}}\;dx} = \answer{{-3 \, \log\left(x - 4\right) + 6 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x - 17}{x^{2} - 15 \, x + 56}}\;dx} = \answer{{4 \, \log\left(x - 7\right) - 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, {\left(x + 1\right)}}{x^{2} + 3 \, x - 28}}\;dx} = \answer{{-6 \, \log\left(x + 7\right) - 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{11 \, x - 21}{x^{2} - 7 \, x}}\;dx} = \answer{{8 \, \log\left(x - 7\right) + 3 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9 \, x - 26}{x^{2} + 2 \, x - 24}}\;dx} = \answer{{-8 \, \log\left(x + 6\right) - \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{16}{x^{2} - 64}}\;dx} = \answer{{\log\left(x + 8\right) - \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x + 9}{x^{2} + 15 \, x + 56}}\;dx} = \answer{{7 \, \log\left(x + 8\right) - 5 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{1}{x - 2}}\;dx} = \answer{{\log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14}{x - 9}}\;dx} = \answer{{-14 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 19\right)}}{x^{2} + 10 \, x + 16}}\;dx} = \answer{{9 \, \log\left(x + 8\right) - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 17}{x^{2} - 11 \, x + 28}}\;dx} = \answer{{-7 \, \log\left(x - 4\right) + 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14 \, {\left(x + 1\right)}}{x^{2} + 2 \, x - 24}}\;dx} = \answer{{-7 \, \log\left(x + 6\right) - 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 9\right)}}{x^{2} + 11 \, x + 30}}\;dx} = \answer{{9 \, \log\left(x + 6\right) - 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, {\left(2 \, x - 17\right)}}{x^{2} - 17 \, x + 72}}\;dx} = \answer{{5 \, \log\left(x - 8\right) + 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x - 17}{x^{2} - x - 30}}\;dx} = \answer{{-2 \, \log\left(x + 5\right) + \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 25\right)}}{x^{2} - 25}}\;dx} = \answer{{9 \, \log\left(x + 5\right) - \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 25\right)}}{x^{2} + 5 \, x - 14}}\;dx} = \answer{{6 \, \log\left(x + 7\right) - 9 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(x - 6\right)}}{x^{2} - 9 \, x}}\;dx} = \answer{{2 \, \log\left(x - 9\right) + 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13}{x + 2}}\;dx} = \answer{{13 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x + 7\right)}}{x^{2} + 7 \, x - 18}}\;dx} = \answer{{-4 \, \log\left(x + 9\right) - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 46}{x^{2} - 7 \, x + 6}}\;dx} = \answer{{9 \, \log\left(x - 1\right) - 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{70}{x^{2} - 25}}\;dx} = \answer{{7 \, \log\left(x + 5\right) - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{14}{x^{2} - 8 \, x + 15}}\;dx} = \answer{{-7 \, \log\left(x - 3\right) + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x + 17}{x^{2} + 4 \, x - 5}}\;dx} = \answer{{8 \, \log\left(x + 5\right) + 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x + 45}{x^{2} + 16 \, x + 63}}\;dx} = \answer{{9 \, \log\left(x + 9\right) - 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14}{x + 4}}\;dx} = \answer{{-14 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{27}{x^{2} - 5 \, x - 14}}\;dx} = \answer{{3 \, \log\left(x + 2\right) - 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x + 26\right)}}{x^{2} + 8 \, x + 12}}\;dx} = \answer{{8 \, \log\left(x + 6\right) + 6 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x - 18}{x^{2} - 15 \, x + 54}}\;dx} = \answer{{4 \, \log\left(x - 6\right) - 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 25\right)}}{x^{2} - 7 \, x - 8}}\;dx} = \answer{{6 \, \log\left(x + 1\right) - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 9}{x^{2} - 10 \, x + 21}}\;dx} = \answer{{-3 \, \log\left(x - 3\right) + 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, x - 57}{x^{2} - 10 \, x + 21}}\;dx} = \answer{{-6 \, \log\left(x - 3\right) - 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 11}{x^{2} - 6 \, x + 5}}\;dx} = \answer{{-3 \, \log\left(x - 1\right) + 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x - 11}{x^{2} - 11 \, x + 18}}\;dx} = \answer{{\log\left(x - 2\right) + \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{12 \, {\left(x + 4\right)}}{x^{2} + 3 \, x - 54}}\;dx} = \answer{{-4 \, \log\left(x + 9\right) - 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 13\right)}}{x^{2} - 2 \, x - 24}}\;dx} = \answer{{\log\left(x + 4\right) - 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x + 56}{x^{2} + 7 \, x}}\;dx} = \answer{{5 \, \log\left(x + 7\right) + 8 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x + 17}{x^{2} + 2 \, x - 15}}\;dx} = \answer{{-\log\left(x + 5\right) - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 37\right)}}{x^{2} - 4 \, x - 21}}\;dx} = \answer{{-8 \, \log\left(x + 3\right) + 6 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x + 23}{x^{2} + 8 \, x + 15}}\;dx} = \answer{{-6 \, \log\left(x + 5\right) - \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, x + 23}{x^{2} + x - 6}}\;dx} = \answer{{-\log\left(x + 3\right) + 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, x + 51}{x^{2} + 3 \, x - 18}}\;dx} = \answer{{-3 \, \log\left(x + 6\right) + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{24}{x^{2} - 4 \, x}}\;dx} = \answer{{6 \, \log\left(x - 4\right) - 6 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 17\right)}}{x^{2} - 2 \, x - 3}}\;dx} = \answer{{9 \, \log\left(x + 1\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(3 \, x - 16\right)}}{x^{2} - 11 \, x + 24}}\;dx} = \answer{{-7 \, \log\left(x - 3\right) - 8 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 19\right)}}{x^{2} - 13 \, x + 42}}\;dx} = \answer{{-2 \, \log\left(x - 6\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, x + 35}{x^{2} + 17 \, x + 72}}\;dx} = \answer{{-\log\left(x + 9\right) - 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8 \, {\left(x - 3\right)}}{x^{2} - 6 \, x - 27}}\;dx} = \answer{{4 \, \log\left(x + 3\right) + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 9\right)}}{x^{2} - 10 \, x + 24}}\;dx} = \answer{{\log\left(x - 4\right) + 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(5 \, x + 21\right)}}{x^{2} + 9 \, x}}\;dx} = \answer{{-8 \, \log\left(x + 9\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{12 \, {\left(x + 1\right)}}{x^{2} + 2 \, x - 48}}\;dx} = \answer{{-6 \, \log\left(x + 8\right) - 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, x + 3}{x^{2} + 13 \, x + 36}}\;dx} = \answer{{-3 \, \log\left(x + 9\right) + \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x - 3\right)}}{x^{2} + 2 \, x - 63}}\;dx} = \answer{{-6 \, \log\left(x + 9\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x - 5\right)}}{x^{2} - 11 \, x + 24}}\;dx} = \answer{{-2 \, \log\left(x - 3\right) - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x + 18}{x^{2} - 6 \, x}}\;dx} = \answer{{-8 \, \log\left(x - 6\right) + 3 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 8\right)}}{x^{2} + 11 \, x + 30}}\;dx} = \answer{{-4 \, \log\left(x + 6\right) + 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, {\left(x + 12\right)}}{x^{2} + 6 \, x - 16}}\;dx} = \answer{{-2 \, \log\left(x + 8\right) + 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(5 \, x - 12\right)}}{x^{2} - 5 \, x + 4}}\;dx} = \answer{{7 \, \log\left(x - 1\right) + 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14 \, {\left(x + 6\right)}}{x^{2} + 11 \, x + 18}}\;dx} = \answer{{-6 \, \log\left(x + 9\right) - 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{15 \, x - 82}{x^{2} - 11 \, x + 30}}\;dx} = \answer{{-7 \, \log\left(x - 5\right) - 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x - 5}{x^{2} - 10 \, x + 21}}\;dx} = \answer{{\log\left(x - 3\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x - 6}{x^{2} + 6 \, x}}\;dx} = \answer{{8 \, \log\left(x + 6\right) - \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{12}{x^{2} - 15 \, x + 54}}\;dx} = \answer{{-4 \, \log\left(x - 6\right) + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x - 21}{x^{2} - 3 \, x + 2}}\;dx} = \answer{{8 \, \log\left(x - 1\right) + 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 80}{x^{2} - 7 \, x - 8}}\;dx} = \answer{{9 \, \log\left(x + 1\right) - 8 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x - 1}{x^{2} + 4 \, x + 3}}\;dx} = \answer{{5 \, \log\left(x + 3\right) - 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 4\right)}}{x^{2} + 4 \, x - 12}}\;dx} = \answer{{-5 \, \log\left(x + 6\right) + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 29\right)}}{x^{2} + 10 \, x + 16}}\;dx} = \answer{{7 \, \log\left(x + 8\right) - 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x + 33}{x^{2} - 11 \, x + 18}}\;dx} = \answer{{5 \, \log\left(x - 2\right) - 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 14\right)}}{x^{2} - 13 \, x + 36}}\;dx} = \answer{{-6 \, \log\left(x - 4\right) + 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x + 78}{x^{2} + 5 \, x - 24}}\;dx} = \answer{{2 \, \log\left(x + 8\right) - 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{42}{x^{2} + 9 \, x + 8}}\;dx} = \answer{{6 \, \log\left(x + 8\right) - 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 11\right)}}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{3 \, \log\left(x + 7\right) - 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8 \, {\left(x - 1\right)}}{x^{2} - 2 \, x}}\;dx} = \answer{{4 \, \log\left(x - 2\right) + 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 17}{x^{2} + 8 \, x + 7}}\;dx} = \answer{{3 \, \log\left(x + 7\right) + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x + 12}{x^{2} + x - 30}}\;dx} = \answer{{6 \, \log\left(x + 6\right) + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8}{x + 8}}\;dx} = \answer{{-8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{13}{x + 9}}\;dx} = \answer{{-13 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 19\right)}}{x^{2} - 6 \, x + 5}}\;dx} = \answer{{8 \, \log\left(x - 1\right) - 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x - 33}{x^{2} - 2 \, x - 15}}\;dx} = \answer{{9 \, \log\left(x + 3\right) + 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x - 65}{x^{2} - 11 \, x + 18}}\;dx} = \answer{{-9 \, \log\left(x - 2\right) + 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x + 19}{x^{2} - x - 2}}\;dx} = \answer{{6 \, \log\left(x + 1\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{11 \, x - 29}{x^{2} - 6 \, x - 7}}\;dx} = \answer{{5 \, \log\left(x + 1\right) + 6 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x + 13\right)}}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{9 \, \log\left(x + 7\right) + 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, x + 33}{x^{2} - x - 6}}\;dx} = \answer{{5 \, \log\left(x + 2\right) - 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 33\right)}}{x^{2} - 12 \, x + 27}}\;dx} = \answer{{7 \, \log\left(x - 3\right) + \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8 \, x - 67}{x^{2} - 3 \, x - 28}}\;dx} = \answer{{-9 \, \log\left(x + 4\right) + \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, x}{x^{2} - 36}}\;dx} = \answer{{3 \, \log\left(x + 6\right) + 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 1}{x^{2} + 2 \, x - 3}}\;dx} = \answer{{-2 \, \log\left(x + 3\right) - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 8\right)}}{x^{2} - 4 \, x}}\;dx} = \answer{{6 \, \log\left(x - 4\right) - 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9 \, x - 44}{x^{2} - 7 \, x + 6}}\;dx} = \answer{{-7 \, \log\left(x - 1\right) - 2 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 10\right)}}{x^{2} + 11 \, x + 28}}\;dx} = \answer{{3 \, \log\left(x + 7\right) - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x - 3}{x^{2} - 3 \, x - 54}}\;dx} = \answer{{-3 \, \log\left(x + 6\right) - 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x - 2}{x^{2} + 9 \, x + 8}}\;dx} = \answer{{-6 \, \log\left(x + 8\right) + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x - 29}{x^{2} - 7 \, x - 8}}\;dx} = \answer{{-4 \, \log\left(x + 1\right) - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 3\right)}}{x^{2} - x}}\;dx} = \answer{{4 \, \log\left(x - 1\right) - 6 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 5\right)}}{x^{2} + 4 \, x - 21}}\;dx} = \answer{{-\log\left(x + 7\right) - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{10 \, {\left(x - 2\right)}}{x^{2} - x - 6}}\;dx} = \answer{{8 \, \log\left(x + 2\right) + 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 5\right)}}{x^{2} + 4 \, x}}\;dx} = \answer{{9 \, \log\left(x + 4\right) - 5 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{49}{x^{2} - 11 \, x + 18}}\;dx} = \answer{{-7 \, \log\left(x - 2\right) + 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{13 \, x - 24}{x^{2} - 6 \, x - 16}}\;dx} = \answer{{-5 \, \log\left(x + 2\right) - 8 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7}{x^{2} + x}}\;dx} = \answer{{-7 \, \log\left(x + 1\right) + 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 22\right)}}{x^{2} - 9 \, x + 8}}\;dx} = \answer{{-6 \, \log\left(x - 1\right) + 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{1}{x - 1}}\;dx} = \answer{{-\log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, x - 17}{x^{2} + x - 20}}\;dx} = \answer{{-3 \, \log\left(x + 5\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x + 19\right)}}{x^{2} + 8 \, x + 7}}\;dx} = \answer{{\log\left(x + 7\right) + 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 7\right)}}{x^{2} - 4 \, x + 3}}\;dx} = \answer{{5 \, \log\left(x - 1\right) - \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(x + 4\right)}}{x^{2} + 6 \, x}}\;dx} = \answer{{2 \, \log\left(x + 6\right) + 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{11 \, x - 59}{x^{2} - 8 \, x - 9}}\;dx} = \answer{{7 \, \log\left(x + 1\right) + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(3 \, x - 5\right)}}{x^{2} + 2 \, x - 63}}\;dx} = \answer{{-8 \, \log\left(x + 9\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x - 18\right)}}{x^{2} - 7 \, x + 12}}\;dx} = \answer{{-6 \, \log\left(x - 3\right) - 4 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x + 64}{x^{2} + x - 30}}\;dx} = \answer{{-2 \, \log\left(x + 6\right) + 9 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x + 50}{x^{2} + 7 \, x + 10}}\;dx} = \answer{{5 \, \log\left(x + 5\right) + 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{10 \, x + 21}{x^{2} - x - 42}}\;dx} = \answer{{-3 \, \log\left(x + 6\right) - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{17 \, x - 35}{x^{2} - 4 \, x + 3}}\;dx} = \answer{{-9 \, \log\left(x - 1\right) - 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 17}{x^{2} - 12 \, x + 35}}\;dx} = \answer{{-4 \, \log\left(x - 5\right) + 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9 \, x - 17}{x^{2} - 10 \, x + 9}}\;dx} = \answer{{-\log\left(x - 1\right) - 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x - 9}{x^{2} - 9 \, x}}\;dx} = \answer{{-4 \, \log\left(x - 9\right) - \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3}{x - 6}}\;dx} = \answer{{3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 13}{x^{2} + 2 \, x - 15}}\;dx} = \answer{{-\log\left(x + 5\right) + 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8 \, x + 41}{x^{2} + 11 \, x + 30}}\;dx} = \answer{{-7 \, \log\left(x + 6\right) - \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(4 \, x + 11\right)}}{x^{2} - x - 42}}\;dx} = \answer{{-3 \, \log\left(x + 6\right) - 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x - 4\right)}}{x^{2} - 3 \, x - 10}}\;dx} = \answer{{-4 \, \log\left(x + 2\right) - 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 9\right)}}{x^{2} + 10 \, x + 21}}\;dx} = \answer{{-2 \, \log\left(x + 7\right) + 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, {\left(x + 2\right)}}{x^{2} + 3 \, x - 18}}\;dx} = \answer{{4 \, \log\left(x + 6\right) + 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x + 23\right)}}{x^{2} + 7 \, x - 18}}\;dx} = \answer{{-4 \, \log\left(x + 9\right) - 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3}{x}}\;dx} = \answer{{3 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 3\right)}}{x^{2} - 14 \, x + 45}}\;dx} = \answer{{-4 \, \log\left(x - 5\right) + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x - 26\right)}}{x^{2} - 5 \, x - 14}}\;dx} = \answer{{8 \, \log\left(x + 2\right) + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 5\right)}}{x^{2} - 8 \, x + 7}}\;dx} = \answer{{2 \, \log\left(x - 1\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 8\right)}}{x^{2} + 11 \, x + 28}}\;dx} = \answer{{2 \, \log\left(x + 7\right) - 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 32}{x^{2} + 2 \, x - 8}}\;dx} = \answer{{6 \, \log\left(x + 4\right) - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x + 13}{x^{2} - 2 \, x - 15}}\;dx} = \answer{{-\log\left(x + 3\right) - 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x + 1\right)}}{x^{2} - 2 \, x - 35}}\;dx} = \answer{{-3 \, \log\left(x + 5\right) - 6 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{16}{x^{2} + 8 \, x + 12}}\;dx} = \answer{{4 \, \log\left(x + 6\right) - 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x - 8\right)}}{x^{2} + 4 \, x - 32}}\;dx} = \answer{{8 \, \log\left(x + 8\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 9\right)}}{x^{2} - 9 \, x + 18}}\;dx} = \answer{{8 \, \log\left(x - 3\right) - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{12 \, {\left(x - 3\right)}}{x^{2} - 2 \, x - 35}}\;dx} = \answer{{8 \, \log\left(x + 5\right) + 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 19}{x^{2} + 7 \, x + 10}}\;dx} = \answer{{8 \, \log\left(x + 5\right) - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 10}{x^{2} + 3 \, x - 28}}\;dx} = \answer{{-\log\left(x + 7\right) - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(x - 8\right)}}{x^{2} - 15 \, x + 54}}\;dx} = \answer{{4 \, \log\left(x - 6\right) + 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x - 41}{x^{2} - 10 \, x + 9}}\;dx} = \answer{{4 \, \log\left(x - 1\right) + 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(x - 8\right)}}{x^{2} - 11 \, x + 28}}\;dx} = \answer{{8 \, \log\left(x - 4\right) - 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8}{x + 6}}\;dx} = \answer{{8 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x - 53}{x^{2} - 14 \, x + 45}}\;dx} = \answer{{2 \, \log\left(x - 5\right) + 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 9\right)}}{x^{2} - 8 \, x + 12}}\;dx} = \answer{{7 \, \log\left(x - 2\right) - 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14 \, {\left(x + 2\right)}}{x^{2} + 4 \, x}}\;dx} = \answer{{-7 \, \log\left(x + 4\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 11\right)}}{x^{2} + 7 \, x - 8}}\;dx} = \answer{{6 \, \log\left(x + 8\right) - 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x - 14}{x^{2} - 2 \, x}}\;dx} = \answer{{-4 \, \log\left(x - 2\right) + 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, x - 5}{x^{2} - 3 \, x + 2}}\;dx} = \answer{{-\log\left(x - 1\right) - 3 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 18}{x^{2} - x - 12}}\;dx} = \answer{{3 \, \log\left(x + 3\right) - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 28}{x^{2} - 10 \, x + 16}}\;dx} = \answer{{-5 \, \log\left(x - 2\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x + 36\right)}}{x^{2} + 10 \, x + 24}}\;dx} = \answer{{6 \, \log\left(x + 6\right) + 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(x + 2\right)}}{x^{2} + 5 \, x + 4}}\;dx} = \answer{{2 \, \log\left(x + 4\right) + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 9\right)}}{x^{2} + 9 \, x}}\;dx} = \answer{{6 \, \log\left(x + 9\right) + 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 9\right)}}{x^{2} - 11 \, x + 28}}\;dx} = \answer{{-5 \, \log\left(x - 4\right) + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x - 2\right)}}{x^{2} - 4 \, x - 21}}\;dx} = \answer{{-4 \, \log\left(x + 3\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 8}{x^{2} + 3 \, x + 2}}\;dx} = \answer{{-6 \, \log\left(x + 2\right) + 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 7\right)}}{x^{2} + 2 \, x}}\;dx} = \answer{{3 \, \log\left(x + 2\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 5\right)}}{x^{2} - 25}}\;dx} = \answer{{3 \, \log\left(x + 5\right) + \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{11 \, x - 58}{x^{2} - 11 \, x + 30}}\;dx} = \answer{{3 \, \log\left(x - 5\right) + 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x - 5\right)}}{x^{2} + x - 6}}\;dx} = \answer{{-8 \, \log\left(x + 3\right) + 3 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x - 38}{x^{2} - 2 \, x - 24}}\;dx} = \answer{{-5 \, \log\left(x + 4\right) + 2 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 21\right)}}{x^{2} + 3 \, x - 18}}\;dx} = \answer{{-2 \, \log\left(x + 6\right) + 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{13 \, x + 35}{x^{2} - 49}}\;dx} = \answer{{4 \, \log\left(x + 7\right) + 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{9 \, x + 74}{x^{2} + 11 \, x + 18}}\;dx} = \answer{{\log\left(x + 9\right) + 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 2\right)}}{x^{2} - 14 \, x + 45}}\;dx} = \answer{{-3 \, \log\left(x - 5\right) + 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 5\right)}}{x^{2} - 4 \, x + 3}}\;dx} = \answer{{-3 \, \log\left(x - 1\right) - 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 21\right)}}{x^{2} - 13 \, x + 36}}\;dx} = \answer{{2 \, \log\left(x - 4\right) + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(4 \, x + 1\right)}}{x^{2} + 5 \, x - 14}}\;dx} = \answer{{9 \, \log\left(x + 7\right) + 3 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x + 15}{x^{2} + 3 \, x - 18}}\;dx} = \answer{{\log\left(x + 6\right) - 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{9 \, x + 38}{x^{2} + 3 \, x - 10}}\;dx} = \answer{{-\log\left(x + 5\right) - 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x + 41}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{-\log\left(x + 7\right) - 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(2 \, x + 9\right)}}{x^{2} + 9 \, x + 8}}\;dx} = \answer{{-5 \, \log\left(x + 8\right) - 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8 \, x + 59}{x^{2} + x - 30}}\;dx} = \answer{{-\log\left(x + 6\right) + 9 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 12}{x^{2} - 2 \, x}}\;dx} = \answer{{7 \, \log\left(x - 2\right) - 6 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 7\right)}}{x^{2} - 5 \, x + 6}}\;dx} = \answer{{6 \, \log\left(x - 2\right) - 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 21\right)}}{x^{2} - 3 \, x - 18}}\;dx} = \answer{{4 \, \log\left(x + 3\right) - 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 56}{x^{2} + 8 \, x}}\;dx} = \answer{{8 \, \log\left(x + 8\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 31\right)}}{x^{2} + 14 \, x + 45}}\;dx} = \answer{{7 \, \log\left(x + 9\right) + 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 11\right)}}{x^{2} + 9 \, x + 18}}\;dx} = \answer{{5 \, \log\left(x + 6\right) - 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x - 7\right)}}{x^{2} - 7 \, x + 10}}\;dx} = \answer{{3 \, \log\left(x - 2\right) + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 25}{x^{2} + 16 \, x + 63}}\;dx} = \answer{{-\log\left(x + 9\right) - 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{11 \, x - 35}{x^{2} - 5 \, x}}\;dx} = \answer{{4 \, \log\left(x - 5\right) + 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x + 31\right)}}{x^{2} + 4 \, x - 45}}\;dx} = \answer{{-2 \, \log\left(x + 9\right) - 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 4\right)}}{x^{2} - 8 \, x}}\;dx} = \answer{{2 \, \log\left(x - 8\right) + 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 21}{x^{2} + 14 \, x + 45}}\;dx} = \answer{{-3 \, \log\left(x + 9\right) + 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{10}{x - 8}}\;dx} = \answer{{-10 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x - 59}{x^{2} - 2 \, x - 63}}\;dx} = \answer{{-5 \, \log\left(x + 7\right) + 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x + 43}{x^{2} + 13 \, x + 36}}\;dx} = \answer{{4 \, \log\left(x + 9\right) + 3 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8 \, x + 67}{x^{2} + 17 \, x + 72}}\;dx} = \answer{{5 \, \log\left(x + 9\right) + 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 44}{x^{2} - 4 \, x - 32}}\;dx} = \answer{{-2 \, \log\left(x + 4\right) + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(x + 10\right)}}{x^{2} - 7 \, x - 8}}\;dx} = \answer{{-3 \, \log\left(x + 1\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, x - 24}{x^{2} - 5 \, x - 6}}\;dx} = \answer{{-5 \, \log\left(x + 1\right) - 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, x + 38}{x^{2} + 5 \, x - 6}}\;dx} = \answer{{-4 \, \log\left(x + 6\right) - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 35}{x^{2} + 5 \, x - 24}}\;dx} = \answer{{\log\left(x + 8\right) - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, x - 56}{x^{2} - 8 \, x}}\;dx} = \answer{{-4 \, \log\left(x - 8\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 14\right)}}{x^{2} - 12 \, x + 32}}\;dx} = \answer{{\log\left(x - 4\right) + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8 \, {\left(x - 1\right)}}{x^{2} - 2 \, x - 3}}\;dx} = \answer{{4 \, \log\left(x + 1\right) + 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(4 \, x + 11\right)}}{x^{2} + 5 \, x + 4}}\;dx} = \answer{{-5 \, \log\left(x + 4\right) - 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(x + 3\right)}}{x^{2} + 11 \, x + 28}}\;dx} = \answer{{4 \, \log\left(x + 7\right) - \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 9\right)}}{x^{2} - 9 \, x + 14}}\;dx} = \answer{{-2 \, \log\left(x - 2\right) - 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 7\right)}}{x^{2} - 6 \, x - 16}}\;dx} = \answer{{3 \, \log\left(x + 2\right) - 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 99}{x^{2} - 81}}\;dx} = \answer{{-5 \, \log\left(x + 9\right) + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x + 3}{x^{2} + 3 \, x - 54}}\;dx} = \answer{{\log\left(x + 9\right) + \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{13 \, x + 51}{x^{2} + 9 \, x + 14}}\;dx} = \answer{{-8 \, \log\left(x + 7\right) - 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{12 \, x + 17}{x^{2} + x - 30}}\;dx} = \answer{{-5 \, \log\left(x + 6\right) - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x - 39}{x^{2} + 3 \, x - 28}}\;dx} = \answer{{-8 \, \log\left(x + 7\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{40}{x^{2} - 13 \, x + 36}}\;dx} = \answer{{8 \, \log\left(x - 4\right) - 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x + 43}{x^{2} - x - 30}}\;dx} = \answer{{-3 \, \log\left(x + 5\right) + 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 21}{x^{2} - 10 \, x + 9}}\;dx} = \answer{{2 \, \log\left(x - 1\right) + 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{12 \, x}{x^{2} - 64}}\;dx} = \answer{{-6 \, \log\left(x + 8\right) - 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 6\right)}}{x^{2} - 16 \, x + 63}}\;dx} = \answer{{\log\left(x - 7\right) - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x - 4}{x^{2} + 12 \, x + 32}}\;dx} = \answer{{-3 \, \log\left(x + 8\right) + 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{48}{x^{2} - 12 \, x + 27}}\;dx} = \answer{{8 \, \log\left(x - 3\right) - 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 53}{x^{2} + 7 \, x - 8}}\;dx} = \answer{{-5 \, \log\left(x + 8\right) + 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, x - 101}{x^{2} + 3 \, x - 40}}\;dx} = \answer{{-9 \, \log\left(x + 8\right) + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x - 57}{x^{2} + 7 \, x - 18}}\;dx} = \answer{{-6 \, \log\left(x + 9\right) + 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, x - 41}{x^{2} - 3 \, x - 4}}\;dx} = \answer{{9 \, \log\left(x + 1\right) - 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 20\right)}}{x^{2} - 5 \, x}}\;dx} = \answer{{2 \, \log\left(x - 5\right) - 8 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 2\right)}}{x^{2} - 3 \, x + 2}}\;dx} = \answer{{-2 \, \log\left(x - 1\right) + 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(8 \, x + 5\right)}}{x^{2} + 2 \, x - 8}}\;dx} = \answer{{-9 \, \log\left(x + 4\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(3 \, x - 4\right)}}{x^{2} - 3 \, x - 4}}\;dx} = \answer{{-7 \, \log\left(x + 1\right) - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x - 40}{x^{2} - 14 \, x + 48}}\;dx} = \answer{{\log\left(x - 6\right) - 8 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 3\right)}}{x^{2} - 3 \, x - 10}}\;dx} = \answer{{2 \, \log\left(x + 2\right) + 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(5 \, x + 26\right)}}{x^{2} + 11 \, x + 28}}\;dx} = \answer{{-9 \, \log\left(x + 7\right) - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 43}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{-\log\left(x + 7\right) + 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 13\right)}}{x^{2} - 10 \, x + 9}}\;dx} = \answer{{-6 \, \log\left(x - 1\right) + 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x + 25\right)}}{x^{2} + 17 \, x + 72}}\;dx} = \answer{{-6 \, \log\left(x + 9\right) - 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x - 2}{x^{2} - 5 \, x + 6}}\;dx} = \answer{{-4 \, \log\left(x - 2\right) + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8}{x - 1}}\;dx} = \answer{{-8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 20\right)}}{x^{2} + 8 \, x + 12}}\;dx} = \answer{{-\log\left(x + 6\right) + 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, x + 59}{x^{2} - 3 \, x - 40}}\;dx} = \answer{{-3 \, \log\left(x + 5\right) + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14}{x - 1}}\;dx} = \answer{{-14 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x - 41}{x^{2} - 4 \, x - 5}}\;dx} = \answer{{-8 \, \log\left(x + 1\right) + \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x + 8\right)}}{x^{2} + 4 \, x - 5}}\;dx} = \answer{{2 \, \log\left(x + 5\right) - 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14 \, {\left(x + 3\right)}}{x^{2} + 6 \, x + 5}}\;dx} = \answer{{-7 \, \log\left(x + 5\right) - 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x + 18}{x^{2} + 9 \, x}}\;dx} = \answer{{5 \, \log\left(x + 9\right) + 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{40}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{5 \, \log\left(x + 7\right) - 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(3 \, x + 19\right)}}{x^{2} + 12 \, x + 35}}\;dx} = \answer{{4 \, \log\left(x + 7\right) + 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{x^{2} + 3 \, x}}\;dx} = \answer{{-7 \, \log\left(x + 3\right) + 3 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x - 16}{x^{2} + 12 \, x + 32}}\;dx} = \answer{{-6 \, \log\left(x + 8\right) + 5 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x - 21\right)}}{x^{2} - 7 \, x}}\;dx} = \answer{{-3 \, \log\left(x - 7\right) + 9 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 18}{x^{2} - x - 6}}\;dx} = \answer{{4 \, \log\left(x + 2\right) - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x - 16\right)}}{x^{2} - 5 \, x + 4}}\;dx} = \answer{{6 \, \log\left(x - 1\right) + 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(2 \, x + 3\right)}}{x^{2} + 3 \, x}}\;dx} = \answer{{6 \, \log\left(x + 3\right) + 6 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{20}{x^{2} - 25}}\;dx} = \answer{{2 \, \log\left(x + 5\right) - 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{6 \, {\left(x - 3\right)}}{x^{2} - 4 \, x - 5}}\;dx} = \answer{{4 \, \log\left(x + 1\right) + 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{1}{x - 9}}\;dx} = \answer{{-\log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 3\right)}}{x^{2} - 3 \, x - 4}}\;dx} = \answer{{-3 \, \log\left(x + 1\right) - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{8 \, x - 49}{x^{2} - 13 \, x + 40}}\;dx} = \answer{{-3 \, \log\left(x - 5\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{16 \, {\left(x - 2\right)}}{x^{2} - 4 \, x - 45}}\;dx} = \answer{{-8 \, \log\left(x + 5\right) - 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 36}{x^{2} + x - 30}}\;dx} = \answer{{6 \, \log\left(x + 6\right) - \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 3}{x^{2} - 17 \, x + 72}}\;dx} = \answer{{-5 \, \log\left(x - 8\right) + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, x + 3}{x^{2} + 3 \, x - 4}}\;dx} = \answer{{-\log\left(x + 4\right) - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{55}{x^{2} + 7 \, x - 18}}\;dx} = \answer{{5 \, \log\left(x + 9\right) - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x - 3\right)}}{x^{2} + 2 \, x - 3}}\;dx} = \answer{{-9 \, \log\left(x + 3\right) - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x - 11\right)}}{x^{2} - 11 \, x + 30}}\;dx} = \answer{{-4 \, \log\left(x - 5\right) - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, {\left(2 \, x - 3\right)}}{x^{2} - 2 \, x}}\;dx} = \answer{{-3 \, \log\left(x - 2\right) - 9 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x - 12\right)}}{x^{2} - 6 \, x}}\;dx} = \answer{{-6 \, \log\left(x - 6\right) - 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x - 17}{x^{2} - 4 \, x + 3}}\;dx} = \answer{{-8 \, \log\left(x - 1\right) + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{8}{x^{2} + 8 \, x}}\;dx} = \answer{{-\log\left(x + 8\right) + \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 13\right)}}{x^{2} + 3 \, x - 4}}\;dx} = \answer{{3 \, \log\left(x + 4\right) - 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 13}{x^{2} - 15 \, x + 56}}\;dx} = \answer{{6 \, \log\left(x - 7\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, x - 29}{x^{2} - 7 \, x + 10}}\;dx} = \answer{{-7 \, \log\left(x - 2\right) + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{12 \, x - 41}{x^{2} - 5 \, x - 24}}\;dx} = \answer{{-7 \, \log\left(x + 3\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 18}{x^{2} - 6 \, x - 16}}\;dx} = \answer{{2 \, \log\left(x + 2\right) - \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 19\right)}}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{4 \, \log\left(x + 7\right) + 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 28}{x^{2} + 4 \, x}}\;dx} = \answer{{4 \, \log\left(x + 4\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{32}{x^{2} + 2 \, x - 15}}\;dx} = \answer{{4 \, \log\left(x + 5\right) - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 19\right)}}{x^{2} + 11 \, x + 24}}\;dx} = \answer{{2 \, \log\left(x + 8\right) + 4 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{13 \, x - 99}{x^{2} - 15 \, x + 56}}\;dx} = \answer{{-8 \, \log\left(x - 7\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, {\left(x + 2\right)}}{x^{2} + 7 \, x}}\;dx} = \answer{{5 \, \log\left(x + 7\right) + 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x + 59}{x^{2} - x - 72}}\;dx} = \answer{{3 \, \log\left(x + 8\right) - 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x - 29}{x^{2} - x - 72}}\;dx} = \answer{{-5 \, \log\left(x + 8\right) - 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, x - 93}{x^{2} - x - 42}}\;dx} = \answer{{9 \, \log\left(x + 6\right) - 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x - 29}{x^{2} - 4 \, x - 21}}\;dx} = \answer{{5 \, \log\left(x + 3\right) + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 19\right)}}{x^{2} + 8 \, x + 7}}\;dx} = \answer{{4 \, \log\left(x + 7\right) - 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 3\right)}}{x^{2} - 4 \, x - 5}}\;dx} = \answer{{\log\left(x + 1\right) - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x - 5\right)}}{x^{2} - 2 \, x}}\;dx} = \answer{{9 \, \log\left(x - 2\right) + 5 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 37\right)}}{x^{2} - 6 \, x - 16}}\;dx} = \answer{{-7 \, \log\left(x + 2\right) + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14}{x^{2} + 2 \, x - 48}}\;dx} = \answer{{\log\left(x + 8\right) - \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{10 \, x - 23}{x^{2} - 5 \, x + 6}}\;dx} = \answer{{-3 \, \log\left(x - 2\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, x + 11}{x^{2} + 13 \, x + 40}}\;dx} = \answer{{7 \, \log\left(x + 8\right) - 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(6 \, x + 19\right)}}{x^{2} + 4 \, x - 45}}\;dx} = \answer{{-5 \, \log\left(x + 9\right) - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x - 26}{x^{2} + 13 \, x + 36}}\;dx} = \answer{{-7 \, \log\left(x + 9\right) + 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, x + 38}{x^{2} + 6 \, x + 8}}\;dx} = \answer{{-3 \, \log\left(x + 4\right) - 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{7 \, x - 12}{x^{2} - 4 \, x}}\;dx} = \answer{{-4 \, \log\left(x - 4\right) - 3 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{12 \, {\left(x - 3\right)}}{x^{2} - 8 \, x + 12}}\;dx} = \answer{{3 \, \log\left(x - 2\right) + 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 7\right)}}{x^{2} - x - 2}}\;dx} = \answer{{-9 \, \log\left(x + 1\right) + 3 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x - 11}{x^{2} + 6 \, x + 5}}\;dx} = \answer{{-9 \, \log\left(x + 5\right) + 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 4\right)}}{x^{2} + 2 \, x - 3}}\;dx} = \answer{{-7 \, \log\left(x + 3\right) + 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x - 3\right)}}{x^{2} - 5 \, x + 4}}\;dx} = \answer{{-4 \, \log\left(x - 1\right) - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x + 17\right)}}{x^{2} + 5 \, x + 4}}\;dx} = \answer{{-2 \, \log\left(x + 4\right) - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{50}{x^{2} + 6 \, x - 16}}\;dx} = \answer{{5 \, \log\left(x + 8\right) - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(6 \, x - 11\right)}}{x^{2} - 2 \, x - 24}}\;dx} = \answer{{-7 \, \log\left(x + 4\right) - 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 17}{x^{2} - 6 \, x - 7}}\;dx} = \answer{{-2 \, \log\left(x + 1\right) + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x + 20\right)}}{x^{2} + 5 \, x}}\;dx} = \answer{{6 \, \log\left(x + 5\right) + 8 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x - 6\right)}}{x^{2} - 4 \, x - 12}}\;dx} = \answer{{-4 \, \log\left(x + 2\right) - 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 8\right)}}{x^{2} + 4 \, x - 21}}\;dx} = \answer{{-3 \, \log\left(x + 7\right) + \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(3 \, x + 1\right)}}{x^{2} + 2 \, x - 3}}\;dx} = \answer{{8 \, \log\left(x + 3\right) + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x + 17}{x^{2} + 6 \, x + 5}}\;dx} = \answer{{-2 \, \log\left(x + 5\right) - 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x + 2}{x^{2} + 3 \, x - 28}}\;dx} = \answer{{-3 \, \log\left(x + 7\right) - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, {\left(x + 4\right)}}{x^{2} + 5 \, x - 24}}\;dx} = \answer{{-4 \, \log\left(x + 8\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 4\right)}}{x^{2} - 4}}\;dx} = \answer{{-3 \, \log\left(x + 2\right) + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{15}{x^{2} + x - 56}}\;dx} = \answer{{-\log\left(x + 8\right) + \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{28}{x^{2} - 12 \, x + 32}}\;dx} = \answer{{-7 \, \log\left(x - 4\right) + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{x + 19}{x^{2} + 5 \, x + 4}}\;dx} = \answer{{5 \, \log\left(x + 4\right) - 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, x + 8}{x^{2} + 2 \, x}}\;dx} = \answer{{-\log\left(x + 2\right) - 4 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, x + 31}{x^{2} + 6 \, x + 5}}\;dx} = \answer{{4 \, \log\left(x + 5\right) - 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, x + 23}{x^{2} + 5 \, x - 14}}\;dx} = \answer{{\log\left(x + 7\right) - 3 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 1\right)}}{x^{2} + 3 \, x + 2}}\;dx} = \answer{{-9 \, \log\left(x + 2\right) + 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 13\right)}}{x^{2} + 5 \, x + 4}}\;dx} = \answer{{6 \, \log\left(x + 4\right) - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 21}{x^{2} + 3 \, x - 18}}\;dx} = \answer{{\log\left(x + 6\right) + 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{72}{x^{2} + x - 20}}\;dx} = \answer{{8 \, \log\left(x + 5\right) - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x + 37}{x^{2} + 13 \, x + 36}}\;dx} = \answer{{-2 \, \log\left(x + 9\right) + 5 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 21\right)}}{x^{2} + x - 56}}\;dx} = \answer{{\log\left(x + 8\right) - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 13\right)}}{x^{2} - 16 \, x + 63}}\;dx} = \answer{{-9 \, \log\left(x - 7\right) + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(5 \, x - 21\right)}}{x^{2} - 8 \, x + 7}}\;dx} = \answer{{-8 \, \log\left(x - 1\right) - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{1}{x - 9}}\;dx} = \answer{{\log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, x - 39}{x^{2} - 11 \, x + 18}}\;dx} = \answer{{5 \, \log\left(x - 2\right) - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x + 13}{x^{2} + 3 \, x - 4}}\;dx} = \answer{{3 \, \log\left(x + 4\right) + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x + 37}{x^{2} + 8 \, x + 7}}\;dx} = \answer{{-5 \, \log\left(x + 7\right) + 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x - 44}{x^{2} - 17 \, x + 72}}\;dx} = \answer{{4 \, \log\left(x - 8\right) + \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{14 \, {\left(x + 4\right)}}{x^{2} + 8 \, x + 15}}\;dx} = \answer{{-7 \, \log\left(x + 5\right) - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(5 \, x - 7\right)}}{x^{2} - 3 \, x}}\;dx} = \answer{{-8 \, \log\left(x - 3\right) - 7 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 9\right)}}{x^{2} + 9 \, x}}\;dx} = \answer{{-6 \, \log\left(x + 9\right) + 3 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x - 26}{x^{2} - 4}}\;dx} = \answer{{8 \, \log\left(x + 2\right) - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(2 \, x - 5\right)}}{x^{2} + x - 56}}\;dx} = \answer{{-7 \, \log\left(x + 8\right) - 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{x - 51}{x^{2} + 3 \, x - 10}}\;dx} = \answer{{8 \, \log\left(x + 5\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x - 1\right)}}{x^{2} + 4 \, x - 21}}\;dx} = \answer{{-6 \, \log\left(x + 7\right) - 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 6\right)}}{x^{2} + 3 \, x - 54}}\;dx} = \answer{{-\log\left(x + 9\right) - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 7\right)}}{x^{2} - 11 \, x + 30}}\;dx} = \answer{{8 \, \log\left(x - 5\right) - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{11 \, x - 87}{x^{2} - 15 \, x + 54}}\;dx} = \answer{{-7 \, \log\left(x - 6\right) - 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x - 51}{x^{2} - 15 \, x + 56}}\;dx} = \answer{{2 \, \log\left(x - 7\right) + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{10 \, x}{x^{2} - 64}}\;dx} = \answer{{5 \, \log\left(x + 8\right) + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{6 \, x + 55}{x^{2} + 9 \, x + 8}}\;dx} = \answer{{\log\left(x + 8\right) - 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{2 \, {\left(7 \, x - 13\right)}}{x^{2} - 5 \, x - 14}}\;dx} = \answer{{-6 \, \log\left(x + 2\right) - 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x - 11\right)}}{x^{2} - 4 \, x - 21}}\;dx} = \answer{{-7 \, \log\left(x + 3\right) + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x + 5\right)}}{x^{2} + 5 \, x + 6}}\;dx} = \answer{{4 \, \log\left(x + 3\right) + 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x - 7\right)}}{x^{2} - 49}}\;dx} = \answer{{6 \, \log\left(x + 7\right) + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{5 \, x + 77}{x^{2} + 10 \, x + 9}}\;dx} = \answer{{-4 \, \log\left(x + 9\right) + 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x - 2}{x^{2} + x}}\;dx} = \answer{{9 \, \log\left(x + 1\right) - 2 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x - 2\right)}}{x^{2} - 3 \, x - 4}}\;dx} = \answer{{3 \, \log\left(x + 1\right) + 6 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 2\right)}}{x^{2} - 12 \, x + 32}}\;dx} = \answer{{-2 \, \log\left(x - 4\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 21\right)}}{x^{2} - 9}}\;dx} = \answer{{-6 \, \log\left(x + 3\right) + 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{13 \, x + 3}{x^{2} - 9}}\;dx} = \answer{{-6 \, \log\left(x + 3\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 19\right)}}{x^{2} + 13 \, x + 42}}\;dx} = \answer{{4 \, \log\left(x + 7\right) + 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7}{x - 1}}\;dx} = \answer{{7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, x - 37}{x^{2} - 5 \, x - 14}}\;dx} = \answer{{5 \, \log\left(x + 2\right) - \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{7 \, x - 23}{x^{2} + 6 \, x - 7}}\;dx} = \answer{{9 \, \log\left(x + 7\right) - 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, x + 3}{x^{2} - 11 \, x + 24}}\;dx} = \answer{{-3 \, \log\left(x - 3\right) + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{14 \, {\left(x + 1\right)}}{x^{2} + 2 \, x - 24}}\;dx} = \answer{{7 \, \log\left(x + 6\right) + 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{108}{x^{2} - 2 \, x - 35}}\;dx} = \answer{{9 \, \log\left(x + 5\right) - 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 14\right)}}{x^{2} - 11 \, x + 24}}\;dx} = \answer{{2 \, \log\left(x - 3\right) + 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 16\right)}}{x^{2} + 8 \, x}}\;dx} = \answer{{-4 \, \log\left(x + 8\right) + 8 \, \log\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x - 6\right)}}{x^{2} - 13 \, x + 36}}\;dx} = \answer{{-2 \, \log\left(x - 4\right) - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0053. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0053.HELP.tex}

\[
\int{{\frac{3 \, x + 14}{x^{2} + 11 \, x + 24}}\;dx} = \answer{{2 \, \log\left(x + 8\right) + \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

