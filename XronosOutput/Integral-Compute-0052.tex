\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@PartialFractions, Sub@LinearFactors, Func@Rational, File@0052}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x - 57}{x^{2} - 7 \, x + 6}}\;dx} = \answer{{8 \, \log\left(x - 1\right) + 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, x - 16}{x^{2} + 6 \, x - 16}}\;dx} = \answer{{-4 \, \log\left(x + 8\right) + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, x}{3 \, x^{2} - 10 \, x - 8}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(3 \, x + 2\right) - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x - 39}{7 \, x^{2} + 12 \, x - 27}}\;dx} = \answer{{-\log\left(7 \, x - 9\right) + 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{10 \, x + 17}{3 \, x^{2} + 19 \, x + 20}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(3 \, x + 4\right) - 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{19 \, x - 3}{4 \, {\left(x^{2} + x - 6\right)}}}\;dx} = \answer{{3 \, \log\left(x + 3\right) + \frac{7}{4} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(5 \, x - 17\right)}}{2 \, {\left(2 \, x^{2} + 15 \, x + 7\right)}}}\;dx} = \answer{{-\frac{9}{4} \, \log\left(2 \, x + 1\right) + 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{36}{x^{2} + 5 \, x - 14}}\;dx} = \answer{{4 \, \log\left(x + 7\right) - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 7\right)}}{x^{2} - 4 \, x - 5}}\;dx} = \answer{{-4 \, \log\left(x + 1\right) + 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{10 \, {\left(x + 1\right)}}{3 \, {\left(x^{2} + 9 \, x + 14\right)}}}\;dx} = \answer{{-4 \, \log\left(x + 7\right) + \frac{2}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{21 \, {\left(x - 1\right)}}{2 \, {\left(2 \, x^{2} - 13 \, x + 15\right)}}}\;dx} = \answer{{\frac{3}{4} \, \log\left(2 \, x - 3\right) - 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(7 \, x - 10\right)}}{2 \, {\left(x^{2} - 4\right)}}}\;dx} = \answer{{-9 \, \log\left(x + 2\right) - \frac{3}{2} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x + 8}{2 \, {\left(x^{2} - x - 2\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x + 1\right) + 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, x - 2}{x^{2} - 5 \, x + 6}}\;dx} = \answer{{4 \, \log\left(x - 2\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x + 38}{7 \, x^{2} - 53 \, x + 28}}\;dx} = \answer{{\frac{6}{7} \, \log\left(7 \, x - 4\right) - \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{46 \, x - 53}{7 \, x^{2} + 37 \, x - 30}}\;dx} = \answer{{\frac{3}{7} \, \log\left(7 \, x - 5\right) - 7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{41 \, x + 77}{9 \, {\left(x^{2} - 7 \, x - 8\right)}}}\;dx} = \answer{{-\frac{4}{9} \, \log\left(x + 1\right) + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x + 6}{5 \, x^{2} - 12 \, x + 4}}\;dx} = \answer{{-\frac{7}{5} \, \log\left(5 \, x - 2\right) + 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, x + 33}{3 \, {\left(x^{2} - 9\right)}}}\;dx} = \answer{{\frac{2}{3} \, \log\left(x + 3\right) - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{41 \, x - 29}{5 \, x^{2} + 11 \, x - 12}}\;dx} = \answer{{-\frac{1}{5} \, \log\left(5 \, x - 4\right) - 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 20\right)}}{x^{2} - 5 \, x - 24}}\;dx} = \answer{{2 \, \log\left(x + 3\right) - 8 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{12 \, {\left(3 \, x - 2\right)}}{7 \, x^{2} + 36 \, x + 5}}\;dx} = \answer{{-\frac{6}{7} \, \log\left(7 \, x + 1\right) + 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{14 \, x - 45}{5 \, x^{2} - 21 \, x - 54}}\;dx} = \answer{{\frac{9}{5} \, \log\left(5 \, x + 9\right) + \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{13 \, x - 16}{2 \, {\left(2 \, x^{2} - 3 \, x - 2\right)}}}\;dx} = \answer{{-\frac{9}{4} \, \log\left(2 \, x + 1\right) - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{29 \, x + 38}{4 \, x^{2} + 19 \, x + 12}}\;dx} = \answer{{-\frac{5}{4} \, \log\left(4 \, x + 3\right) - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{6 \, x + 47}{3 \, x^{2} + 25 \, x + 42}}\;dx} = \answer{{3 \, \log\left(3 \, x + 7\right) - \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{15 \, x - 11}{9 \, x^{2} + 89 \, x + 72}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(9 \, x + 8\right) + 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(2 \, x - 1\right)}}{x^{2} + 3 \, x - 4}}\;dx} = \answer{{-9 \, \log\left(x + 4\right) - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x - 27}{3 \, x^{2} + 25 \, x + 8}}\;dx} = \answer{{\frac{4}{3} \, \log\left(3 \, x + 1\right) - 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{44 \, x - 29}{7 \, x^{2} - 37 \, x - 30}}\;dx} = \answer{{\frac{9}{7} \, \log\left(7 \, x + 5\right) + 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x - 13\right)}}{2 \, x^{2} - 11 \, x - 63}}\;dx} = \answer{{3 \, \log\left(2 \, x + 7\right) + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 5\right)}}{x^{2} + 5 \, x + 4}}\;dx} = \answer{{\log\left(x + 4\right) - 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(19 \, x + 20\right)}}{7 \, x^{2} + 22 \, x + 16}}\;dx} = \answer{{-\frac{4}{7} \, \log\left(7 \, x + 8\right) + 6 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{27 \, x + 17}{3 \, x^{2} + 10 \, x + 3}}\;dx} = \answer{{\log\left(3 \, x + 1\right) + 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{9 \, {\left(x + 3\right)}}{2 \, x^{2} + 15 \, x + 28}}\;dx} = \answer{{-\frac{9}{2} \, \log\left(2 \, x + 7\right) + 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(4 \, x + 3\right)}}{2 \, x^{2} + 17 \, x + 30}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 5\right) + 9 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{0}\;dx} = \answer{{0}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, {\left(2 \, x + 1\right)}}{3 \, {\left(x^{2} + 7 \, x + 10\right)}}}\;dx} = \answer{{8 \, \log\left(x + 5\right) - \frac{8}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, {\left(10 \, x - 13\right)}}{9 \, x^{2} - 53 \, x + 40}}\;dx} = \answer{{-\frac{7}{9} \, \log\left(9 \, x - 8\right) - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{8 \, x - 37}{2 \, x^{2} - 19 \, x + 45}}\;dx} = \answer{{-\log\left(2 \, x - 9\right) - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{67 \, x + 18}{8 \, x^{2} - 75 \, x + 27}}\;dx} = \answer{{\frac{5}{8} \, \log\left(8 \, x - 3\right) - 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{31 \, x - 24}{7 \, x^{2} + 3 \, x - 4}}\;dx} = \answer{{\frac{4}{7} \, \log\left(7 \, x - 4\right) - 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{59 \, x - 44}{9 \, {\left(x^{2} + x - 2\right)}}}\;dx} = \answer{{6 \, \log\left(x + 2\right) + \frac{5}{9} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(7 \, x - 1\right)}}{3 \, x^{2} + 29 \, x + 40}}\;dx} = \answer{{-2 \, \log\left(3 \, x + 5\right) + 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{9 \, x + 47}{x^{2} + 10 \, x + 21}}\;dx} = \answer{{4 \, \log\left(x + 7\right) + 5 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{32 \, {\left(x - 1\right)}}{3 \, {\left(x^{2} - 6 \, x - 7\right)}}}\;dx} = \answer{{\frac{8}{3} \, \log\left(x + 1\right) + 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x - 16}{2 \, {\left(4 \, x^{2} - 7 \, x - 2\right)}}}\;dx} = \answer{{\frac{9}{8} \, \log\left(4 \, x + 1\right) + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7}{x^{2} - 5 \, x - 6}}\;dx} = \answer{{\log\left(x + 1\right) - \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, x + 3}{x^{2} - 9}}\;dx} = \answer{{-2 \, \log\left(x + 3\right) - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x - 7\right)}}{x^{2} + x - 20}}\;dx} = \answer{{-8 \, \log\left(x + 5\right) + 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{9 \, x + 22}{4 \, x^{2} + 17 \, x + 18}}\;dx} = \answer{{\frac{7}{4} \, \log\left(4 \, x + 9\right) - 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 15\right)}}{3 \, x^{2} + 13 \, x - 56}}\;dx} = \answer{{\frac{2}{3} \, \log\left(3 \, x - 8\right) - 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x + 2\right)}}{3 \, {\left(2 \, x^{2} + 3 \, x + 1\right)}}}\;dx} = \answer{{\frac{1}{3} \, \log\left(2 \, x + 1\right) - 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{10 \, {\left(x - 1\right)}}{3 \, x^{2} + x - 2}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(3 \, x - 2\right) + 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(4 \, x + 7\right)}}{3 \, x^{2} - 22 \, x + 7}}\;dx} = \answer{{-\frac{5}{3} \, \log\left(3 \, x - 1\right) + 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{64 \, x}{7 \, x^{2} - 48 \, x - 64}}\;dx} = \answer{{-\frac{8}{7} \, \log\left(7 \, x + 8\right) - 8 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x - 4}{9 \, x^{2} + 44 \, x + 32}}\;dx} = \answer{{-\frac{5}{9} \, \log\left(9 \, x + 8\right) + 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x - 34}{8 \, x^{2} + 25 \, x - 28}}\;dx} = \answer{{-\frac{5}{8} \, \log\left(8 \, x - 7\right) + 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(15 \, x - 7\right)}}{3 \, x^{2} + 5 \, x - 12}}\;dx} = \answer{{-2 \, \log\left(3 \, x - 4\right) - 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{33 \, x - 37}{4 \, x^{2} + 7 \, x - 15}}\;dx} = \answer{{\frac{1}{4} \, \log\left(4 \, x - 5\right) + 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, {\left(2 \, x - 1\right)}}{2 \, x^{2} + 9 \, x + 4}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 1\right) + 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{33 \, x}{4 \, x^{2} + 31 \, x - 8}}\;dx} = \answer{{\frac{1}{4} \, \log\left(4 \, x - 1\right) + 8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(5 \, x - 7\right)}}{6 \, x^{2} + 7 \, x - 10}}\;dx} = \answer{{\frac{5}{6} \, \log\left(6 \, x - 5\right) - 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{13 \, x - 46}{2 \, x^{2} - 15 \, x + 28}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(2 \, x - 7\right) - 6 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, {\left(5 \, x + 3\right)}}{8 \, x^{2} + 13 \, x + 5}}\;dx} = \answer{{\frac{1}{4} \, \log\left(8 \, x + 5\right) - 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(4 \, x - 5\right)}}{7 \, x^{2} - 15 \, x + 8}}\;dx} = \answer{{\frac{9}{7} \, \log\left(7 \, x - 8\right) - 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{10 \, x - 1}{x^{2} + 7 \, x - 8}}\;dx} = \answer{{9 \, \log\left(x + 8\right) + \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{26 \, x - 15}{2 \, x^{2} - 5 \, x - 3}}\;dx} = \answer{{4 \, \log\left(2 \, x + 1\right) + 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{38 \, x - 29}{5 \, {\left(x^{2} + x - 2\right)}}}\;dx} = \answer{{7 \, \log\left(x + 2\right) + \frac{3}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, {\left(x - 1\right)}}{3 \, x^{2} - x - 10}}\;dx} = \answer{{-\frac{8}{3} \, \log\left(3 \, x + 5\right) - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, x + 13}{x^{2} - x - 12}}\;dx} = \answer{{\log\left(x + 3\right) - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 2\right)}}{x^{2} + 3 \, x - 4}}\;dx} = \answer{{-2 \, \log\left(x + 4\right) - 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(11 \, x + 31\right)}}{7 \, x^{2} + 58 \, x - 45}}\;dx} = \answer{{-\frac{8}{7} \, \log\left(7 \, x - 5\right) - 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, x + 10}{2 \, {\left(2 \, x^{2} - 15 \, x - 8\right)}}}\;dx} = \answer{{\frac{1}{4} \, \log\left(2 \, x + 1\right) - \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, x - 8}{x^{2} - 5 \, x + 6}}\;dx} = \answer{{-2 \, \log\left(x - 2\right) + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(9 \, x - 10\right)}}{5 \, x^{2} + 39 \, x - 8}}\;dx} = \answer{{\frac{2}{5} \, \log\left(5 \, x - 1\right) - 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(9 \, x - 23\right)}}{2 \, {\left(3 \, x^{2} + 28 \, x + 9\right)}}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(3 \, x + 1\right) + 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(20 \, x - 9\right)}}{7 \, x^{2} - 69 \, x + 54}}\;dx} = \answer{{\frac{2}{7} \, \log\left(7 \, x - 6\right) - 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(16 \, x + 39\right)}}{8 \, x^{2} + 81 \, x + 81}}\;dx} = \answer{{\log\left(8 \, x + 9\right) + 5 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{55 \, x - 34}{8 \, x^{2} - 13 \, x + 5}}\;dx} = \answer{{\frac{1}{8} \, \log\left(8 \, x - 5\right) - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 1\right)}}{x^{2} + 2 \, x - 3}}\;dx} = \answer{{2 \, \log\left(x^{2} + 2 \, x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{16 \, x - 15}{8 \, x^{2} - 15 \, x - 27}}\;dx} = \answer{{-\log\left(8 \, x^{2} - 15 \, x - 27\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{15 \, x + 31}{6 \, x^{2} - 17 \, x - 3}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(6 \, x + 1\right) + 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{8 \, x + 23}{4 \, x^{2} - 35 \, x + 24}}\;dx} = \answer{{\log\left(4 \, x - 3\right) - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{27 \, x + 37}{9 \, x^{2} + 32 \, x + 15}}\;dx} = \answer{{\log\left(9 \, x + 5\right) + 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{13 \, x + 47}{2 \, x^{2} + 13 \, x - 24}}\;dx} = \answer{{-\frac{7}{2} \, \log\left(2 \, x - 3\right) - 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{47 \, x - 45}{8 \, x^{2} + 17 \, x - 21}}\;dx} = \answer{{\frac{1}{8} \, \log\left(8 \, x - 7\right) - 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{31 \, x - 4}{9 \, x^{2} + 40 \, x + 16}}\;dx} = \answer{{-\frac{5}{9} \, \log\left(9 \, x + 4\right) + 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{20 \, x + 9}{3 \, {\left(2 \, x^{2} - 15 \, x - 27\right)}}}\;dx} = \answer{{\frac{1}{3} \, \log\left(2 \, x + 3\right) + 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x - 3\right)}}{x^{2} - 36}}\;dx} = \answer{{-5 \, \log\left(x + 6\right) - 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(13 \, x - 16\right)}}{5 \, x^{2} - 16 \, x + 12}}\;dx} = \answer{{-\frac{1}{5} \, \log\left(5 \, x - 6\right) - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{47 \, x + 11}{7 \, x^{2} + 45 \, x - 28}}\;dx} = \answer{{\frac{5}{7} \, \log\left(7 \, x - 4\right) + 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{83 \, x + 13}{9 \, x^{2} + 19 \, x + 2}}\;dx} = \answer{{\frac{2}{9} \, \log\left(9 \, x + 1\right) + 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{12 \, x + 29}{7 \, x^{2} + 37 \, x + 36}}\;dx} = \answer{{-\frac{5}{7} \, \log\left(7 \, x + 9\right) - \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{21}{5 \, {\left(x - 1\right)}}}\;dx} = \answer{{\frac{21}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{83 \, x + 86}{9 \, x^{2} + 71 \, x + 56}}\;dx} = \answer{{-\frac{2}{9} \, \log\left(9 \, x + 8\right) - 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{53 \, x + 9}{2 \, {\left(4 \, x^{2} + 33 \, x - 27\right)}}}\;dx} = \answer{{-\frac{5}{8} \, \log\left(4 \, x - 3\right) - 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x - 5}{x^{2} - 5 \, x - 14}}\;dx} = \answer{{3 \, \log\left(x + 2\right) + 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x + 12}{3 \, {\left(x^{2} + 5 \, x + 6\right)}}}\;dx} = \answer{{3 \, \log\left(x + 3\right) - \frac{2}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{22 \, x - 15}{7 \, x^{2} + 55 \, x - 72}}\;dx} = \answer{{-\frac{1}{7} \, \log\left(7 \, x - 8\right) - 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x - 51}{3 \, {\left(x^{2} - 10 \, x + 9\right)}}}\;dx} = \answer{{-\frac{5}{3} \, \log\left(x - 1\right) - 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{23 \, x - 99}{8 \, x^{2} + 47 \, x - 63}}\;dx} = \answer{{-\frac{9}{8} \, \log\left(8 \, x - 9\right) + 4 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{6 \, {\left(4 \, x + 5\right)}}{8 \, x^{2} + 39 \, x - 54}}\;dx} = \answer{{\log\left(8 \, x - 9\right) + 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6}{x^{2} + 8 \, x + 7}}\;dx} = \answer{{\log\left(x + 7\right) - \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 5\right)}}{3 \, x^{2} + 26 \, x - 9}}\;dx} = \answer{{\frac{2}{3} \, \log\left(3 \, x - 1\right) - 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(8 \, x + 5\right)}}{7 \, x^{2} + 29 \, x + 4}}\;dx} = \answer{{\frac{4}{7} \, \log\left(7 \, x + 1\right) + 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{31 \, x + 29}{4 \, x^{2} + 19 \, x - 5}}\;dx} = \answer{{-\frac{7}{4} \, \log\left(4 \, x - 1\right) - 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{30 \, {\left(x + 1\right)}}{4 \, x^{2} + 29 \, x - 24}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(4 \, x - 3\right) - 6 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{80 \, x - 79}{9 \, {\left(x^{2} - 3 \, x + 2\right)}}}\;dx} = \answer{{\frac{1}{9} \, \log\left(x - 1\right) - 9 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(7 \, x - 16\right)}}{4 \, x^{2} - 23 \, x - 35}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(4 \, x + 5\right) - 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{9 \, x + 34}{x^{2} + 7 \, x + 12}}\;dx} = \answer{{-2 \, \log\left(x + 4\right) - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{6 \, {\left(7 \, x + 12\right)}}{8 \, x^{2} - 33 \, x - 35}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(8 \, x + 7\right) + 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 2\right)}}{x^{2} - 8 \, x + 15}}\;dx} = \answer{{-2 \, \log\left(x - 3\right) + 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x - 12}{7 \, x^{2} + 11 \, x - 6}}\;dx} = \answer{{\frac{3}{7} \, \log\left(7 \, x - 3\right) - 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, x - 76}{2 \, x^{2} + 7 \, x - 72}}\;dx} = \answer{{-\frac{5}{2} \, \log\left(2 \, x - 9\right) + 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{80 \, x - 77}{9 \, {\left(x^{2} - 5 \, x + 4\right)}}}\;dx} = \answer{{\frac{1}{9} \, \log\left(x - 1\right) - 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{26 \, x - 7}{7 \, x^{2} - 29 \, x - 30}}\;dx} = \answer{{-\frac{5}{7} \, \log\left(7 \, x + 6\right) - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, {\left(2 \, x - 9\right)}}{3 \, x^{2} + 11 \, x - 20}}\;dx} = \answer{{-\frac{5}{3} \, \log\left(3 \, x - 4\right) + 5 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, x - 13}{7 \, x^{2} + 25 \, x + 12}}\;dx} = \answer{{\log\left(7 \, x + 4\right) - 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{12 \, x - 121}{3 \, x^{2} + 17 \, x - 56}}\;dx} = \answer{{-3 \, \log\left(3 \, x - 7\right) + 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{32 \, x + 15}{6 \, x^{2} - 25 \, x - 25}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(6 \, x + 5\right) - 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, {\left(3 \, x + 1\right)}}{2 \, x^{2} + 3 \, x - 2}}\;dx} = \answer{{\frac{7}{2} \, \log\left(2 \, x - 1\right) + 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x + 1\right)}}{2 \, {\left(4 \, x^{2} + 15 \, x + 9\right)}}}\;dx} = \answer{{-\frac{1}{8} \, \log\left(4 \, x + 3\right) - \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{45 \, x + 62}{9 \, x^{2} + 56 \, x + 12}}\;dx} = \answer{{\log\left(9 \, x + 2\right) + 4 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{18 \, x + 17}{3 \, x^{2} - x - 2}}\;dx} = \answer{{\log\left(3 \, x + 2\right) - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 1\right)}}{2 \, x^{2} - 9 \, x + 9}}\;dx} = \answer{{4 \, \log\left(2 \, x - 3\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, {\left(8 \, x - 1\right)}}{7 \, x^{2} - 13 \, x - 2}}\;dx} = \answer{{-\log\left(7 \, x + 1\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, x - 21}{x^{2} - 8 \, x + 15}}\;dx} = \answer{{-3 \, \log\left(x - 3\right) - 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{37 \, x + 25}{4 \, x^{2} - 5 \, x - 6}}\;dx} = \answer{{\frac{1}{4} \, \log\left(4 \, x + 3\right) + 9 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{52 \, {\left(x - 1\right)}}{7 \, x^{2} + x - 6}}\;dx} = \answer{{\frac{4}{7} \, \log\left(7 \, x - 6\right) - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{9 \, {\left(3 \, x - 8\right)}}{4 \, x^{2} - 33 \, x + 54}}\;dx} = \answer{{\frac{3}{4} \, \log\left(4 \, x - 9\right) + 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x - 70}{4 \, x^{2} - 31 \, x - 8}}\;dx} = \answer{{-\frac{9}{4} \, \log\left(4 \, x + 1\right) - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{43 \, x - 8}{4 \, {\left(x^{2} + 3 \, x - 4\right)}}}\;dx} = \answer{{-9 \, \log\left(x + 4\right) - \frac{7}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{16 \, x - 29}{x^{2} - 5 \, x - 24}}\;dx} = \answer{{7 \, \log\left(x + 3\right) + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{59 \, x + 40}{9 \, x^{2} - 23 \, x - 12}}\;dx} = \answer{{\frac{4}{9} \, \log\left(9 \, x + 4\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, {\left(x - 1\right)}}{9 \, x^{2} + 28 \, x + 3}}\;dx} = \answer{{-\frac{5}{9} \, \log\left(9 \, x + 1\right) + 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, x + 41}{7 \, {\left(x^{2} - 2 \, x - 3\right)}}}\;dx} = \answer{{-\frac{9}{7} \, \log\left(x + 1\right) + 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7}{x + 2}}\;dx} = \answer{{7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(11 \, x - 5\right)}}{7 \, x^{2} - 50 \, x + 7}}\;dx} = \answer{{\frac{1}{7} \, \log\left(7 \, x - 1\right) + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 16\right)}}{3 \, x^{2} + 22 \, x + 24}}\;dx} = \answer{{\frac{4}{3} \, \log\left(3 \, x + 4\right) + 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{71 \, x + 72}{9 \, x^{2} + 26 \, x + 16}}\;dx} = \answer{{-\frac{8}{9} \, \log\left(9 \, x + 8\right) - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x + 4}{x^{2} + 3 \, x - 4}}\;dx} = \answer{{8 \, \log\left(x + 4\right) + 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{25 \, x - 27}{2 \, {\left(3 \, x^{2} + 11 \, x - 20\right)}}}\;dx} = \answer{{-\frac{1}{6} \, \log\left(3 \, x - 4\right) - 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{6 \, x + 65}{x^{2} - x - 72}}\;dx} = \answer{{-\log\left(x + 8\right) + 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{14 \, x}{5 \, x^{2} + 16 \, x + 3}}\;dx} = \answer{{-\frac{1}{5} \, \log\left(5 \, x + 1\right) + 3 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 9\right)}}{4 \, x^{2} - 21 \, x + 5}}\;dx} = \answer{{\frac{3}{2} \, \log\left(4 \, x - 1\right) - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(8 \, x - 11\right)}}{7 \, x^{2} - 59 \, x + 24}}\;dx} = \answer{{-\frac{5}{7} \, \log\left(7 \, x - 3\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{68 \, x - 1}{7 \, x^{2} - 13 \, x - 2}}\;dx} = \answer{{\frac{5}{7} \, \log\left(7 \, x + 1\right) + 9 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{50 \, x - 21}{8 \, x^{2} + 59 \, x + 21}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(8 \, x + 3\right) + 7 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 21\right)}}{2 \, x^{2} - x - 36}}\;dx} = \answer{{3 \, \log\left(2 \, x - 9\right) - 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{54 \, x + 19}{8 \, x^{2} - 47 \, x - 6}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(8 \, x + 1\right) + 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{35 \, x - 13}{7 \, x^{2} + 6 \, x - 1}}\;dx} = \answer{{-\log\left(7 \, x - 1\right) + 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 11\right)}}{3 \, x^{2} - 22 \, x - 16}}\;dx} = \answer{{-\log\left(3 \, x^{2} - 22 \, x - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{27 \, {\left(x + 5\right)}}{4 \, {\left(x^{2} - 5 \, x - 14\right)}}}\;dx} = \answer{{-\frac{9}{4} \, \log\left(x + 2\right) + 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 3\right)}}{3 \, x^{2} - 7 \, x + 2}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(3 \, x - 1\right) + 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(19 \, x - 11\right)}}{9 \, x^{2} - 82 \, x + 9}}\;dx} = \answer{{\frac{4}{9} \, \log\left(9 \, x - 1\right) + 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{12 \, x + 71}{3 \, x^{2} + 23 \, x - 8}}\;dx} = \answer{{-3 \, \log\left(3 \, x - 1\right) - \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x - 50}{x^{2} - 10 \, x + 16}}\;dx} = \answer{{4 \, \log\left(x - 2\right) + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{32 \, x - 59}{4 \, x^{2} + 29 \, x - 24}}\;dx} = \answer{{\log\left(4 \, x - 3\right) - 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(5 \, x - 7\right)}}{4 \, x^{2} - 31 \, x - 8}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(4 \, x + 1\right) - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 3\right)}}{x^{2} - 5 \, x + 6}}\;dx} = \answer{{-2 \, \log\left(x - 2\right) + 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x - 89}{8 \, {\left(x^{2} - 8 \, x - 9\right)}}}\;dx} = \answer{{-\frac{9}{8} \, \log\left(x + 1\right) + \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, x - 109}{x^{2} + x - 72}}\;dx} = \answer{{8 \, \log\left(x + 9\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, x + 38}{8 \, x^{2} - 53 \, x - 21}}\;dx} = \answer{{-\frac{5}{8} \, \log\left(8 \, x + 3\right) + \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, x + 33}{4 \, x^{2} - 9 \, x - 9}}\;dx} = \answer{{2 \, \log\left(4 \, x + 3\right) - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 5\right)}}{x^{2} + 5 \, x + 6}}\;dx} = \answer{{8 \, \log\left(x + 3\right) - 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, {\left(5 \, x + 12\right)}}{4 \, x^{2} + 27 \, x + 35}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(4 \, x + 7\right) - 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{15 \, {\left(x - 1\right)}}{2 \, {\left(3 \, x^{2} + 22 \, x + 7\right)}}}\;dx} = \answer{{\frac{1}{2} \, \log\left(3 \, x + 1\right) - 3 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(4 \, x - 15\right)}}{5 \, x^{2} - 46 \, x + 48}}\;dx} = \answer{{-\frac{6}{5} \, \log\left(5 \, x - 6\right) - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{55 \, {\left(x - 1\right)}}{8 \, x^{2} - 73 \, x + 72}}\;dx} = \answer{{\frac{1}{8} \, \log\left(8 \, x - 9\right) - 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, x + 73}{5 \, x^{2} + 53 \, x + 72}}\;dx} = \answer{{\frac{9}{5} \, \log\left(5 \, x + 8\right) - \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{14 \, x - 5}{5 \, x^{2} - 39 \, x + 28}}\;dx} = \answer{{\frac{1}{5} \, \log\left(5 \, x - 4\right) - 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 14\right)}}{3 \, {\left(x^{2} - 10 \, x + 16\right)}}}\;dx} = \answer{{-\frac{8}{3} \, \log\left(x - 2\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 10\right)}}{2 \, {\left(3 \, x^{2} + 22 \, x + 24\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(3 \, x + 4\right) - 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(10 \, x + 11\right)}}{4 \, x^{2} - 25 \, x - 56}}\;dx} = \answer{{\frac{1}{2} \, \log\left(4 \, x + 7\right) + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{35 \, x - 39}{5 \, x^{2} - 44 \, x - 9}}\;dx} = \answer{{-\log\left(5 \, x + 1\right) - 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{16 \, x}{5 \, {\left(x^{2} - 6 \, x + 5\right)}}}\;dx} = \answer{{\frac{4}{5} \, \log\left(x - 1\right) - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x - 24}{3 \, x^{2} - 32 \, x + 64}}\;dx} = \answer{{\frac{1}{3} \, \log\left(3 \, x - 8\right) - 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(3 \, x - 7\right)}}{2 \, {\left(x^{2} - 9 \, x + 14\right)}}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(x - 2\right) - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(25 \, x + 26\right)}}{7 \, x^{2} + 37 \, x + 10}}\;dx} = \answer{{-\frac{8}{7} \, \log\left(7 \, x + 2\right) - 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{23 \, x + 20}{2 \, {\left(2 \, x^{2} - 15 \, x - 8\right)}}}\;dx} = \answer{{\frac{1}{4} \, \log\left(2 \, x + 1\right) - 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17}{2 \, {\left(x + 4\right)}}}\;dx} = \answer{{\frac{17}{2} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{14 \, {\left(x + 2\right)}}{4 \, x^{2} + 19 \, x - 5}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(4 \, x - 1\right) - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 17\right)}}{2 \, x^{2} + 13 \, x - 7}}\;dx} = \answer{{-\frac{7}{2} \, \log\left(2 \, x - 1\right) + 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{47 \, x - 15}{9 \, x^{2} + 40 \, x - 25}}\;dx} = \answer{{\frac{2}{9} \, \log\left(9 \, x - 5\right) + 5 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{23 \, x - 27}{6 \, {\left(x^{2} - 8 \, x - 9\right)}}}\;dx} = \answer{{\frac{5}{6} \, \log\left(x + 1\right) + 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, x + 17}{3 \, {\left(x^{2} + 7 \, x + 10\right)}}}\;dx} = \answer{{-2 \, \log\left(x + 5\right) - \frac{1}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(22 \, x - 23\right)}}{7 \, x^{2} - 41 \, x + 30}}\;dx} = \answer{{\frac{2}{7} \, \log\left(7 \, x - 6\right) + 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{29 \, x - 35}{3 \, {\left(x^{2} - 5 \, x + 4\right)}}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(x - 1\right) - 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(6 \, x - 7\right)}}{7 \, x^{2} + 41 \, x - 6}}\;dx} = \answer{{\frac{3}{7} \, \log\left(7 \, x - 1\right) - 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{21 \, x - 58}{3 \, x^{2} - 17 \, x + 24}}\;dx} = \answer{{2 \, \log\left(3 \, x - 8\right) + 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, {\left(8 \, x - 5\right)}}{3 \, {\left(3 \, x^{2} + 25 \, x + 8\right)}}}\;dx} = \answer{{-\frac{5}{9} \, \log\left(3 \, x + 1\right) + 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, x + 7}{x^{2} + 7 \, x + 10}}\;dx} = \answer{{6 \, \log\left(x + 5\right) - \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x - 16}{2 \, {\left(2 \, x^{2} + 9 \, x + 4\right)}}}\;dx} = \answer{{-\frac{7}{4} \, \log\left(2 \, x + 1\right) + 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{25 \, x - 41}{7 \, {\left(x^{2} - 6 \, x + 5\right)}}}\;dx} = \answer{{\frac{4}{7} \, \log\left(x - 1\right) + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{10 \, x + 17}{4 \, x^{2} + 29 \, x + 45}}\;dx} = \answer{{\frac{1}{2} \, \log\left(4 \, x + 9\right) - 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(19 \, x - 5\right)}}{9 \, x^{2} - 79 \, x - 18}}\;dx} = \answer{{-\frac{2}{9} \, \log\left(9 \, x + 2\right) - 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{40 \, {\left(x - 1\right)}}{9 \, x^{2} + 22 \, x - 15}}\;dx} = \answer{{\frac{5}{9} \, \log\left(9 \, x - 5\right) - 5 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{42 \, x - 29}{8 \, x^{2} + 17 \, x - 21}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(8 \, x - 7\right) - 5 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{71 \, x + 44}{7 \, x^{2} + 11 \, x + 4}}\;dx} = \answer{{-\frac{8}{7} \, \log\left(7 \, x + 4\right) - 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(19 \, x - 65\right)}}{5 \, x^{2} + 26 \, x - 63}}\;dx} = \answer{{-\frac{7}{5} \, \log\left(5 \, x - 9\right) + 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(11 \, x + 4\right)}}{8 \, {\left(x^{2} + 3 \, x - 4\right)}}}\;dx} = \answer{{3 \, \log\left(x + 4\right) + \frac{9}{8} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{55 \, x + 72}{3 \, {\left(2 \, x^{2} - 15 \, x - 27\right)}}}\;dx} = \answer{{\frac{1}{6} \, \log\left(2 \, x + 3\right) + 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x + 40}{8 \, x^{2} - 65 \, x + 63}}\;dx} = \answer{{\frac{7}{8} \, \log\left(8 \, x - 9\right) - \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{25 \, x - 39}{2 \, x^{2} + 3 \, x - 54}}\;dx} = \answer{{\frac{7}{2} \, \log\left(2 \, x - 9\right) + 9 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 32\right)}}{x^{2} - 8 \, x - 9}}\;dx} = \answer{{7 \, \log\left(x + 1\right) - \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x + 19}{2 \, {\left(3 \, x^{2} + 13 \, x + 12\right)}}}\;dx} = \answer{{\frac{1}{6} \, \log\left(3 \, x + 4\right) + 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, {\left(10 \, x - 3\right)}}{9 \, x^{2} + 29 \, x - 28}}\;dx} = \answer{{\frac{8}{9} \, \log\left(9 \, x - 7\right) + 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(8 \, x - 27\right)}}{4 \, x^{2} + 25 \, x - 56}}\;dx} = \answer{{\log\left(4 \, x - 7\right) - 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{41 \, x + 7}{4 \, {\left(2 \, x^{2} - x - 1\right)}}}\;dx} = \answer{{-\frac{9}{8} \, \log\left(2 \, x + 1\right) - 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x + 82}{3 \, {\left(x^{2} + 10 \, x + 16\right)}}}\;dx} = \answer{{-3 \, \log\left(x + 8\right) - \frac{8}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(7 \, x + 3\right)}}{2 \, x^{2} - 9 \, x - 18}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(2 \, x + 3\right) - 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{21 \, x - 41}{3 \, x^{2} - 19 \, x + 6}}\;dx} = \answer{{2 \, \log\left(3 \, x - 1\right) + 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{39 \, x + 32}{9 \, x^{2} + 41 \, x + 20}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(9 \, x + 5\right) - 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 3\right)}}{4 \, {\left(x^{2} + 4 \, x - 5\right)}}}\;dx} = \answer{{\log\left(x + 5\right) - \frac{1}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{27 \, x + 91}{3 \, x^{2} + 35 \, x + 72}}\;dx} = \answer{{\log\left(3 \, x + 8\right) + 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(6 \, x - 7\right)}}{8 \, x^{2} + 43 \, x + 15}}\;dx} = \answer{{\frac{3}{4} \, \log\left(8 \, x + 3\right) - 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{20 \, {\left(x - 1\right)}}{3 \, x^{2} + x - 2}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(3 \, x - 2\right) + 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x + 3\right)}}{2 \, x^{2} + 7 \, x - 4}}\;dx} = \answer{{-\frac{7}{2} \, \log\left(2 \, x - 1\right) - \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{47 \, x - 96}{9 \, {\left(x^{2} + 5 \, x - 6\right)}}}\;dx} = \answer{{6 \, \log\left(x + 6\right) - \frac{7}{9} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{31 \, x + 27}{3 \, x^{2} + 26 \, x - 9}}\;dx} = \answer{{\frac{4}{3} \, \log\left(3 \, x - 1\right) + 9 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, x + 59}{4 \, x^{2} - 27 \, x - 7}}\;dx} = \answer{{-2 \, \log\left(4 \, x + 1\right) + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(7 \, x - 27\right)}}{3 \, x^{2} + 13 \, x - 30}}\;dx} = \answer{{-2 \, \log\left(3 \, x - 5\right) + 9 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, {\left(7 \, x - 10\right)}}{8 \, x^{2} + 71 \, x - 9}}\;dx} = \answer{{\frac{3}{4} \, \log\left(8 \, x - 1\right) - 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{23 \, x + 39}{4 \, {\left(x^{2} + 11 \, x + 18\right)}}}\;dx} = \answer{{6 \, \log\left(x + 9\right) - \frac{1}{4} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, x + 11}{3 \, x^{2} + 11 \, x - 4}}\;dx} = \answer{{-\log\left(3 \, x^{2} + 11 \, x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{81 \, {\left(x - 1\right)}}{8 \, x^{2} - 37 \, x + 20}}\;dx} = \answer{{\frac{9}{8} \, \log\left(8 \, x - 5\right) + 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(17 \, x - 9\right)}}{3 \, {\left(3 \, x^{2} - 10 \, x + 3\right)}}}\;dx} = \answer{{-\frac{5}{9} \, \log\left(3 \, x - 1\right) - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{10 \, {\left(x - 1\right)}}{2 \, x^{2} - 11 \, x + 12}}\;dx} = \answer{{-\log\left(2 \, x - 3\right) + 6 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x - 11}{8 \, x^{2} + 17 \, x - 21}}\;dx} = \answer{{-\frac{1}{8} \, \log\left(8 \, x - 7\right) - 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(11 \, x + 9\right)}}{7 \, x^{2} + 31 \, x + 12}}\;dx} = \answer{{-\frac{6}{7} \, \log\left(7 \, x + 3\right) - 7 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2}{x - 9}}\;dx} = \answer{{2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{79 \, x - 16}{8 \, x^{2} - 9 \, x + 1}}\;dx} = \answer{{\frac{7}{8} \, \log\left(8 \, x - 1\right) + 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{26}{x^{2} - 3 \, x - 40}}\;dx} = \answer{{2 \, \log\left(x + 5\right) - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{79 \, x - 87}{3 \, {\left(3 \, x^{2} - 28 \, x + 9\right)}}}\;dx} = \answer{{-\frac{7}{9} \, \log\left(3 \, x - 1\right) - 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{9 \, x + 52}{x^{2} + 6 \, x - 16}}\;dx} = \answer{{-2 \, \log\left(x + 8\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{14 \, x + 47}{8 \, x^{2} + 63 \, x - 8}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(8 \, x - 1\right) - \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17}{2 \, {\left(x + 2\right)}}}\;dx} = \answer{{\frac{17}{2} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 5\right)}}{x^{2} + 5 \, x - 14}}\;dx} = \answer{{-4 \, \log\left(x + 7\right) + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{20 \, x - 63}{9 \, x^{2} + 47 \, x - 42}}\;dx} = \answer{{-\frac{7}{9} \, \log\left(9 \, x - 7\right) + 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(25 \, x + 51\right)}}{7 \, x^{2} + 51 \, x + 54}}\;dx} = \answer{{\frac{8}{7} \, \log\left(7 \, x + 9\right) + 6 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{x - 11}{2 \, {\left(2 \, x^{2} + 5 \, x - 3\right)}}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(2 \, x - 1\right) + \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x - 5\right)}}{3 \, x^{2} - 20 \, x - 7}}\;dx} = \answer{{\frac{2}{3} \, \log\left(3 \, x + 1\right) + 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(35 \, x + 1\right)}}{8 \, x^{2} + 51 \, x - 35}}\;dx} = \answer{{\frac{3}{4} \, \log\left(8 \, x - 5\right) + 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{23 \, x - 67}{3 \, {\left(x^{2} - 5 \, x + 6\right)}}}\;dx} = \answer{{7 \, \log\left(x - 2\right) + \frac{2}{3} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x + 72}{3 \, x^{2} - 4 \, x - 32}}\;dx} = \answer{{\frac{4}{3} \, \log\left(3 \, x + 8\right) - 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, x + 1}{x^{2} + 7 \, x + 10}}\;dx} = \answer{{3 \, \log\left(x + 5\right) - \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x + 15}{3 \, {\left(x^{2} - 5 \, x - 6\right)}}}\;dx} = \answer{{\frac{2}{3} \, \log\left(x + 1\right) - \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{37 \, x + 29}{9 \, x^{2} - 26 \, x - 3}}\;dx} = \answer{{-\frac{8}{9} \, \log\left(9 \, x + 1\right) + 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{59 \, x + 20}{9 \, x^{2} - 13 \, x - 10}}\;dx} = \answer{{\frac{5}{9} \, \log\left(9 \, x + 5\right) + 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(7 \, x + 13\right)}}{5 \, x^{2} - 34 \, x - 48}}\;dx} = \answer{{\frac{1}{5} \, \log\left(5 \, x + 6\right) - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, x + 44}{4 \, x^{2} - 17 \, x - 15}}\;dx} = \answer{{\frac{7}{4} \, \log\left(4 \, x + 3\right) - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, {\left(x + 1\right)}}{x^{2} - 7 \, x + 6}}\;dx} = \answer{{-2 \, \log\left(x - 1\right) + 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{10 \, x - 33}{4 \, x^{2} + 31 \, x - 45}}\;dx} = \answer{{\frac{1}{2} \, \log\left(4 \, x - 5\right) - 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{13 \, x - 3}{3 \, x^{2} - 20 \, x - 7}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(3 \, x + 1\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x - 4}{4 \, x^{2} + x - 5}}\;dx} = \answer{{\frac{9}{4} \, \log\left(4 \, x + 5\right) + \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, x + 11}{x^{2} + 7 \, x + 6}}\;dx} = \answer{{-5 \, \log\left(x + 6\right) - \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 18\right)}}{x^{2} + 11 \, x + 24}}\;dx} = \answer{{4 \, \log\left(x + 8\right) - 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{46 \, x - 9}{8 \, x^{2} - 29 \, x - 12}}\;dx} = \answer{{\frac{3}{4} \, \log\left(8 \, x + 3\right) + 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{x - 19}{7 \, {\left(x^{2} + x - 2\right)}}}\;dx} = \answer{{\log\left(x + 2\right) - \frac{6}{7} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{44}{2 \, x^{2} + 3 \, x - 14}}\;dx} = \answer{{4 \, \log\left(2 \, x + 7\right) - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{45 \, x - 88}{9 \, x^{2} - 76 \, x + 32}}\;dx} = \answer{{-\log\left(9 \, x - 4\right) - 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x - 46}{x^{2} - 9 \, x + 8}}\;dx} = \answer{{5 \, \log\left(x - 1\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(31 \, x + 9\right)}}{8 \, x^{2} - 39 \, x - 5}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(8 \, x + 1\right) + 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{29 \, x - 39}{8 \, x^{2} + 69 \, x - 27}}\;dx} = \answer{{\frac{3}{8} \, \log\left(8 \, x - 3\right) - 4 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{12 \, {\left(x + 2\right)}}{7 \, x^{2} + 32 \, x + 16}}\;dx} = \answer{{-\frac{5}{7} \, \log\left(7 \, x + 4\right) - \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{49 \, x - 31}{3 \, {\left(3 \, x^{2} - 5 \, x + 2\right)}}}\;dx} = \answer{{-\frac{5}{9} \, \log\left(3 \, x - 2\right) + 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{14 \, {\left(3 \, x + 2\right)}}{7 \, x^{2} + 20 \, x + 12}}\;dx} = \answer{{-\log\left(7 \, x + 6\right) + 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{49 \, x + 43}{2 \, {\left(4 \, x^{2} + 31 \, x + 21\right)}}}\;dx} = \answer{{-\frac{1}{8} \, \log\left(4 \, x + 3\right) - 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{16 \, {\left(2 \, x + 1\right)}}{7 \, x^{2} - 2 \, x - 5}}\;dx} = \answer{{\frac{4}{7} \, \log\left(7 \, x + 5\right) + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x + 3}{3 \, {\left(x^{2} - 2 \, x - 3\right)}}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(x + 1\right) - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{9 \, x + 2}{2 \, x^{2} - 15 \, x + 7}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(2 \, x - 1\right) + 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{65 \, x + 72}{8 \, x^{2} + 17 \, x + 9}}\;dx} = \answer{{\frac{9}{8} \, \log\left(8 \, x + 9\right) + 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(9 \, x + 14\right)}}{2 \, x^{2} + 7 \, x + 6}}\;dx} = \answer{{-\log\left(2 \, x + 3\right) - 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 17\right)}}{x^{2} + 8 \, x + 15}}\;dx} = \answer{{8 \, \log\left(x + 5\right) + 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{14 \, x + 9}{2 \, x^{2} - 21 \, x + 27}}\;dx} = \answer{{2 \, \log\left(2 \, x - 3\right) - 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 9\right)}}{9 \, x^{2} - 38 \, x + 8}}\;dx} = \answer{{\frac{5}{9} \, \log\left(9 \, x - 2\right) - \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{25 \, x - 31}{3 \, x^{2} + 22 \, x - 45}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(3 \, x - 5\right) - 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(8 \, x + 3\right)}}{3 \, {\left(x^{2} - 8 \, x - 9\right)}}}\;dx} = \answer{{\frac{1}{3} \, \log\left(x + 1\right) + 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{14 \, {\left(5 \, x - 7\right)}}{8 \, x^{2} - 63 \, x + 49}}\;dx} = \answer{{\frac{3}{4} \, \log\left(8 \, x - 7\right) + 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, x - 11}{4 \, {\left(x^{2} - 1\right)}}}\;dx} = \answer{{-2 \, \log\left(x + 1\right) + \frac{3}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x - 29}{x^{2} - 14 \, x + 45}}\;dx} = \answer{{-6 \, \log\left(x - 5\right) + 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 20\right)}}{x^{2} - 2 \, x - 48}}\;dx} = \answer{{-2 \, \log\left(x + 6\right) + 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, x + 11}{x^{2} + 5 \, x + 6}}\;dx} = \answer{{-7 \, \log\left(x + 3\right) + \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(10 \, x + 21\right)}}{2 \, x^{2} + 11 \, x - 6}}\;dx} = \answer{{-4 \, \log\left(2 \, x - 1\right) - 6 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{42 \, x - 31}{7 \, x^{2} - 23 \, x + 6}}\;dx} = \answer{{-\log\left(7 \, x - 2\right) - 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, {\left(x - 9\right)}}{7 \, x^{2} + 54 \, x - 81}}\;dx} = \answer{{-\frac{6}{7} \, \log\left(7 \, x - 9\right) + 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, {\left(5 \, x + 3\right)}}{3 \, {\left(2 \, x^{2} + 7 \, x + 3\right)}}}\;dx} = \answer{{\frac{1}{6} \, \log\left(2 \, x + 1\right) + 4 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{27 \, {\left(2 \, x - 3\right)}}{7 \, x^{2} - x - 8}}\;dx} = \answer{{-\frac{9}{7} \, \log\left(7 \, x - 8\right) + 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{9 \, x - 5}{2 \, x^{2} + 5 \, x - 18}}\;dx} = \answer{{-\frac{7}{2} \, \log\left(2 \, x + 9\right) - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2}{x^{2} - 16 \, x + 63}}\;dx} = \answer{{\log\left(x - 7\right) - \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(7 \, x - 1\right)}}{2 \, x^{2} + 13 \, x - 24}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(2 \, x - 3\right) - 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{43 \, x + 33}{2 \, {\left(3 \, x^{2} + 10 \, x + 3\right)}}}\;dx} = \answer{{\frac{7}{6} \, \log\left(3 \, x + 1\right) + 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{37 \, x - 89}{9 \, x^{2} + 67 \, x - 40}}\;dx} = \answer{{-\frac{8}{9} \, \log\left(9 \, x - 5\right) + 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x + 24}{5 \, x^{2} - 17 \, x - 12}}\;dx} = \answer{{-\frac{3}{5} \, \log\left(5 \, x + 3\right) + 4 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x + 35}{6 \, x^{2} + 17 \, x - 28}}\;dx} = \answer{{-\frac{7}{6} \, \log\left(6 \, x - 7\right) + \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, {\left(x - 2\right)}}{x^{2} - 9 \, x + 8}}\;dx} = \answer{{\log\left(x - 1\right) + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{32 \, x - 67}{5 \, {\left(x^{2} - 7 \, x + 6\right)}}}\;dx} = \answer{{-\frac{7}{5} \, \log\left(x - 1\right) - 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(5 \, x - 33\right)}}{4 \, x^{2} + 33 \, x - 27}}\;dx} = \answer{{\frac{9}{4} \, \log\left(4 \, x - 3\right) - 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(19 \, x - 4\right)}}{9 \, x^{2} - 19 \, x + 2}}\;dx} = \answer{{-\frac{4}{9} \, \log\left(9 \, x - 1\right) - 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(6 \, x - 5\right)}}{5 \, x^{2} + 7 \, x - 24}}\;dx} = \answer{{\frac{3}{5} \, \log\left(5 \, x - 8\right) + 3 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(6 \, x + 1\right)}}{5 \, x^{2} + 9 \, x - 2}}\;dx} = \answer{{-\log\left(5 \, x - 1\right) - 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(17 \, x - 25\right)}}{7 \, x^{2} + 44 \, x - 35}}\;dx} = \answer{{-\frac{5}{7} \, \log\left(7 \, x - 5\right) + 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x + 40}{3 \, x^{2} + 17 \, x - 6}}\;dx} = \answer{{\frac{7}{3} \, \log\left(3 \, x - 1\right) + 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{14 \, {\left(3 \, x + 4\right)}}{5 \, x^{2} + 32 \, x + 12}}\;dx} = \answer{{-\frac{7}{5} \, \log\left(5 \, x + 2\right) - 7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{59}{7 \, {\left(x + 1\right)}}}\;dx} = \answer{{\frac{59}{7} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{21 \, x - 11}{2 \, {\left(3 \, x^{2} + 29 \, x + 18\right)}}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(3 \, x + 2\right) + 4 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, x - 11}{8 \, x^{2} + 3 \, x - 5}}\;dx} = \answer{{\frac{3}{4} \, \log\left(8 \, x - 5\right) - \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{35 \, x - 37}{4 \, x^{2} - x - 5}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(4 \, x - 5\right) - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(12 \, x + 17\right)}}{3 \, x^{2} - 8 \, x - 35}}\;dx} = \answer{{\log\left(3 \, x + 7\right) + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 9\right)}}{x^{2} + 4 \, x + 3}}\;dx} = \answer{{-3 \, \log\left(x + 3\right) + 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(4 \, x - 5\right)}}{3 \, {\left(3 \, x^{2} + 20 \, x - 7\right)}}}\;dx} = \answer{{\frac{2}{9} \, \log\left(3 \, x - 1\right) - 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{9 \, x - 1}{3 \, x^{2} + 25 \, x + 42}}\;dx} = \answer{{2 \, \log\left(3 \, x + 7\right) - 5 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, {\left(3 \, x - 4\right)}}{9 \, x^{2} + 4 \, x - 5}}\;dx} = \answer{{\log\left(9 \, x - 5\right) - 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(9 \, x - 32\right)}}{9 \, x^{2} - 64 \, x + 7}}\;dx} = \answer{{\log\left(9 \, x^{2} - 64 \, x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 11\right)}}{x^{2} - 14 \, x + 48}}\;dx} = \answer{{-5 \, \log\left(x - 6\right) + 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x + 78}{x^{2} + 9 \, x + 18}}\;dx} = \answer{{8 \, \log\left(x + 6\right) + 9 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{76 \, x - 27}{9 \, x^{2} + 83 \, x + 18}}\;dx} = \answer{{\frac{5}{9} \, \log\left(9 \, x + 2\right) - 9 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{40 \, {\left(x - 2\right)}}{3 \, {\left(2 \, x^{2} + 13 \, x - 7\right)}}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(2 \, x - 1\right) + 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x + 60}{2 \, x^{2} + 25 \, x + 72}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(2 \, x + 9\right) - 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{51 \, x + 11}{9 \, x^{2} + 10 \, x + 1}}\;dx} = \answer{{\frac{2}{3} \, \log\left(9 \, x + 1\right) + 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, x - 29}{2 \, {\left(2 \, x^{2} - 13 \, x + 15\right)}}}\;dx} = \answer{{-\frac{7}{4} \, \log\left(2 \, x - 3\right) + \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{40 \, x - 73}{4 \, x^{2} - 33 \, x + 35}}\;dx} = \answer{{\log\left(4 \, x - 5\right) + 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{23 \, x + 1}{7 \, x^{2} + 3 \, x - 4}}\;dx} = \answer{{\frac{9}{7} \, \log\left(7 \, x - 4\right) + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x + 5\right)}}{2 \, {\left(3 \, x^{2} + 7 \, x + 4\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(3 \, x + 4\right) - 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{31 \, x + 41}{5 \, x^{2} + 12 \, x + 7}}\;dx} = \answer{{\frac{6}{5} \, \log\left(5 \, x + 7\right) + 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{34 \, x + 31}{3 \, {\left(2 \, x^{2} + 9 \, x + 4\right)}}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(2 \, x + 1\right) - 5 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, x - 4}{4 \, x^{2} - 25 \, x - 21}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(4 \, x + 3\right) - \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{31 \, x - 5}{6 \, x^{2} - 35 \, x + 25}}\;dx} = \answer{{\frac{5}{6} \, \log\left(6 \, x - 5\right) - 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 5\right)}}{3 \, x^{2} - 10 \, x + 7}}\;dx} = \answer{{-\log\left(3 \, x^{2} - 10 \, x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{61 \, x + 71}{7 \, x^{2} + 44 \, x + 45}}\;dx} = \answer{{-\frac{2}{7} \, \log\left(7 \, x + 9\right) + 9 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{8 \, x - 45}{4 \, x^{2} + 33 \, x - 27}}\;dx} = \answer{{\log\left(4 \, x - 3\right) - 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{44 \, x + 7}{6 \, x^{2} + 41 \, x - 7}}\;dx} = \answer{{\frac{1}{3} \, \log\left(6 \, x - 1\right) + 7 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, {\left(8 \, x - 9\right)}}{9 \, x^{2} + 71 \, x - 8}}\;dx} = \answer{{-\frac{8}{9} \, \log\left(9 \, x - 1\right) + 8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(19 \, x + 20\right)}}{4 \, x^{2} + 31 \, x - 8}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(4 \, x - 1\right) - 8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(23 \, x + 5\right)}}{7 \, x^{2} - 58 \, x + 16}}\;dx} = \answer{{\frac{3}{7} \, \log\left(7 \, x - 2\right) - 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x + 41}{2 \, {\left(x^{2} + 4 \, x + 3\right)}}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(x + 3\right) + 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{16 \, {\left(4 \, x + 5\right)}}{3 \, {\left(3 \, x^{2} - 10 \, x - 8\right)}}}\;dx} = \answer{{\frac{8}{9} \, \log\left(3 \, x + 2\right) - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(11 \, x - 31\right)}}{3 \, {\left(x^{2} + 6 \, x - 7\right)}}}\;dx} = \answer{{-9 \, \log\left(x + 7\right) + \frac{5}{3} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 1\right)}}{x^{2} - 6 \, x + 8}}\;dx} = \answer{{-3 \, \log\left(x - 2\right) + 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{14 \, x - 5}{5 \, x^{2} + 41 \, x + 8}}\;dx} = \answer{{-\frac{1}{5} \, \log\left(5 \, x + 1\right) + 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{69 \, x + 11}{9 \, x^{2} + 67 \, x + 28}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(9 \, x + 4\right) + 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{41 \, x + 13}{5 \, x^{2} - 4 \, x - 1}}\;dx} = \answer{{-\frac{4}{5} \, \log\left(5 \, x + 1\right) + 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x + 30}{2 \, x^{2} + 15 \, x + 25}}\;dx} = \answer{{\frac{5}{2} \, \log\left(2 \, x + 5\right) + \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 5\right)}}{x^{2} + 7 \, x - 18}}\;dx} = \answer{{-4 \, \log\left(x + 9\right) - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(5 \, x - 3\right)}}{2 \, {\left(x^{2} - 1\right)}}}\;dx} = \answer{{-6 \, \log\left(x + 1\right) - \frac{3}{2} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, x + 23}{x^{2} + 11 \, x + 30}}\;dx} = \answer{{5 \, \log\left(x + 6\right) - 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17}{3 \, x^{2} - 7 \, x - 20}}\;dx} = \answer{{-\log\left(3 \, x + 5\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{18 \, {\left(2 \, x + 1\right)}}{5 \, x^{2} + 32 \, x + 35}}\;dx} = \answer{{-\frac{9}{5} \, \log\left(5 \, x + 7\right) + 9 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{26 \, x - 77}{5 \, x^{2} + 33 \, x - 14}}\;dx} = \answer{{\frac{9}{5} \, \log\left(5 \, x - 2\right) - 7 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 2\right)}}{x^{2} - 4 \, x - 5}}\;dx} = \answer{{\log\left(x + 1\right) - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{41 \, x + 58}{2 \, {\left(3 \, x^{2} - 17 \, x - 6\right)}}}\;dx} = \answer{{\frac{7}{6} \, \log\left(3 \, x + 1\right) - 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{21 \, x - 5}{7 \, x^{2} + 25 \, x + 12}}\;dx} = \answer{{-\log\left(7 \, x + 4\right) + 4 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{29 \, x - 22}{2 \, {\left(3 \, x^{2} + 2 \, x - 8\right)}}}\;dx} = \answer{{\frac{5}{6} \, \log\left(3 \, x - 4\right) + 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{55 \, x - 82}{7 \, x^{2} + 54 \, x - 16}}\;dx} = \answer{{\frac{8}{7} \, \log\left(7 \, x - 2\right) - 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, x - 1}{2 \, {\left(x^{2} - 1\right)}}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(x + 1\right) - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{22 \, x + 15}{4 \, x^{2} + 21 \, x + 5}}\;dx} = \answer{{\frac{1}{2} \, \log\left(4 \, x + 1\right) + 5 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, x + 39}{9 \, x^{2} - 59 \, x - 28}}\;dx} = \answer{{-\frac{5}{9} \, \log\left(9 \, x + 4\right) + \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, x + 15}{x^{2} + x - 12}}\;dx} = \answer{{-\log\left(x + 4\right) + 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(7 \, x + 10\right)}}{3 \, x^{2} - 26 \, x + 16}}\;dx} = \answer{{\frac{4}{3} \, \log\left(3 \, x - 2\right) - 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x + 5}{3 \, {\left(x^{2} - 1\right)}}}\;dx} = \answer{{\frac{2}{3} \, \log\left(x + 1\right) - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{36 \, x + 47}{6 \, x^{2} - 7 \, x - 10}}\;dx} = \answer{{\log\left(6 \, x + 5\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{49 \, x + 23}{8 \, {\left(x^{2} + 6 \, x - 7\right)}}}\;dx} = \answer{{-5 \, \log\left(x + 7\right) - \frac{9}{8} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{41 \, x + 4}{6 \, x^{2} + 19 \, x + 3}}\;dx} = \answer{{\frac{1}{6} \, \log\left(6 \, x + 1\right) - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(15 \, x + 8\right)}}{7 \, x^{2} - 22 \, x - 24}}\;dx} = \answer{{-\frac{3}{7} \, \log\left(7 \, x + 6\right) - 6 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{22 \, {\left(x + 1\right)}}{3 \, x^{2} + 13 \, x + 4}}\;dx} = \answer{{\frac{4}{3} \, \log\left(3 \, x + 1\right) + 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(17 \, x + 30\right)}}{7 \, x^{2} - 40 \, x - 12}}\;dx} = \answer{{-\frac{8}{7} \, \log\left(7 \, x + 2\right) + 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{8 \, {\left(2 \, x - 3\right)}}{x^{2} - 4 \, x - 12}}\;dx} = \answer{{-7 \, \log\left(x + 2\right) - 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{x - 18}{7 \, x^{2} - 12 \, x - 4}}\;dx} = \answer{{\frac{8}{7} \, \log\left(7 \, x + 2\right) - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x - 40}{2 \, x^{2} - 17 \, x + 35}}\;dx} = \answer{{\frac{1}{2} \, \log\left(2 \, x - 7\right) + 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{x - 24}{x^{2} + 2 \, x - 24}}\;dx} = \answer{{3 \, \log\left(x + 6\right) - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{53 \, x + 55}{8 \, x^{2} + 45 \, x + 25}}\;dx} = \answer{{\frac{5}{8} \, \log\left(8 \, x + 5\right) + 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 7\right)}}{x^{2} - 10 \, x + 16}}\;dx} = \answer{{3 \, \log\left(x - 2\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, {\left(10 \, x - 3\right)}}{9 \, x^{2} - 11 \, x + 2}}\;dx} = \answer{{-\frac{7}{9} \, \log\left(9 \, x - 2\right) - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x - 4}{2 \, {\left(x^{2} - 5 \, x - 24\right)}}}\;dx} = \answer{{-\frac{5}{2} \, \log\left(x + 3\right) - 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{72 \, {\left(x - 1\right)}}{7 \, x^{2} - 50 \, x + 7}}\;dx} = \answer{{-\frac{9}{7} \, \log\left(7 \, x - 1\right) - 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 15\right)}}{x^{2} + 6 \, x - 27}}\;dx} = \answer{{-8 \, \log\left(x + 9\right) + 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x - 1}{3 \, {\left(x^{2} - 5 \, x + 4\right)}}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(x - 1\right) + 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{x + 23}{x^{2} + 11 \, x + 18}}\;dx} = \answer{{-2 \, \log\left(x + 9\right) + 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{33 \, x - 26}{7 \, x^{2} - 18 \, x + 8}}\;dx} = \answer{{-\frac{5}{7} \, \log\left(7 \, x - 4\right) - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{16 \, {\left(x - 4\right)}}{3 \, x^{2} + 16 \, x - 12}}\;dx} = \answer{{-\frac{8}{3} \, \log\left(3 \, x - 2\right) + 8 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{79 \, x - 44}{9 \, x^{2} + 32 \, x - 16}}\;dx} = \answer{{-\frac{2}{9} \, \log\left(9 \, x - 4\right) + 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{14 \, x - 11}{3 \, {\left(2 \, x^{2} - 11 \, x + 12\right)}}}\;dx} = \answer{{\frac{2}{3} \, \log\left(2 \, x - 3\right) - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{61 \, x - 144}{3 \, {\left(2 \, x^{2} - 21 \, x + 27\right)}}}\;dx} = \answer{{\frac{7}{6} \, \log\left(2 \, x - 3\right) + 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x - 5\right)}}{x^{2} - 5 \, x - 14}}\;dx} = \answer{{4 \, \log\left(x^{2} - 5 \, x - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(22 \, x + 9\right)}}{9 \, x^{2} - 70 \, x - 16}}\;dx} = \answer{{-\frac{1}{9} \, \log\left(9 \, x + 2\right) + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, x - 41}{2 \, x^{2} + 13 \, x - 24}}\;dx} = \answer{{2 \, \log\left(2 \, x - 3\right) - 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{23 \, x + 19}{3 \, {\left(2 \, x^{2} - 13 \, x - 7\right)}}}\;dx} = \answer{{\frac{1}{6} \, \log\left(2 \, x + 1\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x + 17}{2 \, {\left(x^{2} - x - 6\right)}}}\;dx} = \answer{{\frac{1}{2} \, \log\left(x + 2\right) + 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x - 8}{4 \, x^{2} + 5 \, x - 6}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(4 \, x - 3\right) + 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{29 \, x + 1}{5 \, x^{2} + 6 \, x + 1}}\;dx} = \answer{{-\frac{6}{5} \, \log\left(5 \, x + 1\right) + 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{25 \, x + 77}{3 \, x^{2} - 14 \, x - 49}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(3 \, x + 7\right) + 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{12 \, {\left(4 \, x - 1\right)}}{5 \, x^{2} + 8 \, x - 4}}\;dx} = \answer{{\frac{3}{5} \, \log\left(5 \, x - 2\right) + 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{6 \, {\left(8 \, x - 11\right)}}{7 \, x^{2} + 12 \, x - 27}}\;dx} = \answer{{\frac{1}{7} \, \log\left(7 \, x - 9\right) - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x - 1}{3 \, x^{2} + 7 \, x + 2}}\;dx} = \answer{{\frac{4}{3} \, \log\left(3 \, x + 1\right) - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{13 \, x + 15}{6 \, {\left(x^{2} + 2 \, x - 3\right)}}}\;dx} = \answer{{-\log\left(x + 3\right) - \frac{7}{6} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{61 \, x - 41}{8 \, x^{2} - 35 \, x + 12}}\;dx} = \answer{{\frac{5}{8} \, \log\left(8 \, x - 3\right) + 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x + 3}{8 \, x^{2} - 33 \, x + 4}}\;dx} = \answer{{-\frac{1}{8} \, \log\left(8 \, x - 1\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(11 \, x + 19\right)}}{9 \, {\left(x^{2} + 6 \, x + 5\right)}}}\;dx} = \answer{{2 \, \log\left(x + 5\right) + \frac{4}{9} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(5 \, x - 14\right)}}{3 \, x^{2} - 20 \, x + 12}}\;dx} = \answer{{2 \, \log\left(3 \, x - 2\right) + 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{6 \, x + 13}{x^{2} - 5 \, x - 6}}\;dx} = \answer{{-\log\left(x + 1\right) + 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(11 \, x + 21\right)}}{5 \, x^{2} - 42 \, x - 27}}\;dx} = \answer{{-\frac{3}{5} \, \log\left(5 \, x + 3\right) + 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 1\right)}}{x^{2} - 4 \, x - 5}}\;dx} = \answer{{\log\left(x + 1\right) + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(5 \, x - 9\right)}}{2 \, {\left(4 \, x^{2} + 9 \, x - 9\right)}}}\;dx} = \answer{{\frac{7}{8} \, \log\left(4 \, x - 3\right) - 4 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, x + 13}{x^{2} + x - 20}}\;dx} = \answer{{3 \, \log\left(x + 5\right) + 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(5 \, x + 4\right)}}{7 \, x^{2} + 13 \, x + 6}}\;dx} = \answer{{\frac{4}{7} \, \log\left(7 \, x + 6\right) - 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(37 \, x + 29\right)}}{3 \, {\left(3 \, x^{2} + 17 \, x + 10\right)}}}\;dx} = \answer{{-\frac{2}{9} \, \log\left(3 \, x + 2\right) - 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x + 64}{2 \, {\left(x^{2} - 7 \, x - 8\right)}}}\;dx} = \answer{{\frac{7}{2} \, \log\left(x + 1\right) - 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 1\right)}}{2 \, {\left(x^{2} + 11 \, x + 28\right)}}}\;dx} = \answer{{4 \, \log\left(x + 7\right) - \frac{5}{2} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, x - 5}{3 \, {\left(2 \, x^{2} + 9 \, x + 4\right)}}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(2 \, x + 1\right) + \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{35 \, {\left(2 \, x - 1\right)}}{9 \, x^{2} - 29 \, x + 6}}\;dx} = \answer{{\frac{7}{9} \, \log\left(9 \, x - 2\right) + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, {\left(8 \, x + 13\right)}}{9 \, x^{2} - 47 \, x - 42}}\;dx} = \answer{{-\frac{8}{9} \, \log\left(9 \, x + 7\right) + 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x - 60}{5 \, x^{2} + 31 \, x - 72}}\;dx} = \answer{{-\frac{3}{5} \, \log\left(5 \, x - 9\right) + 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{16 \, x + 35}{3 \, {\left(x^{2} + 7 \, x + 10\right)}}}\;dx} = \answer{{5 \, \log\left(x + 5\right) + \frac{1}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{19 \, x + 3}{9 \, x^{2} - 7 \, x - 2}}\;dx} = \answer{{-\frac{1}{9} \, \log\left(9 \, x + 2\right) - 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{52 \, {\left(x - 1\right)}}{6 \, x^{2} - 23 \, x + 15}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(6 \, x - 5\right) - 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, x + 3}{6 \, x^{2} + 7 \, x + 1}}\;dx} = \answer{{\frac{1}{3} \, \log\left(6 \, x + 1\right) + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{x - 33}{x^{2} + 9 \, x + 14}}\;dx} = \answer{{8 \, \log\left(x + 7\right) - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x + 6}{6 \, x^{2} - 23 \, x - 4}}\;dx} = \answer{{\frac{1}{6} \, \log\left(6 \, x + 1\right) - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, {\left(16 \, x + 3\right)}}{9 \, x^{2} - 53 \, x - 6}}\;dx} = \answer{{-\frac{1}{9} \, \log\left(9 \, x + 1\right) + 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(4 \, x - 1\right)}}{3 \, x^{2} - 7 \, x - 6}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(3 \, x + 2\right) - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 19\right)}}{7 \, x^{2} - 58 \, x + 16}}\;dx} = \answer{{\frac{5}{7} \, \log\left(7 \, x - 2\right) - \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{44 \, x + 27}{3 \, {\left(2 \, x^{2} + 7 \, x + 3\right)}}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(2 \, x + 1\right) - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{31 \, x - 55}{7 \, {\left(x^{2} - 10 \, x + 9\right)}}}\;dx} = \answer{{-\frac{3}{7} \, \log\left(x - 1\right) - 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 4\right)}}{2 \, x^{2} + 3 \, x - 2}}\;dx} = \answer{{\log\left(2 \, x - 1\right) - 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, x - 5}{x^{2} - 6 \, x - 7}}\;dx} = \answer{{\log\left(x + 1\right) + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x - 27}{8 \, {\left(x^{2} + 6 \, x + 5\right)}}}\;dx} = \answer{{-\log\left(x + 5\right) + \frac{7}{8} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(11 \, x - 5\right)}}{2 \, {\left(2 \, x^{2} - 3 \, x + 1\right)}}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(2 \, x - 1\right) + 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{20}{2 \, x^{2} + 9 \, x + 7}}\;dx} = \answer{{4 \, \log\left(2 \, x + 7\right) - 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, x - 5}{9 \, x^{2} + 28 \, x + 3}}\;dx} = \answer{{\frac{2}{9} \, \log\left(9 \, x + 1\right) - \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x - 6\right)}}{2 \, x^{2} - 7 \, x - 9}}\;dx} = \answer{{3 \, \log\left(2 \, x - 9\right) + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3}{x - 3}}\;dx} = \answer{{3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(7 \, x + 4\right)}}{6 \, x^{2} - 11 \, x - 21}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(6 \, x + 7\right) - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{31 \, x + 20}{9 \, x^{2} + 68 \, x - 32}}\;dx} = \answer{{\frac{4}{9} \, \log\left(9 \, x - 4\right) + 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, x - 7}{x^{2} - 1}}\;dx} = \answer{{6 \, \log\left(x + 1\right) - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x + 17}{5 \, x^{2} + 33 \, x - 14}}\;dx} = \answer{{\frac{3}{5} \, \log\left(5 \, x - 2\right) + 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{49 \, x + 6}{6 \, x^{2} - 19 \, x + 3}}\;dx} = \answer{{\frac{5}{6} \, \log\left(6 \, x - 1\right) - 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{4 \, {\left(7 \, x - 16\right)}}{4 \, x^{2} + 19 \, x - 30}}\;dx} = \answer{{\log\left(4 \, x - 5\right) - 8 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{13 \, x + 33}{x^{2} + 7 \, x + 6}}\;dx} = \answer{{-9 \, \log\left(x + 6\right) - 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, x + 79}{3 \, x^{2} - 17 \, x - 28}}\;dx} = \answer{{3 \, \log\left(3 \, x + 4\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{39 \, x - 37}{7 \, x^{2} - 57 \, x + 8}}\;dx} = \answer{{\frac{4}{7} \, \log\left(7 \, x - 1\right) + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, x - 5}{x^{2} - 5 \, x + 4}}\;dx} = \answer{{-\log\left(x - 1\right) + 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{17 \, x - 10}{4 \, x^{2} - 25 \, x + 6}}\;dx} = \answer{{\frac{1}{4} \, \log\left(4 \, x - 1\right) + 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(7 \, x + 8\right)}}{3 \, x^{2} + 14 \, x + 8}}\;dx} = \answer{{-\log\left(3 \, x + 2\right) - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{41 \, x - 21}{3 \, {\left(3 \, x^{2} + 28 \, x + 9\right)}}}\;dx} = \answer{{\frac{4}{9} \, \log\left(3 \, x + 1\right) - 5 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{13 \, x - 23}{2 \, {\left(2 \, x^{2} - 5 \, x + 3\right)}}}\;dx} = \answer{{\frac{7}{4} \, \log\left(2 \, x - 3\right) - 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{19 \, x - 4}{5 \, x^{2} - 3 \, x - 14}}\;dx} = \answer{{-\frac{9}{5} \, \log\left(5 \, x + 7\right) - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x - 42}{6 \, x^{2} + 5 \, x - 14}}\;dx} = \answer{{\frac{7}{6} \, \log\left(6 \, x - 7\right) - 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{36 \, x + 17}{6 \, x^{2} + 23 \, x + 15}}\;dx} = \answer{{-\log\left(6 \, x + 5\right) + 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{37 \, x + 14}{4 \, x^{2} + 21 \, x + 5}}\;dx} = \answer{{\frac{1}{4} \, \log\left(4 \, x + 1\right) + 9 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{10 \, {\left(2 \, x - 3\right)}}{3 \, {\left(x^{2} + 8 \, x - 9\right)}}}\;dx} = \answer{{7 \, \log\left(x + 9\right) - \frac{1}{3} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{5 \, {\left(3 \, x - 13\right)}}{3 \, x^{2} + 29 \, x + 18}}\;dx} = \answer{{3 \, \log\left(3 \, x + 2\right) - 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{29 \, x - 47}{7 \, x^{2} - 33 \, x + 20}}\;dx} = \answer{{\frac{8}{7} \, \log\left(7 \, x - 5\right) + 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{20 \, {\left(3 \, x + 5\right)}}{7 \, x^{2} + 71 \, x + 72}}\;dx} = \answer{{\frac{4}{7} \, \log\left(7 \, x + 8\right) + 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x + 9\right)}}{x^{2} + 7 \, x + 6}}\;dx} = \answer{{-6 \, \log\left(x + 6\right) - 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{39 \, {\left(x - 1\right)}}{5 \, x^{2} - 3 \, x - 8}}\;dx} = \answer{{\frac{9}{5} \, \log\left(5 \, x - 8\right) + 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(7 \, x + 9\right)}}{3 \, x^{2} - 19 \, x + 6}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(3 \, x - 1\right) + 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{42 \, x - 43}{8 \, x^{2} - x - 9}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(8 \, x - 9\right) - 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(5 \, x - 11\right)}}{5 \, x^{2} - 38 \, x + 21}}\;dx} = \answer{{\log\left(5 \, x - 3\right) + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(3 \, x + 2\right)}}{x^{2} + 4 \, x - 12}}\;dx} = \answer{{8 \, \log\left(x + 6\right) + 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x - 23}{4 \, {\left(2 \, x^{2} - 17 \, x - 9\right)}}}\;dx} = \answer{{-\frac{3}{8} \, \log\left(2 \, x + 1\right) - \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{43 \, x + 30}{7 \, x^{2} - 36 \, x - 36}}\;dx} = \answer{{\frac{1}{7} \, \log\left(7 \, x + 6\right) + 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{19}{4 \, x^{2} + 37 \, x + 63}}\;dx} = \answer{{-\log\left(4 \, x + 9\right) + \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x + 5}{x^{2} + 3 \, x + 2}}\;dx} = \answer{{9 \, \log\left(x + 2\right) - 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, x - 18}{8 \, x^{2} + 41 \, x + 36}}\;dx} = \answer{{-\frac{9}{8} \, \log\left(8 \, x + 9\right) + 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{29 \, x - 32}{3 \, x^{2} + 2 \, x - 8}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(3 \, x - 4\right) - 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, x - 39}{3 \, x^{2} + 20 \, x - 63}}\;dx} = \answer{{\frac{2}{3} \, \log\left(3 \, x - 7\right) - 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{7 \, {\left(7 \, x - 3\right)}}{8 \, x^{2} - 27 \, x + 9}}\;dx} = \answer{{\frac{1}{8} \, \log\left(8 \, x - 3\right) + 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{6 \, {\left(x - 2\right)}}{x^{2} - 8 \, x + 15}}\;dx} = \answer{{-3 \, \log\left(x - 3\right) + 9 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{25}{6 \, {\left(x - 1\right)}}}\;dx} = \answer{{\frac{25}{6} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{11 \, x - 56}{5 \, x^{2} + 32 \, x - 64}}\;dx} = \answer{{\frac{4}{5} \, \log\left(5 \, x - 8\right) - 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x - 5\right)}}{x^{2} - 2 \, x - 3}}\;dx} = \answer{{7 \, \log\left(x + 1\right) + \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(6 \, x + 11\right)}}{3 \, x^{2} + 11 \, x + 6}}\;dx} = \answer{{-2 \, \log\left(3 \, x^{2} + 11 \, x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{5 \, x - 52}{3 \, x^{2} - 14 \, x - 24}}\;dx} = \answer{{\frac{8}{3} \, \log\left(3 \, x + 4\right) - \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{3 \, {\left(4 \, x - 17\right)}}{6 \, x^{2} + 47 \, x - 8}}\;dx} = \answer{{\log\left(6 \, x - 1\right) - 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 5\right)}}{x^{2} - x - 2}}\;dx} = \answer{{-6 \, \log\left(x + 1\right) - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(20 \, x - 31\right)}}{5 \, x^{2} + 32 \, x - 21}}\;dx} = \answer{{\log\left(5 \, x - 3\right) - 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{35 \, x - 38}{4 \, {\left(x^{2} - 3 \, x + 2\right)}}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(x - 1\right) - 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{51 \, x + 79}{6 \, x^{2} + 49 \, x + 8}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(6 \, x + 1\right) - 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{7 \, x + 30}{3 \, x^{2} + 23 \, x + 14}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(3 \, x + 2\right) - \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{52 \, x + 63}{9 \, x^{2} + 43 \, x + 28}}\;dx} = \answer{{\frac{7}{9} \, \log\left(9 \, x + 7\right) + 5 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{8 \, {\left(7 \, x + 4\right)}}{6 \, x^{2} + 49 \, x - 45}}\;dx} = \answer{{\frac{4}{3} \, \log\left(6 \, x - 5\right) + 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{55 \, x - 76}{7 \, x^{2} + 31 \, x - 20}}\;dx} = \answer{{\frac{8}{7} \, \log\left(7 \, x - 4\right) - 9 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{13 \, x - 29}{2 \, {\left(2 \, x^{2} - 11 \, x + 5\right)}}}\;dx} = \answer{{\frac{5}{4} \, \log\left(2 \, x - 1\right) + 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{16 \, x + 17}{5 \, x^{2} + 29 \, x + 20}}\;dx} = \answer{{\frac{1}{5} \, \log\left(5 \, x + 4\right) + 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{x - 57}{x^{2} + 3 \, x - 40}}\;dx} = \answer{{5 \, \log\left(x + 8\right) - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(11 \, x - 39\right)}}{3 \, x^{2} + 13 \, x - 56}}\;dx} = \answer{{\frac{2}{3} \, \log\left(3 \, x - 8\right) - 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x - 49}{3 \, {\left(2 \, x^{2} - 19 \, x + 24\right)}}}\;dx} = \answer{{\frac{5}{6} \, \log\left(2 \, x - 3\right) + \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{9 \, x + 46}{2 \, {\left(x^{2} - 2 \, x - 24\right)}}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(x + 4\right) + 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{2 \, {\left(5 \, x + 11\right)}}{4 \, x^{2} + 35 \, x + 24}}\;dx} = \answer{{\frac{1}{2} \, \log\left(4 \, x + 3\right) + 2 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{11 \, x - 28}{2 \, {\left(2 \, x^{2} - 17 \, x + 8\right)}}}\;dx} = \answer{{\frac{3}{4} \, \log\left(2 \, x - 1\right) + 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{23 \, x - 2}{4 \, x^{2} + 33 \, x + 8}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(4 \, x + 1\right) + 6 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x - 11\right)}}{x^{2} - 7 \, x + 12}}\;dx} = \answer{{6 \, \log\left(x - 3\right) + 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{18 \, {\left(x - 1\right)}}{8 \, x^{2} - 65 \, x + 8}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(8 \, x - 1\right) - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{17 \, x + 73}{2 \, {\left(x^{2} + 13 \, x + 36\right)}}}\;dx} = \answer{{-8 \, \log\left(x + 9\right) - \frac{1}{2} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, x + 103}{x^{2} - x - 56}}\;dx} = \answer{{-5 \, \log\left(x + 7\right) + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{3 \, {\left(7 \, x + 8\right)}}{3 \, x^{2} + 14 \, x + 8}}\;dx} = \answer{{\log\left(3 \, x + 2\right) + 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{2 \, {\left(17 \, x - 1\right)}}{9 \, x^{2} + 47 \, x + 10}}\;dx} = \answer{{\frac{2}{9} \, \log\left(9 \, x + 2\right) - 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{-\frac{x + 44}{2 \, {\left(3 \, x^{2} - 22 \, x - 16\right)}}}\;dx} = \answer{{\frac{5}{6} \, \log\left(3 \, x + 2\right) - \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0052. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0052.HELP.tex}

\[
\int{{\frac{4 \, x + 59}{x^{2} + 5 \, x - 6}}\;dx} = \answer{{-5 \, \log\left(x + 6\right) + 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

