\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@TrigInt, Func@Trig, File@0028}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-8 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{18} \, \sin\left(6 \, x\right)^{3} - \frac{5}{2} \, x - \frac{1}{8} \, \sin\left(12 \, x\right) + \frac{2}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{8 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{18} \, \sin\left(6 \, x\right)^{3} + \frac{5}{2} \, x + \frac{1}{8} \, \sin\left(12 \, x\right) + \frac{2}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-6 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-3 \, x + \frac{3}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-6 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{9}{4} \, x - \frac{3}{32} \, \sin\left(8 \, x\right) + \frac{3}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{2 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{3}{4} \, x + \frac{1}{32} \, \sin\left(8 \, x\right) + \frac{1}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-3 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{9}{8} \, x - \frac{3}{64} \, \sin\left(8 \, x\right) + \frac{3}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{2 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{3}{4} \, x + \frac{1}{32} \, \sin\left(8 \, x\right) - \frac{1}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{7 \, \sin\left(x\right)^{2}}\;dx} = \answer{{\frac{7}{2} \, x - \frac{7}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{\cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{144} \, \sin\left(6 \, x\right)^{3} + \frac{5}{16} \, x + \frac{1}{64} \, \sin\left(12 \, x\right) + \frac{1}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-2 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{72} \, \sin\left(6 \, x\right)^{3} - \frac{5}{8} \, x - \frac{1}{32} \, \sin\left(12 \, x\right) + \frac{1}{6} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{9 \, \sin\left(x\right)^{2}}\;dx} = \answer{{\frac{9}{2} \, x - \frac{9}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{2 \, \cos\left(x\right)^{2}}\;dx} = \answer{{x + \frac{1}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-3 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{9}{8} \, x - \frac{3}{64} \, \sin\left(8 \, x\right) - \frac{3}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{5 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{15}{8} \, x + \frac{5}{64} \, \sin\left(8 \, x\right) + \frac{5}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-6 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{24} \, \sin\left(6 \, x\right)^{3} - \frac{15}{8} \, x - \frac{3}{32} \, \sin\left(12 \, x\right) + \frac{1}{2} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{5 \, \sin\left(x\right)^{2}}\;dx} = \answer{{\frac{5}{2} \, x - \frac{5}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{7 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{7}{144} \, \sin\left(6 \, x\right)^{3} + \frac{35}{16} \, x + \frac{7}{64} \, \sin\left(12 \, x\right) + \frac{7}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{\sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{144} \, \sin\left(6 \, x\right)^{3} + \frac{5}{16} \, x + \frac{1}{64} \, \sin\left(12 \, x\right) - \frac{1}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{\cos\left(x\right)^{2}}\;dx} = \answer{{\frac{1}{2} \, x + \frac{1}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{6 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{9}{4} \, x + \frac{3}{32} \, \sin\left(8 \, x\right) + \frac{3}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-2 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-x - \frac{1}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{9 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{27}{8} \, x + \frac{9}{64} \, \sin\left(8 \, x\right) - \frac{9}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-5 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-\frac{5}{2} \, x + \frac{5}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-7 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{21}{8} \, x - \frac{7}{64} \, \sin\left(8 \, x\right) + \frac{7}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-8 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-4 \, x + 2 \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-5 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{15}{8} \, x - \frac{5}{64} \, \sin\left(8 \, x\right) - \frac{5}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{2 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{72} \, \sin\left(6 \, x\right)^{3} + \frac{5}{8} \, x + \frac{1}{32} \, \sin\left(12 \, x\right) - \frac{1}{6} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{\sin\left(x\right)^{2}}\;dx} = \answer{{\frac{1}{2} \, x - \frac{1}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-4 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{3}{2} \, x - \frac{1}{16} \, \sin\left(8 \, x\right) + \frac{1}{2} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-8 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-3 \, x - \frac{1}{8} \, \sin\left(8 \, x\right) - \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-\cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{144} \, \sin\left(6 \, x\right)^{3} - \frac{5}{16} \, x - \frac{1}{64} \, \sin\left(12 \, x\right) - \frac{1}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{4 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{36} \, \sin\left(6 \, x\right)^{3} + \frac{5}{4} \, x + \frac{1}{16} \, \sin\left(12 \, x\right) + \frac{1}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-\sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{3}{8} \, x - \frac{1}{64} \, \sin\left(8 \, x\right) + \frac{1}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-2 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{3}{4} \, x - \frac{1}{32} \, \sin\left(8 \, x\right) + \frac{1}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-9 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-\frac{9}{2} \, x + \frac{9}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{7 \, \cos\left(x\right)^{2}}\;dx} = \answer{{\frac{7}{2} \, x + \frac{7}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-6 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{9}{4} \, x - \frac{3}{32} \, \sin\left(8 \, x\right) - \frac{3}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{3 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{48} \, \sin\left(6 \, x\right)^{3} + \frac{15}{16} \, x + \frac{3}{64} \, \sin\left(12 \, x\right) + \frac{1}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-7 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{21}{8} \, x - \frac{7}{64} \, \sin\left(8 \, x\right) - \frac{7}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{\sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{3}{8} \, x + \frac{1}{64} \, \sin\left(8 \, x\right) - \frac{1}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-6 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{24} \, \sin\left(6 \, x\right)^{3} - \frac{15}{8} \, x - \frac{3}{32} \, \sin\left(12 \, x\right) - \frac{1}{2} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-\sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{144} \, \sin\left(6 \, x\right)^{3} - \frac{5}{16} \, x - \frac{1}{64} \, \sin\left(12 \, x\right) + \frac{1}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-4 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{3}{2} \, x - \frac{1}{16} \, \sin\left(8 \, x\right) - \frac{1}{2} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{8 \, \cos\left(x\right)^{2}}\;dx} = \answer{{4 \, x + 2 \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-8 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-4 \, x - 2 \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{6 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{24} \, \sin\left(6 \, x\right)^{3} + \frac{15}{8} \, x + \frac{3}{32} \, \sin\left(12 \, x\right) - \frac{1}{2} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{6 \, \sin\left(x\right)^{2}}\;dx} = \answer{{3 \, x - \frac{3}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-9 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{16} \, \sin\left(6 \, x\right)^{3} - \frac{45}{16} \, x - \frac{9}{64} \, \sin\left(12 \, x\right) - \frac{3}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{4 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{36} \, \sin\left(6 \, x\right)^{3} + \frac{5}{4} \, x + \frac{1}{16} \, \sin\left(12 \, x\right) - \frac{1}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{2 \, \sin\left(x\right)^{2}}\;dx} = \answer{{x - \frac{1}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-\cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{3}{8} \, x - \frac{1}{64} \, \sin\left(8 \, x\right) - \frac{1}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-9 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-\frac{9}{2} \, x - \frac{9}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{2 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{72} \, \sin\left(6 \, x\right)^{3} + \frac{5}{8} \, x + \frac{1}{32} \, \sin\left(12 \, x\right) + \frac{1}{6} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-4 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-2 \, x - \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{9 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{16} \, \sin\left(6 \, x\right)^{3} + \frac{45}{16} \, x + \frac{9}{64} \, \sin\left(12 \, x\right) - \frac{3}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{7 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{21}{8} \, x + \frac{7}{64} \, \sin\left(8 \, x\right) - \frac{7}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-3 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{48} \, \sin\left(6 \, x\right)^{3} - \frac{15}{16} \, x - \frac{3}{64} \, \sin\left(12 \, x\right) - \frac{1}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{8 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{3 \, x + \frac{1}{8} \, \sin\left(8 \, x\right) - \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-4 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-2 \, x + \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{7 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{21}{8} \, x + \frac{7}{64} \, \sin\left(8 \, x\right) + \frac{7}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{4 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{3}{2} \, x + \frac{1}{16} \, \sin\left(8 \, x\right) + \frac{1}{2} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-4 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{36} \, \sin\left(6 \, x\right)^{3} - \frac{5}{4} \, x - \frac{1}{16} \, \sin\left(12 \, x\right) - \frac{1}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{6 \, \cos\left(x\right)^{2}}\;dx} = \answer{{3 \, x + \frac{3}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-9 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{27}{8} \, x - \frac{9}{64} \, \sin\left(8 \, x\right) - \frac{9}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-8 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{18} \, \sin\left(6 \, x\right)^{3} - \frac{5}{2} \, x - \frac{1}{8} \, \sin\left(12 \, x\right) - \frac{2}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{5 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{15}{8} \, x + \frac{5}{64} \, \sin\left(8 \, x\right) - \frac{5}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{4 \, \cos\left(x\right)^{2}}\;dx} = \answer{{2 \, x + \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{4 \, \sin\left(x\right)^{2}}\;dx} = \answer{{2 \, x - \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{6 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{24} \, \sin\left(6 \, x\right)^{3} + \frac{15}{8} \, x + \frac{3}{32} \, \sin\left(12 \, x\right) + \frac{1}{2} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{5 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{5}{144} \, \sin\left(6 \, x\right)^{3} + \frac{25}{16} \, x + \frac{5}{64} \, \sin\left(12 \, x\right) - \frac{5}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{9 \, \cos\left(x\right)^{2}}\;dx} = \answer{{\frac{9}{2} \, x + \frac{9}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-9 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{16} \, \sin\left(6 \, x\right)^{3} - \frac{45}{16} \, x - \frac{9}{64} \, \sin\left(12 \, x\right) + \frac{3}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-\cos\left(x\right)^{2}}\;dx} = \answer{{-\frac{1}{2} \, x - \frac{1}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-7 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-\frac{7}{2} \, x - \frac{7}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-5 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{15}{8} \, x - \frac{5}{64} \, \sin\left(8 \, x\right) + \frac{5}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{3 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{9}{8} \, x + \frac{3}{64} \, \sin\left(8 \, x\right) + \frac{3}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-2 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-x + \frac{1}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{8 \, \sin\left(x\right)^{2}}\;dx} = \answer{{4 \, x - 2 \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{4 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{3}{2} \, x + \frac{1}{16} \, \sin\left(8 \, x\right) - \frac{1}{2} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{7 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{7}{144} \, \sin\left(6 \, x\right)^{3} + \frac{35}{16} \, x + \frac{7}{64} \, \sin\left(12 \, x\right) - \frac{7}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-6 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-3 \, x - \frac{3}{2} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{3 \, \cos\left(x\right)^{2}}\;dx} = \answer{{\frac{3}{2} \, x + \frac{3}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-3 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{48} \, \sin\left(6 \, x\right)^{3} - \frac{15}{16} \, x - \frac{3}{64} \, \sin\left(12 \, x\right) + \frac{1}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-7 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-\frac{7}{2} \, x + \frac{7}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{5 \, \cos\left(x\right)^{2}}\;dx} = \answer{{\frac{5}{2} \, x + \frac{5}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-\sin\left(x\right)^{2}}\;dx} = \answer{{-\frac{1}{2} \, x + \frac{1}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-3 \, \sin\left(x\right)^{2}}\;dx} = \answer{{-\frac{3}{2} \, x + \frac{3}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-3 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-\frac{3}{2} \, x - \frac{3}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-5 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{5}{144} \, \sin\left(6 \, x\right)^{3} - \frac{25}{16} \, x - \frac{5}{64} \, \sin\left(12 \, x\right) + \frac{5}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-4 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{36} \, \sin\left(6 \, x\right)^{3} - \frac{5}{4} \, x - \frac{1}{16} \, \sin\left(12 \, x\right) + \frac{1}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-7 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{7}{144} \, \sin\left(6 \, x\right)^{3} - \frac{35}{16} \, x - \frac{7}{64} \, \sin\left(12 \, x\right) - \frac{7}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-9 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{27}{8} \, x - \frac{9}{64} \, \sin\left(8 \, x\right) + \frac{9}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-2 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{-\frac{3}{4} \, x - \frac{1}{32} \, \sin\left(8 \, x\right) - \frac{1}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{9 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{27}{8} \, x + \frac{9}{64} \, \sin\left(8 \, x\right) + \frac{9}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-2 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{72} \, \sin\left(6 \, x\right)^{3} - \frac{5}{8} \, x - \frac{1}{32} \, \sin\left(12 \, x\right) - \frac{1}{6} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{\cos\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{3}{8} \, x + \frac{1}{64} \, \sin\left(8 \, x\right) + \frac{1}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{3 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{9}{8} \, x + \frac{3}{64} \, \sin\left(8 \, x\right) - \frac{3}{8} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-8 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{-3 \, x - \frac{1}{8} \, \sin\left(8 \, x\right) + \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{9 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{1}{16} \, \sin\left(6 \, x\right)^{3} + \frac{45}{16} \, x + \frac{9}{64} \, \sin\left(12 \, x\right) + \frac{3}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{8 \, \cos\left(2 \, x\right)^{4}}\;dx} = \answer{{3 \, x + \frac{1}{8} \, \sin\left(8 \, x\right) + \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{6 \, \sin\left(2 \, x\right)^{4}}\;dx} = \answer{{\frac{9}{4} \, x + \frac{3}{32} \, \sin\left(8 \, x\right) - \frac{3}{4} \, \sin\left(4 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right)^{2}}\;dx} = \answer{{-\frac{5}{2} \, x - \frac{5}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{3 \, \sin\left(x\right)^{2}}\;dx} = \answer{{\frac{3}{2} \, x - \frac{3}{4} \, \sin\left(2 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{5 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{5}{144} \, \sin\left(6 \, x\right)^{3} + \frac{25}{16} \, x + \frac{5}{64} \, \sin\left(12 \, x\right) + \frac{5}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{8 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{18} \, \sin\left(6 \, x\right)^{3} + \frac{5}{2} \, x + \frac{1}{8} \, \sin\left(12 \, x\right) - \frac{2}{3} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-5 \, \cos\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{5}{144} \, \sin\left(6 \, x\right)^{3} - \frac{25}{16} \, x - \frac{5}{64} \, \sin\left(12 \, x\right) - \frac{5}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{3 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{\frac{1}{48} \, \sin\left(6 \, x\right)^{3} + \frac{15}{16} \, x + \frac{3}{64} \, \sin\left(12 \, x\right) - \frac{1}{4} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0028. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0028.HELP.tex}

\[
\int{{-7 \, \sin\left(3 \, x\right)^{6}}\;dx} = \answer{{-\frac{7}{144} \, \sin\left(6 \, x\right)^{3} - \frac{35}{16} \, x - \frac{7}{64} \, \sin\left(12 \, x\right) + \frac{7}{12} \, \sin\left(6 \, x\right)}+C}
\]
\end{problem}}%}

