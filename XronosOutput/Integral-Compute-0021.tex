\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Func@Poly, Func@Exp, Sub@ByParts, File@0021}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{x}}\;dx} = \answer{{{\left(x^{5} - 5 \, x^{4} + 20 \, x^{3} - 60 \, x^{2} + 120 \, x - 120\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{x}}\;dx} = \answer{{{\left(x^{4} - 4 \, x^{3} + 12 \, x^{2} - 24 \, x + 24\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-x\right)}}\;dx} = \answer{{-{\left(x^{4} + 4 \, x^{3} + 12 \, x^{2} + 24 \, x + 24\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{1}{256} \, {\left(32 \, x^{2} - 8 \, x + 1\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-x\right)}}\;dx} = \answer{{-{\left(x^{3} + 3 \, x^{2} + 6 \, x + 6\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{1}{128} \, {\left(32 \, x^{3} + 24 \, x^{2} + 12 \, x + 3\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{1}{8} \, {\left(4 \, x^{6} + 12 \, x^{5} + 30 \, x^{4} + 60 \, x^{3} + 90 \, x^{2} + 90 \, x + 45\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{324} \, {\left(54 \, x^{4} - 36 \, x^{3} + 18 \, x^{2} - 6 \, x + 1\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{1}{27} \, {\left(9 \, x^{2} + 6 \, x + 2\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{1}{1024} \, {\left(256 \, x^{6} - 384 \, x^{5} + 480 \, x^{4} - 480 \, x^{3} + 360 \, x^{2} - 180 \, x + 45\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{177147} \, {\left(19683 \, x^{5} - 10935 \, x^{4} + 4860 \, x^{3} - 1620 \, x^{2} + 360 \, x - 40\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{1}{243} \, {\left(81 \, x^{6} + 162 \, x^{5} + 270 \, x^{4} + 360 \, x^{3} + 360 \, x^{2} + 240 \, x + 80\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{1}{512} \, {\left(128 \, x^{5} + 160 \, x^{4} + 160 \, x^{3} + 120 \, x^{2} + 60 \, x + 15\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{1}{27} \, {\left(9 \, x^{3} + 9 \, x^{2} + 6 \, x + 2\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{1}{8} \, {\left(4 \, x^{3} + 6 \, x^{2} + 6 \, x + 3\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{1}{823543} \, {\left(117649 \, x^{6} + 100842 \, x^{5} + 72030 \, x^{4} + 41160 \, x^{3} + 17640 \, x^{2} + 5040 \, x + 720\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{177147} \, {\left(19683 \, x^{5} + 10935 \, x^{4} + 4860 \, x^{3} + 1620 \, x^{2} + 360 \, x + 40\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{1}{625} \, {\left(125 \, x^{3} + 75 \, x^{2} + 30 \, x + 6\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{108} \, {\left(18 \, x^{2} - 6 \, x + 1\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{1}{32} \, {\left(8 \, x^{2} - 4 \, x + 1\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{1}{27} \, {\left(9 \, x^{2} - 6 \, x + 2\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{1}{8} \, {\left(4 \, x^{5} + 10 \, x^{4} + 20 \, x^{3} + 30 \, x^{2} + 30 \, x + 15\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{531441} \, {\left(59049 \, x^{6} - 39366 \, x^{5} + 21870 \, x^{4} - 9720 \, x^{3} + 3240 \, x^{2} - 720 \, x + 80\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{1}{131072} \, {\left(16384 \, x^{6} + 12288 \, x^{5} + 7680 \, x^{4} + 3840 \, x^{3} + 1440 \, x^{2} + 360 \, x + 45\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{1}{131072} \, {\left(16384 \, x^{6} - 12288 \, x^{5} + 7680 \, x^{4} - 3840 \, x^{3} + 1440 \, x^{2} - 360 \, x + 45\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{1}{2048} \, {\left(256 \, x^{3} - 96 \, x^{2} + 24 \, x - 3\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{x}}\;dx} = \answer{{{\left(x^{3} - 3 \, x^{2} + 6 \, x - 6\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{1}{81} \, {\left(27 \, x^{4} - 36 \, x^{3} + 36 \, x^{2} - 24 \, x + 8\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-x\right)}}\;dx} = \answer{{-{\left(x^{2} + 2 \, x + 2\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{1}{3125} \, {\left(625 \, x^{5} - 625 \, x^{4} + 500 \, x^{3} - 300 \, x^{2} + 120 \, x - 24\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{1944} \, {\left(324 \, x^{6} - 324 \, x^{5} + 270 \, x^{4} - 180 \, x^{3} + 90 \, x^{2} - 30 \, x + 5\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{1}{343} \, {\left(49 \, x^{2} - 14 \, x + 2\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{1}{32768} \, {\left(4096 \, x^{5} + 2560 \, x^{4} + 1280 \, x^{3} + 480 \, x^{2} + 120 \, x + 15\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{1}{1024} \, {\left(256 \, x^{6} + 384 \, x^{5} + 480 \, x^{4} + 480 \, x^{3} + 360 \, x^{2} + 180 \, x + 45\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{1}{216} \, {\left(36 \, x^{3} + 18 \, x^{2} + 6 \, x + 1\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{1}{125} \, {\left(25 \, x^{2} + 10 \, x + 2\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{19683} \, {\left(2187 \, x^{4} + 972 \, x^{3} + 324 \, x^{2} + 72 \, x + 8\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{1}{2401} \, {\left(343 \, x^{3} + 147 \, x^{2} + 42 \, x + 6\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{1}{2048} \, {\left(256 \, x^{3} + 96 \, x^{2} + 24 \, x + 3\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{1}{243} \, {\left(81 \, x^{5} + 135 \, x^{4} + 180 \, x^{3} + 180 \, x^{2} + 120 \, x + 40\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{1}{128} \, {\left(32 \, x^{4} - 32 \, x^{3} + 24 \, x^{2} - 12 \, x + 3\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{531441} \, {\left(59049 \, x^{6} + 39366 \, x^{5} + 21870 \, x^{4} + 9720 \, x^{3} + 3240 \, x^{2} + 720 \, x + 80\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{1}{4} \, {\left(2 \, x^{2} - 2 \, x + 1\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{1}{4096} \, {\left(512 \, x^{4} + 256 \, x^{3} + 96 \, x^{2} + 24 \, x + 3\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{1}{8} \, {\left(4 \, x^{5} - 10 \, x^{4} + 20 \, x^{3} - 30 \, x^{2} + 30 \, x - 15\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{1}{16807} \, {\left(2401 \, x^{4} - 1372 \, x^{3} + 588 \, x^{2} - 168 \, x + 24\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{1}{3125} \, {\left(625 \, x^{5} + 625 \, x^{4} + 500 \, x^{3} + 300 \, x^{2} + 120 \, x + 24\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{1}{32768} \, {\left(4096 \, x^{5} - 2560 \, x^{4} + 1280 \, x^{3} - 480 \, x^{2} + 120 \, x - 15\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{1944} \, {\left(324 \, x^{5} - 270 \, x^{4} + 180 \, x^{3} - 90 \, x^{2} + 30 \, x - 5\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{1}{343} \, {\left(49 \, x^{2} + 14 \, x + 2\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{1}{117649} \, {\left(16807 \, x^{5} + 12005 \, x^{4} + 6860 \, x^{3} + 2940 \, x^{2} + 840 \, x + 120\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{1}{512} \, {\left(128 \, x^{5} - 160 \, x^{4} + 160 \, x^{3} - 120 \, x^{2} + 60 \, x - 15\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{1}{15625} \, {\left(3125 \, x^{6} - 3750 \, x^{5} + 3750 \, x^{4} - 3000 \, x^{3} + 1800 \, x^{2} - 720 \, x + 144\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{729} \, {\left(81 \, x^{2} + 18 \, x + 2\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{1}{16807} \, {\left(2401 \, x^{4} + 1372 \, x^{3} + 588 \, x^{2} + 168 \, x + 24\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{1}{243} \, {\left(81 \, x^{6} - 162 \, x^{5} + 270 \, x^{4} - 360 \, x^{3} + 360 \, x^{2} - 240 \, x + 80\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{1}{2401} \, {\left(343 \, x^{3} - 147 \, x^{2} + 42 \, x - 6\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{2187} \, {\left(243 \, x^{3} - 81 \, x^{2} + 18 \, x - 2\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{19683} \, {\left(2187 \, x^{4} - 972 \, x^{3} + 324 \, x^{2} - 72 \, x + 8\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{1}{823543} \, {\left(117649 \, x^{6} - 100842 \, x^{5} + 72030 \, x^{4} - 41160 \, x^{3} + 17640 \, x^{2} - 5040 \, x + 720\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{1}{4096} \, {\left(512 \, x^{4} - 256 \, x^{3} + 96 \, x^{2} - 24 \, x + 3\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{1}{324} \, {\left(54 \, x^{4} + 36 \, x^{3} + 18 \, x^{2} + 6 \, x + 1\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{1}{27} \, {\left(9 \, x^{3} - 9 \, x^{2} + 6 \, x - 2\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{729} \, {\left(81 \, x^{2} - 18 \, x + 2\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{1}{8} \, {\left(4 \, x^{3} - 6 \, x^{2} + 6 \, x - 3\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{1}{4} \, {\left(2 \, x^{2} + 2 \, x + 1\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{1}{256} \, {\left(32 \, x^{2} + 8 \, x + 1\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{1}{125} \, {\left(25 \, x^{2} - 10 \, x + 2\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{1}{3125} \, {\left(625 \, x^{4} - 500 \, x^{3} + 300 \, x^{2} - 120 \, x + 24\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{1}{108} \, {\left(18 \, x^{2} + 6 \, x + 1\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-x\right)}}\;dx} = \answer{{-{\left(x^{5} + 5 \, x^{4} + 20 \, x^{3} + 60 \, x^{2} + 120 \, x + 120\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{1}{15625} \, {\left(3125 \, x^{6} + 3750 \, x^{5} + 3750 \, x^{4} + 3000 \, x^{3} + 1800 \, x^{2} + 720 \, x + 144\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{1}{625} \, {\left(125 \, x^{3} - 75 \, x^{2} + 30 \, x - 6\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{x}}\;dx} = \answer{{{\left(x^{6} - 6 \, x^{5} + 30 \, x^{4} - 120 \, x^{3} + 360 \, x^{2} - 720 \, x + 720\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{1}{4} \, {\left(2 \, x^{4} - 4 \, x^{3} + 6 \, x^{2} - 6 \, x + 3\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-x\right)}}\;dx} = \answer{{-{\left(x^{6} + 6 \, x^{5} + 30 \, x^{4} + 120 \, x^{3} + 360 \, x^{2} + 720 \, x + 720\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{1}{128} \, {\left(32 \, x^{3} - 24 \, x^{2} + 12 \, x - 3\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{1}{32} \, {\left(8 \, x^{2} + 4 \, x + 1\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{2187} \, {\left(243 \, x^{3} + 81 \, x^{2} + 18 \, x + 2\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{2}}\cdot{e^{x}}\;dx} = \answer{{{\left(x^{2} - 2 \, x + 2\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{1}{4} \, {\left(2 \, x^{4} + 4 \, x^{3} + 6 \, x^{2} + 6 \, x + 3\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{1}{3125} \, {\left(625 \, x^{4} + 500 \, x^{3} + 300 \, x^{2} + 120 \, x + 24\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{1}{1944} \, {\left(324 \, x^{6} + 324 \, x^{5} + 270 \, x^{4} + 180 \, x^{3} + 90 \, x^{2} + 30 \, x + 5\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{1}{81} \, {\left(27 \, x^{4} + 36 \, x^{3} + 36 \, x^{2} + 24 \, x + 8\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{1}{117649} \, {\left(16807 \, x^{5} - 12005 \, x^{4} + 6860 \, x^{3} - 2940 \, x^{2} + 840 \, x - 120\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{6}}\cdot{e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{1}{8} \, {\left(4 \, x^{6} - 12 \, x^{5} + 30 \, x^{4} - 60 \, x^{3} + 90 \, x^{2} - 90 \, x + 45\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{1}{1944} \, {\left(324 \, x^{5} + 270 \, x^{4} + 180 \, x^{3} + 90 \, x^{2} + 30 \, x + 5\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{3}}\cdot{e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{216} \, {\left(36 \, x^{3} - 18 \, x^{2} + 6 \, x - 1\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{5}}\cdot{e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{1}{243} \, {\left(81 \, x^{5} - 135 \, x^{4} + 180 \, x^{3} - 180 \, x^{2} + 120 \, x - 40\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0021. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0021.HELP.tex}

\[
\int{{x^{4}}\cdot{e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{1}{128} \, {\left(32 \, x^{4} + 32 \, x^{3} + 24 \, x^{2} + 12 \, x + 3\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

