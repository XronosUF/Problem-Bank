\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%\tagged{Ans@MultiAns, Type@Concept, Topic@Derivative, Sub@Theorems-EVT, File@0003}{
\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{7}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-5},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{3}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-13},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{x - 5}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{5},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{10}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{4},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{2}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-18},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{10}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-10},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{4}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-27},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{4}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-8},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{9}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-9},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{3}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-15},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{5}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-9},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{6}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-17},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{3}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{9},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{4}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-7},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{2}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-17},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{\frac{1}{{\left(x + 2\right)}^{5}}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-18},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{10}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-12},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{x - 6}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-11},{27}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{5}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-12},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{4}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-18},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{7}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-14},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{6}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-19},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{4}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{5},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{6}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-3},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{6}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-18},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{8}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-17},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{5}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-4},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{6}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-1},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{2}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-8},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{9}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-12},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{3}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-22},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{2}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-7},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{5}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-21},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{\frac{1}{{\left(x + 2\right)}^{7}}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-7},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-10},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{8}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-23},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{3}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-14},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{10}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-27},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{5}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{5},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{4}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-12},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{6}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-6},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{3}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-17},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{7}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-11},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{3}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-6},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{2}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-6},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{9}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{0},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{2}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-14},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{2}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-22},{1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{5}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{2},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{7}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{4},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{7}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-14},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{10}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-1},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{10}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-11},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{5}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-6},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{5}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-9},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{x + 8}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-8},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{4}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-18},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{9}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-4},{21}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{\frac{1}{{\left(x + 5\right)}^{10}}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-13},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{2}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-16},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{2}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{0},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{8}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-20},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{9}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-5},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{8}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-16},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{5}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-13},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{10}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-6},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{5}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-17},{-6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{2}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-27},{-8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{8}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-10},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{2}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-25},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{{\left(x + 11\right)}^{2}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-28},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{9}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-14},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{8}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{2},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{6}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-6},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{9}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-22},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{x - 7}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-4},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{2}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-10},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-14},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{6}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-14},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{x - 7}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-12},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{8}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-29},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{5}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{4},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{10}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-16},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{3}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-17},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{4}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-2},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{4}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-11},{1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{4}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{1},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{4}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-18},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{x + 2}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-21},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{10}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-7},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{6}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-15},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{6}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-7},{26}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{10}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-11},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{4}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-15},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{4}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-18},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{6}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-14},{-6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{4}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-14},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{4}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{0},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{5}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-12},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{3}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-4},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{4}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-22},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{4}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-5},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{4}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-7},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{6}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-27},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{{\left(x + 11\right)}^{2}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-21},{-8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{8}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-10},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{8}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{6},{27}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{{\left(x + 11\right)}^{4}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-26},{-6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{5}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-20},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{x - 7}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{0},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{10}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-2},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{10}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-3},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{7}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-15},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{9}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-10},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{x - 1}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-7},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{\frac{1}{{\left(x + 5\right)}^{4}}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-6},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{6}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-15},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{4}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-17},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{4}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-12},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{2}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-9},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{6}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-14},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{10}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{1},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{10}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-4},{21}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{4}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-26},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{x - 3}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-7},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{7}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{5},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{6}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-23},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{\frac{1}{x + 7}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-11},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{4}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{1},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{6}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-8},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{7}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{4},{30}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{10}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-8},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{8}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-2},{26}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{6}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-15},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{4}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{1},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{{\left(x + 11\right)}^{2}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-26},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{5}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-6},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{9}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-29},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{3}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-18},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{9}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-19},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{9}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-8},{26}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{5}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{0},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{x + 10}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-22},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{x - 2}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-9},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{3}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-3},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{x + 4}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-17},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{4}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{0},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{2}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-4},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{8}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-9},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{7}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-13},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{10}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-1},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{3}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-21},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{3}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-25},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{6}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-9},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{3}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-11},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{2}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-19},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{x - 2}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{0},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{4}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{2},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{3}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-15},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{5}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{4},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{8}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-16},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{\frac{1}{{\left(x + 7\right)}^{6}}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-7},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{7}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-18},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{2}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-3},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{9}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-4},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{3}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-12},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{9}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-17},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{x + 11}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-18},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{7}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-16},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{10}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-5},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{x - 7}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{0},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{\frac{1}{{\left(x + 5\right)}^{7}}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-20},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{4}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-4},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{8}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-23},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{2}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-3},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{9}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{1},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{7}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-9},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{3}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{2},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{6}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-10},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{3}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{4},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{2}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-17},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{3}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-22},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{2}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-12},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{x + 3}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-12},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{3}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-4},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{6}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{0},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{x - 4}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{2},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{4}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-6},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{2}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-7},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{2}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-6},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{4}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{0},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{3}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-6},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{6}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-15},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{x - 4}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{4},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{2}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{5},{27}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{10}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-20},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{7}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{1},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{7}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-15},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{4}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-14},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{3}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-4},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{7}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-16},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{3}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-14},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{6}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-15},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{6}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-9},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{x + 6}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-17},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{3}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-11},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{2}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-10},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{10}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-9},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{4}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-6},{28}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{10}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-8},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{3}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-6},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{\frac{1}{{\left(x + 5\right)}^{2}}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-16},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{{\left(x + 11\right)}^{3}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-19},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{5}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-13},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{9}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{2},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{3}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{7},{30}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{9}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-18},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{7}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-10},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{x + 3}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-20},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{3}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-9},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{3}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{3},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{2}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-4},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{4}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-10},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{9}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-7},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{9}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-19},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{6}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-10},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{\frac{1}{{\left(x + 5\right)}^{3}}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-19},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{6}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-6},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{\frac{1}{{\left(x + 7\right)}^{10}}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-18},{1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{5}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-27},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{2}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-5},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{x + 6}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-6},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{\frac{1}{{\left(x + 7\right)}^{9}}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-18},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{7}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-3},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{7}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-16},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{6}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-5},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{5}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-6},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{7}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-6},{28}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{\frac{1}{{\left(x + 7\right)}^{5}}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-17},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{\frac{1}{x + 2}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-5},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{6}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-23},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{9}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-4},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{8}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{0},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{9}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-8},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{5}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-25},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{4}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-22},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{8}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-12},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{2}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-27},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{4}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-18},{-7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{10}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-12},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{3}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-9},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{7}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-6},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{2}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-2},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{10}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{0},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{8}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-20},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{2}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-3},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{10}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-22},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{8}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-25},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{7}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{5},{28}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{\frac{1}{{\left(x + 2\right)}^{7}}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-2},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{7}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-14},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{8}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{6},{25}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{5}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-23},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{8}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-13},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{2}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-16},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{8}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{3},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{4}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-16},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{x + 4}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-23},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{6}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-16},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{x - 4}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{3},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{2}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{8},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{9}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-8},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{2}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-20},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{8}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-16},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{8}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-12},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{9}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-4},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{8}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-23},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{10}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-9},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{8}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-10},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{5}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-5},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{\frac{1}{{\left(x + 7\right)}^{2}}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-15},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{3}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-10},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{7}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-12},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{10}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-10},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{7}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-21},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{4}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-13},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{x + 8}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-10},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{x + 4}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-4},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{9}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-3},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{5}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-14},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{6}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-25},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{5}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-14},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{8}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-15},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{9}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-17},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{7}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{2},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{8}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-3},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{3}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-19},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{8}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-9},{25}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{8}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-12},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{x + 10}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-20},{-8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{10}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-3},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{\frac{1}{{\left(x + 2\right)}^{7}}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-8},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{10}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-18},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{7}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-6},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{\frac{1}{{\left(x + 5\right)}^{4}}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-12},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{5}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-17},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{2}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-10},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{7}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{8},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{10}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-7},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{x - 9}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{2},{29}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{7}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-2},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{7}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-7},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{7}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-16},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{5}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-4},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{2}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-3},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{5}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-15},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{4}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{0},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{x + 9}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-12},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{3}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-15},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{5}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-18},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{6}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-4},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{8}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-15},{21}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{x + 6}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-16},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{5}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-12},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{2}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-4},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{2}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-18},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{4}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{3},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{\frac{1}{{\left(x + 2\right)}^{7}}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-14},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{5}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{5},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{10}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-20},{1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{6}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{2},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{4}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-22},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{9}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-26},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{{\left(x + 11\right)}^{6}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-20},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{5}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-6},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{3}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-2},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{3}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-11},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{2}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-9},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{8}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{2},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{5}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-13},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{8}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{2},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{9}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-9},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{10}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-1},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{8}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-11},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{x - 5}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{1},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{x - 6}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{3},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{8}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-16},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{x - 3}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-4},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{10}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-18},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{7}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-3},{26}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{x - 3}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-13},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{10}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-14},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{9}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-13},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{8}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-14},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{9}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-6},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{4}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-3},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{4}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{4},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{3}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-5},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{5}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-16},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{10}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-11},{1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{8}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-21},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{8}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-27},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{7}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-21},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{8}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-19},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{8}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-20},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{7}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-25},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{10}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-14},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{5}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-13},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{6}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-29},{-8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{10}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-19},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{4}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-15},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{9}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-17},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{6}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-4},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{9}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-3},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{x - 7}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-8},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{6}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{1},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{4}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-12},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{10}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-9},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{x + 4}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-23},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{2}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-15},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{8}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-6},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{{\left(x - 3\right)}^{10}}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-11},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{8}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{5},{21}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{\frac{1}{{\left(x + 7\right)}^{3}}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-12},{-4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{x - 8}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{7},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{\frac{1}{x - 3}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-15},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{5}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{4},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{x + 8}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-27},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{{\left(x - 9\right)}^{10}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-4},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{7}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-9},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{10}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-4},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{6}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-16},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{x + 7}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-26},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{8}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{5},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{8}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-12},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{6}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-30},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{8}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-15},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{8}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-12},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{6}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-19},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{5}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-17},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{10}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{3},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{10}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-14},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{4}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{7},{26}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{7}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-17},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{\frac{1}{{\left(x - 5\right)}^{10}}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-11},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{9}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{2},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{8}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-9},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{7}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-6},{1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{6}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-9},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{7}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-12},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{6}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-4},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{7}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-3},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{x + 9}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-9},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{x + 9}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-27},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{3}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-14},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{3}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-10},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{10}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-14},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{{\left(x + 3\right)}^{2}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-19},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{\frac{1}{{\left(x - 7\right)}^{8}}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-8},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{8}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-10},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{6}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-23},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{2}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-19},{1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{9}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-4},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{x + 3}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-17},{17}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{x - 9}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{7},{29}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{10}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{1},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{\frac{1}{x^{7}}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{0},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{7}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-21},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{3}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-11},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{7}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-15},{21}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{7}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{7},{29}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{x + 4}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-18},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{10}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-12},{28}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{9}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-13},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{9}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-6},{21}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{3}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{-8},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{6}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{6},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{10}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-8},{24}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{7}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-15},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{8}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-27},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{x + 10}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-20},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{2}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-1},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{5}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-19},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{{\left(x + 11\right)}^{10}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-20},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{6}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-9},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{2}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{1},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{5}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-8},{12}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{6}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-20},{3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{\frac{1}{{\left(x - 8\right)}^{5}}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-2},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{{\left(x + 4\right)}^{8}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-6},{15}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{10}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-7},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 9\right)}\; , & x<{-10}\\[3pt]
{\frac{1}{{\left(x + 11\right)}^{7}}}\; , & x\geq{-10}
\end{array}\right.\]
 over the interval $[{-20},{10}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{2}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-16},{21}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{x + 9}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-26},{-3}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{10}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-13},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{2}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-9},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 11\right)}\; , & x<{10}\\[3pt]
{\frac{1}{{\left(x - 9\right)}^{5}}}\; , & x\geq{10}
\end{array}\right.\]
 over the interval $[{8},{29}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{\frac{1}{{\left(x + 8\right)}^{8}}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-12},{8}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{4}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-3},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 3\right)}\; , & x<{-4}\\[3pt]
{{\left(x + 5\right)}^{7}}\; , & x\geq{-4}
\end{array}\right.\]
 over the interval $[{-6},{9}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{9}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-7},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{2}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{-11},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{5}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{3},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{\frac{1}{{\left(x - 2\right)}^{7}}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-13},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{10}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-8},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{3}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-16},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{3}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-17},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{2}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{-1},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{\frac{1}{{\left(x + 10\right)}^{3}}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-21},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{3}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-17},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{6}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{8},{18}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{8}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{-8},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 10\right)}\; , & x<{9}\\[3pt]
{{\left(x - 8\right)}^{9}}\; , & x\geq{9}
\end{array}\right.\]
 over the interval $[{2},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{\frac{1}{{\left(x - 1\right)}^{9}}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-12},{22}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{9}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-26},{-5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 7\right)}\; , & x<{6}\\[3pt]
{{\left(x - 5\right)}^{10}}\; , & x\geq{6}
\end{array}\right.\]
 over the interval $[{4},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 5\right)}\; , & x<{-6}\\[3pt]
{{\left(x + 7\right)}^{6}}\; , & x\geq{-6}
\end{array}\right.\]
 over the interval $[{-7},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{x + 4}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-15},{5}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{{\left(x - 4\right)}^{3}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{-3},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{\frac{1}{{\left(x + 6\right)}^{7}}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-18},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{8}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-23},{4}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 4\right)}\; , & x<{3}\\[3pt]
{{\left(x - 2\right)}^{3}}\; , & x\geq{3}
\end{array}\right.\]
 over the interval $[{-4},{19}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 8\right)}\; , & x<{-9}\\[3pt]
{{\left(x + 10\right)}^{10}}\; , & x\geq{-9}
\end{array}\right.\]
 over the interval $[{-16},{2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{10}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-15},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{\frac{1}{{\left(x - 6\right)}^{7}}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-10},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{{\left(x + 9\right)}^{6}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-23},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 6\right)}\; , & x<{5}\\[3pt]
{\frac{1}{{\left(x - 4\right)}^{10}}}\; , & x\geq{5}
\end{array}\right.\]
 over the interval $[{1},{16}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 2\right)}\; , & x<{-3}\\[3pt]
{\frac{1}{{\left(x + 4\right)}^{6}}}\; , & x\geq{-3}
\end{array}\right.\]
 over the interval $[{-11},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 7\right)}\; , & x<{-8}\\[3pt]
{\frac{1}{{\left(x + 9\right)}^{8}}}\; , & x\geq{-8}
\end{array}\right.\]
 over the interval $[{-13},{0}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{8}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-18},{13}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 3\right)}\; , & x<{2}\\[3pt]
{{\left(x - 1\right)}^{5}}\; , & x\geq{2}
\end{array}\right.\]
 over the interval $[{-16},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{2}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-13},{11}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 6\right)}\; , & x<{-7}\\[3pt]
{{\left(x + 8\right)}^{5}}\; , & x\geq{-7}
\end{array}\right.\]
 over the interval $[{-15},{-2}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 4\right)}\; , & x<{-5}\\[3pt]
{{\left(x + 6\right)}^{8}}\; , & x\geq{-5}
\end{array}\right.\]
 over the interval $[{-20},{-1}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x - 1\right)}\; , & x<{-2}\\[3pt]
{\frac{1}{{\left(x + 3\right)}^{7}}}\; , & x\geq{-2}
\end{array}\right.\]
 over the interval $[{-14},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 2\right)}\; , & x<{1}\\[3pt]
{x^{3}}\; , & x\geq{1}
\end{array}\right.\]
 over the interval $[{-14},{14}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 8\right)}\; , & x<{7}\\[3pt]
{{\left(x - 6\right)}^{3}}\; , & x\geq{7}
\end{array}\right.\]
 over the interval $[{-12},{20}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 5\right)}\; , & x<{4}\\[3pt]
{{\left(x - 3\right)}^{7}}\; , & x\geq{4}
\end{array}\right.\]
 over the interval $[{-6},{6}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x + 9\right)}\; , & x<{8}\\[3pt]
{{\left(x - 7\right)}^{7}}\; , & x\geq{8}
\end{array}\right.\]
 over the interval $[{0},{23}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Concept Question 0003. \\ \fi
\begin{problem}

Does the Extreme Value Theorem hold for the function \[f(x)=\left\{\begin{array}{ll}
{\log\left(-x\right)}\; , & x<{-1}\\[3pt]
{{\left(x + 2\right)}^{8}}\; , & x\geq{-1}
\end{array}\right.\]
 over the interval $[{-6},{7}]$?

\input{Derivative-Concept-0003.HELP.tex}
 
 \begin{multipleChoice}
 \choice{Yes}
 \choice[correct]{No}
 \end{multipleChoice}

If not, which condition fails?

\begin{multipleChoice}
\choice[correct]{$f$ is not continuous on the interval}
\choice{The interval is not closed}
\choice{$f$ is not differentiable on the interval}
\choice{Nothing fails; the theorem holds}
\end{multipleChoice}
\end{problem}
}%}

