\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@MC, Type@Compute, Topic@Integral, Sub@Improper, Func@Log, Sub@Sub-u, File@0074}{

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{4 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{6 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{9 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{8 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{8 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{6 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{6 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{\frac{3 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{7 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{7 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{6 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{2 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{\log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{2 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{5 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{5 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{3 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{5 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{3 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{6 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{-\frac{3 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{2 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{-\frac{4 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{3 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{-\frac{7 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{\log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{\log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{6 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{2 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{-\frac{7 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{\frac{2 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{9 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{-\frac{9 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{\frac{4 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{-\frac{6 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{-\frac{6 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{8 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{9 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{7 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{\frac{7 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{2 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{3 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{-\frac{8 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{6 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{5 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{-\frac{2 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{-\frac{\log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{7 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{8 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{\frac{5 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{\log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{4 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{5 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{8 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{4 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{7 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{-\frac{5 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{8 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{4 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{\log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{3 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{4 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{5 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{-\frac{5 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{-\frac{2 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{\log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{3 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{6 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{\log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{-\frac{7 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{2 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{2 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{5 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{9 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{3 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{3 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{7 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{4 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{2 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{3 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{3 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{4 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{7 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{6 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{\frac{4 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{7 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{7 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{7 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{2 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{7 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{\log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{\log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{5 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{\frac{4 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{7 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{6 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{5 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{2 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{4 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{-\frac{2 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{9 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{-\frac{9 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{3 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{\log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{\log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{\frac{\log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{4 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{9 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{-\frac{6 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{5 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{6 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{3 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{3 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{8 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{3 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{9 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{3 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{\frac{\log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{-\frac{6 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{\frac{9 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{3 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{5 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{9 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{2 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{8 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{4 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{3 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{\frac{8 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{8 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{7 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{\frac{\log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{-\frac{3 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{-\frac{9 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{6 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{-\frac{4 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{2 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{8 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{5 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{6 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{9 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{8 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{4 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{5 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{6 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{8 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{9 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{\log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{3 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{4 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{\log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{-\frac{2 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{9 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{3 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{4 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{3 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{\log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{\frac{6 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{6 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{9 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{6 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{\frac{2 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{2 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{-\frac{\log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{\log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{5 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{\frac{2 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{-\frac{4 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{3 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{5 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{8 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{\frac{5 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{-\frac{5 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{5 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{5 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{-\frac{8 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{-\frac{4 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{5 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{3 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{-\frac{\log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{-\frac{8 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{\frac{7 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{6 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{8 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{4 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{\log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{8 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{-\frac{2 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{2 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{6 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{6 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{2 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{7 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{6 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{-\frac{4 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{2 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{5 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{\frac{7 \, \log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{8 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{\log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{-\frac{7 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{-\frac{3 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{\frac{8 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{2 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{2 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{4 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{-\frac{\log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{\log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{-\frac{7 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{9 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{\frac{6 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{-\frac{3 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{-\frac{5 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{7 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{3 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{5 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{-\frac{9 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{5 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{4 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{-\frac{3 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{5 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{2 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{\frac{8 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{2 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{2 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{9 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{-\frac{8 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{7 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{\frac{3 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{\log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{-\frac{9 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{\log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{2 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{-\frac{3 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{7 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{-\frac{7 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{9 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{7 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{4 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{6 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{\log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{2 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{4 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{6 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{7 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{\frac{2 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{-\frac{8 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{\frac{3 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{-\frac{7 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{9 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{6 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{5 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{3 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{-\frac{9 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{2 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{6}^{\infty}{{\frac{8 \, \log\left(x - 5\right)}{x - 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{-\frac{\log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{9 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{-\frac{4 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{8 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{9 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{\frac{5 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{8 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-7}^{\infty}{{-\frac{9 \, \log\left(x + 8\right)}{x + 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{-\frac{\log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{9 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{-\frac{7 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{\frac{9 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{8 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{-\frac{2 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{2 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{6 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{4}^{\infty}{{\frac{9 \, \log\left(x - 3\right)}{x - 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{-\frac{9 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{0}^{\infty}{{-\frac{6 \, \log\left(x + 1\right)}{x + 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{10}^{\infty}{{\frac{9 \, \log\left(x - 9\right)}{x - 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{7 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{3 \, \log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{4 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-8}^{\infty}{{\frac{4 \, \log\left(x + 9\right)}{x + 9}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{9}^{\infty}{{\frac{6 \, \log\left(x - 8\right)}{x - 8}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{5 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-3}^{\infty}{{\frac{\log\left(x + 4\right)}{x + 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{2 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-6}^{\infty}{{\frac{8 \, \log\left(x + 7\right)}{x + 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{8 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{-\frac{4 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{\frac{5 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{4 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{\frac{6 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{1}^{\infty}{{-\frac{8 \, \log\left(x\right)}{x}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{7}^{\infty}{{\frac{9 \, \log\left(x - 6\right)}{x - 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-4}^{\infty}{{\frac{9 \, \log\left(x + 5\right)}{x + 5}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{5 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-5}^{\infty}{{\frac{8 \, \log\left(x + 6\right)}{x + 6}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{\log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{5}^{\infty}{{-\frac{4 \, \log\left(x - 4\right)}{x - 4}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{8}^{\infty}{{-\frac{7 \, \log\left(x - 7\right)}{x - 7}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-2}^{\infty}{{\frac{7 \, \log\left(x + 3\right)}{x + 3}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{\frac{4 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{3}^{\infty}{{-\frac{3 \, \log\left(x - 2\right)}{x - 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{-1}^{\infty}{{\frac{2 \, \log\left(x + 2\right)}{x + 2}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0074. \\ \fi
\begin{problem}
Determine if the integral converges or diverges.  If it converges, determine what it converges to.
\[
\int_{2}^{\infty}{{-\frac{\log\left(x - 1\right)}{x - 1}}\;dx}
\]

\input{Integral-Compute-0074.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Diverges}
\choice{Converges}
\end{multipleChoice}

\end{problem}}%}

