\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@PartialFractions, Sub@LinearFactors, Func@Rational, File@0056}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 18 \, x - 8}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{91}{x + 3} + 48 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 41 \, x + 5}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{54}{x - 1} + 57 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 3 \, x - 27}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{162}{x - 9} + 39 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 2 \, x - 3}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-x - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 67 \, x + 36}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{7 \, x + \frac{70}{x + 2} + 39 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - 11 \, x + 5\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{532}{x + 9} + 94 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 2 \, x + 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{x}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 16\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{14}{x + 3} + 12 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 15 \, x + 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{6}{x - 1} + 11 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 36 \, x + 7}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{5 \, x + 34 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 35 \, x - 25}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{243}{x - 4} - 99 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 67 \, x - 45}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{1206}{x + 9} - 211 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 5 \, x - 6\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{32}{x - 7} + 36 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + x - 8}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{342}{x - 7} - 99 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 11 \, x + 18}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{x + 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 64 \, x + 7}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{896}{x - 7} + 190 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} + 22 \, x - 16\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-9 \, x + 78 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 33 \, x + 54}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{319}{x + 5} - 73 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 21 \, x - 18}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{406}{x + 8} + 85 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x^{2} + 8 \, x - 9\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-5 \, x - \frac{120}{x + 5} + 10 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 4 \, x - 5}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{x - \frac{55}{x - 6} + 16 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 19 \, x - 6}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{470}{x + 7} - 117 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 5 \, x - 14}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{x - \frac{52}{x - 6} + 17 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 41 \, x + 8}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{5 \, x + 39 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 13 \, x - 15}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{78}{x - 3} + 49 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 16 \, x + 3}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{264}{x - 9} + 74 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 17 \, x + 15}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{492}{x + 9} + 89 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} + 19 \, x + 3}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{318}{x + 9} + 89 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 2 \, x - 15}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{x - \frac{65}{x - 8} + 18 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 12 \, x + 32}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{x - \frac{60}{x - 2} + 16 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 3 \, x - 4}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{266}{x + 6} - 87 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 5 \, x - 24\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-2 \, x + 22 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 40 \, x + 16}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{385}{x + 9} - 122 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 2 \, x - 3}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{x - \frac{21}{x + 6} - 10 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} + 9 \, x + 8\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{108}{x + 4} - 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} - x - 2\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{696}{x - 9} + 159 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 25 \, x - 18\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{20}{x - 1} + 62 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 2 \, x - 5}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{203}{x + 8} + 50 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 19 \, x - 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{253}{x + 6} - 67 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} - x - 2\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{70}{x + 3} - 49 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + x - 6\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{12}{x + 1} + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 31 \, x + 12}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{10}{x + 1} - 13 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} - 15 \, x + 9\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{620}{x + 7} + 142 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 9 \, x - 5}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{90}{x + 5} + 29 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} - 5 \, x - 12\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{63}{x + 3} - 51 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 23 \, x - 6}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{150}{x - 4} - 55 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 32 \, x + 16}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{256}{x - 4} + 88 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 3 \, x - 10}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{x + \frac{12}{x - 2} + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - x - 6\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{168}{x + 9} - 38 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 26 \, x + 15}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{111}{x - 6} - 58 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 31 \, x - 4}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{41}{x - 5} + 49 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 62 \, x - 7}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{1065}{x - 8} - 206 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 20 \, x + 32}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{4}{x - 2} + 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 8 \, x - 9\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{192}{x - 7} + 44 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 3 \, x - 9}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{110}{x - 7} + 31 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - x - 12\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{156}{x + 9} + 38 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 13 \, x + 6}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{246}{x - 5} - 83 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 19 \, x + 6}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{28}{x - 1} - 25 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 37 \, x + 35}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{123}{x + 8} - 59 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + x - 12\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{12}{x + 3} + 10 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 15 \, x - 27}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{470}{x + 7} + 127 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 3 \, x - 28}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{x - \frac{60}{x + 8} - 19 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 6 \, x - 16}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{x + \frac{25}{x + 3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 23 \, x + 28}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{385}{x - 7} + 79 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 17 \, x + 10\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{6 \, x + \frac{24}{x + 2} + 10 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 36 \, x - 81}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-5 \, x - \frac{88}{x + 7} + 34 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} + 14 \, x - 5\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{897}{x - 8} - 186 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 11 \, x + 8}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{42}{x - 2} + 23 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x^{2} + x - 2\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{280}{x + 7} + 91 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 23 \, x + 10}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{42}{x - 1} - 41 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 7 \, x + 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{119}{x + 6} - 31 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} + 15 \, x - 27\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{442}{x - 8} + 94 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} - 23 \, x + 15\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{84}{x + 1} + 62 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 17 \, x + 10\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{6 \, x - 26 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 49 \, x + 6}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{225}{x + 3} - 97 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 33 \, x + 4}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{355}{x - 9} + 111 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 27 \, x + 9}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{8 \, x + \frac{13}{x + 2} - 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} - 13 \, x + 7}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{301}{x + 6} + 85 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 6 \, x - 16}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{x + \frac{24}{x + 2} + 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 17 \, x - 6\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{462}{x - 5} + 141 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 2 \, x - 1}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{260}{x + 9} - 56 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} - 11 \, x - 2}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{280}{x + 6} + 83 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} - 13 \, x - 7\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{324}{x + 5} + 99 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 19 \, x - 24}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{4}{x - 1} + 37 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 11 \, x + 6}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{5 \, x + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 36 \, x - 36}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{855}{x + 9} - 162 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 14 \, x + 16}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{320}{x + 8} - 62 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 57 \, x + 56}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{516}{x + 5} - 127 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 81}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-x - \frac{17}{x + 8} + 16 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 15 \, x + 18}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{11}{x - 7} + 13 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 15 \, x - 25}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{4 \, x + \frac{39}{x + 2} - \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 19 \, x + 9}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{15}{x - 8} + 13 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} + 7 \, x + 6\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{98}{x + 8} - 63 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 67 \, x - 45}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{8 \, x + \frac{69}{x + 8} - 61 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 7 \, x + 6\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{28}{x - 8} - 18 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 11 \, x + 18}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-x + \frac{30}{x - 1} - 13 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 13 \, x - 7}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{28}{x - 3} - \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, {\left(x^{2} + 5 \, x + 4\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{6 \, x + \frac{12}{x + 3} - 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 20 \, x - 3}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{120}{x + 3} + 62 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 11 \, x + 6}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{350}{x + 8} - 75 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 4 \, x - 21\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{32}{x + 5} - 12 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 17 \, x - 56}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{154}{x + 6} - 53 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} - 8 \, x + 7\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{189}{x + 2} - 84 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} + 31 \, x - 30}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{56}{x - 2} - 55 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 2 \, x + 1\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{324}{x + 8} - 72 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} - 20 \, x - 7\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{120}{x - 3} + 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} + x - 2\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{440}{x - 9} + 95 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 7 \, x - 15}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{50}{x + 5} - 33 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 50 \, x + 48}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{741}{x - 7} + 148 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 15 \, x - 2}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{511}{x + 9} - 129 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 39 \, x + 20}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{938}{x + 9} - 165 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 6 \, x + 9\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{72}{x - 9} + 24 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, {\left(x^{2} + 9 \, x + 8\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{6 \, x + \frac{60}{x + 3} + 18 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} + 11 \, x - 21\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{6 \, x - 51 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 7 \, x - 18}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-x - \frac{28}{x + 5} + 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 46 \, x + 9}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{5 \, x + \frac{63}{x + 2} + 26 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 16 \, x - 64}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{256}{x - 8} + 64 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 2 \, x - 15}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{x - 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} + 19 \, x + 6\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{30}{x + 1} - 39 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 4 \, x - 12}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{x - \frac{20}{x + 4} - 12 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 2 \, x - 35}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{x - 12 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 29 \, x + 40}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{464}{x + 8} - 77 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 10 \, x - 25}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{128}{x - 9} + 44 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} + 11 \, x + 5\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{6 \, x - 27 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 22 \, x - 16}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{3 \, x - 26 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - x - 10}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{95}{x + 7} + 29 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 41 \, x + 20}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{40}{x - 5} - 49 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 12 \, x + 4}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{420}{x - 8} - 92 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 7 \, x + 6\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{12}{x + 4} - 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} - 19 \, x + 12\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{8 \, x + \frac{20}{x - 2} - 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 22 \, x + 8}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{176}{x + 4} - 62 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 17 \, x - 2}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{74}{x - 4} + 55 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} + 10 \, x + 9\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{63}{x + 8} + 54 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - x - 6}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{x + \frac{4}{x + 1} - 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 26 \, x + 15}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{320}{x - 5} - 96 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} + 13 \, x + 3\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{76}{x + 5} + 54 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 43 \, x + 40}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{430}{x + 5} + 113 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 32 \, x - 16}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{256}{x + 4} - 104 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 61 \, x - 24}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-8 \, x + 67 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 12 \, x - 32}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{5 \, x + \frac{39}{x + 1} + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 17 \, x + 72}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-x + \frac{240}{x + 7} + 31 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} + 8 \, x + 4\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{285}{x + 7} + 102 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 3 \, x + 2}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-x + \frac{12}{x - 2} - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 31 \, x - 20}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{106}{x - 7} - 67 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} - 3 \, x - 1\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{102}{x - 4} - 58 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 2 \, x - 3\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{64}{x + 5} + 24 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 46 \, x + 5}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{1148}{x - 9} - 208 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 5 \, x - 14}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-x + \frac{22}{x + 4} + 13 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 31 \, x + 42}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{372}{x - 6} - 79 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 57 \, x + 8}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{1088}{x - 9} - 183 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 10 \, x - 8}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{217}{x - 5} + 80 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 37 \, x - 40}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{6}{x + 1} + 55 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 3 \, x - 8}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{370}{x - 9} - 87 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} + 4 \, x - 12\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-4 \, x - \frac{60}{x + 1} - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 28 \, x + 32}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{161}{x + 3} + 58 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 8 \, x - 16}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{12}{x - 2} + 20 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 46 \, x + 24}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{689}{x - 7} + 144 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 17 \, x - 28\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{100}{x - 3} + 70 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 52 \, x - 32}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{832}{x - 8} + 164 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 26 \, x - 8}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{72}{x + 2} - 54 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 50 \, x - 24}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{152}{x + 8} + 94 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - x - 4\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{40}{x - 3} + 34 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 26 \, x - 3}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{165}{x - 6} - 82 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 61 \, x - 24}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{976}{x + 8} - 189 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + x - 6\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{3 \, x + \frac{12}{x + 2} - 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 31 \, x + 5}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{637}{x + 8} - 127 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 20 \, x + 12}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-3 \, x - 16 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} - 19 \, x + 9\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{4 \, x + \frac{72}{x - 5} + 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 23 \, x + 6}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{210}{x + 4} + 79 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 24 \, x - 16}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{15}{x + 1} - 38 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 27 \, x + 81}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{221}{x - 4} - 43 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 6 \, x + 8}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-x + \frac{80}{x + 6} + 18 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x^{2} + 15 \, x - 8\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-8 \, x - \frac{84}{x + 1} - 44 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 40 \, x - 12}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{35}{x - 1} + 54 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 58 \, x + 24}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{25}{x - 1} + 40 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} + 3 \, x + 2\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{80}{x + 6} - 36 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 33 \, x + 20}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{290}{x - 9} + 93 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} - 1\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{40}{x + 3} - 30 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 27 \, x - 20}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{15}{x - 1} - 43 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 31 \, x + 12}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{708}{x - 8} + 143 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} + 31 \, x - 8\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{74}{x + 9} - 82 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 4 \, x + 3\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{240}{x - 7} + 54 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 37 \, x - 15}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{300}{x - 9} + 107 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 19 \, x - 40}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-3 \, x - 29 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 6 \, x + 8\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{2}{x - 3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x^{2} - 7 \, x + 6\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{1050}{x + 9} + 175 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 32 \, x + 45}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{493}{x + 8} - 80 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 2 \, x - 7}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{252}{x - 7} + 72 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 31 \, x - 4}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{161}{x - 3} + 79 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 25 \, x + 12}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{660}{x - 8} + 137 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 5 \, x - 1}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{115}{x - 4} + 53 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 8 \, x + 7}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{x + \frac{8}{x + 5} - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 33 \, x - 56}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{156}{x - 4} - 73 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} - 3 \, x - 10\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{54}{x - 7} + 33 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} - 53 \, x + 40}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{62}{x - 6} - 19 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x^{2} + 8 \, x + 7\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-7 \, x - 42 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 44 \, x - 5}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{315}{x + 4} + 116 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 17 \, x + 24}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{352}{x + 8} + 65 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} - 7 \, x + 6\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{126}{x + 1} + 81 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} - 11 \, x - 3\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{150}{x - 6} - 74 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} + 11 \, x + 6\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{30}{x + 4} + 39 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 16 \, x - 12}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{143}{x - 5} + 46 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 47 \, x + 18}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{204}{x - 3} - 77 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 23 \, x - 12}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{70}{x + 2} + 59 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 10 \, x + 3}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{4}{x - 1} + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 7 \, x + 4\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{48}{x + 4} - 34 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 50 \, x - 48}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{7 \, x + \frac{136}{x + 4} - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 11 \, x + 24\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{3 \, x - 15 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + x - 8}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{432}{x + 8} - 111 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} + 25 \, x - 25}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{29}{x + 4} + 23 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} - 7 \, x + 3\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{276}{x + 5} + 94 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - x - 12}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-x + \frac{18}{x - 6} - 11 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 57 \, x + 7}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-8 \, x - \frac{47}{x + 6} + 39 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} + 7 \, x + 6\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{70}{x + 8} - 45 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 13 \, x - 45}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{39}{x + 4} + 29 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} + 3 \, x - 10\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{36}{x + 1} - 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} - 1\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{18}{x + 2} + 24 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 9 \, x + 1}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{235}{x - 6} - 87 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 12 \, x + 5}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{9}{x + 2} + 16 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} - 5 \, x - 1}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{68}{x + 3} + 41 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + x - 6\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{252}{x - 9} + 57 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 11 \, x - 14}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{156}{x + 5} + 79 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 32 \, x + 64}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{99}{x + 1} + 38 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 7 \, x + 12}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{x - \frac{90}{x - 6} + 19 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 10 \, x + 16\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{4 \, x + 24 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 73 \, x - 72}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{880}{x + 7} - 199 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 11 \, x - 36}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{5 \, x + \frac{42}{x - 1} - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 9 \, x - 2}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{390}{x + 8} - 89 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 9 \, x + 18\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{54}{x + 9} - 27 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} + 8 \, x + 12\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{4 \, x + \frac{12}{x + 5} - 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} + 2 \, x + 1\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{196}{x - 6} - 56 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 4 \, x + 3}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{x - \frac{8}{x + 5} - 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 5 \, x - 2\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{30}{x + 3} - 39 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 27 \, x - 40}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{195}{x + 5} - 67 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 35 \, x - 25}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{8 \, x - 45 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 22 \, x + 8}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{5 \, x + \frac{9}{x - 1} - 12 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 35 \, x + 24}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{9 \, x + 19 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 41 \, x - 20}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{708}{x - 7} + 167 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 16\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{40}{x - 6} + 24 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} + 8 \, x + 7\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-8 \, x - \frac{40}{x + 2} - 32 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 38 \, x - 35}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{237}{x + 8} - 106 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 12 \, x + 27}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{x - \frac{55}{x + 2} - 16 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 19 \, x + 3}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{539}{x - 8} + 115 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} - 7 \, x + 2\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{456}{x + 6} - 129 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, {\left(x^{2} + 4 \, x + 3\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{135}{x - 2} + 72 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} + 16 \, x - 12\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{64}{x + 2} - 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} - 5 \, x + 3\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{630}{x + 9} + 123 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 33 \, x + 27}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{8 \, x + 15 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} - 9 \, x + 14\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{42}{x - 9} + 27 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - x - 3\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{204}{x + 7} + 58 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 71 \, x + 72}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{750}{x + 6} - 155 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} + 10 \, x + 9\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{45}{x + 4} - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 9 \, x - 10}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{510}{x - 8} + 121 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 65 \, x + 72}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{7 \, x + \frac{30}{x - 2} - 37 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - x - 6}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{252}{x + 6} + 85 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 6 \, x - 27}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{128}{x - 5} - 56 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 36 \, x - 32}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{5 \, x + \frac{96}{x + 4} - 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 37 \, x - 72}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{544}{x + 8} + 117 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 37 \, x + 40}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{130}{x + 2} - 53 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 7 \, x - 18\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{3 \, x + \frac{72}{x + 6} - 15 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 27 \, x + 35}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{420}{x + 7} + 83 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 23 \, x + 14}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{45}{x - 1} - 39 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 27 \, x + 35}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{420}{x + 7} - 83 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 5 \, x - 3}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{105}{x + 4} + 59 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 47 \, x + 30}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{7 \, x + \frac{46}{x + 4} - 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 17 \, x - 15}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-4 \, x + 23 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 23 \, x - 36}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{22}{x - 2} + 35 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + x - 12}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{x - \frac{18}{x - 5} + 11 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 3 \, x - 18\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{270}{x + 9} + 63 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 31 \, x + 42}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{645}{x + 9} - 103 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} + 35 \, x - 9\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{60}{x - 1} - 86 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 65 \, x - 56}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{658}{x - 6} - 173 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 80 \, x - 9}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{80}{x + 1} - 62 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 27 \, x - 40}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{50}{x - 6} - 57 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, {\left(x^{2} - 8 \, x - 9\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{8 \, x + 80 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} + 5 \, x - 14\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-4 \, x - 36 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 10 \, x + 24\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{198}{x - 5} - 40 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 2 \, x - 1\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{621}{x - 8} + 150 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 9 \, x + 18\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{6}{x - 5} - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} - 26 \, x - 9\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{9 \, x + \frac{171}{x - 6} + 30 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 7 \, x + 2\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{224}{x - 5} + 74 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} + 5 \, x + 4\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{704}{x - 7} - 152 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 43 \, x + 15}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{183}{x + 8} + 85 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + x - 6}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-5 \, x - \frac{2}{x + 1} + 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 3 \, x - 27}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-4 \, x - \frac{5}{x - 2} - 19 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 11 \, x - 20}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{324}{x + 8} + 75 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} - 9 \, x + 2\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{30}{x + 1} - 34 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 29 \, x + 18\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{264}{x + 3} - 94 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 8 \, x + 15}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{x - \frac{24}{x - 1} + 10 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 10 \, x + 3}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{3 \, x + \frac{4}{x + 1} + 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 11 \, x + 18}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{x + \frac{10}{x - 4} - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 32 \, x - 15}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{77}{x + 2} + 60 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 11 \, x + 28\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{416}{x - 9} + 58 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 43 \, x - 30}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{231}{x - 9} - 101 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - x - 72}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-x - \frac{30}{x - 7} - 13 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 7 \, x + 5}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{77}{x - 8} - 25 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 50 \, x - 24}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{600}{x + 6} - 158 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 38 \, x - 35}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{140}{x + 7} - 88 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 9 \, x - 56}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{66}{x - 2} - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 33 \, x - 27}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{108}{x + 3} + 57 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + x - 2}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-x + \frac{70}{x + 9} + 17 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 9 \, x - 81}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{63}{x - 6} - 15 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 53 \, x + 72}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{364}{x + 4} - 93 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 51 \, x + 54}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{434}{x + 5} + 101 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 62 \, x + 48}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{55}{x + 7} + 64 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 17 \, x - 15}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{117}{x - 4} - 49 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 7 \, x - 6\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{288}{x + 6} + 86 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 29 \, x + 18}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{368}{x + 7} - 71 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 16 \, x + 21}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{120}{x - 9} - 38 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 32 \, x - 15}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{177}{x + 8} + 80 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 44 \, x - 35}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{61}{x - 8} - 68 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 47 \, x - 14}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{396}{x - 5} + 117 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} + 5 \, x - 14\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{24}{x - 1} - 21 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} - 2 \, x - 3\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{225}{x - 8} + 70 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} - 5 \, x + 2\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{3}{x - 1} + 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 45 \, x + 25}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{8 \, x + \frac{27}{x - 4} + 19 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 9 \, x - 28}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{117}{x - 5} + 49 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 2 \, x - 3\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{96}{x + 5} + 36 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 17 \, x + 9}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{657}{x - 8} - 145 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 39 \, x - 54}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{611}{x - 7} - 151 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 4 \, x - 21}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-x + \frac{11}{x - 8} - 12 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 55 \, x + 42}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-8 \, x - \frac{50}{x + 4} + 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} - 3 \, x + 2\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{810}{x + 8} + 171 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 7 \, x + 6}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-x + \frac{150}{x + 9} + 25 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 73 \, x - 72}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{518}{x + 5} - 163 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 31 \, x + 12}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{212}{x + 8} + 81 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 5 \, x - 24}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{x - \frac{12}{x + 4} - 13 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} + 5 \, x - 12\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{6 \, x + \frac{45}{x + 1} + 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 4 \, x - 32}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{x + \frac{11}{x - 7} + 10 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 19 \, x - 40}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{3 \, x + \frac{60}{x + 5} - 11 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 5 \, x - 36\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{136}{x + 8} + 42 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 53 \, x - 24}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{110}{x - 2} - 81 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 21 \, x + 54}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{104}{x - 2} - 29 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 3 \, x - 18}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{x - \frac{36}{x - 9} + 15 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 13 \, x + 42}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-x + \frac{6}{x - 4} + 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - x - 2}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-x + \frac{40}{x + 6} + 13 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 8 \, x - 16\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{280}{x + 6} - 88 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} - 4 \, x + 4\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{484}{x + 9} + 88 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 14 \, x + 8}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{272}{x - 6} + 74 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 9}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{x - \frac{72}{x + 9} - 18 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 15 \, x + 56}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-x + \frac{90}{x - 2} - 19 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 26 \, x + 16}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{3 \, x + \frac{24}{x + 2} + 14 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} + 4 \, x - 5\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{200}{x - 5} + 70 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} - 2 \, x - 3\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{616}{x + 8} + 144 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 23 \, x + 12}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{54}{x + 6} - 37 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 54 \, x - 16}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{7 \, x + \frac{88}{x - 6} + 30 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, {\left(x^{2} - 6 \, x + 5\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{40}{x - 6} + 48 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 8 \, x + 12}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{x - \frac{96}{x + 6} - 20 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 11 \, x - 40}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{176}{x - 8} - 43 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - x - 4}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{72}{x - 4} + 39 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 11 \, x + 5}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{2 \, x + 9 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 6 \, x - 16}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-x - \frac{21}{x + 1} - 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 16}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-x - \frac{15}{x - 1} - 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} - 13 \, x + 15\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{27}{x - 6} + 33 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 7 \, x - 4}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{221}{x - 9} + 43 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} + 8 \, x + 12\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{12}{x + 4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 23 \, x + 63}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{2 \, x + 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 17 \, x + 72}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-x + \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 34 \, x + 24}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{735}{x + 9} - 124 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 6 \, x + 8\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{4 \, x + 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 7 \, x - 8}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-x + 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 2 \, x - 8\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{4 \, x + \frac{20}{x - 3} + 16 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 21 \, x - 20}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{88}{x + 3} - 51 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 6 \, x - 16\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{72}{x - 2} + 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 45 \, x + 81}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{592}{x - 7} - 101 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 2 \, x - 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{5 \, x + \frac{4}{x - 1} + 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 21 \, x - 9}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{203}{x + 4} + 85 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 80 \, x + 64}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{1513}{x + 9} + 242 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 7 \, x + 12}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{x - \frac{90}{x + 6} - 19 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} + x - 2\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{280}{x - 8} + 68 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 5 \, x + 4}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-x + \frac{130}{x + 9} + 23 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 28 \, x + 15}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{559}{x + 8} + 108 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 55 \, x - 8}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{54}{x - 1} - 69 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 31 \, x + 36\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{952}{x + 8} + 158 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 6 \, x + 5}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-x - \frac{3}{x + 4} + 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} - 7 \, x + 3\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{6}{x - 1} + 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} - 2 \, x - 63}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{x + \frac{48}{x - 5} + 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 21 \, x - 54}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-5 \, x - \frac{72}{x - 3} - 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 8 \, x + 7\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{48}{x + 1} + 30 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 2 \, x - 9}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{48}{x + 3} - 40 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 43 \, x + 15}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{133}{x - 2} + 75 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 43 \, x + 15}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{871}{x - 8} - 171 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 3 \, x - 28}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-x - \frac{18}{x + 5} + 7 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 23 \, x - 12}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{590}{x + 7} + 149 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 35 \, x - 49}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{276}{x + 5} - 95 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 31 \, x + 36\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{16}{x - 1} + 50 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 71 \, x + 56}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{1136}{x + 8} + 199 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} + 19 \, x + 9\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{60}{x - 1} - 46 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 4 \, x + 3\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{3 \, x + 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 25 \, x + 4}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{70}{x - 6} + 47 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 4 \, x - 5}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{408}{x - 7} + 122 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 10 \, x - 8\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{10}{x + 1} + 32 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 2 \, x - 15\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{24}{x - 1} + 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 22 \, x - 16}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{120}{x - 4} + 46 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 14 \, x + 48}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{x - \frac{80}{x - 2} + 18 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 9 \, x + 8\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{24}{x + 5} - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 21 \, x + 27}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{370}{x - 7} + 77 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 8 \, x - 9\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-2 \, x - 20 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 2 \, x - 24\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{78}{x + 9} + 32 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 34 \, x + 63}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{308}{x + 5} + 64 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 79 \, x + 63}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{1207}{x - 8} - 207 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x^{2} - 5 \, x + 4\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{910}{x + 9} + 161 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 6 \, x - 1}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{215}{x - 6} - 78 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 31 \, x - 20}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{490}{x + 6} + 139 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 11 \, x - 63}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{50}{x + 1} - 15 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} - 29 \, x - 42}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{183}{x - 9} - 79 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 18 \, x + 9}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{224}{x - 5} - 68 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 53 \, x - 21}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-8 \, x - \frac{105}{x + 4} + 11 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + x - 12\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{24}{x + 1} + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 2 \, x - 8\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{32}{x - 4} - 20 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} + x - 1\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{132}{x + 5} - 57 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 13 \, x + 15}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{49}{x + 2} - 21 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 2 \, x - 8}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-x + \frac{40}{x - 8} - 14 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 12 \, x - 27}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-7 \, x - \frac{32}{x - 1} - 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 2 \, x + 1\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{32}{x - 5} + 16 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 15 \, x - 4}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{50}{x - 6} + 33 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 14 \, x - 24\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{50}{x - 7} - 56 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 17 \, x - 6}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{26}{x - 4} - 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 5 \, x - 14\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{16}{x + 6} + 14 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} + x - 2\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{20}{x - 2} + 25 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} - 10 \, x - 8\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{627}{x + 7} + 156 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 13 \, x + 8}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{36}{x + 4} - 27 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 4 \, x - 21\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{33}{x - 4} + 36 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} - 1\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{18}{x - 2} - 24 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 5 \, x + 3}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{15}{x - 4} - 11 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 81 \, x + 81}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{8 \, x - \frac{1241}{x - 8} + 209 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 5 \, x - 6\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{24}{x + 3} + 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 9 \, x - 2}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{246}{x + 8} + 71 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 26 \, x + 48}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{13}{x - 7} + 16 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 16 \, x + 16\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{672}{x + 8} + 128 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 21 \, x - 18}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{7}{x + 1} + 29 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 8 \, x - 21}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{456}{x + 9} - 98 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 48 \, x - 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{7 \, x + \frac{48}{x - 1} - 34 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 47 \, x + 35}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-8 \, x - \frac{34}{x - 3} - \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 29 \, x + 45}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{119}{x + 2} - 45 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - 15 \, x - 27\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-4 \, x + \frac{130}{x + 4} + 62 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 5 \, x - 12}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{90}{x - 6} + 29 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - x - 4\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-6 \, x + \frac{220}{x + 6} + 74 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 33 \, x + 27}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{188}{x + 7} + 79 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 49 \, x + 20}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{9 \, x - \frac{154}{x - 2} + 85 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} + 3 \, x - 18}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-x + \frac{52}{x - 7} - 17 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} + 31 \, x + 21\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{8 \, x + \frac{68}{x + 5} - 18 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 3 \, x - 27}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{125}{x + 8} + 35 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 14 \, x - 49}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{255}{x + 8} + 62 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} - x - 2\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{120}{x + 6} - 39 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 4 \, x - 21}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-x - \frac{25}{x - 2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 41 \, x + 45}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{4 \, x + \frac{46}{x - 7} + 15 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 31 \, x - 45}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{324}{x + 9} + 113 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 21 \, x + 10}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{39}{x + 1} + 37 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 57 \, x + 8}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{348}{x + 4} + 113 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 73 \, x + 72}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{363}{x - 3} - 121 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 2 \, x - 16}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{160}{x - 8} + 46 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 5 \, x - 36\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{180}{x + 9} + 46 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 15 \, x + 56}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{x - \frac{272}{x - 9} + 33 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 47 \, x - 6}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{429}{x - 5} - 127 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 70 \, x + 49}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-9 \, x - 56 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 13 \, x - 45}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{51}{x + 6} + 11 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 3 \, x - 35}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{100}{x - 9} - 33 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} + 8 \, x + 4\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{2}{x + 1} - 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 11 \, x - 20}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-4 \, x - \frac{5}{x - 1} - 19 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 17 \, x + 30}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{204}{x + 6} + 41 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 29 \, x + 24}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{344}{x - 5} + 99 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 8 \, x + 7\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{32}{x - 9} - 20 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 28 \, x + 32}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{5 \, x - \frac{380}{x + 6} - 88 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 23 \, x + 30}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{38}{x - 8} - 25 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 10 \, x + 3}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{3 \, x - \frac{156}{x - 9} + 44 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 9 \, x + 14\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{8}{x - 6} + 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 11 \, x + 9}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{60}{x - 3} + 23 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} - 9 \, x + 8\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-8 \, x - \frac{80}{x - 6} - 24 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 18 \, x + 16}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{480}{x - 8} - 98 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 22 \, x + 7\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{896}{x - 9} + 152 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 10 \, x + 7}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{1}{x + 2} + 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 7 \, x - 72}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{2 \, x + \frac{33}{x + 3} - 19 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 31 \, x + 56}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-3 \, x - \frac{14}{x - 7} - 11 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 5 \, x - 24}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{x - \frac{26}{x - 5} + 15 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x^{2} + 7 \, x + 3\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{72}{x + 5} + 52 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 62 \, x + 48}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{923}{x - 7} - 188 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 16 \, x + 21}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-3 \, x + \frac{65}{x - 2} - 28 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 26 \, x - 63}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-5 \, x + \frac{576}{x + 9} + 116 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 21 \, x + 49}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{2 \, x - \frac{15}{x - 2} - 13 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 47 \, x + 30}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-7 \, x + \frac{152}{x + 2} + 75 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 47 \, x - 42}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{9 \, x + 61 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 8 \, x - 12}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{7 \, x - \frac{132}{x + 4} - 64 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 16 \, x - 12\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{6 \, x + \frac{56}{x + 4} - 16 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 14 \, x - 5\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{32}{x - 1} + 16 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 73 \, x + 8}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-9 \, x + \frac{598}{x + 5} + 163 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 17 \, x + 4}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{54}{x - 2} + 33 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 6 \, x + 8\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{70}{x - 3} - 24 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{x^{2} + 7 \, x + 6}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{x - \frac{50}{x - 4} + 15 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 41 \, x + 20}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-9 \, x - \frac{26}{x + 2} - 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} - 11 \, x - 21\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{234}{x + 6} - 70 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 19 \, x - 36}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-7 \, x - 37 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{x^{2} - 81}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-x - \frac{72}{x + 3} + 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 27 \, x - 7}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{4 \, x + \frac{25}{x + 6} - 21 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 29 \, x + 15}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-8 \, x + \frac{129}{x + 6} + 67 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 10 \, x + 24\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-2 \, x + \frac{70}{x + 1} + 24 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} + 15 \, x - 8\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-6 \, x - \frac{90}{x + 2} - 21 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} - x - 6\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{6 \, x - \frac{342}{x - 8} + 93 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 8 \, x - 9\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{4 \, x - \frac{44}{x + 2} - 48 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0056. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0056.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 13 \, x + 36\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-2 \, x - \frac{12}{x - 7} - 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

