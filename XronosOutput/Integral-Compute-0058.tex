\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@PartialFractions, Sub@LinearFactors, Func@Rational, Func@Log, File@0058}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 10 \, x + 25}{{\left(x + 7\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{2}{5 \, {\left(x + 7\right)}} + \frac{9}{25} \, \log\left(x + 7\right) + \frac{16}{25} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 8 \, x - 3\right)}}{{\left(x + 6\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{24}{7 \, {\left(x - 1\right)}} + \frac{171}{49} \, \log\left(x + 6\right) + \frac{270}{49} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 31 \, x + 56}{{\left(x - 1\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{14}{x - 1} + \frac{11}{2} \, \log\left(x - 1\right) - \frac{5}{2} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 22 \, x + 3}{{\left(x + 5\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{96}{x + 5} + \frac{4}{3} \, \log\left(x + 5\right) - \frac{25}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 8 \, x - 35}{{\left(x - 2\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{31}{2 \, {\left(x - 8\right)}} + \frac{13}{12} \, \log\left(x - 2\right) - \frac{49}{12} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 5 \, x - 28}{{\left(x + 6\right)}^{3}}}\;dx} = \answer{{\frac{31 \, x + 161}{x^{2} + 12 \, x + 36} + 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 6 \, x + 1}{{\left(x + 9\right)}^{2} {\left(x + 7\right)}}}\;dx} = \answer{{\frac{230}{x + 9} - 67 \, \log\left(x + 9\right) + 72 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 10 \, x + 21\right)}}{{\left(x + 9\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{24}{11 \, {\left(x + 9\right)}} + \frac{152}{121} \, \log\left(x + 9\right) + \frac{90}{121} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 89 \, x + 72}{{\left(x + 8\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x - 8} - \frac{85}{16} \, \log\left(x + 8\right) - \frac{59}{16} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x^{2} - 17 \, x + 8\right)}}{{\left(x + 1\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{88}{7 \, {\left(x - 6\right)}} - \frac{108}{49} \, \log\left(x + 1\right) - \frac{284}{49} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 9\right)}}{{\left(x + 8\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{11}{x + 8} - \frac{21}{10} \, \log\left(x + 8\right) + \frac{1}{10} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 71 \, x + 56}{{\left(x + 6\right)} {\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{611}{x + 5} - 770 \, \log\left(x + 6\right) + 762 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} - 25 \, x + 21}{{\left(x + 1\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{35}{2 \, {\left(x - 7\right)}} - \frac{13}{16} \, \log\left(x + 1\right) - \frac{83}{16} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} + 11 \, x - 6\right)}}{{\left(x + 6\right)} {\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{22}{x + 5} + 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 25 \, x - 25}{{\left(x - 2\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{51}{7 \, {\left(x - 2\right)}} + \frac{58}{49} \, \log\left(x - 2\right) + \frac{236}{49} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} + 6 \, x + 5\right)}}{{\left(x + 7\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{48}{5 \, {\left(x + 7\right)}} - \frac{136}{25} \, \log\left(x + 7\right) - \frac{64}{25} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} + 7 \, x + 3\right)}}{{\left(x + 3\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{30}{x - 7} - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 19 \, x + 21}{{\left(x + 8\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{125}{16 \, {\left(x + 8\right)}} - \frac{595}{256} \, \log\left(x + 8\right) - \frac{429}{256} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 17 \, x + 8}{{\left(x + 8\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{15}{x - 7} + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 19 \, x - 5}{{\left(x + 6\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{25}{14 \, {\left(x + 6\right)}} + \frac{381}{196} \, \log\left(x + 6\right) + \frac{403}{196} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 6 \, x + 5\right)}}{{\left(x + 1\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{4}{x + 1} - 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 8 \, x + 15\right)}}{{\left(x - 5\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{198}{x - 6} - 160 \, \log\left(x - 5\right) + 158 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 18 \, x - 35}{{\left(x - 2\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{17}{2 \, {\left(x - 2\right)}} + \frac{13}{12} \, \log\left(x - 2\right) + \frac{47}{12} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 25 \, x + 4}{{\left(x + 5\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{370}{11 \, {\left(x - 6\right)}} + \frac{29}{121} \, \log\left(x + 5\right) + \frac{697}{121} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 4 \, x - 5\right)}}{{\left(x + 5\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{80}{3 \, {\left(x + 5\right)}} + \frac{44}{9} \, \log\left(x + 5\right) - \frac{8}{9} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 67 \, x + 24}{{\left(x + 7\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{1072}{15 \, {\left(x - 8\right)}} - \frac{53}{225} \, \log\left(x + 7\right) + \frac{1853}{225} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 34 \, x + 24}{{\left(x - 1\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{3}{4 \, {\left(x - 1\right)}} + \frac{83}{16} \, \log\left(x - 1\right) + \frac{29}{16} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 80 \, x + 64}{{\left(x + 2\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{260}{11 \, {\left(x + 2\right)}} - \frac{1016}{121} \, \log\left(x + 2\right) - \frac{73}{121} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 17 \, x + 15}{{\left(x + 7\right)}^{2} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{55}{x + 7} - 3 \, \log\left(x + 7\right) - \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, {\left(x^{2} + 7 \, x - 8\right)}}{{\left(x + 7\right)} {\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{45}{x + 3} - \frac{9}{2} \, \log\left(x + 7\right) + \frac{27}{2} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} + x - 20}{{\left(x + 5\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 6 \, x + 9}{{\left(x - 7\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{121}{x - 8} + 100 \, \log\left(x - 7\right) - 99 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 3 \, x - 20}{{\left(x - 3\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{45}{2 \, {\left(x - 5\right)}} - \frac{7}{4} \, \log\left(x - 3\right) - \frac{1}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} + 19 \, x + 20\right)}}{{\left(x + 8\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{8}{7 \, {\left(x + 1\right)}} - \frac{120}{49} \, \log\left(x + 8\right) - \frac{174}{49} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 49 \, x + 72}{{\left(x + 4\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{28}{5 \, {\left(x - 1\right)}} - \frac{348}{25} \, \log\left(x + 4\right) + \frac{223}{25} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 16 \, x + 5}{{\left(x + 6\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{17}{11 \, {\left(x + 6\right)}} - \frac{203}{121} \, \log\left(x + 6\right) - \frac{160}{121} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 28 \, x + 9}{{\left(x - 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{55}{2 \, {\left(x - 4\right)}} + \frac{35}{4} \, \log\left(x - 2\right) - \frac{47}{4} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 2 \, x - 5}{{\left(x + 9\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{290}{9 \, {\left(x - 9\right)}} + \frac{136}{81} \, \log\left(x + 9\right) + \frac{431}{81} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 64 \, x + 64}{{\left(x - 1\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{220}{x - 2} + 135 \, \log\left(x - 1\right) - 128 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} - 1\right)}}{{\left(x + 5\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{6}{x - 3} - \frac{9}{4} \, \log\left(x + 5\right) - \frac{15}{4} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 71 \, x + 56}{{\left(x + 4\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{23}{5 \, {\left(x - 6\right)}} - \frac{121}{25} \, \log\left(x + 4\right) - \frac{104}{25} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 33 \, x - 10}{{\left(x + 9\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{30}{13 \, {\left(x - 4\right)}} - \frac{854}{169} \, \log\left(x + 9\right) - \frac{329}{169} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 8 \, x - 9}{{\left(x + 6\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{8}{3 \, {\left(x - 3\right)}} - \frac{7}{27} \, \log\left(x + 6\right) + \frac{34}{27} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} + 11 \, x + 18\right)}}{{\left(x - 2\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{180}{x - 3} - 132 \, \log\left(x - 2\right) + 129 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 10 \, x + 21\right)}}{{\left(x + 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{77}{3 \, {\left(x - 4\right)}} - \frac{5}{18} \, \log\left(x + 2\right) - \frac{31}{18} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 14 \, x - 49}{{\left(x + 2\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{65}{4 \, {\left(x - 2\right)}} + \frac{9}{16} \, \log\left(x + 2\right) - \frac{57}{16} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} - 5 \, x + 4\right)}}{{\left(x + 4\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{64}{x + 4} - 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 46 \, x + 24}{{\left(x + 1\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{225}{4 \, {\left(x - 3\right)}} - \frac{15}{16} \, \log\left(x + 1\right) + \frac{127}{16} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 5 \, x - 24}{{\left(x - 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{2}{x - 7} + \frac{6}{5} \, \log\left(x - 2\right) - \frac{11}{5} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 4 \, x - 12}{{\left(x + 8\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{7}{13 \, {\left(x - 5\right)}} - \frac{84}{169} \, \log\left(x + 8\right) - \frac{85}{169} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 10 \, x + 8}{{\left(x + 6\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{\frac{176}{3 \, {\left(x + 6\right)}} - \frac{38}{9} \, \log\left(x + 6\right) + \frac{65}{9} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 18 \, x - 35}{{\left(x - 8\right)}^{3}}}\;dx} = \answer{{-\frac{124 \, x - 851}{2 \, {\left(x^{2} - 16 \, x + 64\right)}} + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 29 \, x + 40}{{\left(x + 8\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x + 2} - 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 5 \, x - 42}{{\left(x + 3\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{3}{2 \, {\left(x + 3\right)}} - \frac{37}{12} \, \log\left(x + 3\right) + \frac{13}{12} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 6 \, x - 7\right)}}{{\left(x + 4\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{45}{2 \, {\left(x + 4\right)}} + \frac{57}{4} \, \log\left(x + 4\right) - \frac{45}{4} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 19 \, x - 36}{{\left(x - 3\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{6}{x - 3} + \frac{17}{5} \, \log\left(x - 3\right) - \frac{52}{5} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 3 \, x - 27}{{\left(x - 1\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{4}{x - 1} + \frac{7}{5} \, \log\left(x - 1\right) - \frac{27}{5} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} - 9 \, x + 8\right)}}{{\left(x + 6\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{98}{3 \, {\left(x + 6\right)}} - \frac{77}{18} \, \log\left(x + 6\right) + \frac{5}{18} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 39 \, x - 18}{{\left(x - 1\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{468}{5 \, {\left(x - 6\right)}} + \frac{28}{25} \, \log\left(x - 1\right) + \frac{147}{25} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 52 \, x + 21}{{\left(x + 9\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{176}{3 \, {\left(x + 9\right)}} + \frac{179}{27} \, \log\left(x + 9\right) + \frac{10}{27} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 18 \, x + 9}{{\left(x - 3\right)}^{3}}}\;dx} = \answer{{\frac{12}{x - 3} - 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 17 \, x - 3}{{\left(x + 2\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{11}{5} \, \log\left(x + 2\right) + \frac{19}{5} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 55 \, x + 42}{{\left(x + 8\right)}^{2} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{497}{2 \, {\left(x + 8\right)}} - \frac{131}{8} \, \log\left(x + 8\right) + \frac{195}{8} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 15 \, x + 2}{{\left(x + 9\right)} {\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{10}{3 \, {\left(x + 3\right)}} + \frac{217}{18} \, \log\left(x + 9\right) - \frac{91}{18} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x^{2} - 3 \, x - 4\right)}}{{\left(x + 6\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{250}{9 \, {\left(x + 6\right)}} - \frac{425}{81} \, \log\left(x + 6\right) + \frac{20}{81} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} + 29 \, x - 5}{{\left(x - 4\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{742}{5 \, {\left(x - 9\right)}} - \frac{207}{25} \, \log\left(x - 4\right) + \frac{57}{25} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 55 \, x + 42}{{\left(x + 9\right)}^{2} {\left(x + 6\right)}}}\;dx} = \answer{{\frac{368}{x + 9} - \frac{187}{3} \, \log\left(x + 9\right) + \frac{208}{3} \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 20 \, x + 12}{{\left(x + 5\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{29}{3} \, \log\left(x + 5\right) + \frac{8}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} + 3 \, x - 10\right)}}{{\left(x + 7\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{8}{x + 7} - 4 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 20 \, x + 12}{{\left(x + 5\right)}^{2} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{13}{4 \, {\left(x + 5\right)}} + \frac{53}{16} \, \log\left(x + 5\right) - \frac{5}{16} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} + 13 \, x + 6\right)}}{{\left(x + 9\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{21}{5 \, {\left(x - 1\right)}} - \frac{51}{50} \, \log\left(x + 9\right) - \frac{149}{50} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 40 \, x + 16}{{\left(x + 5\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{41}{3 \, {\left(x + 5\right)}} - \frac{109}{9} \, \log\left(x + 5\right) + \frac{28}{9} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 9 \, x + 9}{{\left(x + 2\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{7}{2 \, {\left(x + 2\right)}} + \frac{27}{20} \, \log\left(x + 2\right) + \frac{13}{20} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 2 \, x - 48}{{\left(x + 8\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{15}{17 \, {\left(x - 9\right)}} + \frac{32}{289} \, \log\left(x + 8\right) + \frac{257}{289} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} + 7 \, x + 6\right)}}{{\left(x + 9\right)}^{2} {\left(x + 7\right)}}}\;dx} = \answer{{-\frac{108}{x + 9} + \frac{9}{2} \, \log\left(x + 9\right) - \frac{27}{2} \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} + 5 \, x - 6\right)}}{{\left(x + 8\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{396}{13 \, {\left(x - 5\right)}} - \frac{162}{169} \, \log\left(x + 8\right) - \frac{1359}{169} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 13 \, x + 6}{{\left(x + 4\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{34}{9 \, {\left(x + 4\right)}} - \frac{209}{81} \, \log\left(x + 4\right) - \frac{196}{81} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + x - 6\right)}}{{\left(x - 1\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{2}{x - 1} - \log\left(x - 1\right) + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 2 \, x + 1\right)}}{{\left(x + 4\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{12}{5 \, {\left(x - 1\right)}} + \frac{27}{25} \, \log\left(x + 4\right) + \frac{48}{25} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 39 \, x - 5}{{\left(x - 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{279}{2 \, {\left(x - 4\right)}} + \frac{105}{4} \, \log\left(x - 2\right) - \frac{73}{4} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 22 \, x + 8}{{\left(x + 6\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{380}{7 \, {\left(x - 8\right)}} - \frac{50}{49} \, \log\left(x + 6\right) - \frac{391}{49} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 17 \, x + 35}{{\left(x - 6\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{5}{3 \, {\left(x - 6\right)}} - \frac{26}{9} \, \log\left(x - 6\right) + \frac{44}{9} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 6 \, x - 8}{{\left(x - 2\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{24}{5 \, {\left(x - 2\right)}} - \frac{154}{25} \, \log\left(x - 2\right) + \frac{279}{25} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 17 \, x + 36}{{\left(x - 1\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{55}{3 \, {\left(x - 1\right)}} + \frac{118}{9} \, \log\left(x - 1\right) - \frac{136}{9} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} + 2 \, x + 1\right)}}{{\left(x + 9\right)} {\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{63}{5 \, {\left(x + 4\right)}} + \frac{448}{25} \, \log\left(x + 9\right) - \frac{273}{25} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 37 \, x + 63}{{\left(x + 6\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{143}{3 \, {\left(x + 6\right)}} - \frac{112}{27} \, \log\left(x + 6\right) + \frac{4}{27} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 19 \, x + 10}{{\left(x - 2\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{9}{2} \, \log\left(x - 2\right) - \frac{23}{2} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 7 \, x + 3}{{\left(x + 6\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{39}{10 \, {\left(x - 4\right)}} - \frac{189}{100} \, \log\left(x + 6\right) - \frac{211}{100} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 8 \, x + 16\right)}}{{\left(x + 7\right)}^{2} {\left(x + 4\right)}}}\;dx} = \answer{{-\frac{242}{3 \, {\left(x + 7\right)}} + \frac{110}{9} \, \log\left(x + 7\right) - \frac{128}{9} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 11 \, x + 8}{{\left(x - 3\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{34}{3 \, {\left(x - 3\right)}} - \frac{121}{18} \, \log\left(x - 3\right) + \frac{175}{18} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 51 \, x + 54}{{\left(x + 3\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{782}{11 \, {\left(x - 8\right)}} + \frac{54}{121} \, \log\left(x + 3\right) - \frac{659}{121} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 9 \, x - 28}{{\left(x + 1\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{11}{x + 1} - \frac{10}{3} \, \log\left(x + 1\right) - \frac{2}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 6 \, x - 16}{{\left(x + 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{12}{x - 4} + \frac{2}{3} \, \log\left(x + 2\right) + \frac{19}{3} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 5 \, x + 2}{{\left(x + 2\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{2}{3 \, {\left(x + 2\right)}} - \frac{19}{18} \, \log\left(x + 2\right) - \frac{35}{18} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 53 \, x - 24}{{\left(x + 9\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{1020}{11 \, {\left(x + 9\right)}} - \frac{949}{121} \, \log\left(x + 9\right) + \frac{102}{121} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 7 \, x - 18}{{\left(x + 9\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{693}{16 \, {\left(x + 9\right)}} + \frac{1723}{256} \, \log\left(x + 9\right) + \frac{325}{256} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 2 \, x - 8\right)}}{{\left(x + 1\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{16}{3 \, {\left(x - 2\right)}} + \frac{10}{9} \, \log\left(x + 1\right) - \frac{28}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 39 \, x - 8}{{\left(x - 3\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{40}{3 \, {\left(x - 3\right)}} - \frac{67}{18} \, \log\left(x - 3\right) - \frac{23}{18} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 21 \, x - 18}{{\left(x + 8\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{495}{17 \, {\left(x - 9\right)}} + \frac{70}{289} \, \log\left(x + 8\right) + \frac{1086}{289} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 23 \, x - 4}{{\left(x + 6\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{175}{6 \, {\left(x + 6\right)}} + \frac{395}{72} \, \log\left(x + 6\right) + \frac{37}{72} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 4 \, x - 12\right)}}{{\left(x + 6\right)}^{2} {\left(x + 4\right)}}}\;dx} = \answer{{12 \, \log\left(x + 6\right) - 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 11 \, x - 42}{{\left(x + 6\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{2}{x - 5} - \frac{12}{11} \, \log\left(x + 6\right) - \frac{21}{11} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - 15 \, x - 8\right)}}{{\left(x + 8\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{480}{13 \, {\left(x + 8\right)}} - \frac{742}{169} \, \log\left(x + 8\right) + \frac{66}{169} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 31 \, x - 45}{{\left(x + 1\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{51}{2 \, {\left(x - 3\right)}} - \frac{5}{8} \, \log\left(x + 1\right) + \frac{37}{8} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 31 \, x - 45}{{\left(x - 3\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{95}{x - 6} + \frac{28}{3} \, \log\left(x - 3\right) - \frac{16}{3} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} + 2 \, x + 1\right)}}{{\left(x + 8\right)}^{2} {\left(x + 5\right)}}}\;dx} = \answer{{-\frac{98}{x + 8} + \frac{14}{3} \, \log\left(x + 8\right) - \frac{32}{3} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 23 \, x + 24}{{\left(x + 7\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{12}{x + 7} - \frac{35}{9} \, \log\left(x + 7\right) - \frac{10}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} + 17 \, x - 9\right)}}{{\left(x - 1\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{91}{x - 4} + \frac{10}{3} \, \log\left(x - 1\right) + \frac{8}{3} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} + 6 \, x - 7\right)}}{{\left(x + 2\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{30}{x + 2} - 6 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 21 \, x + 54}{{\left(x + 8\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{175}{6 \, {\left(x + 8\right)}} - \frac{143}{72} \, \log\left(x + 8\right) - \frac{1}{72} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} - 8 \, x - 9\right)}}{{\left(x - 5\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{48}{x - 5} - 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 38 \, x + 21}{{\left(x - 5\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{429}{x - 6} + 336 \, \log\left(x - 5\right) - 331 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 16 \, x - 15}{{\left(x - 4\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{33}{2 \, {\left(x - 4\right)}} - \frac{113}{4} \, \log\left(x - 4\right) + \frac{141}{4} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 4 \, x + 3\right)}}{{\left(x + 5\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{16}{3} \, \log\left(x + 5\right) - \frac{4}{3} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x^{2} + 19 \, x + 9\right)}}{{\left(x + 3\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{\frac{120}{x + 3} - 92 \, \log\left(x + 3\right) + 84 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 10 \, x + 9}{{\left(x + 1\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{5}{2 \, {\left(x + 1\right)}} + \frac{19}{16} \, \log\left(x + 1\right) - \frac{3}{16} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, {\left(x^{2} - 4 \, x + 3\right)}}{{\left(x + 5\right)} {\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{315}{x + 4} + 432 \, \log\left(x + 5\right) - 423 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 11 \, x - 20}{{\left(x - 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{99}{5 \, {\left(x - 7\right)}} + \frac{26}{25} \, \log\left(x - 2\right) - \frac{126}{25} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 10 \, x + 7}{{\left(x + 7\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{224}{11 \, {\left(x + 7\right)}} - \frac{348}{121} \, \log\left(x + 7\right) - \frac{15}{121} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} + 3 \, x - 4\right)}}{{\left(x + 5\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{22}{x - 7} + \frac{1}{6} \, \log\left(x + 5\right) + \frac{23}{6} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 51 \, x + 18}{{\left(x - 6\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{767}{x - 7} + 612 \, \log\left(x - 6\right) - 604 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 34 \, x - 48}{{\left(x - 5\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{544}{3 \, {\left(x - 8\right)}} - \frac{247}{9} \, \log\left(x - 5\right) + \frac{202}{9} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 8 \, x + 7\right)}}{{\left(x + 4\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{55}{2 \, {\left(x + 4\right)}} - \frac{41}{12} \, \log\left(x + 4\right) + \frac{5}{12} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 2 \, x - 35}{{\left(x + 5\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{8}{x + 1} + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 16 \, x + 4}{{\left(x - 2\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x - 3} + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 3 \, x - 5}{{\left(x + 9\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{30}{7 \, {\left(x - 5\right)}} + \frac{65}{98} \, \log\left(x + 9\right) + \frac{131}{98} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 32 \, x + 16}{{\left(x - 4\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{6 \, \log\left(x - 4\right) - 13 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + x - 6}{{\left(x + 7\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{29}{8} \, \log\left(x + 7\right) - \frac{11}{8} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} + 8 \, x + 7\right)}}{{\left(x + 7\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{21}{4} \, \log\left(x + 7\right) + \frac{7}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 11 \, x + 24}{{\left(x + 8\right)} {\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{150}{x + 7} - 176 \, \log\left(x + 8\right) + 175 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 3 \, x - 54}{{\left(x + 6\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x - 3} - \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 2 \, x + 1}{{\left(x - 4\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{9}{5 \, {\left(x - 4\right)}} - \frac{39}{25} \, \log\left(x - 4\right) + \frac{64}{25} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + x - 24}{{\left(x + 9\right)}^{2} {\left(x + 8\right)}}}\;dx} = \answer{{\frac{210}{x + 9} - 157 \, \log\left(x + 9\right) + 160 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 7 \, x - 6}{{\left(x + 8\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{258}{17 \, {\left(x + 8\right)}} - \frac{983}{289} \, \log\left(x + 8\right) - \frac{462}{289} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - x - 12\right)}}{{\left(x - 2\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{20}{7 \, {\left(x - 2\right)}} + \frac{22}{49} \, \log\left(x - 2\right) - \frac{120}{49} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 8 \, x - 1}{{\left(x - 3\right)}^{3}}}\;dx} = \answer{{\frac{2 \, {\left(23 \, x - 55\right)}}{x^{2} - 6 \, x + 9} - 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x^{2} + 13 \, x + 6\right)}}{{\left(x + 8\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{252}{11 \, {\left(x - 3\right)}} + \frac{120}{121} \, \log\left(x + 8\right) + \frac{848}{121} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} + 3 \, x - 40}{{\left(x + 6\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{68}{15 \, {\left(x - 9\right)}} + \frac{22}{225} \, \log\left(x + 6\right) - \frac{247}{225} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 76 \, x + 32}{{\left(x - 3\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{115}{4 \, {\left(x - 3\right)}} + \frac{203}{16} \, \log\left(x - 3\right) - \frac{59}{16} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 14 \, x + 45}{{\left(x + 4\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{7}{2 \, {\left(x - 2\right)}} - \frac{13}{4} \, \log\left(x + 4\right) + \frac{9}{4} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 29 \, x + 18\right)}}{{\left(x + 7\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{57}{8 \, {\left(x + 7\right)}} + \frac{369}{128} \, \log\left(x + 7\right) + \frac{783}{128} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} - 7 \, x - 8\right)}}{{\left(x + 1\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{27}{4} \, \log\left(x + 1\right) + \frac{3}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 2 \, x - 24\right)}}{{\left(x + 3\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{42}{11 \, {\left(x + 3\right)}} - \frac{130}{121} \, \log\left(x + 3\right) - \frac{112}{121} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x^{2} - 4 \, x + 3\right)}}{{\left(x + 7\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{15}{x - 9} - \frac{25}{16} \, \log\left(x + 7\right) - \frac{55}{16} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 14 \, x - 5\right)}}{{\left(x - 4\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{26}{5 \, {\left(x - 4\right)}} - \frac{74}{25} \, \log\left(x - 4\right) + \frac{224}{25} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} - 8 \, x + 4\right)}}{{\left(x + 3\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{60}{7 \, {\left(x - 4\right)}} + \frac{165}{49} \, \log\left(x + 3\right) + \frac{276}{49} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} - 29 \, x + 7\right)}}{{\left(x + 8\right)}^{3}}}\;dx} = \answer{{-\frac{3 \, {\left(62 \, x + 331\right)}}{x^{2} + 16 \, x + 64} - 8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - x - 18}{{\left(x - 4\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{29}{2 \, {\left(x - 4\right)}} + \frac{107}{8} \, \log\left(x - 4\right) - \frac{147}{8} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 38 \, x - 16}{{\left(x + 4\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{49}{3 \, {\left(x + 1\right)}} + \frac{88}{9} \, \log\left(x + 4\right) - \frac{133}{9} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x^{2} - 8 \, x + 7\right)}}{{\left(x - 1\right)}^{3}}}\;dx} = \answer{{-\frac{42}{x - 1} - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 31 \, x + 21}{{\left(x - 4\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{273}{4 \, {\left(x - 4\right)}} - \frac{653}{16} \, \log\left(x - 4\right) + \frac{781}{16} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} - 25 \, x - 18\right)}}{{\left(x + 1\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{3}{x + 1} - 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 13 \, x + 21}{{\left(x + 8\right)}^{2} {\left(x + 5\right)}}}\;dx} = \answer{{\frac{15}{x + 8} + \frac{4}{3} \, \log\left(x + 8\right) + \frac{2}{3} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} - 4\right)}}{{\left(x + 6\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{180}{13 \, {\left(x - 7\right)}} - \frac{128}{169} \, \log\left(x + 6\right) - \frac{548}{169} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} - 5 \, x - 6\right)}}{{\left(x + 6\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{140}{3 \, {\left(x + 6\right)}} + \frac{217}{27} \, \log\left(x + 6\right) - \frac{28}{27} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 3 \, x - 1}{{\left(x + 5\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{9}{7 \, {\left(x - 2\right)}} - \frac{114}{49} \, \log\left(x + 5\right) - \frac{82}{49} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 22 \, x + 21}{{\left(x + 4\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{517}{12 \, {\left(x - 8\right)}} + \frac{13}{144} \, \log\left(x + 4\right) + \frac{707}{144} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 10 \, x + 24}{{\left(x - 1\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{48}{x - 2} + 35 \, \log\left(x - 1\right) - 34 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + x - 4}{{\left(x + 3\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{124}{5 \, {\left(x - 7\right)}} - \frac{19}{50} \, \log\left(x + 3\right) - \frac{231}{50} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 44 \, x + 45}{{\left(x + 7\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{3}{x + 2} + \frac{16}{5} \, \log\left(x + 7\right) + \frac{19}{5} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} - 7 \, x - 4\right)}}{{\left(x + 1\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{10}{x - 2} + \frac{5}{3} \, \log\left(x + 1\right) + \frac{13}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 7 \, x + 6}{{\left(x + 2\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{15}{x + 1} + 28 \, \log\left(x + 2\right) - 26 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 22 \, x - 16\right)}}{{\left(x + 7\right)}^{2} {\left(x + 5\right)}}}\;dx} = \answer{{-\frac{69}{2 \, {\left(x + 7\right)}} + \frac{189}{4} \, \log\left(x + 7\right) - \frac{153}{4} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 67 \, x + 28}{{\left(x - 1\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{170}{x - 9} + \frac{13}{8} \, \log\left(x - 1\right) + \frac{59}{8} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 27 \, x + 35}{{\left(x - 3\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{341}{3 \, {\left(x - 6\right)}} - \frac{152}{9} \, \log\left(x - 3\right) + \frac{116}{9} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} - 5 \, x - 2\right)}}{{\left(x + 1\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{9}{2 \, {\left(x + 1\right)}} - \frac{57}{8} \, \log\left(x + 1\right) - \frac{15}{8} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 6 \, x - 16}{{\left(x + 8\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{10}{9} \, \log\left(x + 8\right) - \frac{1}{9} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 25 \, x - 25}{{\left(x + 5\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{118}{7 \, {\left(x - 9\right)}} + \frac{125}{98} \, \log\left(x + 5\right) + \frac{463}{98} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 11 \, x + 6}{{\left(x + 8\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{286}{9 \, {\left(x + 8\right)}} - \frac{245}{81} \, \log\left(x + 8\right) + \frac{2}{81} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 33 \, x + 4}{{\left(x + 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{13}{4 \, {\left(x + 5\right)}} + \frac{175}{48} \, \log\left(x + 5\right) + \frac{209}{48} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 47 \, x + 56}{{\left(x + 6\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{259}{6 \, {\left(x + 6\right)}} - \frac{383}{72} \, \log\left(x + 6\right) + \frac{23}{72} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 41 \, x + 42}{{\left(x + 7\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{21}{x - 3} + 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 10 \, x + 16\right)}}{{\left(x + 4\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{18}{x + 4} - \frac{9}{4} \, \log\left(x + 4\right) + \frac{1}{4} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - x - 56}{{\left(x + 4\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{\frac{18}{x + 4} - \frac{27}{2} \, \log\left(x + 4\right) + \frac{25}{2} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 4 \, x - 5}{{\left(x + 3\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{27}{11 \, {\left(x - 8\right)}} - \frac{16}{121} \, \log\left(x + 3\right) - \frac{105}{121} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 8 \, x - 9}{{\left(x + 8\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{11}{6 \, {\left(x + 2\right)}} + \frac{119}{36} \, \log\left(x + 8\right) - \frac{83}{36} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 7 \, x - 6\right)}}{{\left(x + 4\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{216}{5 \, {\left(x - 6\right)}} + \frac{21}{50} \, \log\left(x + 4\right) + \frac{429}{50} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 5 \, x - 12\right)}}{{\left(x - 2\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{5}{x - 2} + \frac{9}{4} \, \log\left(x - 2\right) - \frac{33}{4} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 10 \, x + 8}{{\left(x + 6\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{8}{5 \, {\left(x - 4\right)}} + \frac{44}{25} \, \log\left(x + 6\right) + \frac{31}{25} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 10 \, x + 24\right)}}{{\left(x + 9\right)}^{2} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{6}{x + 9} + 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 81 \, x + 81}{{\left(x + 6\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{855}{13 \, {\left(x + 6\right)}} + \frac{1446}{169} \, \log\left(x + 6\right) - \frac{94}{169} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 19 \, x + 9}{{\left(x - 3\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x - 5} + \frac{15}{2} \, \log\left(x - 3\right) - \frac{19}{2} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} + x - 1\right)}}{{\left(x - 4\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{35}{x - 4} - \frac{69}{2} \, \log\left(x - 4\right) + \frac{77}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} - 22 \, x + 7\right)}}{{\left(x + 8\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{375}{2 \, {\left(x + 8\right)}} - \frac{15}{4} \, \log\left(x + 8\right) - \frac{21}{4} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 55 \, x + 42}{{\left(x - 8\right)}^{3}}}\;dx} = \answer{{-\frac{73 \, x - 527}{x^{2} - 16 \, x + 64} + 8 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 10 \, x + 16}{{\left(x + 3\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{11}{5} \, \log\left(x + 3\right) + \frac{6}{5} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 47 \, x + 35}{{\left(x + 4\right)}^{3}}}\;dx} = \answer{{\frac{190 \, x + 441}{2 \, {\left(x^{2} + 8 \, x + 16\right)}} + 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 79 \, x + 63}{{\left(x + 8\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{1207}{11 \, {\left(x + 8\right)}} - \frac{1070}{121} \, \log\left(x + 8\right) + \frac{102}{121} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 44 \, x + 32}{{\left(x + 4\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-2 \, \log\left(x + 4\right) - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} + 25 \, x + 4}{{\left(x + 9\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{265}{14 \, {\left(x + 9\right)}} - \frac{897}{196} \, \log\left(x + 9\right) - \frac{279}{196} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 50 \, x + 25}{{\left(x + 9\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{301}{3 \, {\left(x + 9\right)}} + \frac{335}{36} \, \log\left(x + 9\right) - \frac{11}{36} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 6 \, x + 8\right)}}{{\left(x + 8\right)}^{2} {\left(x + 6\right)}}}\;dx} = \answer{{\frac{120}{x + 8} - 38 \, \log\left(x + 8\right) + 40 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 28 \, x - 32}{{\left(x + 4\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{11}{2} \, \log\left(x + 4\right) + \frac{7}{2} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, {\left(x^{2} + 8 \, x - 9\right)}}{{\left(x + 3\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{216}{x + 3} + 198 \, \log\left(x + 3\right) - 189 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 33 \, x + 20}{{\left(x + 4\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{16}{7 \, {\left(x - 3\right)}} + \frac{264}{49} \, \log\left(x + 4\right) + \frac{79}{49} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} + 22 \, x + 7\right)}}{{\left(x + 9\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{13}{x + 9} - \frac{83}{12} \, \log\left(x + 9\right) - \frac{25}{12} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 16 \, x - 12}{{\left(x + 8\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{308}{5 \, {\left(x + 8\right)}} - \frac{12}{25} \, \log\left(x + 8\right) - \frac{63}{25} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 9 \, x + 14}{{\left(x + 9\right)}^{2} {\left(x + 7\right)}}}\;dx} = \answer{{\frac{88}{x + 9} - \frac{61}{2} \, \log\left(x + 9\right) + \frac{63}{2} \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 5 \, x - 36}{{\left(x + 8\right)} {\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{12}{5 \, {\left(x + 3\right)}} - \frac{68}{25} \, \log\left(x + 8\right) + \frac{43}{25} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 41 \, x + 45}{{\left(x + 9\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{4}{5 \, {\left(x - 1\right)}} + \frac{369}{50} \, \log\left(x + 9\right) - \frac{169}{50} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 56 \, x + 12}{{\left(x + 2\right)}^{3}}}\;dx} = \answer{{\frac{4 \, {\left(23 \, x + 26\right)}}{x^{2} + 4 \, x + 4} + 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, {\left(x^{2} + 5 \, x - 6\right)}}{{\left(x + 3\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{24}{x - 3} - \frac{8}{3} \, \log\left(x + 3\right) + \frac{32}{3} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} + 15 \, x - 27\right)}}{{\left(x + 6\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{24}{x + 1} + \frac{27}{5} \, \log\left(x + 6\right) - \frac{57}{5} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 17 \, x - 3}{{\left(x + 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{161}{6 \, {\left(x - 4\right)}} - \frac{13}{36} \, \log\left(x + 2\right) + \frac{229}{36} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 12 \, x - 4}{{\left(x + 3\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{48}{5 \, {\left(x - 2\right)}} - \frac{23}{25} \, \log\left(x + 3\right) - \frac{152}{25} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - 5 \, x + 2\right)}}{{\left(x + 6\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{13}{4} \, \log\left(x + 6\right) - \frac{3}{4} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 28 \, x + 32}{{\left(x - 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{473}{5 \, {\left(x - 7\right)}} + \frac{108}{25} \, \log\left(x - 2\right) + \frac{17}{25} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 80 \, x - 9}{{\left(x + 1\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{8}{x + 1} - \frac{27}{5} \, \log\left(x + 1\right) + \frac{72}{5} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 25 \, x + 42}{{\left(x - 2\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{4}{3 \, {\left(x - 2\right)}} + \frac{35}{9} \, \log\left(x - 2\right) - \frac{8}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 4 \, x - 1}{{\left(x + 6\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{29}{2 \, {\left(x + 6\right)}} - \frac{99}{28} \, \log\left(x + 6\right) - \frac{41}{28} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 71 \, x + 72}{{\left(x + 8\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 2} + \frac{4}{3} \, \log\left(x + 8\right) - \frac{25}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 71 \, x - 8}{{\left(x + 5\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{12}{x - 1} + \frac{23}{6} \, \log\left(x + 5\right) - \frac{77}{6} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 27 \, x - 18}{{\left(x + 6\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{3 \, \log\left(x + 6\right) + 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 10 \, x - 16}{{\left(x + 6\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{623}{15 \, {\left(x - 9\right)}} - \frac{368}{225} \, \log\left(x + 6\right) - \frac{1657}{225} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 21 \, x - 18}{{\left(x - 1\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{31}{6 \, {\left(x - 7\right)}} + \frac{35}{36} \, \log\left(x - 1\right) - \frac{179}{36} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 14 \, x - 24}{{\left(x - 4\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{31}{x - 5} - 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} - 9 \, x - 5\right)}}{{\left(x - 3\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{102}{5 \, {\left(x - 8\right)}} - \frac{28}{25} \, \log\left(x - 3\right) + \frac{128}{25} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 18 \, x + 9}{{\left(x + 1\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{7}{3 \, {\left(x - 2\right)}} + \frac{32}{9} \, \log\left(x + 1\right) + \frac{13}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, {\left(x^{2} + 9 \, x + 8\right)}}{{\left(x + 6\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{624}{11 \, {\left(x - 5\right)}} - \frac{80}{121} \, \log\left(x + 6\right) + \frac{1048}{121} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 59 \, x + 24}{{\left(x + 9\right)} {\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{944}{x + 8} - 1122 \, \log\left(x + 9\right) + 1115 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x^{2} + x - 2\right)}}{{\left(x + 1\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{44}{x - 9} + \frac{1}{10} \, \log\left(x + 1\right) - \frac{51}{10} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 69 \, x - 27}{{\left(x - 1\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{25}{4 \, {\left(x - 1\right)}} + \frac{365}{32} \, \log\left(x - 1\right) - \frac{621}{32} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 4 \, x - 7}{{\left(x + 8\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{217}{16 \, {\left(x - 8\right)}} - \frac{153}{256} \, \log\left(x + 8\right) - \frac{615}{256} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 41 \, x + 20}{{\left(x - 3\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{328}{x - 4} + 224 \, \log\left(x - 3\right) - 215 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 10 \, x - 16}{{\left(x - 8\right)}^{3}}}\;dx} = \answer{{-\frac{2 \, {\left(67 \, x - 416\right)}}{x^{2} - 16 \, x + 64} + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} + 2 \, x + 1\right)}}{{\left(x - 7\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{576}{x - 7} + 720 \, \log\left(x - 7\right) - 729 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - x - 6\right)}}{{\left(x - 3\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{85}{2 \, {\left(x - 7\right)}} - \frac{9}{8} \, \log\left(x - 3\right) - \frac{23}{8} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 4 \, x - 5}{{\left(x + 5\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{20}{7 \, {\left(x + 5\right)}} - \frac{39}{49} \, \log\left(x + 5\right) - \frac{10}{49} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} + 5 \, x - 6}{{\left(x - 3\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{22}{x - 5} - \frac{9}{2} \, \log\left(x - 3\right) + \frac{7}{2} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 47 \, x - 6}{{\left(x + 7\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{19}{3 \, {\left(x + 7\right)}} - \frac{176}{27} \, \log\left(x + 7\right) - \frac{40}{27} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 23 \, x - 20}{{\left(x + 6\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{47}{7 \, {\left(x + 6\right)}} - \frac{190}{49} \, \log\left(x + 6\right) - \frac{153}{49} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 44 \, x - 5}{{\left(x - 2\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{583}{4 \, {\left(x - 6\right)}} - \frac{119}{16} \, \log\left(x - 2\right) - \frac{25}{16} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 17 \, x + 14}{{\left(x - 3\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{110}{3 \, {\left(x - 3\right)}} - \frac{251}{9} \, \log\left(x - 3\right) + \frac{296}{9} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 34 \, x - 48}{{\left(x - 3\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{112}{x - 6} + 11 \, \log\left(x - 3\right) - 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} + 17 \, x - 28\right)}}{{\left(x + 3\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{13}{x + 3} + \frac{15}{8} \, \log\left(x + 3\right) + \frac{33}{8} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} + 10 \, x + 3\right)}}{{\left(x + 8\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{115}{4 \, {\left(x + 8\right)}} - \frac{341}{48} \, \log\left(x + 8\right) - \frac{91}{48} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 8 \, x - 9}{{\left(x + 6\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{21}{11 \, {\left(x + 6\right)}} + \frac{65}{121} \, \log\left(x + 6\right) + \frac{56}{121} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 22 \, x - 24}{{\left(x + 8\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{248}{9 \, {\left(x + 8\right)}} + \frac{562}{81} \, \log\left(x + 8\right) + \frac{5}{81} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 4 \, x - 12}{{\left(x + 8\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{28}{5 \, {\left(x + 8\right)}} - \frac{24}{25} \, \log\left(x + 8\right) - \frac{1}{25} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} - 13 \, x + 3\right)}}{{\left(x + 4\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{238}{9 \, {\left(x + 4\right)}} + \frac{572}{81} \, \log\left(x + 4\right) + \frac{76}{81} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + x - 10}{{\left(x - 2\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{20}{x - 3} - 4 \, \log\left(x - 2\right) + \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 3 \, x - 10\right)}}{{\left(x + 6\right)}^{2} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{12}{x + 6} + \frac{15}{2} \, \log\left(x + 6\right) - \frac{9}{2} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 4 \, x + 4\right)}}{{\left(x - 4\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{72}{x - 4} - 96 \, \log\left(x - 4\right) + 98 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 5 \, x - 2}{{\left(x - 6\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{110}{x - 6} - \frac{189}{2} \, \log\left(x - 6\right) + \frac{203}{2} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 4 \, x - 5}{{\left(x + 3\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{10}{3 \, {\left(x - 9\right)}} - \frac{1}{9} \, \log\left(x + 3\right) - \frac{8}{9} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 23 \, x - 36\right)}}{{\left(x + 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{100}{9 \, {\left(x - 7\right)}} - \frac{44}{81} \, \log\left(x + 2\right) - \frac{442}{81} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 26 \, x + 15}{{\left(x - 3\right)}^{3}}}\;dx} = \answer{{\frac{2 \, {\left(34 \, x - 63\right)}}{x^{2} - 6 \, x + 9} - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 7 \, x - 1}{{\left(x + 5\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{41}{4} \, \log\left(x + 5\right) - \frac{9}{4} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + x - 6}{{\left(x - 2\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{8}{x - 5} + \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} + 11 \, x - 6\right)}}{{\left(x + 4\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{36}{13 \, {\left(x + 4\right)}} - \frac{166}{169} \, \log\left(x + 4\right) - \frac{510}{169} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 22 \, x + 24\right)}}{{\left(x + 9\right)}^{3}}}\;dx} = \answer{{\frac{152 \, x + 903}{x^{2} + 18 \, x + 81} + 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + x - 7}{{\left(x + 9\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{632}{11 \, {\left(x + 9\right)}} - \frac{941}{121} \, \log\left(x + 9\right) - \frac{27}{121} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 19 \, x + 3}{{\left(x + 9\right)} {\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{35}{x + 6} + \frac{106}{3} \, \log\left(x + 9\right) - \frac{88}{3} \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - x - 4}{{\left(x + 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{20}{3 \, {\left(x - 4\right)}} - \frac{5}{18} \, \log\left(x + 2\right) - \frac{49}{18} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 30 \, x - 25}{{\left(x + 1\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x - 3} - \frac{3}{4} \, \log\left(x + 1\right) - \frac{25}{4} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} + 9 \, x + 8\right)}}{{\left(x - 2\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{75}{2 \, {\left(x - 2\right)}} - \frac{205}{8} \, \log\left(x - 2\right) + \frac{245}{8} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} - 5 \, x - 14}{{\left(x + 2\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{4}{x + 2} + 5 \, \log\left(x + 2\right) + \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 29 \, x + 45}{{\left(x + 9\right)}^{2} {\left(x + 7\right)}}}\;dx} = \answer{{-\frac{54}{x + 9} + \frac{11}{2} \, \log\left(x + 9\right) - \frac{19}{2} \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 68 \, x + 45}{{\left(x - 1\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{44}{3 \, {\left(x - 7\right)}} - \frac{4}{9} \, \log\left(x - 1\right) + \frac{67}{9} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 7 \, x - 6}{{\left(x + 6\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{66}{7 \, {\left(x + 6\right)}} - \frac{305}{98} \, \log\left(x + 6\right) - \frac{185}{98} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 37 \, x + 20}{{\left(x + 9\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{\frac{1001}{6 \, {\left(x + 9\right)}} + \frac{85}{36} \, \log\left(x + 9\right) + \frac{203}{36} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 24 \, x - 36}{{\left(x + 2\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{55}{x + 1} + 64 \, \log\left(x + 2\right) - 69 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 3 \, x - 10}{{\left(x + 3\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{5}{3 \, {\left(x + 3\right)}} + \frac{7}{9} \, \log\left(x + 3\right) + \frac{2}{9} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 64 \, x + 64}{{\left(x + 5\right)}^{3}}}\;dx} = \answer{{-\frac{268 \, x + 781}{2 \, {\left(x^{2} + 10 \, x + 25\right)}} - 7 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 9 \, x + 1}{{\left(x + 7\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{93}{11 \, {\left(x - 4\right)}} + \frac{456}{121} \, \log\left(x + 7\right) + \frac{512}{121} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 43 \, x + 7}{{\left(x + 7\right)}^{2} {\left(x + 5\right)}}}\;dx} = \answer{{\frac{41}{2} \, \log\left(x + 7\right) - \frac{29}{2} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 10 \, x - 8}{{\left(x + 4\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{40}{x + 4} + 3 \, \log\left(x + 4\right) - 6 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 43 \, x - 10}{{\left(x + 7\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{61}{x + 7} - 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - x - 36}{{\left(x + 1\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{117}{10 \, {\left(x - 9\right)}} + \frac{33}{100} \, \log\left(x + 1\right) - \frac{233}{100} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} + 13 \, x + 40}{{\left(x + 1\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{14}{3 \, {\left(x + 1\right)}} + \frac{47}{18} \, \log\left(x + 1\right) - \frac{65}{18} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} - 29 \, x + 7\right)}}{{\left(x + 3\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{44}{3 \, {\left(x - 3\right)}} + \frac{65}{9} \, \log\left(x + 3\right) + \frac{7}{9} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 42 \, x - 27}{{\left(x - 3\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{108}{5 \, {\left(x - 3\right)}} - \frac{168}{25} \, \log\left(x - 3\right) + \frac{43}{25} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 5 \, x - 6}{{\left(x + 6\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 18 \, x - 8}{{\left(x + 9\right)} {\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{17}{6 \, {\left(x + 3\right)}} - \frac{235}{36} \, \log\left(x + 9\right) + \frac{55}{36} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 67 \, x + 28}{{\left(x - 2\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{196}{x - 5} - 22 \, \log\left(x - 2\right) + 13 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} + 7 \, x - 8\right)}}{{\left(x - 3\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{44}{x - 3} + 48 \, \log\left(x - 3\right) - 52 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 58 \, x + 16}{{\left(x + 1\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{765}{8 \, {\left(x - 7\right)}} - \frac{35}{64} \, \log\left(x + 1\right) + \frac{483}{64} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 5 \, x + 4\right)}}{{\left(x + 1\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{112}{9 \, {\left(x - 8\right)}} + \frac{40}{81} \, \log\left(x + 1\right) + \frac{284}{81} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} + 5 \, x - 6}{{\left(x - 1\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{9}{x - 3} - \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 6 \, x - 7\right)}}{{\left(x + 8\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{27}{10 \, {\left(x - 2\right)}} + \frac{27}{100} \, \log\left(x + 8\right) + \frac{273}{100} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} + 12 \, x + 35}{{\left(x + 3\right)}^{2} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{4}{x + 3} + 5 \, \log\left(x + 3\right) - 6 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} - 2 \, x - 8\right)}}{{\left(x + 1\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{15}{7 \, {\left(x + 1\right)}} + \frac{99}{49} \, \log\left(x + 1\right) + \frac{48}{49} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 55 \, x + 9}{{\left(x + 7\right)} {\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{115}{3 \, {\left(x + 4\right)}} - \frac{82}{9} \, \log\left(x + 7\right) + \frac{136}{9} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 25 \, x + 28\right)}}{{\left(x - 2\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{10}{x - 2} - 18 \, \log\left(x - 2\right) + 12 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 27 \, x - 40}{{\left(x + 6\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{187}{4 \, {\left(x + 6\right)}} + \frac{257}{32} \, \log\left(x + 6\right) - \frac{33}{32} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 17 \, x - 21}{{\left(x + 6\right)} {\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{264}{x + 5} + 369 \, \log\left(x + 6\right) - 361 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 38 \, x + 8}{{\left(x + 5\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{423}{10 \, {\left(x + 5\right)}} - \frac{857}{100} \, \log\left(x + 5\right) - \frac{43}{100} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 26 \, x - 45}{{\left(x + 3\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{69}{x + 2} + 60 \, \log\left(x + 3\right) - 67 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} - 4\right)}}{{\left(x + 5\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{21}{2 \, {\left(x + 5\right)}} - \frac{59}{16} \, \log\left(x + 5\right) - \frac{5}{16} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 3 \, x - 4\right)}}{{\left(x - 6\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{36}{x - 8} + 7 \, \log\left(x - 6\right) - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 10 \, x + 9}{{\left(x + 5\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 2} + \frac{12}{7} \, \log\left(x + 5\right) - \frac{5}{7} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 43 \, x + 40}{{\left(x - 5\right)}^{3}}}\;dx} = \answer{{\frac{27}{x - 5} - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} + 11 \, x + 6\right)}}{{\left(x - 9\right)}^{3}}}\;dx} = \answer{{-\frac{3 \, {\left(65 \, x - 411\right)}}{x^{2} - 18 \, x + 81} + 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 26 \, x - 9\right)}}{{\left(x + 3\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{48}{x + 3} - 10 \, \log\left(x + 3\right) + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 2 \, x - 15}{{\left(x + 7\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{7}{9 \, {\left(x - 2\right)}} + \frac{20}{81} \, \log\left(x + 7\right) + \frac{61}{81} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 21 \, x + 5}{{\left(x + 9\right)}^{3}}}\;dx} = \answer{{-\frac{93 \, x + 578}{x^{2} + 18 \, x + 81} - 4 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 24 \, x + 27}{{\left(x + 7\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{26}{3 \, {\left(x + 7\right)}} - \frac{28}{9} \, \log\left(x + 7\right) - \frac{17}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 41 \, x + 45}{{\left(x - 5\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{350}{3 \, {\left(x - 5\right)}} - \frac{593}{9} \, \log\left(x - 5\right) + \frac{629}{9} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} + 11 \, x - 3\right)}}{{\left(x - 6\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{341}{x - 8} + \frac{207}{2} \, \log\left(x - 6\right) - \frac{191}{2} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 43 \, x + 40}{{\left(x + 7\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{171}{2 \, {\left(x + 7\right)}} + \frac{111}{16} \, \log\left(x + 7\right) + \frac{1}{16} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 13 \, x + 15}{{\left(x + 4\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{72}{7 \, {\left(x - 3\right)}} - \frac{5}{49} \, \log\left(x + 4\right) + \frac{103}{49} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 16 \, x + 12}{{\left(x + 5\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{145}{12 \, {\left(x - 7\right)}} + \frac{217}{144} \, \log\left(x + 5\right) + \frac{503}{144} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 2 \, x + 1\right)}}{{\left(x + 9\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{162}{17 \, {\left(x - 8\right)}} + \frac{128}{289} \, \log\left(x + 9\right) + \frac{450}{289} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 13 \, x + 6}{{\left(x - 1\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{13}{6 \, {\left(x - 7\right)}} + \frac{5}{36} \, \log\left(x - 1\right) - \frac{77}{36} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 10 \, x + 9}{{\left(x + 6\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{105}{13 \, {\left(x + 6\right)}} + \frac{181}{169} \, \log\left(x + 6\right) - \frac{12}{169} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 5 \, x - 12\right)}}{{\left(x + 6\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{21}{x + 6} - \frac{61}{12} \, \log\left(x + 6\right) - \frac{11}{12} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 3 \, x - 8}{{\left(x + 9\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{18}{7 \, {\left(x + 2\right)}} + \frac{424}{49} \, \log\left(x + 9\right) - \frac{179}{49} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} - 9 \, x - 56}{{\left(x + 4\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{12}{13 \, {\left(x + 4\right)}} - \frac{313}{169} \, \log\left(x + 4\right) - \frac{25}{169} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} + 9 \, x - 18\right)}}{{\left(x + 4\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{108}{5 \, {\left(x - 6\right)}} - \frac{11}{25} \, \log\left(x + 4\right) + \frac{111}{25} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 46 \, x + 24}{{\left(x + 7\right)} {\left(x + 6\right)}^{2}}}\;dx} = \answer{{45 \, \log\left(x + 7\right) - 38 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 61 \, x - 14}{{\left(x - 6\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{1264}{3 \, {\left(x - 9\right)}} - \frac{676}{9} \, \log\left(x - 6\right) + \frac{595}{9} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 51 \, x + 54}{{\left(x + 3\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{54}{11 \, {\left(x + 3\right)}} - \frac{177}{121} \, \log\left(x + 3\right) + \frac{782}{121} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} + 5 \, x + 4\right)}}{{\left(x + 3\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{15}{x - 1} + \frac{3}{4} \, \log\left(x + 3\right) - \frac{27}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 28 \, x - 12}{{\left(x + 5\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{7}{2 \, {\left(x - 1\right)}} + \frac{3}{4} \, \log\left(x + 5\right) - \frac{23}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 8 \, x + 12\right)}}{{\left(x + 6\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{8}{11} \, \log\left(x + 6\right) - \frac{14}{11} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x^{2} - 5 \, x - 3\right)}}{{\left(x + 2\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{88}{7 \, {\left(x - 5\right)}} + \frac{60}{49} \, \log\left(x + 2\right) + \frac{332}{49} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x^{2} + 11 \, x + 5\right)}}{{\left(x + 7\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{324}{11 \, {\left(x - 4\right)}} + \frac{104}{121} \, \log\left(x + 7\right) + \frac{864}{121} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 13 \, x - 2}{{\left(x + 4\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x + 1} + 18 \, \log\left(x + 4\right) - 11 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 53 \, x + 28}{{\left(x + 7\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x - 2} + 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} + 13 \, x + 7}{{\left(x - 1\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{196}{3 \, {\left(x - 7\right)}} - \frac{13}{18} \, \log\left(x - 1\right) - \frac{95}{18} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} - 9 \, x + 14\right)}}{{\left(x + 4\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{32}{x + 1} - \frac{88}{3} \, \log\left(x + 4\right) + \frac{76}{3} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - x - 36}{{\left(x + 7\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{69}{13 \, {\left(x + 7\right)}} + \frac{308}{169} \, \log\left(x + 7\right) + \frac{30}{169} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 23 \, x - 8}{{\left(x + 3\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{50}{7 \, {\left(x + 3\right)}} + \frac{15}{49} \, \log\left(x + 3\right) + \frac{132}{49} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 48 \, x - 64}{{\left(x + 1\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{43}{2 \, {\left(x - 5\right)}} - \frac{1}{4} \, \log\left(x + 1\right) + \frac{29}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 5 \, x - 24}{{\left(x - 4\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{3}{x - 4} - 4 \, \log\left(x - 4\right) + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 6 \, x + 1}{{\left(x + 5\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{19}{3 \, {\left(x - 4\right)}} - \frac{52}{27} \, \log\left(x + 5\right) - \frac{83}{27} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} - 14 \, x + 8\right)}}{{\left(x + 6\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{200}{3 \, {\left(x + 6\right)}} - \frac{250}{27} \, \log\left(x + 6\right) + \frac{7}{27} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 13 \, x + 18}{{\left(x - 1\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{297}{8 \, {\left(x - 9\right)}} - \frac{33}{64} \, \log\left(x - 1\right) - \frac{95}{64} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} + 13 \, x - 7\right)}}{{\left(x + 6\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{26}{7 \, {\left(x + 6\right)}} - \frac{180}{49} \, \log\left(x + 6\right) - \frac{16}{49} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} + 3 \, x - 10\right)}}{{\left(x + 5\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} - 4 \, x - 5\right)}}{{\left(x + 8\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{91}{2 \, {\left(x + 8\right)}} - \frac{149}{24} \, \log\left(x + 8\right) + \frac{5}{24} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 33 \, x + 35}{{\left(x + 4\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{21}{11} \, \log\left(x + 4\right) + \frac{23}{11} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 19 \, x + 20\right)}}{{\left(x + 9\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{4}{5 \, {\left(x - 1\right)}} - \frac{217}{25} \, \log\left(x + 9\right) + \frac{67}{25} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 13 \, x - 12}{{\left(x - 3\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{21}{x - 3} + \frac{58}{3} \, \log\left(x - 3\right) - \frac{70}{3} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 13 \, x + 12\right)}}{{\left(x + 8\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{616}{17 \, {\left(x + 8\right)}} + \frac{1458}{289} \, \log\left(x + 8\right) + \frac{276}{289} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} - 6 \, x + 5\right)}}{{\left(x + 5\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{30}{x + 5} + 5 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 45 \, x + 81}{{\left(x + 6\right)}^{2} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{495}{2 \, {\left(x + 6\right)}} - \frac{309}{4} \, \log\left(x + 6\right) + \frac{325}{4} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} - 4\right)}}{{\left(x + 6\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-2 \, \log\left(x + 6\right) - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 13 \, x + 42}{{\left(x - 2\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{2}{3 \, {\left(x - 5\right)}} + \frac{20}{9} \, \log\left(x - 2\right) - \frac{11}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, {\left(x^{2} + 8 \, x - 9\right)}}{{\left(x + 9\right)}^{2} {\left(x + 7\right)}}}\;dx} = \answer{{30 \, \log\left(x + 9\right) - 24 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 23 \, x + 28}{{\left(x + 3\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{133}{11 \, {\left(x + 3\right)}} - \frac{384}{121} \, \log\left(x + 3\right) - \frac{100}{121} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x^{2} - 13 \, x - 10\right)}}{{\left(x - 4\right)}^{3}}}\;dx} = \answer{{\frac{3 \, {\left(11 \, x - 51\right)}}{x^{2} - 8 \, x + 16} - 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 5 \, x - 9}{{\left(x + 8\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{287}{10 \, {\left(x + 8\right)}} + \frac{403}{100} \, \log\left(x + 8\right) - \frac{3}{100} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} + 13 \, x + 18}{{\left(x - 1\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{133}{4 \, {\left(x - 5\right)}} + \frac{33}{16} \, \log\left(x - 1\right) - \frac{1}{16} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 38 \, x + 8}{{\left(x + 5\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{43}{14 \, {\left(x + 5\right)}} + \frac{685}{196} \, \log\left(x + 5\right) + \frac{1079}{196} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} - 6 \, x - 7\right)}}{{\left(x + 2\right)}^{3}}}\;dx} = \answer{{-\frac{3 \, {\left(20 \, x + 31\right)}}{x^{2} + 4 \, x + 4} - 6 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 37 \, x + 14}{{\left(x - 1\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{40}{x - 2} + 18 \, \log\left(x - 1\right) - 23 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 2 \, x - 8}{{\left(x + 5\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{27}{2 \, {\left(x + 5\right)}} + \frac{3}{4} \, \log\left(x + 5\right) - \frac{7}{4} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 45 \, x - 28}{{\left(x + 8\right)}^{3}}}\;dx} = \answer{{-\frac{157 \, x + 866}{x^{2} + 16 \, x + 64} - 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 9 \, x + 14}{{\left(x + 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{1}{2 \, {\left(x + 5\right)}} + \frac{1}{8} \, \log\left(x + 5\right) + \frac{7}{8} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 11 \, x - 4\right)}}{{\left(x - 5\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{8}{x - 5} + \frac{23}{2} \, \log\left(x - 5\right) - \frac{35}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 23 \, x + 10}{{\left(x + 7\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{145}{4 \, {\left(x + 7\right)}} - \frac{267}{32} \, \log\left(x + 7\right) - \frac{21}{32} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 7 \, x - 20}{{\left(x + 6\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{\frac{46}{3 \, {\left(x + 6\right)}} + \frac{41}{9} \, \log\left(x + 6\right) - \frac{14}{9} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 21 \, x + 5}{{\left(x + 5\right)} {\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{153}{x + 4} + 210 \, \log\left(x + 5\right) - 206 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} + 33 \, x - 27\right)}}{{\left(x + 2\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{77}{3 \, {\left(x + 2\right)}} - \frac{25}{18} \, \log\left(x + 2\right) + \frac{169}{18} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 73 \, x - 72}{{\left(x - 1\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{34}{x - 1} + \frac{89}{4} \, \log\left(x - 1\right) - \frac{53}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x^{2} - 2 \, x + 1\right)}}{{\left(x + 8\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{28}{11 \, {\left(x - 3\right)}} - \frac{567}{121} \, \log\left(x + 8\right) - \frac{280}{121} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 52 \, x + 21}{{\left(x - 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{32}{x - 5} + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 38 \, x + 21}{{\left(x - 2\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{44}{3 \, {\left(x - 5\right)}} - \frac{35}{9} \, \log\left(x - 2\right) + \frac{80}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 25 \, x + 28\right)}}{{\left(x + 7\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{3}{2 \, {\left(x - 1\right)}} + \frac{175}{16} \, \log\left(x + 7\right) - \frac{79}{16} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 68 \, x + 35}{{\left(x + 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{50}{x + 5} - 9 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 17 \, x - 42}{{\left(x + 6\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{68}{x + 6} + \log\left(x + 6\right) - 5 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + x - 6}{{\left(x + 9\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{147}{13 \, {\left(x + 9\right)}} - \frac{308}{169} \, \log\left(x + 9\right) - \frac{30}{169} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 7 \, x - 2}{{\left(x + 8\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{126}{x + 8} - 5 \, \log\left(x + 8\right) - 4 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - x - 2}{{\left(x + 5\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{28}{11 \, {\left(x - 6\right)}} + \frac{28}{121} \, \log\left(x + 5\right) + \frac{93}{121} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 24 \, x + 9}{{\left(x + 8\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{8}{9 \, {\left(x - 1\right)}} + \frac{649}{81} \, \log\left(x + 8\right) - \frac{82}{81} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 13 \, x - 30}{{\left(x - 4\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{17}{2 \, {\left(x - 4\right)}} - \frac{5}{8} \, \log\left(x - 4\right) + \frac{29}{8} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 8 \, x + 12}{{\left(x + 8\right)}^{2} {\left(x + 7\right)}}}\;dx} = \answer{{-\frac{140}{x + 8} + 116 \, \log\left(x + 8\right) - 117 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 64 \, x + 7}{{\left(x + 7\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{62}{15} \, \log\left(x + 7\right) - \frac{73}{15} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 27 \, x - 81}{{\left(x + 8\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{41}{11 \, {\left(x + 8\right)}} + \frac{448}{121} \, \log\left(x + 8\right) + \frac{36}{121} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 58 \, x + 24}{{\left(x - 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{871}{5 \, {\left(x - 7\right)}} + \frac{176}{25} \, \log\left(x - 2\right) + \frac{49}{25} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x^{2} + 3 \, x - 4\right)}}{{\left(x + 1\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{312}{5 \, {\left(x - 9\right)}} + \frac{9}{25} \, \log\left(x + 1\right) - \frac{159}{25} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 13 \, x + 10}{{\left(x - 3\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{45}{2 \, {\left(x - 5\right)}} + \frac{7}{4} \, \log\left(x - 3\right) + \frac{9}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 8 \, x + 5}{{\left(x + 6\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{161}{12 \, {\left(x - 6\right)}} - \frac{65}{144} \, \log\left(x + 6\right) - \frac{367}{144} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x^{2} + 11 \, x - 6\right)}}{{\left(x + 4\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{180}{7 \, {\left(x - 3\right)}} - \frac{72}{49} \, \log\left(x + 4\right) + \frac{464}{49} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - 5 \, x - 36\right)}}{{\left(x + 8\right)}^{2} {\left(x + 5\right)}}}\;dx} = \answer{{-\frac{136}{3 \, {\left(x + 8\right)}} + \frac{10}{9} \, \log\left(x + 8\right) - \frac{28}{9} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 33 \, x + 35}{{\left(x - 6\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{19}{x - 6} - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 47 \, x - 63}{{\left(x + 3\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{11}{x + 3} - \log\left(x + 3\right) - 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 6 \, x + 8\right)}}{{\left(x + 5\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{3}{5 \, {\left(x - 5\right)}} + \frac{63}{50} \, \log\left(x + 5\right) + \frac{37}{50} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 50 \, x + 25}{{\left(x + 9\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{86}{x + 9} - 9 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, {\left(x^{2} - 6 \, x + 5\right)}}{{\left(x + 3\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x - 9} + \frac{4}{3} \, \log\left(x + 3\right) + \frac{14}{3} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, x^{2} - 19 \, x + 35}{{\left(x - 5\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{7}{x - 6} - 10 \, \log\left(x - 5\right) + 12 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} - 17 \, x - 3}{{\left(x - 1\right)}^{3}}}\;dx} = \answer{{-\frac{5 \, x + 2}{x^{2} - 2 \, x + 1} - 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 27 \, x + 10}{{\left(x + 2\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{3}{x + 2} - \frac{1}{2} \, \log\left(x + 2\right) + \frac{11}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} - 11 \, x + 6\right)}}{{\left(x - 1\right)}^{3}}}\;dx} = \answer{{\frac{6 \, x - 5}{x^{2} - 2 \, x + 1} + 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} - x - 20\right)}}{{\left(x + 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{44}{9 \, {\left(x - 7\right)}} + \frac{28}{81} \, \log\left(x + 2\right) - \frac{190}{81} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 68 \, x - 32}{{\left(x + 8\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{17}{4} \, \log\left(x + 8\right) + \frac{19}{4} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 16 \, x + 16}{{\left(x + 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{17}{3 \, {\left(x - 7\right)}} - \frac{20}{27} \, \log\left(x + 2\right) - \frac{61}{27} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 6 \, x - 27}{{\left(x + 9\right)}^{2} {\left(x + 5\right)}}}\;dx} = \answer{{-\frac{108}{x + 9} + 3 \, \log\left(x + 9\right) - 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 34 \, x - 8}{{\left(x + 6\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{28}{3 \, {\left(x + 6\right)}} - \frac{97}{18} \, \log\left(x + 6\right) - \frac{65}{18} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 23 \, x + 15}{{\left(x + 6\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{297}{8 \, {\left(x + 6\right)}} - \frac{271}{64} \, \log\left(x + 6\right) + \frac{15}{64} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 43 \, x + 28}{{\left(x + 9\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{572}{5 \, {\left(x + 9\right)}} - \frac{453}{50} \, \log\left(x + 9\right) + \frac{3}{50} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{6 \, x^{2} + 11 \, x - 21}{{\left(x + 3\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{25}{9} \, \log\left(x + 3\right) + \frac{29}{9} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 31 \, x + 36\right)}}{{\left(x + 8\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{119}{2 \, {\left(x + 8\right)}} + \frac{197}{32} \, \log\left(x + 8\right) - \frac{5}{32} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 29 \, x + 24}{{\left(x + 2\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{164}{9 \, {\left(x - 7\right)}} + \frac{110}{81} \, \log\left(x + 2\right) + \frac{457}{81} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 65 \, x + 14}{{\left(x + 8\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{52}{x - 6} + \frac{5}{14} \, \log\left(x + 8\right) + \frac{121}{14} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 53 \, x + 40}{{\left(x + 5\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{37}{11} \, \log\left(x + 5\right) + \frac{62}{11} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 2 \, x - 24}{{\left(x + 9\right)}^{2} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{75}{7 \, {\left(x + 9\right)}} - \frac{65}{49} \, \log\left(x + 9\right) + \frac{16}{49} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 29 \, x + 7}{{\left(x + 8\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{31}{12 \, {\left(x + 8\right)}} + \frac{389}{144} \, \log\left(x + 8\right) + \frac{187}{144} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} - 19 \, x - 5\right)}}{{\left(x + 9\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{148}{9 \, {\left(x - 9\right)}} + \frac{245}{81} \, \log\left(x + 9\right) + \frac{403}{81} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 27 \, x - 20}{{\left(x - 3\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{29}{2 \, {\left(x - 3\right)}} + \frac{13}{4} \, \log\left(x - 3\right) - \frac{45}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x^{2} - 5 \, x + 1\right)}}{{\left(x + 8\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{198}{5 \, {\left(x + 8\right)}} + \frac{164}{25} \, \log\left(x + 8\right) + \frac{36}{25} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x^{2} - 20 \, x - 7\right)}}{{\left(x - 6\right)}^{3}}}\;dx} = \answer{{-\frac{3 \, {\left(32 \, x - 211\right)}}{2 \, {\left(x^{2} - 12 \, x + 36\right)}} + 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 15 \, x - 4}{{\left(x + 9\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{15}{8 \, {\left(x + 1\right)}} - \frac{455}{64} \, \log\left(x + 9\right) + \frac{199}{64} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 13 \, x + 40}{{\left(x + 3\right)} {\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{70}{x + 2} - 88 \, \log\left(x + 3\right) + 87 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 19 \, x - 4}{{\left(x + 7\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{27}{2 \, {\left(x + 7\right)}} + \frac{75}{16} \, \log\left(x + 7\right) + \frac{5}{16} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 13 \, x - 10}{{\left(x + 4\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{15}{x + 4} + \frac{11}{3} \, \log\left(x + 4\right) - \frac{2}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} + 6 \, x + 5\right)}}{{\left(x + 5\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{35}{x - 4} + 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x^{2} - 5 \, x + 1\right)}}{{\left(x + 4\right)} {\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{104}{x + 3} - 170 \, \log\left(x + 4\right) + 162 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 27 \, x + 18}{{\left(x - 1\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{275}{3 \, {\left(x - 7\right)}} - \frac{13}{9} \, \log\left(x - 1\right) - \frac{50}{9} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 67 \, x + 36}{{\left(x - 7\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{1020}{x - 8} + 848 \, \log\left(x - 7\right) - 841 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 26 \, x - 45}{{\left(x - 5\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{260}{x - 5} - 356 \, \log\left(x - 5\right) + 363 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 19 \, x - 24}{{\left(x + 8\right)} {\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{93}{x + 6} + 100 \, \log\left(x + 8\right) - 91 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} + 7 \, x + 6\right)}}{{\left(x - 3\right)}^{3}}}\;dx} = \answer{{\frac{3 \, {\left(13 \, x - 21\right)}}{x^{2} - 6 \, x + 9} - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x^{2} + 6 \, x + 5\right)}}{{\left(x + 4\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{405}{8 \, {\left(x - 4\right)}} + \frac{27}{64} \, \log\left(x + 4\right) - \frac{603}{64} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} - 5 \, x - 12\right)}}{{\left(x + 2\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{45}{4 \, {\left(x - 6\right)}} + \frac{9}{32} \, \log\left(x + 2\right) + \frac{183}{32} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} - 5 \, x - 6\right)}}{{\left(x + 3\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{36}{11 \, {\left(x - 8\right)}} + \frac{36}{121} \, \log\left(x + 3\right) + \frac{206}{121} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 19 \, x - 15}{{\left(x + 7\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{37}{11 \, {\left(x - 4\right)}} + \frac{510}{121} \, \log\left(x + 7\right) + \frac{458}{121} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} + 45 \, x + 25}{{\left(x + 7\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{102}{13 \, {\left(x + 7\right)}} + \frac{769}{169} \, \log\left(x + 7\right) + \frac{583}{169} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 28 \, x + 9}{{\left(x + 2\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x + 1} + 35 \, \log\left(x + 2\right) - 38 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x^{2} - 7 \, x + 6\right)}}{{\left(x - 2\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{7}{x - 2} - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - 13 \, x + 6\right)}}{{\left(x + 9\right)} {\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{121}{2 \, {\left(x + 5\right)}} - \frac{285}{8} \, \log\left(x + 9\right) + \frac{253}{8} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} - 5 \, x + 3\right)}}{{\left(x + 8\right)} {\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{272}{x + 7} - 342 \, \log\left(x + 8\right) + 338 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 55 \, x - 72}{{\left(x + 5\right)} {\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{119}{6 \, {\left(x - 1\right)}} - \frac{403}{36} \, \log\left(x + 5\right) + \frac{115}{36} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 24 \, x + 27}{{\left(x + 1\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{28}{x + 1} - 3 \, \log\left(x + 1\right) - 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 46 \, x + 48}{{\left(x - 2\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{8}{x - 2} - \frac{34}{3} \, \log\left(x - 2\right) + \frac{19}{3} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, {\left(x^{2} + 3 \, x + 2\right)}}{{\left(x + 6\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{32}{5} \, \log\left(x + 6\right) + \frac{8}{5} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 5 \, x - 36}{{\left(x + 9\right)}^{2} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{18}{x + 9} + \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 5 \, x - 14\right)}}{{\left(x - 3\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{21}{x - 5} + 15 \, \log\left(x - 3\right) - 18 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 14 \, x + 8}{{\left(x + 7\right)} {\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{7}{4 \, {\left(x + 3\right)}} + \frac{57}{16} \, \log\left(x + 7\right) - \frac{9}{16} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 1}{{\left(x - 4\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{16}{x - 9} - \frac{3}{5} \, \log\left(x - 4\right) - \frac{2}{5} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 50 \, x - 48}{{\left(x + 1\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{9}{2 \, {\left(x + 1\right)}} + \frac{119}{4} \, \log\left(x + 1\right) - \frac{91}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} - 62 \, x + 48}{{\left(x + 2\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{37}{7 \, {\left(x - 5\right)}} + \frac{208}{49} \, \log\left(x + 2\right) + \frac{233}{49} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - x - 2\right)}}{{\left(x + 7\right)} {\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{224}{x + 6} + 304 \, \log\left(x + 7\right) - 298 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 29 \, x + 6}{{\left(x + 9\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{350}{17 \, {\left(x - 8\right)}} - \frac{996}{289} \, \log\left(x + 9\right) - \frac{1605}{289} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x^{2} - 13 \, x - 10\right)}}{{\left(x + 6\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{352}{3 \, {\left(x + 6\right)}} + \frac{58}{9} \, \log\left(x + 6\right) - \frac{112}{9} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 22 \, x - 16}{{\left(x - 3\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{55}{6 \, {\left(x - 3\right)}} - \frac{79}{36} \, \log\left(x - 3\right) - \frac{29}{36} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x^{2} + 6 \, x - 7\right)}}{{\left(x + 2\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{18}{x - 2} + \frac{15}{2} \, \log\left(x + 2\right) - \frac{31}{2} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} - 52 \, x + 63}{{\left(x + 9\right)}^{2} {\left(x + 3\right)}}}\;dx} = \answer{{\frac{156}{x + 9} - \frac{7}{3} \, \log\left(x + 9\right) + \frac{22}{3} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} + 12 \, x + 27\right)}}{{\left(x + 3\right)}^{2} {\left(x + 1\right)}}}\;dx} = \answer{{-9 \, \log\left(x + 3\right) + 12 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} + 7 \, x - 36}{{\left(x - 2\right)}^{3}}}\;dx} = \answer{{-\frac{23 \, x - 49}{x^{2} - 4 \, x + 4} + 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x^{2} - 10 \, x - 8\right)}}{{\left(x + 6\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{80}{3 \, {\left(x + 6\right)}} + \frac{49}{9} \, \log\left(x + 6\right) + \frac{5}{9} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 57 \, x + 7}{{\left(x + 5\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{6}{x + 5} - \frac{29}{13} \, \log\left(x + 5\right) - \frac{75}{13} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{6 \, x^{2} + 43 \, x - 40}{{\left(x + 5\right)}^{3}}}\;dx} = \answer{{-\frac{34 \, x + 275}{2 \, {\left(x^{2} + 10 \, x + 25\right)}} - 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} - 64 \, x + 7}{{\left(x + 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{35}{2 \, {\left(x - 4\right)}} - \frac{19}{4} \, \log\left(x + 2\right) - \frac{17}{4} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} - 71 \, x + 56}{{\left(x - 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{99}{2 \, {\left(x - 5\right)}} - \frac{81}{4} \, \log\left(x - 5\right) + \frac{49}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} + 19 \, x + 9\right)}}{{\left(x + 2\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{63}{10 \, {\left(x + 2\right)}} + \frac{267}{100} \, \log\left(x + 2\right) - \frac{867}{100} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} - 5 \, x - 14\right)}}{{\left(x + 1\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{20}{x - 3} - 2 \, \log\left(x + 1\right) + 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 12 \, x + 32}{{\left(x + 2\right)}^{2} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{12}{x + 2} - \frac{4}{5} \, \log\left(x + 2\right) - \frac{1}{5} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x^{2} + 13 \, x + 6\right)}}{{\left(x - 2\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{80}{7 \, {\left(x - 2\right)}} - \frac{374}{49} \, \log\left(x - 2\right) + \frac{570}{49} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} + 71 \, x + 72}{{\left(x + 1\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{242}{3 \, {\left(x - 2\right)}} - \frac{8}{9} \, \log\left(x + 1\right) - \frac{55}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 44 \, x + 45}{{\left(x - 6\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{696}{x - 7} + 561 \, \log\left(x - 6\right) - 554 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - 2 \, x - 3}{{\left(x + 3\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{1}{3} \, \log\left(x + 3\right) + \frac{2}{3} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 5 \, x - 18}{{\left(x + 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{91}{6 \, {\left(x + 5\right)}} - \frac{359}{72} \, \log\left(x + 5\right) - \frac{145}{72} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} - 17 \, x + 8\right)}}{{\left(x + 4\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{36}{x + 4} - 7 \, \log\left(x + 4\right) + \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} - x - 6}{{\left(x + 9\right)}^{2} {\left(x + 8\right)}}}\;dx} = \answer{{\frac{84}{x + 9} - 65 \, \log\left(x + 9\right) + 66 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} - 19 \, x + 24\right)}}{{\left(x + 9\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{18}{11 \, {\left(x - 2\right)}} + \frac{1071}{121} \, \log\left(x + 9\right) - \frac{345}{121} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} + 32 \, x + 45}{{\left(x - 1\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{84}{x - 3} + 20 \, \log\left(x - 1\right) - 17 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 9 \, x + 10}{{\left(x + 5\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{57}{4 \, {\left(x - 7\right)}} - \frac{5}{48} \, \log\left(x + 5\right) - \frac{91}{48} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{8 \, x^{2} - 47 \, x + 35}{{\left(x - 2\right)}^{2} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{9}{x - 2} + 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 41 \, x - 56}{{\left(x + 2\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{26}{x - 3} - \frac{22}{5} \, \log\left(x + 2\right) + \frac{57}{5} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, x^{2} + 7 \, x - 15}{{\left(x + 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{13}{12} \, \log\left(x + 5\right) - \frac{11}{12} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} - 23 \, x + 24}{{\left(x + 5\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{13}{x + 1} - \frac{33}{2} \, \log\left(x + 5\right) + \frac{23}{2} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 65 \, x + 18}{{\left(x + 3\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{84}{5 \, {\left(x - 2\right)}} - \frac{276}{25} \, \log\left(x + 3\right) + \frac{101}{25} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} + 11 \, x - 21\right)}}{{\left(x + 5\right)} {\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{54}{x + 3} + \frac{39}{2} \, \log\left(x + 5\right) - \frac{51}{2} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, x^{2} - 28 \, x + 9}{{\left(x - 5\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{56}{x - 5} + 54 \, \log\left(x - 5\right) - 51 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 37 \, x + 36}{{\left(x + 3\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{105}{2 \, {\left(x + 3\right)}} + \frac{53}{8} \, \log\left(x + 3\right) + \frac{3}{8} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 59 \, x + 24}{{\left(x + 2\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{494}{7 \, {\left(x - 5\right)}} - \frac{66}{49} \, \log\left(x + 2\right) + \frac{409}{49} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} + 6 \, x - 16\right)}}{{\left(x + 7\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{33}{10 \, {\left(x - 3\right)}} + \frac{27}{100} \, \log\left(x + 7\right) - \frac{327}{100} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x^{2} + 7 \, x - 18\right)}}{{\left(x + 8\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{84}{13 \, {\left(x - 5\right)}} + \frac{20}{169} \, \log\left(x + 8\right) - \frac{358}{169} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x^{2} - 8 \, x + 12\right)}}{{\left(x - 1\right)} {\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{21}{2 \, {\left(x - 9\right)}} - \frac{5}{16} \, \log\left(x - 1\right) - \frac{59}{16} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{8 \, x^{2} + 33 \, x + 27}{{\left(x - 4\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{287}{4 \, {\left(x - 4\right)}} + \frac{675}{16} \, \log\left(x - 4\right) - \frac{803}{16} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x^{2} - 3 \, x + 1\right)}}{{\left(x + 4\right)}^{2} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{45}{4 \, {\left(x + 4\right)}} + \frac{61}{16} \, \log\left(x + 4\right) + \frac{35}{16} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, x^{2} - 19 \, x - 63}{{\left(x + 9\right)}^{2} {\left(x + 5\right)}}}\;dx} = \answer{{\frac{108}{x + 9} - \frac{17}{4} \, \log\left(x + 9\right) + \frac{33}{4} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 46 \, x - 21}{{\left(x + 3\right)}^{2} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{180}{7 \, {\left(x + 3\right)}} - \frac{436}{49} \, \log\left(x + 3\right) + \frac{93}{49} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} + 13 \, x - 24\right)}}{{\left(x - 3\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{33}{2 \, {\left(x - 3\right)}} + \frac{133}{8} \, \log\left(x - 3\right) - \frac{165}{8} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, x^{2} + 29 \, x - 42}{{\left(x + 1\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{45}{2 \, {\left(x - 3\right)}} - \frac{33}{8} \, \log\left(x + 1\right) + \frac{73}{8} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{x^{2} + 8 \, x + 16}{{\left(x + 5\right)} {\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{9}{4 \, {\left(x + 1\right)}} + \frac{1}{16} \, \log\left(x + 5\right) + \frac{15}{16} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} - 25 \, x + 28}{{\left(x + 7\right)} {\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{2}{x - 3} - \frac{7}{2} \, \log\left(x + 7\right) + \frac{1}{2} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{9 \, x^{2} + 14 \, x + 5}{{\left(x - 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{205}{2 \, {\left(x - 4\right)}} + \frac{69}{4} \, \log\left(x - 2\right) - \frac{33}{4} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} - 19 \, x - 30}{{\left(x - 2\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{21}{x - 4} + 13 \, \log\left(x - 2\right) - 17 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, {\left(x^{2} + 4 \, x + 3\right)}}{{\left(x + 5\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{441}{11 \, {\left(x - 6\right)}} + \frac{56}{121} \, \log\left(x + 5\right) + \frac{791}{121} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x^{2} - 4\right)}}{{\left(x + 8\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{90}{7 \, {\left(x + 8\right)}} - \frac{123}{49} \, \log\left(x + 8\right) - \frac{24}{49} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + 6 \, x + 5\right)}}{{\left(x + 2\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{21}{2 \, {\left(x - 2\right)}} - \frac{3}{8} \, \log\left(x + 2\right) + \frac{19}{8} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x^{2} + x - 3\right)}}{{\left(x - 2\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{14}{5 \, {\left(x - 2\right)}} + \frac{104}{25} \, \log\left(x - 2\right) - \frac{204}{25} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{5 \, {\left(x^{2} - x - 2\right)}}{{\left(x + 4\right)} {\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{10}{x - 5} + \frac{10}{9} \, \log\left(x + 4\right) + \frac{35}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 25}{{\left(x + 2\right)}^{2} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{21}{x + 2} - 25 \, \log\left(x + 2\right) + 24 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, x^{2} + 25 \, x + 8}{{\left(x - 1\right)} {\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{52}{x - 4} - 4 \, \log\left(x - 1\right) + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x^{2} + 13 \, x - 7\right)}}{{\left(x + 7\right)}^{2} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{45}{8} \, \log\left(x + 7\right) - \frac{3}{8} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{9 \, x^{2} + 85 \, x + 36}{{\left(x - 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{343}{x - 5} + 259 \, \log\left(x - 5\right) - 268 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - x - 30}{{\left(x + 6\right)} {\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{12}{13 \, {\left(x - 7\right)}} - \frac{12}{169} \, \log\left(x + 6\right) - \frac{157}{169} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{4 \, x^{2} + 29 \, x - 63}{{\left(x - 1\right)} {\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{425}{7 \, {\left(x - 8\right)}} + \frac{30}{49} \, \log\left(x - 1\right) - \frac{226}{49} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{3 \, {\left(x^{2} - 12 \, x + 27\right)}}{{\left(x + 5\right)} {\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{3}{x - 2} + \frac{48}{7} \, \log\left(x + 5\right) - \frac{27}{7} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{4 \, {\left(x^{2} + 7 \, x - 18\right)}}{{\left(x + 8\right)}^{2} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{40}{17 \, {\left(x + 8\right)}} + \frac{652}{289} \, \log\left(x + 8\right) + \frac{504}{289} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{2 \, {\left(x^{2} + x - 12\right)}}{{\left(x - 1\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{10}{3 \, {\left(x - 1\right)}} - \frac{4}{9} \, \log\left(x - 1\right) + \frac{22}{9} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} - 51 \, x + 54}{{\left(x + 5\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{121}{3 \, {\left(x + 5\right)}} + \frac{121}{18} \, \log\left(x + 5\right) + \frac{5}{18} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{\frac{7 \, x^{2} + 44 \, x + 12}{{\left(x + 4\right)}^{2} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{52}{11 \, {\left(x + 4\right)}} + \frac{184}{121} \, \log\left(x + 4\right) + \frac{663}{121} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{x^{2} - 8 \, x + 15}{{\left(x + 8\right)} {\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{3}{14 \, {\left(x - 6\right)}} - \frac{143}{196} \, \log\left(x + 8\right) - \frac{53}{196} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{7 \, x^{2} - 43 \, x + 6}{{\left(x + 9\right)}^{2} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{960}{11 \, {\left(x + 9\right)}} - \frac{899}{121} \, \log\left(x + 9\right) + \frac{52}{121} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0058. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0058.HELP.tex}

\[
\int{{-\frac{5 \, x^{2} + 49 \, x + 72}{{\left(x + 7\right)}^{2} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{2}{x + 7} - \frac{23}{13} \, \log\left(x + 7\right) - \frac{42}{13} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

