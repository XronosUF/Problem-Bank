\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@TrigInt, Func@Trig, File@0027}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{1}{9} \, \sin\left(x\right)^{9} + \frac{4}{7} \, \sin\left(x\right)^{7} - \frac{6}{5} \, \sin\left(x\right)^{5} + \frac{4}{3} \, \sin\left(x\right)^{3} - \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \sin\left(x\right)^{3}}\;dx} = \answer{{\frac{8}{3} \, \cos\left(x\right)^{3} - 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\cos\left(x\right)^{5} + \frac{10}{3} \, \cos\left(x\right)^{3} - 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{2}{3} \, \cos\left(x\right)^{9} + \frac{24}{7} \, \cos\left(x\right)^{7} - \frac{36}{5} \, \cos\left(x\right)^{5} + 8 \, \cos\left(x\right)^{3} - 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{1}{3} \, \sin\left(x\right)^{9} + \frac{12}{7} \, \sin\left(x\right)^{7} - \frac{18}{5} \, \sin\left(x\right)^{5} + 4 \, \sin\left(x\right)^{3} - 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\frac{2}{9} \, \sin\left(x\right)^{9} - \frac{8}{7} \, \sin\left(x\right)^{7} + \frac{12}{5} \, \sin\left(x\right)^{5} - \frac{8}{3} \, \sin\left(x\right)^{3} + 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\frac{4}{5} \, \sin\left(x\right)^{5} - \frac{8}{3} \, \sin\left(x\right)^{3} + 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{1}{7} \, \sin\left(x\right)^{7} + \frac{3}{5} \, \sin\left(x\right)^{5} - \sin\left(x\right)^{3} + \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\frac{2}{5} \, \sin\left(x\right)^{5} - \frac{4}{3} \, \sin\left(x\right)^{3} + 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{1}{5} \, \cos\left(x\right)^{5} + \frac{2}{3} \, \cos\left(x\right)^{3} - \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{9 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\frac{9}{7} \, \cos\left(x\right)^{7} - \frac{27}{5} \, \cos\left(x\right)^{5} + 9 \, \cos\left(x\right)^{3} - 9 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\frac{8}{7} \, \sin\left(x\right)^{7} - \frac{24}{5} \, \sin\left(x\right)^{5} + 8 \, \sin\left(x\right)^{3} - 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-\frac{4}{3} \, \cos\left(x\right)^{3} + 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\frac{8}{5} \, \cos\left(x\right)^{5} - \frac{16}{3} \, \cos\left(x\right)^{3} + 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-\frac{4}{3} \, \sin\left(x\right)^{3} + 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\frac{4}{9} \, \cos\left(x\right)^{9} - \frac{16}{7} \, \cos\left(x\right)^{7} + \frac{24}{5} \, \cos\left(x\right)^{5} - \frac{16}{3} \, \cos\left(x\right)^{3} + 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \cos\left(x\right)^{3}}\;dx} = \answer{{\sin\left(x\right)^{3} - 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{7}{9} \, \sin\left(x\right)^{9} + 4 \, \sin\left(x\right)^{7} - \frac{42}{5} \, \sin\left(x\right)^{5} + \frac{28}{3} \, \sin\left(x\right)^{3} - 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{1}{5} \, \sin\left(x\right)^{5} + \frac{2}{3} \, \sin\left(x\right)^{3} - \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{2}{3} \, \sin\left(x\right)^{9} + \frac{24}{7} \, \sin\left(x\right)^{7} - \frac{36}{5} \, \sin\left(x\right)^{5} + 8 \, \sin\left(x\right)^{3} - 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\frac{7}{5} \, \sin\left(x\right)^{5} - \frac{14}{3} \, \sin\left(x\right)^{3} + 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-\frac{8}{3} \, \cos\left(x\right)^{3} + 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{6}{5} \, \cos\left(x\right)^{5} + 4 \, \cos\left(x\right)^{3} - 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\frac{6}{7} \, \cos\left(x\right)^{7} - \frac{18}{5} \, \cos\left(x\right)^{5} + 6 \, \cos\left(x\right)^{3} - 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\cos\left(x\right)^{5} - \frac{10}{3} \, \cos\left(x\right)^{3} + 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\frac{2}{3} \, \sin\left(x\right)^{9} - \frac{24}{7} \, \sin\left(x\right)^{7} + \frac{36}{5} \, \sin\left(x\right)^{5} - 8 \, \sin\left(x\right)^{3} + 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{4}{5} \, \cos\left(x\right)^{5} + \frac{8}{3} \, \cos\left(x\right)^{3} - 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{7}{5} \, \cos\left(x\right)^{5} + \frac{14}{3} \, \cos\left(x\right)^{3} - 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{6}{7} \, \cos\left(x\right)^{7} + \frac{18}{5} \, \cos\left(x\right)^{5} - 6 \, \cos\left(x\right)^{3} + 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{8}{9} \, \sin\left(x\right)^{9} + \frac{32}{7} \, \sin\left(x\right)^{7} - \frac{48}{5} \, \sin\left(x\right)^{5} + \frac{32}{3} \, \sin\left(x\right)^{3} - 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\cos\left(x\right)^{7} - \frac{21}{5} \, \cos\left(x\right)^{5} + 7 \, \cos\left(x\right)^{3} - 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{9 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\sin\left(x\right)^{9} - \frac{36}{7} \, \sin\left(x\right)^{7} + \frac{54}{5} \, \sin\left(x\right)^{5} - 12 \, \sin\left(x\right)^{3} + 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{7}{5} \, \sin\left(x\right)^{5} + \frac{14}{3} \, \sin\left(x\right)^{3} - 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{4}{7} \, \cos\left(x\right)^{7} + \frac{12}{5} \, \cos\left(x\right)^{5} - 4 \, \cos\left(x\right)^{3} + 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \cos\left(x\right)^{3}}\;dx} = \answer{{\frac{4}{3} \, \sin\left(x\right)^{3} - 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{4}{7} \, \sin\left(x\right)^{7} + \frac{12}{5} \, \sin\left(x\right)^{5} - 4 \, \sin\left(x\right)^{3} + 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{2}{7} \, \sin\left(x\right)^{7} + \frac{6}{5} \, \sin\left(x\right)^{5} - 2 \, \sin\left(x\right)^{3} + 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{4}{5} \, \sin\left(x\right)^{5} + \frac{8}{3} \, \sin\left(x\right)^{3} - 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{5}{9} \, \sin\left(x\right)^{9} + \frac{20}{7} \, \sin\left(x\right)^{7} - 6 \, \sin\left(x\right)^{5} + \frac{20}{3} \, \sin\left(x\right)^{3} - 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{3}{5} \, \sin\left(x\right)^{5} + 2 \, \sin\left(x\right)^{3} - 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{8}{7} \, \sin\left(x\right)^{7} + \frac{24}{5} \, \sin\left(x\right)^{5} - 8 \, \sin\left(x\right)^{3} + 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-2 \, \cos\left(x\right)^{3} + 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\frac{7}{5} \, \cos\left(x\right)^{5} - \frac{14}{3} \, \cos\left(x\right)^{3} + 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\frac{9}{5} \, \cos\left(x\right)^{5} - 6 \, \cos\left(x\right)^{3} + 9 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{9 \, \sin\left(x\right)^{3}}\;dx} = \answer{{3 \, \cos\left(x\right)^{3} - 9 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\frac{2}{9} \, \cos\left(x\right)^{9} - \frac{8}{7} \, \cos\left(x\right)^{7} + \frac{12}{5} \, \cos\left(x\right)^{5} - \frac{8}{3} \, \cos\left(x\right)^{3} + 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{3}{7} \, \cos\left(x\right)^{7} + \frac{9}{5} \, \cos\left(x\right)^{5} - 3 \, \cos\left(x\right)^{3} + 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\cos\left(x\right)^{7} + \frac{21}{5} \, \cos\left(x\right)^{5} - 7 \, \cos\left(x\right)^{3} + 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\sin\left(x\right)^{7}}\;dx} = \answer{{\frac{1}{7} \, \cos\left(x\right)^{7} - \frac{3}{5} \, \cos\left(x\right)^{5} + \cos\left(x\right)^{3} - \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\sin\left(x\right)^{3}}\;dx} = \answer{{\frac{1}{3} \, \cos\left(x\right)^{3} - \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{2}{5} \, \cos\left(x\right)^{5} + \frac{4}{3} \, \cos\left(x\right)^{3} - 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-2 \, \sin\left(x\right)^{3} + 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\frac{6}{5} \, \sin\left(x\right)^{5} - 4 \, \sin\left(x\right)^{3} + 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\frac{7}{9} \, \sin\left(x\right)^{9} - 4 \, \sin\left(x\right)^{7} + \frac{42}{5} \, \sin\left(x\right)^{5} - \frac{28}{3} \, \sin\left(x\right)^{3} + 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{3}{7} \, \sin\left(x\right)^{7} + \frac{9}{5} \, \sin\left(x\right)^{5} - 3 \, \sin\left(x\right)^{3} + 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \sin\left(x\right)^{3}}\;dx} = \answer{{\frac{7}{3} \, \cos\left(x\right)^{3} - 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\sin\left(x\right)^{7} - \frac{21}{5} \, \sin\left(x\right)^{5} + 7 \, \sin\left(x\right)^{3} - 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{2}{5} \, \sin\left(x\right)^{5} + \frac{4}{3} \, \sin\left(x\right)^{3} - 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\frac{5}{9} \, \cos\left(x\right)^{9} - \frac{20}{7} \, \cos\left(x\right)^{7} + 6 \, \cos\left(x\right)^{5} - \frac{20}{3} \, \cos\left(x\right)^{3} + 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{9 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{9}{5} \, \cos\left(x\right)^{5} + 6 \, \cos\left(x\right)^{3} - 9 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{5}{7} \, \cos\left(x\right)^{7} + 3 \, \cos\left(x\right)^{5} - 5 \, \cos\left(x\right)^{3} + 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\frac{1}{3} \, \sin\left(x\right)^{9} - \frac{12}{7} \, \sin\left(x\right)^{7} + \frac{18}{5} \, \sin\left(x\right)^{5} - 4 \, \sin\left(x\right)^{3} + 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \sin\left(x\right)^{3}}\;dx} = \answer{{\frac{5}{3} \, \cos\left(x\right)^{3} - 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-3 \, \cos\left(x\right)^{3} + 9 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-\frac{7}{3} \, \sin\left(x\right)^{3} + 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{9 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{9}{7} \, \sin\left(x\right)^{7} + \frac{27}{5} \, \sin\left(x\right)^{5} - 9 \, \sin\left(x\right)^{3} + 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\frac{3}{5} \, \cos\left(x\right)^{5} - 2 \, \cos\left(x\right)^{3} + 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\frac{4}{9} \, \sin\left(x\right)^{9} - \frac{16}{7} \, \sin\left(x\right)^{7} + \frac{24}{5} \, \sin\left(x\right)^{5} - \frac{16}{3} \, \sin\left(x\right)^{3} + 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\frac{4}{7} \, \cos\left(x\right)^{7} - \frac{12}{5} \, \cos\left(x\right)^{5} + 4 \, \cos\left(x\right)^{3} - 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \cos\left(x\right)^{3}}\;dx} = \answer{{\frac{8}{3} \, \sin\left(x\right)^{3} - 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{4}{9} \, \sin\left(x\right)^{9} + \frac{16}{7} \, \sin\left(x\right)^{7} - \frac{24}{5} \, \sin\left(x\right)^{5} + \frac{16}{3} \, \sin\left(x\right)^{3} - 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\cos\left(x\right)^{7}}\;dx} = \answer{{\frac{1}{7} \, \sin\left(x\right)^{7} - \frac{3}{5} \, \sin\left(x\right)^{5} + \sin\left(x\right)^{3} - \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\frac{2}{5} \, \cos\left(x\right)^{5} - \frac{4}{3} \, \cos\left(x\right)^{3} + 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{8}{7} \, \cos\left(x\right)^{7} + \frac{24}{5} \, \cos\left(x\right)^{5} - 8 \, \cos\left(x\right)^{3} + 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\frac{4}{5} \, \cos\left(x\right)^{5} - \frac{8}{3} \, \cos\left(x\right)^{3} + 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\frac{7}{9} \, \cos\left(x\right)^{9} - 4 \, \cos\left(x\right)^{7} + \frac{42}{5} \, \cos\left(x\right)^{5} - \frac{28}{3} \, \cos\left(x\right)^{3} + 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\frac{1}{3} \, \cos\left(x\right)^{9} - \frac{12}{7} \, \cos\left(x\right)^{7} + \frac{18}{5} \, \cos\left(x\right)^{5} - 4 \, \cos\left(x\right)^{3} + 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \sin\left(x\right)^{3}}\;dx} = \answer{{\cos\left(x\right)^{3} - 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \cos\left(x\right)^{3}}\;dx} = \answer{{2 \, \sin\left(x\right)^{3} - 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\sin\left(x\right)^{7} + \frac{21}{5} \, \sin\left(x\right)^{5} - 7 \, \sin\left(x\right)^{3} + 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\sin\left(x\right)^{9}}\;dx} = \answer{{\frac{1}{9} \, \cos\left(x\right)^{9} - \frac{4}{7} \, \cos\left(x\right)^{7} + \frac{6}{5} \, \cos\left(x\right)^{5} - \frac{4}{3} \, \cos\left(x\right)^{3} + \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\sin\left(x\right)^{9} + \frac{36}{7} \, \sin\left(x\right)^{7} - \frac{54}{5} \, \sin\left(x\right)^{5} + 12 \, \sin\left(x\right)^{3} - 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-\frac{5}{3} \, \cos\left(x\right)^{3} + 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{9 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\frac{9}{5} \, \sin\left(x\right)^{5} - 6 \, \sin\left(x\right)^{3} + 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{6}{5} \, \sin\left(x\right)^{5} + 4 \, \sin\left(x\right)^{3} - 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{8}{5} \, \sin\left(x\right)^{5} + \frac{16}{3} \, \sin\left(x\right)^{3} - 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{1}{3} \, \cos\left(x\right)^{9} + \frac{12}{7} \, \cos\left(x\right)^{7} - \frac{18}{5} \, \cos\left(x\right)^{5} + 4 \, \cos\left(x\right)^{3} - 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-\cos\left(x\right)^{3} + 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\frac{9}{7} \, \sin\left(x\right)^{7} - \frac{27}{5} \, \sin\left(x\right)^{5} + 9 \, \sin\left(x\right)^{3} - 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{7 \, \sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{7}{9} \, \cos\left(x\right)^{9} + 4 \, \cos\left(x\right)^{7} - \frac{42}{5} \, \cos\left(x\right)^{5} + \frac{28}{3} \, \cos\left(x\right)^{3} - 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{2}{7} \, \cos\left(x\right)^{7} + \frac{6}{5} \, \cos\left(x\right)^{5} - 2 \, \cos\left(x\right)^{3} + 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-\frac{2}{3} \, \sin\left(x\right)^{3} + 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\frac{5}{7} \, \cos\left(x\right)^{7} - 3 \, \cos\left(x\right)^{5} + 5 \, \cos\left(x\right)^{3} - 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \cos\left(x\right)^{3}}\;dx} = \answer{{\frac{7}{3} \, \sin\left(x\right)^{3} - 7 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{5}{9} \, \cos\left(x\right)^{9} + \frac{20}{7} \, \cos\left(x\right)^{7} - 6 \, \cos\left(x\right)^{5} + \frac{20}{3} \, \cos\left(x\right)^{3} - 5 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\frac{2}{7} \, \cos\left(x\right)^{7} - \frac{6}{5} \, \cos\left(x\right)^{5} + 2 \, \cos\left(x\right)^{3} - 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{9 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-3 \, \sin\left(x\right)^{3} + 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-\frac{5}{3} \, \sin\left(x\right)^{3} + 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\frac{2}{3} \, \cos\left(x\right)^{9} - \frac{24}{7} \, \cos\left(x\right)^{7} + \frac{36}{5} \, \cos\left(x\right)^{5} - 8 \, \cos\left(x\right)^{3} + 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\frac{8}{7} \, \cos\left(x\right)^{7} - \frac{24}{5} \, \cos\left(x\right)^{5} + 8 \, \cos\left(x\right)^{3} - 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\cos\left(x\right)^{9}}\;dx} = \answer{{\frac{1}{9} \, \sin\left(x\right)^{9} - \frac{4}{7} \, \sin\left(x\right)^{7} + \frac{6}{5} \, \sin\left(x\right)^{5} - \frac{4}{3} \, \sin\left(x\right)^{3} + \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-\frac{2}{3} \, \cos\left(x\right)^{3} + 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-\frac{8}{3} \, \sin\left(x\right)^{3} + 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \sin\left(x\right)^{5}}\;dx} = \answer{{\frac{6}{5} \, \cos\left(x\right)^{5} - 4 \, \cos\left(x\right)^{3} + 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \cos\left(x\right)^{3}}\;dx} = \answer{{\frac{2}{3} \, \sin\left(x\right)^{3} - 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-8 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\frac{8}{9} \, \cos\left(x\right)^{9} - \frac{32}{7} \, \cos\left(x\right)^{7} + \frac{48}{5} \, \cos\left(x\right)^{5} - \frac{32}{3} \, \cos\left(x\right)^{3} + 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\frac{3}{5} \, \sin\left(x\right)^{5} - 2 \, \sin\left(x\right)^{3} + 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \sin\left(x\right)^{9}}\;dx} = \answer{{\cos\left(x\right)^{9} - \frac{36}{7} \, \cos\left(x\right)^{7} + \frac{54}{5} \, \cos\left(x\right)^{5} - 12 \, \cos\left(x\right)^{3} + 9 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\frac{2}{7} \, \sin\left(x\right)^{7} - \frac{6}{5} \, \sin\left(x\right)^{5} + 2 \, \sin\left(x\right)^{3} - 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-3 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\frac{3}{7} \, \sin\left(x\right)^{7} - \frac{9}{5} \, \sin\left(x\right)^{5} + 3 \, \sin\left(x\right)^{3} - 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{1}{9} \, \cos\left(x\right)^{9} + \frac{4}{7} \, \cos\left(x\right)^{7} - \frac{6}{5} \, \cos\left(x\right)^{5} + \frac{4}{3} \, \cos\left(x\right)^{3} - \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{6}{7} \, \sin\left(x\right)^{7} + \frac{18}{5} \, \sin\left(x\right)^{5} - 6 \, \sin\left(x\right)^{3} + 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{8}{9} \, \cos\left(x\right)^{9} + \frac{32}{7} \, \cos\left(x\right)^{7} - \frac{48}{5} \, \cos\left(x\right)^{5} + \frac{32}{3} \, \cos\left(x\right)^{3} - 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{9}{7} \, \cos\left(x\right)^{7} + \frac{27}{5} \, \cos\left(x\right)^{5} - 9 \, \cos\left(x\right)^{3} + 9 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\frac{9}{5} \, \sin\left(x\right)^{5} + 6 \, \sin\left(x\right)^{3} - 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-7 \, \sin\left(x\right)^{3}}\;dx} = \answer{{-\frac{7}{3} \, \cos\left(x\right)^{3} + 7 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \sin\left(x\right)^{7}}\;dx} = \answer{{\frac{3}{7} \, \cos\left(x\right)^{7} - \frac{9}{5} \, \cos\left(x\right)^{5} + 3 \, \cos\left(x\right)^{3} - 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-4 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\frac{4}{7} \, \sin\left(x\right)^{7} - \frac{12}{5} \, \sin\left(x\right)^{5} + 4 \, \sin\left(x\right)^{3} - 4 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-6 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\frac{6}{7} \, \sin\left(x\right)^{7} - \frac{18}{5} \, \sin\left(x\right)^{5} + 6 \, \sin\left(x\right)^{3} - 6 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{6 \, \sin\left(x\right)^{3}}\;dx} = \answer{{2 \, \cos\left(x\right)^{3} - 6 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right)^{3}}\;dx} = \answer{{\frac{5}{3} \, \sin\left(x\right)^{3} - 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\sin\left(x\right)^{3}}\;dx} = \answer{{-\frac{1}{3} \, \cos\left(x\right)^{3} + \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\sin\left(x\right)^{5}}\;dx} = \answer{{\frac{1}{5} \, \cos\left(x\right)^{5} - \frac{2}{3} \, \cos\left(x\right)^{3} + \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\cos\left(x\right)^{3}}\;dx} = \answer{{\frac{1}{3} \, \sin\left(x\right)^{3} - \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-2 \, \cos\left(x\right)^{9}}\;dx} = \answer{{-\frac{2}{9} \, \sin\left(x\right)^{9} + \frac{8}{7} \, \sin\left(x\right)^{7} - \frac{12}{5} \, \sin\left(x\right)^{5} + \frac{8}{3} \, \sin\left(x\right)^{3} - 2 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-\sin\left(x\right)^{7}}\;dx} = \answer{{-\frac{1}{7} \, \cos\left(x\right)^{7} + \frac{3}{5} \, \cos\left(x\right)^{5} - \cos\left(x\right)^{3} + \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right)^{5}}\;dx} = \answer{{-\sin\left(x\right)^{5} + \frac{10}{3} \, \sin\left(x\right)^{3} - 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \cos\left(x\right)^{7}}\;dx} = \answer{{-\frac{5}{7} \, \sin\left(x\right)^{7} + 3 \, \sin\left(x\right)^{5} - 5 \, \sin\left(x\right)^{3} + 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{4}{9} \, \cos\left(x\right)^{9} + \frac{16}{7} \, \cos\left(x\right)^{7} - \frac{24}{5} \, \cos\left(x\right)^{5} + \frac{16}{3} \, \cos\left(x\right)^{3} - 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\sin\left(x\right)^{5} - \frac{10}{3} \, \sin\left(x\right)^{3} + 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \cos\left(x\right)^{5}}\;dx} = \answer{{\frac{8}{5} \, \sin\left(x\right)^{5} - \frac{16}{3} \, \sin\left(x\right)^{3} + 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-9 \, \cos\left(x\right)^{3}}\;dx} = \answer{{3 \, \sin\left(x\right)^{3} - 9 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{4 \, \sin\left(x\right)^{3}}\;dx} = \answer{{\frac{4}{3} \, \cos\left(x\right)^{3} - 4 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\cos\left(x\right)^{3}}\;dx} = \answer{{-\frac{1}{3} \, \sin\left(x\right)^{3} + \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{5 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\frac{5}{9} \, \sin\left(x\right)^{9} - \frac{20}{7} \, \sin\left(x\right)^{7} + 6 \, \sin\left(x\right)^{5} - \frac{20}{3} \, \sin\left(x\right)^{3} + 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \sin\left(x\right)^{3}}\;dx} = \answer{{\frac{2}{3} \, \cos\left(x\right)^{3} - 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{2 \, \sin\left(x\right)^{9}}\;dx} = \answer{{-\frac{2}{9} \, \cos\left(x\right)^{9} + \frac{8}{7} \, \cos\left(x\right)^{7} - \frac{12}{5} \, \cos\left(x\right)^{5} + \frac{8}{3} \, \cos\left(x\right)^{3} - 2 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right)^{7}}\;dx} = \answer{{\frac{5}{7} \, \sin\left(x\right)^{7} - 3 \, \sin\left(x\right)^{5} + 5 \, \sin\left(x\right)^{3} - 5 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{3}{5} \, \cos\left(x\right)^{5} + 2 \, \cos\left(x\right)^{3} - 3 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{3 \, \cos\left(x\right)^{3}}\;dx} = \answer{{-\sin\left(x\right)^{3} + 3 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \sin\left(x\right)^{5}}\;dx} = \answer{{-\frac{8}{5} \, \cos\left(x\right)^{5} + \frac{16}{3} \, \cos\left(x\right)^{3} - 8 \, \cos\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{8 \, \cos\left(x\right)^{9}}\;dx} = \answer{{\frac{8}{9} \, \sin\left(x\right)^{9} - \frac{32}{7} \, \sin\left(x\right)^{7} + \frac{48}{5} \, \sin\left(x\right)^{5} - \frac{32}{3} \, \sin\left(x\right)^{3} + 8 \, \sin\left(x\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0027. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0027.HELP.tex}

\[
\int{{\cos\left(x\right)^{5}}\;dx} = \answer{{\frac{1}{5} \, \sin\left(x\right)^{5} - \frac{2}{3} \, \sin\left(x\right)^{3} + \sin\left(x\right)}+C}
\]
\end{problem}}%}

