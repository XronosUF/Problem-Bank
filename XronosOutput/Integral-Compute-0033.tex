\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@TrigInt, Func@Trig, Sub@OddOdd, File@0033}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{9}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{9 \, \cos\left(x\right)^{2} - 7}{7 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{3}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{7 \, \cos\left(x\right)^{2} - 5}{35 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{4}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{4}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{5}\tan^{3}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{7 \, \cos\left(x\right)^{2} - 5}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{3}(x)\sec^{9}(x)\;dx} = \answer{{\frac{11 \, \cos\left(x\right)^{2} - 9}{99 \, \cos\left(x\right)^{11}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-7}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{7}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{9 \, \cos\left(x\right)^{2} - 7}{63 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-6}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{6}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-7}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{\frac{9 \, \cos\left(x\right)^{2} - 7}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{1}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-9}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{9}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{9 \, \cos\left(x\right)^{2} - 7}{21 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{1}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{2}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{2}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{3}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{3}(x)\sec^{9}(x)\;dx} = \answer{{\frac{11 \, \cos\left(x\right)^{2} - 9}{33 \, \cos\left(x\right)^{11}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{3}(x)\sec^{5}(x)\;dx} = \answer{{\frac{7 \, \cos\left(x\right)^{2} - 5}{35 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{1}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{7}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{7}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{7}\tan^{3}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{7 \, \cos\left(x\right)^{2} - 5}{5 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{1}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-5}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{5}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-8}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{8}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-7}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{1}{\cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{3}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-2}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{2}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{3}(x)\sec^{1}(x)\;dx} = \answer{{\frac{3 \, \cos\left(x\right)^{2} - 1}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-9}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{9}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-2}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{2}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{\frac{9 \, \cos\left(x\right)^{2} - 7}{63 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{8}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{8}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{5}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{5}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-9}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{\frac{9 \, \cos\left(x\right)^{2} - 7}{7 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{5}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{5}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-7}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{7}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{3}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{5 \, \cos\left(x\right)^{2} - 3}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{3}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{11 \, \cos\left(x\right)^{2} - 9}{33 \, \cos\left(x\right)^{11}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-7}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{7}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-8}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{8}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{9}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{9}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{7}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{7}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{7}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{1}{\cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-2}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{2}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{4}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{4}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{9}\tan^{3}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{11 \, \cos\left(x\right)^{2} - 9}{11 \, \cos\left(x\right)^{11}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-4}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{4}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-2}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{2}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{6}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{2}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-4}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{4}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{1}{3 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-7}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{7}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{1}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{4}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{4}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{3}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{3 \, \cos\left(x\right)^{2} - 1}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{3}(x)\sec^{3}(x)\;dx} = \answer{{\frac{5 \, \cos\left(x\right)^{2} - 3}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{1}{3 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-6}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{6}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{1}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{3}(x)\sec^{1}(x)\;dx} = \answer{{\frac{3 \, \cos\left(x\right)^{2} - 1}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{3}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{8}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{8}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-5}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{5}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-4}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{4}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-9}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{3}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{6}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{2}{3 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{8}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{8}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-9}\tan^{3}(x)\sec^{9}(x)\;dx} = \answer{{\frac{11 \, \cos\left(x\right)^{2} - 9}{11 \, \cos\left(x\right)^{11}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-2}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{2}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{2}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{2}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{3}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{8}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{8}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{3}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-7}\tan^{3}(x)\sec^{5}(x)\;dx} = \answer{{\frac{7 \, \cos\left(x\right)^{2} - 5}{5 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{2}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{2}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-5}\tan^{3}(x)\sec^{5}(x)\;dx} = \answer{{\frac{7 \, \cos\left(x\right)^{2} - 5}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{4}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{4}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-6}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{2}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{2}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{2}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{7}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{7}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{9}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{3}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-5}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{1}{\cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{1}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{8}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{8}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{6}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{\frac{6}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-8}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{8}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{5}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{1}{\cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{9}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{9}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{9}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{1}{\cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{1}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{3}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{1}{\cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{6}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{6}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-9}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{9}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{9}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{9}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-5}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{5}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-9}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{1}{\cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{\frac{9 \, \cos\left(x\right)^{2} - 7}{21 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{5}\tan^{3}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{5 \, \cos\left(x\right)^{2} - 3}{3 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{5}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{5}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{2}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{\frac{2}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-5}\tan^{3}(x)\sec^{3}(x)\;dx} = \answer{{\frac{5 \, \cos\left(x\right)^{2} - 3}{3 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-8}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{8}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{1}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-4}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{4}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-3}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{3}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{1}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{5}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{\frac{5}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{1}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{6}\tan^{1}(x)\sec^{1}(x)\;dx} = \answer{{\frac{6}{\cos\left(x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-6}\tan^{1}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{2}{3 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{7}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{7}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{3}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{5 \, \cos\left(x\right)^{2} - 3}{15 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{4}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{\frac{4}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-1}\tan^{3}(x)\sec^{3}(x)\;dx} = \answer{{\frac{5 \, \cos\left(x\right)^{2} - 3}{15 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{3}(x)\sec^{9}(x)\;dx} = \answer{{-\frac{11 \, \cos\left(x\right)^{2} - 9}{99 \, \cos\left(x\right)^{11}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{7}\tan^{3}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{9 \, \cos\left(x\right)^{2} - 7}{9 \, \cos\left(x\right)^{9}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-6}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{6}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-4}\tan^{1}(x)\sec^{7}(x)\;dx} = \answer{{-\frac{4}{7 \, \cos\left(x\right)^{7}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{1}\tan^{3}(x)\sec^{1}(x)\;dx} = \answer{{-\frac{3 \, \cos\left(x\right)^{2} - 1}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-8}\tan^{1}(x)\sec^{5}(x)\;dx} = \answer{{-\frac{8}{5 \, \cos\left(x\right)^{5}}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0033. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0033.HELP.tex}

\[
\int{{-5}\tan^{1}(x)\sec^{3}(x)\;dx} = \answer{{-\frac{5}{3 \, \cos\left(x\right)^{3}}}+C}
\]
\end{problem}}%}

