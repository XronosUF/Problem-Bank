\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Func@Poly, Func@Log, Sub@ByParts, File@0024}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{6} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{9}{7} \, x^{7} \log\left(5 \, x\right) - \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{7}{2} \, x^{2} \log\left(-7 \, x\right) + \frac{7}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{2} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{5}{3} \, x^{3} \log\left(2 \, x\right) + \frac{5}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{8} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{9} \, x^{9} \log\left(-3 \, x\right) - \frac{1}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{5} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{6} \log\left(4 \, x\right) + \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{9} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{4}{5} \, x^{10} \log\left(-5 \, x\right) - \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{5} \log\left(-4 \, x\right)}\;dx} = \answer{{-x^{6} \log\left(-4 \, x\right) + \frac{1}{6} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{9} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{9}{10} \, x^{10} \log\left(6 \, x\right) - \frac{9}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{7} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{8} \log\left(-5 \, x\right) - \frac{1}{16} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{7} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{5}{8} \, x^{8} \log\left(3 \, x\right) - \frac{5}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{3} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{4} \log\left(5 \, x\right) + \frac{1}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(-8 \, x\right)}\;dx} = \answer{{-x^{2} \log\left(-8 \, x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{4} \log\left(-x\right)}\;dx} = \answer{{-\frac{6}{5} \, x^{5} \log\left(-x\right) + \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{3} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{7}{4} \, x^{4} \log\left(5 \, x\right) - \frac{7}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{6} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{4}{7} \, x^{7} \log\left(-8 \, x\right) - \frac{4}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{8} \log\left(x\right)}\;dx} = \answer{{-\frac{4}{9} \, x^{9} \log\left(x\right) + \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{2} \log\left(-x\right)}\;dx} = \answer{{-\frac{8}{3} \, x^{3} \log\left(-x\right) + \frac{8}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{9} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{10} \log\left(-3 \, x\right) + \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(x\right)}\;dx} = \answer{{-x^{2} \log\left(x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{9} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{10} \log\left(-2 \, x\right) - \frac{1}{20} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{6} \log\left(x\right)}\;dx} = \answer{{-\frac{9}{7} \, x^{7} \log\left(x\right) + \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{4}{5} \, x^{5} \log\left(8 \, x\right) - \frac{4}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{5} \log\left(8 \, x\right) + \frac{4}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{3} \log\left(-x\right)}\;dx} = \answer{{-\frac{5}{4} \, x^{4} \log\left(-x\right) + \frac{5}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x \log\left(-7 \, x\right)}\;dx} = \answer{{4 \, x^{2} \log\left(-7 \, x\right) - 2 \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{5} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{5}{6} \, x^{6} \log\left(-9 \, x\right) + \frac{5}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{4} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{8}{5} \, x^{5} \log\left(2 \, x\right) - \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{5} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{5}{6} \, x^{6} \log\left(3 \, x\right) - \frac{5}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{9} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{7}{10} \, x^{10} \log\left(-9 \, x\right) - \frac{7}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{3} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{4} \log\left(-5 \, x\right) + \frac{1}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{5} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{6} \log\left(-7 \, x\right) + \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{2} \log\left(-8 \, x\right)}\;dx} = \answer{{-2 \, x^{3} \log\left(-8 \, x\right) + \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{5} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{7}{6} \, x^{6} \log\left(-3 \, x\right) - \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{5} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{5}{6} \, x^{6} \log\left(-8 \, x\right) + \frac{5}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{6} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{5}{7} \, x^{7} \log\left(2 \, x\right) - \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{8} \log\left(x\right)}\;dx} = \answer{{-\frac{7}{9} \, x^{9} \log\left(x\right) + \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{8} \log\left(-x\right)}\;dx} = \answer{{-\frac{1}{9} \, x^{9} \log\left(-x\right) + \frac{1}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{5} \log\left(x\right)}\;dx} = \answer{{\frac{7}{6} \, x^{6} \log\left(x\right) - \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{5} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{6} \log\left(4 \, x\right) - \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{6} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{2}{7} \, x^{7} \log\left(-7 \, x\right) + \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{7} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{1}{8} \, x^{8} \log\left(-4 \, x\right) - \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{3} \log\left(-4 \, x\right)}\;dx} = \answer{{-x^{4} \log\left(-4 \, x\right) + \frac{1}{4} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x \log\left(-2 \, x\right)}\;dx} = \answer{{-4 \, x^{2} \log\left(-2 \, x\right) + 2 \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{4} \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{9}{5} \, x^{5} \log\left(-4 \, x\right) + \frac{9}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{2} \log\left(-x\right)}\;dx} = \answer{{-3 \, x^{3} \log\left(-x\right) + x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{3} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{4} \log\left(-9 \, x\right) + \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{8} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{9} \log\left(-2 \, x\right) - \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{8} \log\left(x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{9} \log\left(x\right) + \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{6} \log\left(x\right)}\;dx} = \answer{{-\frac{3}{7} \, x^{7} \log\left(x\right) + \frac{3}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{3} \log\left(-5 \, x\right)}\;dx} = \answer{{2 \, x^{4} \log\left(-5 \, x\right) - \frac{1}{2} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{5} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{6} \log\left(3 \, x\right) + \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{2} \log\left(7 \, x\right) + \frac{1}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{5} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{4}{3} \, x^{6} \log\left(-7 \, x\right) + \frac{2}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{8} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{4}{9} \, x^{9} \log\left(-5 \, x\right) + \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(-7 \, x\right)}\;dx} = \answer{{-x^{2} \log\left(-7 \, x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{8} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{1}{9} \, x^{9} \log\left(-8 \, x\right) - \frac{1}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{2} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{8}{3} \, x^{3} \log\left(3 \, x\right) - \frac{8}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{5} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{7}{6} \, x^{6} \log\left(3 \, x\right) - \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{6} \log\left(7 \, x\right)}\;dx} = \answer{{x^{7} \log\left(7 \, x\right) - \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{5} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{6} \log\left(6 \, x\right) + \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{6} \log\left(-5 \, x\right)}\;dx} = \answer{{x^{7} \log\left(-5 \, x\right) - \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{2} \log\left(-2 \, x\right)}\;dx} = \answer{{2 \, x^{3} \log\left(-2 \, x\right) - \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{5} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{6} \log\left(7 \, x\right) - \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{8} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{4}{9} \, x^{9} \log\left(9 \, x\right) - \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x \log\left(4 \, x\right)}\;dx} = \answer{{\frac{9}{2} \, x^{2} \log\left(4 \, x\right) - \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{9} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, x^{10} \log\left(-9 \, x\right) - \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{8} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{4}{9} \, x^{9} \log\left(-2 \, x\right) + \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{8} \log\left(-x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{9} \log\left(-x\right) - \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{9} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{10} \log\left(2 \, x\right) - \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{8} \log\left(-7 \, x\right)}\;dx} = \answer{{x^{9} \log\left(-7 \, x\right) - \frac{1}{9} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{7} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{9}{8} \, x^{8} \log\left(-6 \, x\right) + \frac{9}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{4} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, x^{5} \log\left(-2 \, x\right) + \frac{1}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{2} \log\left(-x\right)}\;dx} = \answer{{2 \, x^{3} \log\left(-x\right) - \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{4} \log\left(x\right)}\;dx} = \answer{{-\frac{6}{5} \, x^{5} \log\left(x\right) + \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{9} \log\left(-x\right)}\;dx} = \answer{{-\frac{3}{10} \, x^{10} \log\left(-x\right) + \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{8} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{2}{9} \, x^{9} \log\left(-6 \, x\right) + \frac{2}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{4} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{8}{5} \, x^{5} \log\left(9 \, x\right) - \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{3} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{4} \log\left(4 \, x\right) + \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{9} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{9}{10} \, x^{10} \log\left(6 \, x\right) + \frac{9}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{7} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{9}{8} \, x^{8} \log\left(-7 \, x\right) - \frac{9}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{7} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{8} \log\left(-7 \, x\right) + \frac{1}{16} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{3} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{9}{4} \, x^{4} \log\left(2 \, x\right) + \frac{9}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{4} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{8}{5} \, x^{5} \log\left(-5 \, x\right) + \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{6} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{9}{7} \, x^{7} \log\left(-5 \, x\right) - \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{6} \log\left(8 \, x\right)}\;dx} = \answer{{x^{7} \log\left(8 \, x\right) - \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{2} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{8}{3} \, x^{3} \log\left(-7 \, x\right) + \frac{8}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{7} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{5}{8} \, x^{8} \log\left(-3 \, x\right) - \frac{5}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{8} \log\left(x\right)}\;dx} = \answer{{-\frac{2}{9} \, x^{9} \log\left(x\right) + \frac{2}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{5} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{7}{6} \, x^{6} \log\left(6 \, x\right) + \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{9} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{10} \log\left(4 \, x\right) + \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{5} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{6} \log\left(4 \, x\right) + \frac{1}{12} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{8} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{5}{9} \, x^{9} \log\left(7 \, x\right) - \frac{5}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{3} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{4} \log\left(-7 \, x\right) + \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{4} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{8}{5} \, x^{5} \log\left(-3 \, x\right) + \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{5} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{6} \log\left(4 \, x\right) - \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{8} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{9} \log\left(8 \, x\right) - \frac{2}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{9} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, x^{10} \log\left(5 \, x\right) - \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{7} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{7}{8} \, x^{8} \log\left(8 \, x\right) + \frac{7}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{9} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{1}{10} \, x^{10} \log\left(5 \, x\right) + \frac{1}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{5} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{7}{6} \, x^{6} \log\left(5 \, x\right) + \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{5} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{6} \log\left(-5 \, x\right) - \frac{1}{12} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{5} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{6} \log\left(8 \, x\right) - \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{8}{5} \, x^{5} \log\left(8 \, x\right) + \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{7} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{8} \log\left(5 \, x\right) + \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x \log\left(-5 \, x\right)}\;dx} = \answer{{x^{2} \log\left(-5 \, x\right) - \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{5} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{6} \log\left(-7 \, x\right) - \frac{1}{12} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{8} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{8}{9} \, x^{9} \log\left(5 \, x\right) - \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{7} \log\left(x\right)}\;dx} = \answer{{\frac{9}{8} \, x^{8} \log\left(x\right) - \frac{9}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{3} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{3}{4} \, x^{4} \log\left(7 \, x\right) - \frac{3}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{4} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{4}{5} \, x^{5} \log\left(2 \, x\right) - \frac{4}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x \log\left(-x\right)}\;dx} = \answer{{3 \, x^{2} \log\left(-x\right) - \frac{3}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{4} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, x^{5} \log\left(7 \, x\right) + \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x \log\left(6 \, x\right)}\;dx} = \answer{{x^{2} \log\left(6 \, x\right) - \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{5} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{5}{6} \, x^{6} \log\left(8 \, x\right) + \frac{5}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{5} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{7}{6} \, x^{6} \log\left(4 \, x\right) - \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{6} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{5}{7} \, x^{7} \log\left(6 \, x\right) - \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{2} \log\left(-4 \, x\right)}\;dx} = \answer{{-2 \, x^{3} \log\left(-4 \, x\right) + \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{2} \log\left(-9 \, x\right) + \frac{3}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{6} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{4}{7} \, x^{7} \log\left(-4 \, x\right) - \frac{4}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{4} \log\left(-x\right)}\;dx} = \answer{{-\frac{8}{5} \, x^{5} \log\left(-x\right) + \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{6} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{8}{7} \, x^{7} \log\left(6 \, x\right) - \frac{8}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{8} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{4}{9} \, x^{9} \log\left(5 \, x\right) - \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{2} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{4}{3} \, x^{3} \log\left(7 \, x\right) - \frac{4}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{2} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{4}{3} \, x^{3} \log\left(-9 \, x\right) + \frac{4}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{2} \log\left(-x\right)}\;dx} = \answer{{-x^{3} \log\left(-x\right) + \frac{1}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{8} \log\left(x\right)}\;dx} = \answer{{-\frac{5}{9} \, x^{9} \log\left(x\right) + \frac{5}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{4} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{6}{5} \, x^{5} \log\left(-9 \, x\right) + \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x \log\left(8 \, x\right)}\;dx} = \answer{{\frac{7}{2} \, x^{2} \log\left(8 \, x\right) - \frac{7}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{9} \log\left(-x\right)}\;dx} = \answer{{-\frac{1}{10} \, x^{10} \log\left(-x\right) + \frac{1}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{7} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{3}{4} \, x^{8} \log\left(-3 \, x\right) - \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{8} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{4}{9} \, x^{9} \log\left(-8 \, x\right) - \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{7} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{1}{8} \, x^{8} \log\left(7 \, x\right) + \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{7} \log\left(-6 \, x\right)}\;dx} = \answer{{\frac{3}{8} \, x^{8} \log\left(-6 \, x\right) - \frac{3}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{8} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{9} \log\left(7 \, x\right) - \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{3} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{4} \log\left(8 \, x\right) + \frac{1}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{9} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{10} \log\left(4 \, x\right) + \frac{1}{20} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{7} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{9}{8} \, x^{8} \log\left(8 \, x\right) + \frac{9}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{9} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{10} \log\left(9 \, x\right) - \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{4} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{6}{5} \, x^{5} \log\left(-6 \, x\right) + \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{3} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{4} \log\left(8 \, x\right) + \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{8} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{5}{9} \, x^{9} \log\left(4 \, x\right) - \frac{5}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{8} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{8}{9} \, x^{9} \log\left(-2 \, x\right) - \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{9} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, x^{10} \log\left(4 \, x\right) + \frac{1}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{6} \log\left(-8 \, x\right)}\;dx} = \answer{{x^{7} \log\left(-8 \, x\right) - \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{9} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{10} \log\left(-8 \, x\right) + \frac{1}{20} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{4} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{5} \log\left(4 \, x\right) - \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{3} \log\left(3 \, x\right)}\;dx} = \answer{{-x^{4} \log\left(3 \, x\right) + \frac{1}{4} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x \log\left(5 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{2} \log\left(5 \, x\right) - \frac{3}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{5} \log\left(6 \, x\right)}\;dx} = \answer{{-x^{6} \log\left(6 \, x\right) + \frac{1}{6} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{4} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{7}{5} \, x^{5} \log\left(6 \, x\right) + \frac{7}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{2} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{3} \log\left(6 \, x\right) + \frac{1}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{5} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{7}{6} \, x^{6} \log\left(7 \, x\right) + \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{2} \log\left(7 \, x\right)}\;dx} = \answer{{2 \, x^{3} \log\left(7 \, x\right) - \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{5} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{7}{6} \, x^{6} \log\left(-3 \, x\right) + \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{8} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{8}{9} \, x^{9} \log\left(7 \, x\right) - \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{9} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{10} \log\left(3 \, x\right) + \frac{3}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{6} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{6}{7} \, x^{7} \log\left(-3 \, x\right) + \frac{6}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{3} \log\left(-x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{4} \log\left(-x\right) + \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{2} \log\left(-3 \, x\right) - \frac{3}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{3} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{5}{4} \, x^{4} \log\left(8 \, x\right) + \frac{5}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{2} \log\left(6 \, x\right)}\;dx} = \answer{{-2 \, x^{3} \log\left(6 \, x\right) + \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{5} \log\left(-x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{6} \log\left(-x\right) + \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{8} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{4}{9} \, x^{9} \log\left(-3 \, x\right) + \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{9} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, x^{10} \log\left(6 \, x\right) - \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{2} \log\left(5 \, x\right)}\;dx} = \answer{{3 \, x^{3} \log\left(5 \, x\right) - x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{3} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{4} \log\left(-5 \, x\right) - \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x \log\left(-8 \, x\right)}\;dx} = \answer{{x^{2} \log\left(-8 \, x\right) - \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{5} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{6} \log\left(2 \, x\right) + \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{5} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{4}{3} \, x^{6} \log\left(8 \, x\right) + \frac{2}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{9} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{1}{10} \, x^{10} \log\left(8 \, x\right) - \frac{1}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{9} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{10} \log\left(3 \, x\right) - \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{5} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{1}{6} \, x^{6} \log\left(5 \, x\right) - \frac{1}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{6} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{9}{7} \, x^{7} \log\left(6 \, x\right) - \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{8} \log\left(6 \, x\right)}\;dx} = \answer{{x^{9} \log\left(6 \, x\right) - \frac{1}{9} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{9} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{10} \log\left(2 \, x\right) + \frac{3}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{3} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{4} \log\left(3 \, x\right) + \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x \log\left(3 \, x\right)}\;dx} = \answer{{2 \, x^{2} \log\left(3 \, x\right) - x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{6} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{3}{7} \, x^{7} \log\left(3 \, x\right) + \frac{3}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{9} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{10} \log\left(-6 \, x\right) + \frac{1}{20} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{7} \log\left(8 \, x\right)}\;dx} = \answer{{-x^{8} \log\left(8 \, x\right) + \frac{1}{8} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{9} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, x^{10} \log\left(3 \, x\right) + \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{5} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{6} \log\left(9 \, x\right) - \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{4} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{3}{5} \, x^{5} \log\left(-9 \, x\right) - \frac{3}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{6} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{3}{7} \, x^{7} \log\left(8 \, x\right) - \frac{3}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{8} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{7}{9} \, x^{9} \log\left(-2 \, x\right) + \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{3} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{4} \, x^{4} \log\left(-3 \, x\right) - \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{3} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{5}{4} \, x^{4} \log\left(-2 \, x\right) + \frac{5}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{4} \log\left(x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{5} \log\left(x\right) - \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{8} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{8}{9} \, x^{9} \log\left(9 \, x\right) + \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{6} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{5}{7} \, x^{7} \log\left(-5 \, x\right) + \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{3} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{5}{4} \, x^{4} \log\left(-3 \, x\right) + \frac{5}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{3} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{4} \log\left(6 \, x\right) - \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{3} \log\left(-9 \, x\right)}\;dx} = \answer{{2 \, x^{4} \log\left(-9 \, x\right) - \frac{1}{2} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{7} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{9}{8} \, x^{8} \log\left(-4 \, x\right) - \frac{9}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{8} \log\left(-7 \, x\right)}\;dx} = \answer{{-x^{9} \log\left(-7 \, x\right) + \frac{1}{9} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{6} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{4}{7} \, x^{7} \log\left(-3 \, x\right) - \frac{4}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{5} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{2}{3} \, x^{6} \log\left(3 \, x\right) + \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{3} \log\left(-x\right)}\;dx} = \answer{{-\frac{7}{4} \, x^{4} \log\left(-x\right) + \frac{7}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{2} \log\left(x\right)}\;dx} = \answer{{-3 \, x^{3} \log\left(x\right) + x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{3} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{1}{4} \, x^{4} \log\left(-7 \, x\right) - \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{2} \log\left(-5 \, x\right) + \frac{3}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{6} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{5}{7} \, x^{7} \log\left(7 \, x\right) - \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{9} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{1}{5} \, x^{10} \log\left(3 \, x\right) - \frac{1}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{8} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{9} \log\left(9 \, x\right) + \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{5} \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{6} \log\left(-4 \, x\right) + \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{9} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{3}{10} \, x^{10} \log\left(-2 \, x\right) + \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{8} \log\left(-3 \, x\right)}\;dx} = \answer{{-x^{9} \log\left(-3 \, x\right) + \frac{1}{9} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{9} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{1}{10} \, x^{10} \log\left(9 \, x\right) + \frac{1}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x \log\left(x\right)}\;dx} = \answer{{2 \, x^{2} \log\left(x\right) - x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{9} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{10} \log\left(8 \, x\right) - \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{8} \log\left(-9 \, x\right)}\;dx} = \answer{{-x^{9} \log\left(-9 \, x\right) + \frac{1}{9} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{9} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{10} \log\left(-9 \, x\right) - \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{9} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{7}{10} \, x^{10} \log\left(6 \, x\right) - \frac{7}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{4} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{7}{5} \, x^{5} \log\left(7 \, x\right) - \frac{7}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{4} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{5} \log\left(2 \, x\right) + \frac{4}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{2} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{8}{3} \, x^{3} \log\left(9 \, x\right) - \frac{8}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{6} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{4}{7} \, x^{7} \log\left(-7 \, x\right) - \frac{4}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{8} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{7}{9} \, x^{9} \log\left(3 \, x\right) + \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x \log\left(2 \, x\right)}\;dx} = \answer{{-3 \, x^{2} \log\left(2 \, x\right) + \frac{3}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{6} \log\left(-x\right)}\;dx} = \answer{{\frac{8}{7} \, x^{7} \log\left(-x\right) - \frac{8}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{2} \log\left(-9 \, x\right) + \frac{1}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{7} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{1}{8} \, x^{8} \log\left(9 \, x\right) - \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{6} \log\left(x\right)}\;dx} = \answer{{-\frac{8}{7} \, x^{7} \log\left(x\right) + \frac{8}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{5} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{2}{3} \, x^{6} \log\left(-9 \, x\right) + \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{5} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{1}{6} \, x^{6} \log\left(-2 \, x\right) - \frac{1}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{5} \log\left(-6 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{6} \log\left(-6 \, x\right) - \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{6} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{1}{7} \, x^{7} \log\left(2 \, x\right) + \frac{1}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{8} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{7}{9} \, x^{9} \log\left(7 \, x\right) - \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{9} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{10} \log\left(-3 \, x\right) - \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{5} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{6} \log\left(5 \, x\right) - \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{5} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{4}{3} \, x^{6} \log\left(2 \, x\right) + \frac{2}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{5} \log\left(-9 \, x\right)}\;dx} = \answer{{-x^{6} \log\left(-9 \, x\right) + \frac{1}{6} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{2} \log\left(-6 \, x\right)}\;dx} = \answer{{2 \, x^{3} \log\left(-6 \, x\right) - \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{7} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{1}{8} \, x^{8} \log\left(4 \, x\right) + \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{7} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{8} \log\left(9 \, x\right) + \frac{1}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{4} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{7}{5} \, x^{5} \log\left(9 \, x\right) + \frac{7}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{4} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{8}{5} \, x^{5} \log\left(3 \, x\right) - \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{9} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{3}{5} \, x^{10} \log\left(5 \, x\right) - \frac{3}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{7} \log\left(x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{8} \log\left(x\right) - \frac{1}{16} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{9} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, x^{10} \log\left(3 \, x\right) - \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{7}{2} \, x^{2} \log\left(3 \, x\right) + \frac{7}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(-2 \, x\right)}\;dx} = \answer{{-x^{2} \log\left(-2 \, x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{6} \log\left(-6 \, x\right)}\;dx} = \answer{{\frac{1}{7} \, x^{7} \log\left(-6 \, x\right) - \frac{1}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(-5 \, x\right)}\;dx} = \answer{{-x^{2} \log\left(-5 \, x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{3} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{4} \log\left(-6 \, x\right) + \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{6} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{2}{7} \, x^{7} \log\left(6 \, x\right) + \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{3} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{4} \log\left(-5 \, x\right) + \frac{3}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{3} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{5}{4} \, x^{4} \log\left(8 \, x\right) - \frac{5}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{9} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{9}{10} \, x^{10} \log\left(-4 \, x\right) - \frac{9}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{3} \log\left(-x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{4} \log\left(-x\right) + \frac{3}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{4} \log\left(3 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{5} \log\left(3 \, x\right) - \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{2} \log\left(-2 \, x\right) - \frac{3}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{9} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{3}{10} \, x^{10} \log\left(5 \, x\right) + \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{3} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{4} \log\left(-7 \, x\right) + \frac{1}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{5} \log\left(9 \, x\right)}\;dx} = \answer{{-x^{6} \log\left(9 \, x\right) + \frac{1}{6} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{5} \log\left(7 \, x\right)}\;dx} = \answer{{x^{6} \log\left(7 \, x\right) - \frac{1}{6} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{9} \log\left(-6 \, x\right)}\;dx} = \answer{{\frac{9}{10} \, x^{10} \log\left(-6 \, x\right) - \frac{9}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{4} \log\left(x\right)}\;dx} = \answer{{\frac{6}{5} \, x^{5} \log\left(x\right) - \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{4} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{7}{5} \, x^{5} \log\left(4 \, x\right) + \frac{7}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{3} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{9}{4} \, x^{4} \log\left(4 \, x\right) - \frac{9}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{8} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{9} \log\left(-2 \, x\right) + \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{5} \log\left(-x\right)}\;dx} = \answer{{x^{6} \log\left(-x\right) - \frac{1}{6} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{8} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{1}{9} \, x^{9} \log\left(-6 \, x\right) + \frac{1}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{5} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{6} \log\left(-2 \, x\right) - \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{6} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{4}{7} \, x^{7} \log\left(-8 \, x\right) + \frac{4}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{6} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{8}{7} \, x^{7} \log\left(5 \, x\right) - \frac{8}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{4} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{5} \log\left(-2 \, x\right) + \frac{3}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{9} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{10} \log\left(-9 \, x\right) + \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{9} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, x^{10} \log\left(2 \, x\right) - \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{4} \log\left(-x\right)}\;dx} = \answer{{-\frac{2}{5} \, x^{5} \log\left(-x\right) + \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{7} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{1}{4} \, x^{8} \log\left(8 \, x\right) - \frac{1}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{3} \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{4} \log\left(-4 \, x\right) + \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{2} \log\left(-x\right)}\;dx} = \answer{{-\frac{4}{3} \, x^{3} \log\left(-x\right) + \frac{4}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{6} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{5}{7} \, x^{7} \log\left(-8 \, x\right) + \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{3} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{7}{4} \, x^{4} \log\left(4 \, x\right) - \frac{7}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{4} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{9}{5} \, x^{5} \log\left(-2 \, x\right) + \frac{9}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{4} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{7}{5} \, x^{5} \log\left(-5 \, x\right) - \frac{7}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{6} \log\left(-x\right)}\;dx} = \answer{{x^{7} \log\left(-x\right) - \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{7} \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{9}{8} \, x^{8} \log\left(-4 \, x\right) + \frac{9}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{3} \log\left(x\right)}\;dx} = \answer{{\frac{9}{4} \, x^{4} \log\left(x\right) - \frac{9}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{5} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{6} \log\left(-9 \, x\right) - \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{9} \log\left(-x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{10} \log\left(-x\right) + \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{5} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{6} \log\left(8 \, x\right) - \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{5} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{1}{6} \, x^{6} \log\left(-6 \, x\right) + \frac{1}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{9} \log\left(x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{10} \log\left(x\right) + \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{4} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{9}{5} \, x^{5} \log\left(7 \, x\right) - \frac{9}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{4} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, x^{5} \log\left(9 \, x\right) + \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{7} \log\left(x\right)}\;dx} = \answer{{\frac{3}{8} \, x^{8} \log\left(x\right) - \frac{3}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{5} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{6} \log\left(6 \, x\right) + \frac{1}{12} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{4} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{6}{5} \, x^{5} \log\left(-9 \, x\right) - \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x \log\left(x\right)}\;dx} = \answer{{-\frac{9}{2} \, x^{2} \log\left(x\right) + \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{9} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, x^{10} \log\left(-5 \, x\right) + \frac{1}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{9} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{4}{5} \, x^{10} \log\left(5 \, x\right) - \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{7} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{8} \log\left(9 \, x\right) + \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{5} \log\left(-x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{6} \log\left(-x\right) - \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{5} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{6} \log\left(2 \, x\right) - \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{5} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{6} \, x^{6} \log\left(-3 \, x\right) - \frac{1}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{7}{2} \, x^{2} \log\left(-8 \, x\right) + \frac{7}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{x^{5} \log\left(8 \, x\right) - \frac{1}{5} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{8} \log\left(-6 \, x\right)}\;dx} = \answer{{-x^{9} \log\left(-6 \, x\right) + \frac{1}{9} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{7} \log\left(x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{8} \log\left(x\right) + \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{5} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{6} \log\left(8 \, x\right) - \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{9} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, x^{10} \log\left(-7 \, x\right) - \frac{3}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{3} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{4} \log\left(3 \, x\right) + \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{3} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{4} \log\left(-5 \, x\right) - \frac{1}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{2} \log\left(-8 \, x\right)}\;dx} = \answer{{x^{3} \log\left(-8 \, x\right) - \frac{1}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{8} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{9} \log\left(-7 \, x\right) + \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{8} \log\left(6 \, x\right)}\;dx} = \answer{{-x^{9} \log\left(6 \, x\right) + \frac{1}{9} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{3} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{7}{4} \, x^{4} \log\left(-5 \, x\right) + \frac{7}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{7} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{1}{8} \, x^{8} \log\left(-9 \, x\right) - \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{3} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{4} \log\left(8 \, x\right) + \frac{3}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{8} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{2}{9} \, x^{9} \log\left(7 \, x\right) - \frac{2}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{6} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{1}{7} \, x^{7} \log\left(-6 \, x\right) + \frac{1}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{6} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{6}{7} \, x^{7} \log\left(-8 \, x\right) + \frac{6}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{2} \log\left(9 \, x\right)}\;dx} = \answer{{2 \, x^{3} \log\left(9 \, x\right) - \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{8} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{7}{9} \, x^{9} \log\left(9 \, x\right) + \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{4} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{8}{5} \, x^{5} \log\left(-8 \, x\right) - \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{3} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{3}{4} \, x^{4} \log\left(-4 \, x\right) - \frac{3}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x \log\left(6 \, x\right)}\;dx} = \answer{{3 \, x^{2} \log\left(6 \, x\right) - \frac{3}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{3} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{4} \log\left(-3 \, x\right) - \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{3} \log\left(-x\right)}\;dx} = \answer{{\frac{9}{4} \, x^{4} \log\left(-x\right) - \frac{9}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{3} \log\left(9 \, x\right)}\;dx} = \answer{{2 \, x^{4} \log\left(9 \, x\right) - \frac{1}{2} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{4} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{9}{5} \, x^{5} \log\left(7 \, x\right) + \frac{9}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{4} \log\left(7 \, x\right)}\;dx} = \answer{{-x^{5} \log\left(7 \, x\right) + \frac{1}{5} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{8} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{7}{9} \, x^{9} \log\left(5 \, x\right) - \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{6} \log\left(8 \, x\right)}\;dx} = \answer{{-x^{7} \log\left(8 \, x\right) + \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{2} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{4}{3} \, x^{3} \log\left(2 \, x\right) + \frac{4}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{8} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{7}{9} \, x^{9} \log\left(7 \, x\right) + \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{5} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{4}{3} \, x^{6} \log\left(9 \, x\right) - \frac{2}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{4} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{5} \log\left(-5 \, x\right) - \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{8} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{9} \log\left(4 \, x\right) - \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{8} \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{2}{3} \, x^{9} \log\left(-4 \, x\right) + \frac{2}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{2} \log\left(8 \, x\right)}\;dx} = \answer{{x^{3} \log\left(8 \, x\right) - \frac{1}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{8} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{8}{9} \, x^{9} \log\left(7 \, x\right) + \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{2} \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{8}{3} \, x^{3} \log\left(-5 \, x\right) - \frac{8}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{9} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{10} \log\left(6 \, x\right) - \frac{1}{20} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(6 \, x\right)}\;dx} = \answer{{-x^{2} \log\left(6 \, x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{7} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{8} \log\left(-9 \, x\right) + \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{9} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{10} \log\left(-7 \, x\right) - \frac{1}{20} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{6} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{9}{7} \, x^{7} \log\left(6 \, x\right) + \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{8} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{5}{9} \, x^{9} \log\left(9 \, x\right) - \frac{5}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{9} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{9}{10} \, x^{10} \log\left(2 \, x\right) + \frac{9}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, x^{5} \log\left(8 \, x\right) + \frac{1}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{3} \log\left(6 \, x\right)}\;dx} = \answer{{x^{4} \log\left(6 \, x\right) - \frac{1}{4} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{6}{5} \, x^{5} \log\left(8 \, x\right) + \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x \log\left(-5 \, x\right)}\;dx} = \answer{{\frac{9}{2} \, x^{2} \log\left(-5 \, x\right) - \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{7}{5} \, x^{5} \log\left(8 \, x\right) - \frac{7}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{9} \log\left(-2 \, x\right)}\;dx} = \answer{{-\frac{4}{5} \, x^{10} \log\left(-2 \, x\right) + \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{7} \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{1}{8} \, x^{8} \log\left(-4 \, x\right) + \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x \log\left(-4 \, x\right)}\;dx} = \answer{{2 \, x^{2} \log\left(-4 \, x\right) - x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{4} \log\left(-x\right)}\;dx} = \answer{{\frac{9}{5} \, x^{5} \log\left(-x\right) - \frac{9}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{4} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, x^{5} \log\left(4 \, x\right) + \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{4} \log\left(x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{5} \log\left(x\right) + \frac{3}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x \log\left(-9 \, x\right)}\;dx} = \answer{{x^{2} \log\left(-9 \, x\right) - \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x \log\left(9 \, x\right)}\;dx} = \answer{{-2 \, x^{2} \log\left(9 \, x\right) + x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{6} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{6}{7} \, x^{7} \log\left(-7 \, x\right) + \frac{6}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{2} \log\left(-4 \, x\right) + \frac{1}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{7} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{5}{8} \, x^{8} \log\left(3 \, x\right) + \frac{5}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{8} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{9} \log\left(-3 \, x\right) + \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{2} \log\left(7 \, x\right)}\;dx} = \answer{{-3 \, x^{3} \log\left(7 \, x\right) + x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{2} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{7}{3} \, x^{3} \log\left(2 \, x\right) - \frac{7}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{2} \log\left(-3 \, x\right) - \frac{1}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{9}{2} \, x^{2} \log\left(4 \, x\right) + \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{2} \log\left(9 \, x\right)}\;dx} = \answer{{-2 \, x^{3} \log\left(9 \, x\right) + \frac{2}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{7} \log\left(-8 \, x\right)}\;dx} = \answer{{x^{8} \log\left(-8 \, x\right) - \frac{1}{8} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{5} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{6} \log\left(4 \, x\right) + \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{3} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{7}{4} \, x^{4} \log\left(6 \, x\right) - \frac{7}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{3} \log\left(9 \, x\right)}\;dx} = \answer{{-2 \, x^{4} \log\left(9 \, x\right) + \frac{1}{2} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{7} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{8} \log\left(7 \, x\right) + \frac{1}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{2} \log\left(-6 \, x\right)}\;dx} = \answer{{\frac{4}{3} \, x^{3} \log\left(-6 \, x\right) - \frac{4}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{5} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{2}{3} \, x^{6} \log\left(-8 \, x\right) + \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{6} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{5}{7} \, x^{7} \log\left(-9 \, x\right) + \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{3} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{4} \log\left(-8 \, x\right) + \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{4} \log\left(-x\right)}\;dx} = \answer{{-\frac{7}{5} \, x^{5} \log\left(-x\right) + \frac{7}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{5} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{6} \log\left(4 \, x\right) - \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{9} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{7}{10} \, x^{10} \log\left(3 \, x\right) + \frac{7}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{8} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{7}{9} \, x^{9} \log\left(5 \, x\right) + \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{9} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, x^{10} \log\left(-7 \, x\right) - \frac{1}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x \log\left(-6 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{2} \log\left(-6 \, x\right) - \frac{1}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{9}{2} \, x^{2} \log\left(-2 \, x\right) - \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x \log\left(4 \, x\right)}\;dx} = \answer{{3 \, x^{2} \log\left(4 \, x\right) - \frac{3}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{8} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{1}{9} \, x^{9} \log\left(-4 \, x\right) - \frac{1}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{7} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{3}{4} \, x^{8} \log\left(6 \, x\right) - \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{2} \log\left(-6 \, x\right)}\;dx} = \answer{{3 \, x^{3} \log\left(-6 \, x\right) - x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x \log\left(2 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{2} \log\left(2 \, x\right) - \frac{3}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{9}{2} \, x^{2} \log\left(-7 \, x\right) + \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{5} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{6} \log\left(-3 \, x\right) - \frac{1}{9} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{6} \log\left(-4 \, x\right)}\;dx} = \answer{{-\frac{2}{7} \, x^{7} \log\left(-4 \, x\right) + \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{7} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{8} \log\left(-6 \, x\right) + \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{4} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{3}{5} \, x^{5} \log\left(4 \, x\right) - \frac{3}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{2} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{3} \log\left(-7 \, x\right) - \frac{1}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{6} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{2}{7} \, x^{7} \log\left(-9 \, x\right) - \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{4} \log\left(-x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{5} \log\left(-x\right) + \frac{3}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x \log\left(-x\right)}\;dx} = \answer{{-4 \, x^{2} \log\left(-x\right) + 2 \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{6} \log\left(-6 \, x\right)}\;dx} = \answer{{-\frac{4}{7} \, x^{7} \log\left(-6 \, x\right) + \frac{4}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{4} \log\left(-2 \, x\right)}\;dx} = \answer{{x^{5} \log\left(-2 \, x\right) - \frac{1}{5} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{8} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{1}{9} \, x^{9} \log\left(-5 \, x\right) + \frac{1}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{7} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{8} \log\left(-5 \, x\right) + \frac{1}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{4} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{5} \, x^{5} \log\left(-3 \, x\right) - \frac{1}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{9} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{9}{10} \, x^{10} \log\left(-8 \, x\right) - \frac{9}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x \log\left(8 \, x\right)}\;dx} = \answer{{\frac{9}{2} \, x^{2} \log\left(8 \, x\right) - \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{6} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{3}{7} \, x^{7} \log\left(-8 \, x\right) - \frac{3}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{8} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{8}{9} \, x^{9} \log\left(8 \, x\right) + \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{8} \log\left(8 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{9} \log\left(8 \, x\right) - \frac{1}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{6} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{3}{7} \, x^{7} \log\left(-9 \, x\right) + \frac{3}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{2} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{7}{3} \, x^{3} \log\left(2 \, x\right) + \frac{7}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{2} \log\left(-4 \, x\right)}\;dx} = \answer{{-x^{3} \log\left(-4 \, x\right) + \frac{1}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{3} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{1}{4} \, x^{4} \log\left(7 \, x\right) - \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{9} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, x^{10} \log\left(5 \, x\right) + \frac{1}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{3} \log\left(-x\right)}\;dx} = \answer{{\frac{1}{4} \, x^{4} \log\left(-x\right) - \frac{1}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{9} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{4}{5} \, x^{10} \log\left(-4 \, x\right) - \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x \log\left(x\right)}\;dx} = \answer{{-\frac{5}{2} \, x^{2} \log\left(x\right) + \frac{5}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{7} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{7}{8} \, x^{8} \log\left(-2 \, x\right) - \frac{7}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{7} \log\left(-2 \, x\right)}\;dx} = \answer{{-x^{8} \log\left(-2 \, x\right) + \frac{1}{8} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{6} \log\left(x\right)}\;dx} = \answer{{\frac{9}{7} \, x^{7} \log\left(x\right) - \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{3} \log\left(x\right)}\;dx} = \answer{{-\frac{7}{4} \, x^{4} \log\left(x\right) + \frac{7}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{8} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{7}{9} \, x^{9} \log\left(9 \, x\right) - \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{3} \log\left(5 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{4} \log\left(5 \, x\right) - \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{8} \log\left(-8 \, x\right)}\;dx} = \answer{{-\frac{5}{9} \, x^{9} \log\left(-8 \, x\right) + \frac{5}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{4} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{6}{5} \, x^{5} \log\left(4 \, x\right) + \frac{6}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{9} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{10} \, x^{10} \log\left(-3 \, x\right) - \frac{1}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{5} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{6} \log\left(5 \, x\right) + \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{7} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, x^{8} \log\left(-7 \, x\right) + \frac{1}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{7} \log\left(-x\right)}\;dx} = \answer{{\frac{1}{8} \, x^{8} \log\left(-x\right) - \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{6} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{8}{7} \, x^{7} \log\left(-9 \, x\right) + \frac{8}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{5} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{7}{6} \, x^{6} \log\left(-9 \, x\right) + \frac{7}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{2} \log\left(-2 \, x\right)}\;dx} = \answer{{-x^{3} \log\left(-2 \, x\right) + \frac{1}{3} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x \log\left(x\right)}\;dx} = \answer{{-2 \, x^{2} \log\left(x\right) + x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{8} \log\left(-x\right)}\;dx} = \answer{{\frac{7}{9} \, x^{9} \log\left(-x\right) - \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{6} \log\left(5 \, x\right)}\;dx} = \answer{{-\frac{3}{7} \, x^{7} \log\left(5 \, x\right) + \frac{3}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{2} \log\left(7 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{3} \log\left(7 \, x\right) - \frac{1}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{3} \log\left(5 \, x\right)}\;dx} = \answer{{2 \, x^{4} \log\left(5 \, x\right) - \frac{1}{2} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-5 \, x^{3} \log\left(-5 \, x\right)}\;dx} = \answer{{-\frac{5}{4} \, x^{4} \log\left(-5 \, x\right) + \frac{5}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{7} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{7}{8} \, x^{8} \log\left(4 \, x\right) + \frac{7}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(7 \, x\right)}\;dx} = \answer{{-x^{2} \log\left(7 \, x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{5} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{6} \log\left(9 \, x\right) + \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{8} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{7}{9} \, x^{9} \log\left(-4 \, x\right) - \frac{7}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x \log\left(-6 \, x\right)}\;dx} = \answer{{-x^{2} \log\left(-6 \, x\right) + \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{7} \log\left(4 \, x\right)}\;dx} = \answer{{\frac{3}{4} \, x^{8} \log\left(4 \, x\right) - \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{5} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{5}{6} \, x^{6} \log\left(-9 \, x\right) - \frac{5}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{2} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{2}{3} \, x^{3} \log\left(2 \, x\right) + \frac{2}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{9} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{10} \log\left(-7 \, x\right) + \frac{3}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{4} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, x^{5} \log\left(-9 \, x\right) + \frac{2}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{8} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, x^{9} \log\left(-3 \, x\right) - \frac{2}{27} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{3} \log\left(-8 \, x\right)}\;dx} = \answer{{x^{4} \log\left(-8 \, x\right) - \frac{1}{4} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{6} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{6}{7} \, x^{7} \log\left(-3 \, x\right) - \frac{6}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{3} \log\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{4} \log\left(6 \, x\right) - \frac{1}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{7} \log\left(x\right)}\;dx} = \answer{{x^{8} \log\left(x\right) - \frac{1}{8} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x \log\left(9 \, x\right)}\;dx} = \answer{{x^{2} \log\left(9 \, x\right) - \frac{1}{2} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{6} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{1}{7} \, x^{7} \log\left(7 \, x\right) + \frac{1}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{2} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{4}{3} \, x^{3} \log\left(-3 \, x\right) - \frac{4}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{3} \log\left(x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{4} \log\left(x\right) + \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{2} \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{3} \log\left(7 \, x\right) + \frac{1}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{3} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{5}{4} \, x^{4} \log\left(-9 \, x\right) - \frac{5}{16} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{6} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{9}{7} \, x^{7} \log\left(-3 \, x\right) - \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{3} \log\left(7 \, x\right)}\;dx} = \answer{{x^{4} \log\left(7 \, x\right) - \frac{1}{4} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{6} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{2}{7} \, x^{7} \log\left(-9 \, x\right) + \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{6} \log\left(-2 \, x\right)}\;dx} = \answer{{-x^{7} \log\left(-2 \, x\right) + \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{5} \log\left(9 \, x\right)}\;dx} = \answer{{\frac{5}{6} \, x^{6} \log\left(9 \, x\right) - \frac{5}{36} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-8 \, x^{8} \log\left(4 \, x\right)}\;dx} = \answer{{-\frac{8}{9} \, x^{9} \log\left(4 \, x\right) + \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{2} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{8}{3} \, x^{3} \log\left(-9 \, x\right) - \frac{8}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{7} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{1}{8} \, x^{8} \log\left(8 \, x\right) + \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{5} \log\left(6 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, x^{6} \log\left(6 \, x\right) + \frac{1}{18} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{6} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{8}{7} \, x^{7} \log\left(-8 \, x\right) - \frac{8}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{8} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{8}{9} \, x^{9} \log\left(2 \, x\right) - \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{5} \log\left(-5 \, x\right)}\;dx} = \answer{{x^{6} \log\left(-5 \, x\right) - \frac{1}{6} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{6} \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{6}{7} \, x^{7} \log\left(9 \, x\right) + \frac{6}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x \log\left(9 \, x\right)}\;dx} = \answer{{-\frac{7}{2} \, x^{2} \log\left(9 \, x\right) + \frac{7}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{8} \log\left(x\right)}\;dx} = \answer{{\frac{4}{9} \, x^{9} \log\left(x\right) - \frac{4}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x \log\left(9 \, x\right)}\;dx} = \answer{{4 \, x^{2} \log\left(9 \, x\right) - 2 \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{7} \log\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{8} \log\left(-3 \, x\right) - \frac{1}{16} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{9} \log\left(-x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{10} \log\left(-x\right) + \frac{3}{50} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{5} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{6} \log\left(-4 \, x\right) - \frac{1}{4} \, x^{6}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{9} \log\left(-8 \, x\right)}\;dx} = \answer{{\frac{7}{10} \, x^{10} \log\left(-8 \, x\right) - \frac{7}{100} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{7} \log\left(-7 \, x\right)}\;dx} = \answer{{\frac{7}{8} \, x^{8} \log\left(-7 \, x\right) - \frac{7}{64} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{9} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{4}{5} \, x^{10} \log\left(-2 \, x\right) - \frac{2}{25} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{8} \log\left(-9 \, x\right)}\;dx} = \answer{{-\frac{2}{9} \, x^{9} \log\left(-9 \, x\right) + \frac{2}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{6} \log\left(3 \, x\right)}\;dx} = \answer{{-x^{7} \log\left(3 \, x\right) + \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{7 \, x^{6} \log\left(6 \, x\right)}\;dx} = \answer{{x^{7} \log\left(6 \, x\right) - \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{4} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{8}{5} \, x^{5} \log\left(-9 \, x\right) - \frac{8}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{6} \log\left(-3 \, x\right)}\;dx} = \answer{{-\frac{2}{7} \, x^{7} \log\left(-3 \, x\right) + \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-7 \, x^{6} \log\left(-9 \, x\right)}\;dx} = \answer{{-x^{7} \log\left(-9 \, x\right) + \frac{1}{7} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x^{6} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{9}{7} \, x^{7} \log\left(2 \, x\right) + \frac{9}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{2 \, x^{6} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{2}{7} \, x^{7} \log\left(2 \, x\right) - \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-9 \, x \log\left(7 \, x\right)}\;dx} = \answer{{-\frac{9}{2} \, x^{2} \log\left(7 \, x\right) + \frac{9}{4} \, x^{2}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-6 \, x^{7} \log\left(2 \, x\right)}\;dx} = \answer{{-\frac{3}{4} \, x^{8} \log\left(2 \, x\right) + \frac{3}{32} \, x^{8}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{6 \, x^{3} \log\left(-4 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{4} \log\left(-4 \, x\right) - \frac{3}{8} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{6} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{8}{7} \, x^{7} \log\left(2 \, x\right) - \frac{8}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-2 \, x^{6} \log\left(3 \, x\right)}\;dx} = \answer{{-\frac{2}{7} \, x^{7} \log\left(3 \, x\right) + \frac{2}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-4 \, x^{3} \log\left(2 \, x\right)}\;dx} = \answer{{-x^{4} \log\left(2 \, x\right) + \frac{1}{4} \, x^{4}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{9 \, x^{2} \log\left(-4 \, x\right)}\;dx} = \answer{{3 \, x^{3} \log\left(-4 \, x\right) - x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{6} \log\left(-x\right)}\;dx} = \answer{{\frac{5}{7} \, x^{7} \log\left(-x\right) - \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{4 \, x^{6} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{4}{7} \, x^{7} \log\left(-9 \, x\right) - \frac{4}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{6} \log\left(x\right)}\;dx} = \answer{{\frac{5}{7} \, x^{7} \log\left(x\right) - \frac{5}{49} \, x^{7}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{5 \, x^{9} \log\left(2 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{10} \log\left(2 \, x\right) - \frac{1}{20} \, x^{10}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-3 \, x^{4} \log\left(8 \, x\right)}\;dx} = \answer{{-\frac{3}{5} \, x^{5} \log\left(8 \, x\right) + \frac{3}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{3 \, x^{4} \log\left(-x\right)}\;dx} = \answer{{\frac{3}{5} \, x^{5} \log\left(-x\right) - \frac{3}{25} \, x^{5}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{x^{2} \log\left(-2 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, x^{3} \log\left(-2 \, x\right) - \frac{1}{9} \, x^{3}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{8 \, x^{8} \log\left(-9 \, x\right)}\;dx} = \answer{{\frac{8}{9} \, x^{9} \log\left(-9 \, x\right) - \frac{8}{81} \, x^{9}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0024. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0024.HELP.tex}

\[
\int{{-x^{7} \log\left(-7 \, x\right)}\;dx} = \answer{{-\frac{1}{8} \, x^{8} \log\left(-7 \, x\right) + \frac{1}{64} \, x^{8}}+C}
\]
\end{problem}}%}

