\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@TrigSub, Func@Trig, File@0042}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} + 14 \, x + 58}}}\;dx} = \answer{{{\rm arcsinh}\left(\frac{1}{3} \, x + \frac{7}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 16 \, x + 39}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 39} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 10 \, x + 21}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x + 21} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 4 \, x + 21}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{5} \, x + \frac{2}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} + 14 \, x - 24}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{5} \, x + \frac{7}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 4 \, x + 8}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{2} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 2 \, x - 24}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 2 \, x - 24} - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{-x^{2} - 16 \, x - 55}}}\;dx} = \answer{{-4 \, \arcsin\left(-\frac{1}{3} \, x - \frac{8}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 6 \, x + 90}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{9} \, x + \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 14 \, x + 33}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 33} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 18 \, x + 65}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x + 65} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 6 \, x + 8}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x + 8} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 16 \, x + 28}}}\;dx} = \answer{{2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 28} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 4 \, x - 45}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 4 \, x - 45} - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 10 \, x + 26}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 4 \, x}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 16 \, x + 80}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(\frac{1}{4} \, x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 12 \, x + 11}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x + 11} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 8 \, x + 32}}}\;dx} = \answer{{9 \, {\rm arcsinh}\left(\frac{1}{4} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} - 16 \, x + 80}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(\frac{1}{4} \, x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 16 \, x}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} - 12 \, x + 72}}}\;dx} = \answer{{{\rm arcsinh}\left(\frac{1}{6} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} - 4 \, x + 68}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(\frac{1}{8} \, x - \frac{1}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 6 \, x + 40}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{7} \, x + \frac{3}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 10 \, x + 106}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{5}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 18 \, x + 90}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{3} \, x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 16 \, x + 73}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{3} \, x + \frac{8}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 8 \, x - 12}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{2} \, x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 18 \, x - 77}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{2} \, x + \frac{9}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 8 \, x + 52}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 2 \, x}}}\;dx} = \answer{{-8 \, \arcsin\left(-x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 14 \, x}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} + 6 \, x}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 6 \, x} + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 8 \, x - 9}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 9} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{-x^{2} - 10 \, x + 11}}}\;dx} = \answer{{5 \, \arcsin\left(-\frac{1}{6} \, x - \frac{5}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 18 \, x - 45}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{6} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} - 10 \, x + 26}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 8 \, x - 65}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x - 65} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 18 \, x + 77}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x + 77} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 10 \, x - 39}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x - 39} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 18 \, x + 65}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 65} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 14 \, x + 15}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{8} \, x + \frac{7}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 8 \, x + 41}}}\;dx} = \answer{{-2 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{4}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 18 \, x + 65}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 65} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 14 \, x + 24}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 24} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{-x^{2} + 10 \, x + 56}}}\;dx} = \answer{{-4 \, \arcsin\left(-\frac{1}{9} \, x + \frac{5}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} + 2 \, x + 3}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{2} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} - 12 \, x - 35}}}\;dx} = \answer{{-9 \, \arcsin\left(-x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{-x^{2} + 16 \, x - 55}}}\;dx} = \answer{{-4 \, \arcsin\left(-\frac{1}{3} \, x + \frac{8}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 10 \, x + 11}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{6} \, x + \frac{5}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} + 12 \, x + 13}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{7} \, x + \frac{6}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 12 \, x + 20}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x + 20} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 6 \, x + 13}}}\;dx} = \answer{{-7 \, {\rm arcsinh}\left(\frac{1}{2} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 8 \, x + 80}}}\;dx} = \answer{{-7 \, {\rm arcsinh}\left(\frac{1}{8} \, x - \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 6 \, x}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 6 \, x} + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} + 18 \, x - 17}}}\;dx} = \answer{{7 \, \arcsin\left(-\frac{1}{8} \, x + \frac{9}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 8 \, x + 20}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{6} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 16 \, x + 113}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{7} \, x + \frac{8}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 8 \, x - 48}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 48} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} + 16 \, x + 39}}}\;dx} = \answer{{7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 39} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 12 \, x + 85}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(\frac{1}{7} \, x + \frac{6}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 14 \, x - 15}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x - 15} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 12 \, x + 52}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} - 18 \, x + 106}}}\;dx} = \answer{{{\rm arcsinh}\left(\frac{1}{5} \, x - \frac{9}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 10 \, x + 29}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{2} \, x - \frac{5}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} - 2 \, x + 15}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{4} \, x - \frac{1}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 16 \, x - 17}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x - 17} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 6 \, x - 40}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 40} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 4 \, x + 3}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x + 3} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} - 14 \, x + 15}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{8} \, x - \frac{7}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 16 \, x + 39}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 39} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} + 16 \, x - 60}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{2} \, x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} + 4 \, x + 77}}}\;dx} = \answer{{3 \, \arcsin\left(-\frac{1}{9} \, x + \frac{2}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{-x^{2} - 18 \, x}}}\;dx} = \answer{{-4 \, \arcsin\left(-\frac{1}{9} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} + 8 \, x + 9}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{5} \, x + \frac{4}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 18 \, x + 72}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 72} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} + 14 \, x - 33}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{4} \, x + \frac{7}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} - 12 \, x + 32}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x + 32} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} + 2 \, x + 17}}}\;dx} = \answer{{{\rm arcsinh}\left(\frac{1}{4} \, x + \frac{1}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 12 \, x - 45}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x - 45} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} - 4 \, x}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 4 \, x} - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} - 14 \, x - 13}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{6} \, x - \frac{7}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{-x^{2} - 14 \, x - 45}}}\;dx} = \answer{{4 \, \arcsin\left(-\frac{1}{2} \, x - \frac{7}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} + 2 \, x - 48}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 2 \, x - 48} + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 6 \, x - 7}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 7} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{-x^{2} + 18 \, x - 45}}}\;dx} = \answer{{-2 \, \arcsin\left(-\frac{1}{6} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 6 \, x + 34}}}\;dx} = \answer{{9 \, {\rm arcsinh}\left(\frac{1}{5} \, x + \frac{3}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 4 \, x + 45}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{7} \, x - \frac{2}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} - 8 \, x + 20}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{6} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} + 10 \, x - 11}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 10 \, x - 11} + 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} + 8 \, x + 48}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{8} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 4 \, x + 8}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{2} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 4 \, x + 29}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{2}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 14 \, x + 33}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 33} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} - 18 \, x - 72}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{3} \, x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} - 4 \, x + 77}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{9} \, x - \frac{2}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} + 16 \, x + 48}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 48} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 18 \, x + 82}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} + 4 \, x + 5}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 2 \, x + 50}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{7} \, x - \frac{1}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 12 \, x - 20}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{4} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 2 \, x - 80}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 2 \, x - 80} - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} - 18 \, x - 72}}}\;dx} = \answer{{7 \, \arcsin\left(-\frac{1}{3} \, x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 8 \, x + 25}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{3} \, x - \frac{4}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} + 18 \, x + 85}}}\;dx} = \answer{{4 \, {\rm arcsinh}\left(\frac{1}{2} \, x + \frac{9}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} + 14 \, x + 32}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{9} \, x + \frac{7}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 2 \, x + 17}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{1}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 2 \, x + 35}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{6} \, x + \frac{1}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 2 \, x - 35}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 2 \, x - 35} - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{-x^{2} - 10 \, x + 39}}}\;dx} = \answer{{4 \, \arcsin\left(-\frac{1}{8} \, x - \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} + 6 \, x + 34}}}\;dx} = \answer{{-2 \, {\rm arcsinh}\left(\frac{1}{5} \, x + \frac{3}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} + 16 \, x + 55}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 55} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 16 \, x + 15}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 15} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 12 \, x - 13}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 12 \, x - 13} + 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 18 \, x + 82}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 14 \, x}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{7} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 2 \, x + 80}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{9} \, x - \frac{1}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} + 2 \, x + 63}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{8} \, x + \frac{1}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} + 6 \, x - 8}}}\;dx} = \answer{{-5 \, \arcsin\left(-x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} - 6 \, x - 5}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{2} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 8 \, x - 20}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 20} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} - 8 \, x + 65}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{9} \, x - \frac{4}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 8 \, x + 52}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} - 8 \, x - 7}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{3} \, x - \frac{4}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 8 \, x - 65}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x - 65} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 12 \, x + 100}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{8} \, x + \frac{3}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{-x^{2} + 8 \, x - 7}}}\;dx} = \answer{{6 \, \arcsin\left(-\frac{1}{3} \, x + \frac{4}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} + 12 \, x + 28}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{8} \, x + \frac{3}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 4 \, x - 12}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 4 \, x - 12} - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 12 \, x + 52}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} + 10 \, x + 11}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{6} \, x + \frac{5}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} - 6 \, x + 7}}}\;dx} = \answer{{7 \, \arcsin\left(-\frac{1}{4} \, x - \frac{3}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 12 \, x}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{6} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 10 \, x + 41}}}\;dx} = \answer{{-7 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{5}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 18 \, x + 80}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x + 80} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} - 6 \, x - 5}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{2} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 8 \, x + 15}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x + 15} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} - 4 \, x + 45}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{7} \, x - \frac{2}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 16 \, x - 17}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x - 17} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 10 \, x + 106}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{5}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} - 6 \, x + 7}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{4} \, x - \frac{3}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 14 \, x + 48}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 48} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} - 16 \, x + 17}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{9} \, x - \frac{8}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 8 \, x + 20}}}\;dx} = \answer{{-8 \, {\rm arcsinh}\left(\frac{1}{2} \, x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 2 \, x + 26}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{1}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 10 \, x - 16}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{3} \, x + \frac{5}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} + 2 \, x + 50}}}\;dx} = \answer{{-7 \, {\rm arcsinh}\left(\frac{1}{7} \, x + \frac{1}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{-x^{2} - 18 \, x - 45}}}\;dx} = \answer{{5 \, \arcsin\left(-\frac{1}{6} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 18 \, x + 65}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x + 65} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 6 \, x + 45}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} + 2 \, x + 10}}}\;dx} = \answer{{-2 \, {\rm arcsinh}\left(\frac{1}{3} \, x + \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} + 12 \, x - 20}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{4} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 8 \, x + 25}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{3} \, x - \frac{4}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 18 \, x + 117}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(\frac{1}{6} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 8 \, x - 20}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 20} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} - 6 \, x - 16}}}\;dx} = \answer{{7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 16} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} + 14 \, x - 15}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x - 15} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 16 \, x + 63}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 63} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 6 \, x + 25}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{3}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 12 \, x + 117}}}\;dx} = \answer{{9 \, {\rm arcsinh}\left(\frac{1}{9} \, x + \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} - 16 \, x + 17}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{9} \, x - \frac{8}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 6 \, x + 5}}}\;dx} = \answer{{2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 6 \, x + 5} + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 12 \, x + 52}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 2 \, x + 82}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{1}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 18 \, x + 106}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{5} \, x + \frac{9}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} + 18 \, x - 80}}}\;dx} = \answer{{-\arcsin\left(-x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 12 \, x + 52}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 16 \, x + 80}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{4} \, x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 16 \, x - 17}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x - 17} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} + 12 \, x}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{6} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} - 6 \, x - 7}}}\;dx} = \answer{{\log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 7} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 12 \, x + 27}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 12 \, x + 27} + 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 14 \, x + 130}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{7}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 2 \, x + 2}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 4 \, x + 20}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} - 14 \, x - 13}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{6} \, x - \frac{7}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} - 18 \, x - 17}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{8} \, x - \frac{9}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 16 \, x + 15}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 15} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} + 14 \, x + 98}}}\;dx} = \answer{{{\rm arcsinh}\left(\frac{1}{7} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 16 \, x - 17}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x - 17} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} + 8 \, x - 15}}}\;dx} = \answer{{3 \, \arcsin\left(-x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 8 \, x - 7}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{3} \, x - \frac{4}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 10 \, x + 39}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{8} \, x + \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 14 \, x + 65}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{4} \, x + \frac{7}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} + 8 \, x + 7}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x + 7} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 8 \, x + 15}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x + 15} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} - 6 \, x - 40}}}\;dx} = \answer{{7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 40} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} + 4 \, x - 5}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x - 5} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 8 \, x - 65}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 65} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 18 \, x + 117}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 10 \, x + 41}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{5}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} + 6 \, x + 55}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{8} \, x + \frac{3}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 10 \, x - 39}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x - 39} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} - 16 \, x - 48}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{4} \, x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 16 \, x}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 4 \, x - 32}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x - 32} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{-x^{2} - 12 \, x - 32}}}\;dx} = \answer{{4 \, \arcsin\left(-\frac{1}{2} \, x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 10 \, x - 11}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 10 \, x - 11} + 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 6 \, x + 90}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} - 8 \, x + 41}}}\;dx} = \answer{{{\rm arcsinh}\left(\frac{1}{5} \, x - \frac{4}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 18 \, x + 106}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{9}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} - 4 \, x + 21}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{5} \, x - \frac{2}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 10 \, x - 24}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} + 10 \, x - 24} + 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} + 2 \, x + 63}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{8} \, x + \frac{1}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} + 16 \, x - 55}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{3} \, x + \frac{8}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 12 \, x + 35}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x + 35} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} - 10 \, x - 56}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x - 56} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} - 18 \, x - 77}}}\;dx} = \answer{{7 \, \arcsin\left(-\frac{1}{2} \, x - \frac{9}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} + 6 \, x + 13}}}\;dx} = \answer{{-7 \, {\rm arcsinh}\left(\frac{1}{2} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} + 6 \, x + 16}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{5} \, x + \frac{3}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 12 \, x + 72}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{6} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} - 8 \, x - 12}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{2} \, x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 4 \, x - 21}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x - 21} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 2 \, x - 15}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 2 \, x - 15} + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 6 \, x + 90}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} + 16 \, x - 15}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{7} \, x + \frac{8}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} + 12 \, x - 45}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 12 \, x - 45} + 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 8 \, x + 12}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x + 12} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 2 \, x - 48}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 2 \, x - 48} - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 18 \, x + 77}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x + 77} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 8 \, x - 9}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 9} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 14 \, x + 13}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 13} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} - 6 \, x - 5}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{2} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 12 \, x + 37}}}\;dx} = \answer{{9 \, {\rm arcsinh}\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} - 10 \, x - 9}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{4} \, x - \frac{5}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{-x^{2} + 10 \, x + 11}}}\;dx} = \answer{{-4 \, \arcsin\left(-\frac{1}{6} \, x + \frac{5}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} + 6 \, x - 8}}}\;dx} = \answer{{-7 \, \arcsin\left(-x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 2 \, x - 35}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 2 \, x - 35} + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 4 \, x + 77}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{9} \, x - \frac{2}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 8 \, x + 32}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{4} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 10 \, x - 39}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 10 \, x - 39} + 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 14 \, x + 113}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{8} \, x + \frac{7}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 18 \, x + 45}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 45} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} + 8 \, x - 12}}}\;dx} = \answer{{3 \, \arcsin\left(-\frac{1}{2} \, x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 12 \, x + 32}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} + 12 \, x + 32} + 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{-x^{2} + 12 \, x - 20}}}\;dx} = \answer{{6 \, \arcsin\left(-\frac{1}{4} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 16 \, x + 73}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{3} \, x + \frac{8}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 10 \, x + 34}}}\;dx} = \answer{{9 \, {\rm arcsinh}\left(\frac{1}{3} \, x + \frac{5}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 14 \, x - 15}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x - 15} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 4 \, x + 13}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{3} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} + 2 \, x}}}\;dx} = \answer{{\arcsin\left(-x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} + 12 \, x - 27}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{3} \, x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{-x^{2} + 2 \, x + 35}}}\;dx} = \answer{{6 \, \arcsin\left(-\frac{1}{6} \, x + \frac{1}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 10 \, x + 89}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{8} \, x - \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 16 \, x + 55}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 55} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} + 16 \, x - 15}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{7} \, x + \frac{8}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} + 12 \, x + 45}}}\;dx} = \answer{{3 \, \arcsin\left(-\frac{1}{9} \, x + \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 14 \, x + 45}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 45} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 14 \, x + 45}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 45} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 4 \, x + 13}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{3} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 2 \, x + 17}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{1}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{-x^{2} - 8 \, x + 9}}}\;dx} = \answer{{-2 \, \arcsin\left(-\frac{1}{5} \, x - \frac{4}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 10 \, x + 26}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{-x^{2} - 2 \, x + 8}}}\;dx} = \answer{{-2 \, \arcsin\left(-\frac{1}{3} \, x - \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 16 \, x - 15}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{7} \, x + \frac{8}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 4 \, x - 77}}}\;dx} = \answer{{2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 4 \, x - 77} - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} - 12 \, x - 32}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{2} \, x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} + 12 \, x + 45}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{9} \, x + \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 18 \, x + 80}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x + 80} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 14 \, x + 58}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(\frac{1}{3} \, x - \frac{7}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 16 \, x + 28}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 28} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 14 \, x + 45}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 45} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 2 \, x - 24}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 2 \, x - 24} + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 10 \, x + 89}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{8} \, x - \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 14 \, x + 85}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{6} \, x - \frac{7}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 6 \, x + 5}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x + 5} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 10 \, x + 9}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x + 9} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 4 \, x - 5}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x - 5} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 4 \, x + 8}}}\;dx} = \answer{{-8 \, {\rm arcsinh}\left(\frac{1}{2} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} + 18 \, x - 80}}}\;dx} = \answer{{7 \, \arcsin\left(-x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{-x^{2} + 2 \, x + 15}}}\;dx} = \answer{{-2 \, \arcsin\left(-\frac{1}{4} \, x + \frac{1}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 2 \, x + 82}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{9} \, x + \frac{1}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 18 \, x}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{9} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 16 \, x + 60}}}\;dx} = \answer{{2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 60} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 18 \, x + 145}}}\;dx} = \answer{{-2 \, {\rm arcsinh}\left(\frac{1}{8} \, x - \frac{9}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{-x^{2} + 4 \, x + 45}}}\;dx} = \answer{{5 \, \arcsin\left(-\frac{1}{7} \, x + \frac{2}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 2 \, x + 50}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{7} \, x + \frac{1}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 6 \, x + 73}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{8} \, x + \frac{3}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 18 \, x + 85}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(\frac{1}{2} \, x + \frac{9}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} + 8 \, x - 12}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{2} \, x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 2 \, x}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 2 \, x} + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} + 14 \, x + 24}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 24} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 18 \, x + 117}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(\frac{1}{6} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{-x^{2} - 14 \, x + 15}}}\;dx} = \answer{{5 \, \arcsin\left(-\frac{1}{8} \, x - \frac{7}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} + 16 \, x - 39}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{5} \, x + \frac{8}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 12 \, x + 117}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} + 4 \, x}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{2} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} + 4 \, x + 5}}}\;dx} = \answer{{4 \, {\rm arcsinh}\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} + 10 \, x + 39}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{8} \, x + \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 16 \, x + 128}}}\;dx} = \answer{{9 \, {\rm arcsinh}\left(\frac{1}{8} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 14 \, x + 48}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 48} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 8 \, x - 48}}}\;dx} = \answer{{2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x - 48} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 14 \, x + 24}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 24} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} + 4 \, x + 12}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{4} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 4 \, x + 53}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{7} \, x - \frac{2}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} + 6 \, x + 40}}}\;dx} = \answer{{7 \, \arcsin\left(-\frac{1}{7} \, x + \frac{3}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 6 \, x + 13}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{2} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} + 16 \, x - 39}}}\;dx} = \answer{{7 \, \arcsin\left(-\frac{1}{5} \, x + \frac{8}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 18 \, x + 106}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{5} \, x + \frac{9}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} - 12 \, x - 28}}}\;dx} = \answer{{\log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x - 28} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 4 \, x + 85}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{9} \, x - \frac{2}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 18 \, x + 56}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 56} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 8 \, x - 48}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x - 48} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 14 \, x + 32}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{9} \, x - \frac{7}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{-x^{2} + 14 \, x + 15}}}\;dx} = \answer{{-2 \, \arcsin\left(-\frac{1}{8} \, x + \frac{7}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 18 \, x + 80}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 80} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} + 16 \, x + 80}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(\frac{1}{4} \, x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 16 \, x + 28}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 28} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} + 12 \, x + 52}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 6 \, x - 72}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 72} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} - 6 \, x}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{3} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} - 10 \, x + 39}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{8} \, x - \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 14 \, x + 48}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 48} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 18 \, x + 145}}}\;dx} = \answer{{9 \, {\rm arcsinh}\left(\frac{1}{8} \, x - \frac{9}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} + 8 \, x - 9}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 9} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{x^{2} - 12 \, x + 32}}}\;dx} = \answer{{\log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x + 32} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 16 \, x + 55}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 55} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} + 2 \, x + 80}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{9} \, x + \frac{1}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} - 4 \, x}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{2} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} + 6 \, x + 45}}}\;dx} = \answer{{-7 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 14 \, x - 32}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x - 32} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} + 2 \, x}}}\;dx} = \answer{{8 \, \arcsin\left(-x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 2 \, x + 26}}}\;dx} = \answer{{-2 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{1}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} - 8 \, x - 15}}}\;dx} = \answer{{3 \, \arcsin\left(-x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} - 18 \, x}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} - 8 \, x}}}\;dx} = \answer{{7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 14 \, x}}}\;dx} = \answer{{8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 14 \, x + 40}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 40} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 8 \, x + 41}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{4}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} + 4 \, x + 12}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{4} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} + 18 \, x + 77}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x + 77} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 14 \, x + 130}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{9} \, x + \frac{7}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 12 \, x}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 4 \, x - 32}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 4 \, x - 32} - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} + 6 \, x}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 6 \, x} + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 18 \, x + 106}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{9}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} + 14 \, x + 50}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} + 2 \, x}}}\;dx} = \answer{{7 \, \arcsin\left(-x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 14 \, x + 65}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{7}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 8 \, x - 48}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 48} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 10 \, x - 56}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x - 56} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} + 6 \, x + 16}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{5} \, x + \frac{3}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} + 12 \, x - 27}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{3} \, x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 4 \, x + 5}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} + 10 \, x + 9}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 10 \, x + 9} + 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 16 \, x + 63}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 63} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} + 14 \, x}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{7} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 16 \, x - 39}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{5} \, x + \frac{8}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 14 \, x + 98}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{7} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 14 \, x + 53}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{2} \, x - \frac{7}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{-x^{2} + 16 \, x + 17}}}\;dx} = \answer{{5 \, \arcsin\left(-\frac{1}{9} \, x + \frac{8}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 12 \, x - 28}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x - 28} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 18 \, x + 90}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{3} \, x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 14 \, x + 24}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 24} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} - 2 \, x}}}\;dx} = \answer{{\arcsin\left(-x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 12 \, x - 20}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{4} \, x - \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 10 \, x + 61}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{5}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 8 \, x + 25}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{3} \, x + \frac{4}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} - 16 \, x}}}\;dx} = \answer{{3 \, \arcsin\left(-\frac{1}{8} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 8 \, x + 97}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{9} \, x + \frac{4}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} + 4 \, x + 29}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{5} \, x + \frac{2}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{-x^{2} - 2 \, x + 3}}}\;dx} = \answer{{-2 \, \arcsin\left(-\frac{1}{2} \, x - \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 10 \, x - 24}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x - 24} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} + 6 \, x + 34}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{5} \, x + \frac{3}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{-x^{2} + 14 \, x - 40}}}\;dx} = \answer{{-4 \, \arcsin\left(-\frac{1}{3} \, x + \frac{7}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 8 \, x + 17}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 6 \, x + 34}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{3}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 4 \, x + 5}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{3} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} - 6 \, x - 40}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 40} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 14 \, x}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 10 \, x - 39}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x - 39} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 10 \, x + 16}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x + 16} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 16 \, x + 68}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{2} \, x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} + 10 \, x + 39}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{8} \, x + \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 2 \, x + 37}}}\;dx} = \answer{{5 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{1}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 2 \, x + 8}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{3} \, x - \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} + 4 \, x + 40}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 8 \, x + 12}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x + 12} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} + 14 \, x + 50}}}\;dx} = \answer{{-8 \, {\rm arcsinh}\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 16 \, x + 73}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{3} \, x + \frac{8}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} + 8 \, x - 9}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 9} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} - 16 \, x + 39}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 39} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 2 \, x + 10}}}\;dx} = \answer{{-8 \, {\rm arcsinh}\left(\frac{1}{3} \, x - \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} - 2 \, x}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 2 \, x} - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 18 \, x + 32}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 32} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} + 10 \, x + 89}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(\frac{1}{8} \, x + \frac{5}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} - 18 \, x - 77}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{2} \, x - \frac{9}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 18 \, x}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 12 \, x}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{6} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 16 \, x + 48}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 16 \, x + 48} - 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 6 \, x + 7}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{4} \, x - \frac{3}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 16 \, x + 80}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{4} \, x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{x^{2} - 10 \, x + 9}}}\;dx} = \answer{{9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x + 9} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{x^{2} - 6 \, x + 73}}}\;dx} = \answer{{8 \, {\rm arcsinh}\left(\frac{1}{8} \, x - \frac{3}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} + 10 \, x - 21}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{2} \, x + \frac{5}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 8 \, x + 65}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{9} \, x + \frac{4}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} + 12 \, x + 37}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} + 6 \, x - 16}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 6 \, x - 16} + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} + 8 \, x}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{4} \, x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 18 \, x + 17}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 17} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} + 10 \, x - 21}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{2} \, x + \frac{5}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 18 \, x + 80}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 80} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} + 8 \, x + 80}}}\;dx} = \answer{{-5 \, {\rm arcsinh}\left(\frac{1}{8} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} - 16 \, x - 55}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{3} \, x - \frac{8}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} + 14 \, x + 32}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{9} \, x + \frac{7}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} + 12 \, x + 52}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(\frac{1}{4} \, x + \frac{3}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 8 \, x + 52}}}\;dx} = \answer{{-2 \, {\rm arcsinh}\left(\frac{1}{6} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} - 8 \, x + 20}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{6} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} - 14 \, x - 45}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{2} \, x - \frac{7}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} + 6 \, x + 40}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{7} \, x + \frac{3}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} - 12 \, x - 11}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{5} \, x - \frac{6}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} - 12 \, x + 45}}}\;dx} = \answer{{3 \, \arcsin\left(-\frac{1}{9} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{-x^{2} - 4 \, x + 12}}}\;dx} = \answer{{-2 \, \arcsin\left(-\frac{1}{4} \, x - \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} - 12 \, x + 45}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{9} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} - 14 \, x + 65}}}\;dx} = \answer{{4 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{7}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 12 \, x - 28}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} + 12 \, x - 28} + 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 4 \, x - 21}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x - 21} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} - 10 \, x}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 8 \, x + 7}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x + 7} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} + 14 \, x + 13}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 13} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{-x^{2} + 2 \, x + 8}}}\;dx} = \answer{{\arcsin\left(-\frac{1}{3} \, x + \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 8 \, x + 52}}}\;dx} = \answer{{-9 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} + 2 \, x + 3}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{2} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 2 \, x + 65}}}\;dx} = \answer{{3 \, {\rm arcsinh}\left(\frac{1}{8} \, x + \frac{1}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 14 \, x + 40}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 40} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 12 \, x}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 6 \, x + 45}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{6} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 10 \, x}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{-x^{2} + 16 \, x - 39}}}\;dx} = \answer{{-4 \, \arcsin\left(-\frac{1}{5} \, x + \frac{8}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} - 14 \, x + 45}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 45} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} - 8 \, x + 52}}}\;dx} = \answer{{4 \, {\rm arcsinh}\left(\frac{1}{6} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 6 \, x + 8}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x + 8} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{-x^{2} + 14 \, x + 15}}}\;dx} = \answer{{4 \, \arcsin\left(-\frac{1}{8} \, x + \frac{7}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} - 18 \, x + 17}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 17} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 14 \, x + 74}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{5} \, x - \frac{7}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} + 6 \, x - 27}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 6 \, x - 27} + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} - 2 \, x + 24}}}\;dx} = \answer{{3 \, \arcsin\left(-\frac{1}{5} \, x - \frac{1}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 18 \, x + 85}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{2} \, x + \frac{9}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 14 \, x + 33}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 14 \, x + 33} + 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} - 18 \, x}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{9} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 8 \, x + 97}}}\;dx} = \answer{{-9 \, {\rm arcsinh}\left(\frac{1}{9} \, x + \frac{4}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 8 \, x - 33}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 8 \, x - 33} + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 16 \, x + 28}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 28} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{-x^{2} - 6 \, x + 40}}}\;dx} = \answer{{5 \, \arcsin\left(-\frac{1}{7} \, x - \frac{3}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 8 \, x + 9}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{5} \, x - \frac{4}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 6 \, x + 8}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 6 \, x + 8} + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} - 16 \, x - 15}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{7} \, x - \frac{8}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{-x^{2} + 18 \, x - 72}}}\;dx} = \answer{{-7 \, \arcsin\left(-\frac{1}{3} \, x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 18 \, x + 97}}}\;dx} = \answer{{2 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{9}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} - 10 \, x}}}\;dx} = \answer{{-9 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 10 \, x} - 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} + 4 \, x + 60}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{8} \, x + \frac{1}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} - 6 \, x + 40}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{7} \, x - \frac{3}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{5}{\sqrt{x^{2} - 6 \, x - 72}}}\;dx} = \answer{{-5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 72} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} - 4 \, x + 13}}}\;dx} = \answer{{7 \, {\rm arcsinh}\left(\frac{1}{3} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} - 6 \, x + 72}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{9} \, x - \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} - 8 \, x - 9}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x - 9} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{-x^{2} + 18 \, x - 17}}}\;dx} = \answer{{3 \, \arcsin\left(-\frac{1}{8} \, x + \frac{9}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 14 \, x + 65}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{4} \, x - \frac{7}{4}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} + 8 \, x + 65}}}\;dx} = \answer{{-{\rm arcsinh}\left(\frac{1}{7} \, x + \frac{4}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} - 2 \, x + 80}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{9} \, x - \frac{1}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{-x^{2} + 16 \, x + 17}}}\;dx} = \answer{{-5 \, \arcsin\left(-\frac{1}{9} \, x + \frac{8}{9}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} - 4 \, x}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{2} \, x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} + 14 \, x + 85}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{7}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} + 16 \, x + 65}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{7}{\sqrt{x^{2} + 2 \, x - 8}}}\;dx} = \answer{{7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 2 \, x - 8} + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} + 10 \, x - 11}}}\;dx} = \answer{{2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 10 \, x - 11} + 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} + 6 \, x - 8}}}\;dx} = \answer{{-9 \, \arcsin\left(-x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{-x^{2} + 6 \, x + 16}}}\;dx} = \answer{{7 \, \arcsin\left(-\frac{1}{5} \, x + \frac{3}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} - 18 \, x + 130}}}\;dx} = \answer{{4 \, {\rm arcsinh}\left(\frac{1}{7} \, x - \frac{9}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{x^{2} - 14 \, x + 40}}}\;dx} = \answer{{-8 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x + 40} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{1}{\sqrt{x^{2} - 12 \, x + 11}}}\;dx} = \answer{{-\log\left(2 \, x + 2 \, \sqrt{x^{2} - 12 \, x + 11} - 12\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} - 8 \, x + 12}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 8 \, x + 12} - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{8}{\sqrt{-x^{2} + 2 \, x + 63}}}\;dx} = \answer{{-8 \, \arcsin\left(-\frac{1}{8} \, x + \frac{1}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{-x^{2} + 6 \, x + 55}}}\;dx} = \answer{{2 \, \arcsin\left(-\frac{1}{8} \, x + \frac{3}{8}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 2 \, x + 50}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{7} \, x - \frac{1}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{7}{\sqrt{x^{2} + 4 \, x - 77}}}\;dx} = \answer{{-7 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x - 77} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 4 \, x - 77}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 4 \, x - 77} + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} + 18 \, x}}}\;dx} = \answer{{-2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 18 \, x} + 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 16 \, x + 80}}}\;dx} = \answer{{-4 \, {\rm arcsinh}\left(\frac{1}{4} \, x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{2}{\sqrt{x^{2} - 2 \, x - 80}}}\;dx} = \answer{{2 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 2 \, x - 80} - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} + 8 \, x + 80}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(\frac{1}{8} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{-x^{2} - 8 \, x + 20}}}\;dx} = \answer{{-6 \, \arcsin\left(-\frac{1}{6} \, x - \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} + 6 \, x + 45}}}\;dx} = \answer{{6 \, {\rm arcsinh}\left(\frac{1}{6} \, x + \frac{1}{2}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 6 \, x + 10}}}\;dx} = \answer{{-3 \, {\rm arcsinh}\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{5}{\sqrt{x^{2} + 2 \, x - 48}}}\;dx} = \answer{{5 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 2 \, x - 48} + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{1}{\sqrt{-x^{2} + 16 \, x - 28}}}\;dx} = \answer{{-\arcsin\left(-\frac{1}{6} \, x + \frac{4}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{x^{2} + 12 \, x + 117}}}\;dx} = \answer{{-9 \, {\rm arcsinh}\left(\frac{1}{9} \, x + \frac{2}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{2}{\sqrt{x^{2} - 14 \, x + 85}}}\;dx} = \answer{{-2 \, {\rm arcsinh}\left(\frac{1}{6} \, x - \frac{7}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{4}{\sqrt{x^{2} - 14 \, x - 32}}}\;dx} = \answer{{4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 14 \, x - 32} - 14\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{x^{2} + 16 \, x + 60}}}\;dx} = \answer{{3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 16 \, x + 60} + 16\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{4}{\sqrt{x^{2} - 18 \, x + 65}}}\;dx} = \answer{{-4 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 65} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} - 2 \, x + 37}}}\;dx} = \answer{{-6 \, {\rm arcsinh}\left(\frac{1}{6} \, x - \frac{1}{6}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{6}{\sqrt{x^{2} - 18 \, x + 45}}}\;dx} = \answer{{6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 18 \, x + 45} - 18\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{-x^{2} + 2 \, x + 8}}}\;dx} = \answer{{6 \, \arcsin\left(-\frac{1}{3} \, x + \frac{1}{3}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{6}{\sqrt{x^{2} + 10 \, x}}}\;dx} = \answer{{-6 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} + 10 \, x} + 10\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{9}{\sqrt{-x^{2} + 18 \, x - 72}}}\;dx} = \answer{{9 \, \arcsin\left(-\frac{1}{3} \, x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{8}{\sqrt{-x^{2} - 12 \, x + 13}}}\;dx} = \answer{{8 \, \arcsin\left(-\frac{1}{7} \, x - \frac{6}{7}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{-\frac{3}{\sqrt{x^{2} - 6 \, x - 16}}}\;dx} = \answer{{-3 \, \log\left(2 \, x + 2 \, \sqrt{x^{2} - 6 \, x - 16} - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{3}{\sqrt{-x^{2} + 16 \, x - 39}}}\;dx} = \answer{{-3 \, \arcsin\left(-\frac{1}{5} \, x + \frac{8}{5}\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0042. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0042.HELP.tex}

\[
\int{{\frac{9}{\sqrt{-x^{2} + 4 \, x + 77}}}\;dx} = \answer{{-9 \, \arcsin\left(-\frac{1}{9} \, x + \frac{2}{9}\right)}+C}
\]
\end{problem}}%}

