\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@ByParts, File@0022}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-2 \, x \arctan\left(x\right)}\;dx} = \answer{{-x^{2} \arctan\left(x\right) + x - \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-2 \, \arctan\left(x\right)}\;dx} = \answer{{-2 \, x \arctan\left(x\right) + \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-5 \, \log\left(x\right)}\;dx} = \answer{{-5 \, x \log\left(x\right) + 5 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-8 \, \log\left(x\right)}\;dx} = \answer{{-8 \, x \log\left(x\right) + 8 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-8 \, x \arctan\left(x\right)}\;dx} = \answer{{-4 \, x^{2} \arctan\left(x\right) + 4 \, x - 4 \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{7 \, x \arctan\left(x\right)}\;dx} = \answer{{\frac{7}{2} \, x^{2} \arctan\left(x\right) - \frac{7}{2} \, x + \frac{7}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-6 \, x \arctan\left(x\right)}\;dx} = \answer{{-3 \, x^{2} \arctan\left(x\right) + 3 \, x - 3 \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-\arctan\left(x\right)}\;dx} = \answer{{-x \arctan\left(x\right) + \frac{1}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-7 \, \log\left(x\right)}\;dx} = \answer{{-7 \, x \log\left(x\right) + 7 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{5 \, \log\left(x\right)}\;dx} = \answer{{5 \, x \log\left(x\right) - 5 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{8 \, \arctan\left(x\right)}\;dx} = \answer{{8 \, x \arctan\left(x\right) - 4 \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-9 \, x \arctan\left(x\right)}\;dx} = \answer{{-\frac{9}{2} \, x^{2} \arctan\left(x\right) + \frac{9}{2} \, x - \frac{9}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{4 \, x \arctan\left(x\right)}\;dx} = \answer{{2 \, x^{2} \arctan\left(x\right) - 2 \, x + 2 \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-\log\left(x\right)}\;dx} = \answer{{-x \log\left(x\right) + x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-6 \, \arctan\left(x\right)}\;dx} = \answer{{-6 \, x \arctan\left(x\right) + 3 \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-4 \, x \arctan\left(x\right)}\;dx} = \answer{{-2 \, x^{2} \arctan\left(x\right) + 2 \, x - 2 \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{2 \, \log\left(x\right)}\;dx} = \answer{{2 \, x \log\left(x\right) - 2 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{5 \, \arctan\left(x\right)}\;dx} = \answer{{5 \, x \arctan\left(x\right) - \frac{5}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-4 \, \log\left(x\right)}\;dx} = \answer{{-4 \, x \log\left(x\right) + 4 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{x \arctan\left(x\right)}\;dx} = \answer{{\frac{1}{2} \, x^{2} \arctan\left(x\right) - \frac{1}{2} \, x + \frac{1}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-7 \, \arctan\left(x\right)}\;dx} = \answer{{-7 \, x \arctan\left(x\right) + \frac{7}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{3 \, x \arctan\left(x\right)}\;dx} = \answer{{\frac{3}{2} \, x^{2} \arctan\left(x\right) - \frac{3}{2} \, x + \frac{3}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{7 \, \arctan\left(x\right)}\;dx} = \answer{{7 \, x \arctan\left(x\right) - \frac{7}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{4 \, \log\left(x\right)}\;dx} = \answer{{4 \, x \log\left(x\right) - 4 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-3 \, x \arctan\left(x\right)}\;dx} = \answer{{-\frac{3}{2} \, x^{2} \arctan\left(x\right) + \frac{3}{2} \, x - \frac{3}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{9 \, x \arctan\left(x\right)}\;dx} = \answer{{\frac{9}{2} \, x^{2} \arctan\left(x\right) - \frac{9}{2} \, x + \frac{9}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{6 \, x \arctan\left(x\right)}\;dx} = \answer{{3 \, x^{2} \arctan\left(x\right) - 3 \, x + 3 \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{4 \, \arctan\left(x\right)}\;dx} = \answer{{4 \, x \arctan\left(x\right) - 2 \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-9 \, \arctan\left(x\right)}\;dx} = \answer{{-9 \, x \arctan\left(x\right) + \frac{9}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{6 \, \log\left(x\right)}\;dx} = \answer{{6 \, x \log\left(x\right) - 6 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{8 \, x \arctan\left(x\right)}\;dx} = \answer{{4 \, x^{2} \arctan\left(x\right) - 4 \, x + 4 \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-3 \, \arctan\left(x\right)}\;dx} = \answer{{-3 \, x \arctan\left(x\right) + \frac{3}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-4 \, \arctan\left(x\right)}\;dx} = \answer{{-4 \, x \arctan\left(x\right) + 2 \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{3 \, \log\left(x\right)}\;dx} = \answer{{3 \, x \log\left(x\right) - 3 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{2 \, x \arctan\left(x\right)}\;dx} = \answer{{x^{2} \arctan\left(x\right) - x + \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-2 \, \log\left(x\right)}\;dx} = \answer{{-2 \, x \log\left(x\right) + 2 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-5 \, \arctan\left(x\right)}\;dx} = \answer{{-5 \, x \arctan\left(x\right) + \frac{5}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-x \arctan\left(x\right)}\;dx} = \answer{{-\frac{1}{2} \, x^{2} \arctan\left(x\right) + \frac{1}{2} \, x - \frac{1}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-9 \, \log\left(x\right)}\;dx} = \answer{{-9 \, x \log\left(x\right) + 9 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-3 \, \log\left(x\right)}\;dx} = \answer{{-3 \, x \log\left(x\right) + 3 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{8 \, \log\left(x\right)}\;dx} = \answer{{8 \, x \log\left(x\right) - 8 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-7 \, x \arctan\left(x\right)}\;dx} = \answer{{-\frac{7}{2} \, x^{2} \arctan\left(x\right) + \frac{7}{2} \, x - \frac{7}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{3 \, \arctan\left(x\right)}\;dx} = \answer{{3 \, x \arctan\left(x\right) - \frac{3}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{9 \, \log\left(x\right)}\;dx} = \answer{{9 \, x \log\left(x\right) - 9 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{7 \, \log\left(x\right)}\;dx} = \answer{{7 \, x \log\left(x\right) - 7 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{6 \, \arctan\left(x\right)}\;dx} = \answer{{6 \, x \arctan\left(x\right) - 3 \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-5 \, x \arctan\left(x\right)}\;dx} = \answer{{-\frac{5}{2} \, x^{2} \arctan\left(x\right) + \frac{5}{2} \, x - \frac{5}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{2 \, \arctan\left(x\right)}\;dx} = \answer{{2 \, x \arctan\left(x\right) - \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-8 \, \arctan\left(x\right)}\;dx} = \answer{{-8 \, x \arctan\left(x\right) + 4 \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{-6 \, \log\left(x\right)}\;dx} = \answer{{-6 \, x \log\left(x\right) + 6 \, x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{5 \, x \arctan\left(x\right)}\;dx} = \answer{{\frac{5}{2} \, x^{2} \arctan\left(x\right) - \frac{5}{2} \, x + \frac{5}{2} \, \arctan\left(x\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{\log\left(x\right)}\;dx} = \answer{{x \log\left(x\right) - x}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{\arctan\left(x\right)}\;dx} = \answer{{x \arctan\left(x\right) - \frac{1}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0022. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0022.HELP.tex}

\[\int{{9 \, \arctan\left(x\right)}\;dx} = \answer{{9 \, x \arctan\left(x\right) - \frac{9}{2} \, \log\left(x^{2} + 1\right)}+C}\]
\end{problem}}%}

