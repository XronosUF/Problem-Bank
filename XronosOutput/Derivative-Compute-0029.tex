\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%\tagged{Ans@ShortAns, Type@Compute, Topic@Derivative, Sub@Related-Rates, File@0029}{
\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${980}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1200}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2592}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${972}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1440}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1152}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${800}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2800}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${500}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${600}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${900}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${500}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${384}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${216}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1008}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${768}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${216}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3200}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${448}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1568}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2268}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${640}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${160}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${640}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1620}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${400}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1764}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${784}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1280}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1000}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3240}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1944}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${16}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${36}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1536}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${196}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${256}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${180}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2560}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${432}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${36}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2000}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${112}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3200}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${128}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2592}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${200}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${48}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${600}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${448}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1960}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1176}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${588}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1000}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1620}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1944}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2268}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${392}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${800}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1000}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${300}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${196}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${196}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${128}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${180}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${384}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${512}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${64}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${640}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${392}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${512}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${72}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${648}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1600}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${512}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${432}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${72}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2304}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2048}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1600}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${200}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${96}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${324}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2000}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2304}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${64}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${720}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${400}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1568}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${100}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1152}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1280}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${72}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2560}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${48}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${180}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${648}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1764}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2800}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3200}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${96}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${768}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1792}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${700}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${360}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1008}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${800}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${128}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${108}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3600}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${648}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1024}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1372}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${972}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1008}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2800}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${400}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${576}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${32}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${200}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${900}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2560}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1176}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2800}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${128}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1000}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${256}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${980}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${216}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${448}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${32}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${588}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${980}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1792}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1200}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${16}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${980}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${216}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${80}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${36}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2000}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${252}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${256}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${256}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${80}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1568}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2000}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1372}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2048}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${576}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${360}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${864}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${720}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1152}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${512}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2916}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2916}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3600}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${256}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${360}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${360}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${100}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${864}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${96}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2268}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${252}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${800}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${108}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1620}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${800}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${600}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${64}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3240}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${432}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${192}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3200}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${324}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1792}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${196}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${972}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${192}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${128}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${252}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${32}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${576}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${200}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${576}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${576}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${144}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${96}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${400}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${64}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3600}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1764}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1152}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2304}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${588}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${300}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${512}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${512}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1764}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${72}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2268}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${500}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3240}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${320}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${192}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${640}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1600}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2048}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${192}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1372}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2400}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${448}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${972}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${576}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${700}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1536}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${48}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${36}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${588}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${128}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2916}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${500}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${252}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2560}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${324}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1536}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${700}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${160}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1280}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1200}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${576}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1620}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${128}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2400}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3240}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${324}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${400}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2304}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${64}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${512}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${64}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${900}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${80}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${700}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1008}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1440}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1176}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1568}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2400}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1024}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${400}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1960}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${256}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${300}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${288}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${720}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${16}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1200}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1024}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{2048}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1600}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2592}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${900}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${400}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${784}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${320}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{256}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2592}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${100}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${16}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{1}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${3600}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${108}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1944}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${600}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${800}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{500}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{8}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${48}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${864}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${32}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${4000}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${432}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{3}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${288}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${160}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${180}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${36}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{5}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${2916}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{9}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1296}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{4}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${160}\pi$ ${\text{\texttt{line}}}^3/s{\text{\texttt{{ }(a{ }line{ }is{ }1/12{ }of{ }an{ }inch)}}}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{line}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{line}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${112}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{32}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{7}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1960}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{1372}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${1944}\pi$ ${\text{\texttt{cm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${972}\pi$ ${\text{\texttt{cm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{6}$ ${\text{\texttt{cm}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${4000}\pi$ ${\text{\texttt{in}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{in}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{10}$ ${\text{\texttt{in}}}/s$.
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0029. \\ \fi
\begin{problem}

A snowball is melting at a rate of ${800}\pi$ ${\text{\texttt{mm}}}^3/s{}$.  At what rate is the radius decreasing when the volume of the snowball is ${\frac{4000}{3}}\pi$ ${\text{\texttt{mm}}}^3$?\\[0.5in]

\input{Derivative-Compute-0029.HELP.tex}

The radius is decreasing at $\answer{2}$ ${\text{\texttt{mm}}}/s$.
\end{problem}}%}

