\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@PartialFractions, Sub@LinearFactors, Func@Rational, File@0050}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x + 1\right)}}{{\left(x - 1\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{5}{2} \, \log\left(x - 1\right) - \frac{21}{2} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 7}{{\left(x + 7\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{14}{15} \, \log\left(x + 7\right) + \frac{1}{15} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 4}{{\left(x + 6\right)} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{26}{3} \, \log\left(x + 6\right) + \frac{11}{3} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 3\right)}}{{\left(x + 5\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{13}{3} \, \log\left(x + 5\right) - \frac{1}{3} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 5}{{\left(x + 5\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{15}{8} \, \log\left(x + 5\right) + \frac{1}{8} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 7}{{\left(x - 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{5}{2} \, \log\left(x - 3\right) - \frac{7}{2} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 9}{{\left(x + 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x + 3\right) + \frac{1}{2} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 8}{{\left(x + 9\right)} {\left(x + 8\right)}}}\;dx} = \answer{{-71 \, \log\left(x + 9\right) + 64 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 7}{{\left(x + 2\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{1}{9} \, \log\left(x + 2\right) + \frac{28}{9} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x + 1\right)}}{{\left(x + 6\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{7}{2} \, \log\left(x + 6\right) - \frac{7}{2} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 7}{{\left(x + 3\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\log\left(x + 3\right) + 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 1}{{\left(x - 3\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{16}{5} \, \log\left(x - 3\right) + \frac{41}{5} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 8}{{\left(x - 1\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{7}{2} \, \log\left(x - 1\right) - \frac{5}{2} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 1\right)}}{{\left(x + 8\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{21}{10} \, \log\left(x + 8\right) - \frac{9}{10} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 6}{{\left(x + 6\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{18}{7} \, \log\left(x + 6\right) - \frac{17}{7} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 1}{{\left(x - 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{3 \, \log\left(x - 1\right) - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 4}{{\left(x - 3\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{11}{2} \, \log\left(x - 3\right) + \frac{21}{2} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 3\right)}}{{\left(x + 2\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{21}{5} \, \log\left(x + 2\right) - \frac{9}{5} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x - 5}{{\left(x + 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{77}{17} \, \log\left(x + 8\right) + \frac{76}{17} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 3}{{\left(x + 9\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{33}{8} \, \log\left(x + 9\right) + \frac{1}{8} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x - 5}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{45}{x + 5} + 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 8}{{\left(x + 1\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{9}{10} \, \log\left(x + 1\right) + \frac{1}{10} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x + 1\right)}}{{\left(x - 1\right)} {\left(x - 3\right)}}}\;dx} = \answer{{4 \, \log\left(x - 1\right) - 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 8}{{\left(x + 7\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{13}{9} \, \log\left(x + 7\right) - \frac{14}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 8}{{\left(x - 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{43 \, \log\left(x - 5\right) - 50 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 5}{{\left(x - 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{1}{6} \, \log\left(x - 3\right) - \frac{13}{6} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 8}{{\left(x + 5\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{7}{9} \, \log\left(x + 5\right) + \frac{20}{9} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 3}{{\left(x + 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{37}{11} \, \log\left(x + 5\right) + \frac{51}{11} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3}{x + 4}}\;dx} = \answer{{-3 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 7}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{70}{x - 7} - 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 2}{{\left(x - 2\right)} {\left(x - 5\right)}}}\;dx} = \answer{{4 \, \log\left(x - 2\right) - 11 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x + 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{46}{11} \, \log\left(x + 5\right) - \frac{42}{11} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 4\right)}}{{\left(x - 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{38 \, \log\left(x - 5\right) - 44 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x + 5}{{\left(x + 9\right)} {\left(x + 5\right)}}}\;dx} = \answer{{\frac{31}{4} \, \log\left(x + 9\right) - \frac{15}{4} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 3}{{\left(x - 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{7}{4} \, \log\left(x - 2\right) - \frac{27}{4} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 3}{{\left(x + 5\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{22}{3} \, \log\left(x + 5\right) + \frac{7}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, x + 1}{{\left(x + 5\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{29}{6} \, \log\left(x + 5\right) + \frac{7}{6} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 6}{{\left(x + 3\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{27}{8} \, \log\left(x + 3\right) + \frac{29}{8} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 2\right)}}{{\left(x + 8\right)} {\left(x + 7\right)}}}\;dx} = \answer{{-40 \, \log\left(x + 8\right) + 36 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 4\right)}}{{\left(x + 4\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(x + 4\right) - \frac{2}{3} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 5}{{\left(x + 6\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{53}{12} \, \log\left(x + 6\right) - \frac{43}{12} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 2}{{\left(x + 9\right)} {\left(x + 4\right)}}}\;dx} = \answer{{-\frac{79}{5} \, \log\left(x + 9\right) + \frac{34}{5} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 8}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-18 \, \log\left(x - 4\right) + 25 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 5}{{\left(x + 6\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{59}{4} \, \log\left(x + 6\right) + \frac{23}{4} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 8}{{\left(x + 5\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{53}{7} \, \log\left(x + 5\right) - \frac{10}{7} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 2}{{\left(x + 9\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{47}{7} \, \log\left(x + 9\right) + \frac{12}{7} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 4\right)}}{{\left(x + 6\right)} {\left(x + 2\right)}}}\;dx} = \answer{{\log\left(x + 6\right) + \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 1}{{\left(x + 9\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{64}{11} \, \log\left(x + 9\right) - \frac{13}{11} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 1\right)}}{{\left(x + 9\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{13}{2} \, \log\left(x + 9\right) - \frac{1}{2} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 4}{{\left(x - 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{31}{4} \, \log\left(x - 3\right) - \frac{67}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 1}{{\left(x + 6\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{7}{9} \, \log\left(x + 6\right) - \frac{2}{9} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 5}{{\left(x + 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{5}{7} \, \log\left(x + 5\right) + \frac{2}{7} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, x + 5}{{\left(x + 5\right)} {\left(x + 3\right)}}}\;dx} = \answer{{\frac{25}{2} \, \log\left(x + 5\right) - \frac{13}{2} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 2\right)}}{{\left(x + 8\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{18}{13} \, \log\left(x + 8\right) - \frac{21}{13} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 2}{{\left(x + 3\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{17}{2} \, \log\left(x + 3\right) + \frac{7}{2} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 1}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{22}{x + 7} - 3 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 9}{{\left(x + 9\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{18}{7} \, \log\left(x + 9\right) + \frac{11}{7} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 2\right)}}{{\left(x + 7\right)} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{38}{3} \, \log\left(x + 7\right) - \frac{20}{3} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 9}{{\left(x + 8\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{65}{9} \, \log\left(x + 8\right) + \frac{2}{9} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 4}{{\left(x + 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{5}{3} \, \log\left(x + 1\right) - \frac{2}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, x - 5}{{\left(x + 3\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{23}{2} \, \log\left(x + 3\right) - \frac{11}{2} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x + 9}{{\left(x + 9\right)} {\left(x + 5\right)}}}\;dx} = \answer{{-\frac{63}{4} \, \log\left(x + 9\right) + \frac{31}{4} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 3}{{\left(x - 2\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{5}{3} \, \log\left(x - 2\right) - \frac{8}{3} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 3\right)}}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{2}{5} \, \log\left(x + 1\right) + \frac{38}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 2\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{30}{x + 8} + 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 7}{{\left(x + 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{9}{8} \, \log\left(x + 2\right) + \frac{1}{8} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x + 7}{{\left(x + 8\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{25}{16} \, \log\left(x + 8\right) + \frac{39}{16} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 1\right)}}{{\left(x + 2\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x + 2\right) + \frac{9}{2} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 7}{{\left(x + 6\right)} {\left(x + 5\right)}}}\;dx} = \answer{{-\log\left(x + 6\right) + 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 2\right)}}{{\left(x + 8\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{26}{5} \, \log\left(x + 8\right) - \frac{4}{5} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{1}{x - 7}}\;dx} = \answer{{-\log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 2\right)}}{{\left(x - 3\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{21}{2} \, \log\left(x - 3\right) - \frac{39}{2} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 6}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x - 1} + 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 3}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{2}{x + 5} + \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 5}{{\left(x + 4\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{23}{11} \, \log\left(x + 4\right) - \frac{54}{11} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x + 1}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{7}{6} \, \log\left(x + 4\right) - \frac{5}{6} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 3\right)}}{{\left(x + 3\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{18}{7} \, \log\left(x + 3\right) + \frac{10}{7} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 3\right)}}{{\left(x + 7\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-2 \, \log\left(x + 7\right) - \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x + 4\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{19}{4} \, \log\left(x + 4\right) + \frac{13}{4} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 1\right)}}{{\left(x + 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{1}{3} \, \log\left(x + 3\right) + \frac{5}{3} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 7}{{\left(x + 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{13}{10} \, \log\left(x + 4\right) + \frac{37}{10} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 9}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{19}{2} \, \log\left(x - 4\right) - \frac{33}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, x - 7}{{\left(x + 3\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{25}{6} \, \log\left(x + 3\right) + \frac{11}{6} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 5}{{\left(x + 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{43}{17} \, \log\left(x + 8\right) - \frac{59}{17} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 7}{{\left(x + 4\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{1}{8} \, \log\left(x + 4\right) + \frac{15}{8} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, {\left(x - 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{72}{x - 9} + 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 7}{{\left(x + 9\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{34}{7} \, \log\left(x + 9\right) + \frac{13}{7} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 9}{{\left(x + 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{14}{11} \, \log\left(x + 5\right) + \frac{3}{11} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 2}{{\left(x - 6\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-16 \, \log\left(x - 6\right) + 19 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x + 3\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\log\left(x + 3\right) - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 1\right)}}{{\left(x + 9\right)} {\left(x - 5\right)}}}\;dx} = \answer{{5 \, \log\left(x + 9\right) + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x - 7}{{\left(x + 4\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{43}{9} \, \log\left(x + 4\right) + \frac{38}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, x - 7}{{\left(x + 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{37}{14} \, \log\left(x + 5\right) + \frac{47}{14} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 1}{{\left(x + 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{3}{11} \, \log\left(x + 2\right) + \frac{19}{11} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 2}{{\left(x + 4\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{10}{9} \, \log\left(x + 4\right) + \frac{17}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{17}{3} \, \log\left(x + 4\right) - \frac{7}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 5}{{\left(x - 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{13}{7} \, \log\left(x - 2\right) - \frac{76}{7} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x + 9}{{\left(x - 4\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{41}{5} \, \log\left(x - 4\right) - \frac{81}{5} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2}{x - 2}}\;dx} = \answer{{2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 6}{{\left(x + 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(x + 3\right) - \frac{1}{4} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 4\right)}}{{\left(x - 3\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-26 \, \log\left(x - 3\right) + 32 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 1}{{\left(x - 1\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{1}{2} \, \log\left(x - 1\right) - \frac{7}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 4\right)}}{{\left(x + 7\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{11}{5} \, \log\left(x + 7\right) + \frac{1}{5} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x - 7}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{11}{5} \, \log\left(x + 1\right) + \frac{9}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 1}{{\left(x + 7\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{55}{13} \, \log\left(x + 7\right) + \frac{49}{13} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 2\right)}}{{\left(x + 7\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{5}{6} \, \log\left(x + 7\right) - \frac{7}{6} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x + 5\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{26}{3} \, \log\left(x + 5\right) + \frac{5}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5}{x - 1}}\;dx} = \answer{{5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 5}{{\left(x - 5\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{10}{3} \, \log\left(x - 5\right) - \frac{19}{3} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 9}{{\left(x + 3\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{15}{8} \, \log\left(x + 3\right) - \frac{1}{8} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 5}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{17}{6} \, \log\left(x + 4\right) - \frac{1}{6} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 9}{{\left(x + 7\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{1}{5} \, \log\left(x + 7\right) + \frac{6}{5} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 1\right)}}{{\left(x - 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{3}{4} \, \log\left(x - 2\right) + \frac{15}{4} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x + 1\right)}}{{\left(x - 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-11 \, \log\left(x - 5\right) + 19 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 1}{{\left(x + 2\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{7}{9} \, \log\left(x + 2\right) + \frac{20}{9} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x + 1\right)}}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{14}{3} \, \log\left(x + 4\right) - \frac{10}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 5}{{\left(x + 7\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{16}{9} \, \log\left(x + 7\right) - \frac{11}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 9}{{\left(x + 9\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{9}{5} \, \log\left(x + 9\right) - \frac{11}{5} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, x - 5}{{\left(x - 4\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{19}{4} \, \log\left(x - 4\right) + \frac{43}{4} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 5}{{\left(x + 9\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{14}{17} \, \log\left(x + 9\right) - \frac{3}{17} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 2}{{\left(x + 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(x + 2\right) - \frac{5}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 9}{{\left(x + 2\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{1}{7} \, \log\left(x + 2\right) - \frac{29}{7} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 4}{{\left(x + 3\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{5}{9} \, \log\left(x + 3\right) + \frac{22}{9} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x + 1\right)}}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{15}{2} \, \log\left(x - 4\right) + \frac{21}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 3}{{\left(x + 2\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-5 \, \log\left(x + 2\right) + 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3}{x + 7}}\;dx} = \answer{{3 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x + 1}{{\left(x - 1\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{1}{2} \, \log\left(x - 1\right) - \frac{5}{2} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 7}{{\left(x + 8\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{57}{13} \, \log\left(x + 8\right) + \frac{47}{13} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 5}{{\left(x + 5\right)} {\left(x + 4\right)}}}\;dx} = \answer{{-25 \, \log\left(x + 5\right) + 21 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3}{x + 1}}\;dx} = \answer{{-3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x - 9}{{\left(x + 9\right)} {\left(x + 5\right)}}}\;dx} = \answer{{\frac{45}{4} \, \log\left(x + 9\right) - \frac{29}{4} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 1\right)}}{{\left(x - 2\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\log\left(x - 2\right) + 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 7}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x + 6} + \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 3}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{1}{5} \, \log\left(x + 1\right) + \frac{11}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 8}{{\left(x + 6\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{50}{7} \, \log\left(x + 6\right) + \frac{1}{7} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 9}{{\left(x - 4\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(x - 4\right) - \frac{7}{4} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 2}{{\left(x + 3\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{11}{9} \, \log\left(x + 3\right) - \frac{16}{9} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 6}{{\left(x + 5\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{11}{7} \, \log\left(x + 5\right) - \frac{4}{7} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 3}{{\left(x + 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{5}{8} \, \log\left(x + 2\right) - \frac{27}{8} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 1\right)}}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{33}{2} \, \log\left(x - 4\right) - \frac{51}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 4}{{\left(x + 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{28}{17} \, \log\left(x + 8\right) + \frac{23}{17} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 1}{{\left(x + 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\log\left(x + 5\right) - 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 3}{{\left(x - 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-8 \, \log\left(x - 1\right) + 13 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x + 8}{{\left(x - 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{26}{7} \, \log\left(x - 2\right) + \frac{89}{7} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x - 1\right)}}{{\left(x - 4\right)} {\left(x - 7\right)}}}\;dx} = \answer{{6 \, \log\left(x - 4\right) - 12 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 3\right)}}{{\left(x + 1\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{3}{10} \, \log\left(x + 1\right) + \frac{63}{10} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 1\right)}}{{\left(x + 2\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{9}{7} \, \log\left(x + 2\right) - \frac{12}{7} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 8}{{\left(x - 5\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{17}{2} \, \log\left(x - 5\right) - \frac{27}{2} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, {\left(x - 1\right)}}{{\left(x + 9\right)} {\left(x + 4\right)}}}\;dx} = \answer{{12 \, \log\left(x + 9\right) - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 3\right)}}{{\left(x + 8\right)} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{33}{5} \, \log\left(x + 8\right) + \frac{18}{5} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 5}{{\left(x + 4\right)} {\left(x + 3\right)}}}\;dx} = \answer{{\log\left(x + 4\right) - 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 1\right)}}{{\left(x + 9\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{17}{5} \, \log\left(x + 9\right) - \frac{3}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 3}{{\left(x + 6\right)} {\left(x + 3\right)}}}\;dx} = \answer{{9 \, \log\left(x + 6\right) - 4 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x - 1\right)}}{{\left(x - 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{15 \, \log\left(x - 7\right) - 20 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 1}{{\left(x + 8\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{41}{9} \, \log\left(x + 8\right) + \frac{4}{9} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{23}{x + 6} - 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 2}{{\left(x + 4\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{34}{5} \, \log\left(x + 4\right) - \frac{11}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 8}{{\left(x + 2\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-10 \, \log\left(x + 2\right) + 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 4}{{\left(x + 3\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{13}{6} \, \log\left(x + 3\right) + \frac{5}{6} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 1\right)}}{{\left(x + 7\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{11}{2} \, \log\left(x + 7\right) - \frac{7}{2} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 9}{{\left(x + 4\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{41}{5} \, \log\left(x + 4\right) + \frac{1}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x - 9}{{\left(x + 6\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{33}{13} \, \log\left(x + 6\right) + \frac{19}{13} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 2\right)}}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{2}{5} \, \log\left(x + 1\right) + \frac{28}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 6}{{\left(x + 6\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{12}{5} \, \log\left(x + 6\right) - \frac{7}{5} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{1}{x + 2}}\;dx} = \answer{{-\log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 1}{{\left(x - 2\right)} {\left(x - 3\right)}}}\;dx} = \answer{{19 \, \log\left(x - 2\right) - 28 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 3\right)}}{{\left(x + 2\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{1}{2} \, \log\left(x + 2\right) - \frac{5}{2} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 1}{{\left(x + 6\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{13}{9} \, \log\left(x + 6\right) + \frac{5}{9} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 1\right)}}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{7}{2} \, \log\left(x + 4\right) + \frac{5}{2} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 9}{{\left(x + 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{6}{5} \, \log\left(x + 3\right) + \frac{29}{5} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 3}{{\left(x + 2\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{11}{4} \, \log\left(x + 2\right) - \frac{5}{4} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x + 2\right)}}{{\left(x + 8\right)} {\left(x + 6\right)}}}\;dx} = \answer{{-33 \, \log\left(x + 8\right) + 24 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 2\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{12}{x + 2} + 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 2}{{\left(x + 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{9}{16} \, \log\left(x + 7\right) - \frac{7}{16} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 2}{{\left(x + 9\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{29}{14} \, \log\left(x + 9\right) + \frac{13}{14} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 1}{{\left(x + 7\right)} {\left(x + 1\right)}}}\;dx} = \answer{{8 \, \log\left(x + 7\right) - \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x + 4}{{\left(x - 6\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-29 \, \log\left(x - 6\right) + 38 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 6}{{\left(x - 3\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{9}{5} \, \log\left(x - 3\right) - \frac{34}{5} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 8}{{\left(x + 4\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{12}{7} \, \log\left(x + 4\right) + \frac{23}{7} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 3}{{\left(x - 1\right)} {\left(x - 3\right)}}}\;dx} = \answer{{5 \, \log\left(x - 1\right) - 12 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 1}{{\left(x + 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{7}{5} \, \log\left(x + 3\right) - \frac{18}{5} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, {\left(x - 1\right)}}{{\left(x + 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{10}{3} \, \log\left(x + 1\right) + \frac{5}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 1}{{\left(x + 6\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{29}{9} \, \log\left(x + 6\right) - \frac{16}{9} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 5}{{\left(x + 8\right)} {\left(x + 4\right)}}}\;dx} = \answer{{-\frac{43}{4} \, \log\left(x + 8\right) + \frac{19}{4} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 2\right)}}{{\left(x - 2\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-8 \, \log\left(x - 2\right) + 10 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x - 7}{{\left(x + 6\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{61}{5} \, \log\left(x + 6\right) - \frac{16}{5} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 5}{{\left(x + 8\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{19}{14} \, \log\left(x + 8\right) - \frac{23}{14} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 5}{{\left(x + 4\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{19}{12} \, \log\left(x + 4\right) - \frac{53}{12} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 9}{{\left(x - 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{14 \, \log\left(x - 5\right) - 15 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 9}{{\left(x + 5\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{14}{13} \, \log\left(x + 5\right) - \frac{1}{13} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x + 7\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{29}{9} \, \log\left(x + 7\right) - \frac{16}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 3}{{\left(x + 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{1}{8} \, \log\left(x + 2\right) - \frac{9}{8} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{45}{x + 7} - 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x + 1}{{\left(x + 7\right)} {\left(x + 4\right)}}}\;dx} = \answer{{-\frac{55}{3} \, \log\left(x + 7\right) + \frac{31}{3} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x + 5}{{\left(x - 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{13}{7} \, \log\left(x - 2\right) + \frac{41}{7} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{1}{x + 8}}\;dx} = \answer{{\log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 1}{{\left(x + 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{2 \, \log\left(x + 3\right) + 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x - 4}{{\left(x + 9\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{85}{12} \, \log\left(x + 9\right) + \frac{23}{12} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 3}{{\left(x + 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{5}{4} \, \log\left(x + 3\right) - \frac{11}{4} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 2\right)}}{{\left(x + 7\right)} {\left(x + 4\right)}}}\;dx} = \answer{{12 \, \log\left(x + 7\right) - 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 1}{{\left(x + 4\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{5}{3} \, \log\left(x + 4\right) + \frac{2}{3} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 7}{{\left(x + 7\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{14}{3} \, \log\left(x + 7\right) + \frac{1}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 5}{{\left(x - 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{1}{7} \, \log\left(x - 2\right) + \frac{13}{7} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 8}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x - 1} - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 7}{{\left(x + 1\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{3}{2} \, \log\left(x + 1\right) - \frac{7}{2} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 9}{{\left(x + 9\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{9}{4} \, \log\left(x + 9\right) - \frac{11}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x + 3}{{\left(x + 2\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-13 \, \log\left(x + 2\right) + 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 2}{{\left(x + 4\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{26}{11} \, \log\left(x + 4\right) - \frac{51}{11} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 2}{{\left(x + 1\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(x + 1\right) - \frac{5}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 1}{{\left(x + 6\right)} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{7}{3} \, \log\left(x + 6\right) + \frac{4}{3} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 9}{{\left(x + 4\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{23}{5} \, \log\left(x + 4\right) + \frac{17}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 2\right)}}{{\left(x - 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-10 \, \log\left(x - 1\right) + 16 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 2}{{\left(x + 4\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{10}{7} \, \log\left(x + 4\right) + \frac{11}{7} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 7}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{8}{x - 5} - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 2}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x - 2} + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, {\left(x + 1\right)}}{{\left(x + 8\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{49}{12} \, \log\left(x + 8\right) + \frac{35}{12} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x - 1\right)}}{{\left(x + 6\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{49}{12} \, \log\left(x + 6\right) - \frac{35}{12} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 9}{{\left(x + 7\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{47}{11} \, \log\left(x + 7\right) + \frac{41}{11} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 1}{{\left(x + 9\right)} {\left(x + 8\right)}}}\;dx} = \answer{{62 \, \log\left(x + 9\right) - 55 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 2\right)}}{{\left(x - 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-28 \, \log\left(x - 5\right) + 32 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 1}{{\left(x + 4\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{15}{11} \, \log\left(x + 4\right) - \frac{29}{11} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x + 8}{{\left(x - 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{53}{4} \, \log\left(x - 5\right) + \frac{89}{4} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 7}{{\left(x + 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{1}{8} \, \log\left(x + 2\right) - \frac{31}{8} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 7}{{\left(x + 3\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{8}{7} \, \log\left(x + 3\right) + \frac{27}{7} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x + 8\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{34}{15} \, \log\left(x + 8\right) - \frac{41}{15} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 2}{{\left(x - 2\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{12}{5} \, \log\left(x - 2\right) + \frac{47}{5} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 7}{{\left(x + 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{33}{17} \, \log\left(x + 8\right) - \frac{52}{17} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 2}{{\left(x + 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{54}{17} \, \log\left(x + 8\right) - \frac{65}{17} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 1}{{\left(x - 6\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{11}{3} \, \log\left(x - 6\right) + \frac{17}{3} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 5}{{\left(x - 7\right)} {\left(x - 8\right)}}}\;dx} = \answer{{68 \, \log\left(x - 7\right) - 77 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 7}{{\left(x + 4\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{25}{13} \, \log\left(x + 4\right) + \frac{79}{13} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 6}{{\left(x + 6\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{6}{5} \, \log\left(x + 6\right) - \frac{1}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x + 1\right)}}{{\left(x + 2\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{5}{3} \, \log\left(x + 2\right) + \frac{22}{3} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 5}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{3}{x - 2} - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 3}{{\left(x + 5\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{23}{13} \, \log\left(x + 5\right) - \frac{29}{13} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2}{x - 3}}\;dx} = \answer{{2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 5}{{\left(x + 9\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{68}{11} \, \log\left(x + 9\right) + \frac{9}{11} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x + 1\right)}}{{\left(x + 2\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{7}{5} \, \log\left(x + 2\right) - \frac{33}{5} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 7}{{\left(x + 4\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{39}{11} \, \log\left(x + 4\right) - \frac{49}{11} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 4\right)}}{{\left(x + 5\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{38}{3} \, \log\left(x + 5\right) + \frac{20}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 2}{{\left(x - 1\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{3}{7} \, \log\left(x - 1\right) - \frac{10}{7} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 9}{{\left(x - 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{1}{3} \, \log\left(x - 1\right) - \frac{23}{3} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 4}{{\left(x + 9\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{49}{11} \, \log\left(x + 9\right) - \frac{6}{11} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 1}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{4}{5} \, \log\left(x + 1\right) + \frac{21}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x - 1\right)}}{{\left(x - 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{5}{4} \, \log\left(x - 2\right) - \frac{25}{4} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 3}{{\left(x + 7\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{5}{4} \, \log\left(x + 7\right) + \frac{1}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, x + 1}{{\left(x + 6\right)} {\left(x + 3\right)}}}\;dx} = \answer{{\frac{35}{3} \, \log\left(x + 6\right) - \frac{17}{3} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x + 5}{{\left(x - 2\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x - 2\right) - \frac{7}{2} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 3}{{\left(x + 2\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{11}{5} \, \log\left(x + 2\right) - \frac{9}{5} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 7}{{\left(x + 5\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{1}{4} \, \log\left(x + 5\right) + \frac{7}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 1\right)}}{{\left(x + 5\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-3 \, \log\left(x + 5\right) + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 1}{{\left(x - 2\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\log\left(x - 2\right) - 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 6}{{\left(x - 3\right)} {\left(x - 4\right)}}}\;dx} = \answer{{15 \, \log\left(x - 3\right) - 22 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 1}{{\left(x - 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{25 \, \log\left(x - 7\right) - 32 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 2}{{\left(x + 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{38}{17} \, \log\left(x + 8\right) - \frac{47}{17} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 1\right)}}{{\left(x + 1\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{3}{4} \, \log\left(x + 1\right) + \frac{9}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{1}{x - 9}}\;dx} = \answer{{-\log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 4}{{\left(x + 9\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{5}{18} \, \log\left(x + 9\right) + \frac{13}{18} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x - 5}{{\left(x + 8\right)} {\left(x + 2\right)}}}\;dx} = \answer{{\frac{37}{6} \, \log\left(x + 8\right) - \frac{13}{6} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 1}{{\left(x + 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{19}{11} \, \log\left(x + 5\right) - \frac{25}{11} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 8}{{\left(x - 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{27}{4} \, \log\left(x - 5\right) + \frac{55}{4} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 1}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{44}{x - 9} + 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 1\right)}}{{\left(x + 6\right)} {\left(x + 4\right)}}}\;dx} = \answer{{10 \, \log\left(x + 6\right) - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x - 1}{{\left(x + 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{13}{8} \, \log\left(x + 2\right) - \frac{35}{8} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 9}{{\left(x + 8\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{73}{13} \, \log\left(x + 8\right) - \frac{31}{13} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 1}{{\left(x + 6\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{29}{8} \, \log\left(x + 6\right) + \frac{11}{8} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, {\left(x - 1\right)}}{{\left(x + 2\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{21}{10} \, \log\left(x + 2\right) + \frac{49}{10} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 5}{{\left(x + 9\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{67}{10} \, \log\left(x + 9\right) + \frac{13}{10} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 3}{{\left(x + 6\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{39}{5} \, \log\left(x + 6\right) + \frac{4}{5} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 2\right)}}{{\left(x + 9\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{87}{7} \, \log\left(x + 9\right) + \frac{24}{7} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 1\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{14}{x + 4} + 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 4\right)}}{{\left(x - 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{10 \, \log\left(x - 1\right) - 12 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 5}{{\left(x - 1\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{1}{2} \, \log\left(x - 1\right) - \frac{3}{2} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{{\left(x - 1\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{5}{7} \, \log\left(x - 1\right) - \frac{23}{7} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 9}{{\left(x + 5\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{14}{3} \, \log\left(x + 5\right) + \frac{11}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 1}{{\left(x + 9\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{35}{16} \, \log\left(x + 9\right) - \frac{29}{16} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 3\right)}}{{\left(x + 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{21}{11} \, \log\left(x + 5\right) - \frac{45}{11} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 8}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{6 \, \log\left(x + 4\right) + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 2}{{\left(x + 9\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{83}{7} \, \log\left(x + 9\right) + \frac{20}{7} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 1}{{\left(x - 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{7 \, \log\left(x - 1\right) - 13 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 1}{{\left(x + 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{5}{11} \, \log\left(x + 2\right) + \frac{17}{11} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 9}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{5}{2} \, \log\left(x - 4\right) + \frac{3}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 1}{{\left(x + 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{7}{12} \, \log\left(x + 3\right) - \frac{17}{12} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x + 1\right)}}{{\left(x + 4\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{27}{2} \, \log\left(x + 4\right) + \frac{9}{2} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 2\right)}}{{\left(x + 7\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-3 \, \log\left(x + 7\right) - \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x - 9}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{17}{x + 1} + 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x - 3}{{\left(x - 4\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{29}{4} \, \log\left(x - 4\right) + \frac{61}{4} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 3}{{\left(x + 4\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{23}{3} \, \log\left(x + 4\right) + \frac{8}{3} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 5}{{\left(x - 4\right)} {\left(x - 5\right)}}}\;dx} = \answer{{29 \, \log\left(x - 4\right) - 35 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 2\right)}}{{\left(x + 7\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{5}{4} \, \log\left(x + 7\right) - \frac{7}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 3\right)}}{{\left(x + 8\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{19}{3} \, \log\left(x + 8\right) + \frac{7}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 1\right)}}{{\left(x - 3\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{14}{5} \, \log\left(x - 3\right) + \frac{34}{5} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 8}{{\left(x + 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{1}{16} \, \log\left(x + 7\right) + \frac{17}{16} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x - 5}{{\left(x + 2\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{23}{6} \, \log\left(x + 2\right) + \frac{31}{6} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x - 1}{{\left(x + 8\right)} {\left(x + 6\right)}}}\;dx} = \answer{{\frac{65}{2} \, \log\left(x + 8\right) - \frac{49}{2} \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 1}{{\left(x + 3\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{7}{2} \, \log\left(x + 3\right) - \frac{3}{2} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 2}{{\left(x + 8\right)} {\left(x + 7\right)}}}\;dx} = \answer{{6 \, \log\left(x + 8\right) - 5 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, {\left(x + 1\right)}}{{\left(x - 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-4 \, \log\left(x - 1\right) + 10 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x - 1\right)}}{{\left(x - 3\right)} {\left(x - 6\right)}}}\;dx} = \answer{{4 \, \log\left(x - 3\right) - 10 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 1\right)}}{{\left(x + 4\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{18}{7} \, \log\left(x + 4\right) - \frac{24}{7} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 9}{{\left(x - 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(x - 2\right) + \frac{21}{4} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 5}{{\left(x - 6\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{13}{2} \, \log\left(x - 6\right) + \frac{19}{2} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 1\right)}}{{\left(x + 6\right)} {\left(x + 5\right)}}}\;dx} = \answer{{26 \, \log\left(x + 6\right) - 22 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 7}{{\left(x - 1\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\log\left(x - 1\right) - 10 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x - 1\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{8}{x - 2} - 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 2\right)}}{{\left(x + 6\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-\frac{24}{5} \, \log\left(x + 6\right) + \frac{9}{5} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 4\right)}}{{\left(x + 3\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{26}{7} \, \log\left(x + 3\right) + \frac{16}{7} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 5}{{\left(x - 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{50 \, \log\left(x - 5\right) - 59 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 1\right)}}{{\left(x + 4\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{39}{11} \, \log\left(x + 4\right) - \frac{60}{11} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 7}{{\left(x - 2\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x - 2\right) - \frac{19}{2} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 3}{{\left(x + 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{11}{7} \, \log\left(x + 5\right) + \frac{24}{7} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 1\right)}}{{\left(x + 5\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-2 \, \log\left(x + 5\right) - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 7}{{\left(x + 2\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{5}{3} \, \log\left(x + 2\right) + \frac{8}{3} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2}{x + 4}}\;dx} = \answer{{2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2}{x - 4}}\;dx} = \answer{{-2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 8}{{\left(x - 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{17}{6} \, \log\left(x - 3\right) + \frac{35}{6} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 1}{{\left(x + 8\right)} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{41}{4} \, \log\left(x + 8\right) - \frac{21}{4} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 4\right)}}{{\left(x + 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{4}{11} \, \log\left(x + 2\right) + \frac{26}{11} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 2\right)}}{{\left(x + 6\right)} {\left(x + 3\right)}}}\;dx} = \answer{{\frac{32}{3} \, \log\left(x + 6\right) - \frac{20}{3} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 8}{{\left(x - 3\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{35}{2} \, \log\left(x - 3\right) - \frac{53}{2} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 9}{{\left(x + 9\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{9}{8} \, \log\left(x + 9\right) - \frac{1}{8} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 5}{{\left(x + 6\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{19}{4} \, \log\left(x + 6\right) + \frac{3}{4} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3}{x - 6}}\;dx} = \answer{{3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{{\left(x + 5\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{29}{3} \, \log\left(x + 5\right) + \frac{17}{3} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 2}{{\left(x - 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{7}{4} \, \log\left(x - 3\right) - \frac{19}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x - 1\right)}}{{\left(x + 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{9}{2} \, \log\left(x + 4\right) - \frac{9}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 1}{{\left(x + 8\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{55}{13} \, \log\left(x + 8\right) + \frac{36}{13} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 4}{{\left(x + 9\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{59}{18} \, \log\left(x + 9\right) + \frac{67}{18} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 3}{{\left(x + 6\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{3}{10} \, \log\left(x + 6\right) + \frac{7}{10} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 4}{{\left(x + 3\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-31 \, \log\left(x + 3\right) + 22 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 1\right)}}{{\left(x + 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{12}{11} \, \log\left(x + 2\right) - \frac{32}{11} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 5}{{\left(x + 8\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{19}{12} \, \log\left(x + 8\right) - \frac{17}{12} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 3}{{\left(x - 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{8}{3} \, \log\left(x - 1\right) + \frac{23}{3} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 1\right)}}{{\left(x + 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{5}{2} \, \log\left(x + 7\right) + \frac{7}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x + 2\right)}}{{\left(x + 7\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{5}{3} \, \log\left(x + 7\right) + \frac{4}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 5}{{\left(x + 8\right)} {\left(x + 6\right)}}}\;dx} = \answer{{\frac{29}{2} \, \log\left(x + 8\right) - \frac{23}{2} \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 1\right)}}{{\left(x + 6\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{22}{9} \, \log\left(x + 6\right) - \frac{14}{9} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 4}{{\left(x + 3\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{31}{5} \, \log\left(x + 3\right) - \frac{14}{5} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 1}{{\left(x + 9\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{80}{13} \, \log\left(x + 9\right) - \frac{37}{13} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x + 7}{{\left(x - 1\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{11}{4} \, \log\left(x - 1\right) + \frac{27}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 4\right)}}{{\left(x + 1\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{6}{7} \, \log\left(x + 1\right) + \frac{20}{7} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 3}{{\left(x - 3\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\log\left(x - 3\right) + 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 7}{{\left(x + 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{57}{17} \, \log\left(x + 8\right) + \frac{79}{17} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 4}{{\left(x + 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{7}{3} \, \log\left(x + 1\right) + \frac{2}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 2}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{1}{5} \, \log\left(x + 1\right) - \frac{6}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x + 5\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{13}{3} \, \log\left(x + 5\right) - \frac{8}{3} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x - 4}{{\left(x + 8\right)} {\left(x + 6\right)}}}\;dx} = \answer{{38 \, \log\left(x + 8\right) - 29 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x - 7}{{\left(x + 8\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{71}{7} \, \log\left(x + 8\right) - \frac{15}{7} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 8}{{\left(x + 5\right)} {\left(x + 4\right)}}}\;dx} = \answer{{3 \, \log\left(x + 5\right) - 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 6}{{\left(x + 9\right)} {\left(x + 7\right)}}}\;dx} = \answer{{-\frac{15}{2} \, \log\left(x + 9\right) + \frac{13}{2} \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 3\right)}}{{\left(x + 6\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{27}{11} \, \log\left(x + 6\right) + \frac{6}{11} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x - 1\right)}}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{27}{2} \, \log\left(x - 4\right) - \frac{45}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 4}{{\left(x + 1\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(x + 1\right) + \frac{13}{4} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x - 1\right)}}{{\left(x + 4\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{45}{2} \, \log\left(x + 4\right) + \frac{27}{2} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 3}{{\left(x + 9\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{21}{8} \, \log\left(x + 9\right) + \frac{19}{8} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{13}{5} \, \log\left(x + 1\right) - \frac{7}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x + 6\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{50}{13} \, \log\left(x + 6\right) - \frac{54}{13} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{1}{x - 6}}\;dx} = \answer{{\log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 6}{{\left(x + 6\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{12}{7} \, \log\left(x + 6\right) - \frac{5}{7} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 6}{{\left(x + 4\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{2}{5} \, \log\left(x + 4\right) + \frac{7}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 6}{{\left(x + 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{11}{5} \, \log\left(x + 4\right) - \frac{24}{5} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x + 9}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{55}{x + 8} - 8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x + 7}{{\left(x + 5\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{38}{7} \, \log\left(x + 5\right) + \frac{25}{7} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x - 3}{{\left(x + 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{11}{3} \, \log\left(x + 1\right) + \frac{13}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 4}{{\left(x + 9\right)} {\left(x + 4\right)}}}\;dx} = \answer{{-\frac{13}{5} \, \log\left(x + 9\right) + \frac{8}{5} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 1}{{\left(x + 8\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{17}{7} \, \log\left(x + 8\right) - \frac{3}{7} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 3}{{\left(x + 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x + 3\right) + \frac{7}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 7}{{\left(x + 7\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{14}{9} \, \log\left(x + 7\right) + \frac{13}{9} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 5}{{\left(x + 3\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{26}{5} \, \log\left(x + 3\right) - \frac{9}{5} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 1}{{\left(x + 6\right)} {\left(x + 5\right)}}}\;dx} = \answer{{17 \, \log\left(x + 6\right) - 14 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 2}{{\left(x - 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-22 \, \log\left(x - 8\right) + 25 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x + 9\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{25}{16} \, \log\left(x + 9\right) - \frac{7}{16} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 4}{{\left(x + 5\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{9}{11} \, \log\left(x + 5\right) + \frac{2}{11} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 5}{{\left(x - 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{35}{4} \, \log\left(x - 5\right) - \frac{59}{4} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 4}{{\left(x - 1\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{1}{4} \, \log\left(x - 1\right) - \frac{21}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 1}{{\left(x + 5\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{29}{12} \, \log\left(x + 5\right) - \frac{43}{12} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 9}{{\left(x - 3\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x - 3\right) - \frac{13}{2} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 2\right)}}{{\left(x + 3\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{7}{4} \, \log\left(x + 3\right) - \frac{17}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x - 5}{{\left(x + 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{29}{10} \, \log\left(x + 4\right) - \frac{31}{10} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 7}{{\left(x - 1\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\log\left(x - 1\right) + 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 2\right)}}{{\left(x + 6\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{8}{5} \, \log\left(x + 6\right) + \frac{7}{5} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 3}{{\left(x - 3\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-9 \, \log\left(x - 3\right) + 11 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 4}{{\left(x + 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{1}{4} \, \log\left(x + 2\right) - \frac{11}{4} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x - 5}{{\left(x + 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{25}{14} \, \log\left(x + 5\right) + \frac{31}{14} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 7}{{\left(x - 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{49}{2} \, \log\left(x - 7\right) - \frac{65}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 2\right)}}{{\left(x + 8\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{9}{8} \, \log\left(x + 8\right) - \frac{15}{8} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 1}{{\left(x + 3\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{10}{11} \, \log\left(x + 3\right) - \frac{23}{11} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 4}{{\left(x + 4\right)} {\left(x + 2\right)}}}\;dx} = \answer{{16 \, \log\left(x + 4\right) - 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 1\right)}}{{\left(x + 8\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{62}{13} \, \log\left(x + 8\right) + \frac{42}{13} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 3\right)}}{{\left(x + 9\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{9}{4} \, \log\left(x + 9\right) - \frac{3}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 5}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{2}{x + 1} + 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 2\right)}}{{\left(x - 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{11}{3} \, \log\left(x - 3\right) + \frac{29}{3} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x + 3}{{\left(x + 3\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{21}{5} \, \log\left(x + 3\right) - \frac{19}{5} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 5}{{\left(x + 6\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{47}{12} \, \log\left(x + 6\right) - \frac{37}{12} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 5}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{12}{5} \, \log\left(x + 1\right) + \frac{23}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 3\right)}}{{\left(x + 7\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{33}{10} \, \log\left(x + 7\right) - \frac{27}{10} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 4}{{\left(x + 1\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{1}{6} \, \log\left(x + 1\right) + \frac{19}{6} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x - 9}{{\left(x + 8\right)} {\left(x + 4\right)}}}\;dx} = \answer{{\frac{41}{4} \, \log\left(x + 8\right) - \frac{25}{4} \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 1\right)}}{{\left(x + 3\right)} {\left(x + 2\right)}}}\;dx} = \answer{{4 \, \log\left(x + 3\right) - 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 2\right)}}{{\left(x + 8\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{8}{3} \, \log\left(x + 8\right) - \frac{4}{3} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x + 7}{{\left(x + 9\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{74}{11} \, \log\left(x + 9\right) - \frac{25}{11} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 8}{{\left(x - 1\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{1}{8} \, \log\left(x - 1\right) + \frac{55}{8} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 1}{{\left(x + 9\right)} {\left(x + 2\right)}}}\;dx} = \answer{{\frac{71}{7} \, \log\left(x + 9\right) - \frac{15}{7} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 7}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{9}{2} \, \log\left(x - 4\right) - \frac{17}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 2\right)}}{{\left(x + 5\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{28}{9} \, \log\left(x + 5\right) - \frac{8}{9} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 3\right)}}{{\left(x + 4\right)} {\left(x + 1\right)}}}\;dx} = \answer{{5 \, \log\left(x + 4\right) + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 2\right)}}{{\left(x - 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{20 \, \log\left(x - 8\right) - 22 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 1}{{\left(x - 4\right)} {\left(x - 9\right)}}}\;dx} = \answer{{5 \, \log\left(x - 4\right) - 11 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 7}{{\left(x + 4\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{23}{7} \, \log\left(x + 4\right) - \frac{5}{7} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 9}{{\left(x + 4\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{41}{12} \, \log\left(x + 4\right) - \frac{55}{12} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 1\right)}}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{10}{3} \, \log\left(x + 4\right) - \frac{2}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{6 \, {\left(x + 1\right)}}{{\left(x + 9\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{24}{7} \, \log\left(x + 9\right) + \frac{18}{7} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 7}{{\left(x + 8\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{39}{16} \, \log\left(x + 8\right) - \frac{25}{16} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 1}{{\left(x + 3\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{23}{4} \, \log\left(x + 3\right) + \frac{9}{4} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 1}{{\left(x + 7\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{24}{7} \, \log\left(x + 7\right) + \frac{25}{7} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 7}{{\left(x - 1\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{11}{7} \, \log\left(x - 1\right) - \frac{39}{7} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 8}{{\left(x + 9\right)} {\left(x + 5\right)}}}\;dx} = \answer{{\frac{55}{4} \, \log\left(x + 9\right) - \frac{27}{4} \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x + 9}{{\left(x - 6\right)} {\left(x - 7\right)}}}\;dx} = \answer{{33 \, \log\left(x - 6\right) - 37 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 7}{{\left(x + 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{35}{16} \, \log\left(x + 7\right) - \frac{29}{16} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 7}{{\left(x + 7\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{49}{13} \, \log\left(x + 7\right) + \frac{55}{13} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 7}{{\left(x + 3\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{16}{9} \, \log\left(x + 3\right) - \frac{11}{9} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 2\right)}}{{\left(x - 5\right)} {\left(x - 8\right)}}}\;dx} = \answer{{\frac{14}{3} \, \log\left(x - 5\right) - \frac{20}{3} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x + 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{17}{5} \, \log\left(x + 4\right) - \frac{23}{5} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 2\right)}}{{\left(x - 6\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-16 \, \log\left(x - 6\right) + 18 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 9}{{\left(x - 5\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{26}{3} \, \log\left(x - 5\right) + \frac{47}{3} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{9 \, x - 8}{{\left(x - 8\right)} {\left(x - 9\right)}}}\;dx} = \answer{{64 \, \log\left(x - 8\right) - 73 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x - 4}{{\left(x + 8\right)} {\left(x + 7\right)}}}\;dx} = \answer{{76 \, \log\left(x + 8\right) - 67 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 3\right)}}{{\left(x + 9\right)} {\left(x + 5\right)}}}\;dx} = \answer{{-6 \, \log\left(x + 9\right) + 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x + 2}{{\left(x + 9\right)} {\left(x + 1\right)}}}\;dx} = \answer{{\frac{25}{8} \, \log\left(x + 9\right) - \frac{1}{8} \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x + 7}{{\left(x + 5\right)} {\left(x + 3\right)}}}\;dx} = \answer{{-\frac{33}{2} \, \log\left(x + 5\right) + \frac{17}{2} \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x + 8}{{\left(x + 1\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{5}{8} \, \log\left(x + 1\right) - \frac{29}{8} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 1}{{\left(x + 2\right)} {\left(x + 1\right)}}}\;dx} = \answer{{-5 \, \log\left(x + 2\right) + 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 3}{{\left(x + 4\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{1}{9} \, \log\left(x + 4\right) + \frac{8}{9} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 1\right)}}{{\left(x - 7\right)} {\left(x - 8\right)}}}\;dx} = \answer{{39 \, \log\left(x - 7\right) - 45 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x - 1}{{\left(x - 7\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-13 \, \log\left(x - 7\right) + 15 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 6}{{\left(x + 8\right)} {\left(x - 5\right)}}}\;dx} = \answer{{\frac{14}{13} \, \log\left(x + 8\right) - \frac{1}{13} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 5}{{\left(x - 4\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{11}{2} \, \log\left(x - 4\right) - \frac{19}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 7}{{\left(x + 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{33}{14} \, \log\left(x + 5\right) + \frac{79}{14} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x + 7}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{61}{x - 6} + 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, x + 3}{{\left(x - 3\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{5}{2} \, \log\left(x - 3\right) + \frac{13}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 3}{{\left(x + 9\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{3}{2} \, \log\left(x + 9\right) + \frac{1}{2} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 4\right)}}{{\left(x - 5\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{11}{2} \, \log\left(x - 5\right) - \frac{23}{2} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 9}{{\left(x + 4\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{1}{5} \, \log\left(x + 4\right) + \frac{11}{5} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{8 \, x - 7}{{\left(x + 8\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{71}{16} \, \log\left(x + 8\right) - \frac{57}{16} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 7}{{\left(x + 6\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{31}{10} \, \log\left(x + 6\right) - \frac{9}{10} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 3\right)}}{{\left(x + 7\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{33}{5} \, \log\left(x + 7\right) + \frac{3}{5} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 2\right)}}{{\left(x + 9\right)} {\left(x + 6\right)}}}\;dx} = \answer{{\frac{58}{3} \, \log\left(x + 9\right) - \frac{40}{3} \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 3\right)}}{{\left(x - 2\right)} {\left(x - 6\right)}}}\;dx} = \answer{{\frac{7}{2} \, \log\left(x - 2\right) - \frac{15}{2} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 5}{{\left(x + 2\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{19}{4} \, \log\left(x + 2\right) + \frac{9}{4} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 2}{{\left(x + 1\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{1}{7} \, \log\left(x + 1\right) + \frac{8}{7} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(x + 1\right)}}{{\left(x - 6\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{21}{2} \, \log\left(x - 6\right) + \frac{27}{2} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x - 1\right)}}{{\left(x + 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{45}{16} \, \log\left(x + 7\right) + \frac{51}{16} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 4}{{\left(x + 8\right)} {\left(x + 4\right)}}}\;dx} = \answer{{3 \, \log\left(x + 8\right) - 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x - 5}{{\left(x + 5\right)} {\left(x - 7\right)}}}\;dx} = \answer{{-\frac{5}{4} \, \log\left(x + 5\right) - \frac{3}{4} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 4}{{\left(x + 8\right)} {\left(x - 1\right)}}}\;dx} = \answer{{4 \, \log\left(x + 8\right) + \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 7}{{\left(x + 1\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{1}{3} \, \log\left(x + 1\right) - \frac{19}{3} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 6}{{\left(x + 4\right)} {\left(x - 7\right)}}}\;dx} = \answer{{\frac{26}{11} \, \log\left(x + 4\right) + \frac{29}{11} \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x - 9}{{\left(x + 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{14}{5} \, \log\left(x + 1\right) - \frac{11}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 8}{{\left(x + 5\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-9 \, \log\left(x + 5\right) + 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 3\right)}}{{\left(x + 2\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{2}{3} \, \log\left(x + 2\right) - \frac{10}{3} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x + 2} - 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, x + 3}{{\left(x + 9\right)} {\left(x + 6\right)}}}\;dx} = \answer{{5 \, \log\left(x + 9\right) - 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x + 1\right)}}{{\left(x + 7\right)} {\left(x - 9\right)}}}\;dx} = \answer{{\frac{13}{4} \, \log\left(x + 7\right) + \frac{19}{4} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x + 4}{{\left(x + 6\right)} {\left(x - 1\right)}}}\;dx} = \answer{{\frac{26}{7} \, \log\left(x + 6\right) + \frac{9}{7} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{{\left(x + 5\right)} {\left(x - 8\right)}}}\;dx} = \answer{{-\frac{29}{13} \, \log\left(x + 5\right) - \frac{23}{13} \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 3\right)}}{{\left(x + 9\right)} {\left(x - 2\right)}}}\;dx} = \answer{{\frac{42}{11} \, \log\left(x + 9\right) + \frac{2}{11} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{2}{x - 1} - 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, {\left(x - 1\right)}}{{\left(x + 3\right)} {\left(x - 3\right)}}}\;dx} = \answer{{\frac{14}{3} \, \log\left(x + 3\right) + \frac{7}{3} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x + 1\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-5 \, \log\left(x + 1\right) - 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{9 \, x + 2}{{\left(x + 9\right)} {\left(x - 4\right)}}}\;dx} = \answer{{\frac{79}{13} \, \log\left(x + 9\right) + \frac{38}{13} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 1\right)}}{{\left(x + 8\right)} {\left(x + 7\right)}}}\;dx} = \answer{{-18 \, \log\left(x + 8\right) + 16 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 3}{{\left(x + 9\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{11}{2} \, \log\left(x + 9\right) - \frac{3}{2} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 6}{{\left(x + 6\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{6}{5} \, \log\left(x + 6\right) + \frac{1}{5} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x + 9}{{\left(x - 4\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{13}{5} \, \log\left(x - 4\right) + \frac{18}{5} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{x - 4}{{\left(x + 5\right)} {\left(x + 2\right)}}}\;dx} = \answer{{3 \, \log\left(x + 5\right) - 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 3}{{\left(x + 9\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{60}{7} \, \log\left(x + 9\right) + \frac{11}{7} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, x - 5}{{\left(x + 3\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{7}{3} \, \log\left(x + 3\right) - \frac{2}{3} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7}{x + 6}}\;dx} = \answer{{7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 2\right)}}{{\left(x - 6\right)} {\left(x - 9\right)}}}\;dx} = \answer{{4 \, \log\left(x - 6\right) - 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x + 2\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{5}{11} \, \log\left(x + 2\right) - \frac{72}{11} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 2\right)}}{{\left(x - 2\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-4 \, \log\left(x - 2\right) + 10 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, x + 3}{{\left(x + 4\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{5}{2} \, \log\left(x + 4\right) + \frac{1}{2} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x + 1}{{\left(x + 4\right)} {\left(x + 2\right)}}}\;dx} = \answer{{-\frac{23}{2} \, \log\left(x + 4\right) + \frac{11}{2} \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 2}{{\left(x + 4\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-5 \, \log\left(x + 4\right) - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{5 \, x - 3}{{\left(x + 3\right)} {\left(x + 2\right)}}}\;dx} = \answer{{18 \, \log\left(x + 3\right) - 13 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x + 4}{{\left(x + 5\right)} {\left(x + 4\right)}}}\;dx} = \answer{{31 \, \log\left(x + 5\right) - 24 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x - 1\right)}}{{\left(x + 9\right)} {\left(x - 9\right)}}}\;dx} = \answer{{-\frac{38}{9} \, \log\left(x + 9\right) - \frac{34}{9} \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x + 8\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{11}{2} \, \log\left(x + 8\right) - \frac{5}{2} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 1\right)}}{{\left(x + 1\right)} {\left(x - 6\right)}}}\;dx} = \answer{{-\frac{12}{7} \, \log\left(x + 1\right) - \frac{51}{7} \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{7 \, x - 4}{{\left(x + 8\right)} {\left(x + 2\right)}}}\;dx} = \answer{{10 \, \log\left(x + 8\right) - 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 1\right)}}{{\left(x - 3\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-8 \, \log\left(x - 3\right) + 12 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 1\right)}}{{\left(x + 9\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{56}{13} \, \log\left(x + 9\right) - \frac{22}{13} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 1\right)}}{{\left(x + 6\right)} {\left(x - 3\right)}}}\;dx} = \answer{{-\frac{7}{3} \, \log\left(x + 6\right) - \frac{2}{3} \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{6 \, x - 7}{{\left(x + 7\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{49}{11} \, \log\left(x + 7\right) - \frac{17}{11} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x - 5}{{\left(x - 1\right)} {\left(x - 4\right)}}}\;dx} = \answer{{-\frac{4}{3} \, \log\left(x - 1\right) + \frac{1}{3} \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{8 \, x + 9}{{\left(x + 5\right)} {\left(x + 4\right)}}}\;dx} = \answer{{31 \, \log\left(x + 5\right) - 23 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{7 \, x - 9}{{\left(x + 6\right)} {\left(x - 2\right)}}}\;dx} = \answer{{-\frac{51}{8} \, \log\left(x + 6\right) - \frac{5}{8} \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{\frac{3 \, x - 1}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{28}{x + 9} + 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{x + 4}{{\left(x + 6\right)} {\left(x - 1\right)}}}\;dx} = \answer{{-\frac{2}{7} \, \log\left(x + 6\right) - \frac{5}{7} \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0050. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0050.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 3\right)}}{{\left(x + 3\right)} {\left(x - 5\right)}}}\;dx} = \answer{{-\frac{9}{4} \, \log\left(x + 3\right) - \frac{7}{4} \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

