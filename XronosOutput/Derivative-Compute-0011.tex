\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%\tagged{Ans@ShortAns, Type@Compute, Topic@Derivative, Sub@Quotient-Rule, File@0011}{
\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \cos\left(x\right)}{x^{3}}}\right)=\answer{\frac{3 \, \sin\left(x\right)}{x^{3}} + \frac{9 \, \cos\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{3}}{x - 4}}\right)=\answer{-\frac{9 \, x^{2}}{x - 4} + \frac{3 \, x^{3}}{{\left(x - 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{2}}{\cos\left(x\right)}}\right)=\answer{-\frac{3 \, x^{2} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{6 \, x}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{4}}{\log\left(x\right)}}\right)=\answer{\frac{20 \, x^{3}}{\log\left(x\right)} - \frac{5 \, x^{3}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, {\left(x + 3\right)}}{\sqrt{x}}}\right)=\answer{-\frac{3 \, {\left(x + 3\right)}}{2 \, x^{\frac{3}{2}}} + \frac{3}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, e^{x}}{x^{2}}}\right)=\answer{\frac{3 \, e^{x}}{x^{2}} - \frac{6 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-x^{\frac{7}{2}}}\right)=\answer{-\frac{7}{2} \, x^{\frac{5}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, x^{2}}\right)=\answer{-8 \, x}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({3 \, x^{4} e^{\left(-x\right)}}\right)=\answer{-3 \, x^{4} e^{\left(-x\right)} + 12 \, x^{3} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, x}\right)=\answer{-2}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x^{2}}{x + 3}}\right)=\answer{-\frac{2 \, x}{x + 3} + \frac{x^{2}}{{\left(x + 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{2}}{\log\left(x\right)}}\right)=\answer{-\frac{6 \, x}{\log\left(x\right)} + \frac{3 \, x}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{6 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{4}}{\log\left(x\right)}}\right)=\answer{\frac{4 \, x^{3}}{\log\left(x\right)} - \frac{x^{3}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sin\left(x\right)}{x^{2}}}\right)=\answer{-\frac{5 \, \cos\left(x\right)}{x^{2}} + \frac{10 \, \sin\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, x^{4} e^{\left(-x\right)}}\right)=\answer{-4 \, x^{4} e^{\left(-x\right)} + 16 \, x^{3} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\sin\left(x\right)}{x^{3}}}\right)=\answer{\frac{\cos\left(x\right)}{x^{3}} - \frac{3 \, \sin\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, e^{x}}{\cos\left(x\right)}}\right)=\answer{\frac{5 \, e^{x}}{\cos\left(x\right)} + \frac{5 \, e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, e^{x}}{x^{4}}}\right)=\answer{\frac{2 \, e^{x}}{x^{4}} - \frac{8 \, e^{x}}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, e^{x}}{x^{4}}}\right)=\answer{-\frac{4 \, e^{x}}{x^{4}} + \frac{16 \, e^{x}}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{5 \, \cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} - \frac{5}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{2}}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{10 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sqrt{x}}{x + 2}}\right)=\answer{-\frac{1}{2 \, {\left(x + 2\right)} \sqrt{x}} + \frac{\sqrt{x}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, \cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} - 5}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x + 4}{\cos\left(x\right)}}\right)=\answer{\frac{{\left(x + 4\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{1}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x - 4}{x^{3}}}\right)=\answer{\frac{3 \, {\left(x - 4\right)}}{x^{4}} - \frac{1}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, e^{\left(-x\right)} \sin\left(x\right)}\right)=\answer{-5 \, \cos\left(x\right) e^{\left(-x\right)} + 5 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \log\left(x\right)}{x - 1}}\right)=\answer{\frac{5 \, \log\left(x\right)}{{\left(x - 1\right)}^{2}} - \frac{5}{{\left(x - 1\right)} x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, x^{2}}{\sin\left(x\right)}}\right)=\answer{-\frac{2 \, x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{4 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{4}}{x - 3}}\right)=\answer{\frac{4 \, x^{3}}{x - 3} - \frac{x^{4}}{{\left(x - 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sqrt{x}}{\sin\left(x\right)}}\right)=\answer{\frac{\sqrt{x} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{1}{2 \, \sqrt{x} \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x - 4\right)}}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, {\left(x - 4\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{4}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{4}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{16 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, x^{3} e^{\left(-x\right)}}\right)=\answer{5 \, x^{3} e^{\left(-x\right)} - 15 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, e^{x}}{x - 2}}\right)=\answer{-\frac{4 \, e^{x}}{x - 2} + \frac{4 \, e^{x}}{{\left(x - 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{\log\left(x\right)}}\right)=\answer{\frac{4 \, e^{x}}{\log\left(x\right)} - \frac{4 \, e^{x}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{x^{2}}}\right)=\answer{\frac{4 \, e^{x}}{x^{2}} - \frac{8 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sqrt{x}}{\cos\left(x\right)}}\right)=\answer{-\frac{\sqrt{x} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{1}{2 \, \sqrt{x} \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{2}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, x^{2} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{10 \, x}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({2 \, e^{\left(-x\right)} \sin\left(x\right)}\right)=\answer{2 \, \cos\left(x\right) e^{\left(-x\right)} - 2 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x^{3}}{x - 5}}\right)=\answer{-\frac{3 \, x^{2}}{x - 5} + \frac{x^{3}}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-3 \, \sqrt{x} e^{\left(-x\right)}}\right)=\answer{3 \, \sqrt{x} e^{\left(-x\right)} - \frac{3 \, e^{\left(-x\right)}}{2 \, \sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, \sqrt{x} e^{\left(-x\right)}}\right)=\answer{2 \, \sqrt{x} e^{\left(-x\right)} - \frac{e^{\left(-x\right)}}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, {\left(x + 1\right)}}{x^{3}}}\right)=\answer{\frac{6 \, {\left(x + 1\right)}}{x^{4}} - \frac{2}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({5 \, e^{\left(-x\right)} \sin\left(x\right)}\right)=\answer{5 \, \cos\left(x\right) e^{\left(-x\right)} - 5 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, \cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} + 2}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \sin\left(x\right)}{\cos\left(x\right)}}\right)=\answer{\frac{2 \, \sin\left(x\right)^{2}}{\cos\left(x\right)^{2}} + 2}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \sin\left(x\right)}{x^{3}}}\right)=\answer{-\frac{2 \, \cos\left(x\right)}{x^{3}} + \frac{6 \, \sin\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \cos\left(x\right)}{x - 1}}\right)=\answer{-\frac{5 \, \sin\left(x\right)}{x - 1} - \frac{5 \, \cos\left(x\right)}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{2}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{8 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, e^{x}}{x^{4}}}\right)=\answer{-\frac{2 \, e^{x}}{x^{4}} + \frac{8 \, e^{x}}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, e^{x}}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, \cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} - \frac{2 \, e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2}{x^{\frac{3}{2}}}}\right)=\answer{\frac{3}{x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{e^{x}}{x^{4}}}\right)=\answer{\frac{e^{x}}{x^{4}} - \frac{4 \, e^{x}}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, \cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} - \frac{2}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({5 \, x^{3} e^{\left(-x\right)}}\right)=\answer{-5 \, x^{3} e^{\left(-x\right)} + 15 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \log\left(x\right)}{x^{4}}}\right)=\answer{\frac{8 \, \log\left(x\right)}{x^{5}} - \frac{2}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \sqrt{x}}{\log\left(x\right)}}\right)=\answer{\frac{2}{\sqrt{x} \log\left(x\right)} - \frac{4}{\sqrt{x} \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, x^{3} e^{\left(-x\right)}}\right)=\answer{2 \, x^{3} e^{\left(-x\right)} - 6 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x + 3}{x^{2}}}\right)=\answer{-\frac{2 \, {\left(x + 3\right)}}{x^{3}} + \frac{1}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \log\left(x\right)}{x^{2}}}\right)=\answer{\frac{10 \, \log\left(x\right)}{x^{3}} - \frac{5}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\cos\left(x\right)}{x^{3}}}\right)=\answer{\frac{\sin\left(x\right)}{x^{3}} + \frac{3 \, \cos\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \log\left(x\right)}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, \log\left(x\right) \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{4}{x \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, \cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} + \frac{4}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\sin\left(x\right)}{\log\left(x\right)}}\right)=\answer{\frac{\cos\left(x\right)}{\log\left(x\right)} - \frac{\sin\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-x^{2} e^{\left(-x\right)}}\right)=\answer{x^{2} e^{\left(-x\right)} - 2 \, x e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x - 3\right)}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, {\left(x - 3\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{4}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3}{x}}\right)=\answer{-\frac{3}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{x^{4}}}\right)=\answer{\frac{4 \, e^{x}}{x^{4}} - \frac{16 \, e^{x}}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sqrt{x}}{x + 1}}\right)=\answer{\frac{5}{2 \, {\left(x + 1\right)} \sqrt{x}} - \frac{5 \, \sqrt{x}}{{\left(x + 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{2}}{\log\left(x\right)}}\right)=\answer{-\frac{10 \, x}{\log\left(x\right)} + \frac{5 \, x}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{3}}{x + 2}}\right)=\answer{-\frac{6 \, x^{2}}{x + 2} + \frac{2 \, x^{3}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sqrt{x}}{x - 5}}\right)=\answer{-\frac{3}{2 \, {\left(x - 5\right)} \sqrt{x}} + \frac{3 \, \sqrt{x}}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{2}}{x - 4}}\right)=\answer{\frac{10 \, x}{x - 4} - \frac{5 \, x^{2}}{{\left(x - 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{x + 3}}\right)=\answer{-\frac{4 \, \cos\left(x\right)}{x + 3} + \frac{4 \, \sin\left(x\right)}{{\left(x + 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, {\left(x + 4\right)}}{\cos\left(x\right)}}\right)=\answer{\frac{3 \, {\left(x + 4\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{3}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({5 \, \cos\left(x\right) e^{\left(-x\right)}}\right)=\answer{-5 \, \cos\left(x\right) e^{\left(-x\right)} - 5 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{4}}{x - 1}}\right)=\answer{\frac{16 \, x^{3}}{x - 1} - \frac{4 \, x^{4}}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{\cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} + 1}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, x^{\frac{5}{2}}}\right)=\answer{-10 \, x^{\frac{3}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{3}}{\sin\left(x\right)}}\right)=\answer{-\frac{x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{3 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4}{x}}\right)=\answer{-\frac{4}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{3}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, x^{3} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{15 \, x^{2}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \cos\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{4 \, \sin\left(x\right)}{\sqrt{x}} - \frac{2 \, \cos\left(x\right)}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, e^{x}}{\sqrt{x}}}\right)=\answer{-\frac{4 \, e^{x}}{\sqrt{x}} + \frac{2 \, e^{x}}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{3}}{x + 3}}\right)=\answer{-\frac{9 \, x^{2}}{x + 3} + \frac{3 \, x^{3}}{{\left(x + 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({5 \, \sqrt{x} e^{\left(-x\right)}}\right)=\answer{-5 \, \sqrt{x} e^{\left(-x\right)} + \frac{5 \, e^{\left(-x\right)}}{2 \, \sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, x}\right)=\answer{-5}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sqrt{x}}{\sin\left(x\right)}}\right)=\answer{\frac{5 \, \sqrt{x} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{5}{2 \, \sqrt{x} \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, x^{\frac{3}{2}}}\right)=\answer{6 \, \sqrt{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \log\left(x\right)}{\cos\left(x\right)}}\right)=\answer{-\frac{2 \, \log\left(x\right) \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{2}{x \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\cos\left(x\right)}{x + 4}}\right)=\answer{-\frac{\sin\left(x\right)}{x + 4} - \frac{\cos\left(x\right)}{{\left(x + 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-e^{\left(-x\right)} \sin\left(x\right)}\right)=\answer{-\cos\left(x\right) e^{\left(-x\right)} + e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({5 \, x^{4} e^{\left(-x\right)}}\right)=\answer{-5 \, x^{4} e^{\left(-x\right)} + 20 \, x^{3} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{\frac{3 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{9 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, x^{4} e^{\left(-x\right)}}\right)=\answer{4 \, x^{4} e^{\left(-x\right)} - 16 \, x^{3} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, {\left(x + 3\right)}}{x^{2}}}\right)=\answer{-\frac{4 \, {\left(x + 3\right)}}{x^{3}} + \frac{2}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3}{x^{\frac{3}{2}}}}\right)=\answer{\frac{9}{2 \, x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x + 1}{x^{4}}}\right)=\answer{\frac{4 \, {\left(x + 1\right)}}{x^{5}} - \frac{1}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{4}}{\log\left(x\right)}}\right)=\answer{-\frac{16 \, x^{3}}{\log\left(x\right)} + \frac{4 \, x^{3}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, e^{x}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, e^{x}}{\cos\left(x\right)} - \frac{5 \, e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{4}}{\sin\left(x\right)}}\right)=\answer{\frac{4 \, x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{16 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, e^{x}}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, \cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} + \frac{5 \, e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, x}\right)=\answer{-4}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x - 2}{x^{2}}}\right)=\answer{-\frac{2 \, {\left(x - 2\right)}}{x^{3}} + \frac{1}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{2}}{x - 4}}\right)=\answer{-\frac{8 \, x}{x - 4} + \frac{4 \, x^{2}}{{\left(x - 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{e^{x}}{\cos\left(x\right)}}\right)=\answer{-\frac{e^{x}}{\cos\left(x\right)} - \frac{e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{3}}{x - 4}}\right)=\answer{-\frac{6 \, x^{2}}{x - 4} + \frac{2 \, x^{3}}{{\left(x - 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{2 \, \cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} + \frac{2}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, x^{4}}{\sin\left(x\right)}}\right)=\answer{-\frac{3 \, x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{12 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \sin\left(x\right)}{x^{3}}}\right)=\answer{\frac{4 \, \cos\left(x\right)}{x^{3}} - \frac{12 \, \sin\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\sqrt{x}}{\log\left(x\right)}}\right)=\answer{\frac{1}{2 \, \sqrt{x} \log\left(x\right)} - \frac{1}{\sqrt{x} \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \cos\left(x\right)}{\sqrt{x}}}\right)=\answer{\frac{5 \, \sin\left(x\right)}{\sqrt{x}} + \frac{5 \, \cos\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x - 2\right)}}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, {\left(x - 2\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{4}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\cos\left(x\right) e^{\left(-x\right)}}\right)=\answer{-\cos\left(x\right) e^{\left(-x\right)} - e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x - 1\right)}}{\sin\left(x\right)}}\right)=\answer{\frac{5 \, {\left(x - 1\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{5}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2}{x^{\frac{3}{2}}}}\right)=\answer{-\frac{3}{x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sqrt{x}}{x + 5}}\right)=\answer{-\frac{1}{2 \, {\left(x + 5\right)} \sqrt{x}} + \frac{\sqrt{x}}{{\left(x + 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2}{x^{\frac{5}{2}}}}\right)=\answer{-\frac{5}{x^{\frac{7}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5}{x^{2}}}\right)=\answer{-\frac{10}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({x^{3} e^{\left(-x\right)}}\right)=\answer{-x^{3} e^{\left(-x\right)} + 3 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, {\left(x + 3\right)}}{\log\left(x\right)}}\right)=\answer{\frac{5}{\log\left(x\right)} - \frac{5 \, {\left(x + 3\right)}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{4}}{x - 2}}\right)=\answer{\frac{16 \, x^{3}}{x - 2} - \frac{4 \, x^{4}}{{\left(x - 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{4}}{x - 2}}\right)=\answer{-\frac{12 \, x^{3}}{x - 2} + \frac{3 \, x^{4}}{{\left(x - 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \sin\left(x\right)}{x - 3}}\right)=\answer{\frac{2 \, \cos\left(x\right)}{x - 3} - \frac{2 \, \sin\left(x\right)}{{\left(x - 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, x^{\frac{7}{2}}}\right)=\answer{-\frac{35}{2} \, x^{\frac{5}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-x}\right)=\answer{-1}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{2}}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{4 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4}{x^{\frac{3}{2}}}}\right)=\answer{-\frac{6}{x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, \cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} - 4}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, e^{x}}{x^{3}}}\right)=\answer{-\frac{4 \, e^{x}}{x^{3}} + \frac{12 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{2}}{\sin\left(x\right)}}\right)=\answer{-\frac{x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{2 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, x^{\frac{5}{2}}}\right)=\answer{-\frac{25}{2} \, x^{\frac{3}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x + 3\right)}}{x^{2}}}\right)=\answer{\frac{10 \, {\left(x + 3\right)}}{x^{3}} - \frac{5}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{3}}{x + 5}}\right)=\answer{\frac{15 \, x^{2}}{x + 5} - \frac{5 \, x^{3}}{{\left(x + 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sin\left(x\right)}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, \sin\left(x\right)^{2}}{\cos\left(x\right)^{2}} - 5}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, x^{2}}{\log\left(x\right)}}\right)=\answer{\frac{4 \, x}{\log\left(x\right)} - \frac{2 \, x}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{2}}{\log\left(x\right)}}\right)=\answer{-\frac{4 \, x}{\log\left(x\right)} + \frac{2 \, x}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{1}{x}}\right)=\answer{\frac{1}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{3}}{\cos\left(x\right)}}\right)=\answer{\frac{5 \, x^{3} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{15 \, x^{2}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sin\left(x\right)}{\cos\left(x\right)}}\right)=\answer{-\frac{\sin\left(x\right)^{2}}{\cos\left(x\right)^{2}} - 1}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, {\left(x + 5\right)}}{x^{4}}}\right)=\answer{-\frac{12 \, {\left(x + 5\right)}}{x^{5}} + \frac{3}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, {\left(x + 1\right)} e^{\left(-x\right)}}\right)=\answer{2 \, {\left(x + 1\right)} e^{\left(-x\right)} - 2 \, e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, x^{4}}{\log\left(x\right)}}\right)=\answer{\frac{8 \, x^{3}}{\log\left(x\right)} - \frac{2 \, x^{3}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, {\left(x + 3\right)} e^{\left(-x\right)}}\right)=\answer{4 \, {\left(x + 3\right)} e^{\left(-x\right)} - 4 \, e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({5 \, x^{\frac{7}{2}}}\right)=\answer{\frac{35}{2} \, x^{\frac{5}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, x^{2}}\right)=\answer{-4 \, x}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, e^{x}}{x + 1}}\right)=\answer{-\frac{5 \, e^{x}}{x + 1} + \frac{5 \, e^{x}}{{\left(x + 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sin\left(x\right)}{\cos\left(x\right)}}\right)=\answer{-\frac{3 \, \sin\left(x\right)^{2}}{\cos\left(x\right)^{2}} - 3}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{3}}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, x^{3} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{12 \, x^{2}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \sqrt{x}}{x - 3}}\right)=\answer{\frac{3}{2 \, {\left(x - 3\right)} \sqrt{x}} - \frac{3 \, \sqrt{x}}{{\left(x - 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \cos\left(x\right)}{x^{4}}}\right)=\answer{\frac{4 \, \sin\left(x\right)}{x^{4}} + \frac{16 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-3 \, e^{\left(-x\right)} \log\left(x\right)}\right)=\answer{3 \, e^{\left(-x\right)} \log\left(x\right) - \frac{3 \, e^{\left(-x\right)}}{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\sqrt{x} e^{\left(-x\right)}}\right)=\answer{\sqrt{x} e^{\left(-x\right)} - \frac{e^{\left(-x\right)}}{2 \, \sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \log\left(x\right)}{x^{3}}}\right)=\answer{-\frac{9 \, \log\left(x\right)}{x^{4}} + \frac{3}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({x^{\frac{5}{2}}}\right)=\answer{\frac{5}{2} \, x^{\frac{3}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{3}}{\log\left(x\right)}}\right)=\answer{\frac{15 \, x^{2}}{\log\left(x\right)} - \frac{5 \, x^{2}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({x^{2} e^{\left(-x\right)}}\right)=\answer{-x^{2} e^{\left(-x\right)} + 2 \, x e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, {\left(x + 5\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{4 \, {\left(x + 5\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{4}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{3}}{\log\left(x\right)}}\right)=\answer{-\frac{15 \, x^{2}}{\log\left(x\right)} + \frac{5 \, x^{2}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \cos\left(x\right)}{\log\left(x\right)}}\right)=\answer{\frac{4 \, \sin\left(x\right)}{\log\left(x\right)} + \frac{4 \, \cos\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, {\left(x - 1\right)}}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, {\left(x - 1\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{2}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, {\left(x - 2\right)}}{\sqrt{x}}}\right)=\answer{-\frac{5 \, {\left(x - 2\right)}}{2 \, x^{\frac{3}{2}}} + \frac{5}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sin\left(x\right)}{x - 2}}\right)=\answer{\frac{5 \, \cos\left(x\right)}{x - 2} - \frac{5 \, \sin\left(x\right)}{{\left(x - 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{2}}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, x^{2} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{8 \, x}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{4 \, \cos\left(x\right)}{\sqrt{x}} + \frac{2 \, \sin\left(x\right)}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, {\left(x + 3\right)}}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, {\left(x + 3\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{5}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\cos\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{\sin\left(x\right)}{\sqrt{x}} - \frac{\cos\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \cos\left(x\right)}{x - 1}}\right)=\answer{-\frac{4 \, \sin\left(x\right)}{x - 1} - \frac{4 \, \cos\left(x\right)}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{\sqrt{x}}}\right)=\answer{\frac{4 \, e^{x}}{\sqrt{x}} - \frac{2 \, e^{x}}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{4 \, \cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} + 4}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, {\left(x - 3\right)}}{\sqrt{x}}}\right)=\answer{\frac{2 \, {\left(x - 3\right)}}{x^{\frac{3}{2}}} - \frac{4}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{x^{2}}}\right)=\answer{-\frac{4 \, \cos\left(x\right)}{x^{2}} + \frac{8 \, \sin\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sin\left(x\right)}{x - 1}}\right)=\answer{-\frac{5 \, \cos\left(x\right)}{x - 1} + \frac{5 \, \sin\left(x\right)}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, e^{x}}{\sin\left(x\right)}}\right)=\answer{-\frac{2 \, \cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} + \frac{2 \, e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, {\left(x - 1\right)}}{x^{2}}}\right)=\answer{-\frac{6 \, {\left(x - 1\right)}}{x^{3}} + \frac{3}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, x^{\frac{3}{2}}}\right)=\answer{-\frac{15}{2} \, \sqrt{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, e^{x}}{\log\left(x\right)}}\right)=\answer{\frac{5 \, e^{x}}{\log\left(x\right)} - \frac{5 \, e^{x}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{e^{x}}{\log\left(x\right)}}\right)=\answer{-\frac{e^{x}}{\log\left(x\right)} + \frac{e^{x}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sqrt{x}}{x + 1}}\right)=\answer{-\frac{1}{2 \, {\left(x + 1\right)} \sqrt{x}} + \frac{\sqrt{x}}{{\left(x + 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{\cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} - \frac{1}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x^{4}}{\sin\left(x\right)}}\right)=\answer{\frac{x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{4 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, x^{3}}{\log\left(x\right)}}\right)=\answer{\frac{6 \, x^{2}}{\log\left(x\right)} - \frac{2 \, x^{2}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5}{x^{\frac{5}{2}}}}\right)=\answer{-\frac{25}{2 \, x^{\frac{7}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({3 \, e^{\left(-x\right)} \sin\left(x\right)}\right)=\answer{3 \, \cos\left(x\right) e^{\left(-x\right)} - 3 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \cos\left(x\right)}{x^{4}}}\right)=\answer{\frac{5 \, \sin\left(x\right)}{x^{4}} + \frac{20 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sin\left(x\right)}{\sqrt{x}}}\right)=\answer{\frac{5 \, \cos\left(x\right)}{\sqrt{x}} - \frac{5 \, \sin\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \cos\left(x\right)}{x^{3}}}\right)=\answer{-\frac{2 \, \sin\left(x\right)}{x^{3}} - \frac{6 \, \cos\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\sqrt{x}}{x + 4}}\right)=\answer{\frac{1}{2 \, {\left(x + 4\right)} \sqrt{x}} - \frac{\sqrt{x}}{{\left(x + 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \log\left(x\right)}{x^{3}}}\right)=\answer{-\frac{12 \, \log\left(x\right)}{x^{4}} + \frac{4}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-3 \, x^{2} e^{\left(-x\right)}}\right)=\answer{3 \, x^{2} e^{\left(-x\right)} - 6 \, x e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, e^{x}}{x + 2}}\right)=\answer{\frac{3 \, e^{x}}{x + 2} - \frac{3 \, e^{x}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{4}}{\log\left(x\right)}}\right)=\answer{\frac{16 \, x^{3}}{\log\left(x\right)} - \frac{4 \, x^{3}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\sqrt{x}}{x - 3}}\right)=\answer{\frac{1}{2 \, {\left(x - 3\right)} \sqrt{x}} - \frac{\sqrt{x}}{{\left(x - 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, {\left(x + 4\right)}}{\log\left(x\right)}}\right)=\answer{-\frac{3}{\log\left(x\right)} + \frac{3 \, {\left(x + 4\right)}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, x^{4}}{\cos\left(x\right)}}\right)=\answer{\frac{2 \, x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{8 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sqrt{x}}{\log\left(x\right)}}\right)=\answer{-\frac{2}{\sqrt{x} \log\left(x\right)} + \frac{4}{\sqrt{x} \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x + 1}{\log\left(x\right)}}\right)=\answer{\frac{1}{\log\left(x\right)} - \frac{x + 1}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-x^{4} e^{\left(-x\right)}}\right)=\answer{x^{4} e^{\left(-x\right)} - 4 \, x^{3} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \log\left(x\right)}{x^{4}}}\right)=\answer{\frac{12 \, \log\left(x\right)}{x^{5}} - \frac{3}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, x^{3} e^{\left(-x\right)}}\right)=\answer{-4 \, x^{3} e^{\left(-x\right)} + 12 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \log\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{\log\left(x\right)}{x^{\frac{3}{2}}} + \frac{2}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, e^{x}}{x^{3}}}\right)=\answer{-\frac{2 \, e^{x}}{x^{3}} + \frac{6 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \sin\left(x\right)}{\sqrt{x}}}\right)=\answer{\frac{3 \, \cos\left(x\right)}{\sqrt{x}} - \frac{3 \, \sin\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \log\left(x\right)}{\cos\left(x\right)}}\right)=\answer{\frac{5 \, \log\left(x\right) \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{5}{x \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{3 \, \cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} + 3}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \log\left(x\right)}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, \log\left(x\right) \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{5}{x \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{3 \, \cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} + \frac{3}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x - 3}{x^{4}}}\right)=\answer{\frac{4 \, {\left(x - 3\right)}}{x^{5}} - \frac{1}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\cos\left(x\right) e^{\left(-x\right)}}\right)=\answer{\cos\left(x\right) e^{\left(-x\right)} + e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{2}}{\sin\left(x\right)}}\right)=\answer{\frac{3 \, x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{6 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, {\left(x - 1\right)}}{x^{4}}}\right)=\answer{\frac{8 \, {\left(x - 1\right)}}{x^{5}} - \frac{2}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{x^{3}}}\right)=\answer{\frac{4 \, e^{x}}{x^{3}} - \frac{12 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \sin\left(x\right)}{\sqrt{x}}}\right)=\answer{\frac{2 \, \cos\left(x\right)}{\sqrt{x}} - \frac{\sin\left(x\right)}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \sqrt{x}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, \sqrt{x} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{2}{\sqrt{x} \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{x + 2}}\right)=\answer{-\frac{4 \, \cos\left(x\right)}{x + 2} + \frac{4 \, \sin\left(x\right)}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \log\left(x\right)}{x^{2}}}\right)=\answer{-\frac{10 \, \log\left(x\right)}{x^{3}} + \frac{5}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, e^{x}}{\log\left(x\right)}}\right)=\answer{-\frac{2 \, e^{x}}{\log\left(x\right)} + \frac{2 \, e^{x}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{4}}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{16 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{15 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{-\frac{3 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{9 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{\cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} + \frac{1}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{4}}{\sin\left(x\right)}}\right)=\answer{-\frac{x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{4 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5}{x}}\right)=\answer{\frac{5}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, x^{\frac{3}{2}}}\right)=\answer{-6 \, \sqrt{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, e^{x}}{x^{3}}}\right)=\answer{\frac{5 \, e^{x}}{x^{3}} - \frac{15 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \sqrt{x}}{x - 5}}\right)=\answer{\frac{3}{2 \, {\left(x - 5\right)} \sqrt{x}} - \frac{3 \, \sqrt{x}}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, x^{2}}{\log\left(x\right)}}\right)=\answer{\frac{6 \, x}{\log\left(x\right)} - \frac{3 \, x}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, {\left(x - 4\right)}}{\log\left(x\right)}}\right)=\answer{-\frac{3}{\log\left(x\right)} + \frac{3 \, {\left(x - 4\right)}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, {\left(x - 5\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{4 \, {\left(x - 5\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{4}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{2}}{\cos\left(x\right)}}\right)=\answer{\frac{x^{2} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{2 \, x}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \cos\left(x\right)}{x - 1}}\right)=\answer{\frac{3 \, \sin\left(x\right)}{x - 1} + \frac{3 \, \cos\left(x\right)}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, e^{x}}{\sin\left(x\right)}}\right)=\answer{\frac{3 \, \cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} - \frac{3 \, e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \cos\left(x\right)}{x^{4}}}\right)=\answer{-\frac{5 \, \sin\left(x\right)}{x^{4}} - \frac{20 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{3}}{x - 4}}\right)=\answer{\frac{15 \, x^{2}}{x - 4} - \frac{5 \, x^{3}}{{\left(x - 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, e^{x}}{x^{4}}}\right)=\answer{\frac{5 \, e^{x}}{x^{4}} - \frac{20 \, e^{x}}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \log\left(x\right)}{x^{3}}}\right)=\answer{-\frac{6 \, \log\left(x\right)}{x^{4}} + \frac{2}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{4}}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{20 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sqrt{x}}{x + 1}}\right)=\answer{-\frac{3}{2 \, {\left(x + 1\right)} \sqrt{x}} + \frac{3 \, \sqrt{x}}{{\left(x + 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x^{3}}{\cos\left(x\right)}}\right)=\answer{-\frac{x^{3} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{3 \, x^{2}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5}{x^{\frac{3}{2}}}}\right)=\answer{\frac{15}{2 \, x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, {\left(x - 2\right)}}{\sin\left(x\right)}}\right)=\answer{-\frac{2 \, {\left(x - 2\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{2}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, e^{\left(-x\right)} \sin\left(x\right)}\right)=\answer{4 \, \cos\left(x\right) e^{\left(-x\right)} - 4 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{4}}{\log\left(x\right)}}\right)=\answer{-\frac{20 \, x^{3}}{\log\left(x\right)} + \frac{5 \, x^{3}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\log\left(x\right)}{\cos\left(x\right)}}\right)=\answer{\frac{\log\left(x\right) \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{1}{x \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{\frac{2 \, \sin\left(x\right)}{x^{2}} + \frac{4 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sin\left(x\right)}{x - 5}}\right)=\answer{\frac{5 \, \cos\left(x\right)}{x - 5} - \frac{5 \, \sin\left(x\right)}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x + 1\right)}}{x^{3}}}\right)=\answer{-\frac{12 \, {\left(x + 1\right)}}{x^{4}} + \frac{4}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4}{x}}\right)=\answer{\frac{4}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{3}}{x - 1}}\right)=\answer{-\frac{6 \, x^{2}}{x - 1} + \frac{2 \, x^{3}}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3}{x^{2}}}\right)=\answer{\frac{6}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\log\left(x\right)}{x + 2}}\right)=\answer{\frac{\log\left(x\right)}{{\left(x + 2\right)}^{2}} - \frac{1}{{\left(x + 2\right)} x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, e^{\left(-x\right)} \log\left(x\right)}\right)=\answer{-4 \, e^{\left(-x\right)} \log\left(x\right) + \frac{4 \, e^{\left(-x\right)}}{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x + 4\right)}}{x^{4}}}\right)=\answer{-\frac{16 \, {\left(x + 4\right)}}{x^{5}} + \frac{4}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, {\left(x + 5\right)}}{\sqrt{x}}}\right)=\answer{-\frac{3 \, {\left(x + 5\right)}}{2 \, x^{\frac{3}{2}}} + \frac{3}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3}{x^{\frac{5}{2}}}}\right)=\answer{-\frac{15}{2 \, x^{\frac{7}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, x^{3} e^{\left(-x\right)}}\right)=\answer{4 \, x^{3} e^{\left(-x\right)} - 12 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sin\left(x\right)}{x^{4}}}\right)=\answer{\frac{5 \, \cos\left(x\right)}{x^{4}} - \frac{20 \, \sin\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, e^{\left(-x\right)} \log\left(x\right)}\right)=\answer{2 \, e^{\left(-x\right)} \log\left(x\right) - \frac{2 \, e^{\left(-x\right)}}{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({2 \, {\left(x + 1\right)} e^{\left(-x\right)}}\right)=\answer{-2 \, {\left(x + 1\right)} e^{\left(-x\right)} + 2 \, e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \sin\left(x\right)}{x^{4}}}\right)=\answer{\frac{4 \, \cos\left(x\right)}{x^{4}} - \frac{16 \, \sin\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \log\left(x\right)}{x + 1}}\right)=\answer{-\frac{2 \, \log\left(x\right)}{{\left(x + 1\right)}^{2}} + \frac{2}{{\left(x + 1\right)} x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{\frac{4 \, \sin\left(x\right)}{x^{2}} + \frac{8 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{x^{3}}}\right)=\answer{-\frac{4 \, \cos\left(x\right)}{x^{3}} + \frac{12 \, \sin\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-x^{\frac{5}{2}}}\right)=\answer{-\frac{5}{2} \, x^{\frac{3}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sin\left(x\right)}{x^{3}}}\right)=\answer{-\frac{3 \, \cos\left(x\right)}{x^{3}} + \frac{9 \, \sin\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \log\left(x\right)}{x^{2}}}\right)=\answer{\frac{8 \, \log\left(x\right)}{x^{3}} - \frac{4}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({3 \, x^{3} e^{\left(-x\right)}}\right)=\answer{-3 \, x^{3} e^{\left(-x\right)} + 9 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \log\left(x\right)}{x^{4}}}\right)=\answer{\frac{16 \, \log\left(x\right)}{x^{5}} - \frac{4}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x + 2\right)}}{x^{4}}}\right)=\answer{-\frac{16 \, {\left(x + 2\right)}}{x^{5}} + \frac{4}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \cos\left(x\right)}{x^{3}}}\right)=\answer{\frac{4 \, \sin\left(x\right)}{x^{3}} + \frac{12 \, \cos\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-3 \, x^{\frac{3}{2}}}\right)=\answer{-\frac{9}{2} \, \sqrt{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{4}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{20 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x - 5}{\sqrt{x}}}\right)=\answer{-\frac{x - 5}{2 \, x^{\frac{3}{2}}} + \frac{1}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-x^{2}}\right)=\answer{-2 \, x}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{4}}{x - 4}}\right)=\answer{-\frac{16 \, x^{3}}{x - 4} + \frac{4 \, x^{4}}{{\left(x - 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sqrt{x}}{\sin\left(x\right)}}\right)=\answer{\frac{3 \, \sqrt{x} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{3}{2 \, \sqrt{x} \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{2}}{x - 3}}\right)=\answer{\frac{10 \, x}{x - 3} - \frac{5 \, x^{2}}{{\left(x - 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{\cos\left(x\right)}}\right)=\answer{-\frac{4 \, \sin\left(x\right)^{2}}{\cos\left(x\right)^{2}} - 4}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{e^{x}}{x + 1}}\right)=\answer{\frac{e^{x}}{x + 1} - \frac{e^{x}}{{\left(x + 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, {\left(x - 5\right)}}{\sqrt{x}}}\right)=\answer{-\frac{x - 5}{x^{\frac{3}{2}}} + \frac{2}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\cos\left(x\right)}{\log\left(x\right)}}\right)=\answer{-\frac{\sin\left(x\right)}{\log\left(x\right)} - \frac{\cos\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-{\left(x + 5\right)} e^{\left(-x\right)}}\right)=\answer{{\left(x + 5\right)} e^{\left(-x\right)} - e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \cos\left(x\right)}{x^{3}}}\right)=\answer{\frac{2 \, \sin\left(x\right)}{x^{3}} + \frac{6 \, \cos\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{x^{4}}}\right)=\answer{-\frac{4 \, \cos\left(x\right)}{x^{4}} + \frac{16 \, \sin\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({2 \, x^{\frac{5}{2}}}\right)=\answer{5 \, x^{\frac{3}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, {\left(x - 4\right)}}{\log\left(x\right)}}\right)=\answer{\frac{2}{\log\left(x\right)} - \frac{2 \, {\left(x - 4\right)}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3}{x^{\frac{5}{2}}}}\right)=\answer{\frac{15}{2 \, x^{\frac{7}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{2}}{\cos\left(x\right)}}\right)=\answer{-\frac{2 \, x^{2} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{4 \, x}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, e^{x}}{x^{2}}}\right)=\answer{-\frac{2 \, e^{x}}{x^{2}} + \frac{4 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \cos\left(x\right)}{x^{4}}}\right)=\answer{\frac{2 \, \sin\left(x\right)}{x^{4}} + \frac{8 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{3}}{x - 1}}\right)=\answer{-\frac{9 \, x^{2}}{x - 1} + \frac{3 \, x^{3}}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \cos\left(x\right)}{x + 4}}\right)=\answer{\frac{5 \, \sin\left(x\right)}{x + 4} + \frac{5 \, \cos\left(x\right)}{{\left(x + 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{-\frac{2 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{6 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{-\frac{4 \, \sin\left(x\right)}{x^{2}} - \frac{8 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, {\left(x + 1\right)}}{x^{3}}}\right)=\answer{-\frac{15 \, {\left(x + 1\right)}}{x^{4}} + \frac{5}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2}{x}}\right)=\answer{-\frac{2}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{\frac{4 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{12 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({3 \, \sqrt{x} e^{\left(-x\right)}}\right)=\answer{-3 \, \sqrt{x} e^{\left(-x\right)} + \frac{3 \, e^{\left(-x\right)}}{2 \, \sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, e^{x}}{\cos\left(x\right)}}\right)=\answer{\frac{3 \, e^{x}}{\cos\left(x\right)} + \frac{3 \, e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, {\left(x - 4\right)}}{\cos\left(x\right)}}\right)=\answer{\frac{2 \, {\left(x - 4\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{2}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \sqrt{x}}{\log\left(x\right)}}\right)=\answer{\frac{3}{2 \, \sqrt{x} \log\left(x\right)} - \frac{3}{\sqrt{x} \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, \cos\left(x\right) e^{\left(-x\right)}}\right)=\answer{5 \, \cos\left(x\right) e^{\left(-x\right)} + 5 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4}{x^{2}}}\right)=\answer{-\frac{8}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5}{x^{\frac{5}{2}}}}\right)=\answer{\frac{25}{2 \, x^{\frac{7}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-e^{\left(-x\right)} \log\left(x\right)}\right)=\answer{e^{\left(-x\right)} \log\left(x\right) - \frac{e^{\left(-x\right)}}{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, {\left(x + 5\right)}}{x^{2}}}\right)=\answer{\frac{8 \, {\left(x + 5\right)}}{x^{3}} - \frac{4}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, \sqrt{x} e^{\left(-x\right)}}\right)=\answer{-4 \, \sqrt{x} e^{\left(-x\right)} + \frac{2 \, e^{\left(-x\right)}}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({x^{\frac{7}{2}}}\right)=\answer{\frac{7}{2} \, x^{\frac{5}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, {\left(x - 1\right)}}{x^{3}}}\right)=\answer{\frac{6 \, {\left(x - 1\right)}}{x^{4}} - \frac{2}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, {\left(x + 2\right)}}{\cos\left(x\right)}}\right)=\answer{\frac{5 \, {\left(x + 2\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{5}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \log\left(x\right)}{x^{4}}}\right)=\answer{\frac{20 \, \log\left(x\right)}{x^{5}} - \frac{5}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{e^{x}}{\sin\left(x\right)}}\right)=\answer{-\frac{\cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} + \frac{e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, e^{x}}{\cos\left(x\right)}}\right)=\answer{-\frac{4 \, e^{x}}{\cos\left(x\right)} - \frac{4 \, e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{e^{x}}{x^{2}}}\right)=\answer{-\frac{e^{x}}{x^{2}} + \frac{2 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sqrt{x}}{\sin\left(x\right)}}\right)=\answer{\frac{4 \, \sqrt{x} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{2}{\sqrt{x} \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({2 \, {\left(x - 1\right)} e^{\left(-x\right)}}\right)=\answer{-2 \, {\left(x - 1\right)} e^{\left(-x\right)} + 2 \, e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x + 5\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, {\left(x + 5\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{5}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({x^{2}}\right)=\answer{2 \, x}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{4}}{\sin\left(x\right)}}\right)=\answer{\frac{5 \, x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{20 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({x^{\frac{3}{2}}}\right)=\answer{\frac{3}{2} \, \sqrt{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \log\left(x\right)}{x^{4}}}\right)=\answer{-\frac{20 \, \log\left(x\right)}{x^{5}} + \frac{5}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, {\left(x - 2\right)}}{x^{2}}}\right)=\answer{\frac{6 \, {\left(x - 2\right)}}{x^{3}} - \frac{3}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x^{2}}{\sin\left(x\right)}}\right)=\answer{\frac{x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{2 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \sin\left(x\right)}{\log\left(x\right)}}\right)=\answer{\frac{2 \, \cos\left(x\right)}{\log\left(x\right)} - \frac{2 \, \sin\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \cos\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{5 \, \sin\left(x\right)}{\sqrt{x}} - \frac{5 \, \cos\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \cos\left(x\right)}{x^{3}}}\right)=\answer{-\frac{5 \, \sin\left(x\right)}{x^{3}} - \frac{15 \, \cos\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x + 1\right)}}{\log\left(x\right)}}\right)=\answer{-\frac{5}{\log\left(x\right)} + \frac{5 \, {\left(x + 1\right)}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{\frac{5 \, \sin\left(x\right)}{x^{2}} + \frac{10 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, e^{x}}{x^{2}}}\right)=\answer{-\frac{3 \, e^{x}}{x^{2}} + \frac{6 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({3 \, x}\right)=\answer{3}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5}{x^{\frac{7}{2}}}}\right)=\answer{\frac{35}{2 \, x^{\frac{9}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sqrt{x}}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, \sqrt{x} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{5}{2 \, \sqrt{x} \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, {\left(x - 5\right)}}{\sin\left(x\right)}}\right)=\answer{-\frac{2 \, {\left(x - 5\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{2}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{2}}{\log\left(x\right)}}\right)=\answer{\frac{2 \, x}{\log\left(x\right)} - \frac{x}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x + 2\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, {\left(x + 2\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{5}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({2 \, x^{\frac{3}{2}}}\right)=\answer{3 \, \sqrt{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5}{x^{\frac{3}{2}}}}\right)=\answer{-\frac{15}{2 \, x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sin\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{5 \, \cos\left(x\right)}{\sqrt{x}} + \frac{5 \, \sin\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, {\left(x + 5\right)}}{x^{3}}}\right)=\answer{\frac{9 \, {\left(x + 5\right)}}{x^{4}} - \frac{3}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \cos\left(x\right)}{\log\left(x\right)}}\right)=\answer{-\frac{5 \, \sin\left(x\right)}{\log\left(x\right)} - \frac{5 \, \cos\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{e^{x}}{x^{2}}}\right)=\answer{\frac{e^{x}}{x^{2}} - \frac{2 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{2}}{x + 2}}\right)=\answer{-\frac{4 \, x}{x + 2} + \frac{2 \, x^{2}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({2 \, x^{3} e^{\left(-x\right)}}\right)=\answer{-2 \, x^{3} e^{\left(-x\right)} + 6 \, x^{2} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, x^{3}}{x - 1}}\right)=\answer{\frac{6 \, x^{2}}{x - 1} - \frac{2 \, x^{3}}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sin\left(x\right)}{x - 5}}\right)=\answer{-\frac{4 \, \cos\left(x\right)}{x - 5} + \frac{4 \, \sin\left(x\right)}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, x^{2}}\right)=\answer{8 \, x}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sin\left(x\right)}{x + 4}}\right)=\answer{-\frac{5 \, \cos\left(x\right)}{x + 4} + \frac{5 \, \sin\left(x\right)}{{\left(x + 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{4}}{x + 2}}\right)=\answer{-\frac{16 \, x^{3}}{x + 2} + \frac{4 \, x^{4}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, e^{x}}{x^{3}}}\right)=\answer{\frac{3 \, e^{x}}{x^{3}} - \frac{9 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x - 5}{x^{2}}}\right)=\answer{-\frac{2 \, {\left(x - 5\right)}}{x^{3}} + \frac{1}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, e^{x}}{x^{3}}}\right)=\answer{-\frac{3 \, e^{x}}{x^{3}} + \frac{9 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, e^{x}}{x^{2}}}\right)=\answer{\frac{5 \, e^{x}}{x^{2}} - \frac{10 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, x^{4} e^{\left(-x\right)}}\right)=\answer{5 \, x^{4} e^{\left(-x\right)} - 20 \, x^{3} e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x + 3\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, {\left(x + 3\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{5}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{-\frac{3 \, \sin\left(x\right)}{x^{2}} - \frac{6 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sin\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{\cos\left(x\right)}{\sqrt{x}} + \frac{\sin\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({2 \, x^{2} e^{\left(-x\right)}}\right)=\answer{-2 \, x^{2} e^{\left(-x\right)} + 4 \, x e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3}{x^{\frac{7}{2}}}}\right)=\answer{\frac{21}{2 \, x^{\frac{9}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{x + 4}}\right)=\answer{\frac{4 \, e^{x}}{x + 4} - \frac{4 \, e^{x}}{{\left(x + 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, {\left(x + 5\right)} e^{\left(-x\right)}}\right)=\answer{5 \, {\left(x + 5\right)} e^{\left(-x\right)} - 5 \, e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{1}{x^{\frac{7}{2}}}}\right)=\answer{-\frac{7}{2 \, x^{\frac{9}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{4}}{\cos\left(x\right)}}\right)=\answer{\frac{5 \, x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{20 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, e^{x}}{\sin\left(x\right)}}\right)=\answer{-\frac{3 \, \cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} + \frac{3 \, e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{-\frac{2 \, \sin\left(x\right)}{x^{2}} - \frac{4 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \cos\left(x\right)}{x^{4}}}\right)=\answer{-\frac{4 \, \sin\left(x\right)}{x^{4}} - \frac{16 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \log\left(x\right)}{x - 4}}\right)=\answer{\frac{4 \, \log\left(x\right)}{{\left(x - 4\right)}^{2}} - \frac{4}{{\left(x - 4\right)} x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x^{3}}{\log\left(x\right)}}\right)=\answer{-\frac{3 \, x^{2}}{\log\left(x\right)} + \frac{x^{2}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, \sqrt{x} e^{\left(-x\right)}}\right)=\answer{4 \, \sqrt{x} e^{\left(-x\right)} - \frac{2 \, e^{\left(-x\right)}}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\sqrt{x}}{x - 5}}\right)=\answer{\frac{1}{2 \, {\left(x - 5\right)} \sqrt{x}} - \frac{\sqrt{x}}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, {\left(x + 4\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{3 \, {\left(x + 4\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{3}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \sin\left(x\right)}{x^{2}}}\right)=\answer{\frac{2 \, \cos\left(x\right)}{x^{2}} - \frac{4 \, \sin\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{4}}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{8 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{\frac{5 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{15 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x^{4}}{\cos\left(x\right)}}\right)=\answer{-\frac{x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{4 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, x^{2} e^{\left(-x\right)}}\right)=\answer{-4 \, x^{2} e^{\left(-x\right)} + 8 \, x e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4}{x^{\frac{3}{2}}}}\right)=\answer{\frac{6}{x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \sqrt{x}}{\cos\left(x\right)}}\right)=\answer{-\frac{4 \, \sqrt{x} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{2}{\sqrt{x} \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sin\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{3 \, \cos\left(x\right)}{\sqrt{x}} + \frac{3 \, \sin\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({3 \, e^{\left(-x\right)} \log\left(x\right)}\right)=\answer{-3 \, e^{\left(-x\right)} \log\left(x\right) + \frac{3 \, e^{\left(-x\right)}}{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\log\left(x\right)}{x^{2}}}\right)=\answer{-\frac{2 \, \log\left(x\right)}{x^{3}} + \frac{1}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{1}{x^{\frac{3}{2}}}}\right)=\answer{\frac{3}{2 \, x^{\frac{5}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \log\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{5 \, \log\left(x\right)}{2 \, x^{\frac{3}{2}}} + \frac{5}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5}{x}}\right)=\answer{-\frac{5}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, e^{x}}{\sqrt{x}}}\right)=\answer{\frac{3 \, e^{x}}{\sqrt{x}} - \frac{3 \, e^{x}}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-5 \, {\left(x - 2\right)} e^{\left(-x\right)}}\right)=\answer{5 \, {\left(x - 2\right)} e^{\left(-x\right)} - 5 \, e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, e^{\left(-x\right)} \sin\left(x\right)}\right)=\answer{-4 \, \cos\left(x\right) e^{\left(-x\right)} + 4 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, x^{2}}{\sin\left(x\right)}}\right)=\answer{-\frac{3 \, x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{6 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{2}}{x - 1}}\right)=\answer{\frac{2 \, x}{x - 1} - \frac{x^{2}}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \log\left(x\right)}{x^{2}}}\right)=\answer{-\frac{6 \, \log\left(x\right)}{x^{3}} + \frac{3}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2}{x^{2}}}\right)=\answer{-\frac{4}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sin\left(x\right)}{\log\left(x\right)}}\right)=\answer{-\frac{3 \, \cos\left(x\right)}{\log\left(x\right)} + \frac{3 \, \sin\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, e^{x}}{\log\left(x\right)}}\right)=\answer{-\frac{3 \, e^{x}}{\log\left(x\right)} + \frac{3 \, e^{x}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{\frac{3 \, \cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} - \frac{3}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, \cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} + \frac{4 \, e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, {\left(x + 3\right)}}{x^{2}}}\right)=\answer{\frac{4 \, {\left(x + 3\right)}}{x^{3}} - \frac{2}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \log\left(x\right)}{x - 2}}\right)=\answer{-\frac{2 \, \log\left(x\right)}{{\left(x - 2\right)}^{2}} + \frac{2}{{\left(x - 2\right)} x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \sin\left(x\right)}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, \sin\left(x\right)^{2}}{\cos\left(x\right)^{2}} + 4}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x - 3}{x^{2}}}\right)=\answer{\frac{2 \, {\left(x - 3\right)}}{x^{3}} - \frac{1}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, e^{x}}{x^{4}}}\right)=\answer{-\frac{3 \, e^{x}}{x^{4}} + \frac{12 \, e^{x}}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, \cos\left(x\right) e^{\left(-x\right)}}\right)=\answer{4 \, \cos\left(x\right) e^{\left(-x\right)} + 4 \, e^{\left(-x\right)} \sin\left(x\right)}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \log\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{2 \, \log\left(x\right)}{x^{\frac{3}{2}}} + \frac{4}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \cos\left(x\right)}{x^{4}}}\right)=\answer{\frac{3 \, \sin\left(x\right)}{x^{4}} + \frac{12 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, e^{x}}{x^{2}}}\right)=\answer{-\frac{4 \, e^{x}}{x^{2}} + \frac{8 \, e^{x}}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, {\left(x - 5\right)}}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, {\left(x - 5\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{2}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \sqrt{x}}{x + 1}}\right)=\answer{\frac{3}{2 \, {\left(x + 1\right)} \sqrt{x}} - \frac{3 \, \sqrt{x}}{{\left(x + 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{4}}{\cos\left(x\right)}}\right)=\answer{-\frac{4 \, x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{16 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2}{x^{\frac{7}{2}}}}\right)=\answer{\frac{7}{x^{\frac{9}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \log\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{5 \, \cos\left(x\right) \log\left(x\right)}{\sin\left(x\right)^{2}} + \frac{5}{x \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sin\left(x\right)}{x^{3}}}\right)=\answer{\frac{5 \, \cos\left(x\right)}{x^{3}} - \frac{15 \, \sin\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, {\left(x + 1\right)} e^{\left(-x\right)}}\right)=\answer{-4 \, {\left(x + 1\right)} e^{\left(-x\right)} + 4 \, e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, e^{x}}{\sin\left(x\right)}}\right)=\answer{\frac{5 \, \cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} - \frac{5 \, e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{3}}{\log\left(x\right)}}\right)=\answer{\frac{3 \, x^{2}}{\log\left(x\right)} - \frac{x^{2}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x + 5\right)}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, {\left(x + 5\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{4}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{4}}{\log\left(x\right)}}\right)=\answer{-\frac{12 \, x^{3}}{\log\left(x\right)} + \frac{3 \, x^{3}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x + 5\right)}}{x^{3}}}\right)=\answer{\frac{15 \, {\left(x + 5\right)}}{x^{4}} - \frac{5}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\log\left(x\right)}{\cos\left(x\right)}}\right)=\answer{-\frac{\log\left(x\right) \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{1}{x \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sqrt{x}}{\log\left(x\right)}}\right)=\answer{-\frac{5}{2 \, \sqrt{x} \log\left(x\right)} + \frac{5}{\sqrt{x} \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \cos\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{2 \, \sin\left(x\right)}{\sqrt{x}} - \frac{\cos\left(x\right)}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{4}}{x + 2}}\right)=\answer{\frac{4 \, x^{3}}{x + 2} - \frac{x^{4}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-4 \, e^{\left(-x\right)} \log\left(x\right)}\right)=\answer{4 \, e^{\left(-x\right)} \log\left(x\right) - \frac{4 \, e^{\left(-x\right)}}{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sin\left(x\right)}{x^{2}}}\right)=\answer{-\frac{\cos\left(x\right)}{x^{2}} + \frac{2 \, \sin\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x - 4\right)}}{\sqrt{x}}}\right)=\answer{\frac{5 \, {\left(x - 4\right)}}{2 \, x^{\frac{3}{2}}} - \frac{5}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{e^{x}}{\cos\left(x\right)}}\right)=\answer{\frac{e^{x}}{\cos\left(x\right)} + \frac{e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, e^{x}}{x + 4}}\right)=\answer{-\frac{5 \, e^{x}}{x + 4} + \frac{5 \, e^{x}}{{\left(x + 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{2}}{\cos\left(x\right)}}\right)=\answer{-\frac{4 \, x^{2} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{8 \, x}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sqrt{x}}{x + 2}}\right)=\answer{-\frac{3}{2 \, {\left(x + 2\right)} \sqrt{x}} + \frac{3 \, \sqrt{x}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, e^{x}}{x + 1}}\right)=\answer{-\frac{2 \, e^{x}}{x + 1} + \frac{2 \, e^{x}}{{\left(x + 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4}{x^{\frac{5}{2}}}}\right)=\answer{\frac{10}{x^{\frac{7}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x - 3}{x^{3}}}\right)=\answer{\frac{3 \, {\left(x - 3\right)}}{x^{4}} - \frac{1}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \sin\left(x\right)}{\log\left(x\right)}}\right)=\answer{\frac{5 \, \cos\left(x\right)}{\log\left(x\right)} - \frac{5 \, \sin\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, x^{2} e^{\left(-x\right)}}\right)=\answer{2 \, x^{2} e^{\left(-x\right)} - 4 \, x e^{\left(-x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, {\left(x + 4\right)}}{x^{3}}}\right)=\answer{\frac{12 \, {\left(x + 4\right)}}{x^{4}} - \frac{4}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{4}}{\sin\left(x\right)}}\right)=\answer{\frac{3 \, x^{4} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{12 \, x^{3}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, {\left(x - 4\right)}}{\sqrt{x}}}\right)=\answer{\frac{3 \, {\left(x - 4\right)}}{2 \, x^{\frac{3}{2}}} - \frac{3}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{e^{x}}{x^{3}}}\right)=\answer{\frac{e^{x}}{x^{3}} - \frac{3 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\log\left(x\right)}{x^{4}}}\right)=\answer{-\frac{4 \, \log\left(x\right)}{x^{5}} + \frac{1}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x^{3}}{x + 5}}\right)=\answer{\frac{3 \, x^{2}}{x + 5} - \frac{x^{3}}{{\left(x + 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{x - 4}{\sin\left(x\right)}}\right)=\answer{-\frac{{\left(x - 4\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{1}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{3}}{x - 5}}\right)=\answer{-\frac{6 \, x^{2}}{x - 5} + \frac{2 \, x^{3}}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x - 3\right)}}{x^{2}}}\right)=\answer{\frac{10 \, {\left(x - 3\right)}}{x^{3}} - \frac{5}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, {\left(x - 3\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{2 \, {\left(x - 3\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{2}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \cos\left(x\right)}{x^{3}}}\right)=\answer{-\frac{4 \, \sin\left(x\right)}{x^{3}} - \frac{12 \, \cos\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \sqrt{x}}{x + 2}}\right)=\answer{\frac{2}{{\left(x + 2\right)} \sqrt{x}} - \frac{4 \, \sqrt{x}}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{x + 1}{\sqrt{x}}}\right)=\answer{\frac{x + 1}{2 \, x^{\frac{3}{2}}} - \frac{1}{\sqrt{x}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x - 4\right)}}{\log\left(x\right)}}\right)=\answer{\frac{4}{\log\left(x\right)} - \frac{4 \, {\left(x - 4\right)}}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \sqrt{x}}{\sin\left(x\right)}}\right)=\answer{\frac{2 \, \sqrt{x} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{1}{\sqrt{x} \sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \sqrt{x}}{x - 2}}\right)=\answer{-\frac{5}{2 \, {\left(x - 2\right)} \sqrt{x}} + \frac{5 \, \sqrt{x}}{{\left(x - 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\sin\left(x\right)}{\log\left(x\right)}}\right)=\answer{-\frac{\cos\left(x\right)}{\log\left(x\right)} + \frac{\sin\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{\cos\left(x\right)}{x^{4}}}\right)=\answer{\frac{\sin\left(x\right)}{x^{4}} + \frac{4 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, e^{x}}{x - 1}}\right)=\answer{-\frac{5 \, e^{x}}{x - 1} + \frac{5 \, e^{x}}{{\left(x - 1\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, e^{x}}{x + 5}}\right)=\answer{-\frac{3 \, e^{x}}{x + 5} + \frac{3 \, e^{x}}{{\left(x + 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-2 \, x^{\frac{3}{2}}}\right)=\answer{-3 \, \sqrt{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \sin\left(x\right)}{x^{3}}}\right)=\answer{\frac{2 \, \cos\left(x\right)}{x^{3}} - \frac{6 \, \sin\left(x\right)}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \sqrt{x}}{\log\left(x\right)}}\right)=\answer{-\frac{1}{\sqrt{x} \log\left(x\right)} + \frac{2}{\sqrt{x} \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \cos\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{3 \, \sin\left(x\right)}{\sqrt{x}} - \frac{3 \, \cos\left(x\right)}{2 \, x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{\frac{3 \, \sin\left(x\right)}{x^{2}} + \frac{6 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \cos\left(x\right)}{x + 2}}\right)=\answer{-\frac{3 \, \sin\left(x\right)}{x + 2} - \frac{3 \, \cos\left(x\right)}{{\left(x + 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, {\left(x - 2\right)}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, {\left(x - 2\right)} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{4}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, {\left(x - 3\right)}}{\cos\left(x\right)}}\right)=\answer{\frac{5 \, {\left(x - 3\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{5}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, \sin\left(x\right)}{x^{4}}}\right)=\answer{-\frac{3 \, \cos\left(x\right)}{x^{4}} + \frac{12 \, \sin\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, x^{4}}{\cos\left(x\right)}}\right)=\answer{-\frac{3 \, x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{12 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, x^{2}}{x - 5}}\right)=\answer{\frac{10 \, x}{x - 5} - \frac{5 \, x^{2}}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{3}}{\sin\left(x\right)}}\right)=\answer{-\frac{4 \, x^{3} \cos\left(x\right)}{\sin\left(x\right)^{2}} + \frac{12 \, x^{2}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{2}}{\sin\left(x\right)}}\right)=\answer{\frac{4 \, x^{2} \cos\left(x\right)}{\sin\left(x\right)^{2}} - \frac{8 \, x}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\sin\left(x\right)}{x^{4}}}\right)=\answer{\frac{\cos\left(x\right)}{x^{4}} - \frac{4 \, \sin\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \sqrt{x}}{\cos\left(x\right)}}\right)=\answer{-\frac{2 \, \sqrt{x} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{1}{\sqrt{x} \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({e^{\left(-x\right)} \log\left(x\right)}\right)=\answer{-e^{\left(-x\right)} \log\left(x\right) + \frac{e^{\left(-x\right)}}{x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({4 \, x}\right)=\answer{4}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, {\left(x - 4\right)}}{\cos\left(x\right)}}\right)=\answer{\frac{5 \, {\left(x - 4\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{5}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, \sqrt{x}}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, \sqrt{x} \sin\left(x\right)}{\cos\left(x\right)^{2}} + \frac{2}{\sqrt{x} \cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{\log\left(x\right)}{x + 3}}\right)=\answer{-\frac{\log\left(x\right)}{{\left(x + 3\right)}^{2}} + \frac{1}{{\left(x + 3\right)} x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, e^{x}}{x + 3}}\right)=\answer{\frac{3 \, e^{x}}{x + 3} - \frac{3 \, e^{x}}{{\left(x + 3\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \sin\left(x\right)}{\sqrt{x}}}\right)=\answer{-\frac{2 \, \cos\left(x\right)}{\sqrt{x}} + \frac{\sin\left(x\right)}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3}{x^{\frac{7}{2}}}}\right)=\answer{-\frac{21}{2 \, x^{\frac{9}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{3 \, {\left(x - 5\right)}}{x^{4}}}\right)=\answer{\frac{12 \, {\left(x - 5\right)}}{x^{5}} - \frac{3}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{2 \, \cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} - 2}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{3}}{\log\left(x\right)}}\right)=\answer{\frac{12 \, x^{2}}{\log\left(x\right)} - \frac{4 \, x^{2}}{\log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{1}{x}}\right)=\answer{-\frac{1}{x^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3}{x^{2}}}\right)=\answer{-\frac{6}{x^{3}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \sin\left(x\right)}{x + 5}}\right)=\answer{\frac{3 \, \cos\left(x\right)}{x + 5} - \frac{3 \, \sin\left(x\right)}{{\left(x + 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{1}{x^{\frac{5}{2}}}}\right)=\answer{\frac{5}{2 \, x^{\frac{7}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \cos\left(x\right)}{x - 5}}\right)=\answer{-\frac{2 \, \sin\left(x\right)}{x - 5} - \frac{2 \, \cos\left(x\right)}{{\left(x - 5\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{e^{x}}{\sin\left(x\right)}}\right)=\answer{\frac{\cos\left(x\right) e^{x}}{\sin\left(x\right)^{2}} - \frac{e^{x}}{\sin\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, \log\left(x\right)}{x^{3}}}\right)=\answer{\frac{12 \, \log\left(x\right)}{x^{4}} - \frac{4}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{3 \, \cos\left(x\right)}{\sin\left(x\right)}}\right)=\answer{-\frac{3 \, \cos\left(x\right)^{2}}{\sin\left(x\right)^{2}} - 3}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{4 \, x^{2}}{x - 2}}\right)=\answer{-\frac{8 \, x}{x - 2} + \frac{4 \, x^{2}}{{\left(x - 2\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, {\left(x + 4\right)}}{\cos\left(x\right)}}\right)=\answer{-\frac{5 \, {\left(x + 4\right)} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{5}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, \log\left(x\right)}{x + 2}}\right)=\answer{\frac{5 \, \log\left(x\right)}{{\left(x + 2\right)}^{2}} - \frac{5}{{\left(x + 2\right)} x}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, e^{x}}{\cos\left(x\right)}}\right)=\answer{\frac{2 \, e^{x}}{\cos\left(x\right)} + \frac{2 \, e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({5 \, x^{\frac{5}{2}}}\right)=\answer{\frac{25}{2} \, x^{\frac{3}{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, e^{x}}{\sqrt{x}}}\right)=\answer{\frac{2 \, e^{x}}{\sqrt{x}} - \frac{e^{x}}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{2 \, \cos\left(x\right)}{x^{4}}}\right)=\answer{-\frac{2 \, \sin\left(x\right)}{x^{4}} - \frac{8 \, \cos\left(x\right)}{x^{5}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{5 \, e^{x}}{x^{3}}}\right)=\answer{-\frac{5 \, e^{x}}{x^{3}} + \frac{15 \, e^{x}}{x^{4}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \log\left(x\right)}{\sqrt{x}}}\right)=\answer{\frac{\log\left(x\right)}{x^{\frac{3}{2}}} - \frac{2}{x^{\frac{3}{2}}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, x^{2}}{x + 4}}\right)=\answer{\frac{8 \, x}{x + 4} - \frac{4 \, x^{2}}{{\left(x + 4\right)}^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, x^{4}}{\cos\left(x\right)}}\right)=\answer{-\frac{2 \, x^{4} \sin\left(x\right)}{\cos\left(x\right)^{2}} - \frac{8 \, x^{3}}{\cos\left(x\right)}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({-\frac{2 \, \sin\left(x\right)}{\log\left(x\right)}}\right)=\answer{-\frac{2 \, \cos\left(x\right)}{\log\left(x\right)} + \frac{2 \, \sin\left(x\right)}{x \log\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{4 \, e^{x}}{\cos\left(x\right)}}\right)=\answer{\frac{4 \, e^{x}}{\cos\left(x\right)} + \frac{4 \, e^{x} \sin\left(x\right)}{\cos\left(x\right)^{2}}}\] 
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0011. \\ \fi
\begin{problem}

Compute the following derivative:

\input{Derivative-Compute-0011.HELP.tex}

\[\dfrac{d}{dx}\left({\frac{5 \, \cos\left(x\right)}{x^{2}}}\right)=\answer{-\frac{5 \, \sin\left(x\right)}{x^{2}} - \frac{10 \, \cos\left(x\right)}{x^{3}}}\] 
\end{problem}}%}

