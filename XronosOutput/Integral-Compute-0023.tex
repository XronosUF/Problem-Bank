\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Func@Exp, Func@Trig, Sub@ByParts, File@0023}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{7}{37} \, {\left(6 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(-9 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{\frac{2}{9} \, {\left(\cos\left(9 \, x\right) + \sin\left(9 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(4 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{5}{17} \, {\left(\cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(4 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{3}{13} \, {\left(3 \, \cos\left(6 \, x\right) - 2 \, \sin\left(6 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{3}{26} \, {\left(5 \, \cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(9 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{1}{9} \, {\left(\cos\left(9 \, x\right) - \sin\left(9 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(-3 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{9}{34} \, {\left(5 \, \cos\left(3 \, x\right) + 3 \, \sin\left(3 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(6 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{\frac{1}{13} \, {\left(2 \, \cos\left(6 \, x\right) - 3 \, \sin\left(6 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(4 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{3}{97} \, {\left(9 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{\frac{3}{5} \, {\left(3 \, \cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(8 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{9}{34} \, {\left(\cos\left(2 \, x\right) - 4 \, \sin\left(2 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(x\right) e^{\left(-x\right)}}\;dx} = \answer{{-4 \, {\left(\cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(7 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{7}{53} \, {\left(2 \, \cos\left(2 \, x\right) - 7 \, \sin\left(2 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(3 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{\frac{7}{6} \, {\left(\cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(3 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{2}{17} \, {\left(5 \, \cos\left(5 \, x\right) - 3 \, \sin\left(5 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-4 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{8}{97} \, {\left(9 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(-2 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{-\frac{2}{5} \, {\left(\cos\left(x\right) + 2 \, \sin\left(x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{3}{13} \, {\left(5 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(6 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{4}{25} \, {\left(4 \, \cos\left(6 \, x\right) - 3 \, \sin\left(6 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(5 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{1}{61} \, {\left(6 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(6 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{\frac{1}{61} \, {\left(5 \, \cos\left(6 \, x\right) - 6 \, \sin\left(6 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(7 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{8}{53} \, {\left(2 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(6 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{\frac{5}{26} \, {\left(2 \, \cos\left(6 \, x\right) - 3 \, \sin\left(6 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(-8 \, x\right) e^{x}}\;dx} = \answer{{-\frac{7}{65} \, {\left(\cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-2 \, x\right) e^{x}}\;dx} = \answer{{\frac{7}{5} \, {\left(\cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{9}{10} \, {\left(3 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-7 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{7}{58} \, {\left(3 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(3 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{1}{10} \, {\left(3 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(5 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{9}{74} \, {\left(7 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(2 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{10} \, {\left(3 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(-3 \, x\right) e^{x}}\;dx} = \answer{{\frac{1}{5} \, {\left(\cos\left(3 \, x\right) + 3 \, \sin\left(3 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{2}{13} \, {\left(5 \, \cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(3 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{-\frac{2}{73} \, {\left(8 \, \cos\left(8 \, x\right) - 3 \, \sin\left(8 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(-8 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{1}{5} \, {\left(\cos\left(8 \, x\right) - 2 \, \sin\left(8 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-4 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{4}{13} \, {\left(3 \, \cos\left(4 \, x\right) + 2 \, \sin\left(4 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-7 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{\frac{1}{17} \, {\left(6 \, \cos\left(6 \, x\right) + 7 \, \sin\left(6 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-4 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{8}{65} \, {\left(7 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(4 \, x\right) e^{x}}\;dx} = \answer{{\frac{2}{17} \, {\left(\cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{9}{26} \, {\left(5 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(6 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{9}{20} \, {\left(\cos\left(2 \, x\right) - 3 \, \sin\left(2 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-5 \, x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{\frac{4}{5} \, {\left(\cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-7 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{7}{53} \, {\left(2 \, \cos\left(2 \, x\right) + 7 \, \sin\left(2 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(8 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{1}{8} \, {\left(\cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(-8 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{-\frac{7}{145} \, {\left(9 \, \cos\left(9 \, x\right) + 8 \, \sin\left(9 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(-6 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{4} \, {\left(\cos\left(6 \, x\right) + \sin\left(6 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(-5 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{5}{74} \, {\left(7 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{1}{65} \, {\left(8 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{e^{\left(-5 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{1}{29} \, {\left(2 \, \cos\left(2 \, x\right) + 5 \, \sin\left(2 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-3 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{-\frac{7}{58} \, {\left(7 \, \cos\left(7 \, x\right) + 3 \, \sin\left(7 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(-4 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{13} \, {\left(3 \, \cos\left(6 \, x\right) + 2 \, \sin\left(6 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(9 \, x\right)} \sin\left(-3 \, x\right)}\;dx} = \answer{{\frac{2}{15} \, {\left(\cos\left(3 \, x\right) - 3 \, \sin\left(3 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(-9 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{3}{53} \, {\left(5 \, \cos\left(9 \, x\right) - 9 \, \sin\left(9 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-6 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{8}{15} \, {\left(\cos\left(6 \, x\right) + 2 \, \sin\left(6 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-7 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{8}{113} \, {\left(8 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(-7 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{-\frac{7}{113} \, {\left(8 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(-4 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{6}{41} \, {\left(5 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(-2 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{9}{5} \, {\left(\cos\left(x\right) + 2 \, \sin\left(x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(3 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{5}{6} \, {\left(\cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(9 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{4}{53} \, {\left(5 \, \cos\left(5 \, x\right) - 9 \, \sin\left(5 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(9 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{9}{85} \, {\left(2 \, \cos\left(2 \, x\right) - 9 \, \sin\left(2 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-8 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{4}{89} \, {\left(5 \, \cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(7 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{1}{74} \, {\left(5 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{x} \sin\left(8 \, x\right)}\;dx} = \answer{{\frac{8}{65} \, {\left(8 \, \cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{41} \, {\left(9 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-9 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{-\frac{1}{10} \, {\left(\cos\left(3 \, x\right) + 3 \, \sin\left(3 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(-9 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{-\frac{3}{85} \, {\left(2 \, \cos\left(2 \, x\right) + 9 \, \sin\left(2 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(6 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{-\frac{1}{3} \, {\left(\cos\left(6 \, x\right) - \sin\left(6 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(-x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{-\frac{1}{41} \, {\left(9 \, \cos\left(9 \, x\right) + \sin\left(9 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-2 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{1}{2} \, {\left(2 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{\frac{3}{25} \, {\left(7 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-6 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{2}{3} \, {\left(\cos\left(6 \, x\right) - \sin\left(6 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-4 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{-\frac{7}{26} \, {\left(3 \, \cos\left(6 \, x\right) + 2 \, \sin\left(6 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(9 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{1}{13} \, {\left(2 \, \cos\left(9 \, x\right) - 3 \, \sin\left(9 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-7 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{1}{65} \, {\left(4 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(-5 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{\frac{7}{89} \, {\left(8 \, \cos\left(8 \, x\right) + 5 \, \sin\left(8 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(4 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{\frac{1}{5} \, {\left(2 \, \cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(4 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{4}{97} \, {\left(9 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(-8 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{-\frac{1}{145} \, {\left(9 \, \cos\left(9 \, x\right) + 8 \, \sin\left(9 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-5 \, x\right) e^{x}}\;dx} = \answer{{-\frac{1}{26} \, {\left(\cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-5 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{2}{17} \, {\left(3 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(9 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{\frac{5}{82} \, {\left(\cos\left(x\right) - 9 \, \sin\left(x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{1}{82} \, {\left(9 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-7 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{8}{53} \, {\left(2 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-6 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{\frac{2}{13} \, {\left(3 \, \cos\left(9 \, x\right) + 2 \, \sin\left(9 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(-9 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{\frac{1}{97} \, {\left(4 \, \cos\left(4 \, x\right) + 9 \, \sin\left(4 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(4 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{1}{5} \, {\left(2 \, \cos\left(4 \, x\right) - \sin\left(4 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{e^{\left(-8 \, x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{\frac{1}{89} \, {\left(5 \, \cos\left(5 \, x\right) + 8 \, \sin\left(5 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-4 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{5}{41} \, {\left(5 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-6 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{9}{61} \, {\left(5 \, \cos\left(6 \, x\right) - 6 \, \sin\left(6 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-4 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{3}{41} \, {\left(5 \, \cos\left(5 \, x\right) + 4 \, \sin\left(5 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-4 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{\frac{4}{41} \, {\left(5 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-4 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{-\frac{1}{10} \, {\left(2 \, \cos\left(4 \, x\right) + \sin\left(4 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(3 \, x\right)} \sin\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, {\left(\cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(8 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{5}{89} \, {\left(5 \, \cos\left(8 \, x\right) - 8 \, \sin\left(8 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(8 \, x\right) e^{x}}\;dx} = \answer{{-\frac{4}{65} \, {\left(\cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-5 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{6}{89} \, {\left(8 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{7}{50} \, {\left(7 \, \cos\left(7 \, x\right) + \sin\left(7 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{e^{\left(-8 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{-\frac{1}{145} \, {\left(9 \, \cos\left(9 \, x\right) + 8 \, \sin\left(9 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-7 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{3}{53} \, {\left(2 \, \cos\left(2 \, x\right) + 7 \, \sin\left(2 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(6 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{\frac{8}{61} \, {\left(5 \, \cos\left(6 \, x\right) - 6 \, \sin\left(6 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(4 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{2}{17} \, {\left(\cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(6 \, x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{\frac{5}{26} \, {\left(2 \, \cos\left(4 \, x\right) - 3 \, \sin\left(4 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-4 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{3}{65} \, {\left(7 \, \cos\left(7 \, x\right) + 4 \, \sin\left(7 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(5 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{-\frac{6}{61} \, {\left(6 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(5 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{2}{13} \, {\left(\cos\left(x\right) - 5 \, \sin\left(x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(9 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{4}{65} \, {\left(7 \, \cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{7}{5} \, {\left(2 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(3 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{6}{13} \, {\left(2 \, \cos\left(2 \, x\right) - 3 \, \sin\left(2 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(-4 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{-\frac{9}{10} \, {\left(\cos\left(4 \, x\right) + 2 \, \sin\left(4 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{e^{\left(8 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{-\frac{1}{113} \, {\left(7 \, \cos\left(7 \, x\right) - 8 \, \sin\left(7 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(2 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{5}{4} \, {\left(\cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-6 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{3}{25} \, {\left(4 \, \cos\left(6 \, x\right) - 3 \, \sin\left(6 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(9 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{\frac{1}{41} \, {\left(\cos\left(x\right) - 9 \, \sin\left(x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(-x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{3}{5} \, {\left(2 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-7 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{9}{130} \, {\left(9 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(3 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{2}{17} \, {\left(5 \, \cos\left(3 \, x\right) + 3 \, \sin\left(3 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-3 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{\frac{1}{5} \, {\left(2 \, \cos\left(6 \, x\right) + \sin\left(6 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(-3 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{1}{17} \, {\left(5 \, \cos\left(3 \, x\right) + 3 \, \sin\left(3 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(-x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{7}{2} \, {\left(\cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(5 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, {\left(\cos\left(5 \, x\right) - \sin\left(5 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(-5 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{-\frac{5}{106} \, {\left(9 \, \cos\left(9 \, x\right) + 5 \, \sin\left(9 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(9 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{8}{145} \, {\left(8 \, \cos\left(9 \, x\right) - 9 \, \sin\left(9 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(7 \, x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{-\frac{8}{65} \, {\left(4 \, \cos\left(4 \, x\right) - 7 \, \sin\left(4 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(7 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{130} \, {\left(9 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-6 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{8}{85} \, {\left(7 \, \cos\left(6 \, x\right) - 6 \, \sin\left(6 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(8 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{3}{113} \, {\left(7 \, \cos\left(7 \, x\right) - 8 \, \sin\left(7 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(5 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{7}{34} \, {\left(3 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-8 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{\frac{6}{73} \, {\left(3 \, \cos\left(3 \, x\right) + 8 \, \sin\left(3 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-6 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{3}{37} \, {\left(\cos\left(x\right) + 6 \, \sin\left(x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{x} \sin\left(-8 \, x\right)}\;dx} = \answer{{\frac{2}{65} \, {\left(8 \, \cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-4 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{\frac{3}{5} \, {\left(\cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-7 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{4}{85} \, {\left(6 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(6 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{5}{26} \, {\left(2 \, \cos\left(6 \, x\right) + 3 \, \sin\left(6 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(-8 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{1}{73} \, {\left(3 \, \cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(4 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{\frac{9}{97} \, {\left(9 \, \cos\left(9 \, x\right) - 4 \, \sin\left(9 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(-7 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{-\frac{5}{53} \, {\left(2 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(9 \, x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{-\frac{1}{53} \, {\left(5 \, \cos\left(5 \, x\right) - 9 \, \sin\left(5 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-6 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{5}{39} \, {\left(3 \, \cos\left(9 \, x\right) + 2 \, \sin\left(9 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-4 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{1}{17} \, {\left(\cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(4 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{3}{41} \, {\left(5 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(8 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{1}{5} \, {\left(\cos\left(8 \, x\right) + 2 \, \sin\left(8 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(x\right) e^{\left(-x\right)}}\;dx} = \answer{{-\frac{9}{2} \, {\left(\cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(8 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{29} \, {\left(9 \, \cos\left(8 \, x\right) - 8 \, \sin\left(8 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(-2 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{2}{53} \, {\left(7 \, \cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(-x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{-\frac{2}{13} \, {\left(5 \, \cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(8 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{9}{145} \, {\left(9 \, \cos\left(8 \, x\right) - 8 \, \sin\left(8 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-7 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{-\frac{4}{25} \, {\left(\cos\left(x\right) + 7 \, \sin\left(x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-7 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{3}{7} \, {\left(\cos\left(7 \, x\right) + \sin\left(7 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(6 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{8}{85} \, {\left(7 \, \cos\left(7 \, x\right) - 6 \, \sin\left(7 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-3 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{9}{34} \, {\left(5 \, \cos\left(3 \, x\right) + 3 \, \sin\left(3 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(-6 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{2}{25} \, {\left(4 \, \cos\left(6 \, x\right) - 3 \, \sin\left(6 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(3 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{4}{3} \, {\left(\cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(3 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{2}{3} \, {\left(\cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-8 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{8}{113} \, {\left(7 \, \cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(5 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{5}{61} \, {\left(6 \, \cos\left(6 \, x\right) - 5 \, \sin\left(6 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(-5 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{3}{13} \, {\left(\cos\left(x\right) + 5 \, \sin\left(x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(5 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{9}{10} \, {\left(\cos\left(5 \, x\right) - \sin\left(5 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(3 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{9}{58} \, {\left(7 \, \cos\left(7 \, x\right) - 3 \, \sin\left(7 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-2 \, x\right)} \sin\left(-3 \, x\right)}\;dx} = \answer{{\frac{7}{13} \, {\left(3 \, \cos\left(3 \, x\right) + 2 \, \sin\left(3 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-4 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{8}{41} \, {\left(5 \, \cos\left(5 \, x\right) + 4 \, \sin\left(5 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-3 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{8}{73} \, {\left(8 \, \cos\left(8 \, x\right) + 3 \, \sin\left(8 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(-x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{2}{25} \, {\left(7 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(6 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{-\frac{5}{12} \, {\left(\cos\left(6 \, x\right) + \sin\left(6 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(9 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{7}{82} \, {\left(\cos\left(x\right) - 9 \, \sin\left(x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-4 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{3}{97} \, {\left(9 \, \cos\left(9 \, x\right) + 4 \, \sin\left(9 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(-7 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{1}{25} \, {\left(\cos\left(x\right) + 7 \, \sin\left(x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-5 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{\frac{8}{89} \, {\left(8 \, \cos\left(8 \, x\right) + 5 \, \sin\left(8 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(6 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, {\left(\cos\left(2 \, x\right) - 3 \, \sin\left(2 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(6 \, x\right) e^{x}}\;dx} = \answer{{-\frac{7}{37} \, {\left(\cos\left(6 \, x\right) + 6 \, \sin\left(6 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{-\frac{4}{37} \, {\left(6 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-3 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{\frac{3}{5} \, {\left(\cos\left(x\right) + 3 \, \sin\left(x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(5 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{2}{17} \, {\left(3 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{3}{41} \, {\left(9 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-8 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{2}{113} \, {\left(7 \, \cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(5 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{\frac{3}{5} \, {\left(\cos\left(5 \, x\right) - \sin\left(5 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(3 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{7}{13} \, {\left(2 \, \cos\left(2 \, x\right) - 3 \, \sin\left(2 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(2 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{4}{5} \, {\left(\cos\left(x\right) - 2 \, \sin\left(x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(9 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{-\frac{2}{39} \, {\left(2 \, \cos\left(9 \, x\right) + 3 \, \sin\left(9 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(4 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{\frac{1}{13} \, {\left(3 \, \cos\left(6 \, x\right) - 2 \, \sin\left(6 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-3 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{-\frac{2}{15} \, {\left(3 \, \cos\left(9 \, x\right) + \sin\left(9 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(-6 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{1}{10} \, {\left(\cos\left(2 \, x\right) + 3 \, \sin\left(2 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(-7 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{2}{53} \, {\left(2 \, \cos\left(2 \, x\right) + 7 \, \sin\left(2 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-3 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{1}{15} \, {\left(2 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(3 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{\frac{9}{73} \, {\left(8 \, \cos\left(8 \, x\right) - 3 \, \sin\left(8 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(5 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{1}{13} \, {\left(\cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(9 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{-\frac{1}{41} \, {\left(\cos\left(x\right) - 9 \, \sin\left(x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(6 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{1}{4} \, {\left(\cos\left(2 \, x\right) - 3 \, \sin\left(2 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(9 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{\frac{3}{97} \, {\left(4 \, \cos\left(9 \, x\right) - 9 \, \sin\left(9 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(-4 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{3}{41} \, {\left(5 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{5}{17} \, {\left(4 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-2 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{2}{5} \, {\left(3 \, \cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(2 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{-\frac{1}{4} \, {\left(3 \, \cos\left(6 \, x\right) - \sin\left(6 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{-\frac{1}{13} \, {\left(8 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(-9 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{39} \, {\left(2 \, \cos\left(6 \, x\right) + 3 \, \sin\left(6 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(-8 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{\frac{1}{4} \, {\left(\cos\left(8 \, x\right) - 2 \, \sin\left(8 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(6 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{3}{85} \, {\left(7 \, \cos\left(7 \, x\right) - 6 \, \sin\left(7 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(5 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{\frac{9}{29} \, {\left(2 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(-x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{9}{50} \, {\left(7 \, \cos\left(7 \, x\right) + \sin\left(7 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{e^{\left(-3 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{1}{30} \, {\left(3 \, \cos\left(9 \, x\right) + \sin\left(9 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(7 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{1}{26} \, {\left(9 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(4 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{1}{13} \, {\left(7 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(-8 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{1}{8} \, {\left(\cos\left(8 \, x\right) + \sin\left(8 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(-5 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{\frac{3}{41} \, {\left(4 \, \cos\left(4 \, x\right) + 5 \, \sin\left(4 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-9 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{5}{82} \, {\left(\cos\left(x\right) + 9 \, \sin\left(x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(-3 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, {\left(3 \, \cos\left(9 \, x\right) + \sin\left(9 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{\frac{8}{65} \, {\left(8 \, \cos\left(8 \, x\right) + \sin\left(8 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(3 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, {\left(4 \, \cos\left(4 \, x\right) - 3 \, \sin\left(4 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(6 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{\frac{9}{85} \, {\left(7 \, \cos\left(6 \, x\right) - 6 \, \sin\left(6 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(-3 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{2}{73} \, {\left(8 \, \cos\left(8 \, x\right) + 3 \, \sin\left(8 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(8 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{\frac{2}{89} \, {\left(5 \, \cos\left(8 \, x\right) - 8 \, \sin\left(8 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(-8 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{3}{20} \, {\left(\cos\left(8 \, x\right) + 2 \, \sin\left(8 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(-4 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{3}{10} \, {\left(\cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{x} \sin\left(7 \, x\right)}\;dx} = \answer{{-\frac{3}{25} \, {\left(7 \, \cos\left(7 \, x\right) - \sin\left(7 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-8 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{1}{8} \, {\left(\cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-6 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{\frac{1}{26} \, {\left(2 \, \cos\left(6 \, x\right) - 3 \, \sin\left(6 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(3 \, x\right) e^{x}}\;dx} = \answer{{-\frac{2}{5} \, {\left(\cos\left(3 \, x\right) + 3 \, \sin\left(3 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(-2 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{-\frac{7}{20} \, {\left(3 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(2 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{3}{5} \, {\left(\cos\left(x\right) - 2 \, \sin\left(x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(5 \, x\right) e^{x}}\;dx} = \answer{{\frac{3}{26} \, {\left(\cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-6 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{1}{4} \, {\left(\cos\left(2 \, x\right) + 3 \, \sin\left(2 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(7 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{-\frac{2}{113} \, {\left(8 \, \cos\left(8 \, x\right) - 7 \, \sin\left(8 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{-\frac{4}{17} \, {\left(4 \, \cos\left(4 \, x\right) + \sin\left(4 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(4 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{3}{10} \, {\left(\cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(7 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{3}{53} \, {\left(2 \, \cos\left(2 \, x\right) - 7 \, \sin\left(2 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(-2 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{3}{10} \, {\left(3 \, \cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(4 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{-\frac{5}{97} \, {\left(9 \, \cos\left(9 \, x\right) - 4 \, \sin\left(9 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(8 \, x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{\frac{3}{20} \, {\left(\cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-6 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{\frac{2}{5} \, {\left(\cos\left(3 \, x\right) + 2 \, \sin\left(3 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{\frac{2}{13} \, {\left(5 \, \cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(5 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{6}{29} \, {\left(2 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(2 \, x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{-\frac{7}{10} \, {\left(2 \, \cos\left(4 \, x\right) - \sin\left(4 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(-x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{9}{5} \, {\left(2 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(4 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{97} \, {\left(9 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(4 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{13} \, {\left(3 \, \cos\left(6 \, x\right) - 2 \, \sin\left(6 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(7 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{-\frac{2}{53} \, {\left(2 \, \cos\left(2 \, x\right) - 7 \, \sin\left(2 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(-3 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{5} \, {\left(3 \, \cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(4 \, x\right) e^{x}}\;dx} = \answer{{-\frac{6}{17} \, {\left(\cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-8 \, x\right) e^{x}}\;dx} = \answer{{-\frac{6}{65} \, {\left(\cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(-9 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{2}{65} \, {\left(7 \, \cos\left(9 \, x\right) - 9 \, \sin\left(9 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(-2 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{3}{34} \, {\left(4 \, \cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(-6 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{7}{61} \, {\left(5 \, \cos\left(5 \, x\right) + 6 \, \sin\left(5 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(3 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{2}{15} \, {\left(2 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(5 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, {\left(\cos\left(5 \, x\right) - \sin\left(5 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(4 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{1}{20} \, {\left(2 \, \cos\left(4 \, x\right) - \sin\left(4 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(-x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{-{\left(2 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-8 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{7}{89} \, {\left(5 \, \cos\left(8 \, x\right) - 8 \, \sin\left(8 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-4 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{-\frac{5}{97} \, {\left(9 \, \cos\left(9 \, x\right) + 4 \, \sin\left(9 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(9 \, x\right) e^{x}}\;dx} = \answer{{\frac{3}{82} \, {\left(\cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(2 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{4}{17} \, {\left(4 \, \cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(-6 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{-\frac{1}{13} \, {\left(3 \, \cos\left(6 \, x\right) + 2 \, \sin\left(6 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(-5 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{3}{53} \, {\left(9 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(-5 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{3}{74} \, {\left(7 \, \cos\left(7 \, x\right) + 5 \, \sin\left(7 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(7 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{1}{13} \, {\left(4 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(-x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{1}{2} \, {\left(3 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{x} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{2}{13} \, {\left(5 \, \cos\left(5 \, x\right) - \sin\left(5 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(-x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{1}{13} \, {\left(5 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(8 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{-\frac{9}{34} \, {\left(\cos\left(2 \, x\right) - 4 \, \sin\left(2 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(-9 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{3}{130} \, {\left(7 \, \cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(-3 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{-\frac{1}{6} \, {\left(3 \, \cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-7 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{-\frac{4}{65} \, {\left(4 \, \cos\left(4 \, x\right) + 7 \, \sin\left(4 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(7 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{3}{14} \, {\left(\cos\left(7 \, x\right) - \sin\left(7 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(6 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{\frac{1}{39} \, {\left(3 \, \cos\left(9 \, x\right) - 2 \, \sin\left(9 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{\frac{1}{50} \, {\left(7 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-2 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{9}{10} \, {\left(2 \, \cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-9 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{8}{85} \, {\left(2 \, \cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(-5 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{-\frac{5}{29} \, {\left(2 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(-3 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{-\frac{7}{15} \, {\left(2 \, \cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(-7 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{-\frac{7}{130} \, {\left(9 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(4 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{9}{25} \, {\left(3 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-8 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{8}{65} \, {\left(\cos\left(x\right) + 8 \, \sin\left(x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(-3 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{9}{13} \, {\left(2 \, \cos\left(2 \, x\right) + 3 \, \sin\left(2 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(-4 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{-\frac{1}{10} \, {\left(2 \, \cos\left(8 \, x\right) + \sin\left(8 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-7 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{6}{85} \, {\left(6 \, \cos\left(6 \, x\right) + 7 \, \sin\left(6 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(6 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{\frac{6}{85} \, {\left(7 \, \cos\left(6 \, x\right) - 6 \, \sin\left(6 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(2 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{-\frac{7}{5} \, {\left(\cos\left(2 \, x\right) - 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(4 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{1}{5} \, {\left(\cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(4 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{7}{26} \, {\left(3 \, \cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-3 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{1}{3} \, {\left(\cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(4 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{1}{65} \, {\left(7 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(8 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{1}{8} \, {\left(\cos\left(8 \, x\right) + \sin\left(8 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(-7 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{2}{65} \, {\left(9 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(5 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{9}{10} \, {\left(\cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-8 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{5}{34} \, {\left(\cos\left(2 \, x\right) + 4 \, \sin\left(2 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(9 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{2}{9} \, {\left(\cos\left(9 \, x\right) - \sin\left(9 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{-\frac{8}{17} \, {\left(4 \, \cos\left(4 \, x\right) + \sin\left(4 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-8 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{-\frac{7}{16} \, {\left(\cos\left(8 \, x\right) + \sin\left(8 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-9 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{\frac{4}{65} \, {\left(7 \, \cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(5 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{-\frac{9}{89} \, {\left(8 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-4 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{2}{25} \, {\left(3 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-8 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{4}{145} \, {\left(9 \, \cos\left(9 \, x\right) + 8 \, \sin\left(9 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-4 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{\frac{9}{20} \, {\left(2 \, \cos\left(8 \, x\right) + \sin\left(8 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-7 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{2}{65} \, {\left(9 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-2 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{5}{34} \, {\left(4 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(-9 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{1}{17} \, {\left(2 \, \cos\left(2 \, x\right) + 9 \, \sin\left(2 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-3 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{1}{6} \, {\left(\cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(-5 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{-\frac{6}{41} \, {\left(4 \, \cos\left(4 \, x\right) + 5 \, \sin\left(4 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(-2 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{9}{85} \, {\left(9 \, \cos\left(9 \, x\right) + 2 \, \sin\left(9 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(2 \, x\right) e^{x}}\;dx} = \answer{{-\frac{6}{5} \, {\left(\cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{x} \sin\left(-2 \, x\right)}\;dx} = \answer{{-\frac{1}{5} \, {\left(2 \, \cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(7 \, x\right) e^{x}}\;dx} = \answer{{\frac{1}{50} \, {\left(\cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-5 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{4}{37} \, {\left(7 \, \cos\left(7 \, x\right) + 5 \, \sin\left(7 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(4 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{1}{8} \, {\left(\cos\left(4 \, x\right) - \sin\left(4 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(2 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{7}{4} \, {\left(\cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-5 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{9}{29} \, {\left(2 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(9 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{-\frac{7}{39} \, {\left(2 \, \cos\left(6 \, x\right) - 3 \, \sin\left(6 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(8 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{5}{113} \, {\left(7 \, \cos\left(7 \, x\right) - 8 \, \sin\left(7 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-5 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, {\left(\cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(5 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{4}{5} \, {\left(\cos\left(5 \, x\right) - \sin\left(5 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(6 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{3}{85} \, {\left(7 \, \cos\left(7 \, x\right) - 6 \, \sin\left(7 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-3 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{9}{58} \, {\left(7 \, \cos\left(7 \, x\right) + 3 \, \sin\left(7 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(7 \, x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{-\frac{1}{74} \, {\left(5 \, \cos\left(5 \, x\right) - 7 \, \sin\left(5 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-5 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{1}{41} \, {\left(4 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(-6 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{\frac{1}{3} \, {\left(\cos\left(3 \, x\right) + 2 \, \sin\left(3 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-5 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{-\frac{8}{89} \, {\left(8 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(-7 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{5}{58} \, {\left(3 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(8 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{3}{16} \, {\left(\cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(3 \, x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{\frac{7}{25} \, {\left(4 \, \cos\left(4 \, x\right) - 3 \, \sin\left(4 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{-\frac{9}{5} \, {\left(2 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(-9 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{1}{41} \, {\left(\cos\left(x\right) + 9 \, \sin\left(x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-2 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{7}{4} \, {\left(\cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{-\frac{3}{13} \, {\left(5 \, \cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-5 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{\frac{9}{26} \, {\left(\cos\left(x\right) + 5 \, \sin\left(x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{9}{82} \, {\left(9 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(5 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{4}{29} \, {\left(2 \, \cos\left(2 \, x\right) - 5 \, \sin\left(2 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-5 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{1}{26} \, {\left(\cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-8 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{-\frac{5}{113} \, {\left(7 \, \cos\left(7 \, x\right) + 8 \, \sin\left(7 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(-6 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{1}{12} \, {\left(\cos\left(6 \, x\right) + \sin\left(6 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-3 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{7}{30} \, {\left(3 \, \cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-8 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{-\frac{6}{145} \, {\left(9 \, \cos\left(9 \, x\right) + 8 \, \sin\left(9 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-2 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{7}{10} \, {\left(2 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-5 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{8}{41} \, {\left(4 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-5 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{7}{106} \, {\left(9 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-4 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{-\frac{7}{65} \, {\left(7 \, \cos\left(7 \, x\right) + 4 \, \sin\left(7 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(5 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{2}{41} \, {\left(4 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(6 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{-\frac{5}{39} \, {\left(3 \, \cos\left(9 \, x\right) - 2 \, \sin\left(9 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-5 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{4}{5} \, {\left(\cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(6 \, x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{-\frac{4}{61} \, {\left(5 \, \cos\left(5 \, x\right) - 6 \, \sin\left(5 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, e^{\left(-5 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{7}{26} \, {\left(\cos\left(x\right) + 5 \, \sin\left(x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(2 \, x\right)} \sin\left(-3 \, x\right)}\;dx} = \answer{{-\frac{7}{13} \, {\left(3 \, \cos\left(3 \, x\right) - 2 \, \sin\left(3 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(9 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{8}{145} \, {\left(8 \, \cos\left(8 \, x\right) - 9 \, \sin\left(8 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{x} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{1}{13} \, {\left(5 \, \cos\left(5 \, x\right) - \sin\left(5 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(2 \, x\right)} \sin\left(-3 \, x\right)}\;dx} = \answer{{-\frac{1}{13} \, {\left(3 \, \cos\left(3 \, x\right) - 2 \, \sin\left(3 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(2 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{\frac{1}{10} \, {\left(3 \, \cos\left(6 \, x\right) - \sin\left(6 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-2 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{{\left(\cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(7 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{-\frac{3}{65} \, {\left(9 \, \cos\left(9 \, x\right) - 7 \, \sin\left(9 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(2 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{8}{29} \, {\left(5 \, \cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(7 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{9}{53} \, {\left(2 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-7 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{1}{37} \, {\left(5 \, \cos\left(7 \, x\right) + 7 \, \sin\left(7 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(4 \, x\right) e^{x}}\;dx} = \answer{{-\frac{2}{17} \, {\left(\cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(9 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{2}{13} \, {\left(2 \, \cos\left(9 \, x\right) - 3 \, \sin\left(9 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-7 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{\frac{9}{65} \, {\left(4 \, \cos\left(4 \, x\right) + 7 \, \sin\left(4 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(-2 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, {\left(3 \, \cos\left(6 \, x\right) + \sin\left(6 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(-6 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{\frac{3}{26} \, {\left(2 \, \cos\left(4 \, x\right) + 3 \, \sin\left(4 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(2 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{2}{13} \, {\left(3 \, \cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(4 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{-\frac{2}{65} \, {\left(7 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-2 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{8}{53} \, {\left(7 \, \cos\left(7 \, x\right) + 2 \, \sin\left(7 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(8 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{3}{10} \, {\left(\cos\left(8 \, x\right) + 2 \, \sin\left(8 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(2 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{{\left(\cos\left(2 \, x\right) - 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(-8 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{10} \, {\left(3 \, \cos\left(6 \, x\right) + 4 \, \sin\left(6 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-x\right) e^{x}}\;dx} = \answer{{-3 \, {\left(\cos\left(x\right) + \sin\left(x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(2 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{9}{53} \, {\left(7 \, \cos\left(7 \, x\right) - 2 \, \sin\left(7 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-6 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{3}{13} \, {\left(3 \, \cos\left(6 \, x\right) + 2 \, \sin\left(6 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-9 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{1}{17} \, {\left(2 \, \cos\left(9 \, x\right) - 9 \, \sin\left(9 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{e^{\left(5 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{-\frac{1}{34} \, {\left(3 \, \cos\left(3 \, x\right) - 5 \, \sin\left(3 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(6 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{7}{85} \, {\left(7 \, \cos\left(7 \, x\right) - 6 \, \sin\left(7 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-2 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{2 \, {\left(\cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-7 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{\frac{2}{65} \, {\left(4 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(5 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{9}{106} \, {\left(9 \, \cos\left(9 \, x\right) - 5 \, \sin\left(9 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-3 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{\frac{3}{2} \, {\left(\cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{9}{10} \, {\left(3 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(-4 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{-\frac{1}{13} \, {\left(3 \, \cos\left(6 \, x\right) + 2 \, \sin\left(6 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-2 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{\frac{8}{53} \, {\left(7 \, \cos\left(2 \, x\right) - 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-6 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, {\left(\cos\left(2 \, x\right) + 3 \, \sin\left(2 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(2 \, x\right) e^{x}}\;dx} = \answer{{-\frac{8}{5} \, {\left(\cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(4 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{5}{97} \, {\left(9 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-8 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{2}{65} \, {\left(\cos\left(8 \, x\right) - 8 \, \sin\left(8 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(-7 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{5}{14} \, {\left(\cos\left(7 \, x\right) + \sin\left(7 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, e^{\left(-6 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{1}{10} \, {\left(4 \, \cos\left(8 \, x\right) + 3 \, \sin\left(8 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-4 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{{\left(\cos\left(4 \, x\right) - \sin\left(4 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(5 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{7}{41} \, {\left(4 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{9}{37} \, {\left(6 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-5 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{-\frac{2}{29} \, {\left(2 \, \cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{1}{2} \, {\left(3 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(2 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{-\frac{3}{5} \, {\left(\cos\left(2 \, x\right) - 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(-6 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{3}{10} \, {\left(\cos\left(2 \, x\right) + 3 \, \sin\left(2 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, \cos\left(-5 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{-\frac{9}{26} \, {\left(\cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(2 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{1}{29} \, {\left(5 \, \cos\left(2 \, x\right) - 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-3 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{\frac{2}{25} \, {\left(4 \, \cos\left(3 \, x\right) - 3 \, \sin\left(3 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(-2 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{3}{29} \, {\left(5 \, \cos\left(2 \, x\right) - 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{-{\left(2 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{5}{37} \, {\left(6 \, \cos\left(x\right) - \sin\left(x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(-8 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{4}{25} \, {\left(3 \, \cos\left(8 \, x\right) + 4 \, \sin\left(8 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(9 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{\frac{2}{41} \, {\left(\cos\left(x\right) - 9 \, \sin\left(x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(6 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{\frac{3}{61} \, {\left(5 \, \cos\left(6 \, x\right) + 6 \, \sin\left(6 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(4 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{5}{41} \, {\left(5 \, \cos\left(5 \, x\right) - 4 \, \sin\left(5 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(2 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{3}{2} \, {\left(\cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(5 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{8}{29} \, {\left(2 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(5 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{-\frac{3}{26} \, {\left(\cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{x} \sin\left(2 \, x\right)}\;dx} = \answer{{-\frac{9}{5} \, {\left(2 \, \cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-4 \, x\right) e^{\left(-4 \, x\right)}}\;dx} = \answer{{-\frac{7}{8} \, {\left(\cos\left(4 \, x\right) - \sin\left(4 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(-7 \, x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{-\frac{4}{65} \, {\left(4 \, \cos\left(4 \, x\right) + 7 \, \sin\left(4 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(2 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{-\frac{8}{13} \, {\left(3 \, \cos\left(3 \, x\right) - 2 \, \sin\left(3 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(2 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{3}{34} \, {\left(4 \, \cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(3 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{-\frac{6}{73} \, {\left(8 \, \cos\left(8 \, x\right) - 3 \, \sin\left(8 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(-x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{\frac{1}{82} \, {\left(9 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(8 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{2}{65} \, {\left(\cos\left(x\right) - 8 \, \sin\left(x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(2 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{6}{5} \, {\left(\cos\left(x\right) - 2 \, \sin\left(x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(-5 \, x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{-\frac{1}{10} \, {\left(\cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-4 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{-\frac{9}{97} \, {\left(9 \, \cos\left(9 \, x\right) + 4 \, \sin\left(9 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(-8 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{1}{10} \, {\left(\cos\left(8 \, x\right) + 2 \, \sin\left(8 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(-2 \, x\right) e^{x}}\;dx} = \answer{{\frac{6}{5} \, {\left(\cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(-5 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{3}{13} \, {\left(\cos\left(x\right) + 5 \, \sin\left(x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(8 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{9}{65} \, {\left(\cos\left(x\right) - 8 \, \sin\left(x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(-9 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{1}{10} \, {\left(\cos\left(9 \, x\right) + 3 \, \sin\left(9 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-5 \, x\right) e^{x}}\;dx} = \answer{{-\frac{2}{13} \, {\left(\cos\left(5 \, x\right) + 5 \, \sin\left(5 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(-6 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, {\left(\cos\left(6 \, x\right) + \sin\left(6 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-4 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{9}{65} \, {\left(7 \, \cos\left(7 \, x\right) + 4 \, \sin\left(7 \, x\right)\right)} e^{\left(-4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{x} \sin\left(9 \, x\right)}\;dx} = \answer{{\frac{9}{82} \, {\left(9 \, \cos\left(9 \, x\right) - \sin\left(9 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(4 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{\frac{6}{17} \, {\left(\cos\left(x\right) - 4 \, \sin\left(x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-5 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{8}{89} \, {\left(8 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{\cos\left(-7 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{-\frac{1}{50} \, {\left(\cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(3 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{3}{58} \, {\left(7 \, \cos\left(7 \, x\right) - 3 \, \sin\left(7 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-2 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{3}{17} \, {\left(4 \, \cos\left(2 \, x\right) - \sin\left(2 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(5 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{\frac{7}{26} \, {\left(\cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(9 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{3}{145} \, {\left(8 \, \cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(-3 \, x\right)} \sin\left(8 \, x\right)}\;dx} = \answer{{-\frac{5}{73} \, {\left(8 \, \cos\left(8 \, x\right) + 3 \, \sin\left(8 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(3 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{4}{15} \, {\left(2 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(8 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{\frac{7}{16} \, {\left(\cos\left(8 \, x\right) - \sin\left(8 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, e^{\left(4 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{-\frac{3}{97} \, {\left(9 \, \cos\left(9 \, x\right) - 4 \, \sin\left(9 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(4 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{\frac{3}{10} \, {\left(\cos\left(4 \, x\right) + 2 \, \sin\left(4 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(5 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{-\frac{5}{41} \, {\left(4 \, \cos\left(4 \, x\right) - 5 \, \sin\left(4 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, \cos\left(-8 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{2}{25} \, {\left(3 \, \cos\left(8 \, x\right) - 4 \, \sin\left(8 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, \cos\left(2 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{2 \, {\left(\cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(-3 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{10} \, {\left(3 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(8 \, x\right)} \sin\left(-5 \, x\right)}\;dx} = \answer{{\frac{4}{89} \, {\left(5 \, \cos\left(5 \, x\right) - 8 \, \sin\left(5 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(2 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{6}{53} \, {\left(7 \, \cos\left(7 \, x\right) - 2 \, \sin\left(7 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(6 \, x\right)} \sin\left(9 \, x\right)}\;dx} = \answer{{\frac{8}{39} \, {\left(3 \, \cos\left(9 \, x\right) - 2 \, \sin\left(9 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(-6 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{9}{85} \, {\left(7 \, \cos\left(7 \, x\right) + 6 \, \sin\left(7 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-5 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{\frac{4}{37} \, {\left(7 \, \cos\left(7 \, x\right) + 5 \, \sin\left(7 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(-3 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{\frac{1}{58} \, {\left(7 \, \cos\left(3 \, x\right) - 3 \, \sin\left(3 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(9 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{-\frac{1}{85} \, {\left(2 \, \cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(8 \, x\right) e^{\left(7 \, x\right)}}\;dx} = \answer{{-\frac{1}{113} \, {\left(7 \, \cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(-6 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{-\frac{2}{5} \, {\left(\cos\left(3 \, x\right) + 2 \, \sin\left(3 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-3 \, x\right)} \sin\left(-8 \, x\right)}\;dx} = \answer{{\frac{4}{73} \, {\left(8 \, \cos\left(8 \, x\right) + 3 \, \sin\left(8 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(-7 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{-\frac{7}{74} \, {\left(5 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-7 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{-\frac{4}{7} \, {\left(\cos\left(7 \, x\right) + \sin\left(7 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(2 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{2}{13} \, {\left(3 \, \cos\left(2 \, x\right) - 2 \, \sin\left(2 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(6 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{-\frac{2}{39} \, {\left(3 \, \cos\left(6 \, x\right) + 2 \, \sin\left(6 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(-3 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{2}{5} \, {\left(\cos\left(x\right) + 3 \, \sin\left(x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(3 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{1}{15} \, {\left(2 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, \cos\left(-4 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{-\frac{3}{5} \, {\left(\cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(4 \, x\right) e^{\left(-2 \, x\right)}}\;dx} = \answer{{\frac{9}{10} \, {\left(\cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{8 \, e^{\left(-x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{\frac{8}{17} \, {\left(4 \, \cos\left(4 \, x\right) + \sin\left(4 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(4 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{\frac{8}{25} \, {\left(3 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(6 \, x\right)} \sin\left(-4 \, x\right)}\;dx} = \answer{{\frac{3}{26} \, {\left(2 \, \cos\left(4 \, x\right) - 3 \, \sin\left(4 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(8 \, x\right) e^{x}}\;dx} = \answer{{\frac{3}{65} \, {\left(\cos\left(8 \, x\right) + 8 \, \sin\left(8 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{\left(9 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{5}{18} \, {\left(\cos\left(9 \, x\right) - \sin\left(9 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, \cos\left(9 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{-\frac{1}{15} \, {\left(\cos\left(9 \, x\right) + 3 \, \sin\left(9 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-9 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{-\frac{5}{39} \, {\left(2 \, \cos\left(9 \, x\right) - 3 \, \sin\left(9 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(-3 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{1}{13} \, {\left(2 \, \cos\left(2 \, x\right) + 3 \, \sin\left(2 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-e^{\left(3 \, x\right)} \sin\left(7 \, x\right)}\;dx} = \answer{{\frac{1}{58} \, {\left(7 \, \cos\left(7 \, x\right) - 3 \, \sin\left(7 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(7 \, x\right) e^{\left(-3 \, x\right)}}\;dx} = \answer{{-\frac{7}{58} \, {\left(3 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(7 \, x\right)} \sin\left(-2 \, x\right)}\;dx} = \answer{{\frac{3}{53} \, {\left(2 \, \cos\left(2 \, x\right) - 7 \, \sin\left(2 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(-3 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{-\frac{1}{6} \, {\left(3 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(-x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{\frac{4}{17} \, {\left(4 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, e^{\left(8 \, x\right)} \sin\left(-6 \, x\right)}\;dx} = \answer{{\frac{2}{25} \, {\left(3 \, \cos\left(6 \, x\right) - 4 \, \sin\left(6 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, e^{x} \sin\left(-3 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, {\left(3 \, \cos\left(3 \, x\right) - \sin\left(3 \, x\right)\right)} e^{x}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(2 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{8}{53} \, {\left(7 \, \cos\left(7 \, x\right) - 2 \, \sin\left(7 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, e^{\left(5 \, x\right)} \sin\left(4 \, x\right)}\;dx} = \answer{{-\frac{3}{41} \, {\left(4 \, \cos\left(4 \, x\right) - 5 \, \sin\left(4 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, e^{\left(6 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{1}{2} \, {\left(\cos\left(6 \, x\right) - \sin\left(6 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(9 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{9}{106} \, {\left(5 \, \cos\left(5 \, x\right) - 9 \, \sin\left(5 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{7 \, \cos\left(3 \, x\right) e^{\left(3 \, x\right)}}\;dx} = \answer{{\frac{7}{6} \, {\left(\cos\left(3 \, x\right) + \sin\left(3 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(2 \, x\right)} \sin\left(-3 \, x\right)}\;dx} = \answer{{-\frac{9}{13} \, {\left(3 \, \cos\left(3 \, x\right) - 2 \, \sin\left(3 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-3 \, \cos\left(-x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{-\frac{3}{37} \, {\left(6 \, \cos\left(x\right) + \sin\left(x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-8 \, x\right) e^{\left(-5 \, x\right)}}\;dx} = \answer{{\frac{6}{89} \, {\left(5 \, \cos\left(8 \, x\right) - 8 \, \sin\left(8 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, e^{\left(-2 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{2}{85} \, {\left(9 \, \cos\left(9 \, x\right) + 2 \, \sin\left(9 \, x\right)\right)} e^{\left(-2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(-9 \, x\right) e^{\left(2 \, x\right)}}\;dx} = \answer{{-\frac{8}{85} \, {\left(2 \, \cos\left(9 \, x\right) + 9 \, \sin\left(9 \, x\right)\right)} e^{\left(2 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{9 \, e^{\left(8 \, x\right)} \sin\left(x\right)}\;dx} = \answer{{-\frac{9}{65} \, {\left(\cos\left(x\right) - 8 \, \sin\left(x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{e^{\left(-6 \, x\right)} \sin\left(-9 \, x\right)}\;dx} = \answer{{\frac{1}{39} \, {\left(3 \, \cos\left(9 \, x\right) + 2 \, \sin\left(9 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-4 \, e^{\left(8 \, x\right)} \sin\left(3 \, x\right)}\;dx} = \answer{{\frac{4}{73} \, {\left(3 \, \cos\left(3 \, x\right) - 8 \, \sin\left(3 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(7 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{1}{17} \, {\left(6 \, \cos\left(7 \, x\right) - 7 \, \sin\left(7 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(6 \, x\right)} \sin\left(-7 \, x\right)}\;dx} = \answer{{-\frac{7}{85} \, {\left(7 \, \cos\left(7 \, x\right) - 6 \, \sin\left(7 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-2 \, e^{\left(-8 \, x\right)} \sin\left(-x\right)}\;dx} = \answer{{-\frac{2}{65} \, {\left(\cos\left(x\right) + 8 \, \sin\left(x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(6 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{2}{3} \, {\left(\cos\left(6 \, x\right) - \sin\left(6 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, e^{\left(-5 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{\frac{9}{10} \, {\left(\cos\left(5 \, x\right) + \sin\left(5 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{3 \, \cos\left(2 \, x\right) e^{\left(6 \, x\right)}}\;dx} = \answer{{\frac{3}{20} \, {\left(3 \, \cos\left(2 \, x\right) + \sin\left(2 \, x\right)\right)} e^{\left(6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-6 \, \cos\left(-9 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{2}{13} \, {\left(2 \, \cos\left(9 \, x\right) - 3 \, \sin\left(9 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{2 \, \cos\left(-5 \, x\right) e^{\left(-8 \, x\right)}}\;dx} = \answer{{-\frac{2}{89} \, {\left(8 \, \cos\left(5 \, x\right) - 5 \, \sin\left(5 \, x\right)\right)} e^{\left(-8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, \cos\left(4 \, x\right) e^{\left(-7 \, x\right)}}\;dx} = \answer{{\frac{7}{65} \, {\left(7 \, \cos\left(4 \, x\right) - 4 \, \sin\left(4 \, x\right)\right)} e^{\left(-7 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, \cos\left(4 \, x\right) e^{\left(9 \, x\right)}}\;dx} = \answer{{-\frac{8}{97} \, {\left(9 \, \cos\left(4 \, x\right) + 4 \, \sin\left(4 \, x\right)\right)} e^{\left(9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-5 \, \cos\left(4 \, x\right) e^{\left(-6 \, x\right)}}\;dx} = \answer{{\frac{5}{26} \, {\left(3 \, \cos\left(4 \, x\right) - 2 \, \sin\left(4 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{6 \, e^{\left(-6 \, x\right)} \sin\left(5 \, x\right)}\;dx} = \answer{{-\frac{6}{61} \, {\left(5 \, \cos\left(5 \, x\right) + 6 \, \sin\left(5 \, x\right)\right)} e^{\left(-6 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{4 \, \cos\left(3 \, x\right) e^{\left(-x\right)}}\;dx} = \answer{{-\frac{2}{5} \, {\left(\cos\left(3 \, x\right) - 3 \, \sin\left(3 \, x\right)\right)} e^{\left(-x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-8 \, e^{\left(-5 \, x\right)} \sin\left(2 \, x\right)}\;dx} = \answer{{\frac{8}{29} \, {\left(2 \, \cos\left(2 \, x\right) + 5 \, \sin\left(2 \, x\right)\right)} e^{\left(-5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-\cos\left(2 \, x\right) e^{\left(5 \, x\right)}}\;dx} = \answer{{-\frac{1}{29} \, {\left(5 \, \cos\left(2 \, x\right) + 2 \, \sin\left(2 \, x\right)\right)} e^{\left(5 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(3 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{7}{15} \, {\left(2 \, \cos\left(6 \, x\right) - \sin\left(6 \, x\right)\right)} e^{\left(3 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(6 \, x\right) e^{\left(-9 \, x\right)}}\;dx} = \answer{{\frac{3}{13} \, {\left(3 \, \cos\left(6 \, x\right) - 2 \, \sin\left(6 \, x\right)\right)} e^{\left(-9 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-9 \, \cos\left(-6 \, x\right) e^{\left(4 \, x\right)}}\;dx} = \answer{{-\frac{9}{26} \, {\left(2 \, \cos\left(6 \, x\right) + 3 \, \sin\left(6 \, x\right)\right)} e^{\left(4 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{5 \, \cos\left(8 \, x\right) e^{\left(8 \, x\right)}}\;dx} = \answer{{\frac{5}{16} \, {\left(\cos\left(8 \, x\right) + \sin\left(8 \, x\right)\right)} e^{\left(8 \, x\right)}}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0023. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0023.HELP.tex}

\[
\int{{-7 \, e^{\left(7 \, x\right)} \sin\left(6 \, x\right)}\;dx} = \answer{{\frac{7}{85} \, {\left(6 \, \cos\left(6 \, x\right) - 7 \, \sin\left(6 \, x\right)\right)} e^{\left(7 \, x\right)}}+C}
\]
\end{problem}}%}

