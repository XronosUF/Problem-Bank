\ProblemFileHeader{XTL_SV_QUESTIONCOUNT}% Process how many problems are in this file and how to detect if it has a desirable problem
\ifproblemToFind% If it has a desirable problem search the file.
%%\tagged{Ans@ShortAns, Type@Compute, Topic@Integral, Sub@PartialFractions, Sub@LinearFactors, Func@Rational, File@0055}{
\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{78}{x + 9} - 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 1}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{21}{x + 5} - 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 2}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{61}{x - 9} + 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{46}{x - 8} - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x - 5}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{50}{x + 5} - 9 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 2\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{33}{x + 3} - 9 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, {\left(x + 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{80}{x - 9} + 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 4\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{24}{x - 8} - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{23}{x - 8} - 4 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 1}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{21}{x - 4} + 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 5}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{12}{x + 7} + \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 5}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{29}{x - 8} + 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 1}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{25}{x - 8} - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 1\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x + 5} - 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{47}{x + 6} + 7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 3}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{32}{x + 7} + 5 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x + 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x - 1} + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 9}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{17}{x - 1} - 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 2\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x - 4} + 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 7}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x + 3} - 3 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x - 7}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{15}{x + 1} + 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 5}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{1}{x + 2} - 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 9}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x + 5} - 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 4\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{14}{x - 3} + 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 9}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{6}{x + 3} + 5 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 7}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{3}{x - 2} + 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x - 1\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{52}{x + 6} + 8 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 2\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{24}{x + 8} + 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 7}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{88}{x + 9} + 9 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x + 3}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{17}{x + 5} + 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x - 7}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{49}{x + 7} + 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 5}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{3}{x + 1} + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 1\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{46}{x + 8} + 6 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 6}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{12}{x - 6} + \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 3}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x + 5} - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 1}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 1} - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 3}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{7}{x - 4} - \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x - 2\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{69}{x + 7} + 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x + 2\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{3}{x + 1} - 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x - 4}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{67}{x + 7} - 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x - 3}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{45}{x - 6} + 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 7}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 2} - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 9}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{21}{x - 3} - 4 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 2}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{25}{x + 9} + 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 1\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{26}{x - 6} + 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, x - 7}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 3} - 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 3}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{7}{x + 4} + \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 1}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{5}{x + 3} + 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x - 8}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{19}{x - 3} - 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 1}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{4}{x - 5} - \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 9}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 1} - 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 2}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{44}{x + 6} + 7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 4\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{38}{x - 5} + 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x - 3}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x - 1} + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, {\left(x + 1\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{32}{x - 3} + 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 4}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{68}{x + 8} - 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 2\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{24}{x + 6} + 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 4}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{14}{x + 2} + 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x + 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{56}{x - 9} + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 7}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x - 6} - \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 1\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{12}{x + 5} + 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 3}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{8}{x + 5} + \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x - 3\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{39}{x - 8} + 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 3\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{21}{x - 4} - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 1}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{33}{x + 8} - 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x - 1}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{11}{x + 5} + 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x - 5}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{77}{x + 8} - 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 5}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x + 3} - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 5}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{19}{x - 6} - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{42}{x - 6} - 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 8}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 1} + 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x + 5}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x - 1} + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x + 1}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{63}{x + 8} + 8 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 2}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{8}{x - 6} - \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{78}{x - 9} - 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 9}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{40}{x - 7} - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x + 6} - 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 3\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{30}{x + 9} + 4 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x + 1\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{60}{x - 7} - 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 7}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{47}{x - 5} - 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 1}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x + 9} - 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{38}{x + 4} + 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 1}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x + 5} - 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 4\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{28}{x - 6} - 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x - 5}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{21}{x + 2} + 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 2}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x + 4} - 5 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 3}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{52}{x - 7} - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 2\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x + 5} - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 5}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{40}{x + 5} + 7 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 9}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 4} - 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x - 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{40}{x + 7} - 5 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 2}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x - 5} + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 3\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{10}{x - 8} - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{56}{x - 9} - 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 3}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{10}{x + 7} + \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 8}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{33}{x - 5} + 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 2}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{52}{x + 6} + 9 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 9}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{5}{x - 7} - 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 5}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x + 2} - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 1}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{57}{x - 7} - 8 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 5}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{23}{x - 4} + 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x - 5}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 1} + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 8}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{89}{x - 9} + 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 9}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{23}{x + 2} - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 3\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{30}{x - 6} - 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{4}{x - 1} - 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 9}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{26}{x + 7} - 5 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 3\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{18}{x - 9} - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 4}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{14}{x - 2} + 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x - 3}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{35}{x + 8} + 4 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 7}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{37}{x + 6} + 5 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 4}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{9}{x - 1} - 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x - 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{5}{x - 1} + 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 3\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{27}{x + 3} - 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 4}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{25}{x - 7} + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 1\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{40}{x - 7} - 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 2}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{61}{x + 7} + 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 2\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{20}{x + 4} - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 1}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{55}{x + 7} - 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 3}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{17}{x - 4} - 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x + 5}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{27}{x + 4} + 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 3}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{39}{x - 6} - 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 6}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{1}{x + 7} + \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 7}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{13}{x - 4} - 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 1}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{14}{x + 5} + 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 1\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{26}{x + 6} - 4 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 5}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x + 2} - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{29}{x + 6} - 4 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 4}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{45}{x + 7} + 7 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 3}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{15}{x - 6} - 2 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 1}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{8}{x - 3} - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x - 9}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x - 9} + 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 1}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{34}{x - 7} + 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 7}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{33}{x + 5} - 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 3\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{20}{x - 7} - 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, x + 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{13}{x - 1} - 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 4}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{17}{x + 7} - 3 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 1}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x - 3} + 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x - 1\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{24}{x - 4} - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 3}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{53}{x + 8} + 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{60}{x - 9} - 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 8}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{38}{x + 6} - 5 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x - 1}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{25}{x + 6} + 4 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 2}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{12}{x - 2} - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 5}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{9}{x + 7} + 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 6} - 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{7}{x - 7} - 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 2}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{7}{x + 1} + 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x - 3}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{29}{x - 4} + 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{46}{x - 6} - 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 4}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{25}{x + 3} - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 8}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x - 8} + 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{16}{x + 7} + 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 7}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{8}{x + 1} - \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 4}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{32}{x - 4} - 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x - 3}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 1} + 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 3}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x + 2} + \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 4}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{46}{x - 6} + 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, x - 7}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{61}{x + 9} - 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x + 2\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{51}{x - 5} - 9 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 7}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{3}{x - 2} - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 8}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{15}{x + 1} + 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 1}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{26}{x - 5} - 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 8}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{10}{x - 6} + 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 3}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{32}{x + 5} - 7 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x - 1\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{51}{x - 6} + 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{12}{x + 7} - 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6}{x - 1}}\;dx} = \answer{{6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 8}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{27}{x + 5} - 7 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 3}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{31}{x + 4} - 7 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x + 2} - 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 8}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{12}{x - 4} - 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 9}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 8} + \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 1}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{21}{x - 5} - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 2}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x - 4} + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 2\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x + 6} - 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 9}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{1}{x + 1} - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(x + 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{40}{x - 9} + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(3 \, x - 2\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{66}{x - 8} - 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 3}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x + 2} - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 2}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x - 7} + 3 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 8}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 1} + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 1\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{28}{x + 6} + 4 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 8}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 3} - 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x - 9}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{41}{x + 4} - 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 5}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{47}{x - 6} + 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x - 1}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{49}{x + 6} - 8 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 1}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{7}{x - 2} - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 7}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x + 4} - 2 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x + 5}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{29}{x - 3} + 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, x + 5}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{53}{x - 8} - 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 3}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{5}{x - 4} - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 2\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x - 1} + 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 1}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x + 2} - 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 8}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{71}{x + 7} + 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 7}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{1}{x + 2} - 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 2}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{51}{x + 7} + 7 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 1\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{12}{x + 2} + 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 3}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x + 2} - 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x + 3\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{30}{x + 9} - 4 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, x - 1}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 2} - 6 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 4\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x + 2} + 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x + 3\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{38}{x - 4} - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 1}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 6} - 2 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 5}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{17}{x - 4} - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 8}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{5}{x + 3} + \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x + 2\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{16}{x - 2} - 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x + 1\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{40}{x - 4} - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{29}{x + 5} - 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 2}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{8}{x + 6} - \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{30}{x + 7} + 4 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 1\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{14}{x + 3} - 4 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 2}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{37}{x + 5} - 7 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x + 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{52}{x + 7} - 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 7}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{20}{x - 9} - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x - 7}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{23}{x + 8} + 2 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x - 1} + 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 1}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{62}{x - 9} - 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 2}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{79}{x + 9} - 9 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 5}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x + 2} - 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, {\left(x + 1\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{81}{x - 8} - 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 6}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{36}{x + 6} - 5 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 1}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{17}{x - 8} + 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 9}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{12}{x - 3} - \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 5}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{32}{x - 3} - 9 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 5}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{29}{x - 3} - 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 1}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{20}{x - 3} - 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x - 3}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{59}{x + 7} + 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x + 9}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{47}{x + 7} + 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{62}{x + 7} + 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x + 2\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{42}{x - 4} + 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 1}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{5}{x - 4} - \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x + 1}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{53}{x + 9} + 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, {\left(x - 1\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{42}{x + 6} + 6 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 3}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{37}{x - 8} + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 3}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{31}{x + 4} + 7 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{16}{x - 1} - 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 3}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x + 3} - 5 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x + 7}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{25}{x - 3} + 6 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 2}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{28}{x + 6} + 5 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 1}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{10}{x + 9} - \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 2}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{37}{x + 7} - 5 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 7}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{37}{x - 6} - 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 1\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{45}{x - 8} - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 1\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{39}{x + 6} - 6 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 5}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{51}{x - 8} + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 4\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{6}{x - 1} + 2 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(x + 3\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{18}{x + 9} + 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 2}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{27}{x + 5} - 5 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 1}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{3}{x - 2} - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{5}{x - 1} - 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 4}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{9}{x + 1} + 5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 4}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 3} - \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x - 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 1} - 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 1\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{26}{x - 3} + 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x + 1\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{69}{x + 8} + 9 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{1}{x - 4}}\;dx} = \answer{{-\log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 9}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x + 9} - \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 6}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x - 5} + 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{38}{x - 5} + 8 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 9}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{54}{x - 9} - 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, x - 5}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{25}{x - 5} - 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 8}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{55}{x + 9} - 7 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 4}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{23}{x + 9} + 3 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 3}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{19}{x - 8} - 2 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 4\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{26}{x - 9} - 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 2}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{26}{x + 4} - 7 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5}{x + 1}}\;dx} = \answer{{-5 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{1}{x - 2}}\;dx} = \answer{{-\log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x - 3}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x - 7} + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 2}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{8}{x - 2} - 5 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 7}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{3}{x - 4} + \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{21}{x + 7} - 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 7}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{55}{x - 6} - 8 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x + 1\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x + 1} - 8 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 1\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{21}{x - 6} - 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 9} - 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 9}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{19}{x + 2} + 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 3\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{39}{x + 8} + 6 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 3}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x + 8} - 2 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 3\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{12}{x - 9} - 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x - 3\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{27}{x - 6} + 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x - 9}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{25}{x + 8} + 2 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 1}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{15}{x - 2} + 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 4}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{36}{x + 8} - 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 2}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x + 1} + \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 9}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{65}{x + 8} + 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 9}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{23}{x + 2} + 7 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 4\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{44}{x - 6} - 6 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 1\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x + 9} - 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 5}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{8}{x + 3} + \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 8}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{35}{x + 3} + 9 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x + 1}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{15}{x + 4} + 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 1}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{41}{x + 8} + 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x + 1}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{37}{x - 9} + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 8}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{28}{x - 4} + 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x - 2\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{24}{x + 4} - 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 2}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x - 4} + 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x + 1}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{25}{x - 4} + 6 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 8}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{17}{x + 1} + 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 9}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x + 1} - 7 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{72}{x - 9} - 7 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 1}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{17}{x + 4} - 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 7}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{13}{x - 3} - 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 7}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{3}{x + 2} + 5 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 2}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{47}{x - 5} + 9 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x + 1\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{20}{x + 3} + 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x - 3} + 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 2\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{12}{x - 6} + 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 2}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{37}{x - 5} - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x - 3}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{27}{x + 6} + 4 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 5}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x + 4} - \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 6}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{50}{x - 8} + 7 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 7}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{22}{x + 3} - 5 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 2\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{9}{x + 1} + 3 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 4\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{46}{x - 9} + 6 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x + 7} - 3 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 2\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{10}{x - 7} + 2 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 3}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{38}{x + 7} - 5 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 2\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{18}{x + 4} + 3 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x - 3}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{35}{x + 4} + 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 4\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{62}{x + 9} + 6 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 5}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{10}{x - 5} - \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x - 5}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x - 9} + 2 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3}{x - 3}}\;dx} = \answer{{-3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x + 1\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{42}{x + 8} - 6 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 2}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 1} - 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 3}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{32}{x - 7} + 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 3}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{32}{x - 7} - 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 6}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x - 3} + \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x - 2\right)}}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{22}{x + 3} - 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x - 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{3}{x - 1} + 4 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, {\left(x - 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{72}{x - 9} + 9 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 5}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{51}{x + 8} - 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{6 \, {\left(x - 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{48}{x + 7} - 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, {\left(x - 1\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{28}{x - 5} + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 3}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x + 1} + 2 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 2}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{38}{x + 8} + 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x + 1}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{47}{x + 8} + 6 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{33}{x + 6} - 7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 7}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x - 5} + 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x - 1\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{40}{x + 4} - 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 1\right)}}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{6}{x + 2} + 4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{2}{x - 1} + 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 4}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 1} - 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 2}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x - 9} + \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 3}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{24}{x - 3} + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 2}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{42}{x - 8} + 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(4 \, x - 3\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{26}{x - 4} - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 8}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{36}{x - 4} + 7 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x - 2}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x + 1} - 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x + 7}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{9}{x + 2} + 8 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 9}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{9}{x + 9} + 2 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x - 9}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x - 4} + 5 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{26}{x + 5} - 7 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x + 4} - 7 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x + 3}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{31}{x - 7} + 4 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x + 9}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{81}{x - 9} + 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(3 \, x - 1\right)}}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x - 1} + 6 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{31}{x - 5} - 5 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x - 1} - 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 6}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x - 1} + 7 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 2}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{25}{x - 9} - 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 4}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{10}{x - 2} + 3 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4}{x + 2}}\;dx} = \answer{{4 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 2}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{16}{x - 6} - 3 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 4}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{49}{x - 5} - 9 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x + 1\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{27}{x - 4} - 6 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 2}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{65}{x + 7} + 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 7}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{13}{x - 5} - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x + 1}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{23}{x + 3} + 8 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x - 1}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{31}{x - 4} - 8 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x + 1\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{21}{x - 2} - 7 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 1}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{8}{x + 9} - \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 2}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{54}{x + 8} - 7 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 6}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x - 7} + \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{24}{x - 9} + 3 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 4}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{36}{x + 8} + 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(x - 3\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x - 5} + 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 1}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{62}{x + 7} - 9 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 5}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{14}{x + 9} - \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{1}{x + 5}}\;dx} = \answer{{\log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 7}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x + 4} - 4 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{2}{x - 1} - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(2 \, x - 1\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{76}{x + 9} + 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{1}{x - 4}}\;dx} = \answer{{\log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 4}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{44}{x - 8} - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 1}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{36}{x - 5} + 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x - 1\right)}}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{70}{x - 9} + 8 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x - 1\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{27}{x - 5} + 6 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 2\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{30}{x - 8} - 3 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 1}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{48}{x - 7} - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 9}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{-\frac{49}{x + 8} - 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 5}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{14}{x - 1} + 9 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 9}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x + 5} - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, {\left(x - 1\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{80}{x + 9} - 8 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 5}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{2}{x - 7} - \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x + 7}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{-\frac{43}{x - 9} + 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 5}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{12}{x + 7} - \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 7}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{22}{x + 5} + 3 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 9}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{5}{x + 7} + 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 8}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x - 1} + \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 4\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{2}{x + 5} - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x - 3}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{24}{x + 3} - 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 7}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{49}{x + 7} - 8 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 7}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{\frac{2}{x + 1} + 9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, {\left(x + 2\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{12}{x + 5} - 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 1}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{22}{x + 7} + 3 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 2}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{32}{x - 6} + 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x - 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{16}{x + 7} - 2 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 7}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x + 3} + \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 1}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x + 3} - 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(3 \, x - 2\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{60}{x + 6} + 9 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 4}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{67}{x + 9} + 7 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 1}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{11}{x + 6} - 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x + 6} - 3 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x + 2}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{5}{x - 1} + 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9}{x + 1}}\;dx} = \answer{{-9 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x - 2}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{10}{x - 4} - 3 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x - 7}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{31}{x + 4} + 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 4}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 1} - 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 2\right)}}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{10}{x - 3} + 2 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x + 3}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{23}{x - 5} - 4 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 7}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{22}{x - 5} - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 1}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{\frac{17}{x + 2} + 9 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{\frac{1}{x - 4} - 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, x - 9}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{27}{x - 9} - 4 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x - 7}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{43}{x + 4} - 9 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 7}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{47}{x - 8} - 5 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 5}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{2}{x - 1} + 3 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{59}{x + 6} + 9 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 3}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{38}{x - 7} + 5 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, x + 1}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{5}{x + 2} - 3 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, {\left(x - 1\right)}}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{45}{x + 8} + 5 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 7}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x - 1} - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 2}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{19}{x - 3} + 7 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x + 1\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{18}{x - 5} - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 5}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{7}{x + 6} + 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(x - 1\right)}}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{12}{x - 5} - 3 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 5}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x + 2} + 2 \, \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(x + 4\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x + 6} - 2 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 3\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{-\frac{33}{x + 4} - 6 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 3\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{21}{x + 5} + 6 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 4}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{17}{x + 3} + 7 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(2 \, x - 3\right)}}{{\left(x + 1\right)}^{2}}}\;dx} = \answer{{-\frac{10}{x + 1} - 4 \, \log\left(x + 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, x - 1}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{7}{x - 2} + 4 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 8}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{50}{x - 6} + 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x + 1}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{\frac{41}{x + 7} + 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 1\right)}}{{\left(x + 7\right)}^{2}}}\;dx} = \answer{{-\frac{45}{x + 7} - 6 \, \log\left(x + 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, {\left(x - 1\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{90}{x + 9} + 9 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 8}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{46}{x - 6} + 9 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 1}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{-\frac{73}{x - 8} + 9 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x - 4}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{59}{x - 7} + 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 4}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{28}{x + 8} + 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, {\left(x + 1\right)}}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{-\frac{40}{x + 9} - 5 \, \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, {\left(2 \, x + 1\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{33}{x + 6} + 6 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x - 1}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{-\frac{48}{x - 7} + 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 1}{{\left(x - 9\right)}^{2}}}\;dx} = \answer{{\frac{46}{x - 9} - 5 \, \log\left(x - 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x - 3}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{43}{x + 5} - 8 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{8 \, x - 5}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{3}{x - 1} + 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, {\left(x + 1\right)}}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{35}{x + 6} + 7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x + 3\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{14}{x - 4} + 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 5}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{1}{x - 2} - 2 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 7}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{-\frac{5}{x - 2} - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x - 9}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{-\frac{8}{x - 1} - \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{11}{x - 1} - 5 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 6}{{\left(x + 2\right)}^{2}}}\;dx} = \answer{{-\frac{4}{x + 2} + \log\left(x + 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{9 \, x + 1}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{37}{x - 4} + 9 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, {\left(3 \, x + 1\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{50}{x - 8} - 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x - 7}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{-\frac{13}{x + 3} - 2 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x - 2}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{13}{x - 3} - 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 6}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{21}{x - 3} - 5 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x + 7}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{29}{x + 6} + 6 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 1}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{5}{x + 4} + \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x + 3\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{14}{x + 5} + 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{9 \, x + 1}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{64}{x - 7} - 9 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, x + 5}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{-\frac{15}{x - 5} + 2 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{3 \, {\left(2 \, x - 1\right)}}{{\left(x - 8\right)}^{2}}}\;dx} = \answer{{\frac{45}{x - 8} - 6 \, \log\left(x - 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{6 \, x + 5}{{\left(x + 3\right)}^{2}}}\;dx} = \answer{{\frac{13}{x + 3} + 6 \, \log\left(x + 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x - 1}{{\left(x - 1\right)}^{2}}}\;dx} = \answer{{\frac{7}{x - 1} - 8 \, \log\left(x - 1\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{x + 7}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{9}{x - 2} - \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 2}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{8}{x - 6} + \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 5}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{3}{x - 2} + \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{5 \, x + 2}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{32}{x - 6} - 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(x - 1\right)}}{{\left(x - 4\right)}^{2}}}\;dx} = \answer{{-\frac{6}{x - 4} + 2 \, \log\left(x - 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 8}{{\left(x + 8\right)}^{2}}}\;dx} = \answer{{\frac{32}{x + 8} + 3 \, \log\left(x + 8\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{3 \, x - 7}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{-\frac{2}{x - 3} + 3 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 3}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{\frac{45}{x - 6} - 7 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(2 \, x - 3\right)}}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{18}{x - 6} + 4 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{5 \, x + 7}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{37}{x - 6} + 5 \, \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{4 \, {\left(2 \, x - 1\right)}}{{\left(x - 2\right)}^{2}}}\;dx} = \answer{{\frac{12}{x - 2} - 8 \, \log\left(x - 2\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{4 \, {\left(x - 2\right)}}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{\frac{28}{x + 5} + 4 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x + 3}{{\left(x - 6\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x - 6} + \log\left(x - 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, {\left(x - 1\right)}}{{\left(x - 7\right)}^{2}}}\;dx} = \answer{{\frac{42}{x - 7} - 7 \, \log\left(x - 7\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{8 \, x + 3}{{\left(x - 3\right)}^{2}}}\;dx} = \answer{{\frac{27}{x - 3} - 8 \, \log\left(x - 3\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{x - 2}{{\left(x + 9\right)}^{2}}}\;dx} = \answer{{\frac{11}{x + 9} + \log\left(x + 9\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{2 \, {\left(4 \, x + 3\right)}}{{\left(x + 4\right)}^{2}}}\;dx} = \answer{{\frac{26}{x + 4} + 8 \, \log\left(x + 4\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{7 \, x + 9}{{\left(x - 5\right)}^{2}}}\;dx} = \answer{{\frac{44}{x - 5} - 7 \, \log\left(x - 5\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{\frac{7 \, x + 6}{{\left(x + 6\right)}^{2}}}\;dx} = \answer{{\frac{36}{x + 6} + 7 \, \log\left(x + 6\right)}+C}
\]
\end{problem}}%}

\latexProblemContent{
\ifVerboseLocation This is Integration Compute Question 0055. \\ \fi
\begin{problem}

Compute the following integral:

\input{Integral-Compute-0055.HELP.tex}

\[
\int{{-\frac{2 \, x + 1}{{\left(x + 5\right)}^{2}}}\;dx} = \answer{{-\frac{9}{x + 5} - 2 \, \log\left(x + 5\right)}+C}
\]
\end{problem}}%}

