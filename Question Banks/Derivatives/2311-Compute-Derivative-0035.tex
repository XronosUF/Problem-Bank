%%%%%%%%%%%%%%%%%%%%%%
%%\tagged{Cat@One, Cat@Two, Cat@Three, Cat@Four, Cat@Five, Ans@ShortAns, Type@Compute, Topic@Derivative, Sub@Differential}{

\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \log\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \cos\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sqrt{x} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{4} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \cos\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \cos\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{2} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sin\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{2} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \cos\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{3} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{4} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \log\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \cos\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-e^{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{2} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sin\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sin\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sqrt{x} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{4} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\sqrt{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \cos\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{2} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sin\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \sin\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{3} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-15 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{4} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{4} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{4} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \log\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \log\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{4}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sqrt{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{4} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\sqrt{x} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sqrt{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, e^{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{2} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{2} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sin\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{3} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\sqrt{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \cos\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{2} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \cos\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \log\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\sin\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{3} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \sqrt{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sqrt{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \sqrt{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sqrt{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \log\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{4}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, e^{x} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sqrt{x} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \log\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sqrt{x} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sin\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \log\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{4}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \sin\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \cos\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sqrt{x} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\cos\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\sqrt{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \cos\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \cos\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \cos\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, e^{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{2} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, e^{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \log\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{4} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sqrt{x} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{3} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sqrt{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sin\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{2} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sqrt{x} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{4} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sqrt{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, e^{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\sin\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{3} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, e^{x} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \sin\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{4} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, e^{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\sqrt{x} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\log\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, e^{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{2} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\cos\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{2} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{3} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{4} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{4} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{3} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\sin\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \log\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{5}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \cos\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sin\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{3} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{4} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{4} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{3} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \cos\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{4} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{2} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \sin\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{4} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sin\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\log\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \sqrt{x} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{3} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \cos\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{15 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{3} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{3} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, e^{x} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\log\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{3} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \log\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{2} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \log\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\log\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \cos\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \log\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{4}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{2} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \log\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \cos\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{4} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \cos\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{2} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sin\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{2} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, e^{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\cos\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, e^{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{2} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, e^{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \sin\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sin\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{2} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{4} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \cos\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \log\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{2} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sin\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{3} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{3} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-15 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, e^{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{2} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \cos\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{4} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{4} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\cos\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{4} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\log\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{2} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \sqrt{x} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \log\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \sin\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{2} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{2} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{2} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, e^{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \cos\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{4} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{4} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sqrt{x} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{4} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \log\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={e^{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{3} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{15 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sin\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \cos\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{3} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{15 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \log\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \log\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\cos\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \cos\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{2} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \sqrt{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{5}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \sqrt{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{5}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sin\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \log\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, e^{x} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{3} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{4} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{2} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sin\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \cos\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{4} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \log\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\sin\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{3} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\log\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \log\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \log\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, e^{x} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{2} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \cos\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, e^{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sin\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sqrt{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{4} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\sin\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \log\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{2} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{2} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \cos\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \sin\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{4} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{2} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\sin\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{4} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{4} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sqrt{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{3}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, e^{x} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \log\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{3} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sin\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, e^{x} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\log\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \log\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \log\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \sin\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{4} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{2} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \cos\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\log\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{2} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{2} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{4} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \log\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sqrt{x} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \log\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{4}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \sqrt{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \cos\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{4} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, e^{x} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{3} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \cos\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, e^{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{4} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \log\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{2} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{4} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \cos\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sin\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \log\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{3} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \cos\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \log\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{3} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{2} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{4} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \cos\left(x\right) - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sin\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={\log\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sin\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, x^{4} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{1}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, e^{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={x^{3} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{2} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{4} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{3} + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{2} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sqrt{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{1}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, x^{2} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{10 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{3} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \sin\left(x\right) - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\sqrt{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \sin\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{3} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \log\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{2}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{4} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{3} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{3} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{3} - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{3} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-15 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \sin\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-5 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sin\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sin\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \log\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, e^{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \cos\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \sin\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{4} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-20 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, \sqrt{x} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{3}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{2} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sin\left(x\right) - 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{3} - 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, e^{x} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{4} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{8 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, x^{2} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, x^{2} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \log\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \sqrt{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \sin\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{4} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-12 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-\sqrt{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{1}{2 \, \sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \cos\left(x\right) + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, x^{3} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-15 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={3 \, x^{2} - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{6 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-2 \, \sin\left(x\right) - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, \cos\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, e^{x} + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \cos\left(x\right) + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-e^{x} - 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={4 \, \cos\left(x\right) - 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-4 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, e^{x} + 4}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={5 \, \log\left(x\right) + 1}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{\frac{5}{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, \sqrt{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-\frac{2}{\sqrt{x}}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, x^{3} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-9 \, x^{2}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-4 \, x^{4} - 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-16 \, x^{3}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-x^{2} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-2 \, x}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, e^{x} + 3}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-5 \, \cos\left(x\right) + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{5 \, \sin\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={2 \, e^{x} + 2}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{2 \, e^{x}}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the differential of the function $y={-3 \, \sin\left(x\right) + 5}$.

\input{2311-Compute-Derivative-0035.HELP.tex}

\[dy=\answer{{-3 \, \cos\left(x\right)}\,dx}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%

