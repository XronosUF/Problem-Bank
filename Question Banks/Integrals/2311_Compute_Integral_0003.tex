%%%%%%%%%%%%%%%%%%%%%%%
%%\tagged{Cat@One, Cat@Two, Cat@Three, Cat@Four, Cat@Five, Ans@ShortAns, Type@Compute, Topic@Integral, Sub@Poly, Sub@Riemann}{

\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 5}}$ from $x={5}$ to $x={11}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={3}$ to $x={10}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{91} + \sqrt{51} + 2 \, \sqrt{21} + \sqrt{19} + 5 \, \sqrt{3} + 14}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 64}}$ from $x={8}$ to $x={15}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{161} + \sqrt{105} + \sqrt{57} + 2 \, \sqrt{33} + \sqrt{17} + 4 \, \sqrt{5} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={9}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + \sqrt{11} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 8}}$ from $x={8}$ to $x={14}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 8}}$ from $x={2}$ to $x={8}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 4}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + \sqrt{5} + 2 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={9}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 36}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{85} + \sqrt{13} + 2 \, \sqrt{7} + 3 \, \sqrt{5} + 8}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 6}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={10}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={8}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 12}}$ from $x={8}$ to $x={12}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={5}$ to $x={9}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{17} + 2 \, \sqrt{14} + 3 \, \sqrt{5} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={7}$ to $x={11}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{57} + \sqrt{21} + 2 \, \sqrt{10}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 49}}$ from $x={7}$ to $x={11}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{15} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 64}}$ from $x={8}$ to $x={11}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{17} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 5}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 12}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 11}}$ from $x={6}$ to $x={11}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 6}}$ from $x={6}$ to $x={10}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={6}$ to $x={9}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{17} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={12}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + 2 \, \sqrt{14} + \sqrt{11} + 6 \, \sqrt{6} + 5 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 8}}$ from $x={8}$ to $x={14}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 6}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 49}}$ from $x={7}$ to $x={12}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{15} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 64}}$ from $x={8}$ to $x={11}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{57} + \sqrt{17} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={11}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 11}}$ from $x={5}$ to $x={11}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 49}}$ from $x={4}$ to $x={7}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{13} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 64}}$ from $x={1}$ to $x={8}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{55} + \sqrt{39} + 3 \, \sqrt{15} + 5 \, \sqrt{7} + 4 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 1}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={8}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + \sqrt{11} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 11}}$ from $x={4}$ to $x={11}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 8}}$ from $x={4}$ to $x={8}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={6}$ to $x={9}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{15} + \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 4}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + \sqrt{5} + 2 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 8}}$ from $x={8}$ to $x={11}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 16}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{15} + \sqrt{7} + 2 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 12}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 169}}$ from $x={6}$ to $x={13}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{105} + \sqrt{69} + 2 \, \sqrt{30} + 2 \, \sqrt{22} + 4 \, \sqrt{3} + 5}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 16}}$ from $x={4}$ to $x={11}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{65} + \sqrt{33} + 2 \, \sqrt{21} + 2 \, \sqrt{5} + 4 \, \sqrt{3} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 36}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{85} + \sqrt{13} + 2 \, \sqrt{7} + 3 \, \sqrt{5} + 6 \, \sqrt{3} + 8}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 5}}$ from $x={5}$ to $x={12}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 64}}$ from $x={1}$ to $x={8}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{55} + \sqrt{39} + 3 \, \sqrt{15} + 2 \, \sqrt{7} + 4 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={5}$ to $x={10}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{19} + 5 \, \sqrt{3} + 14}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 144}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{95} + \sqrt{23} + 2 \, \sqrt{11} + 3 \, \sqrt{7} + 4 \, \sqrt{5} + 6 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={13}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 7}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 49}}$ from $x={7}$ to $x={10}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{15} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={4}$ to $x={9}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 64}}$ from $x={8}$ to $x={14}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{105} + \sqrt{57} + \sqrt{17} + 4 \, \sqrt{5} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 12}}$ from $x={5}$ to $x={12}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={12}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 49}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{33} + \sqrt{13} + 2 \, \sqrt{10} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 144}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{95} + \sqrt{23} + 2 \, \sqrt{11} + 3 \, \sqrt{7} + 4 \, \sqrt{5}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 10}}$ from $x={3}$ to $x={10}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 8}}$ from $x={8}$ to $x={11}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{15} + \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={6}$ to $x={9}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 49}}$ from $x={7}$ to $x={14}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{95} + \sqrt{51} + 2 \, \sqrt{30} + \sqrt{15} + 7 \, \sqrt{3} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 64}}$ from $x={4}$ to $x={8}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + \sqrt{15} + 2 \, \sqrt{7} + 4 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 13}}$ from $x={6}$ to $x={13}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 196}}$ from $x={8}$ to $x={14}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{115} + 2 \, \sqrt{33} + 2 \, \sqrt{13} + 4 \, \sqrt{6} + 8 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 6}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 49}}$ from $x={7}$ to $x={12}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{95} + \sqrt{51} + \sqrt{15} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 7}}$ from $x={1}$ to $x={7}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 8}}$ from $x={4}$ to $x={8}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 144}}$ from $x={7}$ to $x={12}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{95} + \sqrt{23} + 2 \, \sqrt{11} + 3 \, \sqrt{7} + 4 \, \sqrt{5}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 36}}$ from $x={6}$ to $x={11}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{13} + 2 \, \sqrt{7} + 3 \, \sqrt{5} + 8}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={12}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 5}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={9}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 25}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + 2 \, \sqrt{6} + 7}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={6}$ to $x={11}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{85} + \sqrt{57} + \sqrt{21} + 2 \, \sqrt{10} + 6 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={3}$ to $x={9}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{77} + \sqrt{65} + \sqrt{17} + 2 \, \sqrt{14} + 3 \, \sqrt{5} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={9}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={12}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{119} + \sqrt{39} + 2 \, \sqrt{14} + \sqrt{11} + 6 \, \sqrt{6} + 5 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={8}$ to $x={11}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + 2 \, \sqrt{10}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 36}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{11} + 2 \, \sqrt{5} + 3 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={4}$ to $x={9}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 4}}$ from $x={2}$ to $x={8}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + 4 \, \sqrt{5} + 2 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 16}}$ from $x={4}$ to $x={8}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{33} + 2 \, \sqrt{5} + 4 \, \sqrt{3} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 49}}$ from $x={4}$ to $x={7}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{33} + \sqrt{13} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={8}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{35} + \sqrt{15} + 2 \, \sqrt{6} + 5 \, \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 8}}$ from $x={8}$ to $x={12}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={8}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{35} + \sqrt{15} + 3 \, \sqrt{7} + 2 \, \sqrt{6} + 5 \, \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={11}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + 2 \, \sqrt{14} + \sqrt{11} + 2 \, \sqrt{6} + 5 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 64}}$ from $x={5}$ to $x={8}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + \sqrt{15} + 2 \, \sqrt{7}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={4}$ to $x={10}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + 2 \, \sqrt{21} + \sqrt{19} + 5 \, \sqrt{3} + 14}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={14}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 4}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 12}}$ from $x={8}$ to $x={12}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 16}}$ from $x={4}$ to $x={9}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{33} + 2 \, \sqrt{5} + 4 \, \sqrt{3} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={7}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={5}$ to $x={10}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{19} + 14}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={7}$ to $x={10}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{19} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 9}}$ from $x={3}$ to $x={10}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{55} + 2 \, \sqrt{10} + \sqrt{7} + 3 \, \sqrt{3} + 6 \, \sqrt{2} + 4}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={5}$ to $x={9}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={8}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 8}}$ from $x={8}$ to $x={15}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={10}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 14}}$ from $x={8}$ to $x={14}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 169}}$ from $x={8}$ to $x={13}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{69} + 2 \, \sqrt{22} + 4 \, \sqrt{3} + 5}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 11}}$ from $x={5}$ to $x={11}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 36}}$ from $x={6}$ to $x={9}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{13} + 2 \, \sqrt{7}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 13}}$ from $x={8}$ to $x={13}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={4}$ to $x={9}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{17} + 2 \, \sqrt{14} + 3 \, \sqrt{5} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={6}$ to $x={10}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{19} + 14}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 16}}$ from $x={4}$ to $x={9}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{65} + \sqrt{33} + 2 \, \sqrt{5} + 4 \, \sqrt{3} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={3}$ to $x={9}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{65} + \sqrt{17} + 2 \, \sqrt{14} + 3 \, \sqrt{5} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 6}}$ from $x={6}$ to $x={11}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 5}}$ from $x={5}$ to $x={8}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 169}}$ from $x={6}$ to $x={13}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{133} + \sqrt{105} + \sqrt{69} + 2 \, \sqrt{30} + 2 \, \sqrt{22} + 4 \, \sqrt{3} + 5}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={10}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + 2 \, \sqrt{14} + \sqrt{11} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 36}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{11} + 2 \, \sqrt{5} + 3 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 16}}$ from $x={4}$ to $x={7}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{2 \, \sqrt{5} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={7}$ to $x={11}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{57} + \sqrt{21} + 2 \, \sqrt{10} + 6 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{65} + \sqrt{17} + 2 \, \sqrt{14} + 3 \, \sqrt{5} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={7}$ to $x={10}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{19} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 49}}$ from $x={7}$ to $x={10}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{15} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 8}}$ from $x={8}$ to $x={13}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%



%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 64}}$ from $x={8}$ to $x={12}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{57} + \sqrt{17} + 4 \, \sqrt{5} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 16}}$ from $x={4}$ to $x={10}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{65} + \sqrt{33} + 2 \, \sqrt{5} + 4 \, \sqrt{3} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 8}}$ from $x={1}$ to $x={8}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={11}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 25}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + 7}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={10}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 36}}$ from $x={6}$ to $x={9}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{13} + 2 \, \sqrt{7} + 3 \, \sqrt{5}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={10}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 8}}$ from $x={5}$ to $x={8}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 64}}$ from $x={8}$ to $x={12}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{57} + \sqrt{17} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 7}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={5}$ to $x={11}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{85} + \sqrt{57} + \sqrt{21} + 2 \, \sqrt{10} + 4 \, \sqrt{6} + 6 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 25}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + 7}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 9}}$ from $x={3}$ to $x={9}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 13}}$ from $x={6}$ to $x={13}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 64}}$ from $x={2}$ to $x={8}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{55} + \sqrt{39} + \sqrt{15} + 2 \, \sqrt{7} + 4 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 144}}$ from $x={5}$ to $x={12}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{119} + \sqrt{95} + \sqrt{23} + 2 \, \sqrt{11} + 3 \, \sqrt{7} + 4 \, \sqrt{5} + 6 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 11}}$ from $x={8}$ to $x={11}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 64}}$ from $x={8}$ to $x={13}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{57} + \sqrt{17} + 4 \, \sqrt{5} + 6}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 16}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + 2 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 9}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + 3 \, \sqrt{3} + 4}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={14}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 10}}$ from $x={6}$ to $x={10}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 36}}$ from $x={6}$ to $x={13}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{133} + \sqrt{85} + \sqrt{13} + 2 \, \sqrt{7} + 3 \, \sqrt{5} + 6 \, \sqrt{3} + 8}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={9}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 36}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{35} + \sqrt{11} + 2 \, \sqrt{5} + 3 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 7}}$ from $x={7}$ to $x={13}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{35} + \sqrt{15} + 2 \, \sqrt{6} + \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 225}}$ from $x={8}$ to $x={15}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{29} + 2 \, \sqrt{26} + 2 \, \sqrt{14} + 5 \, \sqrt{5} + 21}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={7}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={7}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{35} + \sqrt{15} + 2 \, \sqrt{6} + \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={4}$ to $x={11}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{105} + \sqrt{85} + \sqrt{57} + \sqrt{21} + 2 \, \sqrt{10} + 4 \, \sqrt{6} + 6 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 4}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + \sqrt{5} + 2 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={5}$ to $x={11}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{85} + \sqrt{57} + \sqrt{21} + 2 \, \sqrt{10} + 6 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 4}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 10}}$ from $x={4}$ to $x={10}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 10}}$ from $x={4}$ to $x={10}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 14}}$ from $x={8}$ to $x={14}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 10}}$ from $x={6}$ to $x={10}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={4}$ to $x={10}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + \sqrt{19} + 5 \, \sqrt{3} + 14}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 64}}$ from $x={4}$ to $x={8}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + \sqrt{15} + 2 \, \sqrt{7}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 169}}$ from $x={7}$ to $x={13}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{105} + \sqrt{69} + 2 \, \sqrt{30} + 2 \, \sqrt{22} + 4 \, \sqrt{3} + 5}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 6}}$ from $x={6}$ to $x={12}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 64}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{55} + \sqrt{39} + \sqrt{15} + 2 \, \sqrt{7} + 4 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 121}}$ from $x={6}$ to $x={11}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{57} + \sqrt{21} + 2 \, \sqrt{10} + 6 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 4}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{77} + \sqrt{21} + 2 \, \sqrt{15} + 4 \, \sqrt{5} + 2 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 49}}$ from $x={7}$ to $x={13}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{95} + \sqrt{51} + \sqrt{15} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 13}}$ from $x={7}$ to $x={13}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 9}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{2 \, \sqrt{10} + \sqrt{7} + 3 \, \sqrt{3} + 4}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 7}}$ from $x={1}$ to $x={7}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 16}}$ from $x={4}$ to $x={7}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{33} + 2 \, \sqrt{5} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 225}}$ from $x={8}$ to $x={15}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{161} + \sqrt{29} + 2 \, \sqrt{26} + 2 \, \sqrt{14} + 5 \, \sqrt{5} + 21}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 6}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 100}}$ from $x={3}$ to $x={10}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{51} + 2 \, \sqrt{21} + \sqrt{19} + 5 \, \sqrt{3} + 14}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 8}}$ from $x={1}$ to $x={8}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 10}}$ from $x={5}$ to $x={10}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 5}}$ from $x={5}$ to $x={9}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={3}$ to $x={9}$ using ${6}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{65} + \sqrt{17} + 2 \, \sqrt{14} + 3 \, \sqrt{5} + 10 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 15}}$ from $x={8}$ to $x={15}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 7}}$ from $x={4}$ to $x={7}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 1}}$ from $x={1}$ to $x={7}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 36}}$ from $x={6}$ to $x={13}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{85} + \sqrt{13} + 2 \, \sqrt{7} + 3 \, \sqrt{5} + 6 \, \sqrt{3} + 8}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 4}}$ from $x={4}$ to $x={11}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={10}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + 2 \, \sqrt{14} + \sqrt{11} + 2 \, \sqrt{6} + 5 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%



\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 1}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 196}}$ from $x={8}$ to $x={14}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{115} + 2 \, \sqrt{13} + 4 \, \sqrt{6} + 8 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 4}}$ from $x={2}$ to $x={5}$ using ${3}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + \sqrt{5} + 2 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 144}}$ from $x={7}$ to $x={12}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{23} + 2 \, \sqrt{11} + 3 \, \sqrt{7} + 4 \, \sqrt{5}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={7}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{35} + \sqrt{15} + 2 \, \sqrt{6} + 5 \, \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 1}}$ from $x={1}$ to $x={8}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 144}}$ from $x={8}$ to $x={12}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{23} + 2 \, \sqrt{11} + 3 \, \sqrt{7}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%



%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 9}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + 4}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 12}}$ from $x={7}$ to $x={12}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 49}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{33} + \sqrt{13} + 2 \, \sqrt{10} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 7}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 9}}$ from $x={3}$ to $x={10}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{91} + \sqrt{55} + 2 \, \sqrt{10} + \sqrt{7} + 3 \, \sqrt{3} + 6 \, \sqrt{2} + 4}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 6}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 8}}$ from $x={3}$ to $x={8}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 36}}$ from $x={1}$ to $x={6}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{11} + 2 \, \sqrt{5} + 3 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 81}}$ from $x={5}$ to $x={9}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{17} + 3 \, \sqrt{5} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 5}}$ from $x={5}$ to $x={8}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={7}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 9}}$ from $x={3}$ to $x={9}$ using ${6}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{55} + 2 \, \sqrt{10} + \sqrt{7} + 3 \, \sqrt{3} + 6 \, \sqrt{2} + 4}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x^{2} + 196}}$ from $x={7}$ to $x={14}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{115} + 2 \, \sqrt{33} + 2 \, \sqrt{13} + 4 \, \sqrt{6} + 8 \, \sqrt{3}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 1}}$ from $x={1}$ to $x={4}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 6}}$ from $x={6}$ to $x={11}$ using ${5}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 11}}$ from $x={7}$ to $x={11}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={6}$ using ${4}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{-x + 14}}$ from $x={7}$ to $x={14}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 2}}$ from $x={2}$ to $x={7}$ using ${5}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 3}}$ from $x={3}$ to $x={6}$ using ${3}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{2} + 1}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x - 6}}$ from $x={6}$ to $x={13}$ using ${7}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{7} + \sqrt{6} + \sqrt{5} + \sqrt{3} + \sqrt{2} + 3}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 4}}$ from $x={2}$ to $x={9}$ using ${7}$ rectangles and ${\text{\texttt{left}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{21} + 2 \, \sqrt{15} + 4 \, \sqrt{5} + 2 \, \sqrt{3} + 4 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 1}}$ from $x={1}$ to $x={5}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{15} + 2 \, \sqrt{6} + \sqrt{3} + 2 \, \sqrt{2}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Estimate the area under the graph of $f(x)={\sqrt{x^{2} - 25}}$ from $x={5}$ to $x={9}$ using ${4}$ rectangles and ${\text{\texttt{right}}}$ endpoints.

\input{2311_Compute_Integral_0003.HELP.tex}

\[\mbox{Area}\approx\answer{\sqrt{39} + 2 \, \sqrt{14} + \sqrt{11} + 2 \, \sqrt{6}}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


