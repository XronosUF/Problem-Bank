%%%%%%%%%%%%%%%%%%%%%%%
%%\tagged{Cat@One, Cat@Two, Cat@Three, Cat@Four, Cat@Five, Ans@ShortAns, Type@Compute, Topic@Integral, Sub@Indefinite, Sub@Sub_u}{

\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-{\left(e^{x} + 6\right)}^{4} e^{x}}\;dx = \answer{{-\frac{1}{5} \, {\left(e^{x} + 6\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{36 \, x^{3}}{{\left(x^{4} + 10\right)}^{2}}}\;dx = \answer{{-\frac{9}{x^{4} + 10}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, e^{\left(x + e^{x} - 3\right)}}\;dx = \answer{{9 \, e^{\left(e^{x} - 3\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8 \, x}{{\left(x^{2} - 6\right)}^{3}}}\;dx = \answer{{-\frac{2}{{\left(x^{2} - 6\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{21 \, {\left(\frac{1}{x^{3}} - 1\right)}^{4}}{x^{4}}}\;dx = \answer{{-\frac{7}{5} \, {\left(\frac{1}{x^{3}} - 1\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9 \, {\left(\frac{1}{x} - 2\right)}^{4}}{x^{2}}}\;dx = \answer{{-\frac{9}{5} \, {\left(\frac{1}{x} - 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, \cos\left(\sqrt{x} + 2\right)}{2 \, \sqrt{x}}}\;dx = \answer{{-9 \, \sin\left(\sqrt{x} + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{21 \, {\left(x^{3} + 5\right)}^{3} x^{2}}\;dx = \answer{{\frac{7}{4} \, {\left(x^{3} + 5\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, {\left(\frac{1}{x} + 12\right)}}{x^{2}}}\;dx = \answer{{-{\left(\frac{1}{x} + 12\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, \cos\left(x + 14\right)}\;dx = \answer{{5 \, \sin\left(x + 14\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{16 \, x \cos\left(x^{2} - 9\right)}\;dx = \answer{{8 \, \sin\left(x^{2} - 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, {\left(\sqrt{x} + 10\right)}^{4}}{2 \, \sqrt{x}}}\;dx = \answer{{-\frac{9}{5} \, {\left(\sqrt{x} + 10\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, x \cos\left(x^{2} + 2\right)}\;dx = \answer{{-3 \, \sin\left(x^{2} + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{30 \, {\left(\frac{1}{x^{3}} + 2\right)}^{4}}{x^{4}}}\;dx = \answer{{2 \, {\left(\frac{1}{x^{3}} + 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, x \cos\left(x^{2} + 9\right)}\;dx = \answer{{-2 \, \sin\left(x^{2} + 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, {\left(\sqrt{x} + 10\right)}^{4}}{2 \, \sqrt{x}}}\;dx = \answer{{{\left(\sqrt{x} + 10\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{e^{x}}{{\left(e^{x} - 10\right)}^{3}}}\;dx = \answer{{\frac{1}{2 \, {\left(e^{x} - 10\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8}{x^{2} {\left(\frac{1}{x} - 2\right)}^{2}}}\;dx = \answer{{\frac{8}{\frac{1}{x} - 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9}{x {\left(\log\left(x\right) + 1\right)}^{3}}}\;dx = \answer{{-\frac{9}{2 \, {\left(\log\left(x\right) + 1\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{10}{x^{2} {\left(\frac{1}{x} + 2\right)}^{2}}}\;dx = \answer{{-\frac{10}{\frac{1}{x} + 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, e^{x}}{e^{x} - 1}}\;dx = \answer{{-7 \, \log\left(e^{x} - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{16 \, x^{3}}{{\left(x^{4} + 8\right)}^{3}}}\;dx = \answer{{\frac{2}{{\left(x^{4} + 8\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, \sqrt{\frac{1}{x^{2}} - 1}}{x^{3}}}\;dx = \answer{{\frac{2}{3} \, {\left(\frac{1}{x^{2}} - 1\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, {\left(\sin\left(x\right) + 10\right)}^{4} \cos\left(x\right)}\;dx = \answer{{-\frac{3}{5} \, {\left(\sin\left(x\right) + 10\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, {\left(e^{x} - 8\right)} e^{x}}\;dx = \answer{{-{\left(e^{x} - 8\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, {\left(\cos\left(x\right) - 1\right)}^{3} \sin\left(x\right)}\;dx = \answer{{-\frac{3}{4} \, {\left(\cos\left(x\right) - 1\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-5 \, {\left(e^{x} - 7\right)}^{3} e^{x}}\;dx = \answer{{-\frac{5}{4} \, {\left(e^{x} - 7\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{14 \, e^{\left(\frac{1}{x^{2}} + 10\right)}}{x^{3}}}\;dx = \answer{{7 \, e^{\left(\frac{1}{x^{2}} + 10\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{30}{x^{4} {\left(\frac{1}{x^{3}} + 6\right)}^{2}}}\;dx = \answer{{-\frac{10}{\frac{1}{x^{3}} + 6}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, \sqrt{\log\left(x\right) + 1}}{x}}\;dx = \answer{{\frac{10}{3} \, {\left(\log\left(x\right) + 1\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9}{x^{4} {\left(\frac{1}{x^{3}} + 4\right)}^{2}}}\;dx = \answer{{-\frac{3}{\frac{1}{x^{3}} + 4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, \sqrt{e^{x} - 4} e^{x}}\;dx = \answer{{-2 \, {\left(e^{x} - 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, \sqrt{\frac{1}{x} + 5}}{x^{2}}}\;dx = \answer{{-2 \, {\left(\frac{1}{x} + 5\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, \sin\left(\log\left(x\right) - 2\right)}{x}}\;dx = \answer{{-6 \, \cos\left(\log\left(x\right) - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, {\left(\log\left(x\right) - 5\right)}^{2}}{x}}\;dx = \answer{{-\frac{5}{3} \, {\left(\log\left(x\right) - 5\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{{\left(\sqrt{x} - 9\right)}^{3}}{2 \, \sqrt{x}}}\;dx = \answer{{-\frac{1}{4} \, {\left(\sqrt{x} - 9\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, \cos\left(\sqrt{x} - 10\right)}{\sqrt{x}}}\;dx = \answer{{-4 \, \sin\left(\sqrt{x} - 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-36 \, {\left(x^{4} - 5\right)}^{3} x^{3}}\;dx = \answer{{-\frac{9}{4} \, {\left(x^{4} - 5\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, e^{x} \log\left(e^{x} + 1\right)}\;dx = \answer{{10 \, {\left(e^{x} + 1\right)} \log\left(e^{x} + 1\right) - 10 \, e^{x} - 10}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, \sqrt{\cos\left(x\right) + 8} \sin\left(x\right)}\;dx = \answer{{\frac{20}{3} \, {\left(\cos\left(x\right) + 8\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, \cos\left(x + 2\right)}\;dx = \answer{{-10 \, \sin\left(x + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{27 \, x^{2} \cos\left(x^{3} - 6\right)}\;dx = \answer{{9 \, \sin\left(x^{3} - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18 \, {\left(\frac{1}{x^{3}} + 8\right)}^{3}}{x^{4}}}\;dx = \answer{{-\frac{3}{2} \, {\left(\frac{1}{x^{3}} + 8\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, \log\left(x + 15\right)}\;dx = \answer{{-2 \, {\left(x + 15\right)} \log\left(x + 15\right) + 2 \, x + 30}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-28 \, x^{3} \log\left(x^{4} - 9\right)}\;dx = \answer{{7 \, x^{4} - 7 \, {\left(x^{4} - 9\right)} \log\left(x^{4} - 9\right) - 63}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{21 \, x^{2}}{{\left(x^{3} - 4\right)}^{2}}}\;dx = \answer{{\frac{7}{x^{3} - 4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3}{\sqrt{x} {\left(\sqrt{x} - 10\right)}^{3}}}\;dx = \answer{{-\frac{3}{{\left(\sqrt{x} - 10\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\cos\left(\cos\left(x\right) - 5\right) \sin\left(x\right)}\;dx = \answer{{\sin\left(\cos\left(x\right) - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, \sin\left(x\right)}{{\left(\cos\left(x\right) - 7\right)}^{2}}}\;dx = \answer{{-\frac{3}{\cos\left(x\right) - 7}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, {\left(e^{x} + 8\right)}^{3} e^{x}}\;dx = \answer{{\frac{3}{4} \, {\left(e^{x} + 8\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{1}{2 \, \sqrt{x} {\left(\sqrt{x} + 9\right)}^{2}}}\;dx = \answer{{-\frac{1}{\sqrt{x} + 9}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8 \, e^{x}}{{\left(e^{x} - 9\right)}^{3}}}\;dx = \answer{{-\frac{4}{{\left(e^{x} - 9\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, x^{2} \log\left(x^{3} + 4\right)}\;dx = \answer{{-x^{3} + {\left(x^{3} + 4\right)} \log\left(x^{3} + 4\right) - 4}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{{\left(\sqrt{x} - 9\right)}^{4}}{\sqrt{x}}}\;dx = \answer{{\frac{2}{5} \, {\left(\sqrt{x} - 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-32 \, {\left(x^{4} - 9\right)} x^{3}}\;dx = \answer{{-4 \, {\left(x^{4} - 9\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, \log\left(\sqrt{x} + 3\right)}{2 \, \sqrt{x}}}\;dx = \answer{{-5 \, {\left(\sqrt{x} + 3\right)} \log\left(\sqrt{x} + 3\right) + 5 \, \sqrt{x} + 15}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10}{x^{2} {\left(\frac{1}{x} - 10\right)}^{3}}}\;dx = \answer{{\frac{5}{{\left(\frac{1}{x} - 10\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, {\left(x^{2} + 1\right)}^{2} x}\;dx = \answer{{-\frac{5}{3} \, {\left(x^{2} + 1\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, \cos\left(\cos\left(x\right) - 10\right) \sin\left(x\right)}\;dx = \answer{{3 \, \sin\left(\cos\left(x\right) - 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, \sqrt{\sqrt{x} + 10}}{\sqrt{x}}}\;dx = \answer{{-\frac{16}{3} \, {\left(\sqrt{x} + 10\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, {\left(e^{x} - 3\right)}^{3} e^{x}}\;dx = \answer{{\frac{9}{4} \, {\left(e^{x} - 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{15 \, {\left(x^{3} - 6\right)} x^{2}}\;dx = \answer{{\frac{5}{2} \, {\left(x^{3} - 6\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3}{x^{4} {\left(\frac{1}{x^{3}} - 4\right)}}}\;dx = \answer{{\log\left(\frac{1}{x^{3}} - 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, {\left(x^{3} + 9\right)}^{2} x^{2}}\;dx = \answer{{-{\left(x^{3} + 9\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, e^{x}}{{\left(e^{x} - 1\right)}^{2}}}\;dx = \answer{{-\frac{5}{e^{x} - 1}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-14 \, \sqrt{x^{2} + 3} x}\;dx = \answer{{-\frac{14}{3} \, {\left(x^{2} + 3\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7}{{\left(x - 5\right)}^{2}}}\;dx = \answer{{\frac{7}{x - 5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2}{x^{3} {\left(\frac{1}{x^{2}} - 9\right)}^{2}}}\;dx = \answer{{-\frac{1}{\frac{1}{x^{2}} - 9}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{2 \, \cos\left(\cos\left(x\right) + 10\right) \sin\left(x\right)}\;dx = \answer{{-2 \, \sin\left(\cos\left(x\right) + 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, {\left(\log\left(x\right) + 8\right)}^{3}}{x}}\;dx = \answer{{\frac{3}{2} \, {\left(\log\left(x\right) + 8\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, {\left(\frac{1}{x^{3}} + 8\right)}^{2}}{x^{4}}}\;dx = \answer{{\frac{1}{3} \, {\left(\frac{1}{x^{3}} + 8\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{27 \, x^{2}}{x^{3} - 6}}\;dx = \answer{{-9 \, \log\left(x^{3} - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{14 \, \sqrt{x^{2} + 3} x}\;dx = \answer{{\frac{14}{3} \, {\left(x^{2} + 3\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9 \, x^{2}}{{\left(x^{3} + 7\right)}^{3}}}\;dx = \answer{{-\frac{3}{2 \, {\left(x^{3} + 7\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5}{x^{2} {\left(\frac{1}{x} - 3\right)}^{2}}}\;dx = \answer{{\frac{5}{\frac{1}{x} - 3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{6 \, \log\left(\log\left(x\right) + 9\right)}{x}}\;dx = \answer{{-6 \, {\left(\log\left(x\right) + 9\right)} \log\left(\log\left(x\right) + 9\right) + 6 \, \log\left(x\right) + 54}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{6 \, {\left(x^{3} + 6\right)}^{2} x^{2}}\;dx = \answer{{\frac{2}{3} \, {\left(x^{3} + 6\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{16 \, x^{3}}{x^{4} - 2}}\;dx = \answer{{4 \, \log\left(x^{4} - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, \log\left(x + 11\right)}\;dx = \answer{{7 \, {\left(x + 11\right)} \log\left(x + 11\right) - 7 \, x - 77}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9 \, {\left(\frac{1}{x^{3}} + 1\right)}}{x^{4}}}\;dx = \answer{{-\frac{3}{2} \, {\left(\frac{1}{x^{3}} + 1\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, {\left(x^{3} + 3\right)}^{4} x^{2}}\;dx = \answer{{-\frac{1}{5} \, {\left(x^{3} + 3\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, \sin\left(x + 12\right)}\;dx = \answer{{6 \, \cos\left(x + 12\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, {\left(x^{3} + 8\right)}^{4} x^{2}}\;dx = \answer{{-\frac{1}{5} \, {\left(x^{3} + 8\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, \cos\left(\log\left(x\right) + 10\right)}{x}}\;dx = \answer{{-8 \, \sin\left(\log\left(x\right) + 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-14 \, {\left(x^{2} + 15\right)} x}\;dx = \answer{{-\frac{7}{2} \, {\left(x^{2} + 15\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{30 \, {\left(\frac{1}{x^{3}} + 3\right)}^{4}}{x^{4}}}\;dx = \answer{{-2 \, {\left(\frac{1}{x^{3}} + 3\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, {\left(e^{x} + 10\right)}^{3} e^{x}}\;dx = \answer{{\frac{5}{2} \, {\left(e^{x} + 10\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, {\left(\frac{1}{x} - 3\right)}^{3}}{x^{2}}}\;dx = \answer{{{\left(\frac{1}{x} - 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, x + 18}\;dx = \answer{{-x^{2} + 18 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, \cos\left(x\right) \log\left(\sin\left(x\right) + 4\right)}\;dx = \answer{{-2 \, {\left(\sin\left(x\right) + 4\right)} \log\left(\sin\left(x\right) + 4\right) + 2 \, \sin\left(x\right) + 8}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{\sqrt{\log\left(x\right) - 8}}{x}}\;dx = \answer{{-\frac{2}{3} \, {\left(\log\left(x\right) - 8\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, \log\left(\sqrt{x} + 2\right)}{\sqrt{x}}}\;dx = \answer{{-6 \, {\left(\sqrt{x} + 2\right)} \log\left(\sqrt{x} + 2\right) + 6 \, \sqrt{x} + 12}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12}{x^{4} {\left(\frac{1}{x^{3}} + 1\right)}}}\;dx = \answer{{4 \, \log\left(\frac{1}{x^{3}} + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, e^{x}}{e^{x} + 1}}\;dx = \answer{{5 \, \log\left(e^{x} + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18 \, x}{{\left(x^{2} + 8\right)}^{3}}}\;dx = \answer{{-\frac{9}{2 \, {\left(x^{2} + 8\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-36 \, x^{3} e^{\left(x^{4} + 8\right)}}\;dx = \answer{{-9 \, e^{\left(x^{4} + 8\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, {\left(x + 1\right)}^{4}}\;dx = \answer{{-\frac{2}{5} \, x^{5} - 2 \, x^{4} - 4 \, x^{3} - 4 \, x^{2} - 2 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, \log\left(\cos\left(x\right) + 9\right) \sin\left(x\right)}\;dx = \answer{{7 \, {\left(\cos\left(x\right) + 9\right)} \log\left(\cos\left(x\right) + 9\right) - 7 \, \cos\left(x\right) - 63}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{6 \, \cos\left(\log\left(x\right) - 4\right)}{x}}\;dx = \answer{{-6 \, \sin\left(\log\left(x\right) - 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, \cos\left(\frac{1}{x^{2}} - 7\right)}{x^{3}}}\;dx = \answer{{2 \, \sin\left(\frac{1}{x^{2}} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, \log\left(x + 3\right)}\;dx = \answer{{7 \, {\left(x + 3\right)} \log\left(x + 3\right) - 7 \, x - 21}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8}{x^{3} {\left(\frac{1}{x^{2}} - 2\right)}^{2}}}\;dx = \answer{{\frac{4}{\frac{1}{x^{2}} - 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{27}{x^{4} {\left(\frac{1}{x^{3}} - 2\right)}^{2}}}\;dx = \answer{{\frac{9}{\frac{1}{x^{3}} - 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{24 \, {\left(\frac{1}{x^{3}} + 1\right)}^{4}}{x^{4}}}\;dx = \answer{{-\frac{8}{5} \, {\left(\frac{1}{x^{3}} + 1\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{7}{2 \, \sqrt{x} {\left(\sqrt{x} + 8\right)}}}\;dx = \answer{{7 \, \log\left(\sqrt{x} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, x^{3}}{{\left(x^{4} - 10\right)}^{2}}}\;dx = \answer{{\frac{2}{x^{4} - 10}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4 \, e^{\left(\log\left(x\right) + 10\right)}}{x}}\;dx = \answer{{4 \, x e^{10}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{20 \, {\left(\frac{1}{x^{2}} - 5\right)}^{4}}{x^{3}}}\;dx = \answer{{-2 \, {\left(\frac{1}{x^{2}} - 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9}{x {\left(\log\left(x\right) - 2\right)}}}\;dx = \answer{{9 \, \log\left(\log\left(x\right) - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, {\left(\sin\left(x\right) - 5\right)} \cos\left(x\right)}\;dx = \answer{{-{\left(\sin\left(x\right) - 5\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{18 \, \sin\left(\frac{1}{x^{3}} - 2\right)}{x^{4}}}\;dx = \answer{{-6 \, \cos\left(\frac{1}{x^{3}} - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12}{x^{4} {\left(\frac{1}{x^{3}} - 7\right)}^{2}}}\;dx = \answer{{-\frac{4}{\frac{1}{x^{3}} - 7}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{{\left(e^{x} - 5\right)}^{3} e^{x}}\;dx = \answer{{\frac{1}{4} \, {\left(e^{x} - 5\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, {\left(\frac{1}{x^{2}} + 11\right)}}{x^{3}}}\;dx = \answer{{2 \, {\left(\frac{1}{x^{2}} + 11\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\log\left(\cos\left(x\right) + 7\right) \sin\left(x\right)}\;dx = \answer{{{\left(\cos\left(x\right) + 7\right)} \log\left(\cos\left(x\right) + 7\right) - \cos\left(x\right) - 7}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-24 \, x^{2} e^{\left(x^{3} - 6\right)}}\;dx = \answer{{-8 \, e^{\left(x^{3} - 6\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{18 \, e^{\left(\frac{1}{x^{2}} + 1\right)}}{x^{3}}}\;dx = \answer{{9 \, e^{\left(\frac{1}{x^{2}} + 1\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, \sqrt{\sqrt{x} - 7}}{2 \, \sqrt{x}}}\;dx = \answer{{-\frac{14}{3} \, {\left(\sqrt{x} - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, x^{3}}{x^{4} + 9}}\;dx = \answer{{-2 \, \log\left(x^{4} + 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{27 \, {\left(\frac{1}{x^{3}} - 8\right)}}{x^{4}}}\;dx = \answer{{-\frac{9}{2} \, {\left(\frac{1}{x^{3}} - 8\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{20 \, x^{3}}{{\left(x^{4} + 4\right)}^{3}}}\;dx = \answer{{\frac{5}{2 \, {\left(x^{4} + 4\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, \sqrt{\log\left(x\right) - 4}}{x}}\;dx = \answer{{\frac{4}{3} \, {\left(\log\left(x\right) - 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, {\left(\frac{1}{x^{2}} + 2\right)}^{4}}{x^{3}}}\;dx = \answer{{-\frac{3}{5} \, {\left(\frac{1}{x^{2}} + 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, e^{x}}{{\left(e^{x} + 1\right)}^{2}}}\;dx = \answer{{-\frac{3}{e^{x} + 1}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, \sqrt{e^{x} - 7} e^{x}}\;dx = \answer{{2 \, {\left(e^{x} - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, {\left(x^{2} + 4\right)} x}\;dx = \answer{{-\frac{5}{2} \, {\left(x^{2} + 4\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{15 \, \sin\left(\frac{1}{x^{3}} - 8\right)}{x^{4}}}\;dx = \answer{{-5 \, \cos\left(\frac{1}{x^{3}} - 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, \log\left(\log\left(x\right) - 3\right)}{x}}\;dx = \answer{{-2 \, {\left(\log\left(x\right) - 3\right)} \log\left(\log\left(x\right) - 3\right) + 2 \, \log\left(x\right) - 6}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, {\left(x^{2} + 8\right)}^{2} x}\;dx = \answer{{-\frac{5}{3} \, {\left(x^{2} + 8\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, {\left(\cos\left(x\right) + 3\right)}^{3} \sin\left(x\right)}\;dx = \answer{{-\frac{7}{4} \, {\left(\cos\left(x\right) + 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{1}{\sqrt{x} {\left(\sqrt{x} + 2\right)}^{2}}}\;dx = \answer{{\frac{2}{\sqrt{x} + 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, {\left(x^{2} + 6\right)}^{3} x}\;dx = \answer{{\frac{1}{2} \, {\left(x^{2} + 6\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{24}{x^{7}}}\;dx = \answer{{-\frac{4}{x^{6}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(\sin\left(x\right) - 10\right)}^{2} \cos\left(x\right)}\;dx = \answer{{-\frac{4}{3} \, {\left(\sin\left(x\right) - 10\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{e^{\left(\sqrt{x} - 10\right)}}{2 \, \sqrt{x}}}\;dx = \answer{{e^{\left(\sqrt{x} - 10\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(x^{2} - 10\right)} x}\;dx = \answer{{-{\left(x^{2} - 10\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4 \, x^{3}}{{\left(x^{4} - 8\right)}^{2}}}\;dx = \answer{{-\frac{1}{x^{4} - 8}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, {\left(\cos\left(x\right) - 3\right)} \sin\left(x\right)}\;dx = \answer{{-5 \, {\left(\cos\left(x\right) - 3\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{7 \, \log\left(\log\left(x\right) - 10\right)}{x}}\;dx = \answer{{7 \, {\left(\log\left(x\right) - 10\right)} \log\left(\log\left(x\right) - 10\right) - 7 \, \log\left(x\right) + 70}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{2 \, \cos\left(x\right) \cos\left(\sin\left(x\right) - 6\right)}\;dx = \answer{{2 \, \sin\left(\sin\left(x\right) - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, {\left(x^{2} + 3\right)}^{4} x}\;dx = \answer{{{\left(x^{2} + 3\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, e^{\left(\frac{1}{x} + 1\right)}}{x^{2}}}\;dx = \answer{{5 \, e^{\left(\frac{1}{x} + 1\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, \cos\left(x\right) \sin\left(\sin\left(x\right) - 4\right)}\;dx = \answer{{9 \, \cos\left(\sin\left(x\right) - 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, {\left(\frac{1}{x} - 6\right)}^{3}}{x^{2}}}\;dx = \answer{{{\left(\frac{1}{x} - 6\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-18 \, x^{2} e^{\left(x^{3} - 4\right)}}\;dx = \answer{{-6 \, e^{\left(x^{3} - 4\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{18 \, {\left(x^{2} - 9\right)} x}\;dx = \answer{{\frac{9}{2} \, {\left(x^{2} - 9\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{36 \, x^{3}}{{\left(x^{4} - 9\right)}^{3}}}\;dx = \answer{{\frac{9}{2 \, {\left(x^{4} - 9\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-5 \, \cos\left(x\right) \log\left(\sin\left(x\right) - 10\right)}\;dx = \answer{{-5 \, {\left(\sin\left(x\right) - 10\right)} \log\left(\sin\left(x\right) - 10\right) + 5 \, \sin\left(x\right) - 50}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, \sin\left(x\right)}{{\left(\cos\left(x\right) - 1\right)}^{3}}}\;dx = \answer{{\frac{3}{{\left(\cos\left(x\right) - 1\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{10 \, {\left(\frac{1}{x} - 9\right)}^{3}}{x^{2}}}\;dx = \answer{{\frac{5}{2} \, {\left(\frac{1}{x} - 9\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4 \, {\left(\frac{1}{x^{2}} + 10\right)}^{2}}{x^{3}}}\;dx = \answer{{-\frac{2}{3} \, {\left(\frac{1}{x^{2}} + 10\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, \cos\left(x\right) e^{\left(\sin\left(x\right) - 8\right)}}\;dx = \answer{{-2 \, e^{\left(\sin\left(x\right) - 8\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, \cos\left(x\right) \cos\left(\sin\left(x\right) + 4\right)}\;dx = \answer{{5 \, \sin\left(\sin\left(x\right) + 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{16}{x^{3} {\left(\frac{1}{x^{2}} + 5\right)}^{3}}}\;dx = \answer{{-\frac{4}{{\left(\frac{1}{x^{2}} + 5\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8}{x^{3} {\left(\frac{1}{x^{2}} + 3\right)}^{2}}}\;dx = \answer{{-\frac{4}{\frac{1}{x^{2}} + 3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, {\left(\cos\left(x\right) + 8\right)} \sin\left(x\right)}\;dx = \answer{{-\frac{5}{2} \, {\left(\cos\left(x\right) + 8\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4}{x^{3} {\left(\frac{1}{x^{2}} - 5\right)}}}\;dx = \answer{{-2 \, \log\left(\frac{1}{x^{2}} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, e^{\left(x + e^{x} - 6\right)}}\;dx = \answer{{7 \, e^{\left(e^{x} - 6\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, \log\left(\frac{1}{x} - 4\right)}{x^{2}}}\;dx = \answer{{3 \, {\left(\frac{1}{x} - 4\right)} \log\left(\frac{1}{x} - 4\right) - \frac{3}{x} + 12}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, \cos\left(\frac{1}{x} - 10\right)}{x^{2}}}\;dx = \answer{{-2 \, \sin\left(\frac{1}{x} - 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, {\left(x - 3\right)}^{2}}\;dx = \answer{{3 \, x^{3} - 27 \, x^{2} + 81 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, {\left(\frac{1}{x} + 9\right)}^{2}}{x^{2}}}\;dx = \answer{{-\frac{5}{3} \, {\left(\frac{1}{x} + 9\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, {\left(\sin\left(x\right) + 5\right)}^{2} \cos\left(x\right)}\;dx = \answer{{-\frac{10}{3} \, {\left(\sin\left(x\right) + 5\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, {\left(\cos\left(x\right) - 8\right)}^{3} \sin\left(x\right)}\;dx = \answer{{-{\left(\cos\left(x\right) - 8\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, \cos\left(\sqrt{x} - 5\right)}{\sqrt{x}}}\;dx = \answer{{6 \, \sin\left(\sqrt{x} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, x \sin\left(x^{2} - 1\right)}\;dx = \answer{{3 \, \cos\left(x^{2} - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{15 \, {\left(\frac{1}{x^{3}} - 5\right)}^{4}}{x^{4}}}\;dx = \answer{{-{\left(\frac{1}{x^{3}} - 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{20 \, x}{{\left(x^{2} - 2\right)}^{2}}}\;dx = \answer{{\frac{10}{x^{2} - 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9}{x^{2} {\left(\frac{1}{x} - 10\right)}^{2}}}\;dx = \answer{{-\frac{9}{\frac{1}{x} - 10}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, {\left(\cos\left(x\right) - 8\right)}^{3} \sin\left(x\right)}\;dx = \answer{{-\frac{5}{2} \, {\left(\cos\left(x\right) - 8\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, {\left(\sin\left(x\right) - 9\right)}^{2} \cos\left(x\right)}\;dx = \answer{{-3 \, {\left(\sin\left(x\right) - 9\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, x + 24}\;dx = \answer{{\frac{3}{2} \, x^{2} + 24 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{16 \, x \cos\left(x^{2} - 8\right)}\;dx = \answer{{8 \, \sin\left(x^{2} - 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{15}{x^{4} {\left(\frac{1}{x^{3}} - 3\right)}^{3}}}\;dx = \answer{{\frac{5}{2 \, {\left(\frac{1}{x^{3}} - 3\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, \sqrt{x^{2} + 7} x}\;dx = \answer{{-\frac{2}{3} \, {\left(x^{2} + 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, {\left(\cos\left(x\right) - 16\right)} \sin\left(x\right)}\;dx = \answer{{-2 \, {\left(\cos\left(x\right) - 16\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8}{x^{3} {\left(\frac{1}{x^{2}} - 10\right)}^{2}}}\;dx = \answer{{-\frac{4}{\frac{1}{x^{2}} - 10}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{6 \, e^{x}}{{\left(e^{x} + 7\right)}^{3}}}\;dx = \answer{{\frac{3}{{\left(e^{x} + 7\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{21}{x^{4} {\left(\frac{1}{x^{3}} - 7\right)}}}\;dx = \answer{{-7 \, \log\left(\frac{1}{x^{3}} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{15 \, x^{2} \cos\left(x^{3} - 6\right)}\;dx = \answer{{5 \, \sin\left(x^{3} - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{6 \, \cos\left(x\right) \cos\left(\sin\left(x\right) + 10\right)}\;dx = \answer{{6 \, \sin\left(\sin\left(x\right) + 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, \cos\left(\frac{1}{x} - 1\right)}{x^{2}}}\;dx = \answer{{-5 \, \sin\left(\frac{1}{x} - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{\cos\left(\frac{1}{x} - 7\right)}{x^{2}}}\;dx = \answer{{-\sin\left(\frac{1}{x} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6}{x {\left(\log\left(x\right) - 2\right)}^{2}}}\;dx = \answer{{-\frac{6}{\log\left(x\right) - 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{24 \, x^{3} e^{\left(x^{4} + 7\right)}}\;dx = \answer{{6 \, e^{\left(x^{4} + 7\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, e^{\left(\frac{1}{x^{2}} + 3\right)}}{x^{3}}}\;dx = \answer{{e^{\left(\frac{1}{x^{2}} + 3\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-8 \, {\left(\sin\left(x\right) - 1\right)} \cos\left(x\right)}\;dx = \answer{{-4 \, {\left(\sin\left(x\right) - 1\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{7}{2 \, \sqrt{x} {\left(\sqrt{x} + 3\right)}^{2}}}\;dx = \answer{{-\frac{7}{\sqrt{x} + 3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{1}{x - 1}}\;dx = \answer{{\log\left(x - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18}{x^{3} {\left(\frac{1}{x^{2}} + 6\right)}^{3}}}\;dx = \answer{{\frac{9}{2 \, {\left(\frac{1}{x^{2}} + 6\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{16 \, {\left(x^{2} - 10\right)}^{4} x}\;dx = \answer{{\frac{8}{5} \, {\left(x^{2} - 10\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\sqrt{x - 7}}\;dx = \answer{{-\frac{2}{3} \, {\left(x - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{40 \, {\left(x^{4} - 3\right)}^{3} x^{3}}\;dx = \answer{{\frac{5}{2} \, {\left(x^{4} - 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{16 \, x^{3} \sin\left(x^{4} - 5\right)}\;dx = \answer{{-4 \, \cos\left(x^{4} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{2 \, {\left(x^{2} - 10\right)} x}\;dx = \answer{{\frac{1}{2} \, {\left(x^{2} - 10\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12 \, \cos\left(\frac{1}{x^{3}} + 9\right)}{x^{4}}}\;dx = \answer{{4 \, \sin\left(\frac{1}{x^{3}} + 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{27 \, x^{2}}{x^{3} + 8}}\;dx = \answer{{9 \, \log\left(x^{3} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, \log\left(x + 2\right)}\;dx = \answer{{7 \, {\left(x + 2\right)} \log\left(x + 2\right) - 7 \, x - 14}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2}{x^{3} {\left(\frac{1}{x^{2}} - 4\right)}^{3}}}\;dx = \answer{{\frac{1}{2 \, {\left(\frac{1}{x^{2}} - 4\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, \sin\left(\frac{1}{x^{2}} - 1\right)}{x^{3}}}\;dx = \answer{{-4 \, \cos\left(\frac{1}{x^{2}} - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{15 \, x^{2} \log\left(x^{3} - 8\right)}\;dx = \answer{{-5 \, x^{3} + 5 \, {\left(x^{3} - 8\right)} \log\left(x^{3} - 8\right) + 40}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, {\left(\frac{1}{x^{2}} - 4\right)}^{2}}{x^{3}}}\;dx = \answer{{\frac{4}{3} \, {\left(\frac{1}{x^{2}} - 4\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, e^{\left(\cos\left(x\right) - 3\right)} \sin\left(x\right)}\;dx = \answer{{-4 \, e^{\left(\cos\left(x\right) - 3\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, {\left(e^{x} + 10\right)} e^{x}}\;dx = \answer{{-\frac{9}{2} \, {\left(e^{x} + 10\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{{\left(\sqrt{x} + 10\right)}^{2}}{\sqrt{x}}}\;dx = \answer{{-\frac{2}{3} \, {\left(\sqrt{x} + 10\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{{\left(\sqrt{x} - 4\right)}^{2}}{\sqrt{x}}}\;dx = \answer{{\frac{2}{3} \, {\left(\sqrt{x} - 4\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, {\left(\cos\left(x\right) + 12\right)} \sin\left(x\right)}\;dx = \answer{{-4 \, {\left(\cos\left(x\right) + 12\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, \sin\left(x\right)}{{\left(\cos\left(x\right) - 1\right)}^{3}}}\;dx = \answer{{-\frac{5}{2 \, {\left(\cos\left(x\right) - 1\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18 \, e^{\left(\frac{1}{x^{2}} - 4\right)}}{x^{3}}}\;dx = \answer{{-9 \, e^{\left(\frac{1}{x^{2}} - 4\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, x^{3}}{x^{4} + 9}}\;dx = \answer{{-\log\left(x^{4} + 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{\cos\left(\log\left(x\right) + 6\right)}{x}}\;dx = \answer{{\sin\left(\log\left(x\right) + 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{21 \, \log\left(\frac{1}{x^{3}} + 10\right)}{x^{4}}}\;dx = \answer{{-7 \, {\left(\frac{1}{x^{3}} + 10\right)} \log\left(\frac{1}{x^{3}} + 10\right) + \frac{7}{x^{3}} + 70}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-8 \, \cos\left(x\right) \log\left(\sin\left(x\right) + 7\right)}\;dx = \answer{{-8 \, {\left(\sin\left(x\right) + 7\right)} \log\left(\sin\left(x\right) + 7\right) + 8 \, \sin\left(x\right) + 56}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-{\left(\sin\left(x\right) - 5\right)}^{4} \cos\left(x\right)}\;dx = \answer{{-\frac{1}{5} \, {\left(\sin\left(x\right) - 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, \cos\left(x\right) \log\left(\sin\left(x\right) + 3\right)}\;dx = \answer{{-2 \, {\left(\sin\left(x\right) + 3\right)} \log\left(\sin\left(x\right) + 3\right) + 2 \, \sin\left(x\right) + 6}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, {\left(e^{x} - 19\right)} e^{x}}\;dx = \answer{{4 \, {\left(e^{x} - 19\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8 \, e^{x}}{{\left(e^{x} - 9\right)}^{2}}}\;dx = \answer{{-\frac{8}{e^{x} - 9}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(e^{x} - 1\right)}^{3} e^{x}}\;dx = \answer{{-{\left(e^{x} - 1\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, \sin\left(x\right)}{{\left(\cos\left(x\right) - 10\right)}^{3}}}\;dx = \answer{{-\frac{1}{{\left(\cos\left(x\right) - 10\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{21 \, x^{2} \sin\left(x^{3} + 7\right)}\;dx = \answer{{-7 \, \cos\left(x^{3} + 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4}{x^{2} {\left(\frac{1}{x} + 2\right)}^{3}}}\;dx = \answer{{\frac{2}{{\left(\frac{1}{x} + 2\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{27 \, e^{\left(\frac{1}{x^{3}} - 6\right)}}{x^{4}}}\;dx = \answer{{-9 \, e^{\left(\frac{1}{x^{3}} - 6\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, \cos\left(\frac{1}{x^{3}} - 3\right)}{x^{4}}}\;dx = \answer{{-\sin\left(\frac{1}{x^{3}} - 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, {\left(e^{x} - 2\right)}^{4} e^{x}}\;dx = \answer{{\frac{4}{5} \, {\left(e^{x} - 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, {\left(\log\left(x\right) + 5\right)}^{4}}{x}}\;dx = \answer{{-\frac{8}{5} \, {\left(\log\left(x\right) + 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-16 \, \sqrt{x^{2} - 3} x}\;dx = \answer{{-\frac{16}{3} \, {\left(x^{2} - 3\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, \sqrt{\sin\left(x\right) - 4} \cos\left(x\right)}\;dx = \answer{{\frac{20}{3} \, {\left(\sin\left(x\right) - 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, e^{x}}{e^{x} + 10}}\;dx = \answer{{-2 \, \log\left(e^{x} + 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{24 \, \cos\left(\frac{1}{x^{3}} + 3\right)}{x^{4}}}\;dx = \answer{{-8 \, \sin\left(\frac{1}{x^{3}} + 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, {\left(\frac{1}{x} + 12\right)}}{x^{2}}}\;dx = \answer{{\frac{3}{2} \, {\left(\frac{1}{x} + 12\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, {\left(\frac{1}{x} - 10\right)}^{3}}{x^{2}}}\;dx = \answer{{\frac{1}{2} \, {\left(\frac{1}{x} - 10\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-{\left(\sin\left(x\right) - 5\right)} \cos\left(x\right)}\;dx = \answer{{-\frac{1}{2} \, {\left(\sin\left(x\right) - 5\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(\cos\left(x\right) - 3\right)} \sin\left(x\right)}\;dx = \answer{{2 \, {\left(\cos\left(x\right) - 3\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-12 \, x \sin\left(x^{2} - 2\right)}\;dx = \answer{{6 \, \cos\left(x^{2} - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, {\left(\sqrt{x} + 5\right)}^{3}}{\sqrt{x}}}\;dx = \answer{{-\frac{5}{2} \, {\left(\sqrt{x} + 5\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, {\left(\sin\left(x\right) - 3\right)} \cos\left(x\right)}\;dx = \answer{{\frac{9}{2} \, {\left(\sin\left(x\right) - 3\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-12 \, x^{3} e^{\left(x^{4} - 10\right)}}\;dx = \answer{{-3 \, e^{\left(x^{4} - 10\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{18}{x^{4} {\left(\frac{1}{x^{3}} + 6\right)}^{2}}}\;dx = \answer{{-\frac{6}{\frac{1}{x^{3}} + 6}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-e^{x} \log\left(e^{x} + 1\right)}\;dx = \answer{{-{\left(e^{x} + 1\right)} \log\left(e^{x} + 1\right) + e^{x} + 1}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, x - 92}\;dx = \answer{{-2 \, x^{2} - 92 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9}{{\left(x - 2\right)}^{3}}}\;dx = \answer{{-\frac{9}{2 \, {\left(x - 2\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6}{x^{2} {\left(\frac{1}{x} - 6\right)}}}\;dx = \answer{{-6 \, \log\left(\frac{1}{x} - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{\log\left(\log\left(x\right) + 7\right)}{x}}\;dx = \answer{{-{\left(\log\left(x\right) + 7\right)} \log\left(\log\left(x\right) + 7\right) + \log\left(x\right) + 7}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, {\left(x^{3} + 14\right)} x^{2}}\;dx = \answer{{\frac{3}{2} \, {\left(x^{3} + 14\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{2 \, \sqrt{e^{x} + 3} e^{x}}\;dx = \answer{{\frac{4}{3} \, {\left(e^{x} + 3\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{28 \, x^{3} \sin\left(x^{4} + 8\right)}\;dx = \answer{{-7 \, \cos\left(x^{4} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, {\left(\sqrt{x} - 17\right)}}{2 \, \sqrt{x}}}\;dx = \answer{{-\frac{7}{2} \, {\left(\sqrt{x} - 17\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{7 \, {\left(\sqrt{x} + 8\right)}^{2}}{2 \, \sqrt{x}}}\;dx = \answer{{\frac{7}{3} \, {\left(\sqrt{x} + 8\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-12 \, x^{2} \cos\left(x^{3} - 1\right)}\;dx = \answer{{-4 \, \sin\left(x^{3} - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, \cos\left(x\right) e^{\left(\sin\left(x\right) - 5\right)}}\;dx = \answer{{4 \, e^{\left(\sin\left(x\right) - 5\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, e^{\left(x + e^{x} + 6\right)}}\;dx = \answer{{9 \, e^{\left(e^{x} + 6\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9}{x {\left(\log\left(x\right) - 8\right)}}}\;dx = \answer{{9 \, \log\left(\log\left(x\right) - 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9 \, {\left(\frac{1}{x^{3}} - 6\right)}^{2}}{x^{4}}}\;dx = \answer{{-{\left(\frac{1}{x^{3}} - 6\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, {\left(\sqrt{x} + 5\right)}^{4}}{\sqrt{x}}}\;dx = \answer{{\frac{6}{5} \, {\left(\sqrt{x} + 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12}{x^{3} {\left(\frac{1}{x^{2}} + 4\right)}^{2}}}\;dx = \answer{{-\frac{6}{\frac{1}{x^{2}} + 4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, {\left(\sin\left(x\right) - 8\right)} \cos\left(x\right)}\;dx = \answer{{-\frac{7}{2} \, {\left(\sin\left(x\right) - 8\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-18 \, {\left(x^{2} + 1\right)}^{2} x}\;dx = \answer{{-3 \, {\left(x^{2} + 1\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, {\left(x - 13\right)}^{2}}\;dx = \answer{{-3 \, x^{3} + 117 \, x^{2} - 1521 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{24 \, x^{2} \log\left(x^{3} - 8\right)}\;dx = \answer{{-8 \, x^{3} + 8 \, {\left(x^{3} - 8\right)} \log\left(x^{3} - 8\right) + 64}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, {\left(\sqrt{x} - 4\right)}^{3}}{2 \, \sqrt{x}}}\;dx = \answer{{\frac{3}{4} \, {\left(\sqrt{x} - 4\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{30 \, x^{2}}{x^{3} + 3}}\;dx = \answer{{10 \, \log\left(x^{3} + 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, \sin\left(x - 10\right)}\;dx = \answer{{-8 \, \cos\left(x - 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{2 \, {\left(x^{2} - 5\right)}^{4} x}\;dx = \answer{{\frac{1}{5} \, {\left(x^{2} - 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-20 \, {\left(x^{2} + 4\right)}^{2} x}\;dx = \answer{{-\frac{10}{3} \, {\left(x^{2} + 4\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{15 \, \log\left(\frac{1}{x^{3}} + 2\right)}{x^{4}}}\;dx = \answer{{-5 \, {\left(\frac{1}{x^{3}} + 2\right)} \log\left(\frac{1}{x^{3}} + 2\right) + \frac{5}{x^{3}} + 10}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, e^{\left(x + e^{x} - 2\right)}}\;dx = \answer{{3 \, e^{\left(e^{x} - 2\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, x^{2} \cos\left(x^{3} + 7\right)}\;dx = \answer{{-2 \, \sin\left(x^{3} + 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5}{2 \, \sqrt{x} {\left(\sqrt{x} + 5\right)}^{3}}}\;dx = \answer{{-\frac{5}{2 \, {\left(\sqrt{x} + 5\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9}{2 \, \sqrt{x} {\left(\sqrt{x} - 3\right)}^{2}}}\;dx = \answer{{\frac{9}{\sqrt{x} - 3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{16 \, {\left(\frac{1}{x^{2}} + 6\right)}^{4}}{x^{3}}}\;dx = \answer{{-\frac{8}{5} \, {\left(\frac{1}{x^{2}} + 6\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, {\left(\sqrt{x} + 17\right)}}{2 \, \sqrt{x}}}\;dx = \answer{{\frac{3}{2} \, {\left(\sqrt{x} + 17\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, \cos\left(x\right)}{\sin\left(x\right) + 1}}\;dx = \answer{{-3 \, \log\left(\sin\left(x\right) + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, \log\left(x - 8\right)}\;dx = \answer{{-2 \, {\left(x - 8\right)} \log\left(x - 8\right) + 2 \, x - 16}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{6 \, x^{2}}{x^{3} + 4}}\;dx = \answer{{-2 \, \log\left(x^{3} + 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(\sin\left(x\right) + 9\right)}^{4} \cos\left(x\right)}\;dx = \answer{{-\frac{4}{5} \, {\left(\sin\left(x\right) + 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, \sqrt{\cos\left(x\right) + 4} \sin\left(x\right)}\;dx = \answer{{\frac{8}{3} \, {\left(\cos\left(x\right) + 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6}{x {\left(\log\left(x\right) + 9\right)}^{3}}}\;dx = \answer{{-\frac{3}{{\left(\log\left(x\right) + 9\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, {\left(e^{x} - 12\right)} e^{x}}\;dx = \answer{{-\frac{9}{2} \, {\left(e^{x} - 12\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{12 \, x \cos\left(x^{2} - 5\right)}\;dx = \answer{{6 \, \sin\left(x^{2} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, \sqrt{\sin\left(x\right) + 10} \cos\left(x\right)}\;dx = \answer{{6 \, {\left(\sin\left(x\right) + 10\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{12 \, x^{2} e^{\left(x^{3} + 9\right)}}\;dx = \answer{{4 \, e^{\left(x^{3} + 9\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{20 \, x^{3}}{{\left(x^{4} - 1\right)}^{2}}}\;dx = \answer{{-\frac{5}{x^{4} - 1}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-12 \, x^{2} e^{\left(x^{3} + 10\right)}}\;dx = \answer{{-4 \, e^{\left(x^{3} + 10\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9}{x^{2} {\left(\frac{1}{x} - 8\right)}^{2}}}\;dx = \answer{{\frac{9}{\frac{1}{x} - 8}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, x}{{\left(x^{2} + 8\right)}^{3}}}\;dx = \answer{{-\frac{1}{2 \, {\left(x^{2} + 8\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, \sqrt{\sqrt{x} - 3}}{2 \, \sqrt{x}}}\;dx = \answer{{-6 \, {\left(\sqrt{x} - 3\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{12 \, x^{2}}{x^{3} + 8}}\;dx = \answer{{4 \, \log\left(x^{3} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{15}{x^{4} {\left(\frac{1}{x^{3}} - 10\right)}^{2}}}\;dx = \answer{{-\frac{5}{\frac{1}{x^{3}} - 10}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{1}{2 \, \sqrt{x} {\left(\sqrt{x} + 1\right)}^{3}}}\;dx = \answer{{-\frac{1}{2 \, {\left(\sqrt{x} + 1\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, \log\left(\frac{1}{x^{2}} + 4\right)}{x^{3}}}\;dx = \answer{{2 \, {\left(\frac{1}{x^{2}} + 4\right)} \log\left(\frac{1}{x^{2}} + 4\right) - \frac{2}{x^{2}} - 8}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{6 \, {\left(\sin\left(x\right) + 3\right)} \cos\left(x\right)}\;dx = \answer{{3 \, {\left(\sin\left(x\right) + 3\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{16 \, \sqrt{x^{2} - 7} x}\;dx = \answer{{\frac{16}{3} \, {\left(x^{2} - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, {\left(\log\left(x\right) + 7\right)}^{3}}{x}}\;dx = \answer{{\frac{5}{4} \, {\left(\log\left(x\right) + 7\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{28 \, x^{3}}{{\left(x^{4} - 1\right)}^{3}}}\;dx = \answer{{-\frac{7}{2 \, {\left(x^{4} - 1\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{12 \, \cos\left(\frac{1}{x^{2}} - 9\right)}{x^{3}}}\;dx = \answer{{-6 \, \sin\left(\frac{1}{x^{2}} - 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, e^{\left(x - 11\right)}}\;dx = \answer{{-9 \, e^{\left(x - 11\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, \sqrt{\cos\left(x\right) - 9} \sin\left(x\right)}\;dx = \answer{{-2 \, {\left(\cos\left(x\right) - 9\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{14 \, \log\left(\frac{1}{x^{2}} - 10\right)}{x^{3}}}\;dx = \answer{{-7 \, {\left(\frac{1}{x^{2}} - 10\right)} \log\left(\frac{1}{x^{2}} - 10\right) + \frac{7}{x^{2}} - 70}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5}{x^{2} {\left(\frac{1}{x} - 4\right)}}}\;dx = \answer{{-5 \, \log\left(\frac{1}{x} - 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8 \, \cos\left(\log\left(x\right) - 1\right)}{x}}\;dx = \answer{{8 \, \sin\left(\log\left(x\right) - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{32 \, x^{3}}{{\left(x^{4} + 4\right)}^{3}}}\;dx = \answer{{-\frac{4}{{\left(x^{4} + 4\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, \cos\left(x\right)}{{\left(\sin\left(x\right) + 8\right)}^{2}}}\;dx = \answer{{\frac{9}{\sin\left(x\right) + 8}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4 \, \sqrt{\frac{1}{x} - 8}}{x^{2}}}\;dx = \answer{{-\frac{8}{3} \, {\left(\frac{1}{x} - 8\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{18 \, x \log\left(x^{2} - 3\right)}\;dx = \answer{{-9 \, x^{2} + 9 \, {\left(x^{2} - 3\right)} \log\left(x^{2} - 3\right) + 27}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{14 \, {\left(\frac{1}{x^{2}} + 9\right)}^{3}}{x^{3}}}\;dx = \answer{{\frac{7}{4} \, {\left(\frac{1}{x^{2}} + 9\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, {\left(x + 17\right)}^{3}}\;dx = \answer{{-\frac{3}{4} \, x^{4} - 51 \, x^{3} - \frac{2601}{2} \, x^{2} - 14739 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{24 \, \sin\left(\frac{1}{x^{3}} + 1\right)}{x^{4}}}\;dx = \answer{{8 \, \cos\left(\frac{1}{x^{3}} + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, \sqrt{\cos\left(x\right) - 9} \sin\left(x\right)}\;dx = \answer{{\frac{14}{3} \, {\left(\cos\left(x\right) - 9\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-40 \, {\left(x^{4} - 3\right)}^{3} x^{3}}\;dx = \answer{{-\frac{5}{2} \, {\left(x^{4} - 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, \sqrt{e^{x} + 6} e^{x}}\;dx = \answer{{\frac{14}{3} \, {\left(e^{x} + 6\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, {\left(x^{3} + 2\right)} x^{2}}\;dx = \answer{{\frac{3}{2} \, {\left(x^{3} + 2\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3}{{\left(x - 1\right)}^{2}}}\;dx = \answer{{\frac{3}{x - 1}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{12 \, x^{2}}{x^{3} + 10}}\;dx = \answer{{4 \, \log\left(x^{3} + 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{6 \, {\left(x - 3\right)}^{2}}\;dx = \answer{{2 \, x^{3} - 18 \, x^{2} + 54 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, \sqrt{\cos\left(x\right) + 2} \sin\left(x\right)}\;dx = \answer{{\frac{14}{3} \, {\left(\cos\left(x\right) + 2\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, \cos\left(x\right)}{\sin\left(x\right) - 8}}\;dx = \answer{{-9 \, \log\left(\sin\left(x\right) - 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, x + 98}\;dx = \answer{{\frac{7}{2} \, x^{2} + 98 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, {\left(\sin\left(x\right) + 4\right)}^{4} \cos\left(x\right)}\;dx = \answer{{{\left(\sin\left(x\right) + 4\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10}{x^{2} {\left(\frac{1}{x} - 8\right)}^{2}}}\;dx = \answer{{\frac{10}{\frac{1}{x} - 8}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{24 \, {\left(x^{4} + 9\right)}^{4} x^{3}}\;dx = \answer{{\frac{6}{5} \, {\left(x^{4} + 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4 \, \log\left(\frac{1}{x} + 10\right)}{x^{2}}}\;dx = \answer{{-4 \, {\left(\frac{1}{x} + 10\right)} \log\left(\frac{1}{x} + 10\right) + \frac{4}{x} + 40}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, x e^{\left(x^{2} + 6\right)}}\;dx = \answer{{-e^{\left(x^{2} + 6\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{24 \, {\left(\frac{1}{x^{3}} - 14\right)}}{x^{4}}}\;dx = \answer{{-4 \, {\left(\frac{1}{x^{3}} - 14\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9 \, \log\left(\frac{1}{x} + 9\right)}{x^{2}}}\;dx = \answer{{-9 \, {\left(\frac{1}{x} + 9\right)} \log\left(\frac{1}{x} + 9\right) + \frac{9}{x} + 81}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, x \sin\left(x^{2} - 3\right)}\;dx = \answer{{-2 \, \cos\left(x^{2} - 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, e^{\left(\sqrt{x} + 8\right)}}{\sqrt{x}}}\;dx = \answer{{-8 \, e^{\left(\sqrt{x} + 8\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-32 \, x^{3} \log\left(x^{4} - 9\right)}\;dx = \answer{{8 \, x^{4} - 8 \, {\left(x^{4} - 9\right)} \log\left(x^{4} - 9\right) - 72}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, e^{x} \sin\left(e^{x} + 1\right)}\;dx = \answer{{-10 \, \cos\left(e^{x} + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{20 \, x^{3}}{x^{4} - 7}}\;dx = \answer{{5 \, \log\left(x^{4} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{10 \, {\left(\frac{1}{x^{2}} - 7\right)}^{2}}{x^{3}}}\;dx = \answer{{\frac{5}{3} \, {\left(\frac{1}{x^{2}} - 7\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, {\left(\sin\left(x\right) - 9\right)}^{3} \cos\left(x\right)}\;dx = \answer{{-\frac{5}{2} \, {\left(\sin\left(x\right) - 9\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, \log\left(\cos\left(x\right) - 6\right) \sin\left(x\right)}\;dx = \answer{{-4 \, {\left(\cos\left(x\right) - 6\right)} \log\left(\cos\left(x\right) - 6\right) + 4 \, \cos\left(x\right) - 24}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{21 \, x^{2}}{x^{3} - 6}}\;dx = \answer{{-7 \, \log\left(x^{3} - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, \sin\left(x\right) \sin\left(\cos\left(x\right) - 3\right)}\;dx = \answer{{8 \, \cos\left(\cos\left(x\right) - 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, \cos\left(x - 11\right)}\;dx = \answer{{10 \, \sin\left(x - 11\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9 \, \cos\left(x\right)}{{\left(\sin\left(x\right) + 7\right)}^{2}}}\;dx = \answer{{-\frac{9}{\sin\left(x\right) + 7}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, \sqrt{\cos\left(x\right) - 7} \sin\left(x\right)}\;dx = \answer{{-\frac{20}{3} \, {\left(\cos\left(x\right) - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-16 \, x^{3} e^{\left(x^{4} + 9\right)}}\;dx = \answer{{-4 \, e^{\left(x^{4} + 9\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, {\left(\sqrt{x} - 7\right)}^{4}}{\sqrt{x}}}\;dx = \answer{{\frac{4}{5} \, {\left(\sqrt{x} - 7\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{27 \, {\left(\frac{1}{x^{3}} - 7\right)}^{4}}{x^{4}}}\;dx = \answer{{-\frac{9}{5} \, {\left(\frac{1}{x^{3}} - 7\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, \sqrt{\cos\left(x\right) + 3} \sin\left(x\right)}\;dx = \answer{{4 \, {\left(\cos\left(x\right) + 3\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, {\left(\cos\left(x\right) + 3\right)}^{3} \sin\left(x\right)}\;dx = \answer{{-\frac{3}{4} \, {\left(\cos\left(x\right) + 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, {\left(\frac{1}{x} + 9\right)}^{4}}{x^{2}}}\;dx = \answer{{\frac{2}{5} \, {\left(\frac{1}{x} + 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, {\left(\frac{1}{x} + 2\right)}^{2}}{x^{2}}}\;dx = \answer{{\frac{5}{3} \, {\left(\frac{1}{x} + 2\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, {\left(x + 3\right)}^{4}}\;dx = \answer{{\frac{7}{5} \, x^{5} + 21 \, x^{4} + 126 \, x^{3} + 378 \, x^{2} + 567 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, \sin\left(\frac{1}{x^{3}} - 4\right)}{x^{4}}}\;dx = \answer{{2 \, \cos\left(\frac{1}{x^{3}} - 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, {\left(\cos\left(x\right) - 5\right)} \sin\left(x\right)}\;dx = \answer{{-4 \, {\left(\cos\left(x\right) - 5\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, x^{2}}{x^{3} + 3}}\;dx = \answer{{2 \, \log\left(x^{3} + 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{12 \, e^{\left(\frac{1}{x^{3}} + 1\right)}}{x^{4}}}\;dx = \answer{{-4 \, e^{\left(\frac{1}{x^{3}} + 1\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2}{\sqrt{x} {\left(\sqrt{x} + 8\right)}^{3}}}\;dx = \answer{{\frac{2}{{\left(\sqrt{x} + 8\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-2 \, x \log\left(x^{2} - 5\right)}\;dx = \answer{{x^{2} - {\left(x^{2} - 5\right)} \log\left(x^{2} - 5\right) - 5}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, \sqrt{\frac{1}{x^{3}} + 10}}{x^{4}}}\;dx = \answer{{-\frac{2}{3} \, {\left(\frac{1}{x^{3}} + 10\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4}{\sqrt{x} {\left(\sqrt{x} - 7\right)}}}\;dx = \answer{{8 \, \log\left(\sqrt{x} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-16 \, {\left(x^{4} - 5\right)} x^{3}}\;dx = \answer{{-2 \, {\left(x^{4} - 5\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, {\left(e^{x} - 10\right)}^{4} e^{x}}\;dx = \answer{{-\frac{9}{5} \, {\left(e^{x} - 10\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{30 \, \sqrt{\frac{1}{x^{3}} + 1}}{x^{4}}}\;dx = \answer{{\frac{20}{3} \, {\left(\frac{1}{x^{3}} + 1\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, {\left(\sin\left(x\right) - 2\right)}^{4} \cos\left(x\right)}\;dx = \answer{{-\frac{7}{5} \, {\left(\sin\left(x\right) - 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10 \, \sin\left(x\right)}{{\left(\cos\left(x\right) - 6\right)}^{2}}}\;dx = \answer{{\frac{10}{\cos\left(x\right) - 6}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, \log\left(x + 9\right)}\;dx = \answer{{-4 \, {\left(x + 9\right)} \log\left(x + 9\right) + 4 \, x + 36}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10 \, \cos\left(\frac{1}{x^{2}} - 2\right)}{x^{3}}}\;dx = \answer{{-5 \, \sin\left(\frac{1}{x^{2}} - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{20 \, x}{{\left(x^{2} - 6\right)}^{3}}}\;dx = \answer{{\frac{5}{{\left(x^{2} - 6\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, e^{\left(\log\left(x\right) + 6\right)}}{x}}\;dx = \answer{{2 \, x e^{6}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{18 \, x^{2} \sin\left(x^{3} - 9\right)}\;dx = \answer{{-6 \, \cos\left(x^{3} - 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-8 \, e^{x} \sin\left(e^{x} + 6\right)}\;dx = \answer{{8 \, \cos\left(e^{x} + 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-8 \, {\left(\sin\left(x\right) - 4\right)}^{3} \cos\left(x\right)}\;dx = \answer{{-2 \, {\left(\sin\left(x\right) - 4\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-21 \, \sqrt{x^{3} + 10} x^{2}}\;dx = \answer{{-\frac{14}{3} \, {\left(x^{3} + 10\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(x^{4} + 9\right)}^{2} x^{3}}\;dx = \answer{{-\frac{1}{3} \, {\left(x^{4} + 9\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, {\left(\sin\left(x\right) - 6\right)}^{2} \cos\left(x\right)}\;dx = \answer{{-\frac{7}{3} \, {\left(\sin\left(x\right) - 6\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, {\left(e^{x} - 2\right)}^{4} e^{x}}\;dx = \answer{{-2 \, {\left(e^{x} - 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, {\left(\log\left(x\right) + 8\right)}^{3}}{x}}\;dx = \answer{{-\frac{7}{4} \, {\left(\log\left(x\right) + 8\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18 \, x^{2}}{{\left(x^{3} + 8\right)}^{3}}}\;dx = \answer{{-\frac{3}{{\left(x^{3} + 8\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, \log\left(\cos\left(x\right) + 8\right) \sin\left(x\right)}\;dx = \answer{{-5 \, {\left(\cos\left(x\right) + 8\right)} \log\left(\cos\left(x\right) + 8\right) + 5 \, \cos\left(x\right) + 40}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, {\left(x^{2} - 11\right)} x}\;dx = \answer{{{\left(x^{2} - 11\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{14 \, x}{x^{2} + 2}}\;dx = \answer{{7 \, \log\left(x^{2} + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, e^{\left(\frac{1}{x^{3}} + 10\right)}}{x^{4}}}\;dx = \answer{{e^{\left(\frac{1}{x^{3}} + 10\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8}{x {\left(\log\left(x\right) + 5\right)}}}\;dx = \answer{{8 \, \log\left(\log\left(x\right) + 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, {\left(\cos\left(x\right) - 3\right)}^{2} \sin\left(x\right)}\;dx = \answer{{-\frac{7}{3} \, {\left(\cos\left(x\right) - 3\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-8 \, {\left(x^{4} + 9\right)}^{2} x^{3}}\;dx = \answer{{-\frac{2}{3} \, {\left(x^{4} + 9\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{18 \, x e^{\left(x^{2} - 3\right)}}\;dx = \answer{{9 \, e^{\left(x^{2} - 3\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3}{x^{7}}}\;dx = \answer{{\frac{1}{2 \, x^{6}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, {\left(\sqrt{x} + 5\right)}^{3}}{2 \, \sqrt{x}}}\;dx = \answer{{\frac{3}{4} \, {\left(\sqrt{x} + 5\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{6 \, {\left(e^{x} + 2\right)}^{2} e^{x}}\;dx = \answer{{2 \, {\left(e^{x} + 2\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18 \, \sin\left(\frac{1}{x^{3}} + 4\right)}{x^{4}}}\;dx = \answer{{6 \, \cos\left(\frac{1}{x^{3}} + 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, {\left(e^{x} + 10\right)}^{2} e^{x}}\;dx = \answer{{\frac{7}{3} \, {\left(e^{x} + 10\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2}{x^{2} {\left(\frac{1}{x} - 5\right)}^{3}}}\;dx = \answer{{-\frac{1}{{\left(\frac{1}{x} - 5\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, {\left(x^{3} + 14\right)} x^{2}}\;dx = \answer{{\frac{1}{2} \, {\left(x^{3} + 14\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, \cos\left(\sqrt{x} - 6\right)}{2 \, \sqrt{x}}}\;dx = \answer{{-5 \, \sin\left(\sqrt{x} - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-21 \, {\left(x^{3} + 10\right)}^{2} x^{2}}\;dx = \answer{{-\frac{7}{3} \, {\left(x^{3} + 10\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, e^{\left(x + 3\right)}}\;dx = \answer{{5 \, e^{\left(x + 3\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, \sqrt{\cos\left(x\right) - 6} \sin\left(x\right)}\;dx = \answer{{-\frac{8}{3} \, {\left(\cos\left(x\right) - 6\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, {\left(x + 5\right)}^{3}}\;dx = \answer{{-\frac{9}{4} \, x^{4} - 45 \, x^{3} - \frac{675}{2} \, x^{2} - 1125 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{10 \, \cos\left(x\right)}{{\left(\sin\left(x\right) + 9\right)}^{2}}}\;dx = \answer{{\frac{10}{\sin\left(x\right) + 9}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{6 \, x \cos\left(x^{2} - 4\right)}\;dx = \answer{{3 \, \sin\left(x^{2} - 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{10 \, \cos\left(x\right)}{{\left(\sin\left(x\right) - 1\right)}^{3}}}\;dx = \answer{{\frac{5}{{\left(\sin\left(x\right) - 1\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-18 \, x \sin\left(x^{2} - 5\right)}\;dx = \answer{{9 \, \cos\left(x^{2} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, \cos\left(x\right)}{{\left(\sin\left(x\right) - 4\right)}^{2}}}\;dx = \answer{{\frac{9}{\sin\left(x\right) - 4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8}{x {\left(\log\left(x\right) + 4\right)}^{3}}}\;dx = \answer{{-\frac{4}{{\left(\log\left(x\right) + 4\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, {\left(\cos\left(x\right) - 10\right)}^{4} \sin\left(x\right)}\;dx = \answer{{-\frac{3}{5} \, {\left(\cos\left(x\right) - 10\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{21 \, x^{2} e^{\left(x^{3} - 9\right)}}\;dx = \answer{{7 \, e^{\left(x^{3} - 9\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, \sqrt{\cos\left(x\right) + 5} \sin\left(x\right)}\;dx = \answer{{-6 \, {\left(\cos\left(x\right) + 5\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{28 \, x^{3}}{x^{4} + 7}}\;dx = \answer{{7 \, \log\left(x^{4} + 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{12 \, \sqrt{\frac{1}{x^{3}} - 3}}{x^{4}}}\;dx = \answer{{-\frac{8}{3} \, {\left(\frac{1}{x^{3}} - 3\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{16 \, x^{3}}{x^{4} + 7}}\;dx = \answer{{4 \, \log\left(x^{4} + 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{24 \, {\left(x^{4} + 7\right)}^{3} x^{3}}\;dx = \answer{{\frac{3}{2} \, {\left(x^{4} + 7\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{36 \, {\left(x^{4} + 10\right)} x^{3}}\;dx = \answer{{\frac{9}{2} \, {\left(x^{4} + 10\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, {\left(e^{x} + 9\right)}^{4} e^{x}}\;dx = \answer{{{\left(e^{x} + 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{16 \, x^{3}}{{\left(x^{4} - 8\right)}^{2}}}\;dx = \answer{{\frac{4}{x^{4} - 8}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12 \, {\left(\frac{1}{x^{3}} + 9\right)}^{2}}{x^{4}}}\;dx = \answer{{\frac{4}{3} \, {\left(\frac{1}{x^{3}} + 9\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8}{{\left(x + 3\right)}^{2}}}\;dx = \answer{{-\frac{8}{x + 3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{6}{x - 4}}\;dx = \answer{{-6 \, \log\left(x - 4\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, {\left(\frac{1}{x^{2}} - 4\right)}}{x^{3}}}\;dx = \answer{{2 \, {\left(\frac{1}{x^{2}} - 4\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4}{x + 8}}\;dx = \answer{{-4 \, \log\left(x + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{12 \, {\left(\frac{1}{x^{2}} - 3\right)}^{3}}{x^{3}}}\;dx = \answer{{-\frac{3}{2} \, {\left(\frac{1}{x^{2}} - 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4}{{\left(x - 6\right)}^{2}}}\;dx = \answer{{\frac{4}{x - 6}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, x^{2} e^{\left(x^{3} - 5\right)}}\;dx = \answer{{-2 \, e^{\left(x^{3} - 5\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-28 \, \sqrt{x^{4} - 9} x^{3}}\;dx = \answer{{-\frac{14}{3} \, {\left(x^{4} - 9\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12 \, \cos\left(\frac{1}{x^{3}} - 2\right)}{x^{4}}}\;dx = \answer{{4 \, \sin\left(\frac{1}{x^{3}} - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, \sqrt{\frac{1}{x} - 5}}{x^{2}}}\;dx = \answer{{\frac{16}{3} \, {\left(\frac{1}{x} - 5\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{2 \, x e^{\left(x^{2} - 8\right)}}\;dx = \answer{{e^{\left(x^{2} - 8\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, \cos\left(x\right) \log\left(\sin\left(x\right) - 6\right)}\;dx = \answer{{9 \, {\left(\sin\left(x\right) - 6\right)} \log\left(\sin\left(x\right) - 6\right) - 9 \, \sin\left(x\right) + 54}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, e^{x}}{e^{x} + 1}}\;dx = \answer{{6 \, \log\left(e^{x} + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{12 \, x^{3} e^{\left(x^{4} - 5\right)}}\;dx = \answer{{3 \, e^{\left(x^{4} - 5\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\cos\left(x\right) \sin\left(\sin\left(x\right) - 6\right)}\;dx = \answer{{-\cos\left(\sin\left(x\right) - 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18 \, x^{2}}{x^{3} + 8}}\;dx = \answer{{6 \, \log\left(x^{3} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{27 \, \sqrt{\frac{1}{x^{3}} + 8}}{x^{4}}}\;dx = \answer{{6 \, {\left(\frac{1}{x^{3}} + 8\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4 \, {\left(\sqrt{x} - 1\right)}^{3}}{\sqrt{x}}}\;dx = \answer{{2 \, {\left(\sqrt{x} - 1\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8}{x^{2} {\left(\frac{1}{x} - 7\right)}^{2}}}\;dx = \answer{{\frac{8}{\frac{1}{x} - 7}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, {\left(\frac{1}{x} + 8\right)}^{4}}{x^{2}}}\;dx = \answer{{-\frac{3}{5} \, {\left(\frac{1}{x} + 8\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, e^{\left(\cos\left(x\right) + 10\right)} \sin\left(x\right)}\;dx = \answer{{-5 \, e^{\left(\cos\left(x\right) + 10\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{16 \, x e^{\left(x^{2} - 9\right)}}\;dx = \answer{{8 \, e^{\left(x^{2} - 9\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, \sqrt{x - 14}}\;dx = \answer{{-6 \, {\left(x - 14\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, \sqrt{\frac{1}{x^{2}} - 4}}{x^{3}}}\;dx = \answer{{-\frac{2}{3} \, {\left(\frac{1}{x^{2}} - 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-5 \, \sqrt{\cos\left(x\right) - 4} \sin\left(x\right)}\;dx = \answer{{\frac{10}{3} \, {\left(\cos\left(x\right) - 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, {\left(\frac{1}{x^{2}} + 9\right)}^{2}}{x^{3}}}\;dx = \answer{{-\frac{1}{3} \, {\left(\frac{1}{x^{2}} + 9\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{6 \, x \log\left(x^{2} + 1\right)}\;dx = \answer{{-3 \, x^{2} + 3 \, {\left(x^{2} + 1\right)} \log\left(x^{2} + 1\right) - 3}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{18}{x^{3} {\left(\frac{1}{x^{2}} + 7\right)}^{3}}}\;dx = \answer{{-\frac{9}{2 \, {\left(\frac{1}{x^{2}} + 7\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{6 \, {\left(\log\left(x\right) + 12\right)}}{x}}\;dx = \answer{{-3 \, {\left(\log\left(x\right) + 12\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{9 \, \sqrt{x^{3} - 7} x^{2}}\;dx = \answer{{2 \, {\left(x^{3} - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-24 \, x^{2} e^{\left(x^{3} - 8\right)}}\;dx = \answer{{-8 \, e^{\left(x^{3} - 8\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{\sqrt{x} + 3}{\sqrt{x}}}\;dx = \answer{{-{\left(\sqrt{x} + 3\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{5 \, \sqrt{\sin\left(x\right) + 6} \cos\left(x\right)}\;dx = \answer{{\frac{10}{3} \, {\left(\sin\left(x\right) + 6\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, \sqrt{\frac{1}{x^{3}} - 5}}{x^{4}}}\;dx = \answer{{2 \, {\left(\frac{1}{x^{3}} - 5\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, {\left(\cos\left(x\right) + 5\right)}^{3} \sin\left(x\right)}\;dx = \answer{{\frac{3}{4} \, {\left(\cos\left(x\right) + 5\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{30 \, x^{2} e^{\left(x^{3} - 4\right)}}\;dx = \answer{{10 \, e^{\left(x^{3} - 4\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4}{\sqrt{x} {\left(\sqrt{x} + 4\right)}^{3}}}\;dx = \answer{{\frac{4}{{\left(\sqrt{x} + 4\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{28 \, x^{3} \cos\left(x^{4} - 10\right)}\;dx = \answer{{7 \, \sin\left(x^{4} - 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, {\left(\cos\left(x\right) + 1\right)}^{4} \sin\left(x\right)}\;dx = \answer{{-\frac{4}{5} \, {\left(\cos\left(x\right) + 1\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, \log\left(\frac{1}{x} - 7\right)}{x^{2}}}\;dx = \answer{{-5 \, {\left(\frac{1}{x} - 7\right)} \log\left(\frac{1}{x} - 7\right) + \frac{5}{x} - 35}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, e^{\left(x + e^{x} - 8\right)}}\;dx = \answer{{8 \, e^{\left(e^{x} - 8\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{10 \, \sqrt{x + 4}}\;dx = \answer{{\frac{20}{3} \, {\left(x + 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, \sin\left(\sqrt{x} + 8\right)}{\sqrt{x}}}\;dx = \answer{{8 \, \cos\left(\sqrt{x} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{e^{x}}{{\left(e^{x} + 7\right)}^{3}}}\;dx = \answer{{-\frac{1}{2 \, {\left(e^{x} + 7\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(\cos\left(x\right) + 4\right)}^{2} \sin\left(x\right)}\;dx = \answer{{\frac{4}{3} \, {\left(\cos\left(x\right) + 4\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10}{x {\left(\log\left(x\right) + 4\right)}^{3}}}\;dx = \answer{{-\frac{5}{{\left(\log\left(x\right) + 4\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, \cos\left(e^{x} + 7\right) e^{x}}\;dx = \answer{{-4 \, \sin\left(e^{x} + 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, {\left(x + 10\right)}^{2}}\;dx = \answer{{x^{3} + 30 \, x^{2} + 300 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4}{\sqrt{x} {\left(\sqrt{x} - 2\right)}}}\;dx = \answer{{-8 \, \log\left(\sqrt{x} - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{20 \, \sin\left(\frac{1}{x^{2}} + 9\right)}{x^{3}}}\;dx = \answer{{10 \, \cos\left(\frac{1}{x^{2}} + 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{14 \, x \sin\left(x^{2} - 10\right)}\;dx = \answer{{-7 \, \cos\left(x^{2} - 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6}{x^{3} {\left(\frac{1}{x^{2}} + 7\right)}}}\;dx = \answer{{-3 \, \log\left(\frac{1}{x^{2}} + 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-21 \, {\left(x^{3} + 9\right)}^{4} x^{2}}\;dx = \answer{{-\frac{7}{5} \, {\left(x^{3} + 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-16 \, {\left(x^{2} + 8\right)} x}\;dx = \answer{{-4 \, {\left(x^{2} + 8\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, {\left(\cos\left(x\right) - 9\right)}^{3} \sin\left(x\right)}\;dx = \answer{{-2 \, {\left(\cos\left(x\right) - 9\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-8 \, x \cos\left(x^{2} - 7\right)}\;dx = \answer{{-4 \, \sin\left(x^{2} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{{\left(\log\left(x\right) + 5\right)}^{4}}{x}}\;dx = \answer{{-\frac{1}{5} \, {\left(\log\left(x\right) + 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{24 \, {\left(x^{3} - 1\right)}^{3} x^{2}}\;dx = \answer{{2 \, {\left(x^{3} - 1\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10 \, {\left(\frac{1}{x^{2}} - 4\right)}^{4}}{x^{3}}}\;dx = \answer{{-{\left(\frac{1}{x^{2}} - 4\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4}{x {\left(\log\left(x\right) - 6\right)}^{3}}}\;dx = \answer{{\frac{2}{{\left(\log\left(x\right) - 6\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, {\left(\sqrt{x} + 3\right)}^{3}}{\sqrt{x}}}\;dx = \answer{{-\frac{3}{2} \, {\left(\sqrt{x} + 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{20 \, e^{\left(\frac{1}{x^{2}} + 3\right)}}{x^{3}}}\;dx = \answer{{10 \, e^{\left(\frac{1}{x^{2}} + 3\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{14 \, \cos\left(\frac{1}{x^{2}} - 2\right)}{x^{3}}}\;dx = \answer{{-7 \, \sin\left(\frac{1}{x^{2}} - 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, \sqrt{\sin\left(x\right) + 2} \cos\left(x\right)}\;dx = \answer{{\frac{16}{3} \, {\left(\sin\left(x\right) + 2\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{30 \, {\left(x^{3} - 4\right)}^{4} x^{2}}\;dx = \answer{{2 \, {\left(x^{3} - 4\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8 \, \cos\left(x\right)}{\sin\left(x\right) - 5}}\;dx = \answer{{8 \, \log\left(\sin\left(x\right) - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5}{x {\left(\log\left(x\right) - 1\right)}}}\;dx = \answer{{-5 \, \log\left(\log\left(x\right) - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-5 \, \sin\left(x\right) \sin\left(\cos\left(x\right) - 5\right)}\;dx = \answer{{-5 \, \cos\left(\cos\left(x\right) - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, \sin\left(x\right) \sin\left(\cos\left(x\right) + 8\right)}\;dx = \answer{{-3 \, \cos\left(\cos\left(x\right) + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, e^{x}}{{\left(e^{x} + 2\right)}^{2}}}\;dx = \answer{{-\frac{2}{e^{x} + 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9}{x {\left(\log\left(x\right) + 2\right)}^{3}}}\;dx = \answer{{\frac{9}{2 \, {\left(\log\left(x\right) + 2\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5}{2 \, \sqrt{x} {\left(\sqrt{x} - 7\right)}}}\;dx = \answer{{-5 \, \log\left(\sqrt{x} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{36 \, x^{3}}{{\left(x^{4} + 6\right)}^{3}}}\;dx = \answer{{\frac{9}{2 \, {\left(x^{4} + 6\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-18 \, {\left(x^{2} - 6\right)}^{3} x}\;dx = \answer{{-\frac{9}{4} \, {\left(x^{2} - 6\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{{\left(\log\left(x\right) + 8\right)}^{2}}{x}}\;dx = \answer{{\frac{1}{3} \, {\left(\log\left(x\right) + 8\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, \sqrt{\sin\left(x\right) - 7} \cos\left(x\right)}\;dx = \answer{{-\frac{20}{3} \, {\left(\sin\left(x\right) - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, e^{x}}{{\left(e^{x} + 1\right)}^{3}}}\;dx = \answer{{\frac{7}{2 \, {\left(e^{x} + 1\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{30 \, \cos\left(\frac{1}{x^{3}} - 3\right)}{x^{4}}}\;dx = \answer{{10 \, \sin\left(\frac{1}{x^{3}} - 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{12 \, x}{x^{2} - 5}}\;dx = \answer{{6 \, \log\left(x^{2} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{24 \, {\left(\frac{1}{x^{3}} - 4\right)}^{4}}{x^{4}}}\;dx = \answer{{-\frac{8}{5} \, {\left(\frac{1}{x^{3}} - 4\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, \sin\left(\sqrt{x} + 3\right)}{\sqrt{x}}}\;dx = \answer{{-10 \, \cos\left(\sqrt{x} + 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, \sqrt{\sin\left(x\right) + 10} \cos\left(x\right)}\;dx = \answer{{\frac{8}{3} \, {\left(\sin\left(x\right) + 10\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{1}{x {\left(\log\left(x\right) - 5\right)}^{3}}}\;dx = \answer{{\frac{1}{2 \, {\left(\log\left(x\right) - 5\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{7 \, e^{\left(x - 2\right)}}\;dx = \answer{{7 \, e^{\left(x - 2\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, \sqrt{x^{2} + 4} x}\;dx = \answer{{\frac{4}{3} \, {\left(x^{2} + 4\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2}{x^{2} {\left(\frac{1}{x} + 4\right)}^{2}}}\;dx = \answer{{-\frac{2}{\frac{1}{x} + 4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, e^{\left(\log\left(x\right) - 4\right)}}{x}}\;dx = \answer{{-8 \, x e^{\left(-4\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7}{x {\left(\log\left(x\right) - 9\right)}^{2}}}\;dx = \answer{{\frac{7}{\log\left(x\right) - 9}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-30 \, {\left(x^{3} + 17\right)} x^{2}}\;dx = \answer{{-5 \, {\left(x^{3} + 17\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{12 \, \sqrt{x^{3} + 8} x^{2}}\;dx = \answer{{\frac{8}{3} \, {\left(x^{3} + 8\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8 \, \sin\left(x\right)}{\cos\left(x\right) + 2}}\;dx = \answer{{8 \, \log\left(\cos\left(x\right) + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2}{x^{3} {\left(\frac{1}{x^{2}} + 8\right)}}}\;dx = \answer{{\log\left(\frac{1}{x^{2}} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, {\left(\cos\left(x\right) + 10\right)}^{3} \sin\left(x\right)}\;dx = \answer{{\frac{7}{4} \, {\left(\cos\left(x\right) + 10\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12 \, \sin\left(\frac{1}{x^{2}} - 7\right)}{x^{3}}}\;dx = \answer{{-6 \, \cos\left(\frac{1}{x^{2}} - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, \cos\left(\log\left(x\right) - 7\right)}{x}}\;dx = \answer{{5 \, \sin\left(\log\left(x\right) - 7\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-18 \, \sqrt{x^{2} - 2} x}\;dx = \answer{{-6 \, {\left(x^{2} - 2\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8}{x^{2} {\left(\frac{1}{x} - 3\right)}}}\;dx = \answer{{8 \, \log\left(\frac{1}{x} - 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{16 \, x}{{\left(x^{2} - 2\right)}^{2}}}\;dx = \answer{{\frac{8}{x^{2} - 2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, {\left(x^{3} - 6\right)}^{3} x^{2}}\;dx = \answer{{-\frac{1}{2} \, {\left(x^{3} - 6\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, x^{2} \sin\left(x^{3} + 10\right)}\;dx = \answer{{2 \, \cos\left(x^{3} + 10\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, e^{x} \sin\left(e^{x} - 9\right)}\;dx = \answer{{3 \, \cos\left(e^{x} - 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{14 \, x e^{\left(x^{2} - 10\right)}}\;dx = \answer{{7 \, e^{\left(x^{2} - 10\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-30 \, x^{2} \log\left(x^{3} + 2\right)}\;dx = \answer{{10 \, x^{3} - 10 \, {\left(x^{3} + 2\right)} \log\left(x^{3} + 2\right) + 20}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{28 \, x^{3} \sin\left(x^{4} + 9\right)}\;dx = \answer{{-7 \, \cos\left(x^{4} + 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, \log\left(x - 1\right)}\;dx = \answer{{8 \, {\left(x - 1\right)} \log\left(x - 1\right) - 8 \, x + 8}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{20}{x^{3} {\left(\frac{1}{x^{2}} - 3\right)}^{3}}}\;dx = \answer{{-\frac{5}{{\left(\frac{1}{x^{2}} - 3\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{1}{2 \, \sqrt{x} {\left(\sqrt{x} - 10\right)}^{3}}}\;dx = \answer{{-\frac{1}{2 \, {\left(\sqrt{x} - 10\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10 \, {\left(\frac{1}{x^{2}} - 7\right)}^{2}}{x^{3}}}\;dx = \answer{{-\frac{5}{3} \, {\left(\frac{1}{x^{2}} - 7\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{28 \, x^{3}}{{\left(x^{4} - 5\right)}^{3}}}\;dx = \answer{{\frac{7}{2 \, {\left(x^{4} - 5\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{4 \, x e^{\left(x^{2} - 6\right)}}\;dx = \answer{{2 \, e^{\left(x^{2} - 6\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{12 \, {\left(x^{4} - 3\right)}^{2} x^{3}}\;dx = \answer{{{\left(x^{4} - 3\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9}{x^{2} {\left(\frac{1}{x} + 10\right)}^{2}}}\;dx = \answer{{-\frac{9}{\frac{1}{x} + 10}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{12 \, {\left(x^{3} + 9\right)}^{4} x^{2}}\;dx = \answer{{\frac{4}{5} \, {\left(x^{3} + 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-16 \, x^{7}}\;dx = \answer{{-2 \, x^{8}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, \sin\left(\frac{1}{x} + 9\right)}{x^{2}}}\;dx = \answer{{-7 \, \cos\left(\frac{1}{x} + 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, {\left(\sqrt{x} + 3\right)}^{4}}{2 \, \sqrt{x}}}\;dx = \answer{{-\frac{7}{5} \, {\left(\sqrt{x} + 3\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, e^{\left(\frac{1}{x} - 2\right)}}{x^{2}}}\;dx = \answer{{-6 \, e^{\left(\frac{1}{x} - 2\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{10 \, \sqrt{\log\left(x\right) + 1}}{x}}\;dx = \answer{{-\frac{20}{3} \, {\left(\log\left(x\right) + 1\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, {\left(\frac{1}{x} - 6\right)}^{4}}{x^{2}}}\;dx = \answer{{\frac{3}{5} \, {\left(\frac{1}{x} - 6\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, {\left(e^{x} - 7\right)}^{2} e^{x}}\;dx = \answer{{-\frac{7}{3} \, {\left(e^{x} - 7\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, {\left(\sin\left(x\right) + 7\right)}^{2} \cos\left(x\right)}\;dx = \answer{{-\frac{7}{3} \, {\left(\sin\left(x\right) + 7\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{30 \, e^{\left(\frac{1}{x^{3}} + 2\right)}}{x^{4}}}\;dx = \answer{{10 \, e^{\left(\frac{1}{x^{3}} + 2\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, {\left(\cos\left(x\right) + 5\right)}^{4} \sin\left(x\right)}\;dx = \answer{{\frac{6}{5} \, {\left(\cos\left(x\right) + 5\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, {\left(x + 3\right)}^{2}}\;dx = \answer{{-\frac{7}{3} \, x^{3} - 21 \, x^{2} - 63 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, \sin\left(\frac{1}{x^{3}} + 8\right)}{x^{4}}}\;dx = \answer{{\cos\left(\frac{1}{x^{3}} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, e^{\left(\frac{1}{x} - 9\right)}}{x^{2}}}\;dx = \answer{{-5 \, e^{\left(\frac{1}{x} - 9\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, {\left(\frac{1}{x} + 10\right)}^{3}}{x^{2}}}\;dx = \answer{{\frac{1}{2} \, {\left(\frac{1}{x} + 10\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{32 \, {\left(x^{4} + 2\right)}^{4} x^{3}}\;dx = \answer{{\frac{8}{5} \, {\left(x^{4} + 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18}{x^{3} {\left(\frac{1}{x^{2}} - 5\right)}}}\;dx = \answer{{-9 \, \log\left(\frac{1}{x^{2}} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{6}{x^{4} {\left(\frac{1}{x^{3}} + 4\right)}^{3}}}\;dx = \answer{{-\frac{1}{{\left(\frac{1}{x^{3}} + 4\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, e^{x}}{{\left(e^{x} + 10\right)}^{3}}}\;dx = \answer{{\frac{3}{2 \, {\left(e^{x} + 10\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, e^{\left(\sqrt{x} - 7\right)}}{\sqrt{x}}}\;dx = \answer{{-8 \, e^{\left(\sqrt{x} - 7\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{4 \, e^{x}}{{\left(e^{x} - 4\right)}^{2}}}\;dx = \answer{{-\frac{4}{e^{x} - 4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{18 \, {\left(\frac{1}{x^{3}} - 3\right)}^{3}}{x^{4}}}\;dx = \answer{{\frac{3}{2} \, {\left(\frac{1}{x^{3}} - 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-10 \, e^{\left(x + e^{x} - 7\right)}}\;dx = \answer{{-10 \, e^{\left(e^{x} - 7\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{8}{x {\left(\log\left(x\right) - 1\right)}}}\;dx = \answer{{-8 \, \log\left(\log\left(x\right) - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\cos\left(\cos\left(x\right) + 2\right) \sin\left(x\right)}\;dx = \answer{{\sin\left(\cos\left(x\right) + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{18 \, {\left(x^{2} + 7\right)}^{2} x}\;dx = \answer{{3 \, {\left(x^{2} + 7\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-9 \, \sin\left(x + 2\right)}\;dx = \answer{{9 \, \cos\left(x + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, \sin\left(\log\left(x\right) + 1\right)}{x}}\;dx = \answer{{-3 \, \cos\left(\log\left(x\right) + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{5 \, {\left(\sqrt{x} - 2\right)}^{4}}{\sqrt{x}}}\;dx = \answer{{-2 \, {\left(\sqrt{x} - 2\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, \sin\left(\log\left(x\right) - 1\right)}{x}}\;dx = \answer{{-6 \, \cos\left(\log\left(x\right) - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-5 \, x - 60}\;dx = \answer{{-\frac{5}{2} \, x^{2} - 60 \, x}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, {\left(\log\left(x\right) + 7\right)}^{4}}{x}}\;dx = \answer{{\frac{3}{5} \, {\left(\log\left(x\right) + 7\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{9 \, {\left(\frac{1}{x} + 2\right)}}{x^{2}}}\;dx = \answer{{\frac{9}{2} \, {\left(\frac{1}{x} + 2\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3}{x {\left(\log\left(x\right) + 5\right)}}}\;dx = \answer{{-3 \, \log\left(\log\left(x\right) + 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{9 \, {\left(\log\left(x\right) + 7\right)}^{4}}{x}}\;dx = \answer{{\frac{9}{5} \, {\left(\log\left(x\right) + 7\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-4 \, {\left(\cos\left(x\right) - 6\right)}^{2} \sin\left(x\right)}\;dx = \answer{{\frac{4}{3} \, {\left(\cos\left(x\right) - 6\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{16 \, {\left(x^{4} - 9\right)}^{4} x^{3}}\;dx = \answer{{\frac{4}{5} \, {\left(x^{4} - 9\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{18 \, \cos\left(\frac{1}{x^{3}} + 2\right)}{x^{4}}}\;dx = \answer{{-6 \, \sin\left(\frac{1}{x^{3}} + 2\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{20 \, x}{{\left(x^{2} + 7\right)}^{2}}}\;dx = \answer{{-\frac{10}{x^{2} + 7}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, \cos\left(x\right) \log\left(\sin\left(x\right) + 10\right)}\;dx = \answer{{-7 \, {\left(\sin\left(x\right) + 10\right)} \log\left(\sin\left(x\right) + 10\right) + 7 \, \sin\left(x\right) + 70}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{27 \, x^{2} e^{\left(x^{3} - 2\right)}}\;dx = \answer{{9 \, e^{\left(x^{3} - 2\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3}{x {\left(\log\left(x\right) + 7\right)}^{2}}}\;dx = \answer{{\frac{3}{\log\left(x\right) + 7}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{10 \, \cos\left(\frac{1}{x} + 8\right)}{x^{2}}}\;dx = \answer{{10 \, \sin\left(\frac{1}{x} + 8\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-7 \, \sqrt{\sin\left(x\right) - 8} \cos\left(x\right)}\;dx = \answer{{-\frac{14}{3} \, {\left(\sin\left(x\right) - 8\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{3 \, {\left(\sqrt{x} - 7\right)}}{2 \, \sqrt{x}}}\;dx = \answer{{-\frac{3}{2} \, {\left(\sqrt{x} - 7\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, \cos\left(\cos\left(x\right) - 1\right) \sin\left(x\right)}\;dx = \answer{{-8 \, \sin\left(\cos\left(x\right) - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, {\left(\cos\left(x\right) - 10\right)}^{3} \sin\left(x\right)}\;dx = \answer{{-2 \, {\left(\cos\left(x\right) - 10\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{30}{x^{4} {\left(\frac{1}{x^{3}} - 5\right)}}}\;dx = \answer{{-10 \, \log\left(\frac{1}{x^{3}} - 5\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{3 \, e^{\left(x + e^{x} + 7\right)}}\;dx = \answer{{3 \, e^{\left(e^{x} + 7\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{12 \, x}{{\left(x^{2} + 3\right)}^{3}}}\;dx = \answer{{\frac{3}{{\left(x^{2} + 3\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{8}{{\left(x + 5\right)}^{3}}}\;dx = \answer{{-\frac{4}{{\left(x + 5\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, {\left(\sqrt{x} - 6\right)}^{3}}{\sqrt{x}}}\;dx = \answer{{{\left(\sqrt{x} - 6\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, x^{3}}{{\left(x^{4} + 3\right)}^{2}}}\;dx = \answer{{\frac{1}{x^{4} + 3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-21 \, {\left(x^{3} + 9\right)} x^{2}}\;dx = \answer{{-\frac{7}{2} \, {\left(x^{3} + 9\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-8 \, \cos\left(x\right) \log\left(\sin\left(x\right) - 8\right)}\;dx = \answer{{-8 \, {\left(\sin\left(x\right) - 8\right)} \log\left(\sin\left(x\right) - 8\right) + 8 \, \sin\left(x\right) - 64}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-3 \, \cos\left(x\right) e^{\left(\sin\left(x\right) - 1\right)}}\;dx = \answer{{-3 \, e^{\left(\sin\left(x\right) - 1\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{2 \, {\left(\log\left(x\right) + 6\right)}^{2}}{x}}\;dx = \answer{{\frac{2}{3} \, {\left(\log\left(x\right) + 6\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, e^{x}}{e^{x} + 6}}\;dx = \answer{{-2 \, \log\left(e^{x} + 6\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-5 \, {\left(\sin\left(x\right) - 3\right)}^{3} \cos\left(x\right)}\;dx = \answer{{-\frac{5}{4} \, {\left(\sin\left(x\right) - 3\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{6 \, \sin\left(x\right)}{{\left(\cos\left(x\right) + 9\right)}^{2}}}\;dx = \answer{{\frac{6}{\cos\left(x\right) + 9}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{18 \, x^{2} \log\left(x^{3} - 8\right)}\;dx = \answer{{-6 \, x^{3} + 6 \, {\left(x^{3} - 8\right)} \log\left(x^{3} - 8\right) + 48}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-30 \, {\left(x^{3} + 10\right)}^{4} x^{2}}\;dx = \answer{{-2 \, {\left(x^{3} + 10\right)}^{5}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{20 \, \log\left(\frac{1}{x^{2}} - 1\right)}{x^{3}}}\;dx = \answer{{-10 \, {\left(\frac{1}{x^{2}} - 1\right)} \log\left(\frac{1}{x^{2}} - 1\right) + \frac{10}{x^{2}} - 10}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{\sqrt{\log\left(x\right) - 6}}{x}}\;dx = \answer{{-\frac{2}{3} \, {\left(\log\left(x\right) - 6\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{3 \, \cos\left(\sqrt{x} - 1\right)}{\sqrt{x}}}\;dx = \answer{{6 \, \sin\left(\sqrt{x} - 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{2 \, {\left(\sqrt{x} - 11\right)}}{\sqrt{x}}}\;dx = \answer{{-2 \, {\left(\sqrt{x} - 11\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{12 \, x^{2} \cos\left(x^{3} - 9\right)}\;dx = \answer{{4 \, \sin\left(x^{3} - 9\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{8 \, x \log\left(x^{2} + 7\right)}\;dx = \answer{{-4 \, x^{2} + 4 \, {\left(x^{2} + 7\right)} \log\left(x^{2} + 7\right) - 28}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{5 \, {\left(\log\left(x\right) + 4\right)}^{2}}{x}}\;dx = \answer{{\frac{5}{3} \, {\left(\log\left(x\right) + 4\right)}^{3}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{10 \, e^{\left(\log\left(x\right) - 5\right)}}{x}}\;dx = \answer{{10 \, x e^{\left(-5\right)}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, \sqrt{\sqrt{x} - 7}}{\sqrt{x}}}\;dx = \answer{{-\frac{16}{3} \, {\left(\sqrt{x} - 7\right)}^{\frac{3}{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{7 \, \sin\left(x\right)}{{\left(\cos\left(x\right) + 2\right)}^{3}}}\;dx = \answer{{-\frac{7}{2 \, {\left(\cos\left(x\right) + 2\right)}^{2}}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{\frac{15 \, \cos\left(\frac{1}{x^{3}} - 3\right)}{x^{4}}}\;dx = \answer{{-5 \, \sin\left(\frac{1}{x^{3}} - 3\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-6 \, {\left(\cos\left(x\right) + 8\right)}^{3} \sin\left(x\right)}\;dx = \answer{{\frac{3}{2} \, {\left(\cos\left(x\right) + 8\right)}^{4}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{{\left(\cos\left(x\right) - 4\right)} \sin\left(x\right)}\;dx = \answer{{-\frac{1}{2} \, {\left(\cos\left(x\right) - 4\right)}^{2}}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Compute the indefinite integral:

\input{2311-Compute-Integral-0017.HELP.tex}

\[\int\;{-\frac{4 \, \sin\left(\frac{1}{x^{2}} + 1\right)}{x^{3}}}\;dx = \answer{{-2 \, \cos\left(\frac{1}{x^{2}} + 1\right)}+C}\]
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



