%%%%%%%%%%%%%%%%%%%%%%%
%%\tagged{Cat@One, Cat@Two, Cat@Three, Cat@Four, Cat@Five, Ans@MC, Type@Concept, Topic@Integral, Sub@Definite, Sub@Theorems_FTC, Sub@Rational}{

\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{9}{x^{4}}}\;dx = {\frac{3}{x^{3}}}\Bigg\vert_{-5}^{5} = {\frac{6}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {\frac{4}{x}}\;dx = {4 \, \log\left(x\right)}\Bigg\vert_{-1}^{1} = {-4 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-2}^{6} = {\frac{5}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{5}{x^{4}}}\;dx = {\frac{5}{3 \, x^{3}}}\Bigg\vert_{-1}^{3} = {\frac{140}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {-\frac{1}{x}}\;dx = {-\log\left(x\right)}\Bigg\vert_{-1}^{4} = {i \, \pi - \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {\frac{10}{x}}\;dx = {10 \, \log\left(x\right)}\Bigg\vert_{-4}^{6} = {-10 i \, \pi + 10 \, \log\left(6\right) - 10 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{9}{x^{3}}}\;dx = {-\frac{9}{2 \, x^{2}}}\Bigg\vert_{-5}^{1} = {-\frac{108}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {\frac{10}{x^{2}}}\;dx = {-\frac{10}{x}}\Bigg\vert_{-3}^{3} = {-\frac{20}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {\frac{6}{x^{2}}}\;dx = {-\frac{6}{x}}\Bigg\vert_{-2}^{5} = {-\frac{21}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {-\frac{3}{x^{4}}}\;dx = {\frac{1}{x^{3}}}\Bigg\vert_{-3}^{2} = {\frac{35}{216}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{6}{x^{3}}}\;dx = {\frac{3}{x^{2}}}\Bigg\vert_{-5}^{5} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-1}^{5} = {9 i \, \pi - 9 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-6}^{1} = {8 i \, \pi + 8 \, \log\left(6\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {\frac{4}{x^{3}}}\;dx = {-\frac{2}{x^{2}}}\Bigg\vert_{-4}^{2} = {-\frac{3}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{6}{x^{3}}}\;dx = {\frac{3}{x^{2}}}\Bigg\vert_{-1}^{6} = {-\frac{35}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{5}{x^{3}}}\;dx = {\frac{5}{2 \, x^{2}}}\Bigg\vert_{-2}^{6} = {-\frac{5}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {\frac{9}{x^{2}}}\;dx = {-\frac{9}{x}}\Bigg\vert_{-6}^{6} = {-3}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-6}^{2} = {\frac{4}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{3}{x}}\;dx = {-3 \, \log\left(x\right)}\Bigg\vert_{-2}^{3} = {3 i \, \pi - 3 \, \log\left(3\right) + 3 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {\frac{1}{x^{4}}}\;dx = {-\frac{1}{3 \, x^{3}}}\Bigg\vert_{-1}^{5} = {-\frac{42}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {-\frac{5}{x^{3}}}\;dx = {\frac{5}{2 \, x^{2}}}\Bigg\vert_{-5}^{3} = {\frac{8}{45}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-4}^{2} = {-\frac{3}{16}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-1}^{1} = {9 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {-\frac{4}{x^{2}}}\;dx = {\frac{4}{x}}\Bigg\vert_{-1}^{2} = {6}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{7}{x^{4}}}\;dx = {-\frac{7}{3 \, x^{3}}}\Bigg\vert_{-5}^{2} = {-\frac{931}{3000}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{9}{x^{4}}}\;dx = {-\frac{3}{x^{3}}}\Bigg\vert_{-4}^{5} = {-\frac{567}{8000}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{2}{x^{3}}}\;dx = {-\frac{1}{x^{2}}}\Bigg\vert_{-5}^{1} = {-\frac{24}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {-\frac{5}{x}}\;dx = {-5 \, \log\left(x\right)}\Bigg\vert_{-5}^{1} = {5 i \, \pi + 5 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{5}{x}}\;dx = {-5 \, \log\left(x\right)}\Bigg\vert_{-1}^{3} = {5 i \, \pi - 5 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{2}{x^{2}}}\;dx = {\frac{2}{x}}\Bigg\vert_{-5}^{5} = {\frac{4}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-2}^{2} = {\frac{5}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{4}{x}}\;dx = {4 \, \log\left(x\right)}\Bigg\vert_{-5}^{4} = {-4 i \, \pi - 4 \, \log\left(5\right) + 4 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {-\frac{9}{x^{2}}}\;dx = {\frac{9}{x}}\Bigg\vert_{-1}^{4} = {\frac{45}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{2}{x}}\;dx = {-2 \, \log\left(x\right)}\Bigg\vert_{-2}^{3} = {2 i \, \pi - 2 \, \log\left(3\right) + 2 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {\frac{2}{x^{3}}}\;dx = {-\frac{1}{x^{2}}}\Bigg\vert_{-6}^{1} = {-\frac{35}{36}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{1}{x^{4}}}\;dx = {\frac{1}{3 \, x^{3}}}\Bigg\vert_{-1}^{6} = {\frac{217}{648}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{7}{x^{2}}}\;dx = {-\frac{7}{x}}\Bigg\vert_{-2}^{1} = {-\frac{21}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {-\frac{5}{x^{3}}}\;dx = {\frac{5}{2 \, x^{2}}}\Bigg\vert_{-4}^{4} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {-\frac{3}{x^{3}}}\;dx = {\frac{3}{2 \, x^{2}}}\Bigg\vert_{-1}^{2} = {-\frac{9}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {-\frac{10}{x^{2}}}\;dx = {\frac{10}{x}}\Bigg\vert_{-4}^{6} = {\frac{25}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-6}^{6} = {\frac{1}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{1}{x}}\;dx = {-\log\left(x\right)}\Bigg\vert_{-1}^{6} = {i \, \pi - \log\left(6\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{6}{x^{3}}}\;dx = {-\frac{3}{x^{2}}}\Bigg\vert_{-4}^{4} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{1}{x^{3}}}\;dx = {-\frac{1}{2 \, x^{2}}}\Bigg\vert_{-2}^{6} = {\frac{1}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-3}^{3} = {-\frac{20}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{5}{x^{4}}}\;dx = {\frac{5}{3 \, x^{3}}}\Bigg\vert_{-2}^{2} = {\frac{5}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-6}^{2} = {-\frac{8}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-1}^{3} = {-i \, \pi + \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {-\frac{7}{x}}\;dx = {-7 \, \log\left(x\right)}\Bigg\vert_{-6}^{2} = {7 i \, \pi + 7 \, \log\left(6\right) - 7 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {-\frac{4}{x^{2}}}\;dx = {\frac{4}{x}}\Bigg\vert_{-6}^{2} = {\frac{8}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {\frac{8}{x^{4}}}\;dx = {-\frac{8}{3 \, x^{3}}}\Bigg\vert_{-3}^{2} = {-\frac{35}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {\frac{10}{x}}\;dx = {10 \, \log\left(x\right)}\Bigg\vert_{-6}^{1} = {-10 i \, \pi - 10 \, \log\left(6\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-2}^{4} = {\frac{15}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{3}{x}}\;dx = {3 \, \log\left(x\right)}\Bigg\vert_{-2}^{1} = {-3 i \, \pi - 3 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{3}{x^{3}}}\;dx = {-\frac{3}{2 \, x^{2}}}\Bigg\vert_{-6}^{2} = {-\frac{1}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{9}{x^{4}}}\;dx = {\frac{3}{x^{3}}}\Bigg\vert_{-2}^{2} = {\frac{3}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {-\frac{5}{x^{4}}}\;dx = {\frac{5}{3 \, x^{3}}}\Bigg\vert_{-2}^{4} = {\frac{15}{64}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-1}^{5} = {\frac{36}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-5}^{3} = {8 i \, \pi + 8 \, \log\left(5\right) - 8 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-4}^{4} = {-i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{5}{x}}\;dx = {5 \, \log\left(x\right)}\Bigg\vert_{-4}^{5} = {-5 i \, \pi + 5 \, \log\left(5\right) - 5 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-2}^{3} = {-\frac{5}{18}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{8}{x^{3}}}\;dx = {\frac{4}{x^{2}}}\Bigg\vert_{-3}^{3} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-4}^{5} = {\frac{441}{8000}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-4}^{4} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-1}^{4} = {\frac{65}{96}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-2}^{1} = {9}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-6}^{3} = {9 i \, \pi + 9 \, \log\left(6\right) - 9 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-5}^{2} = {9 i \, \pi + 9 \, \log\left(5\right) - 9 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-4}^{4} = {-2}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {\frac{10}{x^{2}}}\;dx = {-\frac{10}{x}}\Bigg\vert_{-6}^{4} = {-\frac{25}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {-\frac{8}{x^{3}}}\;dx = {\frac{4}{x^{2}}}\Bigg\vert_{-4}^{2} = {\frac{3}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{9}{x^{2}}}\;dx = {-\frac{9}{x}}\Bigg\vert_{-5}^{2} = {-\frac{63}{10}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{7}{x}}\;dx = {7 \, \log\left(x\right)}\Bigg\vert_{-4}^{5} = {-7 i \, \pi + 7 \, \log\left(5\right) - 7 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{1}{x^{2}}}\;dx = {\frac{1}{x}}\Bigg\vert_{-3}^{3} = {\frac{2}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{5} {\frac{8}{x^{4}}}\;dx = {-\frac{8}{3 \, x^{3}}}\Bigg\vert_{-6}^{5} = {-\frac{341}{10125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {-\frac{2}{x^{3}}}\;dx = {\frac{1}{x^{2}}}\Bigg\vert_{-4}^{2} = {\frac{3}{16}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {-\frac{5}{x^{3}}}\;dx = {\frac{5}{2 \, x^{2}}}\Bigg\vert_{-6}^{2} = {\frac{5}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{3}{x}}\;dx = {-3 \, \log\left(x\right)}\Bigg\vert_{-1}^{1} = {3 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{7}{x^{4}}}\;dx = {-\frac{7}{3 \, x^{3}}}\Bigg\vert_{-2}^{6} = {-\frac{49}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{6}{x}}\;dx = {6 \, \log\left(x\right)}\Bigg\vert_{-4}^{4} = {-6 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{7}{x^{3}}}\;dx = {\frac{7}{2 \, x^{2}}}\Bigg\vert_{-1}^{1} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{1} {-\frac{4}{x^{4}}}\;dx = {\frac{4}{3 \, x^{3}}}\Bigg\vert_{-3}^{1} = {\frac{112}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{9}{x^{2}}}\;dx = {\frac{9}{x}}\Bigg\vert_{-5}^{5} = {\frac{18}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{8}{x^{4}}}\;dx = {\frac{8}{3 \, x^{3}}}\Bigg\vert_{-3}^{3} = {\frac{16}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {\frac{9}{x^{2}}}\;dx = {-\frac{9}{x}}\Bigg\vert_{-4}^{1} = {-\frac{45}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {\frac{8}{x}}\;dx = {8 \, \log\left(x\right)}\Bigg\vert_{-2}^{2} = {-8 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {\frac{5}{x^{2}}}\;dx = {-\frac{5}{x}}\Bigg\vert_{-2}^{4} = {-\frac{15}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {-\frac{1}{x}}\;dx = {-\log\left(x\right)}\Bigg\vert_{-4}^{2} = {i \, \pi + \log\left(4\right) - \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{8}{x^{2}}}\;dx = {-\frac{8}{x}}\Bigg\vert_{-1}^{6} = {-\frac{28}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{6}{x}}\;dx = {6 \, \log\left(x\right)}\Bigg\vert_{-5}^{2} = {-6 i \, \pi - 6 \, \log\left(5\right) + 6 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {-\frac{7}{x^{3}}}\;dx = {\frac{7}{2 \, x^{2}}}\Bigg\vert_{-4}^{3} = {\frac{49}{288}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {\frac{1}{x^{4}}}\;dx = {-\frac{1}{3 \, x^{3}}}\Bigg\vert_{-6}^{6} = {-\frac{1}{324}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{8}{x^{3}}}\;dx = {\frac{4}{x^{2}}}\Bigg\vert_{-1}^{6} = {-\frac{35}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{1} {\frac{7}{x^{3}}}\;dx = {-\frac{7}{2 \, x^{2}}}\Bigg\vert_{-3}^{1} = {-\frac{28}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{3}{x^{3}}}\;dx = {\frac{3}{2 \, x^{2}}}\Bigg\vert_{-3}^{4} = {-\frac{7}{96}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {\frac{8}{x}}\;dx = {8 \, \log\left(x\right)}\Bigg\vert_{-4}^{1} = {-8 i \, \pi - 8 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-4}^{5} = {-\frac{63}{800}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{3}{x^{2}}}\;dx = {-\frac{3}{x}}\Bigg\vert_{-6}^{2} = {-2}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{9}{x^{2}}}\;dx = {\frac{9}{x}}\Bigg\vert_{-2}^{3} = {\frac{15}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {\frac{8}{x}}\;dx = {8 \, \log\left(x\right)}\Bigg\vert_{-4}^{6} = {-8 i \, \pi + 8 \, \log\left(6\right) - 8 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{9}{x^{3}}}\;dx = {\frac{9}{2 \, x^{2}}}\Bigg\vert_{-1}^{3} = {-4}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {\frac{3}{x^{2}}}\;dx = {-\frac{3}{x}}\Bigg\vert_{-2}^{5} = {-\frac{21}{10}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-2}^{2} = {\frac{1}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{10}{x^{2}}}\;dx = {-\frac{10}{x}}\Bigg\vert_{-1}^{6} = {-\frac{35}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {-\frac{2}{x^{3}}}\;dx = {\frac{1}{x^{2}}}\Bigg\vert_{-6}^{1} = {\frac{35}{36}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {-\frac{9}{x^{4}}}\;dx = {\frac{3}{x^{3}}}\Bigg\vert_{-1}^{4} = {\frac{195}{64}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{7}{x}}\;dx = {-7 \, \log\left(x\right)}\Bigg\vert_{-3}^{4} = {7 i \, \pi - 7 \, \log\left(4\right) + 7 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {-\frac{3}{x^{4}}}\;dx = {\frac{1}{x^{3}}}\Bigg\vert_{-5}^{1} = {\frac{126}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {-\frac{1}{x}}\;dx = {-\log\left(x\right)}\Bigg\vert_{-5}^{2} = {i \, \pi + \log\left(5\right) - \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {\frac{10}{x^{3}}}\;dx = {-\frac{5}{x^{2}}}\Bigg\vert_{-3}^{5} = {\frac{16}{45}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{1}{x^{4}}}\;dx = {\frac{1}{3 \, x^{3}}}\Bigg\vert_{-2}^{6} = {\frac{7}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {\frac{7}{x^{4}}}\;dx = {-\frac{7}{3 \, x^{3}}}\Bigg\vert_{-1}^{3} = {-\frac{196}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{7}{x^{2}}}\;dx = {\frac{7}{x}}\Bigg\vert_{-2}^{6} = {\frac{14}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{4}{x^{4}}}\;dx = {\frac{4}{3 \, x^{3}}}\Bigg\vert_{-3}^{5} = {\frac{608}{10125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{4}{x}}\;dx = {4 \, \log\left(x\right)}\Bigg\vert_{-2}^{6} = {-4 i \, \pi + 4 \, \log\left(6\right) - 4 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {\frac{1}{x^{3}}}\;dx = {-\frac{1}{2 \, x^{2}}}\Bigg\vert_{-3}^{3} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{2}{x^{3}}}\;dx = {\frac{1}{x^{2}}}\Bigg\vert_{-4}^{1} = {\frac{15}{16}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {\frac{6}{x}}\;dx = {6 \, \log\left(x\right)}\Bigg\vert_{-3}^{4} = {-6 i \, \pi + 6 \, \log\left(4\right) - 6 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{2}{x}}\;dx = {-2 \, \log\left(x\right)}\Bigg\vert_{-3}^{5} = {2 i \, \pi - 2 \, \log\left(5\right) + 2 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {-\frac{9}{x^{3}}}\;dx = {\frac{9}{2 \, x^{2}}}\Bigg\vert_{-5}^{3} = {\frac{8}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-4}^{4} = {-\frac{5}{48}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {\frac{7}{x^{4}}}\;dx = {-\frac{7}{3 \, x^{3}}}\Bigg\vert_{-5}^{3} = {-\frac{1064}{10125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{6}{x^{3}}}\;dx = {-\frac{3}{x^{2}}}\Bigg\vert_{-4}^{5} = {\frac{27}{400}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-2}^{5} = {\frac{21}{40}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-6}^{4} = {\frac{35}{2592}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {-\frac{1}{x^{4}}}\;dx = {\frac{1}{3 \, x^{3}}}\Bigg\vert_{-6}^{2} = {\frac{7}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{1} {-\frac{9}{x^{4}}}\;dx = {\frac{3}{x^{3}}}\Bigg\vert_{-3}^{1} = {\frac{28}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{2}{x^{4}}}\;dx = {-\frac{2}{3 \, x^{3}}}\Bigg\vert_{-5}^{4} = {-\frac{63}{4000}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {\frac{1}{x^{2}}}\;dx = {-\frac{1}{x}}\Bigg\vert_{-6}^{3} = {-\frac{1}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{8}{x^{3}}}\;dx = {-\frac{4}{x^{2}}}\Bigg\vert_{-4}^{5} = {\frac{9}{100}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {-\frac{5}{x}}\;dx = {-5 \, \log\left(x\right)}\Bigg\vert_{-2}^{4} = {5 i \, \pi - 5 \, \log\left(4\right) + 5 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {\frac{1}{x^{2}}}\;dx = {-\frac{1}{x}}\Bigg\vert_{-2}^{3} = {-\frac{5}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-6}^{3} = {-2}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-2}^{6} = {-\frac{8}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {-\frac{3}{x^{2}}}\;dx = {\frac{3}{x}}\Bigg\vert_{-3}^{2} = {\frac{5}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {\frac{10}{x^{3}}}\;dx = {-\frac{5}{x^{2}}}\Bigg\vert_{-3}^{4} = {\frac{35}{144}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{5} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-6}^{5} = {\frac{341}{8100}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{5}{x}}\;dx = {-5 \, \log\left(x\right)}\Bigg\vert_{-4}^{1} = {5 i \, \pi + 5 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{1} {\frac{9}{x^{2}}}\;dx = {-\frac{9}{x}}\Bigg\vert_{-3}^{1} = {-12}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{7}{x}}\;dx = {7 \, \log\left(x\right)}\Bigg\vert_{-2}^{6} = {-7 i \, \pi + 7 \, \log\left(6\right) - 7 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {-\frac{3}{x^{3}}}\;dx = {\frac{3}{2 \, x^{2}}}\Bigg\vert_{-2}^{5} = {-\frac{63}{200}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-3}^{4} = {-\frac{7}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-2}^{3} = {-\frac{35}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{5} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-6}^{5} = {9 i \, \pi + 9 \, \log\left(6\right) - 9 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-5}^{4} = {-i \, \pi - \log\left(5\right) + \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{1}{x^{4}}}\;dx = {-\frac{1}{3 \, x^{3}}}\Bigg\vert_{-5}^{1} = {-\frac{42}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-3}^{4} = {\frac{455}{2592}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{4}{x^{3}}}\;dx = {-\frac{2}{x^{2}}}\Bigg\vert_{-5}^{1} = {-\frac{48}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-5}^{1} = {\frac{48}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{9}{x^{3}}}\;dx = {\frac{9}{2 \, x^{2}}}\Bigg\vert_{-1}^{6} = {-\frac{35}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {\frac{7}{x^{4}}}\;dx = {-\frac{7}{3 \, x^{3}}}\Bigg\vert_{-4}^{6} = {-\frac{245}{5184}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-5}^{6} = {8 i \, \pi - 8 \, \log\left(6\right) + 8 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-6}^{3} = {-\frac{1}{18}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {-\frac{8}{x^{3}}}\;dx = {\frac{4}{x^{2}}}\Bigg\vert_{-3}^{2} = {\frac{5}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{4}{x}}\;dx = {-4 \, \log\left(x\right)}\Bigg\vert_{-1}^{3} = {4 i \, \pi - 4 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-1}^{3} = {8}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-2}^{2} = {\frac{7}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {\frac{5}{x}}\;dx = {5 \, \log\left(x\right)}\Bigg\vert_{-1}^{4} = {-5 i \, \pi + 5 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {-\frac{5}{x^{4}}}\;dx = {\frac{5}{3 \, x^{3}}}\Bigg\vert_{-5}^{6} = {\frac{341}{16200}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {-\frac{1}{x}}\;dx = {-\log\left(x\right)}\Bigg\vert_{-4}^{4} = {i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {-\frac{5}{x^{3}}}\;dx = {\frac{5}{2 \, x^{2}}}\Bigg\vert_{-2}^{1} = {\frac{15}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {\frac{1}{x^{2}}}\;dx = {-\frac{1}{x}}\Bigg\vert_{-1}^{1} = {-2}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-2}^{5} = {9 i \, \pi - 9 \, \log\left(5\right) + 9 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {\frac{1}{x^{3}}}\;dx = {-\frac{1}{2 \, x^{2}}}\Bigg\vert_{-3}^{5} = {\frac{8}{225}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-4}^{6} = {6 i \, \pi - 6 \, \log\left(6\right) + 6 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {\frac{1}{x^{3}}}\;dx = {-\frac{1}{2 \, x^{2}}}\Bigg\vert_{-3}^{2} = {-\frac{5}{72}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{1}{x^{3}}}\;dx = {-\frac{1}{2 \, x^{2}}}\Bigg\vert_{-4}^{4} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {-\frac{2}{x^{2}}}\;dx = {\frac{2}{x}}\Bigg\vert_{-3}^{2} = {\frac{5}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {\frac{8}{x^{2}}}\;dx = {-\frac{8}{x}}\Bigg\vert_{-6}^{4} = {-\frac{10}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-1}^{2} = {\frac{15}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {\frac{10}{x}}\;dx = {10 \, \log\left(x\right)}\Bigg\vert_{-1}^{5} = {-10 i \, \pi + 10 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {-\frac{5}{x^{3}}}\;dx = {\frac{5}{2 \, x^{2}}}\Bigg\vert_{-5}^{2} = {\frac{21}{40}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{4}{x}}\;dx = {-4 \, \log\left(x\right)}\Bigg\vert_{-3}^{5} = {4 i \, \pi - 4 \, \log\left(5\right) + 4 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-6}^{6} = {-i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {\frac{10}{x^{2}}}\;dx = {-\frac{10}{x}}\Bigg\vert_{-1}^{4} = {-\frac{25}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{8}{x^{2}}}\;dx = {\frac{8}{x}}\Bigg\vert_{-2}^{2} = {8}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {-\frac{4}{x}}\;dx = {-4 \, \log\left(x\right)}\Bigg\vert_{-4}^{6} = {4 i \, \pi - 4 \, \log\left(6\right) + 4 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {\frac{3}{x}}\;dx = {3 \, \log\left(x\right)}\Bigg\vert_{-4}^{6} = {-3 i \, \pi + 3 \, \log\left(6\right) - 3 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {\frac{3}{x^{3}}}\;dx = {-\frac{3}{2 \, x^{2}}}\Bigg\vert_{-6}^{3} = {-\frac{1}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{10}{x^{3}}}\;dx = {\frac{5}{x^{2}}}\Bigg\vert_{-4}^{1} = {\frac{75}{16}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{6}{x^{3}}}\;dx = {-\frac{3}{x^{2}}}\Bigg\vert_{-2}^{6} = {\frac{2}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {-\frac{6}{x^{3}}}\;dx = {\frac{3}{x^{2}}}\Bigg\vert_{-6}^{4} = {\frac{5}{48}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {\frac{7}{x^{4}}}\;dx = {-\frac{7}{3 \, x^{3}}}\Bigg\vert_{-6}^{4} = {-\frac{245}{5184}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{2}{x^{3}}}\;dx = {-\frac{1}{x^{2}}}\Bigg\vert_{-4}^{5} = {\frac{9}{400}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{8}{x}}\;dx = {8 \, \log\left(x\right)}\Bigg\vert_{-5}^{1} = {-8 i \, \pi - 8 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {\frac{4}{x^{3}}}\;dx = {-\frac{2}{x^{2}}}\Bigg\vert_{-2}^{2} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{10}{x}}\;dx = {-10 \, \log\left(x\right)}\Bigg\vert_{-1}^{1} = {10 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {\frac{2}{x^{3}}}\;dx = {-\frac{1}{x^{2}}}\Bigg\vert_{-1}^{3} = {\frac{8}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-6}^{4} = {\frac{5}{288}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{6}{x^{4}}}\;dx = {\frac{2}{x^{3}}}\Bigg\vert_{-3}^{3} = {\frac{4}{27}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-2}^{4} = {-3}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-5}^{5} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{5}{x^{2}}}\;dx = {\frac{5}{x}}\Bigg\vert_{-2}^{2} = {5}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{5}{x^{2}}}\;dx = {\frac{5}{x}}\Bigg\vert_{-3}^{3} = {\frac{10}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{10}{x}}\;dx = {10 \, \log\left(x\right)}\Bigg\vert_{-2}^{1} = {-10 i \, \pi - 10 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-6}^{6} = {6 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {\frac{3}{x}}\;dx = {3 \, \log\left(x\right)}\Bigg\vert_{-4}^{1} = {-3 i \, \pi - 3 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {\frac{4}{x}}\;dx = {4 \, \log\left(x\right)}\Bigg\vert_{-5}^{6} = {-4 i \, \pi + 4 \, \log\left(6\right) - 4 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-6}^{2} = {-\frac{14}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{7}{x}}\;dx = {-7 \, \log\left(x\right)}\Bigg\vert_{-3}^{3} = {7 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {\frac{4}{x^{3}}}\;dx = {-\frac{2}{x^{2}}}\Bigg\vert_{-2}^{4} = {\frac{3}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-4}^{3} = {-\frac{35}{288}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {-\frac{7}{x^{3}}}\;dx = {\frac{7}{2 \, x^{2}}}\Bigg\vert_{-4}^{4} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{4}{x^{4}}}\;dx = {\frac{4}{3 \, x^{3}}}\Bigg\vert_{-1}^{3} = {\frac{112}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-3}^{6} = {-\frac{1}{18}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {\frac{6}{x^{4}}}\;dx = {-\frac{2}{x^{3}}}\Bigg\vert_{-5}^{3} = {-\frac{304}{3375}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{7}{x^{2}}}\;dx = {-\frac{7}{x}}\Bigg\vert_{-4}^{4} = {-\frac{7}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{7}{x}}\;dx = {7 \, \log\left(x\right)}\Bigg\vert_{-5}^{2} = {-7 i \, \pi - 7 \, \log\left(5\right) + 7 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {\frac{8}{x^{2}}}\;dx = {-\frac{8}{x}}\Bigg\vert_{-1}^{3} = {-\frac{32}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{6}{x}}\;dx = {6 \, \log\left(x\right)}\Bigg\vert_{-1}^{6} = {-6 i \, \pi + 6 \, \log\left(6\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {-\frac{10}{x^{3}}}\;dx = {\frac{5}{x^{2}}}\Bigg\vert_{-2}^{1} = {\frac{15}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {-\frac{3}{x}}\;dx = {-3 \, \log\left(x\right)}\Bigg\vert_{-4}^{6} = {3 i \, \pi - 3 \, \log\left(6\right) + 3 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{4}{x^{2}}}\;dx = {\frac{4}{x}}\Bigg\vert_{-1}^{1} = {8}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {\frac{6}{x^{4}}}\;dx = {-\frac{2}{x^{3}}}\Bigg\vert_{-4}^{3} = {-\frac{91}{864}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {\frac{8}{x^{4}}}\;dx = {-\frac{8}{3 \, x^{3}}}\Bigg\vert_{-1}^{4} = {-\frac{65}{24}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{7}{x^{2}}}\;dx = {\frac{7}{x}}\Bigg\vert_{-1}^{6} = {\frac{49}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{6}{x}}\;dx = {6 \, \log\left(x\right)}\Bigg\vert_{-5}^{1} = {-6 i \, \pi - 6 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-5}^{5} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {-\frac{6}{x^{3}}}\;dx = {\frac{3}{x^{2}}}\Bigg\vert_{-2}^{4} = {-\frac{9}{16}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{1}{x^{4}}}\;dx = {-\frac{1}{3 \, x^{3}}}\Bigg\vert_{-5}^{2} = {-\frac{133}{3000}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-4}^{4} = {8 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {-\frac{4}{x^{4}}}\;dx = {\frac{4}{3 \, x^{3}}}\Bigg\vert_{-5}^{6} = {\frac{341}{20250}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-3}^{2} = {\frac{175}{324}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{8}{x^{2}}}\;dx = {-\frac{8}{x}}\Bigg\vert_{-6}^{2} = {-\frac{16}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{10}{x}}\;dx = {-10 \, \log\left(x\right)}\Bigg\vert_{-1}^{3} = {10 i \, \pi - 10 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{3}{x^{2}}}\;dx = {\frac{3}{x}}\Bigg\vert_{-1}^{5} = {\frac{18}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {\frac{7}{x}}\;dx = {7 \, \log\left(x\right)}\Bigg\vert_{-3}^{2} = {-7 i \, \pi - 7 \, \log\left(3\right) + 7 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{7}{x}}\;dx = {-7 \, \log\left(x\right)}\Bigg\vert_{-2}^{6} = {7 i \, \pi - 7 \, \log\left(6\right) + 7 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {\frac{2}{x}}\;dx = {2 \, \log\left(x\right)}\Bigg\vert_{-6}^{1} = {-2 i \, \pi - 2 \, \log\left(6\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {\frac{3}{x^{4}}}\;dx = {-\frac{1}{x^{3}}}\Bigg\vert_{-2}^{4} = {-\frac{9}{64}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-4}^{3} = {8 i \, \pi + 8 \, \log\left(4\right) - 8 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {\frac{9}{x}}\;dx = {9 \, \log\left(x\right)}\Bigg\vert_{-6}^{1} = {-9 i \, \pi - 9 \, \log\left(6\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-5}^{1} = {\frac{84}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {-\frac{1}{x^{2}}}\;dx = {\frac{1}{x}}\Bigg\vert_{-4}^{3} = {\frac{7}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {-\frac{3}{x^{2}}}\;dx = {\frac{3}{x}}\Bigg\vert_{-6}^{3} = {\frac{3}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-3}^{3} = {4}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-3}^{5} = {\frac{1064}{10125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{1}{x}}\;dx = {-\log\left(x\right)}\Bigg\vert_{-2}^{3} = {i \, \pi - \log\left(3\right) + \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {-\frac{3}{x}}\;dx = {-3 \, \log\left(x\right)}\Bigg\vert_{-2}^{4} = {3 i \, \pi - 3 \, \log\left(4\right) + 3 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-1}^{1} = {8 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-5}^{2} = {-\frac{133}{300}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{8}{x^{2}}}\;dx = {-\frac{8}{x}}\Bigg\vert_{-2}^{1} = {-12}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-4}^{3} = {\frac{7}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{7}{x^{3}}}\;dx = {-\frac{7}{2 \, x^{2}}}\Bigg\vert_{-5}^{4} = {-\frac{63}{800}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{8}{x^{2}}}\;dx = {\frac{8}{x}}\Bigg\vert_{-3}^{3} = {\frac{16}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {\frac{8}{x^{3}}}\;dx = {-\frac{4}{x^{2}}}\Bigg\vert_{-3}^{2} = {-\frac{5}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-1}^{3} = {-\frac{112}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-1}^{3} = {-\frac{16}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{4}{x^{4}}}\;dx = {\frac{4}{3 \, x^{3}}}\Bigg\vert_{-1}^{6} = {\frac{217}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-3}^{4} = {\frac{637}{5184}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {\frac{2}{x^{4}}}\;dx = {-\frac{2}{3 \, x^{3}}}\Bigg\vert_{-1}^{4} = {-\frac{65}{96}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-1}^{6} = {-i \, \pi + \log\left(6\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-2}^{4} = {-\frac{3}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {\frac{3}{x^{4}}}\;dx = {-\frac{1}{x^{3}}}\Bigg\vert_{-4}^{1} = {-\frac{65}{64}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{1}{x^{2}}}\;dx = {\frac{1}{x}}\Bigg\vert_{-5}^{5} = {\frac{2}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{7}{x^{2}}}\;dx = {-\frac{7}{x}}\Bigg\vert_{-1}^{2} = {-\frac{21}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {-\frac{10}{x^{2}}}\;dx = {\frac{10}{x}}\Bigg\vert_{-3}^{6} = {5}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {\frac{6}{x^{4}}}\;dx = {-\frac{2}{x^{3}}}\Bigg\vert_{-4}^{6} = {-\frac{35}{864}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-5}^{3} = {\frac{32}{225}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {\frac{2}{x^{2}}}\;dx = {-\frac{2}{x}}\Bigg\vert_{-5}^{5} = {-\frac{4}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{5}{x}}\;dx = {-5 \, \log\left(x\right)}\Bigg\vert_{-3}^{4} = {5 i \, \pi - 5 \, \log\left(4\right) + 5 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {-\frac{7}{x}}\;dx = {-7 \, \log\left(x\right)}\Bigg\vert_{-5}^{6} = {7 i \, \pi - 7 \, \log\left(6\right) + 7 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {-\frac{9}{x^{2}}}\;dx = {\frac{9}{x}}\Bigg\vert_{-6}^{2} = {6}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-4}^{5} = {\frac{9}{160}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-2}^{6} = {-i \, \pi + \log\left(6\right) - \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{10}{x}}\;dx = {10 \, \log\left(x\right)}\Bigg\vert_{-5}^{2} = {-10 i \, \pi - 10 \, \log\left(5\right) + 10 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {\frac{9}{x^{3}}}\;dx = {-\frac{9}{2 \, x^{2}}}\Bigg\vert_{-3}^{2} = {-\frac{5}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-5}^{1} = {\frac{84}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {\frac{8}{x^{3}}}\;dx = {-\frac{4}{x^{2}}}\Bigg\vert_{-5}^{3} = {-\frac{64}{225}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-1}^{5} = {-\frac{12}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {\frac{9}{x^{4}}}\;dx = {-\frac{3}{x^{3}}}\Bigg\vert_{-3}^{6} = {-\frac{1}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{4}{x^{2}}}\;dx = {\frac{4}{x}}\Bigg\vert_{-5}^{5} = {\frac{8}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{1}{x^{3}}}\;dx = {-\frac{1}{2 \, x^{2}}}\Bigg\vert_{-5}^{4} = {-\frac{9}{800}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{7}{x^{2}}}\;dx = {\frac{7}{x}}\Bigg\vert_{-1}^{5} = {\frac{42}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {-\frac{1}{x^{2}}}\;dx = {\frac{1}{x}}\Bigg\vert_{-5}^{4} = {\frac{9}{20}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-5}^{3} = {-\frac{304}{2025}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-1}^{4} = {-\frac{15}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {\frac{6}{x}}\;dx = {6 \, \log\left(x\right)}\Bigg\vert_{-6}^{6} = {-6 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-3}^{4} = {\frac{7}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {\frac{9}{x^{3}}}\;dx = {-\frac{9}{2 \, x^{2}}}\Bigg\vert_{-4}^{1} = {-\frac{135}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {-\frac{8}{x^{2}}}\;dx = {\frac{8}{x}}\Bigg\vert_{-5}^{2} = {\frac{28}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {\frac{10}{x^{3}}}\;dx = {-\frac{5}{x^{2}}}\Bigg\vert_{-2}^{5} = {\frac{21}{20}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-2}^{2} = {-i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {-\frac{9}{x^{3}}}\;dx = {\frac{9}{2 \, x^{2}}}\Bigg\vert_{-6}^{6} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-3}^{5} = {\frac{16}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{1}{x^{2}}}\;dx = {\frac{1}{x}}\Bigg\vert_{-2}^{6} = {\frac{2}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {\frac{7}{x^{2}}}\;dx = {-\frac{7}{x}}\Bigg\vert_{-6}^{4} = {-\frac{35}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-3}^{5} = {\frac{8}{45}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{3}{x^{2}}}\;dx = {\frac{3}{x}}\Bigg\vert_{-3}^{3} = {2}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-3}^{3} = {6 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{5}{x^{2}}}\;dx = {-\frac{5}{x}}\Bigg\vert_{-5}^{1} = {-6}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {-\frac{6}{x^{3}}}\;dx = {\frac{3}{x^{2}}}\Bigg\vert_{-5}^{6} = {-\frac{11}{300}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {\frac{10}{x}}\;dx = {10 \, \log\left(x\right)}\Bigg\vert_{-1}^{1} = {-10 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-3}^{4} = {6 i \, \pi - 6 \, \log\left(4\right) + 6 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-4}^{5} = {-i \, \pi + \log\left(5\right) - \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{8}{x^{4}}}\;dx = {-\frac{8}{3 \, x^{3}}}\Bigg\vert_{-6}^{2} = {-\frac{28}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-2}^{6} = {6 i \, \pi - 6 \, \log\left(6\right) + 6 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {-\frac{4}{x}}\;dx = {-4 \, \log\left(x\right)}\Bigg\vert_{-1}^{2} = {4 i \, \pi - 4 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {-\frac{7}{x^{2}}}\;dx = {\frac{7}{x}}\Bigg\vert_{-6}^{3} = {\frac{7}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {-\frac{1}{x^{2}}}\;dx = {\frac{1}{x}}\Bigg\vert_{-6}^{6} = {\frac{1}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{10}{x}}\;dx = {-10 \, \log\left(x\right)}\Bigg\vert_{-2}^{2} = {10 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-5}^{4} = {-\frac{63}{800}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {\frac{3}{x}}\;dx = {3 \, \log\left(x\right)}\Bigg\vert_{-6}^{6} = {-3 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {\frac{6}{x}}\;dx = {6 \, \log\left(x\right)}\Bigg\vert_{-4}^{3} = {-6 i \, \pi - 6 \, \log\left(4\right) + 6 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-3}^{4} = {\frac{91}{2592}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {-\frac{10}{x^{3}}}\;dx = {\frac{5}{x^{2}}}\Bigg\vert_{-6}^{3} = {\frac{5}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {-\frac{9}{x^{3}}}\;dx = {\frac{9}{2 \, x^{2}}}\Bigg\vert_{-2}^{4} = {-\frac{27}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{9}{x^{3}}}\;dx = {-\frac{9}{2 \, x^{2}}}\Bigg\vert_{-2}^{6} = {1}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {\frac{5}{x}}\;dx = {5 \, \log\left(x\right)}\Bigg\vert_{-5}^{3} = {-5 i \, \pi - 5 \, \log\left(5\right) + 5 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{5} {\frac{10}{x^{3}}}\;dx = {-\frac{5}{x^{2}}}\Bigg\vert_{-6}^{5} = {-\frac{11}{180}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {\frac{7}{x}}\;dx = {7 \, \log\left(x\right)}\Bigg\vert_{-2}^{3} = {-7 i \, \pi + 7 \, \log\left(3\right) - 7 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{5}{x}}\;dx = {5 \, \log\left(x\right)}\Bigg\vert_{-5}^{1} = {-5 i \, \pi - 5 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {\frac{8}{x}}\;dx = {8 \, \log\left(x\right)}\Bigg\vert_{-6}^{3} = {-8 i \, \pi - 8 \, \log\left(6\right) + 8 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{10}{x^{3}}}\;dx = {-\frac{5}{x^{2}}}\Bigg\vert_{-1}^{6} = {\frac{175}{36}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {\frac{6}{x^{3}}}\;dx = {-\frac{3}{x^{2}}}\Bigg\vert_{-6}^{1} = {-\frac{35}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-1}^{5} = {\frac{84}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-3}^{6} = {8 i \, \pi - 8 \, \log\left(6\right) + 8 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {\frac{3}{x^{2}}}\;dx = {-\frac{3}{x}}\Bigg\vert_{-6}^{3} = {-\frac{3}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-2}^{3} = {6 i \, \pi - 6 \, \log\left(3\right) + 6 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{10}{x^{2}}}\;dx = {\frac{10}{x}}\Bigg\vert_{-3}^{3} = {\frac{20}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{1} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-3}^{1} = {\frac{4}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-2}^{6} = {-\frac{35}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{7}{x^{4}}}\;dx = {-\frac{7}{3 \, x^{3}}}\Bigg\vert_{-4}^{4} = {-\frac{7}{96}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-5}^{3} = {\frac{304}{2025}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{1}{x^{4}}}\;dx = {\frac{1}{3 \, x^{3}}}\Bigg\vert_{-4}^{1} = {\frac{65}{192}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {-\frac{8}{x^{4}}}\;dx = {\frac{8}{3 \, x^{3}}}\Bigg\vert_{-4}^{2} = {\frac{3}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{9}{x}}\;dx = {9 \, \log\left(x\right)}\Bigg\vert_{-4}^{4} = {-9 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{5}{x^{4}}}\;dx = {-\frac{5}{3 \, x^{3}}}\Bigg\vert_{-5}^{2} = {-\frac{133}{600}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {-\frac{6}{x^{3}}}\;dx = {\frac{3}{x^{2}}}\Bigg\vert_{-6}^{6} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {\frac{8}{x^{2}}}\;dx = {-\frac{8}{x}}\Bigg\vert_{-2}^{3} = {-\frac{20}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{6}{x^{4}}}\;dx = {-\frac{2}{x^{3}}}\Bigg\vert_{-2}^{1} = {-\frac{9}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {-\frac{2}{x^{2}}}\;dx = {\frac{2}{x}}\Bigg\vert_{-5}^{4} = {\frac{9}{10}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{7}{x}}\;dx = {7 \, \log\left(x\right)}\Bigg\vert_{-2}^{1} = {-7 i \, \pi - 7 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{2}{x}}\;dx = {-2 \, \log\left(x\right)}\Bigg\vert_{-5}^{5} = {2 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{5} {\frac{7}{x^{2}}}\;dx = {-\frac{7}{x}}\Bigg\vert_{-6}^{5} = {-\frac{77}{30}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{5}{x^{4}}}\;dx = {\frac{5}{3 \, x^{3}}}\Bigg\vert_{-4}^{1} = {\frac{325}{192}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {\frac{5}{x^{4}}}\;dx = {-\frac{5}{3 \, x^{3}}}\Bigg\vert_{-4}^{3} = {-\frac{455}{5184}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-2}^{1} = {-\frac{3}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{10}{x^{2}}}\;dx = {\frac{10}{x}}\Bigg\vert_{-5}^{5} = {4}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {\frac{9}{x^{4}}}\;dx = {-\frac{3}{x^{3}}}\Bigg\vert_{-3}^{2} = {-\frac{35}{72}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{4}{x^{3}}}\;dx = {-\frac{2}{x^{2}}}\Bigg\vert_{-4}^{4} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {-\frac{5}{x^{4}}}\;dx = {\frac{5}{3 \, x^{3}}}\Bigg\vert_{-4}^{5} = {\frac{63}{1600}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-4}^{1} = {\frac{325}{96}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {-\frac{7}{x^{3}}}\;dx = {\frac{7}{2 \, x^{2}}}\Bigg\vert_{-5}^{2} = {\frac{147}{200}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{3} {-\frac{7}{x}}\;dx = {-7 \, \log\left(x\right)}\Bigg\vert_{-5}^{3} = {7 i \, \pi + 7 \, \log\left(5\right) - 7 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{3} {\frac{4}{x}}\;dx = {4 \, \log\left(x\right)}\Bigg\vert_{-4}^{3} = {-4 i \, \pi - 4 \, \log\left(4\right) + 4 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {\frac{9}{x^{4}}}\;dx = {-\frac{3}{x^{3}}}\Bigg\vert_{-1}^{5} = {-\frac{378}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {\frac{6}{x^{4}}}\;dx = {-\frac{2}{x^{3}}}\Bigg\vert_{-3}^{6} = {-\frac{1}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{5} {\frac{1}{x}}\;dx = {\log\left(x\right)}\Bigg\vert_{-6}^{5} = {-i \, \pi - \log\left(6\right) + \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{5} {-\frac{4}{x^{4}}}\;dx = {\frac{4}{3 \, x^{3}}}\Bigg\vert_{-6}^{5} = {\frac{341}{20250}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {-\frac{2}{x^{3}}}\;dx = {\frac{1}{x^{2}}}\Bigg\vert_{-5}^{4} = {\frac{9}{400}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {\frac{10}{x}}\;dx = {10 \, \log\left(x\right)}\Bigg\vert_{-3}^{4} = {-10 i \, \pi + 10 \, \log\left(4\right) - 10 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{9}{x^{4}}}\;dx = {\frac{3}{x^{3}}}\Bigg\vert_{-1}^{1} = {6}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{2}{x^{2}}}\;dx = {\frac{2}{x}}\Bigg\vert_{-3}^{4} = {\frac{7}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{9}{x^{4}}}\;dx = {\frac{3}{x^{3}}}\Bigg\vert_{-3}^{3} = {\frac{2}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {\frac{6}{x^{4}}}\;dx = {-\frac{2}{x^{3}}}\Bigg\vert_{-6}^{1} = {-\frac{217}{108}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{6} {\frac{5}{x^{2}}}\;dx = {-\frac{5}{x}}\Bigg\vert_{-4}^{6} = {-\frac{25}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {-\frac{3}{x}}\;dx = {-3 \, \log\left(x\right)}\Bigg\vert_{-3}^{6} = {3 i \, \pi - 3 \, \log\left(6\right) + 3 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{2}{x^{2}}}\;dx = {-\frac{2}{x}}\Bigg\vert_{-6}^{2} = {-\frac{4}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{1}{x}}\;dx = {-\log\left(x\right)}\Bigg\vert_{-4}^{1} = {i \, \pi + \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{2}{x^{4}}}\;dx = {-\frac{2}{3 \, x^{3}}}\Bigg\vert_{-1}^{2} = {-\frac{3}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-2}^{6} = {\frac{49}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{10}{x^{3}}}\;dx = {\frac{5}{x^{2}}}\Bigg\vert_{-3}^{3} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {\frac{3}{x^{2}}}\;dx = {-\frac{3}{x}}\Bigg\vert_{-6}^{4} = {-\frac{5}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {\frac{2}{x}}\;dx = {2 \, \log\left(x\right)}\Bigg\vert_{-3}^{5} = {-2 i \, \pi + 2 \, \log\left(5\right) - 2 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{4}{x^{3}}}\;dx = {-\frac{2}{x^{2}}}\Bigg\vert_{-6}^{2} = {-\frac{4}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-3}^{4} = {9 i \, \pi - 9 \, \log\left(4\right) + 9 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-2}^{6} = {8 i \, \pi - 8 \, \log\left(6\right) + 8 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{7}{x^{3}}}\;dx = {\frac{7}{2 \, x^{2}}}\Bigg\vert_{-1}^{3} = {-\frac{28}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {\frac{8}{x}}\;dx = {8 \, \log\left(x\right)}\Bigg\vert_{-3}^{5} = {-8 i \, \pi + 8 \, \log\left(5\right) - 8 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {-\frac{5}{x^{3}}}\;dx = {\frac{5}{2 \, x^{2}}}\Bigg\vert_{-6}^{1} = {\frac{175}{72}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-4}^{1} = {-\frac{65}{48}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{2}{x^{3}}}\;dx = {-\frac{1}{x^{2}}}\Bigg\vert_{-1}^{2} = {\frac{3}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {-\frac{4}{x^{3}}}\;dx = {\frac{2}{x^{2}}}\Bigg\vert_{-2}^{5} = {-\frac{21}{50}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-4}^{2} = {6 i \, \pi + 6 \, \log\left(4\right) - 6 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-5}^{4} = {-\frac{9}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{6} {\frac{2}{x^{4}}}\;dx = {-\frac{2}{3 \, x^{3}}}\Bigg\vert_{-6}^{6} = {-\frac{1}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{6}{x^{2}}}\;dx = {\frac{6}{x}}\Bigg\vert_{-5}^{5} = {\frac{12}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {\frac{6}{x^{2}}}\;dx = {-\frac{6}{x}}\Bigg\vert_{-2}^{3} = {-5}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {\frac{4}{x^{4}}}\;dx = {-\frac{4}{3 \, x^{3}}}\Bigg\vert_{-5}^{2} = {-\frac{133}{750}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-4}^{2} = {-\frac{15}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{3}{x^{3}}}\;dx = {\frac{3}{2 \, x^{2}}}\Bigg\vert_{-1}^{5} = {-\frac{36}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-5}^{6} = {-\frac{11}{1800}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{5} {\frac{2}{x^{4}}}\;dx = {-\frac{2}{3 \, x^{3}}}\Bigg\vert_{-2}^{5} = {-\frac{133}{1500}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-3}^{4} = {-\frac{7}{288}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{10}{x^{2}}}\;dx = {\frac{10}{x}}\Bigg\vert_{-2}^{2} = {10}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {\frac{9}{x}}\;dx = {9 \, \log\left(x\right)}\Bigg\vert_{-1}^{5} = {-9 i \, \pi + 9 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{2}{x^{4}}}\;dx = {-\frac{2}{3 \, x^{3}}}\Bigg\vert_{-1}^{6} = {-\frac{217}{324}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {\frac{2}{x}}\;dx = {2 \, \log\left(x\right)}\Bigg\vert_{-4}^{2} = {-2 i \, \pi - 2 \, \log\left(4\right) + 2 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{7}{x^{3}}}\;dx = {-\frac{7}{2 \, x^{2}}}\Bigg\vert_{-1}^{2} = {\frac{21}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{5}{x^{2}}}\;dx = {\frac{5}{x}}\Bigg\vert_{-1}^{5} = {6}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {-\frac{1}{x^{2}}}\;dx = {\frac{1}{x}}\Bigg\vert_{-6}^{4} = {\frac{5}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{6}{x^{2}}}\;dx = {-\frac{6}{x}}\Bigg\vert_{-5}^{4} = {-\frac{27}{10}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {-\frac{5}{x^{2}}}\;dx = {\frac{5}{x}}\Bigg\vert_{-6}^{3} = {\frac{5}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{4} {-\frac{5}{x^{2}}}\;dx = {\frac{5}{x}}\Bigg\vert_{-2}^{4} = {\frac{15}{4}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-1}^{5} = {\frac{294}{125}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{2}{x}}\;dx = {2 \, \log\left(x\right)}\Bigg\vert_{-1}^{2} = {-2 i \, \pi + 2 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {-\frac{4}{x^{4}}}\;dx = {\frac{4}{3 \, x^{3}}}\Bigg\vert_{-3}^{2} = {\frac{35}{162}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{8}{x}}\;dx = {-8 \, \log\left(x\right)}\Bigg\vert_{-3}^{3} = {8 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-1}^{3} = {\frac{196}{81}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-3}^{5} = {6 i \, \pi - 6 \, \log\left(5\right) + 6 \, \log\left(3\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-5}^{5} = {0}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {-\frac{6}{x^{4}}}\;dx = {\frac{2}{x^{3}}}\Bigg\vert_{-5}^{4} = {\frac{189}{4000}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {\frac{9}{x^{3}}}\;dx = {-\frac{9}{2 \, x^{2}}}\Bigg\vert_{-3}^{4} = {\frac{7}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{6}{x^{2}}}\;dx = {-\frac{6}{x}}\Bigg\vert_{-1}^{6} = {-7}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {\frac{10}{x^{4}}}\;dx = {-\frac{10}{3 \, x^{3}}}\Bigg\vert_{-3}^{6} = {-\frac{5}{36}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{4} {\frac{3}{x^{3}}}\;dx = {-\frac{3}{2 \, x^{2}}}\Bigg\vert_{-3}^{4} = {\frac{7}{96}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {-\frac{9}{x^{2}}}\;dx = {\frac{9}{x}}\Bigg\vert_{-6}^{1} = {\frac{21}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{3}{x^{3}}}\;dx = {\frac{3}{2 \, x^{2}}}\Bigg\vert_{-3}^{5} = {-\frac{8}{75}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{3} {-\frac{2}{x^{4}}}\;dx = {\frac{2}{3 \, x^{3}}}\Bigg\vert_{-6}^{3} = {\frac{1}{36}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {-\frac{2}{x}}\;dx = {-2 \, \log\left(x\right)}\Bigg\vert_{-2}^{1} = {2 i \, \pi + 2 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{6} {\frac{9}{x^{2}}}\;dx = {-\frac{9}{x}}\Bigg\vert_{-3}^{6} = {-\frac{9}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {\frac{2}{x^{3}}}\;dx = {-\frac{1}{x^{2}}}\Bigg\vert_{-1}^{5} = {\frac{24}{25}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {-\frac{3}{x^{2}}}\;dx = {\frac{3}{x}}\Bigg\vert_{-3}^{5} = {\frac{8}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {\frac{9}{x^{2}}}\;dx = {-\frac{9}{x}}\Bigg\vert_{-6}^{1} = {-\frac{21}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{1}{x^{2}}}\;dx = {-\frac{1}{x}}\Bigg\vert_{-1}^{2} = {-\frac{3}{2}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {-\frac{7}{x}}\;dx = {-7 \, \log\left(x\right)}\Bigg\vert_{-1}^{4} = {7 i \, \pi - 7 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {\frac{1}{x^{3}}}\;dx = {-\frac{1}{2 \, x^{2}}}\Bigg\vert_{-4}^{5} = {\frac{9}{800}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{5}{x^{2}}}\;dx = {\frac{5}{x}}\Bigg\vert_{-2}^{3} = {\frac{25}{6}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{2} {-\frac{7}{x^{2}}}\;dx = {\frac{7}{x}}\Bigg\vert_{-5}^{2} = {\frac{49}{10}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{1}{x^{3}}}\;dx = {\frac{1}{2 \, x^{2}}}\Bigg\vert_{-2}^{6} = {-\frac{1}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{8}{x^{4}}}\;dx = {-\frac{8}{3 \, x^{3}}}\Bigg\vert_{-4}^{4} = {-\frac{1}{12}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {\frac{5}{x}}\;dx = {5 \, \log\left(x\right)}\Bigg\vert_{-4}^{4} = {-5 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-2}^{6} = {9 i \, \pi - 9 \, \log\left(6\right) + 9 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{4} {-\frac{5}{x}}\;dx = {-5 \, \log\left(x\right)}\Bigg\vert_{-1}^{4} = {5 i \, \pi - 5 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{1} {-\frac{6}{x^{4}}}\;dx = {\frac{2}{x^{3}}}\Bigg\vert_{-4}^{1} = {\frac{65}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {\frac{5}{x^{2}}}\;dx = {-\frac{5}{x}}\Bigg\vert_{-3}^{3} = {-\frac{10}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-1}^{6} = {\frac{175}{72}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{2} {\frac{3}{x^{3}}}\;dx = {-\frac{3}{2 \, x^{2}}}\Bigg\vert_{-4}^{2} = {-\frac{9}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {-\frac{9}{x^{4}}}\;dx = {\frac{3}{x^{3}}}\Bigg\vert_{-1}^{3} = {\frac{28}{9}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{5} {-\frac{6}{x}}\;dx = {-6 \, \log\left(x\right)}\Bigg\vert_{-4}^{5} = {6 i \, \pi - 6 \, \log\left(5\right) + 6 \, \log\left(4\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{1} {-\frac{1}{x^{4}}}\;dx = {\frac{1}{3 \, x^{3}}}\Bigg\vert_{-6}^{1} = {\frac{217}{648}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{2} {\frac{5}{x^{3}}}\;dx = {-\frac{5}{2 \, x^{2}}}\Bigg\vert_{-3}^{2} = {-\frac{25}{72}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{2} {\frac{3}{x^{3}}}\;dx = {-\frac{3}{2 \, x^{2}}}\Bigg\vert_{-1}^{2} = {\frac{9}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{6} {\frac{9}{x^{4}}}\;dx = {-\frac{3}{x^{3}}}\Bigg\vert_{-2}^{6} = {-\frac{7}{18}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{1} {\frac{1}{x^{4}}}\;dx = {-\frac{1}{3 \, x^{3}}}\Bigg\vert_{-2}^{1} = {-\frac{3}{8}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{5} {\frac{8}{x^{2}}}\;dx = {-\frac{8}{x}}\Bigg\vert_{-3}^{5} = {-\frac{64}{15}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {\frac{5}{x^{4}}}\;dx = {-\frac{5}{3 \, x^{3}}}\Bigg\vert_{-5}^{4} = {-\frac{63}{1600}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{4} {-\frac{10}{x^{4}}}\;dx = {\frac{10}{3 \, x^{3}}}\Bigg\vert_{-5}^{4} = {\frac{63}{800}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{2} {\frac{9}{x^{3}}}\;dx = {-\frac{9}{2 \, x^{2}}}\Bigg\vert_{-6}^{2} = {-1}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-3}^{3} {-\frac{9}{x}}\;dx = {-9 \, \log\left(x\right)}\Bigg\vert_{-3}^{3} = {9 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-6}^{4} {-\frac{9}{x^{3}}}\;dx = {\frac{9}{2 \, x^{2}}}\Bigg\vert_{-6}^{4} = {\frac{5}{32}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {\frac{9}{x}}\;dx = {9 \, \log\left(x\right)}\Bigg\vert_{-2}^{3} = {-9 i \, \pi + 9 \, \log\left(3\right) - 9 \, \log\left(2\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{1} {-\frac{2}{x^{2}}}\;dx = {\frac{2}{x}}\Bigg\vert_{-1}^{1} = {4}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {\frac{4}{x}}\;dx = {4 \, \log\left(x\right)}\Bigg\vert_{-1}^{5} = {-4 i \, \pi + 4 \, \log\left(5\right)}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {-\frac{7}{x^{4}}}\;dx = {\frac{7}{3 \, x^{3}}}\Bigg\vert_{-2}^{3} = {\frac{245}{648}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {\frac{3}{x^{2}}}\;dx = {-\frac{3}{x}}\Bigg\vert_{-2}^{2} = {-3}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{6} {-\frac{10}{x^{2}}}\;dx = {\frac{10}{x}}\Bigg\vert_{-5}^{6} = {\frac{11}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {\frac{2}{x^{3}}}\;dx = {-\frac{1}{x^{2}}}\Bigg\vert_{-1}^{6} = {\frac{35}{36}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{3} {\frac{6}{x^{2}}}\;dx = {-\frac{6}{x}}\Bigg\vert_{-1}^{3} = {-8}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{6} {-\frac{3}{x^{3}}}\;dx = {\frac{3}{2 \, x^{2}}}\Bigg\vert_{-1}^{6} = {-\frac{35}{24}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{2} {-\frac{2}{x}}\;dx = {-2 \, \log\left(x\right)}\Bigg\vert_{-2}^{2} = {2 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-2}^{3} {\frac{4}{x^{2}}}\;dx = {-\frac{4}{x}}\Bigg\vert_{-2}^{3} = {-\frac{10}{3}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{5} {-\frac{5}{x^{2}}}\;dx = {\frac{5}{x}}\Bigg\vert_{-5}^{5} = {2}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-4}^{4} {-\frac{10}{x}}\;dx = {-10 \, \log\left(x\right)}\Bigg\vert_{-4}^{4} = {10 i \, \pi}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-1}^{5} {\frac{9}{x^{2}}}\;dx = {-\frac{9}{x}}\Bigg\vert_{-1}^{5} = {-\frac{54}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

What is wrong with the following equation:

\[\int_{-5}^{1} {\frac{6}{x^{2}}}\;dx = {-\frac{6}{x}}\Bigg\vert_{-5}^{1} = {-\frac{36}{5}}\]

\input{2311-Concept-Integral-0001.HELP.tex}

\begin{multipleChoice}
\choice The bounds are evaluated in the wrong order.
\choice The antiderivative is incorrect.
\choice[correct] The integrand is not defined over the entire interval.
\choice Nothing is wrong.  The equation is correct, as is.
\end{multipleChoice}

\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


