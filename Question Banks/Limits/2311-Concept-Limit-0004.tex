
%%%%%%%%%%%%%%%%%%%%%%%
%%\tagged{Cat@One, Cat@Two, Cat@Three, Cat@Four, Cat@Five, Ans@MultiAns, Type@Concept, Topic@Limit, Sub@Continuity, Sub@Disc_Jump, Sub@Piecewise}{

\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} + 25} & , & x\leq {2}\\
{{\left(x + 5\right)}^{2}} & , & {2}<x\leq{7}\\
{{\left(x + 3\right)}^{2} + 44} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 5\right)}^{2} + 12} & , & x\leq {-3}\\
{x^{2} - 2 \, x} & , & {-3}<x\leq{2}\\
{x^{2} + 5 \, x - 14} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 1} & , & x\leq {0}\\
{x} & , & {0}<x\leq{3}\\
{x} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} + 1} & , & x\leq {3}\\
{{\left(x + 4\right)}^{2}} & , & {3}<x\leq{6}\\
{{\left(x + 4\right)}^{2}} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 7} & , & x\leq {2}\\
{-x + 4} & , & {2}<x\leq{6}\\
{{\left(x - 3\right)}^{2} - 11} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 19} & , & x\leq {-1}\\
{{\left(x - 3\right)}^{2}} & , & {-1}<x\leq{4}\\
{{\left(x - 3\right)}^{2}} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 47} & , & x\leq {3}\\
{{\left(x + 4\right)}^{2}} & , & {3}<x\leq{8}\\
{x^{2} - 5 \, x + 120} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 6} & , & x\leq {-1}\\
{x^{2} - 4} & , & {-1}<x\leq{3}\\
{x + 2} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 19} & , & x\leq {-2}\\
{x - 4} & , & {-2}<x\leq{2}\\
{x^{2} + 5 \, x - 16} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 35} & , & x\leq {0}\\
{x^{2} - x - 20} & , & {0}<x\leq{4}\\
{x^{2} + 4 \, x - 40} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 12} & , & x\leq {-2}\\
{x^{2} + 2 \, x - 15} & , & {-2}<x\leq{1}\\
{x^{2} - 13} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 1} & , & x\leq {3}\\
{x - 2} & , & {3}<x\leq{5}\\
{{\left(x - 3\right)}^{2} - 1} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x} & , & x\leq {-1}\\
{x - 4} & , & {-1}<x\leq{1}\\
{{\left(x + 5\right)}^{2} - 39} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 2} & , & x\leq {0}\\
{{\left(x - 1\right)}^{2}} & , & {0}<x\leq{4}\\
{x^{2} - 5 \, x + 13} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 4} & , & x\leq {0}\\
{-x - 5} & , & {0}<x\leq{2}\\
{x - 9} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 10} & , & x\leq {-3}\\
{-x - 2} & , & {-3}<x\leq{1}\\
{x - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 24} & , & x\leq {3}\\
{-x - 1} & , & {3}<x\leq{8}\\
{x^{2} + 4 \, x - 105} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} - 5} & , & x\leq {0}\\
{x^{2} + 4 \, x - 5} & , & {0}<x\leq{2}\\
{{\left(x - 1\right)}^{2} + 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 5} & , & x\leq {-2}\\
{-x - 4} & , & {-2}<x\leq{2}\\
{x^{2} + 5 \, x - 20} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 1} & , & x\leq {0}\\
{x^{2} - 4 \, x} & , & {0}<x\leq{5}\\
{x} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 47} & , & x\leq {-3}\\
{{\left(x - 5\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} + 3 \, x + 38} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 24} & , & x\leq {-3}\\
{-x - 4} & , & {-3}<x\leq{1}\\
{x^{2} + 5 \, x - 11} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 23} & , & x\leq {3}\\
{{\left(x + 2\right)}^{2}} & , & {3}<x\leq{6}\\
{x^{2} + 2 \, x + 16} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 2} & , & x\leq {-2}\\
{x + 1} & , & {-2}<x\leq{1}\\
{x^{2} - 3 \, x + 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 5} & , & x\leq {-2}\\
{x + 4} & , & {-2}<x\leq{2}\\
{x^{2} + 5 \, x - 8} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 10} & , & x\leq {3}\\
{x + 1} & , & {3}<x\leq{5}\\
{x^{2} - 2 \, x - 9} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {3}\\
{{\left(x - 5\right)}^{2}} & , & {3}<x\leq{7}\\
{x^{2} - 4 \, x - 17} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 8} & , & x\leq {2}\\
{-x - 1} & , & {2}<x\leq{4}\\
{{\left(x - 1\right)}^{2} - 14} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 2} & , & x\leq {2}\\
{-x + 5} & , & {2}<x\leq{4}\\
{x^{2} - x - 11} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 11} & , & x\leq {0}\\
{{\left(x - 2\right)}^{2}} & , & {0}<x\leq{5}\\
{x^{2} + 4 \, x - 36} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 20} & , & x\leq {2}\\
{x^{2}} & , & {2}<x\leq{7}\\
{x^{2} + 3 \, x - 21} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 4} & , & x\leq {0}\\
{x + 3} & , & {0}<x\leq{5}\\
{x^{2} + 4 \, x - 37} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 3} & , & x\leq {2}\\
{x^{2} - 5 \, x + 4} & , & {2}<x\leq{7}\\
{x^{2} - 5 \, x + 4} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 28} & , & x\leq {2}\\
{{\left(x + 3\right)}^{2}} & , & {2}<x\leq{6}\\
{x^{2} + 3 \, x + 27} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 12} & , & x\leq {2}\\
{{\left(x + 3\right)}^{2}} & , & {2}<x\leq{5}\\
{x + 59} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 8} & , & x\leq {1}\\
{{\left(x - 4\right)}^{2}} & , & {1}<x\leq{3}\\
{{\left(x + 1\right)}^{2} - 15} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 8} & , & x\leq {-3}\\
{-x + 1} & , & {-3}<x\leq{2}\\
{x^{2} + 3 \, x - 11} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x} & , & x\leq {2}\\
{x^{2} - 10 \, x + 25} & , & {2}<x\leq{4}\\
{x - 3} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-1}\\
{-x} & , & {-1}<x\leq{4}\\
{x - 8} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 3} & , & x\leq {0}\\
{x - 4} & , & {0}<x\leq{5}\\
{x^{2} + 5 \, x - 49} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 14} & , & x\leq {2}\\
{x^{2} - 6 \, x + 5} & , & {2}<x\leq{6}\\
{x^{2} - 4 \, x - 7} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} + 4} & , & x\leq {-3}\\
{{\left(x + 1\right)}^{2}} & , & {-3}<x\leq{2}\\
{x^{2} + 4 \, x - 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} + 10} & , & x\leq {-2}\\
{{\left(x + 5\right)}^{2}} & , & {-2}<x\leq{3}\\
{x^{2} + 2 \, x + 49} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 10} & , & x\leq {3}\\
{x + 1} & , & {3}<x\leq{8}\\
{x^{2} - 2 \, x - 39} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 5} & , & x\leq {2}\\
{x - 2} & , & {2}<x\leq{4}\\
{{\left(x - 4\right)}^{2} + 2} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 2} & , & x\leq {0}\\
{-x + 1} & , & {0}<x\leq{5}\\
{x^{2} + 5 \, x - 54} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 2} & , & x\leq {1}\\
{x - 3} & , & {1}<x\leq{5}\\
{x^{2} + 2 \, x - 33} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 2} & , & x\leq {0}\\
{-x - 3} & , & {0}<x\leq{5}\\
{x^{2} - 3 \, x - 18} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 7} & , & x\leq {1}\\
{x - 5} & , & {1}<x\leq{5}\\
{{\left(x - 3\right)}^{2} - 4} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 1} & , & x\leq {2}\\
{x - 4} & , & {2}<x\leq{4}\\
{x^{2} - 3 \, x - 4} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 2} & , & x\leq {0}\\
{x + 1} & , & {0}<x\leq{3}\\
{x^{2} - 5 \, x + 10} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 12} & , & x\leq {1}\\
{x^{2} + 2 \, x - 15} & , & {1}<x\leq{3}\\
{x^{2} + 3 \, x - 18} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 12} & , & x\leq {1}\\
{-x - 3} & , & {1}<x\leq{5}\\
{x^{2} + 4 \, x - 53} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 8} & , & x\leq {-2}\\
{x - 1} & , & {-2}<x\leq{3}\\
{{\left(x + 1\right)}^{2} - 14} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 32} & , & x\leq {-3}\\
{x^{2} - 5 \, x + 4} & , & {-3}<x\leq{1}\\
{x^{2} - 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2} & , & x\leq {-1}\\
{-x - 3} & , & {-1}<x\leq{1}\\
{x - 5} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 16} & , & x\leq {-1}\\
{x^{2} + x - 20} & , & {-1}<x\leq{4}\\
{x^{2} + 4 \, x - 32} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 3} & , & x\leq {1}\\
{x^{2} - 5 \, x + 4} & , & {1}<x\leq{3}\\
{{\left(x + 3\right)}^{2} - 38} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 18} & , & x\leq {1}\\
{x^{2} + x - 20} & , & {1}<x\leq{6}\\
{x^{2} - 14} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} + 2} & , & x\leq {2}\\
{x^{2} + 2 \, x - 3} & , & {2}<x\leq{6}\\
{x^{2} + 4 \, x - 15} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 18} & , & x\leq {-3}\\
{x^{2} + x} & , & {-3}<x\leq{1}\\
{x^{2} + 2 \, x - 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 4} & , & x\leq {3}\\
{x - 5} & , & {3}<x\leq{7}\\
{x^{2} + 3 \, x - 68} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 3} & , & x\leq {0}\\
{x - 4} & , & {0}<x\leq{5}\\
{x - 4} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} + 9} & , & x\leq {0}\\
{{\left(x + 3\right)}^{2}} & , & {0}<x\leq{2}\\
{{\left(x - 1\right)}^{2} + 24} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 11} & , & x\leq {-3}\\
{x} & , & {-3}<x\leq{-1}\\
{x} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 9} & , & x\leq {2}\\
{-x + 4} & , & {2}<x\leq{7}\\
{{\left(x + 4\right)}^{2} - 124} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x + 26} & , & x\leq {0}\\
{{\left(x + 5\right)}^{2}} & , & {0}<x\leq{2}\\
{x^{2} + x + 43} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 5\right)}^{2} + 1} & , & x\leq {2}\\
{{\left(x + 5\right)}^{2}} & , & {2}<x\leq{5}\\
{x^{2} - 5 \, x + 100} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 5} & , & x\leq {-1}\\
{-x + 2} & , & {-1}<x\leq{1}\\
{{\left(x - 2\right)}^{2}} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 7} & , & x\leq {-1}\\
{-x + 4} & , & {-1}<x\leq{2}\\
{x^{2} + 2 \, x - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 6} & , & x\leq {-3}\\
{x + 2} & , & {-3}<x\leq{1}\\
{{\left(x + 5\right)}^{2} - 33} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 5} & , & x\leq {3}\\
{{\left(x - 2\right)}^{2}} & , & {3}<x\leq{5}\\
{{\left(x + 4\right)}^{2} - 72} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 6} & , & x\leq {-1}\\
{{\left(x - 1\right)}^{2}} & , & {-1}<x\leq{3}\\
{x^{2} + x - 8} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 11} & , & x\leq {0}\\
{{\left(x - 2\right)}^{2}} & , & {0}<x\leq{5}\\
{x^{2} - 4 \, x + 4} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 2} & , & x\leq {3}\\
{x - 2} & , & {3}<x\leq{6}\\
{x - 2} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 9} & , & x\leq {1}\\
{x^{2} + 3 \, x - 10} & , & {1}<x\leq{6}\\
{x^{2} + 3 \, x - 10} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 8} & , & x\leq {2}\\
{x^{2} - 9} & , & {2}<x\leq{6}\\
{x + 21} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 27} & , & x\leq {3}\\
{-x - 4} & , & {3}<x\leq{6}\\
{{\left(x - 4\right)}^{2} - 14} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 39} & , & x\leq {-3}\\
{x^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} + 4 \, x + 4} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 12} & , & x\leq {2}\\
{{\left(x + 1\right)}^{2}} & , & {2}<x\leq{7}\\
{x^{2} + 3 \, x - 6} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 48} & , & x\leq {-2}\\
{{\left(x - 5\right)}^{2}} & , & {-2}<x\leq{3}\\
{x + 1} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2}} & , & x\leq {-1}\\
{-x + 2} & , & {-1}<x\leq{1}\\
{x^{2} - 3 \, x + 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 13} & , & x\leq {-3}\\
{x^{2}} & , & {-3}<x\leq{1}\\
{{\left(x + 1\right)}^{2} - 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2}} & , & x\leq {0}\\
{x - 1} & , & {0}<x\leq{4}\\
{x^{2} - 13} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x} & , & x\leq {1}\\
{-x + 2} & , & {1}<x\leq{4}\\
{x - 6} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} + 3} & , & x\leq {2}\\
{x^{2} - 8 \, x + 15} & , & {2}<x\leq{5}\\
{{\left(x - 3\right)}^{2} - 4} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 15} & , & x\leq {-1}\\
{x^{2} + 4 \, x + 3} & , & {-1}<x\leq{4}\\
{{\left(x - 3\right)}^{2} + 34} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 11} & , & x\leq {-1}\\
{x^{2} - 4 \, x + 3} & , & {-1}<x\leq{3}\\
{x - 3} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 12} & , & x\leq {-2}\\
{{\left(x + 5\right)}^{2}} & , & {-2}<x\leq{2}\\
{{\left(x - 3\right)}^{2} + 48} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 7} & , & x\leq {1}\\
{-x + 2} & , & {1}<x\leq{3}\\
{x^{2} + 4 \, x - 22} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 7} & , & x\leq {2}\\
{x^{2} - 9 \, x + 20} & , & {2}<x\leq{6}\\
{x^{2} - 5 \, x - 4} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 1} & , & x\leq {-3}\\
{x - 3} & , & {-3}<x\leq{2}\\
{{\left(x + 5\right)}^{2} - 50} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {3}\\
{x + 2} & , & {3}<x\leq{5}\\
{{\left(x - 3\right)}^{2} + 3} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 11} & , & x\leq {-3}\\
{{\left(x - 1\right)}^{2}} & , & {-3}<x\leq{2}\\
{x^{2} - x - 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2}} & , & x\leq {-2}\\
{x + 2} & , & {-2}<x\leq{3}\\
{{\left(x + 3\right)}^{2} - 31} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 6} & , & x\leq {2}\\
{-x + 5} & , & {2}<x\leq{6}\\
{x^{2} + 3 \, x - 55} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 6} & , & x\leq {2}\\
{x + 5} & , & {2}<x\leq{7}\\
{x^{2} - 2 \, x - 23} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 12} & , & x\leq {3}\\
{{\left(x + 3\right)}^{2}} & , & {3}<x\leq{6}\\
{x^{2} - 4 \, x + 69} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 8} & , & x\leq {2}\\
{-x - 5} & , & {2}<x\leq{6}\\
{x^{2} - 2 \, x - 35} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 27} & , & x\leq {-1}\\
{{\left(x - 4\right)}^{2}} & , & {-1}<x\leq{3}\\
{{\left(x - 3\right)}^{2} + 1} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 2} & , & x\leq {-2}\\
{-x - 3} & , & {-2}<x\leq{2}\\
{x^{2} + 3 \, x - 15} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 1} & , & x\leq {1}\\
{{\left(x + 1\right)}^{2}} & , & {1}<x\leq{6}\\
{x + 43} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} + 6} & , & x\leq {-3}\\
{-x + 3} & , & {-3}<x\leq{1}\\
{{\left(x + 4\right)}^{2} - 23} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 43} & , & x\leq {-3}\\
{x - 5} & , & {-3}<x\leq{1}\\
{x^{2} + 3 \, x - 8} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} + 1} & , & x\leq {-3}\\
{{\left(x - 4\right)}^{2}} & , & {-3}<x\leq{1}\\
{x^{2} + 4 \, x + 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} + 6} & , & x\leq {1}\\
{x + 5} & , & {1}<x\leq{5}\\
{{\left(x - 2\right)}^{2} + 1} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2}} & , & x\leq {2}\\
{x + 1} & , & {2}<x\leq{6}\\
{x^{2} - 29} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x - 15} & , & x\leq {0}\\
{x^{2} - 16} & , & {0}<x\leq{3}\\
{x - 10} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 8} & , & x\leq {3}\\
{-x - 3} & , & {3}<x\leq{7}\\
{x^{2} + 5 \, x - 94} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 10} & , & x\leq {0}\\
{{\left(x - 3\right)}^{2}} & , & {0}<x\leq{5}\\
{{\left(x - 4\right)}^{2} + 3} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 11} & , & x\leq {2}\\
{x - 4} & , & {2}<x\leq{4}\\
{x^{2} - 3 \, x - 4} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 2} & , & x\leq {0}\\
{-x + 1} & , & {0}<x\leq{4}\\
{x - 7} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 10} & , & x\leq {-1}\\
{x^{2} - 6 \, x + 5} & , & {-1}<x\leq{4}\\
{{\left(x - 2\right)}^{2} - 7} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 5} & , & x\leq {2}\\
{x^{2} - 7 \, x + 10} & , & {2}<x\leq{6}\\
{x^{2} - x - 26} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 14} & , & x\leq {-2}\\
{x^{2} - 9} & , & {-2}<x\leq{2}\\
{x - 7} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 39} & , & x\leq {-2}\\
{{\left(x - 4\right)}^{2}} & , & {-2}<x\leq{1}\\
{x^{2} - 3 \, x + 11} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {3}\\
{x + 2} & , & {3}<x\leq{6}\\
{x^{2} - 2 \, x - 16} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 2} & , & x\leq {-2}\\
{x^{2} + 5 \, x + 4} & , & {-2}<x\leq{1}\\
{{\left(x + 3\right)}^{2} - 6} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 11} & , & x\leq {2}\\
{x^{2} + x - 2} & , & {2}<x\leq{4}\\
{x^{2} + 5 \, x - 18} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 3} & , & x\leq {-2}\\
{x} & , & {-2}<x\leq{3}\\
{{\left(x - 1\right)}^{2} - 1} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 16} & , & x\leq {2}\\
{{\left(x + 1\right)}^{2}} & , & {2}<x\leq{7}\\
{x + 57} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 1} & , & x\leq {2}\\
{{\left(x - 2\right)}^{2}} & , & {2}<x\leq{4}\\
{{\left(x - 3\right)}^{2} + 3} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 14} & , & x\leq {-3}\\
{-x} & , & {-3}<x\leq{1}\\
{x^{2} + 3 \, x - 5} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x + 11} & , & x\leq {3}\\
{{\left(x + 1\right)}^{2}} & , & {3}<x\leq{8}\\
{x^{2} - x + 25} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 2} & , & x\leq {-1}\\
{{\left(x + 1\right)}^{2}} & , & {-1}<x\leq{4}\\
{{\left(x + 2\right)}^{2} - 11} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 8} & , & x\leq {-2}\\
{{\left(x - 1\right)}^{2}} & , & {-2}<x\leq{2}\\
{x - 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 4} & , & x\leq {3}\\
{x^{2} - x} & , & {3}<x\leq{7}\\
{x^{2} + 2 \, x - 21} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 8} & , & x\leq {-2}\\
{{\left(x + 3\right)}^{2}} & , & {-2}<x\leq{2}\\
{x^{2} - 5 \, x + 31} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 1} & , & x\leq {1}\\
{{\left(x - 2\right)}^{2}} & , & {1}<x\leq{4}\\
{x^{2} - 12} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {-1}\\
{-x - 1} & , & {-1}<x\leq{4}\\
{x^{2} + 2 \, x - 29} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 3} & , & x\leq {1}\\
{-x - 3} & , & {1}<x\leq{6}\\
{x^{2} - x - 39} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 18} & , & x\leq {1}\\
{-x - 2} & , & {1}<x\leq{5}\\
{x^{2} - 3 \, x - 17} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 4} & , & x\leq {1}\\
{{\left(x + 1\right)}^{2}} & , & {1}<x\leq{3}\\
{x + 13} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x - 2} & , & x\leq {3}\\
{{\left(x - 3\right)}^{2}} & , & {3}<x\leq{7}\\
{x + 9} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 17} & , & x\leq {-3}\\
{-x + 3} & , & {-3}<x\leq{1}\\
{x^{2} + 5 \, x - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 10} & , & x\leq {3}\\
{-x - 4} & , & {3}<x\leq{5}\\
{x^{2} + 5 \, x - 59} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 16} & , & x\leq {2}\\
{x^{2} - 2 \, x - 8} & , & {2}<x\leq{7}\\
{x + 20} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 5} & , & x\leq {3}\\
{-x + 4} & , & {3}<x\leq{7}\\
{x^{2} + 4 \, x - 80} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 9} & , & x\leq {1}\\
{{\left(x + 2\right)}^{2}} & , & {1}<x\leq{5}\\
{x^{2} + 24} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} + 2} & , & x\leq {-2}\\
{-x} & , & {-2}<x\leq{2}\\
{{\left(x + 3\right)}^{2} - 27} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} + 1} & , & x\leq {3}\\
{{\left(x - 1\right)}^{2}} & , & {3}<x\leq{6}\\
{x^{2} - 5 \, x + 19} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 1} & , & x\leq {-1}\\
{-x} & , & {-1}<x\leq{2}\\
{x - 4} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 11} & , & x\leq {-1}\\
{{\left(x + 4\right)}^{2}} & , & {-1}<x\leq{1}\\
{x^{2} + 4 \, x + 20} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 27} & , & x\leq {-2}\\
{x^{2} - 7 \, x + 12} & , & {-2}<x\leq{2}\\
{x^{2} - 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 7} & , & x\leq {1}\\
{x^{2} - x - 2} & , & {1}<x\leq{3}\\
{x^{2} + 5 \, x - 20} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 3} & , & x\leq {2}\\
{x} & , & {2}<x\leq{7}\\
{x} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 8} & , & x\leq {-3}\\
{{\left(x + 5\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} + 15} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 1} & , & x\leq {2}\\
{{\left(x - 4\right)}^{2}} & , & {2}<x\leq{5}\\
{x^{2} - 24} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x + 3} & , & x\leq {0}\\
{-x + 2} & , & {0}<x\leq{2}\\
{x^{2} + x - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 2} & , & x\leq {3}\\
{-x + 3} & , & {3}<x\leq{7}\\
{x^{2} + 3 \, x - 74} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 6} & , & x\leq {0}\\
{-x + 5} & , & {0}<x\leq{5}\\
{{\left(x - 4\right)}^{2} - 1} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 7} & , & x\leq {3}\\
{-x - 2} & , & {3}<x\leq{5}\\
{{\left(x + 3\right)}^{2} - 71} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 16} & , & x\leq {-3}\\
{{\left(x + 4\right)}^{2}} & , & {-3}<x\leq{1}\\
{x^{2} - 3 \, x + 27} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} + 8} & , & x\leq {3}\\
{x + 5} & , & {3}<x\leq{7}\\
{{\left(x - 4\right)}^{2} + 3} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 1} & , & x\leq {3}\\
{{\left(x - 5\right)}^{2}} & , & {3}<x\leq{8}\\
{x^{2} - x - 47} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} + 28} & , & x\leq {-1}\\
{{\left(x - 5\right)}^{2}} & , & {-1}<x\leq{1}\\
{x^{2} - 2 \, x + 17} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 3} & , & x\leq {2}\\
{-x} & , & {2}<x\leq{6}\\
{x - 12} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 5} & , & x\leq {0}\\
{x + 4} & , & {0}<x\leq{2}\\
{x^{2} + 2 \, x - 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 3} & , & x\leq {1}\\
{x + 5} & , & {1}<x\leq{5}\\
{x + 5} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 15} & , & x\leq {-3}\\
{x - 4} & , & {-3}<x\leq{2}\\
{x^{2} - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 13} & , & x\leq {2}\\
{x^{2} + 3 \, x - 10} & , & {2}<x\leq{6}\\
{x^{2} - 5 \, x + 38} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 10} & , & x\leq {3}\\
{-x - 5} & , & {3}<x\leq{8}\\
{x^{2} - 5 \, x - 37} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 6} & , & x\leq {2}\\
{-x + 1} & , & {2}<x\leq{7}\\
{x^{2} + 5 \, x - 90} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 6} & , & x\leq {1}\\
{{\left(x + 2\right)}^{2}} & , & {1}<x\leq{5}\\
{x^{2} + 3 \, x + 9} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 9} & , & x\leq {0}\\
{x^{2} + 6 \, x + 8} & , & {0}<x\leq{4}\\
{x^{2} - 5 \, x + 52} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 5} & , & x\leq {-3}\\
{{\left(x + 2\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} + 4 \, x + 4} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} - 32} & , & x\leq {3}\\
{-x - 5} & , & {3}<x\leq{8}\\
{{\left(x + 2\right)}^{2} - 113} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x - 4} & , & x\leq {-1}\\
{-x - 3} & , & {-1}<x\leq{1}\\
{{\left(x + 2\right)}^{2} - 13} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 11} & , & x\leq {-1}\\
{x^{2} - 9} & , & {-1}<x\leq{2}\\
{{\left(x + 3\right)}^{2} - 30} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 4} & , & x\leq {-3}\\
{x^{2} - 9} & , & {-3}<x\leq{1}\\
{x - 9} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} - 3} & , & x\leq {-3}\\
{{\left(x + 3\right)}^{2}} & , & {-3}<x\leq{1}\\
{x^{2} + x + 14} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 3} & , & x\leq {-1}\\
{x^{2} - 2 \, x - 8} & , & {-1}<x\leq{4}\\
{x - 4} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 3} & , & x\leq {-1}\\
{x - 4} & , & {-1}<x\leq{2}\\
{{\left(x + 2\right)}^{2} - 18} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} - 17} & , & x\leq {3}\\
{-x + 1} & , & {3}<x\leq{6}\\
{x^{2} + x - 47} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 8} & , & x\leq {3}\\
{{\left(x + 2\right)}^{2}} & , & {3}<x\leq{7}\\
{{\left(x - 3\right)}^{2} + 65} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 23} & , & x\leq {3}\\
{{\left(x + 2\right)}^{2}} & , & {3}<x\leq{8}\\
{x^{2} - 3 \, x + 60} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 46} & , & x\leq {-2}\\
{{\left(x - 5\right)}^{2}} & , & {-2}<x\leq{3}\\
{x^{2} - 5} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 6} & , & x\leq {-3}\\
{-x + 2} & , & {-3}<x\leq{1}\\
{x^{2} - 3 \, x + 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 10} & , & x\leq {3}\\
{-x - 5} & , & {3}<x\leq{7}\\
{x^{2} + 2 \, x - 75} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 8} & , & x\leq {-2}\\
{-x + 3} & , & {-2}<x\leq{3}\\
{x - 3} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 3} & , & x\leq {0}\\
{x^{2} - 3 \, x + 2} & , & {0}<x\leq{5}\\
{x^{2} - x - 8} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} - 6} & , & x\leq {1}\\
{x - 4} & , & {1}<x\leq{3}\\
{{\left(x + 1\right)}^{2} - 17} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} + 8} & , & x\leq {-3}\\
{-x + 4} & , & {-3}<x\leq{-1}\\
{x + 6} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 18} & , & x\leq {1}\\
{{\left(x + 3\right)}^{2}} & , & {1}<x\leq{6}\\
{{\left(x - 2\right)}^{2} + 65} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x} & , & x\leq {3}\\
{x - 4} & , & {3}<x\leq{8}\\
{x - 4} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 5} & , & x\leq {-2}\\
{x} & , & {-2}<x\leq{2}\\
{{\left(x - 5\right)}^{2} - 7} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 9} & , & x\leq {1}\\
{{\left(x + 1\right)}^{2}} & , & {1}<x\leq{5}\\
{x + 31} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 3} & , & x\leq {-1}\\
{x^{2} - 4 \, x - 5} & , & {-1}<x\leq{3}\\
{x^{2} + 3 \, x - 26} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 10} & , & x\leq {1}\\
{x^{2} + 3 \, x + 2} & , & {1}<x\leq{6}\\
{x^{2} + 4 \, x - 4} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 6} & , & x\leq {-3}\\
{-x - 1} & , & {-3}<x\leq{2}\\
{x^{2} - 5 \, x + 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 8} & , & x\leq {-2}\\
{x^{2} - 6 \, x + 5} & , & {-2}<x\leq{2}\\
{x^{2} - 5 \, x + 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} + 5} & , & x\leq {3}\\
{x^{2} - 1} & , & {3}<x\leq{5}\\
{x^{2} + x - 6} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 4} & , & x\leq {1}\\
{-x - 4} & , & {1}<x\leq{6}\\
{x - 16} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 10} & , & x\leq {1}\\
{x - 3} & , & {1}<x\leq{3}\\
{x - 3} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 11} & , & x\leq {3}\\
{{\left(x - 5\right)}^{2}} & , & {3}<x\leq{5}\\
{x^{2} + 5 \, x - 50} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 11} & , & x\leq {-1}\\
{{\left(x + 4\right)}^{2}} & , & {-1}<x\leq{1}\\
{x + 24} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 33} & , & x\leq {2}\\
{-x + 4} & , & {2}<x\leq{6}\\
{{\left(x + 4\right)}^{2} - 102} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} - 4} & , & x\leq {0}\\
{x - 1} & , & {0}<x\leq{2}\\
{x^{2} - 2 \, x + 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 1} & , & x\leq {-1}\\
{-x} & , & {-1}<x\leq{2}\\
{x^{2} - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 8} & , & x\leq {-3}\\
{x} & , & {-3}<x\leq{-1}\\
{{\left(x - 1\right)}^{2} - 5} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-2}\\
{-x - 2} & , & {-2}<x\leq{2}\\
{x^{2} - x - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} + 3} & , & x\leq {3}\\
{x^{2} + x - 6} & , & {3}<x\leq{8}\\
{x + 58} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x - 8} & , & x\leq {3}\\
{x^{2} - 2 \, x - 15} & , & {3}<x\leq{5}\\
{x^{2} + 4 \, x - 45} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 6} & , & x\leq {-2}\\
{-x + 1} & , & {-2}<x\leq{2}\\
{x^{2} + 4 \, x - 13} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {-1}\\
{x^{2} - 1} & , & {-1}<x\leq{4}\\
{x^{2} - x + 3} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 11} & , & x\leq {0}\\
{x + 4} & , & {0}<x\leq{4}\\
{x + 4} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 2} & , & x\leq {-3}\\
{{\left(x + 1\right)}^{2}} & , & {-3}<x\leq{1}\\
{x^{2} + 2 \, x + 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-2}\\
{-x - 2} & , & {-2}<x\leq{2}\\
{x - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {1}\\
{x + 2} & , & {1}<x\leq{3}\\
{x^{2} + 3 \, x - 13} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 23} & , & x\leq {-3}\\
{x + 3} & , & {-3}<x\leq{1}\\
{x^{2} + 5 \, x - 2} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 10} & , & x\leq {0}\\
{{\left(x + 3\right)}^{2}} & , & {0}<x\leq{2}\\
{x + 23} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} + 21} & , & x\leq {1}\\
{{\left(x + 5\right)}^{2}} & , & {1}<x\leq{3}\\
{{\left(x - 5\right)}^{2} + 60} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 2} & , & x\leq {-2}\\
{x + 5} & , & {-2}<x\leq{1}\\
{x^{2} + x + 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5} & , & x\leq {0}\\
{x + 4} & , & {0}<x\leq{2}\\
{x^{2} + 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 36} & , & x\leq {1}\\
{{\left(x + 5\right)}^{2}} & , & {1}<x\leq{4}\\
{x^{2} + 3 \, x + 53} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 6} & , & x\leq {-2}\\
{x + 1} & , & {-2}<x\leq{3}\\
{{\left(x + 5\right)}^{2} - 60} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x} & , & x\leq {-2}\\
{x - 1} & , & {-2}<x\leq{1}\\
{x^{2} + 3 \, x - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 2} & , & x\leq {-2}\\
{-x + 1} & , & {-2}<x\leq{2}\\
{x - 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5} & , & x\leq {-1}\\
{x - 4} & , & {-1}<x\leq{1}\\
{x^{2} - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 3} & , & x\leq {2}\\
{-x} & , & {2}<x\leq{5}\\
{x^{2} - 2 \, x - 20} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x} & , & x\leq {1}\\
{{\left(x - 1\right)}^{2}} & , & {1}<x\leq{6}\\
{{\left(x - 1\right)}^{2}} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 11} & , & x\leq {-1}\\
{{\left(x - 2\right)}^{2}} & , & {-1}<x\leq{3}\\
{x - 2} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {-1}\\
{-x - 1} & , & {-1}<x\leq{1}\\
{x - 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-3}\\
{x + 2} & , & {-3}<x\leq{-1}\\
{x^{2} - x - 1} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 14} & , & x\leq {-2}\\
{-x + 5} & , & {-2}<x\leq{1}\\
{{\left(x + 5\right)}^{2} - 32} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 1} & , & x\leq {1}\\
{-x + 4} & , & {1}<x\leq{4}\\
{{\left(x + 4\right)}^{2} - 64} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 5} & , & x\leq {-1}\\
{x + 4} & , & {-1}<x\leq{3}\\
{x^{2} - 2 \, x + 4} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} - 7} & , & x\leq {-1}\\
{-x - 5} & , & {-1}<x\leq{1}\\
{x - 7} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 37} & , & x\leq {3}\\
{x^{2} - 5 \, x + 4} & , & {3}<x\leq{7}\\
{x^{2} - 3 \, x - 10} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 12} & , & x\leq {3}\\
{x^{2} - 6 \, x + 5} & , & {3}<x\leq{7}\\
{x^{2} - 37} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x - 22} & , & x\leq {-3}\\
{-x - 5} & , & {-3}<x\leq{1}\\
{x - 7} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 9} & , & x\leq {-1}\\
{x^{2} + 3 \, x - 4} & , & {-1}<x\leq{3}\\
{x + 11} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 11} & , & x\leq {-2}\\
{x} & , & {-2}<x\leq{2}\\
{x^{2} - 3 \, x + 4} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2}} & , & x\leq {-2}\\
{x^{2} + x - 2} & , & {-2}<x\leq{1}\\
{x^{2} + 3 \, x - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 7} & , & x\leq {2}\\
{x^{2}} & , & {2}<x\leq{4}\\
{x + 12} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 17} & , & x\leq {3}\\
{{\left(x + 2\right)}^{2}} & , & {3}<x\leq{7}\\
{x^{2} + 32} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 5} & , & x\leq {0}\\
{x^{2} + x - 6} & , & {0}<x\leq{3}\\
{x^{2} - 3 \, x + 6} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 2} & , & x\leq {1}\\
{x} & , & {1}<x\leq{5}\\
{{\left(x - 3\right)}^{2} + 1} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 13} & , & x\leq {1}\\
{{\left(x - 4\right)}^{2}} & , & {1}<x\leq{3}\\
{{\left(x + 4\right)}^{2} - 48} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 11} & , & x\leq {-3}\\
{-x + 4} & , & {-3}<x\leq{2}\\
{{\left(x - 4\right)}^{2} - 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 24} & , & x\leq {3}\\
{x^{2} + 6 \, x + 5} & , & {3}<x\leq{6}\\
{x^{2} + 41} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 5\right)}^{2} + 1} & , & x\leq {3}\\
{{\left(x + 5\right)}^{2}} & , & {3}<x\leq{8}\\
{{\left(x + 5\right)}^{2}} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 24} & , & x\leq {0}\\
{x^{2} - 25} & , & {0}<x\leq{3}\\
{x - 19} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 1} & , & x\leq {-3}\\
{x^{2}} & , & {-3}<x\leq{1}\\
{x^{2}} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {-1}\\
{x^{2} - 2 \, x - 3} & , & {-1}<x\leq{4}\\
{x^{2} + 5 \, x - 31} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 23} & , & x\leq {3}\\
{-x + 3} & , & {3}<x\leq{5}\\
{x^{2} + 5 \, x - 52} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 1} & , & x\leq {-2}\\
{x^{2} + 3 \, x} & , & {-2}<x\leq{1}\\
{x^{2} + 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 28} & , & x\leq {3}\\
{x^{2} + 2 \, x - 8} & , & {3}<x\leq{7}\\
{x^{2} + 3 \, x - 15} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 9} & , & x\leq {2}\\
{{\left(x - 4\right)}^{2}} & , & {2}<x\leq{7}\\
{x + 2} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 4} & , & x\leq {-2}\\
{x + 1} & , & {-2}<x\leq{1}\\
{{\left(x - 4\right)}^{2} - 7} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 2} & , & x\leq {-1}\\
{x^{2} + 7 \, x + 12} & , & {-1}<x\leq{2}\\
{x^{2} + 2 \, x + 22} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 8} & , & x\leq {-3}\\
{{\left(x - 1\right)}^{2}} & , & {-3}<x\leq{2}\\
{x^{2} - 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 26} & , & x\leq {0}\\
{{\left(x + 5\right)}^{2}} & , & {0}<x\leq{2}\\
{x^{2} - 4 \, x + 53} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 7} & , & x\leq {2}\\
{-x - 4} & , & {2}<x\leq{5}\\
{x^{2} - x - 29} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 9} & , & x\leq {-2}\\
{-x + 4} & , & {-2}<x\leq{3}\\
{x - 2} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 3} & , & x\leq {-3}\\
{x^{2} + 6 \, x + 5} & , & {-3}<x\leq{1}\\
{{\left(x + 3\right)}^{2} - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 36} & , & x\leq {-2}\\
{x^{2} - 8 \, x + 15} & , & {-2}<x\leq{2}\\
{x^{2} - 2 \, x + 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 43} & , & x\leq {-2}\\
{x^{2} - 8 \, x + 16} & , & {-2}<x\leq{3}\\
{{\left(x + 5\right)}^{2} - 63} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x} & , & x\leq {0}\\
{x - 1} & , & {0}<x\leq{4}\\
{x^{2} + x - 17} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} + 8} & , & x\leq {1}\\
{{\left(x - 5\right)}^{2}} & , & {1}<x\leq{3}\\
{x + 1} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 6} & , & x\leq {0}\\
{-x + 2} & , & {0}<x\leq{3}\\
{{\left(x + 3\right)}^{2} - 37} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 9} & , & x\leq {-3}\\
{-x - 1} & , & {-3}<x\leq{1}\\
{x - 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 20} & , & x\leq {2}\\
{x^{2} + 6 \, x + 5} & , & {2}<x\leq{6}\\
{x + 71} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 6} & , & x\leq {-3}\\
{-x - 1} & , & {-3}<x\leq{1}\\
{x^{2} - 4 \, x + 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x} & , & x\leq {1}\\
{x - 1} & , & {1}<x\leq{4}\\
{{\left(x - 1\right)}^{2} - 6} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 1} & , & x\leq {2}\\
{{\left(x - 4\right)}^{2}} & , & {2}<x\leq{6}\\
{x^{2} + x - 38} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 4} & , & x\leq {1}\\
{x + 3} & , & {1}<x\leq{4}\\
{x + 3} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 56} & , & x\leq {3}\\
{{\left(x + 5\right)}^{2}} & , & {3}<x\leq{5}\\
{x^{2} + 75} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 2} & , & x\leq {2}\\
{{\left(x - 3\right)}^{2}} & , & {2}<x\leq{5}\\
{x - 1} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 14} & , & x\leq {-3}\\
{x^{2} - x - 2} & , & {-3}<x\leq{-1}\\
{x + 1} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 27} & , & x\leq {-1}\\
{{\left(x - 4\right)}^{2}} & , & {-1}<x\leq{1}\\
{x^{2} + 3 \, x + 5} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4} & , & x\leq {1}\\
{-x - 3} & , & {1}<x\leq{4}\\
{x^{2} - 23} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-2}\\
{{\left(x + 2\right)}^{2}} & , & {-2}<x\leq{3}\\
{x + 22} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {0}\\
{x + 1} & , & {0}<x\leq{4}\\
{{\left(x - 5\right)}^{2} + 4} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-1}\\
{x^{2}} & , & {-1}<x\leq{1}\\
{{\left(x + 2\right)}^{2} - 8} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 5} & , & x\leq {-2}\\
{-x - 4} & , & {-2}<x\leq{3}\\
{{\left(x - 5\right)}^{2} - 11} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 2} & , & x\leq {-1}\\
{x - 2} & , & {-1}<x\leq{1}\\
{x^{2} + x - 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3} & , & x\leq {-1}\\
{x - 2} & , & {-1}<x\leq{4}\\
{x - 2} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 5} & , & x\leq {2}\\
{-x - 2} & , & {2}<x\leq{7}\\
{x^{2} + x - 65} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 50} & , & x\leq {2}\\
{{\left(x + 5\right)}^{2}} & , & {2}<x\leq{7}\\
{{\left(x - 2\right)}^{2} + 119} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} + 5} & , & x\leq {3}\\
{{\left(x - 5\right)}^{2}} & , & {3}<x\leq{5}\\
{x^{2} - 3 \, x - 10} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {3}\\
{x + 1} & , & {3}<x\leq{8}\\
{x^{2} - 55} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-1}\\
{{\left(x + 2\right)}^{2}} & , & {-1}<x\leq{3}\\
{x^{2} - 4 \, x + 28} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x - 15} & , & x\leq {-3}\\
{-x + 2} & , & {-3}<x\leq{-1}\\
{x^{2} - 4 \, x - 2} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 1} & , & x\leq {-2}\\
{x - 2} & , & {-2}<x\leq{2}\\
{{\left(x - 3\right)}^{2} - 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 5} & , & x\leq {0}\\
{{\left(x - 2\right)}^{2}} & , & {0}<x\leq{3}\\
{x - 2} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 9} & , & x\leq {-1}\\
{x} & , & {-1}<x\leq{4}\\
{x} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5} & , & x\leq {-1}\\
{x - 4} & , & {-1}<x\leq{3}\\
{x^{2} - 10} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x} & , & x\leq {0}\\
{-x - 1} & , & {0}<x\leq{5}\\
{x^{2} - x - 26} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-1}\\
{-x} & , & {-1}<x\leq{3}\\
{x^{2} + 3 \, x - 21} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} + 1} & , & x\leq {-3}\\
{-x - 2} & , & {-3}<x\leq{-1}\\
{{\left(x + 2\right)}^{2} - 2} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 31} & , & x\leq {0}\\
{x^{2} - 16} & , & {0}<x\leq{3}\\
{x^{2} - 4 \, x - 4} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 8} & , & x\leq {-2}\\
{x + 5} & , & {-2}<x\leq{1}\\
{x^{2} - 4 \, x + 9} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} - 6} & , & x\leq {-1}\\
{x - 2} & , & {-1}<x\leq{2}\\
{{\left(x - 1\right)}^{2} - 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} - 3} & , & x\leq {3}\\
{x^{2} - x - 6} & , & {3}<x\leq{7}\\
{x + 29} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 21} & , & x\leq {-2}\\
{{\left(x - 2\right)}^{2}} & , & {-2}<x\leq{1}\\
{x^{2} + 4 \, x - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 1} & , & x\leq {3}\\
{-x + 3} & , & {3}<x\leq{5}\\
{{\left(x + 3\right)}^{2} - 66} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 1} & , & x\leq {2}\\
{x^{2} - 6 \, x + 8} & , & {2}<x\leq{4}\\
{{\left(x + 2\right)}^{2} - 36} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2} & , & x\leq {2}\\
{{\left(x - 3\right)}^{2}} & , & {2}<x\leq{6}\\
{x + 3} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 10} & , & x\leq {0}\\
{{\left(x - 3\right)}^{2}} & , & {0}<x\leq{4}\\
{x^{2} + 3 \, x - 27} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 4} & , & x\leq {2}\\
{{\left(x - 5\right)}^{2}} & , & {2}<x\leq{6}\\
{x^{2} + x - 41} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 47} & , & x\leq {3}\\
{{\left(x + 4\right)}^{2}} & , & {3}<x\leq{7}\\
{x + 114} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 40} & , & x\leq {-3}\\
{{\left(x - 3\right)}^{2}} & , & {-3}<x\leq{2}\\
{x - 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x - 3} & , & x\leq {1}\\
{x^{2}} & , & {1}<x\leq{3}\\
{{\left(x - 4\right)}^{2} + 8} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 1} & , & x\leq {1}\\
{-x + 2} & , & {1}<x\leq{4}\\
{x^{2} - 2 \, x - 10} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 14} & , & x\leq {-3}\\
{-x} & , & {-3}<x\leq{1}\\
{{\left(x - 3\right)}^{2} - 5} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 9} & , & x\leq {0}\\
{-x - 1} & , & {0}<x\leq{2}\\
{x - 5} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 38} & , & x\leq {-3}\\
{{\left(x - 2\right)}^{2}} & , & {-3}<x\leq{2}\\
{x^{2} + 5 \, x - 14} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 65} & , & x\leq {3}\\
{x^{2} + 10 \, x + 25} & , & {3}<x\leq{8}\\
{x^{2} + 3 \, x + 81} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 6} & , & x\leq {3}\\
{-x} & , & {3}<x\leq{6}\\
{x - 12} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 7} & , & x\leq {-2}\\
{x^{2}} & , & {-2}<x\leq{1}\\
{x^{2} - 4 \, x + 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 22} & , & x\leq {-1}\\
{x^{2} - 25} & , & {-1}<x\leq{2}\\
{{\left(x + 3\right)}^{2} - 46} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 14} & , & x\leq {2}\\
{-x - 3} & , & {2}<x\leq{5}\\
{x^{2} + 3 \, x - 48} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 8} & , & x\leq {0}\\
{x^{2} + 4 \, x} & , & {0}<x\leq{2}\\
{{\left(x - 3\right)}^{2} + 11} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x + 4} & , & x\leq {1}\\
{-x + 2} & , & {1}<x\leq{6}\\
{x - 10} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 52} & , & x\leq {-2}\\
{{\left(x - 5\right)}^{2}} & , & {-2}<x\leq{3}\\
{x^{2} - 4 \, x + 7} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 1} & , & x\leq {1}\\
{x - 5} & , & {1}<x\leq{5}\\
{x^{2} + 5 \, x - 50} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 8} & , & x\leq {-1}\\
{{\left(x - 3\right)}^{2}} & , & {-1}<x\leq{2}\\
{x - 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 14} & , & x\leq {0}\\
{x^{2} + 2 \, x - 15} & , & {0}<x\leq{2}\\
{x^{2} + 4 \, x - 19} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 2} & , & x\leq {-3}\\
{x^{2} + 5 \, x} & , & {-3}<x\leq{1}\\
{x^{2} - 3 \, x + 8} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 1} & , & x\leq {2}\\
{x} & , & {2}<x\leq{6}\\
{x^{2} - 5 \, x} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4} & , & x\leq {1}\\
{-x - 3} & , & {1}<x\leq{3}\\
{x - 9} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 1} & , & x\leq {1}\\
{-x} & , & {1}<x\leq{3}\\
{x - 6} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 14} & , & x\leq {0}\\
{x^{2} - 2 \, x - 15} & , & {0}<x\leq{5}\\
{x - 5} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x} & , & x\leq {2}\\
{-x + 3} & , & {2}<x\leq{4}\\
{x^{2} - 2 \, x - 9} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {-1}\\
{x^{2} - 4 \, x - 5} & , & {-1}<x\leq{1}\\
{x^{2} - 3 \, x - 6} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 1} & , & x\leq {-2}\\
{{\left(x + 4\right)}^{2}} & , & {-2}<x\leq{3}\\
{x^{2} - x + 43} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 9} & , & x\leq {3}\\
{-x - 4} & , & {3}<x\leq{6}\\
{x^{2} + 3 \, x - 64} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} + 1} & , & x\leq {3}\\
{{\left(x - 2\right)}^{2}} & , & {3}<x\leq{8}\\
{{\left(x - 4\right)}^{2} + 20} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} - 3} & , & x\leq {2}\\
{x^{2} + 2 \, x - 3} & , & {2}<x\leq{6}\\
{x^{2} - x + 15} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 6} & , & x\leq {2}\\
{x + 5} & , & {2}<x\leq{7}\\
{x + 5} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 3} & , & x\leq {0}\\
{x^{2} - 4} & , & {0}<x\leq{5}\\
{x + 16} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} + 1} & , & x\leq {1}\\
{x + 3} & , & {1}<x\leq{4}\\
{x + 3} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 4} & , & x\leq {0}\\
{x^{2} + 4 \, x + 3} & , & {0}<x\leq{2}\\
{x + 13} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} + 22} & , & x\leq {2}\\
{x^{2} + 6 \, x + 5} & , & {2}<x\leq{7}\\
{{\left(x - 2\right)}^{2} + 71} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 2} & , & x\leq {2}\\
{-x + 5} & , & {2}<x\leq{5}\\
{x^{2} - 4 \, x - 5} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 4} & , & x\leq {1}\\
{-x + 3} & , & {1}<x\leq{3}\\
{{\left(x + 2\right)}^{2} - 25} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} + 3} & , & x\leq {2}\\
{x} & , & {2}<x\leq{6}\\
{x^{2} + 2 \, x - 42} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} - 1} & , & x\leq {-1}\\
{x^{2} + 3 \, x} & , & {-1}<x\leq{4}\\
{x + 24} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x - 14} & , & x\leq {0}\\
{x^{2} + 2 \, x - 15} & , & {0}<x\leq{5}\\
{x + 15} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 53} & , & x\leq {-2}\\
{x - 3} & , & {-2}<x\leq{3}\\
{x^{2} + 5 \, x - 24} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-2}\\
{{\left(x + 2\right)}^{2}} & , & {-2}<x\leq{2}\\
{x^{2} + 4 \, x + 4} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x} & , & x\leq {-1}\\
{x + 2} & , & {-1}<x\leq{4}\\
{x^{2} - x - 6} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x - 6} & , & x\leq {-2}\\
{{\left(x + 3\right)}^{2}} & , & {-2}<x\leq{2}\\
{x^{2} + 2 \, x + 17} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 15} & , & x\leq {-2}\\
{x^{2} - x - 6} & , & {-2}<x\leq{3}\\
{x^{2} + 2 \, x - 15} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 7} & , & x\leq {-2}\\
{-x + 2} & , & {-2}<x\leq{2}\\
{x - 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} + 17} & , & x\leq {1}\\
{{\left(x - 5\right)}^{2}} & , & {1}<x\leq{6}\\
{x^{2} + x - 41} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2}} & , & x\leq {0}\\
{-x + 3} & , & {0}<x\leq{2}\\
{{\left(x + 2\right)}^{2} - 15} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 7} & , & x\leq {3}\\
{{\left(x - 5\right)}^{2}} & , & {3}<x\leq{8}\\
{{\left(x + 1\right)}^{2} - 72} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 5\right)}^{2} - 21} & , & x\leq {0}\\
{x + 3} & , & {0}<x\leq{2}\\
{x + 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 4} & , & x\leq {-3}\\
{x^{2} + 4 \, x + 3} & , & {-3}<x\leq{2}\\
{x^{2} - x + 13} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} - 8} & , & x\leq {1}\\
{-x + 1} & , & {1}<x\leq{5}\\
{{\left(x + 2\right)}^{2} - 53} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 47} & , & x\leq {-2}\\
{x + 3} & , & {-2}<x\leq{3}\\
{x^{2} - 5 \, x + 12} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 14} & , & x\leq {3}\\
{{\left(x + 1\right)}^{2}} & , & {3}<x\leq{7}\\
{x + 57} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%



\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 9} & , & x\leq {-2}\\
{x^{2} + 7 \, x + 12} & , & {-2}<x\leq{2}\\
{x + 28} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 5} & , & x\leq {-1}\\
{x + 4} & , & {-1}<x\leq{3}\\
{x^{2} - 2 \, x + 4} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 8} & , & x\leq {2}\\
{-x - 3} & , & {2}<x\leq{5}\\
{x - 13} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2} & , & x\leq {-1}\\
{x - 1} & , & {-1}<x\leq{4}\\
{x^{2} - 13} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 5} & , & x\leq {0}\\
{-x + 4} & , & {0}<x\leq{5}\\
{x - 6} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 1} & , & x\leq {1}\\
{x} & , & {1}<x\leq{6}\\
{x} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 5} & , & x\leq {2}\\
{{\left(x - 4\right)}^{2}} & , & {2}<x\leq{5}\\
{x^{2} - 2 \, x - 14} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 9} & , & x\leq {0}\\
{x^{2} - 6 \, x + 8} & , & {0}<x\leq{5}\\
{x^{2} - 2 \, x - 12} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} + 5} & , & x\leq {-3}\\
{{\left(x + 1\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x + 1} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 5} & , & x\leq {-1}\\
{x + 1} & , & {-1}<x\leq{2}\\
{x^{2} - 5 \, x + 9} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} - 8} & , & x\leq {1}\\
{x^{2} - 4 \, x + 3} & , & {1}<x\leq{5}\\
{{\left(x + 2\right)}^{2} - 41} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {-1}\\
{-x} & , & {-1}<x\leq{1}\\
{{\left(x - 2\right)}^{2} - 2} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x + 3} & , & x\leq {-1}\\
{{\left(x + 3\right)}^{2}} & , & {-1}<x\leq{2}\\
{x + 23} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 68} & , & x\leq {-3}\\
{{\left(x - 5\right)}^{2}} & , & {-3}<x\leq{1}\\
{x + 15} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 13} & , & x\leq {-3}\\
{x^{2}} & , & {-3}<x\leq{2}\\
{x^{2} - x + 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 21} & , & x\leq {3}\\
{x^{2} + 3 \, x - 4} & , & {3}<x\leq{6}\\
{x^{2} + 5 \, x - 16} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 16} & , & x\leq {1}\\
{{\left(x + 3\right)}^{2}} & , & {1}<x\leq{6}\\
{x^{2} + 3 \, x + 27} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x + 23} & , & x\leq {3}\\
{{\left(x + 2\right)}^{2}} & , & {3}<x\leq{8}\\
{x + 92} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 35} & , & x\leq {2}\\
{{\left(x + 4\right)}^{2}} & , & {2}<x\leq{4}\\
{x^{2} + x + 44} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} + 27} & , & x\leq {-1}\\
{x^{2} - 9 \, x + 20} & , & {-1}<x\leq{2}\\
{x^{2} + 3 \, x - 4} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 1} & , & x\leq {-1}\\
{x + 1} & , & {-1}<x\leq{3}\\
{{\left(x + 1\right)}^{2} - 12} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 5} & , & x\leq {-2}\\
{x - 2} & , & {-2}<x\leq{2}\\
{x^{2} - x - 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 17} & , & x\leq {3}\\
{-x} & , & {3}<x\leq{6}\\
{x - 12} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} - 5} & , & x\leq {-2}\\
{-x + 1} & , & {-2}<x\leq{2}\\
{x^{2} - x - 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2}} & , & x\leq {-1}\\
{x^{2} - x - 2} & , & {-1}<x\leq{3}\\
{x^{2} - 5} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} - 23} & , & x\leq {3}\\
{-x - 5} & , & {3}<x\leq{7}\\
{{\left(x + 1\right)}^{2} - 76} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 5} & , & x\leq {0}\\
{x^{2} + x - 6} & , & {0}<x\leq{5}\\
{x^{2} - 2 \, x + 9} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 9} & , & x\leq {-3}\\
{-x + 5} & , & {-3}<x\leq{2}\\
{x^{2} - 3 \, x + 5} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} + 3} & , & x\leq {-2}\\
{x^{2} + x} & , & {-2}<x\leq{1}\\
{x^{2} + 2 \, x - 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 1} & , & x\leq {2}\\
{-x - 2} & , & {2}<x\leq{5}\\
{{\left(x - 3\right)}^{2} - 11} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 19} & , & x\leq {-1}\\
{{\left(x - 3\right)}^{2}} & , & {-1}<x\leq{3}\\
{x^{2} + 3 \, x - 18} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} - 15} & , & x\leq {2}\\
{{\left(x - 2\right)}^{2}} & , & {2}<x\leq{5}\\
{x + 4} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 8} & , & x\leq {-3}\\
{{\left(x + 1\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} + 2 \, x + 1} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 1} & , & x\leq {1}\\
{x^{2}} & , & {1}<x\leq{4}\\
{{\left(x - 3\right)}^{2} + 15} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 4} & , & x\leq {-3}\\
{x + 1} & , & {-3}<x\leq{-1}\\
{x^{2} + 2 \, x + 1} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3} & , & x\leq {-1}\\
{-x - 4} & , & {-1}<x\leq{1}\\
{x^{2} - 6} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 5} & , & x\leq {-2}\\
{-x + 4} & , & {-2}<x\leq{1}\\
{{\left(x + 1\right)}^{2} - 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 12} & , & x\leq {3}\\
{x^{2} + 6 \, x + 5} & , & {3}<x\leq{6}\\
{{\left(x - 4\right)}^{2} + 73} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} - 1} & , & x\leq {-3}\\
{-x - 4} & , & {-3}<x\leq{2}\\
{x - 8} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 4} & , & x\leq {2}\\
{-x - 1} & , & {2}<x\leq{4}\\
{{\left(x + 3\right)}^{2} - 54} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 5} & , & x\leq {2}\\
{x + 4} & , & {2}<x\leq{5}\\
{x + 4} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x + 3} & , & x\leq {-1}\\
{{\left(x + 3\right)}^{2}} & , & {-1}<x\leq{2}\\
{x^{2} + x + 19} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} + 3} & , & x\leq {1}\\
{x + 1} & , & {1}<x\leq{4}\\
{{\left(x - 1\right)}^{2} - 4} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 21} & , & x\leq {3}\\
{x^{2} - 3 \, x - 4} & , & {3}<x\leq{7}\\
{x^{2} - 3 \, x - 4} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 8} & , & x\leq {-3}\\
{{\left(x - 1\right)}^{2}} & , & {-3}<x\leq{1}\\
{x - 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 22} & , & x\leq {2}\\
{x^{2} - 25} & , & {2}<x\leq{6}\\
{x^{2} + 2 \, x - 37} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 4} & , & x\leq {0}\\
{-x - 5} & , & {0}<x\leq{2}\\
{{\left(x - 2\right)}^{2} - 7} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 26} & , & x\leq {3}\\
{x^{2}} & , & {3}<x\leq{8}\\
{x^{2} + 3 \, x - 24} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 5\right)}^{2} - 21} & , & x\leq {0}\\
{-x + 3} & , & {0}<x\leq{3}\\
{x^{2} + 5 \, x - 24} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 1} & , & x\leq {0}\\
{-x} & , & {0}<x\leq{4}\\
{x^{2} - 5 \, x} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 15} & , & x\leq {-2}\\
{x + 2} & , & {-2}<x\leq{3}\\
{x + 2} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x + 31} & , & x\leq {-2}\\
{{\left(x - 4\right)}^{2}} & , & {-2}<x\leq{2}\\
{x^{2} + x - 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 14} & , & x\leq {3}\\
{{\left(x + 1\right)}^{2}} & , & {3}<x\leq{8}\\
{x^{2} + 17} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 8} & , & x\leq {-3}\\
{{\left(x + 5\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x + 17} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 6} & , & x\leq {3}\\
{x + 5} & , & {3}<x\leq{6}\\
{x^{2} - 5 \, x + 5} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} + 25} & , & x\leq {1}\\
{{\left(x + 4\right)}^{2}} & , & {1}<x\leq{5}\\
{x + 76} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 4} & , & x\leq {2}\\
{{\left(x - 1\right)}^{2}} & , & {2}<x\leq{4}\\
{x^{2} - x - 3} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} + 3} & , & x\leq {-3}\\
{-x} & , & {-3}<x\leq{1}\\
{x - 2} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 24} & , & x\leq {3}\\
{-x + 2} & , & {3}<x\leq{8}\\
{{\left(x - 5\right)}^{2} - 15} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {1}\\
{x + 2} & , & {1}<x\leq{6}\\
{x^{2} - 28} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x + 4} & , & x\leq {2}\\
{x + 3} & , & {2}<x\leq{6}\\
{{\left(x + 1\right)}^{2} - 40} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 4} & , & x\leq {-2}\\
{x + 3} & , & {-2}<x\leq{2}\\
{{\left(x + 1\right)}^{2} - 4} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 8} & , & x\leq {2}\\
{x - 1} & , & {2}<x\leq{6}\\
{x^{2} + 5 \, x - 61} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 2} & , & x\leq {-1}\\
{x^{2}} & , & {-1}<x\leq{4}\\
{x^{2} + x - 4} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 7} & , & x\leq {1}\\
{-x + 3} & , & {1}<x\leq{4}\\
{{\left(x + 5\right)}^{2} - 82} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 11} & , & x\leq {3}\\
{x + 1} & , & {3}<x\leq{8}\\
{x + 1} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 36} & , & x\leq {-2}\\
{x^{2} - 16} & , & {-2}<x\leq{3}\\
{x^{2} + 3 \, x - 25} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 25} & , & x\leq {-1}\\
{{\left(x - 4\right)}^{2}} & , & {-1}<x\leq{2}\\
{x^{2}} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x} & , & x\leq {-1}\\
{-x + 2} & , & {-1}<x\leq{2}\\
{{\left(x + 3\right)}^{2} - 25} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4} & , & x\leq {1}\\
{x - 5} & , & {1}<x\leq{6}\\
{x^{2} - 35} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 3} & , & x\leq {-1}\\
{x - 4} & , & {-1}<x\leq{2}\\
{x^{2} - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 14} & , & x\leq {-2}\\
{{\left(x + 5\right)}^{2}} & , & {-2}<x\leq{3}\\
{x + 61} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 4} & , & x\leq {-3}\\
{x + 3} & , & {-3}<x\leq{2}\\
{x^{2} - 3 \, x + 7} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 26} & , & x\leq {0}\\
{x^{2} + 10 \, x + 25} & , & {0}<x\leq{3}\\
{x + 61} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 1} & , & x\leq {1}\\
{x^{2}} & , & {1}<x\leq{6}\\
{{\left(x + 4\right)}^{2} - 64} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x} & , & x\leq {-1}\\
{-x - 4} & , & {-1}<x\leq{2}\\
{{\left(x + 3\right)}^{2} - 31} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 56} & , & x\leq {3}\\
{x^{2} + x - 20} & , & {3}<x\leq{7}\\
{x + 29} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 14} & , & x\leq {3}\\
{{\left(x + 1\right)}^{2}} & , & {3}<x\leq{7}\\
{{\left(x - 1\right)}^{2} + 28} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 3} & , & x\leq {-2}\\
{x - 4} & , & {-2}<x\leq{2}\\
{x^{2} - x - 4} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 1} & , & x\leq {3}\\
{{\left(x - 4\right)}^{2}} & , & {3}<x\leq{6}\\
{x^{2} - x - 26} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 4} & , & x\leq {-1}\\
{x + 3} & , & {-1}<x\leq{3}\\
{x + 3} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 5} & , & x\leq {-3}\\
{{\left(x + 5\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} - 3 \, x + 12} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3} & , & x\leq {-2}\\
{x^{2} - 4} & , & {-2}<x\leq{1}\\
{x - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 4} & , & x\leq {-3}\\
{{\left(x + 4\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} - x + 7} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5} & , & x\leq {0}\\
{x^{2} + 5 \, x + 4} & , & {0}<x\leq{4}\\
{x + 36} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 11} & , & x\leq {1}\\
{{\left(x - 3\right)}^{2}} & , & {1}<x\leq{6}\\
{x + 3} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 3} & , & x\leq {0}\\
{x - 4} & , & {0}<x\leq{3}\\
{x - 4} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} - 3} & , & x\leq {2}\\
{x - 5} & , & {2}<x\leq{5}\\
{{\left(x - 1\right)}^{2} - 16} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 3} & , & x\leq {0}\\
{x - 4} & , & {0}<x\leq{5}\\
{{\left(x - 5\right)}^{2} + 1} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 14} & , & x\leq {2}\\
{x + 5} & , & {2}<x\leq{7}\\
{x^{2} - 5 \, x - 2} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 8} & , & x\leq {3}\\
{{\left(x - 3\right)}^{2}} & , & {3}<x\leq{6}\\
{x + 3} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 8} & , & x\leq {-1}\\
{-x + 5} & , & {-1}<x\leq{2}\\
{x^{2} - 2 \, x + 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 17} & , & x\leq {-2}\\
{x^{2} + x - 20} & , & {-2}<x\leq{1}\\
{x - 19} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} + 13} & , & x\leq {0}\\
{x^{2} - 8 \, x + 16} & , & {0}<x\leq{3}\\
{x - 2} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 17} & , & x\leq {0}\\
{x^{2} + 8 \, x + 16} & , & {0}<x\leq{4}\\
{x^{2} + 4 \, x + 32} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 2} & , & x\leq {1}\\
{x^{2}} & , & {1}<x\leq{3}\\
{x + 6} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 3} & , & x\leq {0}\\
{x + 2} & , & {0}<x\leq{3}\\
{x + 2} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 3} & , & x\leq {-2}\\
{-x + 2} & , & {-2}<x\leq{3}\\
{{\left(x - 1\right)}^{2} - 5} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 9} & , & x\leq {2}\\
{x^{2} + 3 \, x} & , & {2}<x\leq{4}\\
{x + 24} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 16} & , & x\leq {2}\\
{{\left(x + 3\right)}^{2}} & , & {2}<x\leq{6}\\
{x^{2} - 3 \, x + 63} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 1} & , & x\leq {-1}\\
{{\left(x + 1\right)}^{2}} & , & {-1}<x\leq{4}\\
{{\left(x - 1\right)}^{2} + 16} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 23} & , & x\leq {1}\\
{-x + 2} & , & {1}<x\leq{6}\\
{x^{2} - 4 \, x - 16} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 36} & , & x\leq {1}\\
{{\left(x + 5\right)}^{2}} & , & {1}<x\leq{5}\\
{x^{2} - 5 \, x + 100} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} - 23} & , & x\leq {2}\\
{-x + 3} & , & {2}<x\leq{4}\\
{x^{2} - 3 \, x - 5} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} + 1} & , & x\leq {2}\\
{x^{2} - 10 \, x + 25} & , & {2}<x\leq{6}\\
{{\left(x - 5\right)}^{2}} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 6} & , & x\leq {-3}\\
{-x - 4} & , & {-3}<x\leq{2}\\
{x^{2} + 5 \, x - 20} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 7} & , & x\leq {1}\\
{x} & , & {1}<x\leq{3}\\
{x^{2} + 4 \, x - 18} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x - 22} & , & x\leq {3}\\
{-x + 4} & , & {3}<x\leq{7}\\
{x - 10} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 2} & , & x\leq {3}\\
{-x + 3} & , & {3}<x\leq{5}\\
{x^{2} - x - 22} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 1} & , & x\leq {0}\\
{x} & , & {0}<x\leq{3}\\
{x^{2} + 5 \, x - 21} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 30} & , & x\leq {-3}\\
{x - 4} & , & {-3}<x\leq{1}\\
{x^{2} + 5 \, x - 9} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} + 10} & , & x\leq {0}\\
{{\left(x + 5\right)}^{2}} & , & {0}<x\leq{5}\\
{x^{2} + 4 \, x + 55} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x + 1} & , & x\leq {1}\\
{x - 5} & , & {1}<x\leq{6}\\
{x^{2} - 5 \, x - 5} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x - 5} & , & x\leq {-2}\\
{-x + 4} & , & {-2}<x\leq{3}\\
{x^{2} - 4 \, x + 4} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x} & , & x\leq {1}\\
{-x} & , & {1}<x\leq{3}\\
{x^{2} - x - 9} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 4} & , & x\leq {1}\\
{x^{2} + 2 \, x - 3} & , & {1}<x\leq{5}\\
{{\left(x + 4\right)}^{2} - 49} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 3\right)}^{2} + 1} & , & x\leq {-2}\\
{x + 3} & , & {-2}<x\leq{1}\\
{{\left(x + 3\right)}^{2} - 12} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 15} & , & x\leq {2}\\
{{\left(x + 2\right)}^{2}} & , & {2}<x\leq{7}\\
{{\left(x - 1\right)}^{2} + 45} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 1} & , & x\leq {2}\\
{-x + 4} & , & {2}<x\leq{4}\\
{x^{2} - 16} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 7} & , & x\leq {2}\\
{-x + 2} & , & {2}<x\leq{7}\\
{x^{2} - 2 \, x - 40} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 4} & , & x\leq {-1}\\
{x^{2} + x - 6} & , & {-1}<x\leq{1}\\
{{\left(x + 2\right)}^{2} - 13} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 5} & , & x\leq {-2}\\
{-x} & , & {-2}<x\leq{3}\\
{x - 6} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} + 17} & , & x\leq {2}\\
{{\left(x + 3\right)}^{2}} & , & {2}<x\leq{5}\\
{x^{2} - x + 44} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2}} & , & x\leq {1}\\
{x^{2} + x - 2} & , & {1}<x\leq{5}\\
{x + 23} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x - 13} & , & x\leq {-2}\\
{x^{2} + 5 \, x + 4} & , & {-2}<x\leq{3}\\
{{\left(x + 4\right)}^{2} - 21} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 2} & , & x\leq {1}\\
{x^{2}} & , & {1}<x\leq{3}\\
{x^{2} + 3 \, x - 9} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 30} & , & x\leq {-2}\\
{x^{2} + 3 \, x - 4} & , & {-2}<x\leq{3}\\
{{\left(x - 3\right)}^{2} + 14} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 4} & , & x\leq {2}\\
{{\left(x - 1\right)}^{2}} & , & {2}<x\leq{4}\\
{x + 5} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 12} & , & x\leq {-2}\\
{-x + 5} & , & {-2}<x\leq{3}\\
{x^{2} - 4 \, x + 5} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x + 7} & , & x\leq {-3}\\
{x^{2}} & , & {-3}<x\leq{-1}\\
{x + 2} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5} & , & x\leq {2}\\
{x - 4} & , & {2}<x\leq{4}\\
{x^{2} - 16} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5} & , & x\leq {3}\\
{x} & , & {3}<x\leq{5}\\
{x^{2} - 20} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 1} & , & x\leq {3}\\
{x + 4} & , & {3}<x\leq{6}\\
{x^{2} - 26} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 4} & , & x\leq {-1}\\
{x - 5} & , & {-1}<x\leq{4}\\
{x^{2} + 4 \, x - 33} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 6} & , & x\leq {-3}\\
{x + 5} & , & {-3}<x\leq{2}\\
{x^{2} + 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 4} & , & x\leq {0}\\
{-x - 5} & , & {0}<x\leq{3}\\
{{\left(x + 3\right)}^{2} - 44} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3} & , & x\leq {-2}\\
{-x - 2} & , & {-2}<x\leq{2}\\
{x - 6} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x + 35} & , & x\leq {-2}\\
{x^{2} - 8 \, x + 16} & , & {-2}<x\leq{1}\\
{x^{2} + x + 7} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 1\right)}^{2} - 3} & , & x\leq {3}\\
{{\left(x - 3\right)}^{2}} & , & {3}<x\leq{8}\\
{x^{2} + x - 47} & , & x>{8}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 5 \, x - 25} & , & x\leq {-3}\\
{x + 1} & , & {-3}<x\leq{1}\\
{x + 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 15} & , & x\leq {2}\\
{x^{2} + 6 \, x + 8} & , & {2}<x\leq{4}\\
{{\left(x - 3\right)}^{2} + 47} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 8} & , & x\leq {-3}\\
{x + 3} & , & {-3}<x\leq{2}\\
{x^{2} + 1} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 5\right)}^{2} - 48} & , & x\leq {-2}\\
{-x - 2} & , & {-2}<x\leq{1}\\
{x^{2} - 5 \, x + 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 6} & , & x\leq {3}\\
{-x + 5} & , & {3}<x\leq{6}\\
{x^{2} - 37} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + x - 4} & , & x\leq {2}\\
{-x + 3} & , & {2}<x\leq{4}\\
{x^{2} - x - 13} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 4} & , & x\leq {1}\\
{x^{2} + 7 \, x + 12} & , & {1}<x\leq{4}\\
{x^{2} + 4 \, x + 24} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3} & , & x\leq {1}\\
{x - 4} & , & {1}<x\leq{6}\\
{x - 4} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x - 18} & , & x\leq {3}\\
{x^{2} - 25} & , & {3}<x\leq{7}\\
{x + 17} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 4\right)}^{2} - 42} & , & x\leq {2}\\
{-x - 5} & , & {2}<x\leq{6}\\
{x^{2} - 4 \, x - 23} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 2\right)}^{2} - 14} & , & x\leq {-2}\\
{{\left(x + 1\right)}^{2}} & , & {-2}<x\leq{1}\\
{x + 3} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 6} & , & x\leq {-1}\\
{x + 2} & , & {-1}<x\leq{4}\\
{x^{2} + 5 \, x - 30} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 50} & , & x\leq {-3}\\
{x + 1} & , & {-3}<x\leq{1}\\
{x + 1} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 9} & , & x\leq {-1}\\
{-x + 3} & , & {-1}<x\leq{2}\\
{{\left(x + 5\right)}^{2} - 48} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 4 \, x + 5} & , & x\leq {-3}\\
{{\left(x + 4\right)}^{2}} & , & {-3}<x\leq{2}\\
{x^{2} - 4 \, x + 40} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x - 11} & , & x\leq {-3}\\
{-x} & , & {-3}<x\leq{2}\\
{x^{2} + 2 \, x - 10} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 4 \, x + 10} & , & x\leq {2}\\
{x + 3} & , & {2}<x\leq{6}\\
{x^{2} + 4 \, x - 51} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 3\right)}^{2} - 9} & , & x\leq {1}\\
{-x - 5} & , & {1}<x\leq{6}\\
{{\left(x - 3\right)}^{2} - 20} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x} & , & x\leq {2}\\
{{\left(x - 1\right)}^{2}} & , & {2}<x\leq{7}\\
{x^{2} - 4 \, x + 15} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} + 2} & , & x\leq {0}\\
{x^{2} - 3 \, x + 2} & , & {0}<x\leq{2}\\
{x^{2} - x - 2} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x - 13} & , & x\leq {-3}\\
{x + 4} & , & {-3}<x\leq{1}\\
{{\left(x + 2\right)}^{2} - 4} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - x - 1} & , & x\leq {3}\\
{{\left(x - 5\right)}^{2}} & , & {3}<x\leq{6}\\
{x^{2} - x - 29} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x + 5} & , & x\leq {-1}\\
{-x + 1} & , & {-1}<x\leq{4}\\
{{\left(x + 3\right)}^{2} - 52} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x + 4} & , & x\leq {-3}\\
{x^{2} + 4 \, x} & , & {-3}<x\leq{2}\\
{{\left(x - 5\right)}^{2} + 3} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 2 \, x - 11} & , & x\leq {0}\\
{x^{2} - x - 12} & , & {0}<x\leq{3}\\
{x - 9} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 3 \, x - 13} & , & x\leq {2}\\
{-x - 2} & , & {2}<x\leq{4}\\
{x - 10} & , & x>{4}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 2\right)}^{2} + 1} & , & x\leq {-3}\\
{-x - 2} & , & {-3}<x\leq{-1}\\
{x^{2} - 2 \, x - 4} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 2 \, x - 3} & , & x\leq {-2}\\
{x^{2}} & , & {-2}<x\leq{1}\\
{x^{2} + 2 \, x - 2} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 9} & , & x\leq {2}\\
{x^{2} - 3 \, x - 4} & , & {2}<x\leq{6}\\
{x^{2} - 22} & , & x>{6}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} - 3 \, x - 5} & , & x\leq {-1}\\
{-x - 3} & , & {-1}<x\leq{3}\\
{{\left(x - 3\right)}^{2} - 6} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x^{2} + 5 \, x} & , & x\leq {0}\\
{-x - 1} & , & {0}<x\leq{5}\\
{x^{2} - 5 \, x - 6} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{0}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{0}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x - 4\right)}^{2} - 39} & , & x\leq {-2}\\
{x - 2} & , & {-2}<x\leq{1}\\
{x^{2} - 4 \, x + 2} & , & x>{1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 1} & , & x\leq {1}\\
{-x + 2} & , & {1}<x\leq{5}\\
{x - 8} & , & x>{5}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{1}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{1}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 20} & , & x\leq {-3}\\
{{\left(x - 1\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} + 5 \, x + 8} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 1\right)}^{2} - 1} & , & x\leq {-2}\\
{-x - 3} & , & {-2}<x\leq{3}\\
{x - 9} & , & x>{3}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 5} & , & x\leq {-3}\\
{{\left(x + 4\right)}^{2}} & , & {-3}<x\leq{-1}\\
{x^{2} - 3 \, x + 5} & , & x>{-1}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-3}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-3}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{x + 24} & , & x\leq {2}\\
{{\left(x + 3\right)}^{2}} & , & {2}<x\leq{7}\\
{{\left(x + 4\right)}^{2} - 21} & , & x>{7}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%




\latexProblemContent{
\begin{problem}

Let $f(x)=\left\{\begin{array}{clc}
{{\left(x + 5\right)}^{2} - 15} & , & x\leq {-2}\\
{x^{2} - 2 \, x - 15} & , & {-2}<x\leq{2}\\
{x - 17} & , & x>{2}
\end{array}\right.$.  

Find the numbers at which $f$ is discontinuous: $x=\answer{-2}$\\ \qquad (If no such numbers exist, enter ``None'')

\input{2311_Concept_Limit_0004.HELP.tex}

At which of these points of discontinuity is $f$ continuous from the right?\\  $x=$\answer{None} \qquad (If no such numbers exist, enter ``None'')

At which of these points of discontinuity is $f$ continuous from the left?\\  $x=\answer{-2}$ \qquad (If no such numbers exist, enter ``None'')
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%

