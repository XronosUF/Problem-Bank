%%%%%%%%%%%%%%%%%%%%%%%
%%\tagged{Cat@One, Cat@Two, Cat@Three, Cat@Four, Cat@Five, Ans@MC, Type@Concept, Topic@Limit, Sub@Continuity, Sub@Theorems_IVT, Sub@Poly}{

\latexProblemContent{
\begin{problem}

Does ${x^{3} - 7 \, x^{2} + 10 \, x}$ have a root in the interval $({1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 12 \, x^{2} + 39 \, x - 28}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 7 \, x^{3} + 10 \, x^{2}}$ have a root in the interval $({1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 4 \, x^{3} - 6 \, x^{2} - 36 \, x - 27}$ have a root in the interval $({-2},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 2 \, x^{2} - 11 \, x + 12}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 9 \, x + 18}$ have a root in the interval $({2},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - x - 2}$ have a root in the interval $({-3},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - x - 2}$ have a root in the interval $({-2},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 7 \, x + 10}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 13 \, x^{2} + 50 \, x - 56}$ have a root in the interval $({3},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 10 \, x + 24}$ have a root in the interval $({2},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 8 \, x^{3} + 15 \, x^{2}}$ have a root in the interval $({1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 8 \, x + 12}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 6 \, x + 5}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 6 \, x + 5}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 6 \, x + 8}$ have a root in the interval $({-1},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 5 \, x^{3} + x^{2} - 21 \, x - 18}$ have a root in the interval $({-2},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 6 \, x + 8}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 11 \, x^{2} + 31 \, x - 21}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 10 \, x^{3} + 21 \, x^{2}}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 15 \, x + 54}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 9 \, x + 18}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 9 \, x^{2} + 14 \, x + 24}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - x^{2} - 9 \, x + 9}$ have a root in the interval $({-2},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 4}$ have a root in the interval $({-3},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 6 \, x^{3} - 3 \, x^{2} - 56 \, x - 48}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 13 \, x - 12}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 12 \, x^{3} + 35 \, x^{2}}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 10 \, x + 24}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 4 \, x^{3} - 3 \, x^{2} + 10 \, x + 8}$ have a root in the interval $({0},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 5 \, x^{2} + 2 \, x + 8}$ have a root in the interval $({0},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} + 4 \, x^{2} - 4 \, x - 16}$ have a root in the interval $({-3},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 8 \, x + 12}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 19 \, x^{3} + 118 \, x^{2} - 276 \, x + 216}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 12 \, x + 35}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 4 \, x^{2} - 7 \, x + 10}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 8 \, x + 15}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 10 \, x + 21}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 2 \, x^{3} - 15 \, x^{2} - 4 \, x + 20}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 6 \, x^{2} + 3 \, x + 10}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 10 \, x + 24}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 16 \, x^{3} + 84 \, x^{2} - 176 \, x + 128}$ have a root in the interval $({3},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 18 \, x^{2} + 95 \, x - 126}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 11 \, x + 24}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 9 \, x^{2} - 4 \, x + 12}$ have a root in the interval $({-1},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 12 \, x + 35}$ have a root in the interval $({3},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 6 \, x^{2} - x + 6}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 7 \, x^{3} + x^{2} + 27 \, x + 18}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 4 \, x^{3} - 13 \, x^{2} - 40 \, x + 48}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 7 \, x^{3} + 6 \, x^{2} - 32 \, x - 32}$ have a root in the interval $({-3},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 13 \, x^{2} + 44 \, x - 32}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} + x^{2} - 9 \, x - 9}$ have a root in the interval $({-2},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 13 \, x^{2} + 47 \, x - 35}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 6 \, x^{3} + 22 \, x + 15}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 4 \, x^{2} - x + 4}$ have a root in the interval $({0},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 11 \, x^{2} + 28 \, x}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 8 \, x^{3} + 12 \, x^{2} - 32 \, x - 64}$ have a root in the interval $({-3},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 5 \, x^{2} - x + 5}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 2 \, x^{2} - 5 \, x + 6}$ have a root in the interval $({-1},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 4 \, x + 3}$ have a root in the interval $({-1},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 9 \, x^{2} + 18 \, x}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} + 3 \, x^{2} - 10 \, x - 24}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - x - 6}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 4 \, x^{3} - 13 \, x^{2} + 28 \, x + 60}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 12 \, x + 35}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 11 \, x + 28}$ have a root in the interval $({3},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 7 \, x + 10}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 15 \, x^{3} + 67 \, x^{2} - 93 \, x + 40}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 5 \, x + 4}$ have a root in the interval $({-1},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 3 \, x^{3} - 14 \, x^{2} + 12 \, x + 40}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 7 \, x + 10}$ have a root in the interval $({1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 14 \, x + 45}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 11 \, x + 28}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 5 \, x + 4}$ have a root in the interval $({0},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 4 \, x + 3}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 14 \, x^{2} + 56 \, x - 64}$ have a root in the interval $({3},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 16 \, x^{3} + 87 \, x^{2} - 188 \, x + 140}$ have a root in the interval $({3},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 13 \, x + 12}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 12 \, x + 32}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 14 \, x + 48}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 5 \, x + 4}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 13 \, x + 36}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 12 \, x + 32}$ have a root in the interval $({3},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 12 \, x^{3} + 42 \, x^{2} - 52 \, x + 21}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 9 \, x + 18}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 7 \, x^{2} + 4 \, x + 12}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + x^{3} - 17 \, x^{2} - 21 \, x + 36}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 8 \, x + 15}$ have a root in the interval $({1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 5 \, x^{2} - 4 \, x + 20}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 13 \, x + 40}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 2 \, x - 3}$ have a root in the interval $({-2},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 4 \, x^{2} - 4 \, x + 16}$ have a root in the interval $({-1},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 15 \, x^{3} + 76 \, x^{2} - 156 \, x + 112}$ have a root in the interval $({3},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 10 \, x^{2} + 24 \, x}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 8 \, x^{3} + 12 \, x^{2}}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 3 \, x^{3} - 5 \, x^{2} + 3 \, x + 4}$ have a root in the interval $({0},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 10 \, x + 21}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 11 \, x^{3} + 37 \, x^{2} - 45 \, x + 18}$ have a root in the interval $({2},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 7 \, x^{2} + 7 \, x + 15}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 8 \, x^{2} + 15 \, x}$ have a root in the interval $({1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 13 \, x^{3} + 51 \, x^{2} - 67 \, x + 28}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 8 \, x^{3} + 5 \, x^{2} + 38 \, x + 24}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 18 \, x^{3} + 108 \, x^{2} - 248 \, x + 192}$ have a root in the interval $({3},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 14 \, x^{2} + 53 \, x - 40}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 3 \, x^{3} - 13 \, x^{2} - 51 \, x - 36}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 10 \, x^{2} + 27 \, x - 18}$ have a root in the interval $({2},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 9 \, x^{3} + 18 \, x^{2}}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - x^{3} - 12 \, x^{2} - 4 \, x + 16}$ have a root in the interval $({-1},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 16 \, x + 63}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 11 \, x^{2} + 34 \, x - 24}$ have a root in the interval $({2},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 4 \, x + 3}$ have a root in the interval $({-2},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 3 \, x - 4}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 11 \, x^{3} + 28 \, x^{2}}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} + 2 \, x^{2} - 5 \, x - 6}$ have a root in the interval $({-2},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} + 2 \, x^{2} - 11 \, x - 12}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 19 \, x^{2} - 6 \, x + 72}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 4 \, x^{3} - 6 \, x^{2} + 4 \, x + 5}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 16 \, x^{2} + 76 \, x - 96}$ have a root in the interval $({3},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 5 \, x^{3} - 3 \, x^{2} + 13 \, x + 10}$ have a root in the interval $({0},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 12 \, x^{2} + 35 \, x - 24}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 12 \, x^{3} + 45 \, x^{2} - 58 \, x + 24}$ have a root in the interval $({2},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 5 \, x^{3} - 7 \, x^{2} + 5 \, x + 6}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 14 \, x^{3} + 60 \, x^{2} - 82 \, x + 35}$ have a root in the interval $({2},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 10 \, x^{3} + 24 \, x^{2}}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 7 \, x^{3} + 2 \, x^{2} - 64 \, x - 96}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 16 \, x^{3} + 77 \, x^{2} - 110 \, x + 48}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} + 3 \, x^{2} - 6 \, x - 8}$ have a root in the interval $({-3},{1})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({1})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({1})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({1})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({1})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 8 \, x^{2} + 9 \, x + 18}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 2 \, x^{3} - 12 \, x^{2} + 8 \, x + 32}$ have a root in the interval $({-1},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 17 \, x^{3} + 92 \, x^{2} - 196 \, x + 144}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 17 \, x^{2} + 84 \, x - 108}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 9 \, x^{3} + 14 \, x^{2}}$ have a root in the interval $({1},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 6 \, x^{3} - 3 \, x^{2} + 16 \, x + 12}$ have a root in the interval $({0},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\begin{problem}

Does ${x^{3} - 14 \, x^{2} + 59 \, x - 70}$ have a root in the interval $({3},{6})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({6})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({6})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({6})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({6})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 2 \, x - 3}$ have a root in the interval $({-3},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-3})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-3})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-3})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-3})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} - 20 \, x^{3} + 131 \, x^{2} - 316 \, x + 252}$ have a root in the interval $({3},{8})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({8})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({8})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({8})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({8})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 6 \, x + 8}$ have a root in the interval $({0},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({0})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({0})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({0})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({0})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 3 \, x^{2} - 10 \, x + 24}$ have a root in the interval $({-2},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 13 \, x + 40}$ have a root in the interval $({3},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({3})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({3})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({3})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({3})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 8 \, x^{2} + 12 \, x}$ have a root in the interval $({1},{5})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({1})<0$ and $f({5})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({1})>0$ and $f({5})<0$, so $f$ has a root by IVT.}
\choice{$f({1})<0$ and $f({5})<0$, so $f$ does not have a root by IVT.}
\choice{$f({1})>0$ and $f({5})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 15 \, x^{2} + 62 \, x - 48}$ have a root in the interval $({2},{7})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({2})<0$ and $f({7})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({2})>0$ and $f({7})<0$, so $f$ has a root by IVT.}
\choice{$f({2})<0$ and $f({7})<0$, so $f$ does not have a root by IVT.}
\choice{$f({2})>0$ and $f({7})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 3 \, x^{2} - 6 \, x + 8}$ have a root in the interval $({-1},{3})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({3})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({3})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({3})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({3})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{3} - 6 \, x^{2} - x + 30}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{2} - 8 \, x + 15}$ have a root in the interval $({-1},{4})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-1})<0$ and $f({4})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-1})>0$ and $f({4})<0$, so $f$ has a root by IVT.}
\choice{$f({-1})<0$ and $f({4})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-1})>0$ and $f({4})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


\latexProblemContent{
\begin{problem}

Does ${x^{4} + 2 \, x^{3} - 12 \, x^{2} - 18 \, x + 27}$ have a root in the interval $({-2},{2})$?  

\input{2311_Concept_Limit_0006.HELP.tex}

\begin{multipleChoice}
\choice[correct]{Yes}
\choice{No}
\choice{Inconclusive}
\end{multipleChoice}

What is the reason for this answer?\begin{multipleChoice}
\choice{$f({-2})<0$ and $f({2})>0$, so $f$ has a root by IVT.}
\choice[correct]{$f({-2})>0$ and $f({2})<0$, so $f$ has a root by IVT.}
\choice{$f({-2})<0$ and $f({2})<0$, so $f$ does not have a root by IVT.}
\choice{$f({-2})>0$ and $f({2})>0$, so $f$ does not have a root by IVT.}
\choice{The answer could not be determined.}
\end{multipleChoice}
\end{problem}}%}

%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%


%%%%%%%%%%%%%%%%%%%%%%
