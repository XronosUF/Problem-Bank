% Ans        : ShortAns
% File       : 0018
% Sub        : Implicit, Trig, Poly
% Topic      : Derivative
% Type       : Compute

\ProblemFileHeader{429}
\ifquestionPull
\ifproblemToFind\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y^{2}\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x^{2} \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y^{2}\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \cos\left(y\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \cos\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \cos\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x^{2} \sin\left(y\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x^{2} \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \cos\left(y^{2}\right)=4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x \sin\left(y^{2}\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x^{2} \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x^{2} \sin\left(y^{2}\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $x \sin\left(y\right)=1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $9 \, x \sin\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x^{2} \sin\left(y^{2}\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x^{2} \sin\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{2 \, x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $2 \, x \sin\left(y\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $5 \, x \cos\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \cos\left(y\right)=-1$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x \sin\left(y^{2}\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{2 \, x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \cos\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{2 \, \cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \sin\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $8 \, x \sin\left(y\right)=3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \cos\left(y\right)=-5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $6 \, x^{2} \sin\left(y^{2}\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y^{2}\right)}{x y \cos\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $3 \, x \cos\left(y\right)=-3$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y\right)}{x \sin\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $4 \, x^{2} \cos\left(y^{2}\right)=5$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{\frac{\cos\left(y^{2}\right)}{x y \sin\left(y^{2}\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $10 \, x^{2} \sin\left(y\right)=-2$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{2 \, \sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}

%%%%%%%%%%%%%%%%%%%%%%

\latexProblemContent{
\ifVerboseLocation This is Derivative Compute Question 0018. \\ \fi
\begin{problem}

Implicitly derive the expression $7 \, x \sin\left(y\right)=-4$ and solve for $\dfrac{dy}{dx}$.

\input{Derivative-Compute-0018.HELP.tex}

\[\dfrac{dy}{dx}=\answer{-\frac{\sin\left(y\right)}{x \cos\left(y\right)}}\] 
\end{problem}}\fi             %% end of \ifproblemToFind near top of file
\fi             %% end of \ifquestionCount near top of file
\ProblemFileFooter